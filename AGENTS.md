# AGENTS.md

## 项目定位

- 这里是 `juhe-ai`，定位为轻量级 OpenAI 兼容中转与账号管理项目。
- 前端使用 Vue 3 + TypeScript + Ant Design Vue。
- 后端使用 Node.js + TypeScript。
- 产品完整架构包含供应商、账户管理、API 密钥、代理管理、使用记录、系统设置、分组。
- 第一阶段只做 OpenAI 供应商，并支持 OpenAI OAuth 与 OpenAI API Key 两种账户创建方式。
- 分组绑定账户，API Key 绑定分组；中转调度时通过 API Key 找到分组，再从分组内账户选择可用账号。

## 工作原则

- 先做轻量闭环，再扩展能力。
- 需求变化如果影响模块边界，先更新 `docs/architecture.md` 和 `docs/phase-1-plan.md`。
- 账户相关语义保持轻量、清晰、可维护，避免提前引入重型实现。
- 不要擅自把复杂的网关逻辑提前塞进第一阶段；第一阶段以管理面、数据结构和 OpenAI 账号接入为主。
- UI、API、数据结构尽量先对齐，再开始大规模实现。
- 前端面向中文用户，页面文案、提示、空态、悬浮提示、组件内置文案都必须保持中文；Ant Design Vue 必须通过全局 `a-config-provider` 使用中文 locale，不要让默认英文文案暴露到页面。

## 通用规范入口

- 涉及前端页面、布局、样式、表格、表单或交互时，先参考 `docs/前端样式规范指导.md`。
- 涉及登录页、首页、应用壳、品牌展示或前端信息架构时，先参考 `docs/前端设计.md`。
- 涉及新功能、字段、接口、存储、脚本或文档同步时，先参考 `docs/功能开发指导.md`。
- 涉及发布包、服务器部署、启动脚本或发布注意事项时，构建先参考 `docs/deploy/build.md`，部署先参考 `docs/deploy/deploy.md`，并同时确认构建平台与目标部署平台。
- 涉及大文件、职责拆分、模块重构或重复逻辑收敛时，先参考 `docs/大文件重构指南.md`。
- 涉及 bug、异常、测试失败、数据不一致或运行问题时，先参考 `docs/问题修复指导.md`；如果问题有复用价值、容易复发或需要历史关联，再记录到 `docs/bug/README.md`。
- 以上规范只写通用原则；具体业务范围仍以 `docs/architecture.md`、`docs/phase-1-plan.md` 和 `docs/dev-runbook.md` 为准。

## 目录约定

- `frontend/`：前端页面、组件、状态和 API 请求层。
- `backend/`：Node API、业务逻辑、数据访问、中间件和后续网关能力。
- `docs/`：阶段计划、参考说明、决策记录。
- `docs/bug/`：bug 编号、修复记录、复发记录和历史关联。

## 模块边界

- `providers`：供应商定义，描述不同供应商支持哪些账户创建方式。
- `accounts`：上游账号管理，保存账号状态、凭据引用、并发、代理、透传和错误策略。
- `groups`：账号分组，承接账号集合，并作为 API Key 的授权边界。
- `api-keys`：对外访问密钥，绑定分组并控制访问状态。
- `proxies`：代理配置，供账号绑定使用。
- `usage-records`：请求、用量、耗时、错误和账号命中记录。
- `settings`：系统级配置、默认策略和安全配置。

## 变更习惯

- 改代码前先看计划文档是否需要补充。
- 如果新增字段或流程，先写清楚默认值和用途。
- 后续如果涉及真实转发、代理或并发控制，再补对应验证步骤。

