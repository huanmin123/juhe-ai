import type { CreatedMockdata, MockdataOptions } from '../shared.js'

/**
 * F2 storage snapshots are owned and generated by Go. Node mockdata must not
 * read the stats database or write any F2 snapshot rows.
 */
export function createStorageMockdata(_created: CreatedMockdata, _options: MockdataOptions): void {
  console.log('F2 存储快照由 Go owner 生成；Node mockdata 跳过该数据段')
}
