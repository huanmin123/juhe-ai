# 高性能模式部署指南

> `performance = PostgreSQL + PgBouncer + Redis cache + Redis state + Redis queue`。
> 默认轻量部署看 [Docker 部署指南](Docker部署指南.md)。

## 1. 适用边界

适合 SQLite + 进程内缓存已经不够的部署。家庭宽带入口默认不选高性能模式。

当前边界：

- 核心 PostgreSQL repository、DB service、Redis cache/state/stream、网关事实写入、System API 高频管理链路和主要统计窗口已通过远端 PG/Redis smoke、readiness 和压测验证。
- 未覆盖或未启用的低频入口、复杂筛选和长周期运维任务在 PostgreSQL 模式下必须 fail-fast，不允许静默回退 SQLite。
- 关键共享缓存已接 Redis `SharedJsonCache` 并有门禁；剩余本地 LRU 是已登记的本进程快照或热路径保留项，不代表全量 Redis 化。
- 高性能模式上线前必须在目标机器跑一次本机验证和业务回归。
- Go F1、F2、F3 由唯一 `juhe-ai-go-sidecar` 承载，三个功能保留独立 Store、schema 与 owner lease，但不再有独立 launchd service、PID 或发布二进制。F3 只接受显式 loopback HMAC 输入配置与 PostgreSQL；不能回退通用密钥、SQLite、Redis、Node worker 或旧审计队列。开发 PostgreSQL 闭环和脚本 dry-run 都不能替代 Mac `--apply`、listener、handover、rollback 或完整 temporary release 预演。

压测和容量结论只放 [reports 目录](../reports/README.md)，不写进部署 runbook。

## 2. 组件

| 组件 | 默认镜像 | 边界 |
| --- | --- | --- |
| PostgreSQL | `postgres:18-bookworm` | 事实数据、统计结果、日志索引 |
| PgBouncer | `edoburu/pgbouncer:v1.25.2-p0` | 应用只连 PgBouncer |
| Redis cache | `redis:8.2.7-bookworm` | 可丢弃缓存，`allkeys-lru` |
| Redis state | `redis:8.2.7-bookworm` | 短 TTL 运行态、原子计数，`noeviction` |
| Redis queue | `redis:8.2.7-bookworm` | Redis Streams 未落库消息队列，`noeviction`，必须监控 pending + lag |
| juhe-ai | `node:22-bookworm-slim` | 同机 control、gateway、worker 与各自 DB service 子进程 |

不要使用 `latest` 镜像。Redis 8 授权如果不接受，需要单独评估替代方案。

## 3. 部署文件

```text
docker/
  compose.performance.yml
  .env.performance.example
  postgres/init/001-extensions.sql
```

默认生产入口是宿主机 Caddy HTTPS：

```text
公网 80/443 -> Caddy -> 127.0.0.1:3000 juhe-ai
```

高并发网关和管理后台共用同一域名时，macOS 裸机部署使用同机多进程拓扑：

```text
公网 80/443 -> Caddy -> 127.0.0.1:3000 Nginx
  /__aisys__/*、/__aipublic__/* -> control:3200
  其他 AI 路径                  -> least_conn(gateway:3101..3103)

control: 1 server + Usage Worker 2 + Log Worker 2 + Stats Worker 1 + Ops Worker 1
gateway: 3 个独立 Node 事件循环，各自只处理客户端请求
go-sidecar: 1 个 Go 常驻进程，承载 F1 运行日志索引、F2 表监控、F3 审计与 F4 操作日志持久化；各数据 owner 在进程内隔离（F4 待独立发布切流）
```

所有 Node 端口只绑定 `127.0.0.1`。gateway 与 control 各自看护 DB service 子进程并使用动态本机端口，DB service 不对 Nginx 或公网暴露。Redis cache/state/queue 与 PostgreSQL 是同一套共享事实源；Usage 和公开接口日志通过 Redis Stream consumer group 分工，禁止每个完整 server 重复启动一套 consumer。审计和操作日志分别通过 Node 到 Go sidecar 的一次签名 RPC 进入 F3/F4，不注册 Redis consumer。

F1 不使用 Redis、Asynq 或 Node worker；sidecar 直接读取 Node 写入的 JSONL，向 PostgreSQL `juhe_dataset` 写入索引并执行保留清理。Node control/gateway 只负责生成日志文件和读取 Go 产物，不设置 Node/Go owner 开关。macOS 性能拓扑只安装一个 sidecar；组件运行错误在进程内记录并单独退避恢复，不因 Node 服务重启而创建第二个 writer。

拓扑权威边界见 [高性能模式同机多进程拓扑设计](../functions/高性能模式同机多进程拓扑设计.md)。Go 迁移完成后，正式部署应按 [Go 迁移开发构建部署调整](../migration/开发构建部署调整.md) 改为 Go server / worker、PostgreSQL、Redis / Asynq、health、Prometheus metrics、pprof 和 worker lag 口径；不得继续把 `db-service`、Node event loop 或 SQLite 文件体积作为 Go 服务健康和容量判断依据。

Compose 默认端口边界：

- 应用：`JUHE_AI_PUBLIC_BIND=127.0.0.1`
- 中间件：`JUHE_AI_MIDDLEWARE_BIND=127.0.0.1`
- 公网只开放 Caddy `80/443`

## 4. 配置

```bash
cd docker
cp .env.performance.example .env.performance
nano .env.performance
```

必须填写：

```dotenv
JUHE_AI_PUBLIC_BIND=127.0.0.1
JUHE_AI_PUBLIC_PORT=3000
JUHE_AI_PUBLIC_ORIGIN=https://ai.example.com
JUHE_AI_SECRET=至少32位稳定随机密钥
JUHE_AI_COOKIE_SECURE=true
JUHE_AI_TRUST_PROXY=true
JUHE_AI_DB_SERVICE_HTTP_HOST=127.0.0.1
JUHE_AI_DB_SERVICE_HTTP_PORT=0
JUHE_AI_ACCOUNT_HEALTH_CHECK_DISPATCH_URL=http://127.0.0.1:3200

JUHE_AI_MIDDLEWARE_BIND=127.0.0.1
JUHE_AI_POSTGRES_PASSWORD=PostgreSQL强密码
JUHE_AI_REDIS_CACHE_PASSWORD=Redis缓存强密码
JUHE_AI_REDIS_STATE_PASSWORD=Redis运行态强密码

JUHE_AI_RUNTIME_MODE=performance
JUHE_AI_DATABASE_DRIVER=postgres
JUHE_AI_CACHE_DRIVER=redis
JUHE_AI_RUNTIME_STATE_DRIVER=redis
JUHE_AI_QUEUE_DRIVER=redis_stream
# production performance 必填；同机每个 Node 进程使用不同的稳定值。
JUHE_AI_INSTANCE_ID=juhe-ai-control-1
JUHE_AI_GATEWAY_REPLICAS=3
JUHE_AI_USAGE_WORKER_REPLICAS=2
JUHE_AI_LOG_WORKER_REPLICAS=2
JUHE_AI_STATS_WORKER_REPLICAS=1
JUHE_AI_OPS_WORKER_REPLICAS=1
JUHE_AI_USAGE_SPOOL_DIR=/绝对路径/juhe-ai-lite/shared/usage-spool
JUHE_AI_USAGE_SPOOL_MAX_ITEMS=250000
JUHE_AI_USAGE_SPOOL_MAX_MB=4096
JUHE_AI_USAGE_SPOOL_REPLAY_BATCH_SIZE=500
JUHE_AI_USAGE_SPOOL_REPLAY_INTERVAL_MS=1000
JUHE_AI_GATEWAY_TIMING_DETAIL_SAMPLE_PERMILLE=50
JUHE_AI_GATEWAY_STAGE_PRESSURE_MAX_PENDING_MB=8

JUHE_AI_POSTGRES_URL=postgres://juhe_ai:<PostgreSQL密码URL编码>@pgbouncer:5432/juhe_ai
JUHE_AI_REDIS_CACHE_URL=redis://:<Redis缓存密码URL编码>@redis-cache:6379/0
JUHE_AI_REDIS_STATE_URL=redis://:<Redis运行态密码URL编码>@redis-state:6379/0
JUHE_AI_REDIS_QUEUE_URL=redis://:<Redis队列密码URL编码>@redis-queue:6379/0
JUHE_AI_REDIS_NAMESPACE=prod

JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT=256
JUHE_AI_DB_POOL_MAX=150
JUHE_AI_DB_WRITE_MAX_CONCURRENCY=100
JUHE_AI_DB_WRITE_QUEUE_MAX_ITEMS=50000
JUHE_AI_POSTGRES_STATEMENT_TIMEOUT_MS=30000
JUHE_AI_POSTGRES_LOCK_TIMEOUT_MS=2000
JUHE_AI_POSTGRES_IDLE_IN_TRANSACTION_TIMEOUT_MS=30000

JUHE_AI_GATEWAY_UPSTREAM_AGENT_MAX_SOCKETS=2048
JUHE_AI_GATEWAY_UPSTREAM_AGENT_MAX_FREE_SOCKETS=512
JUHE_AI_GATEWAY_UPSTREAM_AGENT_MAX_TOTAL_SOCKETS=8192
```

规则：

- `.env.performance.example` 的密钥和连接串保持空值，未填写时 Compose 必须失败。
- 改数据库或 Redis 密码时，同步改对应 URL；密码和 URL 不能漂移。
- URL 里的密码必须百分号编码；避免直接 `source .env.performance` 后拼接原始密码。
- `JUHE_AI_TRUST_PROXY=true` 只在应用端口被 Caddy、负载均衡或可信内网入口保护时使用。
- 同机多进程模式由每个 server 自动分配 DB service 本机端口；Nginx 不直连 DB service。管理流量进入 control，AI 流量只进入 gateway 池。每个 gateway 必须把 `JUHE_AI_ACCOUNT_HEALTH_CHECK_DISPATCH_URL` 指向实际 control loopback 端口，使真实请求失败触发能经 HMAC 内部通道到达 ops-worker；该地址不得指向公网入口，缺失或非法时 gateway 会拒绝启动。
- control 的站内 AI 问答通过 Redis state 自动发现本机 Gateway 实例，再直接访问其 loopback 端口；不经过 Nginx ingress，也不需要额外环境变量。standalone 与 combined 继续直连同一应用实例的 `JUHE_AI_PORT`。
- macOS performance 安装先按 Gateway 逐个重启并通过 `/__aisys__/api/health` DB-service 健康门禁，再重启 control 及其 worker；确认 Node 完整拓扑角色已注册后，启动并验证唯一 Go sidecar，才能切换 Nginx。指标 publisher 对在线请求保持 fail-open，但部署 gate 对注册失败 fail-closed 并触发安装回滚，防止 sidecar 在 Node/DB service 尚未就绪时提前上线。F1 的 `JUHE_AI_RUNTIME_LOG_STORE` 固定为 `postgres`，`JUHE_AI_RUNTIME_LOG_POSTGRES_URL` 优先读取、缺失时由 `JUHE_AI_POSTGRES_URL` 提供；两者都缺失时安装启动失败，不回退 SQLite 或 Redis。
- `JUHE_AI_USAGE_SPOOL_DIR` 必须位于 release 目录之外并由 control 与所有 gateway 进程共同可读写；每个 gateway 只写自己的实例子目录，Usage worker 副本 0 扫描全部实例目录并在 Redis 恢复后重放。
- performance 默认只保留 50/1000 的正常网关阶段详情；失败、中断、慢阶段和每请求终态摘要仍保留。日志 publisher 待写字节达到压力阈值时，正常阶段详情暂停，不能反向阻塞 AI 流。
- `JUHE_AI_GATEWAY_UPSTREAM_AGENT_MAX_*` 控制网关到上游的 Node keep-alive 连接池。慢 SSE 或长对话并发较高时优先加大这些上限，并同步确认系统 `nofile` / launchd / systemd 文件句柄上限；不要改成无限连接。
- 公网 Edge、WireGuard、Caddy/Nginx、Linux/macOS 系统参数和入口容量门禁由 [反向代理与高并发隧道部署指南](反向代理与高并发隧道部署指南.md) 维护；本指南只负责应用与数据层高性能模式。
- `JUHE_AI_REDIS_NAMESPACE` 是同一套部署的 Redis key 前缀，生产建议显式设置并保持稳定；多套环境共用 Redis 实例时必须使用不同 namespace。
- 高性能模式要求 `JUHE_AI_REDIS_CACHE_URL`、`JUHE_AI_REDIS_STATE_URL`、`JUHE_AI_REDIS_QUEUE_URL` 指向三个不同的物理 `host:port`；同一进程不同 DB 不算隔离，也不存在共享绕过开关。Redis URL host 不接受 `localhost` 或 `::1`，同机部署统一使用 canonical `127.0.0.1`。
- 普通 PostgreSQL SQL 直接通过 pool / PgBouncer 执行，`statement_timeout`、`lock_timeout` 和 `idle_in_transaction_session_timeout` 必须配置为目标数据库或应用 role 默认值；部署迁移后要通过 PgBouncer 新连接执行 `SHOW` 精确验证。显式多语句事务仍由应用按上述环境变量执行 `SET LOCAL` 覆盖。不要在 URL 里拼 startup parameter，避免 PgBouncer 拒绝或跨事务污染。
- 当前 Docker 模板的 `POSTGRES_USER=juhe_ai` 是 PostgreSQL 初始化账号，按官方镜像语义具备较高权限；生产必须把 PostgreSQL / PgBouncer 端口限制在本机、WireGuard 私网或安全组白名单内。独立 admin/app 账号改造需要同步 PgBouncer、初始化脚本和回归验证。

PowerShell 生成 URL 编码密码：

```powershell
[uri]::EscapeDataString('替换为强密码')
```

Node 生成 URL 编码密码：

```bash
node -e "console.log(encodeURIComponent(process.argv[1]))" '替换为强密码'
```

## 5. 启动

macOS 裸机先确保 Nginx 主配置已经 include 一个专用绝对路径，再用默认 dry-run 执行不可变发布输入的只读门禁：

```bash
bash docs/deploy/macos/operations/install-performance-topology.sh --dry-run \
  --scope user \
  --base-dir "$HOME/juhe-ai-lite" \
  --label-prefix com.example.juhe-ai.performance \
  --control-port 3200 \
  --gateway-base-port 3101 \
  --gateway-count 3 \
  --usage-workers 2 \
  --log-workers 2 \
  --ingress-port 3000 \
  --nginx-config /opt/homebrew/etc/nginx/servers/juhe-ai-performance.conf \
  --nginx-bin /opt/homebrew/bin/nginx \
  --nginx-main-config /opt/homebrew/etc/nginx/nginx.conf
```

dry-run 不模拟启动流程，也不调用 Node、launchctl、sudo 或 Nginx；它只读验证真实发布目录、required Node 构建文件和预检脚本、`backend/.env`、物理 base 目录，以及可执行的常规文件 `backend-go/juhe-ai-go-sidecar`。任一缺失即失败。system scope 必须显式传 `--nginx-main-config`，且 `--nginx-config` 必须是主配置已 include 的独立槽位文件，不能等于主配置。apply 时只验证服务用户可读/执行该二进制，并在 Node DB-service readiness 后加载一个 Go sidecar。F3 强制 PostgreSQL、稳定 instance ID、专用 blob/hot-search 目录、loopback `204` health、显式 `JUHE_AI_AUDIT_LOG_INPUT_SECRET` 与一致的 input URL/listen address；F3 优先读取 `JUHE_AI_AUDIT_LOG_POSTGRES_URL`，只在缺失时回退 `JUHE_AI_POSTGRES_URL`。脚本不读取凭据、不直接查询数据库，也不会把 liveness 当成 F1/F2 freshness 或 F3 审计提交成功。

同端口、同 label 的原地 apply 会逐项重启 active 服务，不是零停机发布。生产必须按 [生产发布快速流程](生产发布快速流程.md) 把构建、上传和 candidate 启动放在旧槽承流期间；candidate 使用独立 Node runtime、label、端口、Nginx slot include、upstream suffix、Node instance ID 和 Redis 身份，但必须以 `--go-sidecar-mode reuse` 复用承流槽唯一 sidecar，禁止创建第二个 F1/F2/F3/F4 owner。F4 当前仍是切换前实现；开始其独立 candidate 验收前，不得借复用模式跳过 F4 listener、owner lease 和 Node 写后读回。常规 candidate 启动可传 `--quick`，只等待一次 Node control/API health、gateway health 与 shared Go sidecar health，跳过指标注册和重复稳定轮询。之后使用 `quick-performance-cutover.sh` 完成一次 route 原子切换，并通过公网 control/API health 与无 Key gateway `401` 验证结果；任一失败自动恢复原 route。`performance-handover-controller.sh` 仅用于异常回切或深入调查。2026-08-12 的首次 F3 正式上线发生过硬停机和前端 API-base 构建错误，因此静态首页或单个 health 不能放行，但也不再把长 preflight、F1/F2 freshness、审计读回、worker/PID 集合和多轮稳定观察强制放在每次 routine release 前。

项目根目录先构建：

```bash
pnpm install
pnpm build
```

启动：

```bash
cd docker
docker compose --env-file .env.performance -f compose.performance.yml config --quiet
docker compose --env-file .env.performance -f compose.performance.yml up -d --build --wait
docker compose --env-file .env.performance -f compose.performance.yml ps
```

看日志：

```bash
docker compose --env-file .env.performance -f compose.performance.yml logs --tail=100 postgres
docker compose --env-file .env.performance -f compose.performance.yml logs --tail=100 pgbouncer
docker compose --env-file .env.performance -f compose.performance.yml logs --tail=100 redis-cache
docker compose --env-file .env.performance -f compose.performance.yml logs --tail=100 redis-state
docker compose --env-file .env.performance -f compose.performance.yml logs --tail=100 redis-queue
docker compose --env-file .env.performance -f compose.performance.yml logs --tail=100 juhe-ai
```

`postgres`、`pgbouncer`、`redis-cache`、`redis-state`、`redis-queue` 应为 healthy。`juhe-ai` 如果因未覆盖入口 fail-fast，先按第 7 节定位具体入口和回归范围，不要让配置回退 SQLite。

## 6. PostgreSQL 初始化

以下命令在 Linux / bash Docker 宿主机执行。Docker 宿主机验证要把 URL 改成宿主机发布端口：

```bash
export JUHE_AI_RUNTIME_MODE=performance
export JUHE_AI_DATABASE_DRIVER=postgres
export JUHE_AI_CACHE_DRIVER=redis
export JUHE_AI_RUNTIME_STATE_DRIVER=redis
export JUHE_AI_QUEUE_DRIVER=redis_stream
export JUHE_AI_INSTANCE_ID='juhe-ai-control-1'
export JUHE_AI_POSTGRES_URL='postgres://juhe_ai:<PostgreSQL密码URL编码>@127.0.0.1:6432/juhe_ai'
export JUHE_AI_REDIS_CACHE_URL='redis://:<Redis缓存密码URL编码>@127.0.0.1:6379/0'
export JUHE_AI_REDIS_STATE_URL='redis://:<Redis运行态密码URL编码>@127.0.0.1:6380/0'
export JUHE_AI_REDIS_QUEUE_URL='redis://:<Redis队列密码URL编码>@127.0.0.1:6381/0'
export JUHE_AI_REDIS_NAMESPACE='prod'
```

空库、首次部署或测试库重建执行完整初始化：

```bash
pnpm --filter juhe-ai-backend postgres:init-schema
```

已有当前版本库只需要复查 DDL 时，才执行 schema-only：

```bash
pnpm --filter juhe-ai-backend postgres:init-schema-only
```

`postgres:init-schema` 面向空 PostgreSQL 库或明确要重建的测试库，会创建当前版本 schema 并写入默认系统账号、供应商、协议档案、默认分组和系统设置种子；只跑 `schema-only` 会导致创建分组 / 账号时缺默认供应商数据。`postgres:init-schema-only` 只用于当前版本库的 DDL 复查，不作为旧库结构迁移方案。生产执行前先完成临时库预演、项目备份和业务备份，执行后跑第 7 节 readiness。完整初始化会写入默认管理员，开放公网前必须先在内网登录后台轮换默认管理员密码，未完成改密禁止放通 Caddy / Nginx / 防火墙公网入口。

抽样检查：

```bash
docker compose --env-file .env.performance -f compose.performance.yml exec -T postgres \
  sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SELECT count(*) FROM juhe_business.system_accounts;"'
```

### 6.1 数据切换边界

项目代码库不提供 SQLite -> PostgreSQL 数据迁移脚本、旧 PostgreSQL 结构迁移脚本、启动期自动迁移、双读双写或旧 schema 兼容。高性能模式部署只负责“当前版本 PostgreSQL schema 初始化 + 当前代码路径验证”。历史数据是否保留、如何导入、如何对齐旧结构，由运维在代码库外按当次上线窗口单独处理；处理结果必须落到当前 schema，应用代码不承担旧结构成本。

切换到高性能模式后至少执行：

```bash
pnpm --filter juhe-ai-backend postgres:init-schema
export JUHE_PERFORMANCE_SYSTEM_API_POSTGRES_URL="$JUHE_AI_POSTGRES_URL"
export JUHE_PERFORMANCE_SYSTEM_API_REDIS_CACHE_URL="$JUHE_AI_REDIS_CACHE_URL"
export JUHE_PERFORMANCE_SYSTEM_API_REDIS_STATE_URL="$JUHE_AI_REDIS_STATE_URL"
export JUHE_PERFORMANCE_SYSTEM_API_REDIS_QUEUE_URL="$JUHE_AI_REDIS_QUEUE_URL"
pnpm --filter juhe-ai-backend test:performance-system-api-smoke
pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness
pnpm --filter juhe-ai-backend test:performance-boundary
```

### 6.2 业务数据只读对账

已有 PostgreSQL 业务数据需要在两个数据库之间核对时，使用只读对账入口。源库和目标库必须是两个不同的 PostgreSQL 数据库；两端账号只授予 `juhe_business` 的读取权限，不使用生产写账号：

```bash
export JUHE_AI_BUSINESS_VERIFY_SOURCE_POSTGRES_URL='postgres://<只读用户>:<密码>@<源地址>:5432/<源库>'
export JUHE_AI_BUSINESS_VERIFY_TARGET_POSTGRES_URL='postgres://<只读用户>:<密码>@<目标地址>:5432/<目标库>'
export JUHE_AI_BUSINESS_VERIFY_ALLOWED_NEW_TABLES=''
export JUHE_AI_BUSINESS_VERIFY_ALLOWED_ADDITION_TABLES=''
pnpm --filter juhe-ai-backend ops:verify-business-preservation
```

`JUHE_AI_BUSINESS_VERIFY_ALLOWED_NEW_TABLES` 是允许只存在于目标库的 `juhe_business` 表名，`JUHE_AI_BUSINESS_VERIFY_ALLOWED_ADDITION_TABLES` 是允许目标库新增行的既有表名；两者都使用英文逗号分隔，默认留空。只有经过当次变更审计的表才能加入白名单，不得为了让检查通过而放宽范围。

该命令会在源库和目标库分别开启 `REPEATABLE READ READ ONLY` 事务，比较业务表、源列、主键和源行内容，只输出表名、行数、摘要与违规项。它不会执行 migration、DDL、修复、删除或其他写入；发现同一数据库、缺表、缺列、主键变化、源行缺失或修改、未批准的新表或新增行时以非零状态退出。连接串只通过环境变量传入，不写入命令参数、源码或日志。

## 7. 健康检查和回归

中间件：

```bash
docker compose --env-file .env.performance -f compose.performance.yml exec postgres \
  sh -c 'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"'

docker compose --env-file .env.performance -f compose.performance.yml exec pgbouncer \
  sh -c 'pg_isready -h 127.0.0.1 -p 5432 -U "$DB_USER" -d "$DB_NAME"'

docker compose --env-file .env.performance -f compose.performance.yml exec redis-cache \
  sh -c 'redis-cli -a "$REDIS_CACHE_PASSWORD" ping'

docker compose --env-file .env.performance -f compose.performance.yml exec redis-state \
  sh -c 'redis-cli -a "$REDIS_STATE_PASSWORD" ping'
```

应用：

```bash
curl -i http://127.0.0.1:3000/__aisys__/health
curl -i http://127.0.0.1:3000/__aisys__/api/health
curl -I http://127.0.0.1:3000/__aisys__/
```

已迁移链路回归：

```bash
pnpm --filter juhe-ai-backend test:provider-repository-driver
pnpm --filter juhe-ai-backend test:system-account-session-driver
pnpm --filter juhe-ai-backend test:system-account-management-driver
pnpm --filter juhe-ai-backend test:group-management-driver
pnpm --filter juhe-ai-backend test:api-key-management-driver
pnpm --filter juhe-ai-backend test:authorization-return-driver
pnpm --filter juhe-ai-backend test:settings-public-driver
pnpm --filter juhe-ai-backend test:settings-management-driver
pnpm --filter juhe-ai-backend test:runtime-state-cache-redis
pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-runtime-state
pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-index
pnpm --filter juhe-ai-backend test:db-service-sqlite-dispatch-standalone
pnpm --filter juhe-ai-backend test:redis-stream-queue
pnpm --filter juhe-ai-backend test:performance-boundary
pnpm --filter juhe-ai-backend test:performance-no-sqlite-fallback
pnpm --filter juhe-ai-backend test:performance-redis-boundary
pnpm --filter juhe-ai-backend test:public-api-logs
pnpm --filter juhe-ai-backend test:public-api-log-db-service-ipc
pnpm --filter juhe-ai-backend test:audit-log-settings-contract
export JUHE_PERFORMANCE_SYSTEM_API_POSTGRES_URL="$JUHE_AI_POSTGRES_URL"
export JUHE_PERFORMANCE_SYSTEM_API_REDIS_CACHE_URL="$JUHE_AI_REDIS_CACHE_URL"
export JUHE_PERFORMANCE_SYSTEM_API_REDIS_STATE_URL="$JUHE_AI_REDIS_STATE_URL"
export JUHE_PERFORMANCE_SYSTEM_API_REDIS_QUEUE_URL="$JUHE_AI_REDIS_QUEUE_URL"
pnpm --filter juhe-ai-backend test:performance-system-api-smoke
pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness
```

真实网关压测建议在业务回归和 readiness 通过后执行。高性能模式成功审计策略必须和单机模式使用同一组实际配置，默认完全成功请求最近 1 小时全量热保留，超过热窗口后只保留 10% 稳定采样，问题审计仍全量。100 并发压测建议 `JUHE_AI_GATEWAY_LOAD_SETTLE_SECONDS` 不低于 `60`，并同时检查 usage / public API / record maintenance 三条 Redis Stream 的 pending 与 `XINFO GROUPS` lag；原始审计和操作日志分别检查 F3/F4 loopback health、写后读回和 owner lease，普通运行日志另查角色 JSONL 文件 backlog、Go F1 indexer cursor 和索引 freshness，不能把它们当作 Redis Stream。只看 pending 可能漏掉 consumer 尚未读取的尾部 backlog。

管理 API 读写混合压测可直接打真实已启动服务：

```bash
export JUHE_PERFORMANCE_LOAD_BASE_URL='https://ai.example.com'
export JUHE_PERFORMANCE_LOAD_DURATION_MS=30000
export JUHE_PERFORMANCE_LOAD_CONCURRENCY=50
export JUHE_PERFORMANCE_LOAD_WRITE_RATIO=0.15
export JUHE_PERFORMANCE_LOAD_MAX_READ_P95_MS=1500
export JUHE_PERFORMANCE_LOAD_MAX_READ_P99_MS=3000
pnpm --filter juhe-ai-backend test:performance-system-api-load
```

这个脚本会创建并清理压测用分组、AI 账户、策略路由和 API Key。只能指向测试环境或维护窗口目标环境，不能直接对生产业务数据执行。

## 8. 项目备份、业务备份与运行数据边界

高性能模式的每个生产发布窗口只在首次变更前生成一次恢复点，并分别只保留最近 3 个已完成窗口的备份：

- `project-时间戳`：升级前当前运行 release 的干净压缩包、清单和校验值；一次性迁移脚本仅作附加证据，不含 `.env`、数据、日志、数据库、`node_modules` 或链接。
- `business-时间戳`：只含同一时刻的 PostgreSQL `juhe_business` custom dump、schema SQL、恢复所需的 `backend.env` / `.env.performance`、清单和校验值；它与同 stamp 的项目备份组成升级前恢复点。

同一发布窗口的后续修复、重新预检、候选重启、切流重试、自动回退或前向修复都复用这一个已验证 stamp，禁止为了“再保险”重复导出业务库。只有明确结束当前窗口并开启新的生产发布窗口，才重新建立恢复点；非业务库、日志、审计、统计和 usage 数据始终不备份。

业务备份示例：

```bash
set -euo pipefail
umask 077

STAMP=$(date +%Y%m%d-%H%M%S)
BACKUP_ROOT=backups
STAGE="$BACKUP_ROOT/.business-$STAMP.tmp-$$"
FINAL="$BACKUP_ROOT/business-$STAMP"
test ! -e "$FINAL"
mkdir -p "$STAGE/pg"
trap 'rm -rf -- "$STAGE"' EXIT

cp .env.performance "$STAGE/backend.env"
chmod 600 "$STAGE/backend.env"
docker compose --env-file .env.performance -f compose.performance.yml exec -T postgres \
  sh -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" --schema=juhe_business --format=custom --no-owner --no-acl' \
  > "$STAGE/pg/juhe_business.dump"
docker compose --env-file .env.performance -f compose.performance.yml exec -T postgres \
  sh -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" --schema=juhe_business --schema-only --format=plain --no-owner --no-acl' \
  > "$STAGE/pg/juhe_business-schema.sql"
pg_restore --list "$STAGE/pg/juhe_business.dump" > "$STAGE/pg/juhe_business.manifest"
pg_restore --file=/dev/null --no-owner --no-acl "$STAGE/pg/juhe_business.dump"
{
  printf 'created_at=%s\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
  printf 'schema=juhe_business\n'
  printf 'format=custom\n'
} > "$STAGE/manifest.txt"
(
  cd "$STAGE"
  shasum -a 256 backend.env pg/juhe_business.dump pg/juhe_business-schema.sql \
    pg/juhe_business.manifest manifest.txt > SHA256SUMS
  shasum -a 256 -c SHA256SUMS
)
chmod -R go-rwx "$STAGE"
mv "$STAGE" "$FINAL"
trap - EXIT

retain_latest_three() {
  local prefix="$1" item index
  local -a backups=()
  while IFS= read -r item; do
    [ -d "$item" ] && [ ! -L "$item" ] && backups+=("$item")
  done < <(printf '%s\n' "$BACKUP_ROOT"/"$prefix"-* | LC_ALL=C sort -r)
  for ((index = 3; index < ${#backups[@]}; index += 1)); do
    rm -rf -- "${backups[$index]}"
  done
}

retain_latest_three project
retain_latest_three business
```

示例中的 `pg_restore --list` 生成恢复清单，`pg_restore --file=/dev/null` 会完整读取 custom dump；两项都成功、校验值回读一致并原子改名后，才分别删除 `project-*` 和 `business-*` 的第 4 份及更早备份。生产脚本还应清理上次异常退出遗留的 `.project-*.tmp-*` / `.business-*.tmp-*`，但不得把暂存目录计入最近 3 次。`juhe_dataset`、`juhe_usage`、`juhe_stats`、`juhe_codex_context`、Redis、Compose 数据 volume、`data/`、日志和审计 payload 都不进入项目备份或业务备份。

### 上线数据决策

生产上线的数据安全目标只有一个：`juhe_business` 必须安全、平滑升级。它是唯一需要导出、恢复验证、逐表对账和作为回退依据的数据域。不得为了验证这条目标而导出、传输或恢复整个 PostgreSQL 数据库。

`juhe_dataset`、`juhe_usage`、`juhe_stats`、`juhe_codex_context`、审计 payload、运行日志和 Redis 都属于可重建的非业务运行数据。每次上线先按实际影响范围选择处理方式：

- 不受本次变更影响时，原样保留，不做无意义重建。
- 存在小范围兼容问题且定向修复成本更低时，只修复受影响对象。
- 涉及大规模历史迁移、重放、多 GB 数据搬运，或重建明显比修复安全、便宜时，先完成队列排空和写入冻结，再清空对应非业务数据域并由新版本重建。

非业务数据不得进入 `business-*` 备份、业务数据逐表对账或临时库恢复演练。即使决定清空，也必须保留 schema、明确清理对象、确认与 `juhe_business` 没有跨域外键依赖，并在新版本 ready 但生产入口尚未切流时执行；不得把服务端遗留的日志、审计或统计问题归因成业务数据迁移失败。

隔离 clone 只用于预演，绝不能被 outer route fragment 引用或承接生产流量。预演确实需要复制生产 `juhe_business` 时，使用管道直接恢复到临时库，不持久化 dump：

```bash
docker compose --env-file .env.performance -f compose.performance.yml exec -T postgres \
  sh -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" --schema=juhe_business --format=custom' \
  | pg_restore --dbname="$TEMP_POSTGRES_URL" --no-owner --no-acl
```

预演完成后可以删除该临时数据库。正式生产切流的候选槽必须连接已完成备份和 schema 验证的同一份生产 `juhe_business`，不能把 clone 当作正式业务权威库后再删除；在任一时刻只允许入口指向一个可处理业务写入的槽位。若架构因故必须让临时库承接生产流量，则必须在切流前启用并验证业务写冻结，且在恢复主库前完成有审计的增量同步与逐表对账；没有该能力时，禁止这种切流方式。跨主机数据转移必须作为单独授权的迁移操作处理，不得把非业务历史混入常规备份。

Redis：

- `redis-cache` 使用 `appendonly no`、`save ""`、`allkeys-lru`，不做备份。
- `redis-state` 是可重建的短 TTL 运行态，使用 `appendonly no`、`save ""`、`noeviction`；关闭持久化是为了避免 AOF rewrite 拖慢租约、限频和调度状态，不表示允许随机淘汰。
- `redis-queue` 是 Redis Streams 可靠队列事实源，使用 `appendonly yes`、`appendfsync everysec`、`save ""`、`noeviction`；入队不使用 `MAXLEN` 静默裁剪未确认消息，消费成功 `XACK` 后同步 `XDEL`。未 ACK 消息在落 PostgreSQL 前不能按缓存丢弃。
- 上线需要清理 Redis 时，先停应用并确认所有 Redis Streams 的 pending 和 lag 为 0；确认后只按 `JUHE_AI_REDIS_NAMESPACE` 使用 `SCAN/DEL` 删除本项目 key，不做 Redis 备份。共享 Redis 禁止 `FLUSHDB` / `FLUSHALL`；未确认 drain 前清理会丢未落库队列消息。

Redis Stream drain 检查示例：

```bash
docker compose --env-file .env.performance -f compose.performance.yml exec -T redis-queue \
  sh -c 'redis-cli -a "$REDIS_QUEUE_PASSWORD" XINFO GROUPS "juhe-ai:prod:queue:usage-records"'

docker compose --env-file .env.performance -f compose.performance.yml exec -T redis-queue \
  sh -c 'redis-cli -a "$REDIS_QUEUE_PASSWORD" XPENDING "juhe-ai:prod:queue:usage-records" "juhe-ai:prod:usage-record-writers"'
```

同样检查 `public-api-logs` 和 `record-maintenance`。正式切换使用 `backend/dist/scripts/operations/drain-redis-streams.js`：先用 token 建立 queue fence，再由独立 one-shot 进程只启动 usage、public API 和 record maintenance 三类 consumer；只有三个目标 group 均存在、所有 Stream `XLEN=0`、全部 group `pending=0/lag=0`，并且连续两个窗口 `XADD` 计数不增长才成功。错误 token、缺组、检查失败或超时都保留 fence 和现场。F3/F4 不参与 Redis drain，必须另行确认其 loopback health、owner lease 和写后读回；普通运行日志不参与 Redis drain，必须另行确认各角色 JSONL 文件的 `pendingFileCount=0`、`pendingBytes=0`，并确认唯一 Go sidecar 内 F1 的 cursor/freshness 已持续推进后再切换。

`lag`、`pending` 或 Redis 返回字段为 `null` / 缺失 / 非法值时按 unknown 阻断，不得折算为 0。one-shot 在启动任何 consumer 之前先确认三个既有目标 group，避免错误 namespace 被启动过程自动创建空 Stream/group 后误通过。无法解码的 Stream 消息保留 pending 并阻断排空，禁止自动 XACK/XDEL 删除唯一现场。

应用 volume：

- `juhe-ai-data` 仍可能保存审计 blob、上下文文件、自动生成文件和临时数据，不进入项目备份或业务备份。
- `juhe-ai-logs` 保存日志，不进入项目备份或业务备份。

## 9. 调优基线

起步值：

```text
PostgreSQL max_connections=300
PgBouncer default_pool_size=50
JUHE_AI_DB_POOL_MAX=150
JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT=256
JUHE_AI_DB_WRITE_MAX_CONCURRENCY=100
Redis cache/state maxmemory=512mb
```

调优顺序：

- 先看 PgBouncer 等待、PostgreSQL active session、慢 SQL、锁等待和 Redis Stream backlog（pending + consumer group lag）。
- 不直接通过加大连接数解决慢查询或热点行冲突。
- `redis-cache` 可淘汰；`redis-state` 和 `redis-queue` 不允许随机淘汰。
- Redis Streams 是 at-least-once 写入队列，不是最终事实表；但未 ACK 条目代表尚未落库的队列事实，不能按缓存丢弃。

## 10. 非 Docker 边界

非 Docker 只要部署出等价边界即可：

```text
PostgreSQL 18
PgBouncer 6432
Redis cache 6379
Redis state 6380
Redis queue 6381
```

三个端口必须由三个不同 PID 持有。macOS 使用 `docs/deploy/macos/operations/install-redis-role-services.sh` 以 dry-run/apply 方式安装或替换 system LaunchDaemon，并使用 `verify-redis-role-isolation.sh` 验证 URL、PID、launchd、PING、策略与持久化；任何角色切换失败都恢复原 plist/config/loaded 状态。完成后仍使用本文第 4、6、7 节的 env、初始化和回归命令。

硬性要求：

- PgBouncer 和 Redis 默认绑定 `127.0.0.1`。
- 跨机器访问只走内网 IP、WireGuard 私网或安全组白名单；业务回源只允许 WireGuard。
- `pg_hba.conf` 用真实可信 CIDR 占位，不写死示例私网。
- 应用和 worker 连接 PgBouncer，不直连 PostgreSQL。

## 11. 停止和清理

停止：

```bash
docker compose --env-file .env.performance -f compose.performance.yml down
```

删除 volume：

```bash
docker compose --env-file .env.performance -f compose.performance.yml down -v
```

`down -v` 会删除 PostgreSQL、Redis、应用数据和日志，只能用于临时测试环境。
