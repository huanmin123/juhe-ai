# PLAN-0164-20260723T145206511Z 列表手动刷新完整响应

## 基本信息

- 状态：已完成（保留已知验证缺口）
- 创建时间：2026-07-23
- 需求来源：用户对话
- 执行者：Codex

## 需求目标

彻底移除 AI 账户、分组和策略路由列表的前端动态快照补发，让管理侧与“我的”用户侧列表在用户操作触发时由单个列表接口返回当前页完整事实，并消除并发“未知 / 暂不可用”和账户 `active -> 正常` 的错误降级展示。

## 范围边界

- 包含 Node 后端、Vue 前端、共享类型、回归测试和权威文档。
- 包含 AI 账户、分组、策略路由三类已确认存在列表补发的领域。
- 不修改 Go 实现；后续迁移根据本轮 Git 事实对齐。
- 不删除账户测试、聊天生成、模型检测等活动任务自身需要的轮询或心跳。
- 不执行生产部署或线上数据操作。

## 关联文档

- [列表手动刷新完整响应设计](../functions/列表手动刷新完整响应设计.md)
- [接口契约与权限矩阵](../functions/接口契约与权限矩阵.md)
- [前端架构设计](../architecture/frontend/README.md)
- [后端架构设计](../architecture/backend/README.md)
- [开发测试与验证说明](../develop/测试与验证说明.md)

## 执行拆解

- [x] 审计所有前端列表动态快照、自动刷新和占位展示。
- [x] AI 账户列表内联状态、并发、用量、余额和状态提示事实。
- [x] 分组列表内联当前并发和今日用量。
- [x] 策略路由列表内联计数和绑定预览。
- [x] 删除三类列表的前端快照 API、状态机和无消费者 HTTP 路由。
- [x] 更新类型、专项回归、接口文档和测试入口。
- [x] 完成定向测试、类型检查、构建和代码复查；页面实机验证因 Browser 插件不可用未执行。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 后端契约 | 账户完整列表 | `account-status-snapshot-contract-regression`、`runtime-snapshot-unavailable-contract-regression` | 当前页含真实状态、并发、用量、余额摘要 | 通过 | 并发读取失败统一为 0 |
| 后端契约 | 分组完整列表 | `runtime-snapshot-unavailable-contract-regression`、`management-list-contract-regression` | 当前页含并发和今日用量 | 通过 |  |
| 后端契约 | 策略路由完整列表 | `route-strategy-list-snapshot-regression`、`management-list-contract-regression` | 当前页含计数和绑定预览 | 通过 |  |
| 前端契约 | 无快照补发 | 三个 inline regression + account status polling regression | 不引用 status/list snapshot API 或渐进状态机 | 通过 |  |
| 权限边界 | 管理 / self | `pnpm test:scope` | 普通用户不能越权，my 接口固定当前用户 | 阻断 | 现存用户侧分组选项断言失败，非本次列表补发改动 |
| 代码检查 | 类型与构建 | 前后端 `typecheck`、前端 `build` | 通过 |  |
| 页面体验 | 列表加载与手动刷新 | Browser / Playwright | 并发显示 0/上限，状态显示可调度或真实阻断 | 未执行 | Browser 插件当前不可用 |

## 进度记录

| 日期 | 状态 | 记录 |
| --- | --- | --- |
| 2026-07-23 | 已实现待交付 | 完成三类列表单接口完整响应、删除外部快照补发、并发 0 兜底、真实状态文案和回归验证；全局 scope 回归有既有分组选项失败，页面实机验证受 Browser 插件不可用影响。 |

## 决策记录

| 日期 | 决策 | 原因 |
| --- | --- | --- |
| 2026-07-23 | 列表请求一次性返回当前页完整事实 | 已取消动态刷新，分阶段请求只会制造字段缺失和错误占位。 |
| 2026-07-23 | 并发无数据或运行态读取失败统一返回 0 | 产品不再暴露“未知 / 暂不可用”，并发语义收敛为当前占用数。 |
| 2026-07-23 | 本轮不改 Go | 用户明确要求后续按 Git 事实单独整改。 |

## 验收标准

- [x] 三类列表每次加载只调用一个领域列表接口。
- [x] AI 账户显示综合调度状态和完整悬浮信息，并发不出现未知占位。
- [x] 分组并发与今日用量不出现未知占位。
- [x] 策略路由计数和预览不出现加载中或暂不可用。
- [x] 所有补充读取保持当前页批量查询，无 N+1。
- [ ] 定向回归、权限回归、类型检查和构建通过。

## 验证记录

- 前端类型检查、构建和四个列表专项回归通过；后端账户/分组/策略路由/运行态/网关/凭据回归通过。
- `pnpm test:scope` 失败于现存用户侧分组选项权限断言；未发现与本次列表补发改动的调用链关联。
- Browser 插件不可用，未执行页面实机截图验证。

## 风险与注意事项

- 完整列表需要等待当前页批量运行态与统计读取，必须通过有界页大小、批量查询和并行读取控制尾延迟。
- 删除独立快照路由前必须确认无前端和 Node 外部消费者；Go 路由不在本轮改动范围。
- 动态状态筛选必须继续按综合状态返回准确结果，不能退化为只筛数据库持久状态。

## 完成总结

- 已完成实现和定向验证；保留全局 scope 回归与页面实机验证两个外部验证项。
