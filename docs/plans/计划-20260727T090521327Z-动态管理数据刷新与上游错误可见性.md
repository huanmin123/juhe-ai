# PLAN-20260727T090521327Z 动态管理数据刷新与上游错误可见性

## 基本信息

- 计划 ID：PLAN-20260727T090521327Z
- 状态：已完成（本地验证）
- 创建时间：2026-07-27
- 需求来源：用户反馈“我的使用记录”筛选/刷新看不到最新记录，并要求后端动态数据保证最新准确、使用记录展示真实上游错误。
- 范围：Node 后端、Vue 前端、自动化测试、权威文档和本地隔离 Browser 验证；不执行生产部署。

## 关联设计

- [动态管理数据刷新与缓存边界设计](../functions/动态管理数据刷新与缓存边界设计.md)
- [列表手动刷新完整响应设计](../functions/列表手动刷新完整响应设计.md)
- [页面数据缓存与增量更新设计（历史归档）](../functions/页面数据缓存与增量更新设计.md)
- [安全与日志策略](../functions/安全与日志策略.md)

## 任务清单

- [x] 审计后端管理 API 的响应缓存、TTL、预热与失效链路。
- [x] 审计前端页面状态缓存、完整响应缓存和手动刷新请求代次。
- [x] 删除使用记录后端首屏缓存、写入发布、预热 job、候选查询和专项旧测试。
- [x] 保证桌面与移动端手动刷新产生独立的最新列表请求。
- [x] 修复其他已证实会让动态页面陈旧的缓存路径；稳定元数据与网关运行态缓存保持不变。
- [x] 使用记录保留脱敏后的上游错误详情，不再被统一文案覆盖。
- [x] 补充缓存退场、刷新代次、错误解析与使用记录写入回归。
- [x] 运行相关回归、前后端 typecheck/build 和本次改动范围 `git diff --check`。
- [x] 启动独立端口、独立数据库/日志/runtime 的自动登录实例，用 Browser 验证页面与网络。
- [x] 完成需求、性能、边界、并发、安全和文档复查。

## 验收标准

- 使用记录列表 API 不再命中后端首屏响应缓存。
- 手动刷新绕过刷新前的前端在途请求，实际发出新的业务请求。
- 其他页面审计结论有代码证据，不把统计预聚合或网关运行缓存误删。
- 非 `2xx` 上游自定义错误经过脱敏和长度限制后进入使用记录；空/不可解析错误有清晰回退。
- 自动化和 Browser 验证均记录实际结果与剩余风险。

## 进度记录

- 2026-07-27：确认使用记录默认首屏会读取 36 小时后端热缓存；当前刷新仅重置分页/选项，未改变后端缓存读取。
- 2026-07-27：确认权威文档已规定页面恢复直接调用业务接口，不得恢复通用 page-data/持久响应缓存。
- 2026-07-27：删除使用记录首屏缓存与预热全链路；表监控删除 30 秒响应缓存；动态页面 `KeepAlive` 改为显式 opt-in；代理选项删除跨页面结果缓存。
- 2026-07-27：上游完整非 `2xx` 的 `message/code/type` 经脱敏、限长后进入失败使用记录；固定统一文案已无生产引用。
- 2026-07-27：前后端 typecheck、完整 build 和定向回归通过。本次改动范围 `git diff --check` 通过；全工作区检查仅被并行账户批量编辑改动中的尾随空格阻断。
- 2026-07-27：隔离实例后端 `3151`、前端 `5174`。桌面刷新捕获独立 GET/200，写入唯一标记后刷新成为首条；1280px 与 390px 均无横向溢出、遮罩或控制台错误。Browser 不支持真实移动 touch 注入，移动下拉刷新以代码契约回归验证。
- 2026-07-27：追加全页面复扫，删除账户与日志等动态详情 5 秒结果缓存、模型检测账户选项 10 秒结果缓存和聊天模型列表 30 秒后端快照；保留筛选状态、路由代码预加载、稳定元数据与相同在途请求合并。
- 2026-07-27：隔离 Browser 连续两次展开模型检测目标账户下拉，均实际发出新的 `account-options` 请求且无控制台错误；聊天页因隔离库无默认 API Key 未做伪造交互通过，使用专项回归验证。
