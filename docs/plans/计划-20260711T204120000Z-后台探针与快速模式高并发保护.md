# PLAN-20260711T204120000Z 后台探针与快速模式高并发保护

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除冷却复测头部阻塞，并让“普通路由 + 快速模式 + 高并发分组 + 大上下文”在保留有界等待语义的同时，不堆积完整请求体、不发生无目标切号和额度并发超发。

**Architecture:** ops-worker 冷却复测使用有界复合游标补充队列，任何非完整成功结果都写失败退避并推进下次时间。网关在 `preResolveGatewayRuntime` 之后、`express.raw` 之前对 `normal/speed_first` 高并发分组发放轻量入场租约，等待请求依靠 TCP backpressure 保留在客户端。确认慢切号前同时申请路由级迁移预算和目标账号真实并发槽；请求派发前根据模型价格和请求规模原子预占 API Key 在途成本。

**Tech Stack:** Node.js 22、TypeScript、Express、SQLite/PostgreSQL、Redis runtime state、现有账号并发槽、retry queue、模型价格和额度快照。

---

## 问题证据

- 生产库曾有 246 个已逾期冷却账户，目标账户逾期数小时仍为复测 0 次。
- 冷却查询固定按 `cooldown_until` 取头部小窗口；`accountFailureEligible === false` 时不更新 `cooldown_until`，同一批账户每轮重复占位。
- 网关先由 `express.raw` 读完请求体，后在 dispatch preparation 中进入高并发队列；队列元数据轻量，但请求调用栈仍持有 `rawBody`。
- 首字 deadline 回调只确认“剩余候选存在”，即中止当前上游；没有在中止前保留替补账号并发槽。
- 当前 API Key 额度只读后台统计快照并带 5 秒缓存，没有在途请求成本预占。

## 确定边界

- 后台系统探针只认完整成功与失败；状态码、错误码和错误分类只用于日志和展示。
- 普通用户请求仍保留本地参数错误边界，不因用户发错 JSON、模型名或上下文超限直接写坏账号。
- 入场排队只对 `normal + speed_first + high_concurrency` 生效；个人分组、成本优先和其他路由模式不变。
- 入场容量按当前分组唯一并发事实账号的硬并发上限求和；队列总量、单 Key 上限和等待时间继续使用分组策略。
- 确认慢切号共享预算固定为同一 `系统账户 + 路由策略 + 分组 + 慢账号` 同时 2 个迁移，不新增用户配置。
- 在途成本使用 `ceil(rawBodyBytes / 4)` 估算输入 token；输出 token 优先读 `max_output_tokens/max_tokens`，缺省按 4096；无定价模型不伪造成本。
- 在途预占在响应结束后延迟 65 秒释放，覆盖生产 60 秒统计快照窗口；进程退出时请求也同时结束，本地在途状态可清空。

## Task 1：冷却复测二元失败和有界公平扫描

**Files:** `cooldown-account-retest.service.ts`、`account-probe-jobs.ts`、`account-cooldown-retest.repository.ts`、DB service 类型和定向回归。

- [x] 修正旧回归夹具，用当前流程激活账户，先证明旧夹具因 `pending_test` 失效。
- [x] 写 RED：`accountFailureEligible=false` 仍必须记录复测失败和新 `cooldown_until`。
- [x] 写 RED：队列有已运行/待执行项时，复合游标只补足到 batch 上限；到尾部后回绕，不固定取头部。
- [x] 实现 `(cooldown_until, priority, created_at, id)` 稳定游标和有界队列补位。

## Task 2：大请求读取前入场租约

**Files:** 新增 `speed-first-body-admission.service.ts`、`speed-first-body-admission.middleware.ts`，修改 `server.ts`，新增定向回归。

- [x] 写 RED：第二个请求在第一个租约释放前不进入 body parser，且队列只保存身份与 deadline。
- [x] 写 RED：等待超时、队列满、单 Key 满和客户断开均释放状态并返回/记录本地 429。
- [x] 实现中间件与响应 `finish/close` 幂等释放，插入 `preResolveGatewayRuntime` 之后和 `express.raw` 之前。

## Task 3：目标槽预留和路由级迁移预算

**Files:** 新增 `normal-route-cutover-reservation.service.ts`，修改 `upstream-dispatch.ts`、`routes.ts` 和速度优先回归。

- [x] 写 RED：替补账号没有真实并发槽时，当前慢请求继续等待，不中止。
- [x] 写 RED：拿到目标槽后才中止；下一次 dispatch 消费同一槽，所有失败/取消路径幂等释放。
- [x] 写 RED：同一慢账号最多 2 个存量请求同时迁出，其余继续当前上游。

## Task 4：API Key 在途成本预占

**Files:** 新增 `api-key-inflight-reservation.service.ts`，修改 API Key 额度、preflight、routes 和大 JSON 元数据扫描，新增回归。

- [x] 写 RED：并发请求只有第一个能在剩余额度内原子预占，第二个立即被额度 429 拒绝。
- [x] 写 RED：大 JSON 元数据扫描可读取 `max_output_tokens/max_tokens`，无定价模型不伪造成本。
- [x] 实现单进程 API Key 原子锁、窗口成本 + 在途成本判定、响应终态延迟释放和泄漏 TTL。

## Task 5：文档、压力验证和发布

- [x] 同步架构总览、高并发分组、速度优先、探针恢复和测试说明。
- [x] 执行全部定向回归、`pnpm typecheck`、`pnpm build`、`git diff --check`。
- [x] 用大 Body + 高并发 Mock AI 压力脚本验证峰值堆内存、队列超时、客户断开和切号 attempt 数。
- [ ] 从隔离分支合并到 `master`、推送、严格临时服务接管上线，再将 `master` 合入 `feature/20260706-go`。

## 验证记录

- 2026-07-12：定向回归覆盖 4MB 双请求正文入场、排队超时、客户断开、单 Key 队列上限、目标槽预留、同慢账号迁移预算、Mock AI 首字切换和 API Key 并发额度预占。
- 2026-07-12：`pnpm typecheck`、`pnpm build` 和 `git diff --check` 通过；发布构建仅保留 Vite 既有大 chunk 警告。
- 2026-07-12：综合压测首次发现共享 Mock 夹具账户仍为 `pending_test`、SQLite read worker 清理顺序错误且 CLI 清理后未显式退出；修正后完成 1MB 请求体、50 并发 Responses / Chat 验证，各 50 个请求全部成功，上游命中 100 次，错误率为 0%，峰值 RSS 约 1.03GB，脚本自行以退出码 0 结束。
- 2026-07-12：第一次生产接管在主进程健康、工作目录、进程拓扑、前端哈希、PostgreSQL 和 Redis 均通过后，被启动日志门禁拦截：ops-worker 在启动 2 秒后按 batch=10 同时执行 10 路冷却完整诊断，与约 22 秒的统计窗口初始化争用 DB service，出现 cache invalidation / DB service timeout 和 unhandled rejection。Nginx 立即切回临时服务，current 恢复到上一稳定 release，主服务复核通过后清理临时 PostgreSQL / Redis；失败版本未继续承载生产流量。
- 2026-07-12：针对上线现场将每个进程内的自动完整诊断共享限制为最多 3 路，server 运行态恢复探针、升级后的 precheck 和 Redis 运行态探针也共享同一 3 路门禁；冷却复测、Key 级复测和速度优先恢复首轮错峰到 60 / 65 / 75 秒，探针 DB service 请求窗口统一为 30 秒并在超时错误中携带 operation 类型。不同 worker 不引入 Redis 全局诊断锁，继续依靠任务归属、错峰和 DB service 优先级隔离；同步修正旧网关回归夹具按当前 `pending_test -> 健康检查激活` 流程建立可调度账户。
- 开发共享 PostgreSQL 测试库仍是旧 schema，缺少 `provider_protocol_profiles.default_health_check_model`，因此不在该库上伪造通过；上线使用当前代码初始化的临时 PostgreSQL 数据库执行严格接管验证。

## 回滚

- 代码回滚不需要数据库逆迁移。
- 关闭新入场中间件时恢复旧 body 读取和 dispatch 队列路径。
- 速度优先可通过将路由策略切回 `cost_first` 停止新切号路径。
- 在途预占是进程内易失状态，回滚/重启自动清空，不需要数据清理。
