# PLAN-20260822T140000000Z：J3a 代理延迟检测 Node↔Go 深度对照与补缺

| 项目 | 内容 |
| --- | --- |
| 状态 | 深度对照已完成；三项跨运行时阻断待后续独立阶段 |
| 创建时间 | 2026-08-22T14:00:00.000Z |
| 需求来源 | J3a 完整迁移契约、后台任务迁移路线图、Node/Go 独立审计与 dev PG smoke |
| 负责人 | `/root` |
| 关联报告 | [Node↔Go 深度对照报告](../reports/J3a代理延迟检测Node-Go深度对照报告-2026-08-22.md) |
| 关联契约 | [J3a 代理延迟检测完整迁移契约](../migration/J3a-代理延迟检测完整迁移契约.md) |

## 1. 目标与边界

目标是防止 Node 机制在迁移接线时遗忘：把候选、代理、wire、限额、结果、CAS、调度、健康和手动入口逐项映射到 Go，并把不能在当前范围实现的 projector、owner handoff、manual bridge 固化为后续门禁。矩阵中的“通过”仅代表 Node 事实或 Go local-pass；没有同一 fixture runtime 输出的条目必须标记 `cross-runtime-unverified`。

本计划不修改 Node，不启用 Go owner，不停止 Node scheduler，不做双写，不访问生产；dev PG smoke 只作为 jobs/reader 基础域证据。

## 2. 执行拆解

1. [x] 读取 Node proxy-test、proxy repository、routes、background scheduler 和 regression 事实源。
2. [x] 读取 Go direct reader、Store、transport、executor、runner、main health 与现有测试。
3. [x] 形成 Node→Go 矩阵，区分通过、确定差异、未验证、非目标和阻断。
4. [x] 将三项阻断写入报告、契约和总迁移路线：outcome projector、scheduler drain/owner handoff、manual bridge。
5. [x] 明确数值默认值、provider target order、IPv6/SOCKS/407/502/TLS/slow-drip golden evidence 缺口。
6. [ ] 另立 Node→Go→Node projector 闭环实施任务，并在同 fixture 上验证 cursor/receipt/CAS/replay/restart。
7. [ ] 另立 scheduler drain/owner handoff 任务，完成 active-path-zero、旧 owner manifest、回滚和 owner gate 证据。
8. [ ] 另立 manual bridge 任务，覆盖 404、503/Retry-After、200、502、25 秒、取消和 outbound readback。
9. [ ] 另立 cross-runtime golden matrix 任务，冻结数值、排序、IPv6 authority、SOCKS wire 和异常线级行为。
10. [x] 补齐 Node 周期异常 `unknown` 写回与 Go receipt-only/no-committed-outcome 的差异决策，禁止 projector 默认为 unknown。
11. [x] 补齐 Node `ProxyTestReport` 字段、provider display name、synthetic base item、outbound fallback 顺序和手动 404/503/200/502/CAS stale/no-provider/slot/auth/logging 边界。
12. [x] 补齐 Node scheduler 的 stable phase、resource lane、ops-worker、timeout 占用、stopAndDrain、global concurrency、candidate pool/deadline/lease grace 事实，并标记 Go serial runtime 的非等价项。
13. [x] 明确六个文档索引入口和 exact staging allowlist；路线图、transport.go/transport_test.go 及其他 dirty worktree 不得随本计划提交。

## 3. 测试与验收项

| 类型 | 测试项 | 当前状态 |
| --- | --- | --- |
| Node 事实 | proxy URL、credential、DNS、headers、limits、result、CAS、scheduler、manual route 源码取证 | 已完成 |
| Go 基础域 | unit/race/vet、transport capture、Store fence/replay、health snapshot | 已完成 |
| dev 基础域 | scratch PostgreSQL/PgBouncer `6432` required smoke、权限/事务/cleanup | 已完成；不等于跨运行时闭环 |
| 跨运行时 | Node input→Go outcome→Node readback/projector | 未完成，阻断 |
| owner | Node drain、active-path-zero、Go owner handoff、回滚 | 未完成，阻断 |
| 手动 | Node route 与 Go bridge 同步语义 | 未完成，阻断 |
| golden | 同 fixture Node/Go wire、数值、排序和异常矩阵 | 未完成，`cross-runtime-unverified` |

| 语义差异 | Node 周期异常 unknown 写回 vs Go receipt-only；Node ProxyTestReport vs Go Outcome；scheduler lane/stopAndDrain | 已冻结差异；projector/bridge/handoff 前必须单独验收 |

## 4. 进度与验证记录

- 2026-08-22：Node oracle、Go parity、契约覆盖三条只读审计完成；结论确认基础探测域对齐，但完整迁移仍有三项阻断。
- 2026-08-22：新增深度对照报告并同步 J3a 契约/总路线；owner gate 继续关闭。
- 2026-08-22：未执行 Node/Go 跨服务写入、生产部署、owner 切换或真实外部闭环；后续任务必须单独申请范围和写锁。
- 2026-08-22：对账既有 dev PgBouncer `6432` required smoke 已通过并清理；本轮深度对照未重跑外部服务，Docker/Redis/projector/owner handoff/manual bridge 仍未完成。

## 5. 关闭条件

本计划只有在第 2 节第 6–9 项分别完成并通过独立质量门后，才可标记为“完整迁移对照关闭”。当前仅关闭取证与文档固化单元；不得以本计划或 dev smoke 结果宣称 J3a 已切换。

## 6. 主分支提交边界

本计划允许暂存的路径只有：本深度报告、本计划、J3a 完整迁移契约、`docs/reports/README.md`、`docs/plans/README.md`、`docs/migration/README.md`。`docs/migration/后台任务迁移总设计与路线图.md`、`backend-go/projects/jobs/internal/proxylatency/transport.go`、`transport_test.go` 与其他工作树改动属于其他任务或未归属变更，必须保持未暂存。提交前以 `git diff --cached --name-only` 验证，不得使用 `git add .`。
