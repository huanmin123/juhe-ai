# PLAN-20260728T003942514Z 网关 Body 解析性能治理

## 状态

已完成：Node 全链路 Body 解析治理按一次性交付收口，不再保留后续阶段。入口按需解析、响应 JSON/SSE、账号准备、账户诊断、模型检测、OAuth、Hybrid 和日志链路均已完成实现、专项回归和边界复查。

设计依据：[网关 Body 解析性能治理设计](../functions/网关Body解析性能治理设计.md)。

## 目标

- 找出网关全链路所有原始请求/响应 Body JSON 与 SSE 解析点。
- 删除只服务日志和审计展示的解析。
- 将同一请求、响应和 SSE event 的必要解析收敛为一次并复用。
- 保持协议转换、usage、错误语义、响应检查和容量保护不变。

## 任务

- [x] 完成入站请求 Body 接收、元数据扫描和完整解析清单。
- [x] 完成混合路由、账号适配和协议 bridge 解析清单。
- [x] 完成非流式响应、SSE、usage 和错误解析清单。
- [x] 完成审计、使用记录、运行日志和诊断解析清单。
- [x] 删除审计超限摘要的原始 Body 结构解析。
- [x] 增加 request-scoped JSON 完整解析复用，并迁移所有原始请求调用点。
- [x] 删除 Anthropic driver 重复规范化、Codex Body hash、limited 探针展示解析和模型检测重复 fallback。
- [x] 取消普通网关小 JSON 无条件完整解析，原生 passthrough 只执行严格语法和必要元数据扫描。
- [x] 实施 attempt-scoped 非流式成功/错误 JSON 解析复用。
- [x] 统一 SSE event decoder，让响应策略、协议 inspector、usage、账户诊断和模型检测共享解析事件。
- [x] 收敛账户诊断、模型检测、OAuth、请求覆盖、Hybrid 和 Anthropic preparation 的重复解析。
- [x] 删除审计/运行日志纯展示解析并收敛重复编码。
- [x] 完成全部专项回归、性能边界复查和独立复查；已执行全仓类型检查和构建，当前仅被本计划范围外的 OAuth Header 元组类型工作区改动阻断。

## 验证结果

- 原生 API Key raw passthrough 完整物化 0 次；按需消费者跨模块为 1 次。
- 同一成功/错误非流式正文最多完整解析 1 次；generic opaque 无策略错误正文为 0 次。
- 同一 SSE event 在响应策略、协议 inspector、Codex guard 和诊断链之间只解码 1 次。
- 图片探针通过有界 envelope scanner 验证成功/错误，不物化 base64。
- OAuth 大 Body 跨等价账号重试复用解析与规范化 worker Promise；请求覆盖错误保留账号级切换语义。
- 请求级物化结果绑定回原始 Buffer，后续账户覆盖和无 Request 参数的构建器不会再次解析。
- 非 2xx attempt 携带 parsed response，错误策略与最终诊断错误复用同一次解析。
- 审计预处理结果不被 IPC 二次裁剪，4 MiB envelope 与原有保全降级继续生效。
- 入口、请求构建、非流式、SSE、协议边界、诊断、模型检测、审计、公开日志和操作日志专项回归通过。
- 全仓 `typecheck`/`build` 已重新执行；当前只报 Anthropic、Grok、OpenAI OAuth 服务中本计划范围外的 `HeadersInit` 元组类型错误。本计划相关 TypeScript 文件未出现在错误列表中。

## 不做

- 不删除协议转换实际依赖的结构化解析。
- 不通过正则猜测完整协议语义替代正确解析。
- 不跨请求缓存 Body 对象。
- 不在缺少实证时改变计费和客户端错误语义。
