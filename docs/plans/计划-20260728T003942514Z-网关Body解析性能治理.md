# PLAN-20260728T003942514Z 网关 Body 解析性能治理

## 状态

进行中：按一次性交付完成 Node 全链路 Body 解析治理，不再保留后续阶段。请求侧已有解析复用继续保留，本计划同时完成入口按需解析、响应 JSON/SSE、账号准备、账户诊断、模型检测、OAuth 和日志链路的剩余优化。

设计依据：[网关 Body 解析性能治理设计](../functions/网关Body解析性能治理设计.md)。

## 目标

- 找出网关全链路所有原始请求/响应 Body JSON 与 SSE 解析点。
- 删除只服务日志和审计展示的解析。
- 将同一请求、响应和 SSE event 的必要解析收敛为一次并复用。
- 保持协议转换、usage、错误语义、响应检查和容量保护不变。

## 任务

- [x] 完成入站请求 Body 接收、元数据扫描和完整解析清单。
- [x] 完成混合路由、账号适配和协议 bridge 解析清单。
- [x] 完成非流式响应、SSE、usage 和错误解析清单。
- [x] 完成审计、使用记录、运行日志和诊断解析清单。
- [x] 删除审计超限摘要的原始 Body 结构解析。
- [x] 增加 request-scoped JSON 完整解析复用，并迁移所有原始请求调用点。
- [x] 删除 Anthropic driver 重复规范化、Codex Body hash、limited 探针展示解析和模型检测重复 fallback。
- [ ] 取消普通网关小 JSON 无条件完整解析，原生 passthrough 只扫描必要顶层元数据。
- [ ] 实施 attempt-scoped 非流式成功/错误 JSON 解析复用。
- [ ] 统一 SSE event decoder，让响应策略、协议 inspector 和 usage 共享解析事件。
- [ ] 收敛账户诊断、模型检测、OAuth、请求覆盖和 Anthropic preparation 的重复解析。
- [ ] 删除审计/运行日志纯展示解析并收敛重复编码。
- [ ] 完成全部专项回归、类型检查、性能边界复查和独立复查。

## 不做

- 不删除协议转换实际依赖的结构化解析。
- 不通过正则猜测完整协议语义替代正确解析。
- 不跨请求缓存 Body 对象。
- 不在缺少实证时改变计费和客户端错误语义。
