# PLAN-20260728T183036249Z Node 提交审查问题修复与网关验收

## 状态

已完成（未部署）

设计依据：[Node 提交审查问题修复与网关验收设计](../superpowers/specs/2026-07-28-Node提交审查问题修复与网关验收设计.md)。

## 目标

- 修复今天 Node/Vue 提交审查确认的 9 个问题。
- 复查字段级 PATCH、并发锁、缓存代际、OAuth 锁和 Codex 大正文边界。
- 完成网关核心链路测试与隔离本地启动、页面访问验收。

## 实施清单

- [x] 补齐分组 PATCH helper 并通过类型检查。
- [x] 修复账户表单完整供应商定义初始化及异步详情回填。
- [x] 消除 Codex 大正文主线程重复解析和切号时重复规范化。
- [x] 修复 OpenAI OAuth 滚动发布锁兼容与取消计数。
- [x] 修复策略路由锁前快照并发问题。
- [x] 修复过期账户显式激活及时间计划派生恢复边界。
- [x] 修复 API Key 缓存代际耗尽错误语义。
- [x] 将分组编辑纯读接入 SQLite read worker。
- [x] 完成定向、类型、构建、网关核心链路与 Browser 验收。
- [x] 完成代码与文档复查。

## 验收记录

- `pnpm typecheck` 与 `pnpm build` 通过；构建仅保留既有的大 chunk 提示。
- 网关鉴权、候选路由、HTTP/流式响应、Codex 协议与切号、缓存失效共 19 项核心回归通过，最终 `test:smoke:mock` 通过。
- Codex 2MB+ 双账户真实 attempt、账户过期激活/时间计划恢复、供应商详情异步默认值专项回归通过。
- 使用独立临时数据库和新端口启动 Node 后端及 Vue 前端；健康、页面和自动登录接口均返回 200。
- Browser 实测进入账户页、打开新增表单并输入账户名，权威 Base URL 为 `https://api.openai.com/v1`，控制台无 error/warn。

## 完成标准

- 设计中的 9 项问题全部闭合，且无未解释的类型、构建或核心网关测试失败。
- 隔离本地实例可启动，管理页面可访问并完成至少一次真实交互。
- 未触碰 Go，未覆盖或回退用户已有工作区修改。
