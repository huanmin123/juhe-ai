# PLAN-0166-20260723T233206838Z 运行日志降噪与终态语义修复

## 基本信息

- 计划 ID：PLAN-0166-20260723T233206838Z
- 状态：已完成
- 创建时间：2026-07-24
- 需求来源：用户对话
- 关联模块：Node 后端 / DB service / 网关 / 运行日志索引 / Vue 日志搜索 / 文档 / 验证
- 关联设计：`docs/functions/运行日志降噪与终态语义设计.md`
- 关联问题：`BUG-0027`、`BUG-0124`

## 目标

- [x] DB service 正常快速请求不再产生 `info` 开始/完成日志。
- [x] 网关正常快速阶段不再逐条写 `info`，但最终 timing summary 保持完整。
- [x] 成功协议终止后的连接关闭不再被记为 `aborted`。
- [x] 日志搜索正确映射点号事件，并显示有意义的慢阶段 / 慢操作消息。
- [x] 回归、类型检查、构建、日志量对比和页面渲染验证通过。

## 实施边界

- 不删除错误、重试、预期失败、中断或慢请求证据。
- 不删除 timing summary 内的有界阶段数组。
- 不为后台任务生成伪造 trace；沿用现有 `jobId` / `operationId`。
- 不清理或改写历史日志；新策略只影响发布后的新日志。
- 不执行生产部署。

## 执行清单

- [x] 只读分析本地 SQLite 与日志文件，建立事件量和耗时基线。
- [x] 核对 DB service、请求上下文、流式终态和前端映射根因。
- [x] 建立设计、计划和 bug 记录。
- [x] 实现日志级别、慢阈值和终态修复。
- [x] 更新前端事件映射和展示。
- [x] 补齐自动化与隔离页面验证。
- [x] 完成复查和验证记录。

## 验收标准

- [x] 快速 DB service 成功请求默认零条 `info`；慢请求一条 `info`；失败一条 `error`。
- [x] 70 个快速网关阶段在 `info` 探针中不产生 70 条阶段日志，只保留一次汇总。
- [x] 慢阶段和三类问题阶段均有单条、正确级别证据。
- [x] 成功协议终止后 close 的 timing summary 为 `success` 且无关闭告警。
- [x] 前端不再把目标点号事件显示为未映射。
- [x] 定向回归、前后端类型检查、构建、`git diff --check` 和页面验证通过。

## 验证记录

- `pnpm --filter juhe-ai-backend test:log-event-contract`：通过；70 个正常阶段只保留 1 个构造的慢阶段 info，summary 的 `stageCount=70`，成功终态 close 为 success，真实提前 close 为 aborted，失败终态 close 保持失败。
- `pnpm --filter juhe-ai-backend test:gateway-response-lifecycle`、`test:response-inspection-gateway-e2e`：通过。
- `pnpm --filter juhe-ai-frontend test:runtime-log-formatters`：通过。
- 后端、前端 typecheck 和 production build：通过；前端仅有既有大 chunk 告警。
- 隔离 standalone 实例使用全新业务库、聊天库、数据集库、usage catalog、统计库、shard / runtime / assets / logs 目录；默认 info 运行 6 秒期间 DB service 日志只有 1 条启动事件，`db_service.request.start=0`、`db_service.request.complete=0`、error=0。
- 固定 trace 的 401 网关请求只产生 1 条问题阶段和 1 条 timing summary；阶段消息为 `请求阶段预期失败：runtime_resolution，4.6ms`，汇总消息带 `7.5ms`、attempt 数和 outcome。
- Playwright 回退验证桌面 1440x900 与移动 390x844：自动登录生效，事件中文映射和动态消息可见，无未映射事件、控制台 / page error、遮罩残留或文档横向溢出。截图保存在系统临时验证目录，不进入仓库。
- 旧 `test:runtime-display-contract` 仍因当前工作区 `SystemMetricsStatsView.vue` 已不符合其旧 `Promise.all` 源码断言而失败；本次未改动该页面，日志映射已拆为独立专项回归。

## 完成总结

- 完成时间：2026-07-24。
- 默认 info 不再记录 DB service 开始 / 快完成或网关快速成功阶段；错误、问题阶段、慢请求和单次耗时汇总继续保留。
- 协议终态与 HTTP 连接终态已解耦，成功和失败协议终态都不会被 close 伪装成客户端中断。
- 日志搜索已识别目标点号事件，并直接显示操作 / 阶段 / 耗时摘要。
