# PLAN-0149 审计与运行日志索引临时关闭

## 基本信息

- 状态：进行中
- 创建时间：2026-07-22
- 需求来源：生产 Node 旁路数据处理放大，用户要求在 Go 迁移前临时关闭原始审计和日志搜索数据库索引
- 执行者：Codex

## 需求目标

- 用部署环境变量临时整体关闭原始审计的新捕获、传输、队列和持久化。
- 临时关闭普通运行日志文件到数据库搜索索引的导入，同时保留正常日志文件、轮转和 grep。
- 功能代码、schema、管理页面、查询接口和重新启用能力保留。
- 发布稳定后清空生产审计数据和运行日志数据库索引数据。

## 范围边界

本次不修改、不关闭、不清理 `usage_records`，也不影响统计、额度、成本和账户质量。操作日志、公开 API 日志、模型检测、网关路由、重试和账号状态同样不在范围内。本轮不改 Pino JSON 文件格式。

## 执行清单

- [x] 完成合并设计和实施计划。
- [ ] 实现两个默认启用、严格解析的部署开关。
- [ ] 完成审计关闭态零工作回归。
- [ ] 完成运行日志索引关闭且文件继续写入回归。
- [ ] 完成 runtime disabled 契约和历史读取回归。
- [ ] 完成配置示例、Docker 和权威文档同步。
- [ ] 完成本地全量验证与双阶段代码审查。
- [ ] 获取部署计划锁并 temporary-first 发布。
- [ ] 验证审计/索引不增长且使用记录继续增长。
- [ ] 清空生产审计三介质和五张运行日志索引表。
- [ ] 完成稳定观察、记录结论并释放部署锁。

## 验收标准

- 生产 `JUHE_AI_AUDIT_LOG_ENABLED=false` 与 `JUHE_AI_RUNTIME_LOG_INDEX_ENABLED=false` 生效。
- 新请求不新增审计或 `runtime_logs`，日志文件和 `usage_records` 继续增长。
- app、DB service、公网 health 和 `1+1+3` worker 正常。
- 清理目标全部为 0，使用记录及其他日志数据未被触碰。
