# PLAN-0022 网关性能综合测试

## 基本信息

- 编号：PLAN-0022
- 状态：已完成
- 创建时间：2026-05-17
- 更新时间：2026-05-17
- 需求来源：用户对话
- 执行者：AI
- 关联模块：后端 / 网关 / 使用记录 / 审计日志 / 文档 / 验证

## 需求目标

- 背景：需要通过模拟测试评估项目可支持的 QPS、延迟、错误率、资源占用等性能指标，形成可重复执行的本地压测入口。
- 目标：新增不消耗真实 OpenAI 额度、不污染本地业务库的性能综合测试脚本，覆盖 OpenAI 兼容网关主要请求路径。
- 交付物：性能测试脚本、`pnpm test:perf` 命令、验证文档、执行结果记录。

## 范围边界

### 本次包含

- [x] 使用临时 SQLite 业务库和记录库，避免影响 `backend/data/`。
- [x] 启动本机模拟 OpenAI 上游，支持 `/v1/models`、`/v1/responses`、`/v1/chat/completions` 和流式 responses。
- [x] 自动种子压测分组、账户和本地网关 API Key。
- [x] 通过真实 `/v1` 网关路由压测鉴权缓存、账号调度、上游转发、使用记录队列和审计采样队列。
- [x] 输出 QPS、成功 QPS、错误率、延迟分位、事件循环延迟、CPU、内存、使用记录和审计日志增量。
- [x] 支持环境变量调整场景、并发、时长、上游延迟、响应体大小和报告路径。

### 本次不包含

- 真实 OpenAI 上游压测：避免消耗真实额度，也避免把网络和上游限流混入本机网关容量判断。
- 生产多进程拓扑压测：脚本以本机临时库直接运行网关模块，不启动独立 DB service / worker 子进程；生产容量需在部署实例上补测。
- 前端页面性能测试：本计划聚焦后端 OpenAI 兼容网关。

## 关联文档

- 功能开发指导：`docs/architecture/功能开发指导.md`
- 运行手册：`docs/develop/运行说明.md`
- 验证手册：`docs/develop/测试与验证说明.md`
- 网关相关设计：`docs/functions/核心功能设计.md`
- 测试报告：`docs/plans/网关性能综合测试报告-2026-05-17.md`
- 极限测试报告：`docs/plans/网关极限性能测试报告-2026-05-17.md`
- 极限测试原始 JSON：`reports/perf-limit-report-2026-05-17.json`

## 方案概述

- 方案原则：压测走真实网关 HTTP 路由，但上游和数据库都用临时资源，保证可重复、安全、低成本。
- 数据变化：无生产 schema 变更；脚本运行时创建临时 SQLite 文件，结束后删除。
- 接口变化：无新增业务接口。
- 后端变化：新增 `backend/src/scripts/performance/gateway-performance-test.ts`，并增加 `pnpm test:perf` 命令。
- 数据处理策略：测试产生的使用记录和审计日志只写入临时记录库；脚本输出聚合指标和可选 JSON 报告。

## 执行拆解

- [x] 确认网关压测入口和临时库种子方式
- [x] 新增模拟性能压测脚本
- [x] 新增根目录和后端 workspace 的 `test:perf` 命令
- [x] 补充验证文档
- [x] 执行综合压测并记录结果
- [x] 完成计划验证记录和总结

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 后端类型检查 | `pnpm --filter juhe-ai-backend typecheck` | TypeScript 类型检查通过 | 已通过 | 2026-05-17 执行通过 |
| 功能主流程 | 单场景小规模压测 | `JUHE_AI_PERF_SCENARIOS=responses` 等环境变量 + `pnpm --filter juhe-ai-backend test:perf` | 脚本能启动临时网关和模拟上游，输出 QPS 和延迟指标 | 已通过 | 并发 2、1 秒 responses 场景通过 |
| 功能主流程 | 默认综合压测 | `pnpm test:perf` | 默认多场景、多并发档位跑完并输出汇总 | 已通过 | 2026-05-17 执行通过，默认 9 个场景档位均 0 错误 |
| 功能主流程 | 极限高并发压测 | 设置 `JUHE_AI_PERF_CONCURRENCY=50,100,200,400,800` 后执行 `pnpm test:perf` | 输出极限 QPS、延迟退化点和原始 JSON 报告 | 已通过 | 2026-05-17 执行通过；业务转发链路稳定 QPS 约 350-370，所有档位错误率 0 |
| 异常与边界 | 模拟上游错误率 | 设置 `JUHE_AI_PERF_UPSTREAM_ERROR_RATE` 后执行 | 错误率、状态码和错误计数能反映上游失败 | 未执行 | 可后续专项跑 |
| 回归场景 | 使用记录和审计队列 | 压测结束检查输出中的 queue 长度 | 使用记录队列和审计队列最终清空 | 已通过 | 小规模压测中两类队列均为 0 |
| 未覆盖说明 | 生产部署容量 | 在目标机器对正式进程拓扑补测 | 形成真实部署容量结论 | 不适用 | 本计划先提供本机模拟压测工具 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-05-17 | 进行中 | AI | 创建计划，确认使用临时库 + 模拟上游 + 真实网关路由的方式做性能综合测试。 |
| 2026-05-17 | 进行中 | AI | 已新增性能脚本和 `test:perf` 命令，小规模 responses 压测跑通，后端类型检查通过。 |
| 2026-05-17 | 已完成 | AI | 默认 `pnpm test:perf` 跑通并输出多场景 QPS / 延迟指标；文档和计划已同步。 |
| 2026-05-17 | 已完成 | AI | 补充独立测试报告 `docs/plans/网关性能综合测试报告-2026-05-17.md`，沉淀测试环境、配置、结果表和容量参考。 |
| 2026-05-17 | 已完成 | AI | 复跑极限高并发压测，生成 `reports/perf-limit-report-2026-05-17.json`，并补充 `docs/plans/网关极限性能测试报告-2026-05-17.md`。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-05-17 | 默认使用模拟 OpenAI 上游 | 压测目标是网关本身，不应消耗真实额度或受真实上游限流影响 | 输出结果更适合作为本机链路容量参考 |
| 2026-05-17 | 临时库运行，结束即清理 | 避免污染本地真实账户、API Key、使用记录和审计数据 | 可放心多次重复执行 |
| 2026-05-17 | 输出稳定容量参考而非绝对承诺 | 本机脚本不等同生产多进程和真实网络拓扑 | 文档明确需要生产环境补测 |

## 验收标准

- [x] 提供一条可直接执行的性能综合测试命令。
- [x] 默认测试不依赖真实 OpenAI 凭据，不写入本地真实业务库。
- [x] 输出包含 QPS、成功 QPS、错误率、延迟分位、事件循环延迟、CPU、内存和记录库增量。
- [x] 文档说明可调环境变量和结果解读边界。
- [x] 默认综合压测执行完成，并记录结果。

## 验证记录

- 类型检查：已执行，命令：`pnpm --filter juhe-ai-backend typecheck`，结果通过。
- 单项验证：已执行，命令：小规模 `pnpm --filter juhe-ai-backend test:perf`，结果通过，responses 场景并发 2 下成功 QPS 133.26、错误率 0、p95 25.739ms。
- 综合验证：已执行，命令：`pnpm test:perf`，默认 3 个场景 x 3 个并发档位全部通过；最高成功 QPS 为 `models` 场景并发 25 下 641.69，`responses` 场景最高成功 QPS 为并发 50 下 441.45，`chat` 场景最高成功 QPS 为并发 50 下 482.84；所有档位错误率均为 0。
- 极限验证：已执行，命令：设置 `JUHE_AI_PERF_SCENARIOS=models,responses,chat`、`JUHE_AI_PERF_CONCURRENCY=50,100,200,400,800`、`JUHE_AI_PERF_DURATION_SECONDS=12`、`JUHE_AI_PERF_WARMUP_SECONDS=3`、`JUHE_AI_PERF_REPORT_PATH=reports/perf-limit-report-2026-05-17.json` 后运行 `pnpm test:perf`；结果通过并成功写入 JSON 报告。按 p95 不超过 1000ms 和错误率不高于 1% 计算，`models` 稳定成功 QPS 为 511.60，`responses` 稳定成功 QPS 为 368.90，`chat` 稳定成功 QPS 为 358.96；全部档位错误率均为 0。
- 全量类型检查：首次并行执行 `pnpm typecheck` 时命中工作区既有 `resource-authorization-helpers` 拆分导出瞬态错误；随后 `pnpm --filter juhe-ai-backend typecheck` 已通过，最终全量复跑结果见本计划外部验证记录。
- 未验证项：生产部署实例、多进程 DB service / worker 拓扑、真实 OpenAI 网络和上游限流表现。

## 风险与注意事项

- 本机模拟结果受 CPU、磁盘、Node 版本、SQLite WAL 状态和开发机负载影响，不能直接等同生产容量。
- 默认脚本压测的是本进程临时网关，覆盖网关主要逻辑，但不覆盖主进程、DB service、worker 三进程间 IPC 的真实部署开销。
- 上游响应越慢，测试结果越接近“并发等待能力”；上游响应越快，结果越接近“本机网关和 SQLite 写入能力”。
- 回滚建议：如脚本影响开发体验，可先移除 package 命令；脚本不改运行时代码和生产 schema。

## 完成总结

- 完成时间：2026-05-17
- 实际完成内容：新增模拟 OpenAI 上游 + 临时 SQLite + 真实网关路由的性能综合测试脚本，支持多场景、多并发、可调上游延迟和响应体大小，输出 QPS、延迟分位、错误率、事件循环延迟、CPU、内存、队列和记录库增量指标。
- 主要改动位置：`backend/src/scripts/performance/gateway-performance-test.ts`、`backend/package.json`、`package.json`、`docs/develop/测试与验证说明.md`、`docs/plans/README.md`、`docs/plans/网关性能综合测试报告-2026-05-17.md`、`docs/plans/网关极限性能测试报告-2026-05-17.md`、`reports/perf-limit-report-2026-05-17.json`。
- 验证结果：小规模 `responses` 压测通过；默认 `pnpm test:perf` 通过；`pnpm --filter juhe-ai-backend typecheck` 通过。
- 后续建议：在正式部署机器上增加一次目标拓扑压测，尤其补测独立 DB service / worker IPC、真实磁盘、代理链路和真实上游耗时下的容量。
