# PLAN-20260726T030356119Z 模型质量定时检查与处罚恢复

## 基本信息

- 计划 ID：`PLAN-20260726T030356119Z`
- 状态：进行中
- 创建时间：2026-07-26
- 需求来源：用户提出模型检测配置、定时检查、处罚阈值与方式、质量隔离、自动恢复及健康监控强制不可用联动
- 执行者：Codex / 项目维护者

## 需求目标

- 在模型检测页提供集中配置和定时检查管理。
- 手工检测处罚开关默认开启，使手工快速 / 深度检测可以按同一策略处罚自有账户；关闭后不修改账户，但有效质量失败仍保留日志并同步健康事实。
- 手工、定时和质量恢复统一进入外部可见模型质量测试日志，并将每账户历史硬限制为 1000 条；列表按账户筛选后普通分页，按新数据在前稳定排序。
- 用可审计、可恢复、并发安全的状态机执行质量处罚。
- 让模型质量失败在健康监控当前小时稳定显示不可用，不被普通健康检查覆盖。
- 增加 `quality_isolated` 账户状态及 `ops-worker` 质量恢复闭环。

## 范围边界

### 本次包含

- Vue 模型检测工具栏、配置浮窗、定时检查浮窗和账户状态展示。
- Node 当前 owner 下的策略、计划、处罚、恢复、统计联动、API 和存储实现。
- Node 当前支持的 SQLite 与 PostgreSQL schema 同步。
- 只交付当前 Node 运行链路，不并行实现其他语言版本或迁移适配层。
- 健康监控查询、详情和 Mockdata / 回归同步。
- 操作日志、运行日志、缓存失效和状态筛选同步。

### 本次不包含

- 使用普通业务请求正文训练新的模型真假分类器。
- 被授权人对来源账户设置定时处罚。
- 秒、小时、Cron 表达式或任意时区日历计划。
- 在 HTTP server 中运行定时器，或创建 Node 之外的第二套调度、执行器和双 owner 逻辑。
- 保留旧 schema 的运行时双读、双写或启动迁移兼容分支。

## 关联文档

- [模型质量定时检查与处罚恢复设计](../functions/模型质量定时检查与处罚恢复设计.md)
- [模型检测设计](../functions/模型检测设计.md)
- [AI 健康监控设计](../functions/AI健康监控设计.md)
- [AI 账户错误语义与状态变更边界](../functions/AI账户错误语义与状态变更边界.md)
- [AI 账户运行态探针恢复设计](../functions/AI账户运行态探针恢复设计.md)
- [后台任务使用说明](../architecture/backend/后台任务使用说明.md)

## 方案概述

- 前端：配置图标浮窗保存系统账户级策略；定时检查浮窗维护自有账户计划；深度开关离开主工具栏；快速检查右侧增加定时检查。
- 后端：业务库保存策略、计划和处罚决策；模型检测 run 保存触发来源和不可变策略快照；可处罚运行结束后通过幂等决策应用账户现有降级备用配置或账户状态。
- 健康监控：新增独立质量失败小时表，查询时质量失败优先，彻底消除普通健康写入覆盖风险。
- 状态：新增 `quality_isolated`，恢复任务只允许带匹配 generation 的 CAS 转移。
- 后台：定时检查和质量恢复都归 `ops-worker`，使用批次、租约、去重和单账户互斥。

## 执行拆解

### 阶段 0：契约与冲突审计

- [~] 固定 `triggerKind`、策略字段、处罚枚举、`quality_isolated` 和有效可用性枚举。
- [~] 列出所有 AccountStatus 解析、SQL CHECK、列表筛选、分组统计、导入导出和前端 formatter 落点。
- [~] 列出所有可能把 `disabled/quality_isolated` 恢复为 active 的路径，逐个建立禁止覆盖门禁。
- [~] 确认当前 Node 模型检测 run、SSE、dataset writer 和 `ops-worker` 的字段扩展顺序。

### 阶段 1：当前 schema 与存储

- [ ] 增加 `model_quality_policies`、`model_quality_schedules`、`account_quality_enforcements`。
- [ ] 增加 run 级 `model_quality_decisions` 权威处罚事实，覆盖策略快照、判定、实际动作、状态前后值、恢复和健康同步结果。
- [ ] 增加模型检测 run 触发来源和不可变策略快照。
- [ ] 增加 `account_quality_health_hourly` 及稳定索引、保留和账户删除清理。
- [ ] 增加 `quality_isolated` 当前 schema CHECK、repository mapper 和领域类型。
- [ ] 为 `model_check_runs` 增加触发方式、处罚摘要和 `(account_id, created_at DESC, id DESC)` 日志索引。
- [ ] 在统一 run 创建入口实现每账户 1000 条硬上限；账户已有 1000 条时，先确认该账户最老 100 条终态 run 的 observation 已被统计游标消费，再同事务淘汰并创建新 run；未消费时不突破上限并告警重试。
- [ ] 同步 Node 当前使用的 SQLite 完整 schema 与 PostgreSQL schema 所需变更，不生成其他语言的数据访问代码。

### 阶段 2：策略与计划 API

- [ ] 实现管理 / 用户策略读取与保存，后端收口默认值和整数边界。
- [ ] 增加默认开启的 `manualEnforcementEnabled`，并在手工运行策略快照、权限判断和页面风险提示中使用。
- [ ] 实现计划分页、创建、编辑、暂停、删除和 run-now。
- [ ] 强制管理端具体系统账户作用域、自有账户权限和授权账户排除。
- [ ] 增加 revision / generation 冲突响应和操作日志。

### 阶段 3：定时执行与处罚

- [ ] 在后台任务注册表登记定时质量检查和质量恢复任务。
- [ ] 实现到期计划有界 claim、租约、去重、漏跑单次补偿和单账户互斥。
- [ ] 复用模型检测执行器并写入 `triggerKind` 与策略快照，不复制探针实现。
- [ ] 实现硬失败、阈值和 unavailable 规则；快速 / 深度 profile 严格互斥并继承已保存配置，不自动升级、降级或补发第二种检查。
- [ ] “降级备用”直接复用现有账户更新 service 写 `accounts.fallback_enabled`，同步遵守与超级优先互斥，不新增备用字段或网关覆盖层。
- [ ] 实现三种处罚、配置版本校验、幂等决策和网关缓存失效。
- [ ] 在手工检测 SSE 中按顺序发送质量判定、处罚开始、处罚结果和健康同步事件，首次执行尝试完成后才发送检测终态。

### 阶段 4：质量隔离与恢复

- [ ] 在有效可用性、候选过滤、来源账户传播和账户展示中接入 `quality_isolated`。
- [ ] 实现恢复扫描、generation fencing、结果判断和下次恢复时间。
- [ ] 阻止健康检查、冷却复测、OAuth、时间计划和旧任务覆盖质量隔离。
- [ ] 增加“立即复检”和有审计的人工解除入口。

### 阶段 5：健康监控强制不可用

- [ ] 实现模型质量完成事实到质量小时表的稳定游标同步。
- [ ] 健康监控批量合并普通小时表和质量小时表，质量失败优先。
- [ ] 详情展示模型质量来源、分数、阈值和安全摘要。
- [ ] 补统计重建、保留期、账户删除清理和 Mockdata。

### 阶段 6：前端

- [ ] 调整 `ModelCheckRunPanel.vue` 工具栏和响应式布局。
- [ ] 新增检测配置浮窗、后端事实加载、提交错误和并发冲突提示。
- [ ] 新增定时检查浮窗、远程账户选择、分页列表、空态和操作状态。
- [ ] 模型检测历史列表统一展示手工、定时和恢复来源及处罚结果；支持账户筛选、普通手工翻页，并按 `created_at DESC, id DESC` 新数据优先排序。
- [ ] 手工检测终端展示后端真实处罚事件；定时 / 恢复详情新增完整“质量判定与处罚”卡片。
- [ ] 同步账户状态标签、筛选、tooltip、操作菜单和质量恢复时间。
- [ ] 使用隔离开发实例和 Browser 插件完成桌面 / 移动端视觉与交互验证。

### 阶段 7：文档与发布准备

- [ ] 更新模型检测、健康监控、账户错误边界、后台任务、接口矩阵和存储说明。
- [ ] 更新 Node 功能、接口、worker、存储和本地验证文档；不在本计划中维护其他语言迁移记录。
- [ ] 形成离线 schema 变更 / 重建步骤，不在运行时保留旧结构兼容分支。
- [ ] 准备功能开关或发布回退预案，确认关闭调度不删除策略、计划和处罚事实。

## 测试项

| 测试类型 | 测试项 | 验证方式 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 单元 | 阈值 69/70、硬失败、unavailable、manual 运行 | 后端单元测试 | 69 失败、70 通过；硬失败始终失败；unavailable 只写健康；manual 开关开时处罚、关时不处罚但有效失败仍写健康 | 未执行 | |
| 校验 | 处罚阈值配置边界 | 前后端校验测试 | 39、101、浮点和字符串拒绝；40、70、100 整数接受；默认值为 70 | 未执行 | |
| 权限 | 自有 / 授权账户手工处罚 | API / service 回归 | 自有账户按开关处罚；授权账户始终不反向处罚来源账户，质量事实归属被检测账户并遵守授权展示边界 | 未执行 | |
| 单元 | 时间字段只接受整数分钟 | 前后端校验测试 | 9、浮点、秒和带单位字符串拒绝；10 接受 | 未执行 | |
| 存储 | 计划 claim、run 决策和恢复幂等 | repository 回归 | 重放只产生一次处罚 / 恢复 | 未执行 | |
| 存储 | 单账户 1000 条模型质量日志上限 | SQLite / PG repository 回归 | 账户已有 1000 条时先淘汰该账户最老 100 条终态记录，再同事务创建新 run，提交后为 901 条 | 未执行 | |
| 存储 | observation 聚合游标门禁 | repository / stats-worker 回归 | 候选 observation 未消费时拒绝新建并告警重试，已消费或没有 observation 时允许淘汰；任何时刻不突破该账户 1000 条 | 未执行 | |
| 存储 | 日志淘汰并发、稳定顺序与级联 | 并发回归 / PG smoke | 并发创建不突破账户上限；running/new run 和其他账户日志不删除；items 等子记录无孤儿 | 未执行 | |
| 列表 | 手工、定时、恢复日志来源与账户分页 | API / 前端回归 | 所有来源均可见；按账户筛选后可手工翻完最多 1000 条，且新数据始终在前 | 未执行 | |
| SSE | 手工处罚终端事件顺序与失败语义 | 流式回归 / Browser | 判定、执行、健康同步、终态顺序稳定；applied / pending_retry / failed 文案真实 | 未执行 | |
| 详情 | 定时检查处罚决策完整性 | API / 前端回归 | 未触发和已处罚都展示策略快照、实际动作、前后值、恢复与健康联动 | 未执行 | |
| 一致性 | SSE、非流式响应、列表和详情处罚结果 | 契约回归 | 同一 run 的 `qualityDecision` 一致，页面不按当前账户状态反推历史 | 未执行 | |
| 并发 | 策略 revision、账户 config revision、处罚 generation 变化 | 并发回归 | 旧任务记录 stale，不覆盖新状态 | 未执行 | |
| 状态机 | 质量隔离与人工停用 / 异常 / 编辑竞态 | service 回归 | 自动恢复只恢复匹配代次的质量隔离 | 未执行 | |
| 网关 | 现有 fallback 配置与 quality_isolated 候选过滤 | 网关回归 | 处罚直接写账户现有降级备用；不新增排序分支；隔离账户不进入候选 | 未执行 | |
| 状态机 | 已是降级备用、超级优先和用户并发编辑 | service 回归 | 已有备用不重复写；按现有规则关闭超级优先；快速 / 深度不自动切换，不存在临时止损回滚 | 未执行 | |
| 授权 | 来源质量隔离传播、被授权人权限 | API / 网关回归 | 授权实例退出候选；被授权人不能建计划或处罚来源 | 未执行 | |
| 统计 | 同小时质量失败后普通健康成功 | stats repository 回归 | 健康监控仍为 failure，详情显示质量来源 | 未执行 | |
| 统计 | 下一个小时无质量失败 | stats repository 回归 | 新小时按普通健康结果显示，不永久污染 | 未执行 | |
| Worker | 重启、租约过期、漏跑和并发 claim | worker 回归 | 一次补跑、不并行、旧 owner 不能提交 | 未执行 | |
| API | 管理全部作用域、具体用户、普通用户、自有 / 授权账户 | HTTP 回归 | 权限和作用域符合设计 | 未执行 | |
| 前端 | 配置、计划列表、加载 / 空 / 错误 / 冲突态 | Browser 插件 | 交互清晰，中文完整，移动端不溢出 | 未执行 | |
| 类型 / 构建 | 前后端类型检查和构建 | 项目既有命令 | 全部通过 | 未执行 | |
| PG | 真实 PostgreSQL lease / unique / 状态写回 | PG smoke | 多实例 claim 和幂等约束成立 | 未执行 | |

## 决策记录

| 日期 | 决策 | 原因 |
| --- | --- | --- |
| 2026-07-26 | 手工检测处罚开关默认开启 | 用户明确要求手工模型检测默认参与处罚；关闭开关时才保持无状态诊断。 |
| 2026-07-26 | 所有模型质量运行复用模型检测历史作为外部测试日志，每账户硬上限 1000 条 | 避免复制公开接口日志；按最短 10 分钟间隔约保留 6.94 天，溢出批删 100 条预留空间。 |
| 2026-07-26 | 测试日志按账户筛选后使用普通分页并按新到旧排序 | 用户需要选择账户后手工翻页查看该账户最多 1000 条记录；移除全局 1001 行窗口和静默页码钳制。 |
| 2026-07-26 | 淘汰严格按账户执行并受 observation 聚合游标门禁 | 不删除其他账户日志，也不丢失尚未进入模型可信统计的证据；未消费时保持账户 1000 条硬上限并告警重试。 |
| 2026-07-26 | 手工处罚通过后端进度事件写终端，定时处罚通过 run 级权威决策写详情 | 防止前端按分数或当前账户状态猜测处罚结果，确保失败、重试和历史状态可审计。 |
| 2026-07-26 | 质量失败使用独立小时表，不改普通健康表最后写入规则 | 从存储结构上保证普通健康成功无法覆盖质量失败。 |
| 2026-07-26 | 授权账户不允许被被授权人加入自动计划 | 使用权不等于修改来源物理账户生产状态的权限。 |
| 2026-07-26 | 降级备用直接复用自有账户现有 `accounts.fallback_enabled` | 用户明确要求使用账户已有的降级备用能力，不创建质量专用覆盖或第二套路由机制。 |
| 2026-07-26 | 快速与深度 profile 严格互斥，默认快速且不自动补深度确认 | 用户明确快速检查不达标时无需再追加深度检查；只有显式开启深度开关后才使用深度 profile，也不自动降级回快速。 |

## 验收标准

- 功能设计第 10 节全部通过。
- 所有账户状态写入和恢复路径完成查漏补缺，没有普通后台任务能覆盖质量隔离。
- 健康监控覆盖竞态回归稳定复现“质量失败锁红”。
- API、探针和后台调度都由当前 Node owner 完成；仓库内不新增第二套语言实现、双写或双 owner。
- 类型检查、专项回归、PG smoke 和 Browser 视觉验收均有记录。

## 风险与回退预案

- 最大风险是误处罚和旧任务覆盖新状态；发布前必须用策略 revision、账户 config revision 和处罚 generation 三重 fencing 证明。
- 第二风险是检查任务与分钟级重试造成队列积压；必须限制批次、并发、单账户互斥并展示下次执行时间。深度检测默认关闭，仅在用户显式开启后使用。
- 第三风险是健康统计双事实不同步；使用稳定游标和可重建质量小时表，禁止 API 临时回扫。
- 回退时先关闭新计划 claim 和恢复 claim，等待已 claim 任务进入终态；保留策略、计划、处罚和质量小时事实。不得直接把所有隔离账户批量改为 active；需按 generation 输出待人工处理清单。

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-07-26 | 草稿 | Codex | 完成现状审计和可落地设计，等待用户确认后进入实现。 |
| 2026-07-26 | 进行中 | Codex | 用户确认开始实施；进入 Node 当前链路的存储、状态机、worker、API 与前端落地。 |
