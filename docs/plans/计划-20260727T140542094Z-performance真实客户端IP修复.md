# PLAN-20260727T140542094Z：performance 真实客户端 IP 修复

## 目标

修复生产 performance 双层 Nginx 把真实客户端 IP 折叠为 `127.0.0.1` 的问题，使新请求重新进入 IP 注册、统计和封禁链路，并保留可验证的回滚路径。

## 已确认事实

- 公网边缘通过 PROXY protocol v2 把真实来源交给 TLS 入口；TLS 入口已覆盖写入单值 `X-Real-IP` 和 `X-Forwarded-For`。
- 外层 Nginx 正确透传来源头；performance 槽位使用 `$proxy_add_x_forwarded_for` 追加本机回环地址。
- 生产 `JUHE_AI_TRUST_PROXY=1`，Express 因而选择转发链最右侧的 `127.0.0.1`。
- IP 聚合后台任务正常，页面与查询仓储没有丢数据；错误发生在请求来源采集之前。
- 当前非业务统计库在 2026-07-27 performance 切换时重建，切换前的历史 IP 不在当前库内。已写错的记录不删除、不猜测回填。

## 设计与执行清单

- [x] 审计边缘、TLS 入口、外层 Nginx、performance 槽位、Express 信任配置和数据库事实。
- [x] 将 performance 槽位四类路由改为原样透传可信来源头。
- [x] 添加静态回归，强制三个可信来源头在四类路由中各出现四次，并禁止旧写法。
- [x] 运行 macOS 运维回归、Shell 语法和差异检查。
- [x] 生产变更前复核 active release、入口、健康、锁和目标配置计数。
- [x] 备份目标配置，执行 `nginx -t` 后平滑 reload；失败立即恢复备份。
- [x] 通过公网请求、网关日志、新增用量和 IP 注册表验证非回环来源恢复。
- [x] 完成代码复查、问题记录和生产证据回填。

## 回滚预案

生产只修改当前 active performance 槽位配置。修改前保存带哈希的原文件；新配置语法检查失败时不 reload，reload 后健康或来源验证失败时恢复原文件、再次 `nginx -t` 并平滑 reload。Node、数据库、外层入口和边缘配置不在本次变更范围内。

## 完成标准

- 生产入口和所有 Node 实例持续健康，active release 与数据库服务身份不变。
- 新公网请求的网关日志和 `usage_records.client_ip` 均为真实 IPv4，不再是 `127.0.0.1`。
- `client_ip_registry` 出现新真实来源，统计任务继续成功推进。
- 源码生成器、回归门禁、设计文档和问题记录一致。

## 完成结果

- 2026-07-27 生产 active release 保持 `bf3398093`，应用和 DB service 均保持 `127.0.0.1:3399`，没有切换 release、重启 Node 或修改数据库。
- performance 槽位配置从 `6cdb4841...ece7` 更新为 `dfe4bfa0...6900`，三个可信来源头在四类路由中各四处；`nginx -t`、平滑 reload、本地双 health 和公网 health 全部通过。
- 原配置备份位于生产 `performance/forward-bf3398093/evidence/client-ip-hotfix-20260727T141540Z/`，远端互斥锁已清理。
- 公网 trace `01c4e668-afe6-41c2-866e-5122f56fb131` 在 `gateway-3` 日志中记录为非回环真实 IPv4；随后 `client_ip_registry` 已出现 4 个不同真实来源。
- `client_ip_stats_aggregation` 最后成功时间为 `2026-07-27T14:17:21.656Z`，无错误，观测 lag 为 15 秒。
- 本地 `pnpm test:macos-operations` 与目标文件 `git diff --check` 通过。Windows 环境按门禁设计跳过依赖 Darwin/Linux 的 launchctl 原生命令桩模拟。
