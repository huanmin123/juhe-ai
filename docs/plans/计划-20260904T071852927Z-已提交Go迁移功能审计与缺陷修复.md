# 已提交 Go 迁移功能审计与缺陷修复

## 基本信息

- 计划 ID：PLAN-20260904T071852927Z
- 状态：进行中
- 创建时间：2026-09-04 07:18:52 UTC
- 来源：维护者要求逐项审核 AI 已提交的 Node → Go 迁移，确认最终功能结果与 Node 一致；确认缺陷后才修复、复审并及时提交。
- 执行边界：当前工作区、当前 master；不创建分支或 worktree。

## 目标

对纳入范围的每个已提交迁移功能，以 Node 当前历史实现为行为参照，逐项证明 Go 的可观察结果是否一致：路由可达性、鉴权、输入校验、响应状态/DTO、持久化状态、缓存/运行态/日志等必要副作用，以及失败和并发边界。

审计不要求逐行或代码结构一致。Go 可以采用更合适的并发、抽象和复用方式，也不需要保留仅因 Node 单线程模型产生的低吞吐限制；前提是业务规则、接口结果和必要副作用不偏离。不得用 Node → Go → Node、跨进程回调或双 owner 作为兼容修复。

## 范围与冻结边界

### 纳入

- 从 af841ce7（含）开始，到 aed567991（M08）为止的已提交迁移功能及其相邻翻转提交。
- 审计单元：K1–K7、S-SQ、S-PG、M01、M03–M08。M02 没有独立迁移提交，记录为顺延依赖，不虚构审计结论。
- 迁移后的缺陷修复提交也需在对应问题关闭前接受定向复审；它们不是新的迁移范围。

### 明确排除

- 最新迁移 fbe850239（M09）和 08769dfe8（M09b），以及之后的新迁移提交；待维护者另行纳入后再审。
- 当前未提交的 backend-go/projects/gateway/internal/accounts/、internal/authz/ 与 internal/providers/ 相关改动。这些属于并行迁移工作，不读取、不暂存、不修改，也不作为本计划结论的证据。
- 新功能迁移、Node 代码重写、发布、生产切流、删除 Node 归档。发现这些是完成接管的前置条件时只记录为缺陷/门禁，不擅自扩大为迁移任务。

## 已知基线与审计状态

以下“已审”只表示已有第一轮证据和问题记录，不表示迁移功能已经验收或可切流。没有通过“逐项复核 → 定向测试 → 独立复审”的问题均保持待修复。

| 范围 | 第一轮结论 | 当前状态 | 关联记录 |
| --- | --- | --- | --- |
| K2 路由 owner | Node /auth、/system-accounts 已摘除而 Go 未接入唯一 gateway 入口 | 阻塞验收 | [BUG-0152](../bug/问题-0152-Go管理迁移未接入唯一入口导致K2路由失效.md) |
| K2 会话 | 已修复资料回执、Bearer 大小写、登出/存储错误、cookie、改密、revision、临时令牌失败处理、登录保护 username 维度和 profile effectiveRequestLimits 等 10 个已证实偏差；Redis CAPTCHA/登录保护仍待复核 | 进行中 | [BUG-0171](../bug/问题-0171-K2会话接口与多实例状态契约偏离.md) |
| K2 高权限与系统账户 | 临时令牌 allowlist/严格参数契约、管理 API `super_admin` 角色治理、options 查询及 PATCH 非 nullable 回执已修复并复审；已确认 `authsys` owner gate 缺失，默认资源、nullable、缓存和操作日志等仍待复核 | 待逐项复核 | [BUG-0169](../bug/问题-0169-K2认证高权限令牌与所有权门禁缺失.md)、[BUG-0170](../bug/问题-0170-K2系统账户创建与管理结果不等价.md) |
| K1、K3–K7 | HTTP 内核、限流、日志、失效总线、legacy bridge、mock 上游均已有可复现的候选偏差 | 待逐项复核 | [BUG-0154](../bug/问题-0154-K1-GoHTTP内核横切契约偏离.md)、BUG-0156–BUG-0160 |
| M01、M03–M08 | 公告、团队、授权、分组、路由、API Key、账户均存在端点、作用域、DTO、状态或副作用遗漏候选 | 待逐项复核 | [BUG-0153](../bug/问题-0153-M01公告迁移端点与数据契约偏离.md)、[BUG-0155](../bug/问题-0155-M04授权去重ProcessingTTL单位错误.md)、BUG-0161–BUG-0166 |
| S-SQ、S-PG | 初始化入口和默认 seed 的 fresh/upgrade 结果尚不能证明等价 | 待逐项复核 | [BUG-0167](../bug/问题-0167-S-SQSQLite初始化与Seed未接入.md)、[BUG-0168](../bug/问题-0168-S-PGSchema与默认Seed迁移不完整.md) |

## 固定工作法

每次只处理一个小功能切片或一个可验证缺陷，不因为同属一个大模块而一起改动。

1. 冻结证据：定位对应 Node 路由、服务、存储和测试，再定位 Go 路由、服务、store、调用方及已提交 diff；标明比较的历史提交。
2. 比较结果：按成功、失败、空值、权限、并发/幂等和必要副作用列出 Node 与 Go 的可观察结果。实现方式不同但结果等价时记录为“非缺陷”并说明依据。
3. 先记录：将确认的差异、触发条件、影响、Node/Go 证据和最小复现补入既有 BUG 文档；无法证明的线索不升级为缺陷。
4. 再修复：只修改该差异的最小 Go 范围，补充可回放的 Mock/单元或 HTTP 回归用例；不得同时夹带迁移、重构或未审计的并行文件。
5. 验证与复审：运行受影响 Go 包的定向测试、-race、必要的 go vet 与 git diff --check；由未参与写入的审阅者根据 Node 证据、diff 和测试独立复核。发现 blocker 则回到第 1 步。
6. 提交：只精确暂存本切片的代码、测试和问题记录，使用独立提交；提交后重新核对 git status --short，确保未带入并行迁移改动。

## 执行顺序

| 顺序 | 工作项 | 完成条件 | 状态 |
| --- | --- | --- | --- |
| A0 | 建立范围、问题索引和本计划 | 范围、排除项、已知问题、审计方法可追踪 | [x] |
| A1 | K2 会话剩余差异 | 逐项复核 CAPTCHA/登录保护存储 owner；profile limits 已修复并独立复审 | [~] |
| A2 | K2 高权限与系统账户 | 临时令牌 allowlist/参数契约已修复并复审；继续逐项复核 owner gate、super_admin 不变量、默认资源与 DTO/副作用；不以入口未挂载掩盖实现差异 | [~] |
| A3 | K1、K3–K7 横切能力 | 分别复核 HTTP 内核、限流、操作日志、失效总线、legacy bridge、mock upstream；每次只关闭一个独立行为 | [ ] |
| A4 | M04 与 M03 授权/团队 | 先处理 ProcessingTTL 单位，再按作用域、成员/授权 fanout、DTO、事务和失效副作用逐项验证 | [ ] |
| A5 | M05、M06、M07、M08 管理资源 | 按 groups → route strategies → API keys → accounts 的依赖顺序，核对端点完整性、授权视角、DTO、运行态和删除副作用 | [ ] |

| A6 | M01 公告 | 单独核对公开/管理端点、时间与 revision、投影和副作用 | [ ] |
| A7 | S-SQ、S-PG | 通过可销毁 fresh/upgrade 库和默认 seed 对照验证初始化结果；真实依赖不具备时保留未验证状态 | [ ] |
| A8 | 功能 owner 收口复审 | 对已修复的完整功能复核唯一入口、Node 活跃路径、归档/回滚门禁；BUG-0152 等 owner 缺陷未闭合前不宣称接管完成 | [ ] |

上述顺序是审计与修复优先级，不授权顺带迁移。某项需要跨模块设计、生产状态、Node 删除或新的功能范围时，先在问题记录中明确缺失授权和验收门槛，再等待维护者决定。

## 验证矩阵

| 类型 | 最低验证 | 通过标准 |
| --- | --- | --- |
| Node/Go 对照 | 源码、路由挂载、调用链、已有测试和提交 diff | 可定位到同一入口与同一结果，不以注释或迁移说明替代行为证据 |
| 回归测试 | 受影响 Go 包的定向 go test；有并发、缓存或状态机时补 go test -race | 覆盖成功、确认差异的失败路径和至少一个相邻回归路径 |
| 外部依赖 | 优先可回放 Mock；Redis/PostgreSQL/多实例行为仅在依赖真实可用时做定向集成验证 | Mock 不代替真实 owner/多实例结论；不可执行时标为未验证 |
| 静态检查 | go vet（受影响包）与 git diff --check | 无新增诊断或格式错误 |
| 提交后复审 | 独立审阅 Node 证据、Go diff、测试和范围 | 无 blocker；否则回到证据阶段，不能标记完成 |

## 验收标准

本计划只有在纳入的每个迁移单元都满足以下条件时才可关闭：

- 每项都存在可追踪的“等价”“已修复”或“经维护者确认不做”的结论；不存在只凭迁移提交说明推断的通过项。
- 已确认缺陷都有问题记录、最小回归测试、独立复审和精确 Git 提交。
- 对完整接管相关功能，唯一 Go owner、Node 活跃路径归零、migration-backup 归档和切换/回滚证据按迁移规则单独验收；这些缺失不得被单元测试掩盖。
- M09、M09b 和并行未提交工作仍保持排除，除非维护者显式更新本计划范围。

## 风险与注意事项

- 当前工作区已有其他 AI 的未提交迁移改动；任何提交前必须精确指定路径，禁止 git add .、git commit -a、reset、restore、stash 或清理。
- 已发现的问题通常是“遗漏或逻辑偏离”的聚合记录。聚合问题不能被一次局部改动自动关闭，必须逐个子项复核。
- 不把 Go 的更高并发能力误判为功能差异；也不把 Node 的限制、错误映射、权限或状态副作用当成可随意删掉的实现细节。
- K2 入口缺失会遮蔽真实运行行为，但不改变对 Go 实现本身继续做静态/Mock 对照的必要性。

## 进度记录

- 2026-09-04：建立本计划，冻结 M08 及以前的已提交迁移范围，显式排除 M09、M09b 与当前未提交迁移工作。
- 2026-09-04：K2 会话已完成 10 个小切片的定向修复和复审；`3c7ca413a` 修复第十次同 IP 失败仍累计 username 维度，`76c352987` 补齐 profile effectiveRequestLimits 及其 raw request-limits 规范化，`29cd2a585` 保持 NewDeps 构造器兼容并支持 settings 注入。多实例 CAPTCHA/登录保护已确认仍使用进程内状态，记录为下一独立修复切片。
- 2026-09-04：A2 临时访问令牌切片完成；`3b8497c1a` 修复并测试 Go 与 Node 的严格字段、`null`、整数数字和 TTL 范围契约，独立复审通过。A2 继续处理 `super_admin`、owner gate、默认资源及副作用差异。
- 2026-09-04：A2 管理账户角色切片完成；`31207137d` 收敛 Go 管理 HTTP create/PATCH 到 Node 的 `admin | user` 角色集合，完整包测试、race、vet 和独立复审通过。A2 继续处理 owner gate、超级管理员并发不变量、默认资源及副作用差异。
- 2026-09-04：A2 owner gate 取证完成；`authsys.AccountStore` 未接入 `OwnerGate`/`CheckContract`，与 `business/system_accounts` 的 fail-closed 契约分叉。该项需要启动装配级修复，保持待修复。
- 2026-09-04：A2 options 查询切片完成；`821cc210e` 对齐 `display_name`、IDs+keyword 联合过滤和精确/前缀关键词语义，定向测试、race、vet 和独立复审通过。A2 继续处理默认资源、nullable、PATCH 回执与副作用。
- 2026-09-04：A2 PATCH 回执切片完成；`6bc723f2b` 返回实际变更的非 nullable 字段，定向测试、race、vet 和独立复审通过。后续历史提交已带入 nullable 清除语义，但 K2 日志 scope/viewer、默认资源、缓存和副作用仍待逐项复核。
- 2026-09-04：A4 M04 去重单位切片完成；修复 `f953d071b` 的 `120_000_000` 纳秒误用为 `authorizationsProcessingTTL = 120 * time.Second`，定向测试、`-race`、`go vet` 和 `git diff --check` 通过。随后按 Node upsert 契约修复相同 active 授权重复创建应返回 `created=false` 的幂等切片，提交 `5f2341504`；提交 `a1dafba60` 修复 terminal revival 复用历史行并返回 `created=false` + `previousStatus`；提交 `c4b2886e7` 修复团队 active grant 上限的第 21 条误放行并补回归；提交 `f8a3c9669` 让团队 runtime fanout 排除资源 owner；提交 `25ddb320e` 拒绝 owner-only 团队并补回归；提交 `a5ef114ab` 在创建路径补 owner scope 门禁并补回归。M04 端点、其他写操作的作用域、DTO 和副作用缺口继续保留。
- 2026-09-04：A4 M04 去重单位切片完成；修复 `f953d071b` 的 `120_000_000` 纳秒误用为 `authorizationsProcessingTTL = 120 * time.Second`，定向测试、`-race`、`go vet` 和 `git diff --check` 通过。随后按 Node upsert 契约修复相同 active 授权重复创建应返回 `created=false` 的幂等切片，提交 `5f2341504`；提交 `a1dafba60` 修复 terminal revival 复用历史行并返回 `created=false` + `previousStatus`；提交 `c4b2886e7` 修复团队 active grant 上限的第 21 条误放行并补回归；提交 `f8a3c9669` 让团队 runtime fanout 排除资源 owner；提交 `25ddb320e` 拒绝 owner-only 团队并补回归；提交 `a5ef114ab` 在创建路径补 owner scope 门禁并补回归。空 `limits:{}` 与 `NULL` 的幂等等价修复已随并行 W3 提交 `d6ea6eef1` 落库；仅采纳该 M04 定向复审结论，W3 本身仍明确排除在本计划范围外。随后对创建 expiry 进行 Node/Go 深度对照，确认并修复严格 RFC3339、UTC 毫秒规范化、未来时间、账户到期上限以及资源错误优先级偏差；提交 `46a61aae4`，代码和回归已通过定向测试、`-race`、`go vet`、`git diff --check` 与独立复审。M04 端点、其他写操作的作用域、PATCH 校验、DTO 和副作用缺口继续保留。

- 2026-09-04：A4 M04 PATCH 首轮静态对照完成，确认字段 presence/JSON null、expiresAt 状态机与账户上限、runtime 投影、unchanged 结果及管理员 `systemAccountId` scope 均偏离 Node；已登记 BUG-0165，尚未改动代码。下一切片先以可观测的 `expiresAt` PATCH 契约为范围，scope、limits 和 unchanged 结果分别复核后再修。
- 2026-09-04：A4 M04 PATCH owner scope 子项实施最小修复：`routes.patch` 将管理员 `?systemAccountId` 传入 `Store.PatchForOwner`，不匹配 owner 返回 `not_found`，原无 scope 调用保持兼容；提交 `f4360fb45`，新增跨 scope 回归并经独立复审通过。expiresAt、limits、状态机、runtime 投影和 unchanged 子项仍未修改。
- 2026-09-05：A4 M04 PATCH `expiresAt` 切片完成：提交 `3fb3379d4` 对齐字段 presence/null、RFC3339 UTC 毫秒规范化、过去时间→expired、expired 恢复、账户到期上限、runtime expiry/status/source 投影及 unchanged 回执；独立复审发现 expired+null 状态回退、移除团队成员误复活及 unchanged 日志三个边界问题，提交 `8288bfb6f` 修复并补回归；再次复审发现 active 状态切换未重验已有 expiry，提交 `f512accc1` 修复并补回归。最新定向 `go test`、`-race`、`go vet`、`git diff --check` 通过。limits schema/DTO、expectedUpdatedAt 严格校验和其他端点保持后续切片。（该组提交已随 `662bd85f5` 回退，当前状态见下方重修记录。）
- 2026-09-05：A4 M04 PATCH `limits` 取证完成：确认 Go 无法区分缺失/null、`{}`/NULL 归一化不一致、schema 与数值精度校验缺失、runtime limits 未投影、响应回显原始请求；已登记为独立下一切片，暂未改动代码。
- 2026-09-05：A4 M04 PATCH `limits` 切片完成：`eee4cf59b` 接入共享 `gatewayquota` 归一化并覆盖 PATCH/创建 presence、null/空对象清除、grant/runtime 投影、规范化回执与边界校验；独立复审发现 revoked/returned runtime 不应被改写，`7d83f65d1` 修复并补回归。定向 `go test`、authz `-race`、`go vet` 和切片范围 `git diff --check` 通过；expectedUpdatedAt 严格校验、完整 DTO、其他端点及跨表副作用继续保持待审。（该组提交已随 `662bd85f5` 回退，当前状态见下方重修记录。）
- 2026-09-05：A4 M04 PATCH `expectedUpdatedAt` 取证完成：Node 严格校验并 canonicalize 为 UTC 毫秒后 CAS，Go 仅检查非空，已登记为下一独立修复切片，暂未改动代码。
- 2026-09-05：A4 M04 PATCH/expire `expectedUpdatedAt` 切片完成：`c1073e9c1` 接入 RFC3339/UTC 毫秒规范化、等价瞬间 CAS 和数据库当前版本推进，`d61af7577` 补齐缺失/null 的 Node 一致 400 错误；新增回归并通过定向 `go test`、`-race`、`go vet`、切片范围 `git diff --check`，独立复审 PASS。revoke/return 及其他 DTO/副作用仍按独立切片继续审核。（该组提交已随 `662bd85f5` 回退，当前状态见下方重修记录。）
- 2026-09-05：A4 M04 revoke/return `expectedUpdatedAt` 取证完成：Node 两端点共用严格 RFC3339 instant schema 并 canonicalize，Go 仅检查非空且 Store 按字符串比较，已登记为下一独立修复切片，暂未改动代码。
- 2026-09-05：A4 M04 revoke/return `expectedUpdatedAt` 切片完成：`3c4ac0c0c` 为两条路由和 Store 接入严格 RFC3339/UTC 毫秒规范化、时间瞬间 CAS，并兼容历史省略 `.000` 的 Go 版本值；新增等价 offset、非法/bare 回归，定向 `go test`、`-race`、`go vet` 和切片范围 `git diff --check` 通过。其他端点、DTO 和跨表副作用继续保持待审。（该提交已随 `662bd85f5` 回退，当前状态见下方重修记录。）
- 2026-09-05：A4 M04 revoke 幂等/日志取证完成：Node 已 revoked + 版本匹配返回 `unchanged` 200 且不写日志，更新时记录真实 `previousStatus`；Go 返回 `not_found`、无条件记日志并硬编码 `active`，已登记为下一独立修复切片，暂未改动代码。
- 2026-09-05：A4 M04 revoke 幂等/日志切片完成：`85a6062a7` 对齐 CAS 后的 revoked unchanged 结果、仅 updated 记日志和真实 `previousStatus`，并覆盖重复 revoke、旧版本 conflict、paused→revoked 回归；定向 `go test`、`-race`、`go vet` 和切片范围 `git diff --check` 通过。其他端点、DTO 和跨表副作用继续保持待审。（该提交已随 `662bd85f5` 回退，当前状态见下方重修记录。）
- 2026-09-05：A4 M04 回退后正规重修（当前工作区）：`662bd85f5` 以「打破 M10 已归档可见性测试」为由将 authz 回退至 `7336e220f`（W7）状态，上述 `3fb3379d4`、`8288bfb6f`、`f512accc1`、`eee4cf59b`、`7d83f65d1`、`c1073e9c1`、`d61af7577`、`3c4ac0c0c`、`85a6062a7` 九个切片实现已不在当前工作区，其「已修复、复审通过」记录仅代表历史。独立审查随后裁决 12 项 M04 契约主张中 9 项为真实偏差、3 项基线已对齐；当前工作区按该裁决单切片正规修复 `internal/authz`：新增 `sync.go` 忠实移植 Node `syncResourceAuthorizationGrantRuntimeAsync`/`upsertResourceAuthorizationForUserAsync`/`refreshResourceAuthorizationEffectiveSourceAsync`（patch 回 active 不再用内联近似），Patch 接入 expiresAt 毫秒 Z 规范化、JSON null 清空、空串报错、presence 区分、相同值 unchanged、过期状态机、expired 恢复须同调过期时间、激活重验与账户到期上限；limits create/patch 复用 `gatewayquota` schema 归一化并随 sync 投影 runtime、响应回显规范化值（revoked/returned 终态 runtime 不可变）；revoke/return/patch 版本路由层 `rfc3339InstantSchema('授权配置版本格式不正确')` 校验+canonicalize、Store 按 instant CAS；revoke/return conflict 判定先于终态检查、版本匹配的终态重复返回 unchanged+previousStatus、操作日志仅 updated 记录且 revoke 记录真实前状态、return 校验 runtime 与 active manual source；`/my-authorizations` 的 `direction` selfOnly 解析（非法 400）并传入 `Filters.Direction`。`authorized_reads_test.go` resume 断言按 Node 契约改回「恢复可见」，每项修复附 Node 证据行号对齐测试。`go build ./...`、`go vet ./internal/authz/...`、`go test ./internal/authz/... ./internal/accounts/... ./internal/systemteams/... -count=1` 与 authz `-race` 通过；代码尚未提交，待复审后随本切片提交。
- 2026-09-05：A4 M03 团队写操作版本输入取证完成：历史 `640470638` 的 PATCH/AddMembers/RemoveMember 仅做非空检查和字符串 CAS，Node 三条对应路由严格 RFC3339/UTC 毫秒 canonicalize；已登记 BUG-0166 独立切片。因最新 `662bd85f5` 回退/收敛提交不在本轮审计边界，暂不修改其代码。
- 2026-09-05：A4 M03 成员分页/DTO 取证完成：Node 成员列表按 active、`joined_at/id` 和 `pageSize+1` 返回完整分页/团队字段，历史 Go 全量复用详情并忽略分页，已登记 BUG-0166 独立修复切片，暂未改动代码。
- 2026-09-05：A4 M03 成员历史取证完成：Node history 只返回 `removed` 成员并按 `joined_at DESC, id DESC` 排序；历史 Go 同时返回 active/removed 成员并按 `created_at DESC, id DESC` 排序，已登记 BUG-0166 独立修复切片。因最新 `662bd85f5` 不在本轮审计边界，暂未改动代码。
- 2026-09-05：A4 M03 团队写操作 access scope 取证完成：历史 Go 虽接收管理员 `?systemAccountId` 生成的 `AccessScope`，但 PATCH/成员增删 Store 查询未使用该参数；Node 会要求指定账户为团队 active 成员，否则 not found。已登记 BUG-0166 独立修复切片，未改动最新迁移代码。
- 2026-09-05：A4 M03 成员新增授权 fanout 取证完成：Node 在成员事务内调用 `applyActiveTeamGrantsToMembersAsync` 写入 `sourceType=team` 的用户授权并失败回滚；历史 Go `AddMembers` 提交成员后没有 authz 调用，仅以未接入的 M04 wiring 注释代替。已登记 BUG-0166 独立修复切片，未改动最新迁移代码。
- 2026-09-05：A4 M03 团队恢复授权状态机取证完成：Node 同事务、真实 actor、team source 和 grant 字段恢复；历史 Go 嵌套事务且构造 system-account/manual source，丢 grant 字段并向 owner fanout。已登记 BUG-0166 独立修复切片，未改动最新迁移代码。
- 2026-09-05：A4 M03 团队写回执取证完成：Node PATCH/成员增删回传最新 version、成员数和局部更新字段；历史 Go 只返回 status 或部分 ID。前端版本防回退和局部重排直接依赖这些字段，已登记 BUG-0166 独立修复切片，未改动最新迁移代码。
- 2026-09-05：A4 M04 `my-authorizations` direction 取证完成：Node self-list 会传递 `direction`，Go SQL 已支持但路由遗漏映射，已登记为下一独立读路径修复切片，暂未改动代码。（已随上方「回退后正规重修」一并实现。）
- 2026-09-05：A5 M05 分组分页约束取证完成：Node pageSize 采用默认 50、范围 1–500；历史 Go 默认 20、范围 1–100，0/501 及前端批量 500 会导致页边界偏离。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组可选字段 null 取证完成：Node optional 非 nullable 字段传 null 必须 400；历史 Go create/patch parser 会跳过 null，可能错误成功创建或部分更新。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组调度策略子字段取证完成：Node 拒绝策略数值/enum 子字段的 null；历史 Go `schedulingPolicyJSON` 跳过 null 并落默认值。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组调度策略全局上限取证完成：Node 默认策略实时跟随 `runtimeConfig.concurrency.globalMax`（`JUHE_AI_CONCURRENCY_GLOBAL_MAX`，默认 5000、范围 1–50000），历史 Go 仅硬编码 5000，非默认环境会导致默认并发/队列容量和回显偏离。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组调度策略存储完整性取证完成：Node 对高并发分组的缺失、缺字段、strict key、类型/范围/enum 错误策略明确抛错，历史 Go `parseSchedulingPolicy` 静默返回 `nil`/部分或任意对象并继续成功响应；已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组调度策略同值 PATCH 取证完成：Node 深比较解析后的策略对象，历史 Go 比较原始 JSON 文本，键顺序/空白差异会被 Go 误判为变更并推进版本、触发失效。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组类型存储完整性取证完成：Node 对未知 `group_type` 抛错，历史 Go 静默回退 `personal` 并成功返回；已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组授权视角取证完成：Node 列表/详情会合并 `resource_authorizations` 授予当前账户的非所有者分组并返回 `accessType=authorized` 及授权覆盖字段；历史 Go 仅按 `groups.system_account_id` owner 过滤，导致 `/my-groups` 与详情丢失授权分组。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组统计投影取证完成：Node 列表/详情读取 `juhe_stats.group_account_stats`，并补充 today/total usage、currentConcurrency 与列表 `generatedAt`；历史 Go 列表固定返回零值 `AccountStats`，详情结构不含 `accountStats`，导致前端状态、用量和并发显示偏离。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组成员投影过滤取证完成：Node 仅展示自有账号或仍处于 active/paused/expired 授权状态的绑定，历史 Go `memberCounts`/`groupAccountIDs` 未校验所有者与授权状态，会包含 revoked/returned 或无效跨所有者绑定。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 `GET /groups/options` 端点取证完成：Node/前端提供 admin 与 self 两套 options 路径、purpose 和过滤参数，并返回授权感知选项；历史 Go 未注册 `/groups/options` 或 `/my-groups/options`。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 `GET /groups/authorization-options` 端点取证完成：Node/前端授权编辑器依赖 admin/self 两套路径及 `canAuthorize` 结果；历史 Go 未注册该端点，授权分组选项加载与已选项补齐会失败。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 `GET /groups/account-options` 端点取证完成：Node/前端依赖 admin/self 两套路径及 `accountIds`，授权视角必须隐藏所有者账号；历史 Go 未注册该端点。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 `GET /groups/route-strategy-options` 端点取证完成：Node/前端路由策略选择器依赖 admin/self 路径、过滤参数及授权后的 `enabled`；历史 Go 未注册该端点。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 `GET /groups/:id/edit-basic` 端点取证完成：Node/前端编辑页依赖 admin/self 专用响应及授权设置覆盖；历史 Go 未注册两套路径，`FindDetail` 不能替代其字段与授权覆盖语义。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 `POST /groups/:id/return-authorization` 端点取证完成：Node/self 路径执行授权归还，成功 204、无可归还授权 404，并带幂等保护与 owner/grantee 审计 viewers；历史 Go 未注册该端点或等价 store/日志。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组删除审计 targets/metadata 取证完成：Node 将受影响策略路由写入最多 20 个 `targets` 并附带数量/样本 `metadata`；历史 Go 仅写 Changes 摘要，且 `OperationLogEntry` 无 targets 字段。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组操作日志变更值类型取证完成：Node 保留 boolean/object 等结构化值并统一规范化，历史 Go `Change` 将 before/after 全部字符串化，导致 `enabled`、调度策略等审计 JSON 类型和结构偏离。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组删除/停用可用性守卫取证完成：Node 的剩余可用分组统计包含有效授权分组，历史 Go 只统计所有者自有绑定，可能错误放行唯一授权分组的删除/停用。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组删除后统计脏标记失败语义取证完成：Node PostgreSQL 后台标记失败只记录日志并保持删除 204，历史 Go 提交后同步标记失败会对已删除资源返回 500。已登记 BUG-0163 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略端点矩阵取证完成：Node/前端依赖 admin/self 两套 options、edit-basic、speed-first-runtime 路径及列表速度运行态投影，历史 Go 仅注册基础 CRUD，已登记 BUG-0164 独立修复切片，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略配置读取完整性取证完成：历史 Node 对损坏/非法 `config_json` 读取抛错并进入错误处理，历史 Go 列表/详情吞掉解析错误并静默返回默认或空配置，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略存储 mode/status 完整性取证完成：历史 Node 读模型会规范化默认值并拒绝未知值，历史 Go 列表/详情原样透传数据库字符串；已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略绑定 weight 完整性取证完成：历史 Node 读模型拒绝非 1–100 整数，历史 Go `scanBindingRow` 未校验并会透传脏值；已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略创建/PATCH null 契约取证完成：历史 Node 对 mode/status、绑定字段及配置子字段的显式 null 返回 400，历史 Go 跳过 null 并补默认值，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略 `systemAccountId` scope 校验取证完成：历史 Node 详情/编辑/运行态/PATCH/DELETE 拒绝空 scope，历史 Go 未校验并将空值视为未过滤范围，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略 PATCH `expectedUpdatedAt` 取证完成：历史 Node 在 schema 层 canonicalize RFC3339 到 UTC 毫秒后 CAS，历史 Go 仅验证原始字符串并直接比较，等价 offset/小数位版本会错误失败，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略列表分页数字查询取证完成：历史 Node 通过 `Number()` 接受 `1e2`/`1.0` 等整数表示，历史 Go `Atoi` 判无效并回退默认页参数，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略描述长度取证完成：历史 Node/Zod 按 UTF-16 code unit 限制 200，历史 Go 按 rune 计数，astral emoji 边界会产生不同 400/成功结果，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略变更异常状态码取证完成：历史 Node 对未分类 create/PATCH/DELETE 异常多返回 400，历史 Go 未分类错误返回 500，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略 create operation log 投影取证完成：历史 Node 固定记录六类字段并保留输入 presence，历史 Go 条件性省略配置且补写绑定默认值，已登记 BUG-0164 独立修复子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M06 路由策略分组绑定并发锁取证完成：历史 Node PG create/PATCH 对可绑定分组使用 `FOR UPDATE OF groups`，历史 Go 未加行锁，已登记 BUG-0164 并发一致性子项，暂未改动最新迁移代码。
- 2026-09-05：A5 M05 分组删除后缓存失效复核完成：Node 的 lookup/account-id cache 在 Go 读路径中没有对应实现，Go 直接查库且仍触发 runtime invalidation；该差异暂判为实现差异、非缺陷，不安排修复。
- 2026-09-05：A5 M08 账户删除授权链取证完成：Node 删除资源拥有者账户时在同一事务撤销 `resource_authorization_grants`、`resource_authorization_sources` 与 `resource_authorizations`，并清理授权 quota scope；历史 Go `Store.Delete` 仅软删除账户、标签和名称索引，未触碰授权链，删除后授权仍可能继续生效。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户删除健康 tombstone 取证完成：Node 删除事务按账户版本写入 `account_health_jobs_input_outbox` 的 `tombstone/account_deleted` 事件，历史 Go `Store.Delete` 未读取版本或投递 outbox，删除后健康投影/运行态可能残留。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户删除缓存/运行态失效取证完成：Node 删除提交后刷新 group stats、失效 account/group/auth lookup 并广播 gateway runtime 与 authorization quota；历史 Go 删除路由只记日志并返回 204，Store 无对应依赖或调用，删除后缓存可能继续返回旧账户/授权状态。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 `ENGAGED` 锁配置取证完成：Node death timeout 变化时按原 incident 起点重算 deadline，配置未变化时保留 retry lease；历史 Go 始终复用旧 deadline，并在每次 upsert 无条件清空 `next_retry_at_ms/lease_id/lease_until_ms`。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 API Key 凭据归一化取证完成：Node 写入要求 `base_url`、最多 10 个 Key，并去空白/去重后生成池与策略；历史 Go 仅提取第一个凭据源后原样加密，PATCH 也只做浅合并，重复/空白/超限池和缺失 `base_url` 可被成功写入。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 凭据 `base_url` 安全策略取证完成：Node API Key/OAuth/Google OAuth 写入统一拒绝 localhost、私网、环回和保留地址（除显式 allowlist），历史 Go 未做任何对应校验，带 `http://127.0.0.1:9/v1` 等地址可成功落库。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 创建初始健康检查取证完成：Node 创建成功后对 `pending_test` 或 active 单 Key API Key（无余额配置）投递 `activation` 健康检查；历史 Go create 仅写账户/日志并返回 201，没有 dispatch 或 health outbox 调用。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 PATCH 健康状态重置取证完成：Node 对凭据/代理/模型/检查协议等连接变化清空 `next_health_check_at` 和旧检查结果，并提交后投递 configuration 探针；历史 Go Patch 未读取或更新这些健康字段，也没有 `healthCheckRequired`/dispatch 接线。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户高级详情端点取证完成：Node `/accounts/:id/advanced` 与 `/my-accounts/:id/advanced` 返回高级凭据策略、模型映射、余额/排程/锁状态等 DTO；历史 Go 未注册 `/advanced`，普通详情和 edit-basic 不能替代其结果。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户 `clone-context` 取证完成：Node 返回脱敏凭据选项、模型/标签/映射/分组快照并执行授权实例 403 与配置/绑定快照 409 保护；历史 Go 未注册该端点或等价查询。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 Google OAuth 重新授权上下文取证完成：Node 仅对 owner Gemini `google_oauth` 返回脱敏 OAuth 元数据，并对授权实例返回 403；历史 Go 未注册 `/oauth-reauthorization-context` 或等价逻辑。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 API Key 运行态详情取证完成：Node owner 路径返回脱敏的 `{accountId, configRevision, items}`，授权实例 403；历史 Go 未注册 `/api-key-runtime`，也无运行态查询或投影。已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 API Key 运行态主动重验证取证完成：Node `POST /api-key-runtime/revalidate` 校验 body revision、scope、owner/授权实例、active+schedulable 多 Key 资格，并按版本栅栏只重置可探测非 active/disabled Key、清除错误/冷却、设置立即探测时间和运行态失效；历史 Go 未注册端点或实现等价状态更新，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户批量更新取证完成：Node `POST /batch-update`（admin/self）在同一事务锁定同作用域 owner 账户，逐目标校验 configRevision/授权实例边界并复用归一化，保证全成功或全回滚，返回 batchId/逐账户新版本并触发统计、运行态、dispatch 与余额快照副作用；历史 Go 未注册批量端点或等价事务服务，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户批量编辑上下文取证完成：Node `POST /batch-edit-context`（admin/self）严格校验 2–100 个目标和字段，按同一 owner scope 返回 configRevision、provider/protocol/type 及按需的支持模型/模型映射/endpoint modes；历史 Go 未注册端点或等价读取/越权与完整性校验，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户导出取证完成：Node `POST /export`（admin/self）支持按 ID/筛选导出，限制最多 500 个 owner 账户，排除授权实例并按账户类型白名单导出凭据，去重导出启用代理，返回固定导入协议文档/摘要并写可见性正确的操作日志；历史 Go 未注册端点或等价导出边界，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户导入预览取证完成：Node `POST /import/preview`（admin/self）只读构建导入计划，适配多来源协议并校验版本、50 账户/20 代理上限、凭据/模型/代理/分组/重复项，返回逐项 action/messages/warnings 与 `canImport`；历史 Go 未注册端点或等价预览验证，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户导入确认取证完成：Node `POST /import/confirm`（admin/self）带 mutation guard 并执行导入计划，创建/复用代理和分组、逐账户处理重复与失败、投递 pending health check，返回部分成功明细和汇总并写审计日志；历史 Go 未注册端点或等价执行链，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户余额手动刷新取证完成：Node `POST /:id/balance/refresh`（admin/self）限制 owner 可编辑的已启用 API Key 余额账户，通过共享 lease、单/多 Key 适配器、超时/错误分类和 configRevision CAS 写回 snapshot，lease busy/配置变化返回 409；历史 Go 未注册端点或实现余额查询/快照写回，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户余额明细取证完成：Node `GET /:id/balance/details`（admin/self）只允许 owner 查看，按 configRevision/nextRefresh 世代过滤旧快照，按当前 Key 池补 masked pending 并返回逐 Key 状态/聚合信息；历史 Go 未注册端点或实现等价读取/权限/世代保护，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户草稿余额测试取证完成：Node `POST /balance/test-draft`（admin/self）只读构造并校验未保存账户草稿，验证余额能力后查询上游返回 snapshot，多 Key/输入/上游错误均按 400 处理且不落库；历史 Go 未注册端点或实现等价草稿测试链，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户上游模型目录刷新取证完成：Node `POST /model-catalog/refresh`（admin/self）只读构造草稿并沿真实分组/代理/凭据链路发现模型，多 Key 取上游交集，再按 provider 协议筛选新增模型并推荐检查模型，支持请求取消；历史 Go 未注册端点或等价发现链，已登记 BUG-0162 独立子项，暂未改动代码。
- 2026-09-05：A5 M08 账户运行态重置取证完成：Node `POST /:id/runtime-reset` 以 config revision CAS 为边界，按 owner/授权视角处理锁死与来源阻断，并清理 gateway runtime、延迟降级、API Key transient/runtime、dispatch fence，必要时重新投递 health check，返回逐项清理结果；历史 Go 未注册 admin/self 路径，也无等价服务或副作用接线，已登记 BUG-0162 独立子项，暂未改动代码。
