# PLAN-0089 AI 账户上游余额查询

## 基本信息

- 编号：PLAN-0089
- 状态：15 秒总超时调整已完成，待发布
- 创建时间：2026-07-11
- 需求来源：Codex 会话 `019f50a8-4d4a-73c1-8506-0dca19b9a684`
- 关联模块：AI 账户 / 上游适配器 / ops-worker / 统计快照 / 前端账户列表 / 权限 / 存储
- 关联设计：[AI 账户上游余额查询设计](../functions/AI账户上游余额查询设计.md)
- 实施方案：[AI 账户上游余额查询实施方案](计划-0089-AI账户上游余额查询实施方案.md)

## 实施顺序

- [x] 账户 schema、领域类型、API DTO 和配置校验。
- [x] `relay_balance` 快照读写、到期候选查询和 SQLite/PostgreSQL 一致性。
- [x] Sub2API、New API、LiteLLM 与自定义接口适配器。
- [x] 单账户查询服务、并发去重、代理与 URL 安全边界。
- [x] ops-worker 每分钟有界刷新任务。
- [x] 手动刷新接口与权限边界。
- [x] 账户列表批量补齐余额快照，授权实例排除。
- [x] 账户编辑配置和 `AccountUsageCell` 状态展示。
- [x] 功能文档和测试说明同步。
- [x] 类型检查、构建和定向回归。
- [x] 增加通用 `GET /user/balance` 内置适配器。
- [x] 新增/编辑弹窗使用独立草稿测试接口，查询只显示顶部消息，不保存配置、调度或余额快照。
- [x] 新账户首次激活后非阻塞自动探测，命中才自动开启。
- [x] 增加所有系统账户的一次性游标扫描维护命令，所有查询路径复用账户绑定代理。
- [x] 前端查询类型收敛为“内置适配 / 自定义接口”，内部记录最近成功适配器并在失败时全量回退重识别。
- [x] 将单账户余额查询共享总 deadline 从 8 秒调整为 15 秒，不改变并发、租约或接口契约。
- [~] 更新超时回归断言并完成类型检查和构建；生产验证待发布后执行。
- [ ] Mockdata 可视化样本。
- [ ] 登录后的桌面与移动端浏览器验收；当前临时验收库无有效 session，验证码登录需人工完成。

## 当前验证结果

- `test:account-balance-config`、`test:account-balance-adapters`、`test:account-balance-refresh` 已通过。
- `test:account-balance-auto-detect` 已通过，覆盖适配器顺序、严格命中、代理字段传递、首次激活投递和全量扫描游标/并发边界。
- SQLite writer 边界、DB service 读写调度和查询期间关闭配置的竞态回归已通过；内置适配已调整为共享 15 秒总 deadline。
- 发布包已验证保留全量扫描命令和编译产物；隔离 PostgreSQL schema 已验证迁移与 JSONB 条件更新。
- PostgreSQL schema SQL、后台 job registry、账户列表查询门禁、凭据脱敏、账户编辑保存和作用域边界回归已通过。
- 前后端全项目 typecheck 和前端生产构建已通过；构建只有既有 chunk size warning。
- 15 秒调整按 TDD 验证：新增断言先在 `8_000` 上按预期失败，修改为 `15_000` 后余额刷新、适配器和自动探测专项回归均通过；后端与全项目 typecheck、全项目 build 通过。
- 使用旧 SQLite 直接启动会因缺少新字段失败，符合“不做运行时向下兼容”约束；上线前必须离线同步当前 schema。

## 验收标准

- 失败时清空金额并显示“查询失败”，悬浮可见具体原因。
- 自动刷新只处理启用、到期、当前可用的物理单 Key API Key 账户。
- 功能关闭、OAuth、多 Key 池和授权实例不展示、不查询。
- 五类适配器只消费明确查询接口响应，不使用兼容特征或本地数据推算余额。
- 手动刷新使用无边框 `ReloadOutlined` 图标，最长等待 15 秒且不重复并发查询。
- 新增/编辑页使用当前表单配置进行无持久化测试；列表刷新图标只同步更新当前账户单元格，不刷新整表。
- 新账户创建不等待余额探测；首次激活命中后自动开启，未命中或配置版本变化保持关闭。
- 上线后对全部系统账户执行一次游标扫描，账户配置代理时所有探测和刷新均走该代理。
- 查询结果不影响账户状态、调度、健康检查和本地用量统计。
- 列表读取无 N+1，后台单轮查询有固定上限和稳定排序。

## 2026-07-12 超时调整设计

- 现状：单账户内置适配器共同使用 8 秒总 deadline；NewAPI 可能连续请求两个接口，代理或上游响应偏慢时容易在形成有效余额前耗尽预算。
- 方案：把共享总 deadline 固定调整为 15 秒。所有候选适配器继续消费同一截止时间，不能按适配器分别获得 15 秒。
- 保持不变：后台单轮最多 100 个账户、并发 4、同账户租约 30 秒、响应体上限 256 KiB、禁止重定向、严格 JSON 解析和账户代理规则。
- 不新增系统设置或账户字段，避免为单一安全边界增加配置负担；后续只有生产证据表明 15 秒仍不足时再重新评估。
- 回归要求：静态断言总 deadline 为 15 秒；验证优先适配、回退适配和超时错误仍共享同一预算；执行余额专项回归、后端类型检查和构建。

## 发布边界

- 本计划涉及 `accounts` 当前 schema 和 `account_usage_snapshots` kind 契约。
- 不在运行时代码中加入旧 schema 兼容或自动迁移。
- 上线前单独生成并预演 SQLite/PostgreSQL 结构同步与回滚方案。
- 与 PLAN-0088 合并验证后再决定提交和上线，本计划实现期间不部署生产。
- 本次修复从 `master` 独立 worktree 开发；上线后执行一次 `maintenance:backfill-account-balance`，完成后再把 master 合入 `feature/20260706-go`。
