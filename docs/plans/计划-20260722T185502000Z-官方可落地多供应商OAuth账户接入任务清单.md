# 官方可落地多供应商 OAuth 账户接入任务清单

> 对应设计：[官方可落地 OAuth 账户接入设计](../functions/官方可落地OAuth账户接入设计.md)
> 对应计划：[PLAN-20260722T174032816Z 官方可落地多供应商 OAuth 账户接入](计划-20260722T174032816Z-官方可落地多供应商OAuth账户接入.md)

## 范围与验收

- [x] 从本地 `master` 创建隔离 worktree 和开发分支，不改当前 `F:\sub2api-lite` 工作区。
- [x] 先落设计文档、计划文档、任务清单和私有资源入口。
- [ ] 后端按供应商 profile 收口 `oauth` 凭据归一化、endpoint modes 和网关鉴权。
- [ ] 前端按供应商区分“支持 OAuth 账户类型”与“支持管理式授权按钮”。
- [ ] Anthropic Bearer Token 型 OAuth 账户可创建、编辑、测试、参与网关调度。
- [ ] OpenAI 管理式 OAuth 与 Gemini 导入型 OAuth 不回归。
- [ ] 开发期间至少检查一次本地 `master` 是否前进，并在必要时合并最新 `master`。
- [ ] 所有改动完成后合并回本地 `master`，处理冲突并重新验证。

## 执行清单

- [x] 建立 worktree `C:\Users\Administrator\.codex\worktrees\sub2api-lite\oauth-matrix-safe-20260722`
- [x] 建立分支 `codex/oauth-matrix-safe-20260722`
- [x] 创建 `.local/project-resources/` 并加入 `.gitignore`
- [x] 创建设计文档 `docs/functions/官方可落地OAuth账户接入设计.md`
- [x] 创建计划文档 `docs/plans/计划-20260722T174032816Z-官方可落地多供应商OAuth账户接入.md`
- [x] 创建本任务清单
- [ ] 盘点后端剩余缺口与可复用测试
- [ ] 盘点前端剩余缺口与可复用测试
- [ ] 实现后端补丁
- [ ] 实现前端补丁
- [ ] 更新 Anthropic / Gemini / OpenAI 长期文档
- [ ] 执行类型检查与专项回归
- [ ] 按需执行 PostgreSQL / Redis / 真实模型最小联调
- [ ] 合并最新本地 `master`
- [ ] 合并回本地 `master` 并记录最终验证结果

## 合并与分支策略

- 开发分支：`codex/oauth-matrix-safe-20260722`
- 基线分支：本地 `master`
- 开发期间定期执行：检查 `master` 是否前进；如前进则合并到开发分支并立即处理冲突。
- 最终交付：开发分支验证通过后合并回本地 `master`，再在 `master` 上重跑关键验证。

## 环境与安全边界

- 私有资源入口：`.local/project-resources/README.md`
- 测试 PostgreSQL / Redis 主机：`192.168.1.203`
- 账号与数据库具体密码：只从 `.local/project-resources/` 引用的外部文件读取
- 本地 SQLite 问题允许删除当前任务生成的测试库并重建
- PostgreSQL / Redis 只清理本任务专用测试对象
- 不执行生产部署或线上运维动作
