# 计划-0163 账户错误作用域与重试选择验证

## 状态

进行中

## 清单

- [ ] 建立请求/会话/Key/账户/路由五层错误作用域矩阵。
- [ ] 验证坏会话并发不会误杀正常账户或全池。
- [ ] 验证多 Key 隔离、Key 恢复和跨优先级切换规则。
- [ ] 验证 handoff 后新请求重新按当前质量/速度/优先级选号。
- [ ] 验证原地重试、切号、总超时和全池最大等待。
- [ ] 补充交叉回归并复查客户端响应、质量归因和状态流转。
- [ ] 合并最新 `origin/master`，更新文档并推送远程 `master`。

## 完成标准

所有核心 Mock 回归通过；坏会话失败不改变健康账户状态；单 Key 故障不影响同账户其他 Key；高优先级恢复后新请求可重新选回；全池耗尽有界 handoff；无未解释的账户误杀或无限等待。
