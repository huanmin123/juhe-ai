# PLAN-20260718T230201008Z 生产管理端性能优先治理实施计划

> **给执行 Agent：** 使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按子计划逐项实施；每个阶段必须经过功能完整性审查和性能门禁后才能继续。

**目标：** 在不删减任何页面、接口、字段、统计、审计、日志或管理操作的前提下，消除生产管理页面与 System API 的秒级长尾，使 PostgreSQL、Redis、worker 和前端加载链路符合性能优先、弱一致性、允许非核心记录偶发丢失的项目定位。

**架构：** 方案拆成三个可独立交付的子项目：PostgreSQL 普通查询去事务与统计降载、Redis 队列隔离与后台任务降载、管理接口渐进加载与事件循环治理。主列表和核心业务事实保持同步可用；动态状态与非核心副作用使用有界异步、缓存快照、批量写和可观测降级，不通过关闭功能或返回假空数据达标。

**技术栈：** Node.js 22、TypeScript、Express、Vue 3、Ant Design Vue、PostgreSQL 18、PgBouncer、Redis Streams、PowerShell 7、pnpm workspace。

---

## 基本信息

- 计划 ID：PLAN-20260718T230201008Z
- 状态：待开始
- 创建时间：2026-07-16
- 更新时间：2026-07-16
- 需求来源：用户反馈生产管理页面和接口经常出现秒级到十几秒延迟，并明确要求只提速、不删减功能；项目不追求金融级数据耐久性和强一致性，允许非核心记录偶发丢失。
- 执行者：AI / 维护者
- 关联模块：前端 / System API / DB service / PostgreSQL / PgBouncer / Redis / stats-worker / ingest-worker / ops-worker / 部署 / 压测 / 文档

## 不可变约束

- 不删除菜单、页面、接口、字段、筛选项、详情、统计、审计、日志、余额、运行状态或后台任务入口。
- 不通过返回 `items: []`、伪造 `0`、吞掉错误、隐藏字段或关闭统计任务来制造性能改善。
- 普通只读和单条写入默认不使用显式事务；只有真实多语句业务不变量保留最小事务。
- 核心 CRUD、权限、API Key 路由、授权、额度判断和网关调用语义保持完整。
- 账户状态、并发、余额、今日用量和最近使用时间可以短暂过期或异步补齐，但必须保留最后成功值和明确可用性标记。
- 审计、日志、usage 和统计功能完整保留；队列断连、饱和、超时和 payload 超限时允许个别非核心记录丢失，但必须记录 drop 指标。
- 每个阶段独立发布、独立验证、独立回退，不做一次性大爆炸改造。

## 问题定义

- 生产管理接口典型耗时约 `0.4s` 到 `1.5s`，账户列表出现约 `3.99s` 到 `6.7s`，供应商选项出现约 `10s` 到 `15s`。
- PostgreSQL 普通 query / execute 固定执行 `BEGIN + SET LOCAL + SQL + COMMIT`，生产累计约 4950 万次事务包装。
- Redis state 与 queue 共享同一物理实例和 DB，大 Stream payload 与状态命令产生单线程头阻塞。
- stats-worker、ingest-worker、server 和 db-service 均存在数百毫秒到数秒事件循环 lag。
- 账户页面 options、主列表和动态快照存在串行等待与重复轮询，后端 DTO 同步补齐多组动态字段。
- 统计窗口、额度窗口、分组统计和审计维护存在高频全量或逐行写入，形成周期性资源尖峰。

## 目标与非目标

### 目标

- 管理主列表 P95 `<=300ms`，P99 `<=800ms`。
- options 不阻塞主列表发起和首行显示。
- 普通独立 PostgreSQL SQL 不再产生自动 `BEGIN` / `COMMIT`。
- Redis state 与 queue 物理隔离，队列失败不阻塞前台请求或触发主进程 fatal。
- 统计、审计、日志、余额和动态状态功能继续可用，只改变数据刷新与传输方式。
- 建立可重复的生产前压测、功能回归和上线观察流程。

### 非目标

- 不重写整个后端技术栈，不借本计划推进无关 Node -> Go 迁移。
- 不引入 Kafka、ClickHouse、分布式事务或新的重型基础设施。
- 不增加旧 schema 兼容、双读双写或自动迁移。
- 不改变 API Key -> 路由策略 -> 分组 -> AI 账户的业务路由层级。
- 不修改真实上游模型请求协议和调度算法。

## 关联文档

- 根因报告：`docs/reports/生产管理端卡顿根因与性能优先治理报告-2026-07-16.md`
- PLAN-20260718T230201009Z：`docs/plans/计划-20260718T230201009Z-PostgreSQL去事务与统计降载.md`
- PLAN-20260718T230201010Z：`docs/plans/计划-20260718T230201010Z-Redis队列隔离与后台任务降载.md`
- PLAN-20260718T230201011Z：`docs/plans/计划-20260718T230201011Z-管理接口渐进加载与事件循环治理.md`
- 历史基线：`docs/plans/计划-20260704T095333000Z-管理接口读写隔离与前台防阻塞.md`
- 历史基线：`docs/plans/计划-20260704T095333001Z-管理接口DTO分层与渐进式加载.md`
- 被部分取代的可靠性决策：`docs/plans/计划-20260705T092619000Z-可靠统计与读写资源隔离.md`
- 动态快照关联：`docs/plans/计划-0104-账户动态快照与私网BaseURL白名单.md`

## 总体架构决策

### 1. 核心业务同步，附加信息渐进

- 主列表同步返回稳定身份、权限、静态调度、关联摘要和操作判定。
- 动态状态、并发、今日用量、最近使用和余额通过动态快照补齐。
- 页面保持所有字段和操作入口；渐进加载只改变请求阶段，不改变最终可见功能。

### 2. 普通 SQL 无事务，真实不变量短事务

- `DatabaseClient.query`、`one` 和单条 `execute` 直接执行 SQL。
- 账户 / 分组 / API Key / 授权等真正需要多表共同成功的写操作继续使用显式 `transaction()`。
- 统计小批结果和游标需要共同推进时保留短事务；展示性日志、审计、清理单批和单条状态写不使用额外事务。

### 3. Redis state 与 queue 隔离

- cache、state、queue 使用三个物理 Redis 进程。
- state 只承接会话、验证码、限流、并发槽和调度运行态。
- queue 承接记录型异步副作用；producer 和 consumer 使用独立连接。
- 大 payload 先落内容寻址 staging，Stream 只传引用。

### 4. 非核心副作用 best-effort

- 主请求不得等待审计、运行日志、操作日志、公开接口日志和展示性 usage 的长期重试。
- producer 采用有界内存和短 deadline，超时 / 断连 / 饱和后丢弃并计数。
- 该策略不关闭记录功能；Redis 恢复后继续正常入队和展示。

### 5. 统计保留，计算方式降载

- 所有现有统计页面、额度字段、趋势和 TopN 保留。
- 热窗口改 changed scope/date 增量，范围窗口低频刷新，分组统计批量 upsert。
- 离线全量重建只用于恢复和验证，不进入 System API 请求路径。

## 子计划边界

| 子计划 | 负责内容 | 独立验收 |
| --- | --- | --- |
| PLAN-0121 | 普通 PG 查询去事务、连接预算、统计批量与增量刷新 | 普通 SQL 自动事务为 0，统计功能完整，管理读 P95 达标 |
| PLAN-0122 | Redis 物理隔离、队列有界化、payload 引用、维护降载 | state P99 达标，队列故障不阻塞请求，记录功能仍可用 |
| PLAN-0123 | options 并行、主列表稳定字段、动态/余额快照、轮询退避、可观测性 | 首列表可交互时间达标，字段与操作入口无删减 |

## 总体执行顺序

### Task 1：建立基线和功能完整性门禁

**Files:**
- Modify: `backend/src/scripts/performance/performance-system-api-load-test.ts`
- Modify: `backend/src/scripts/regression/management-list-contract-regression.ts`
- Modify: `backend/src/scripts/regression/account-status-snapshot-contract-regression.ts`
- Modify: `frontend/src/scripts/regression/account-status-snapshot-polling-regression.ts`
- Modify: `docs/develop/测试与验证说明.md`

- [ ] 记录 endpoint P50 / P95 / P99 / max、Server-Timing、event-loop lag、PgBouncer transaction/query time、Redis state slowlog 和 worker CPU。
- [ ] 回归断言页面所需字段仍存在；动态字段允许 `available:false` 或旧 `generatedAt`，但不能被删除或无提示变成 `0`。
- [ ] 运行：`pnpm --filter juhe-ai-backend test:management-list-contract`。
- [ ] 运行：`pnpm --filter juhe-ai-backend test:account-status-snapshot`。
- [ ] 运行：`pnpm --filter juhe-ai-frontend test:account-status-snapshot-polling`。

### Task 2：先实施 PLAN-0122 的 Redis 拓扑隔离

**Files:**
- Modify: `backend/src/config/runtime.ts`
- Modify: `deploy/*`
- Modify: `docs/deploy/高性能模式部署指南.md`
- Test: `backend/src/scripts/regression/performance-redis-boundary-regression.ts`

- [ ] 新增独立 queue Redis 地址，生产禁止共享 cache/state/queue URL。
- [ ] 运行：`pnpm --filter juhe-ai-backend test:performance-redis-boundary`。
- [ ] 只发布配置并观察至少 30 分钟 state Redis P99、slowlog、管理接口 P95、队列 length / pending 和进程重启次数。

### Task 3：实施 PLAN-0121 的普通查询去事务

**Files:**
- Modify: `backend/src/storage/database-client.ts`
- Modify: `backend/src/storage/postgres-client.ts`
- Modify: `backend/src/scripts/regression/postgres-client-boundary-regression.ts`
- Create: `backend/src/scripts/regression/postgres-query-transaction-boundary-regression.ts`

- [ ] 先写失败回归：普通 `query` / `one` / `execute` 只允许一条业务 SQL；显式事务必须有 BEGIN / timeout / COMMIT / ROLLBACK。
- [ ] 旧实现运行回归必须因普通 query 仍有 BEGIN / COMMIT 而失败。
- [ ] 修改后运行：`pnpm --filter juhe-ai-backend test:postgres-client-boundary`。
- [ ] 修改后运行新回归，预期普通 SQL 无显式事务，真实 `transaction()` 语义不变。

### Task 4：实施 PLAN-0123 的主列表和快照解耦

**Files:**
- Modify: `frontend/src/views/accounts/useAccountListData.ts`
- Modify: `frontend/src/views/accounts/accountStatusSnapshotPolling.ts`
- Modify: `backend/src/modules/accounts/account-list.routes.ts`
- Modify: `backend/src/modules/accounts/account-status-snapshot.service.ts`
- Modify: `backend/src/storage/account-status-snapshot.repository.ts`

- [ ] 主列表立即发起；provider / proxy options 后台并行加载。
- [ ] 主列表不再同步等待 current concurrency、余额、今日用量和最近使用；快照返回相同功能字段并增加 `generatedAt` 与可用性。
- [ ] 成功态保持 `10s +/- 1s`；失败按 `20s -> 40s -> 80s -> 120s` 退避；隐藏页停止；单飞且不补偿追赶。
- [ ] 运行前后端 snapshot、polling 和 management-list-contract 回归。

### Task 5：实施 PLAN-0122 的队列引用化和 best-effort

**Files:**
- Modify: `backend/src/shared/redis-client.ts`
- Modify: `backend/src/shared/redis-stream-queue.ts`
- Modify: `backend/src/modules/audit-logs/audit-log-queue.service.ts`
- Modify: `backend/src/modules/gateway/usage/record-queue.service.ts`
- Modify: `backend/src/modules/runtime-logs/runtime-log-index-queue.service.ts`
- Modify: `backend/src/modules/record-maintenance/record-maintenance-queue.service.ts`

- [ ] 复用 runtime log producer 的有界、短 deadline 和 drop 计数模式。
- [ ] 禁止非核心队列错误触发请求失败或进程 fatal。
- [ ] 大 payload 先写 staging / blob；Stream 只保存引用、大小、hash 和必要索引字段。
- [ ] 运行 Redis Stream、enqueue failure、audit large payload 和 maintenance 回归。

### Task 6：实施 PLAN-0121 的统计批量与增量刷新

**Files:**
- Modify: `backend/src/modules/background/background-jobs.ts`
- Modify: `backend/src/modules/background/background-stats-writer.ts`
- Modify: `backend/src/storage/usage-stats.repository.ts`
- Modify: `backend/src/storage/usage-quota-hourly-window.repository.ts`
- Modify: `backend/src/storage/group-account-stats-cache.repository.ts`

- [ ] 在线热任务只处理本轮变化的 scope/date；范围窗口和 TopN 使用独立低频任务。
- [ ] 分组统计和额度窗口移除逐行 DELETE / INSERT，改为单条批量 upsert 或短批事务。
- [ ] 运行 usage hot-window、batch statement、quota window 和 group stats 回归。

### Task 7：后台维护降载

**Files:**
- Modify: `backend/src/modules/background/maintenance-cleanup-jobs.ts`
- Modify: `backend/src/modules/record-maintenance/record-maintenance-queue.service.ts`
- Modify: `backend/src/modules/record-maintenance/temporary-maintenance-worker-runner.ts`

- [ ] 同一维护 job 单飞，重复触发合并成下一轮 intent。
- [ ] 每批最多 100 行或 500ms，PG pool 预算 1 到 2；超时等待下一轮，不追求本轮完全清完。
- [ ] 运行 temporary-maintenance-worker、audit-log-retention 和 record-maintenance-queue 回归。

### Task 8：全量验证与灰度

**Files:**
- Modify: `docs/develop/测试与验证说明.md`
- Modify: `docs/deploy/高性能模式部署指南.md`
- Modify: `docs/functions/PostgreSQL与Redis高性能模式设计.md`
- Modify: `docs/functions/可靠统计与读写资源隔离设计.md`
- Modify: `docs/architecture/架构总览.md`

- [ ] 运行：`pnpm typecheck`。
- [ ] 运行：`pnpm build`。
- [ ] 运行 System API smoke、read burst、management load 和 usage real-entry mixed load。
- [ ] 每个发布阶段观察至少 30 分钟；最终版本执行 15 分钟并发 6 / 12 的持续管理读写混合测试。
- [ ] 逐页验证登录、首页、供应商、系统账户、团队、授权、AI 账户、分组、API Key、代理、策略路由、使用记录、统计、日志、审计、系统设置和后台任务。

## 测试矩阵

| 测试类型 | 测试项 | 验证方式 | 预期结果 | 状态 |
| --- | --- | --- | --- | --- |
| 类型检查 | 全仓 TypeScript | `pnpm typecheck` | 前后端类型通过 | 未执行 |
| 构建 | 全量构建 | `pnpm build` | 前后端构建通过 | 未执行 |
| PG 边界 | 普通 SQL 无事务 | PLAN-0121 新回归 | 普通 SQL 无 BEGIN / COMMIT，显式事务完整 | 未执行 |
| Redis 边界 | cache/state/queue 隔离 | `test:performance-redis-boundary` | 生产不允许共享 URL | 未执行 |
| 管理契约 | 页面字段与操作完整 | `test:management-list-contract` | 不删字段、不返回假空数据 | 未执行 |
| 动态快照 | 异步补齐和旧值保留 | snapshot 前后端回归 | 失败不清空列表，恢复后更新 | 未执行 |
| 统计 | 口径与功能完整 | 窗口、额度、分组统计回归 | 页面数字能力保留，写入降载 | 未执行 |
| 记录队列 | 非核心副作用不阻塞 | Redis queue failure 回归 | 超时可丢，功能恢复后继续 | 未执行 |
| 维护 | 小批和单飞 | maintenance 回归 | 清理功能保留，前台不被拖慢 | 未执行 |
| 生产压测 | 管理读写混合 | 15 分钟并发 6 / 12 | 达到性能门槛，无功能回归 | 未执行 |

## 性能验收标准

- [ ] 管理主列表 P50 `<=200ms`、P95 `<=300ms`、P99 `<=800ms`。
- [ ] options P95 `<=300ms`，且不阻塞主列表发起。
- [ ] 动态快照 P95 `<=500ms`，失败不清空已有数据。
- [ ] server event-loop lag P95 `<=200ms`。
- [ ] db-service event-loop lag P95 `<=100ms`。
- [ ] DB service queue wait P95 `<=50ms`。
- [ ] 普通独立 SQL 自动事务数为 `0`。
- [ ] PgBouncer 平均 transaction time `<10ms`。
- [ ] state Redis P99 `<5ms`。
- [ ] 常规 Stream envelope `<=32KB`，最终字段硬上限 `128KiB`。
- [ ] 维护期间管理接口 P95 劣化不超过 `20%`。
- [ ] 15 分钟混合测试错误率 `<0.1%`，且错误不能来自非核心记录队列。

## 功能完整性验收标准

- [ ] 所有现有菜单和页面仍可访问。
- [ ] 所有现有 CRUD、详情、测试、导入导出和管理操作仍可执行。
- [ ] 账户状态、并发、余额、用量和最近使用字段仍可展示。
- [ ] 统计、趋势、TopN、额度和授权消耗页面仍可使用。
- [ ] 审计、操作日志、运行日志、公开接口日志和使用记录页面仍可使用。
- [ ] 依赖失败显示不可用、过期或错误提示，不伪造成功或假空数据。
- [ ] 网关 API Key 校验、路由、调度、上游转发和错误处理语义不变。

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-07-16 | 采用分阶段性能优先方案，不采用配置止血或关闭功能方案 | 用户要求完整治理且不得删减功能 | 三个子计划逐阶段发布 |
| 2026-07-16 | 普通 PostgreSQL 查询不使用显式事务 | 事务包装累计约 4950 万次，成本显著高于实际 SQL | 真实多语句不变量仍保留最小事务 |
| 2026-07-16 | 非核心记录允许在过载时丢失 | 项目不要求金融级耐久性，前台可用性优先 | 必须保留 drop 指标和恢复能力 |
| 2026-07-16 | 动态字段异步补齐但功能完整保留 | 列表不应等待并发、余额、用量等附加依赖 | 页面保留最后成功值和新鲜度标记 |
| 2026-07-16 | PLAN-0079 的“非核心事实绝不丢、队列失败阻断”不再作为默认策略 | 与当前业务标准冲突 | 核心业务事实与非核心记录分级处理 |

## 发布与回退

1. 先发布观测和回归，不改变运行行为。
2. 只拆 queue Redis，异常时恢复旧 URL。
3. 发布普通 SQL 去事务，异常时回滚发布包，不做 schema 回退。
4. 发布 options 解耦和动态快照，异常时回滚同一前后端发布包。
5. 发布队列 best-effort 和 payload 引用化；回退时允许丢弃队列 backlog，不增加双写兼容。
6. 发布统计批量 / 增量和维护降载；异常时停对应 job，清空可重建窗口后离线重建。
7. 所有阶段稳定后更新长期架构和部署基线。

任何阶段出现核心 CRUD、网关调度、额度判断绕过、持续 HTTP 5xx、页面字段缺失或操作入口不可用，立即停止后续发布并回退当前阶段。

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-07-16 | 待开始 | AI | 完成生产只读排障、多 Agent 分域设计、根因报告和总实施计划；尚未修改业务代码或生产配置。 |

## 验证记录

- 本轮只制定方案，没有修改业务代码，因此没有把类型检查、构建或业务回归宣称为已通过。
- 已完成生产只读接口、事件循环、PostgreSQL、PgBouncer、Redis 和进程采样；详细证据见关联报告。
- 方案尚未实施，所有性能目标和功能完整性门禁需要在各子计划执行时验证。

## 完成总结

- 当前状态：实施计划已设计，代码和生产改造尚未开始。
- 计划交付：根因报告、总计划和三个可独立实施的子计划。
- 关闭条件：三个子计划完成，功能完整性矩阵全部通过，生产持续验证达到性能门槛，并更新长期架构与部署文档。
