# PLAN-20260727T111830372Z 统一会话 Header 与审计串联

## 状态

阶段一已完成（未部署）

设计依据：[统一会话 Header 与审计串联设计](../functions/统一会话身份与审计串联设计.md)。

## 目标

只基于模型厂商官方客户端的稳定会话请求头建立客户端专属 resolver，把厂商 Header 投影为内部 `sessionId`，按客户端隔离普通/混合路由亲和，并用审计 `session_id` 串联一个会话的全部请求。

## 完成项

- [x] 调研 Codex、Claude Code、Gemini CLI 及主流中转的会话 Header 行为。
- [x] 明确没有跨厂商通用行业 Header，不提供 generic Header 粘黏。
- [x] 按客户端画像分别接入 Codex `session-id` 和 Claude Code `x-claude-code-session-id`。
- [x] 明确排除 body、thread、turn、agent、request、continuation 与 cache 标识。
- [x] 完成重复 Header 冲突、长度/控制字符校验与版本化 HMAC canonicalizer。
- [x] 普通/混合路由统一消费 request identity，完成首次并发 claim 和条件解绑。
- [x] 审计增加 `session_id` / `session_client_type`、精确查询、SQLite/PostgreSQL 迁移及管理端筛选。
- [x] 成功请求保留轻量 audit envelope，payload / attempts 独立采样与保留。
- [x] 会话 Header 与审计 payload 身份字段脱敏，转发请求保持不变。

## 验收边界

- 已确认客户端有合法 Header：记录 `session_id`，生成客户端隔离的亲和键并参与审计聚合。
- 无 Header：`missing`，正常调度，不承诺粘黏。
- Header 冲突或非法：不生成 key、不污染亲和，审计记录冲突状态。
- Gemini CLI 等没有官方会话 Header 的客户端：本阶段明确不支持会话串联。

## 验证记录

- `test:session-identity`：客户端专属 Header、generic Header ignored、body ignored、冲突和 HMAC 隔离。
- `test:session-affinity-identity-integration`：统一 identity 接入亲和及并发 claim。
- `test:codex-turn-switch-e2e`：真实 Header 会话切号、失败解绑与 64 路并发。
- `test:audit-log-session-correlation`、`test:audit-log-retention`、`test:audit-log-list-projection`：会话查询与父记录保留。
- `test:postgres-schema-sql`：PostgreSQL 列和索引；SQLite 旧表升级由会话串联回归覆盖。
- `test:audit-session-identity-redaction`：Header / payload 审计副本脱敏。
- 前端 `test:audit-log-session-identity`：精确筛选、列表和详情字段。

## 后续扩展规则

只有发现新的模型厂商官方、稳定、请求 Header 会话 ID 时才新增 resolver。body 或其他层级 ID 若未来要支持，必须另立需求，不在本阶段隐式扩张。
