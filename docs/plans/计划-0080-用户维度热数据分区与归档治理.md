# PLAN-0080 用户维度热数据分区与归档治理

## 基本信息

- 编号：PLAN-0080
- 状态：草稿
- 创建时间：2026-07-05
- 更新时间：2026-07-05
- 需求来源：用户对话
- 执行者：AI / 维护者
- 关联模块：前端 / 后端 / 存储 / SQLite / PostgreSQL / 统计 / 日志 / 审计 / 表监控 / 文档 / 验证

## 需求目标

- 背景：生产表监控显示 `usage_record_shard_entries`、`usage_records`、`usage_scope_range_windows` 等表增长快，且索引体积明显大于表体积。当前很多索引服务低频全局筛选，但产品使用场景以用户维度为主。
- 目标：建立长期热数据治理模型，把高增长表统一收敛到“用户维度 + 日期窗口 + 分区 / shard + 归档 + 索引白名单”。
- 交付物：长期设计文档、查询契约、schema / 索引调整、SQLite 与 PG 双模式落地、归档与表监控增强、回归和压测记录。

## 范围边界

### 本次包含

- [ ] 高增长表查询契约收敛：用户必选、默认今天、cursor 翻页。
- [ ] `usage_records` SQLite 日期 shard 查询优化和 PG 日分区改造。
- [ ] `usage_record_shard_entries` 索引瘦身；PG 模式移除实体 catalog 依赖。
- [ ] `usage_scope_range_windows` 从全范围物化改为热窗口 + 按需窗口。
- [ ] 审计、日志、公开接口日志、统计桶、IP 统计、系统监控和表监控的表级治理矩阵。
- [ ] 在线保留、归档、manifest、手动清空和恢复边界。
- [ ] 表监控索引膨胀、分区状态、归档增长和清理追平告警。
- [ ] SQLite standalone 与 PostgreSQL performance 双模式验证。

### 本次不包含

- 不引入 ClickHouse、Kafka、RabbitMQ、外部 OLAP 或新的分布式数据库。
- 不为旧表结构保留运行时双读、双写或自动迁移。
- 不保证管理员全局 raw 明细无条件浏览；管理员全局视图改为预聚合摘要。
- 不把归档数据自动回写热表；恢复归档必须是显式维护动作。

## 关联文档

- 架构文档：`docs/architecture/架构总览.md`
- 长期设计：`docs/functions/用户维度热数据分区与归档治理设计.md`
- 存储说明：`docs/functions/SQLite存储说明.md`
- SQLite 分片：`docs/functions/数据集库分片写入设计.md`
- PG 高性能模式：`docs/functions/PostgreSQL与Redis高性能模式设计.md`
- 统计聚合：`docs/functions/统计指标与分层聚合设计.md`
- 表监控：`docs/functions/表数据监控设计.md`
- 可靠统计：`docs/functions/可靠统计与读写资源隔离设计.md`

## 方案概述

- 方案原则：高增长表先按用户和日期收敛，再做筛选；默认今天；近期在线最多 7 天；历史进入归档。
- 数据变化：拆分 raw 明细在线保留和归档保留设置；PG 高增长表改时间分区；SQLite 高增长明细使用日期 segment / shard。
- 接口变化：高增长 raw 列表接口要求 `systemAccountId` 和日期 cursor，不再返回精确全量 `total`。
- 前端变化：管理员进入高增长 raw 页面先选择用户；未选用户时只展示摘要和用户选择器。
- 后端变化：repository 禁止无用户 / 无日期窗口扫大表；统计窗口由全范围物化改为热窗口 + 按需后台生成。
- 数据处理策略：上线窗口按当前 schema 离线归档或重建旧数据；运行路径不保留旧结构兼容分支。

## 执行拆解

- [x] 建立长期设计文档。
- [ ] 更新高增长页面查询契约和前端用户选择器。
- [ ] 重写使用记录列表 cursor，默认今天并按日期回退。
- [ ] 裁剪 `usage_record_shard_entries` 和 usage shard 内低频索引。
- [ ] PostgreSQL `usage_records` 日分区改造，移除 PG catalog 列表依赖。
- [ ] 范围窗口表改 `window_key` 热窗口和按需窗口请求队列。
- [ ] 审计 / 日志 / 公开接口日志按表级矩阵收敛索引和保留策略。
- [ ] 新增归档 manifest、seal / detach / archive / delete 任务。
- [ ] 增强表监控，展示索引膨胀、分区状态和归档增长。
- [ ] 补充 SQLite 与 PG 回归、EXPLAIN 门禁和压测报告。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 类型检查 | `pnpm typecheck` | 前后端类型检查通过 | 未执行 | 文档阶段未执行 |
| 命令类验证 | 构建验证 | `pnpm build` | 前后端构建通过 | 未执行 | 文档阶段未执行 |
| 查询契约 | 管理员未选用户访问使用记录 | 接口和页面手动验证 | 不查 raw 表，提示先选择用户 | 未执行 | 待实现 |
| 查询契约 | 使用记录默认列表 | 打开使用记录页 | 默认只查当前用户今天数据 | 未执行 | 待实现 |
| SQLite 回归 | 跨天 cursor 翻页 | 新增回归脚本 | 今天耗尽后按 cursor 查上一天，不扫全 shard | 未执行 | 待实现 |
| PostgreSQL 回归 | usage 分区写入和查询 | PG smoke + EXPLAIN | 新数据进入当天分区，查询命中用户 + 日期局部索引 | 未执行 | 待实现 |
| 索引门禁 | 大表索引白名单 | schema SQL 回归 | 不存在未登记的全局低频索引 | 未执行 | 待实现 |
| 范围窗口 | 热窗口刷新 | stats-worker 回归 | 只生成固定热窗口 | 未执行 | 待实现 |
| 范围窗口 | 自定义范围按需生成 | API + worker 回归 | 请求登记后台生成，接口不现场聚合 | 未执行 | 待实现 |
| 归档 | 旧数据 seal / archive | 维护脚本回归 | 在线表释放，archive manifest 可见 | 未执行 | 待实现 |
| 表监控 | 索引膨胀告警 | 表监控回归 | 能识别索引 / 表大小比异常 | 未执行 | 待实现 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-07-05 | 草稿 | AI | 根据生产表增长和用户维度使用场景创建长期治理计划，新增功能设计文档。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-07-05 | 高增长 raw 列表必须先限定用户和日期 | 产品实际使用以用户维度为主，低频管理员全局浏览不应拖累高频写入 | 前端管理员入口需要先选择用户；后端禁止无界 raw 查询 |
| 2026-07-05 | PG 使用记录走时间分区，不复制 SQLite catalog 模型 | PG 同库内继续维护 `usage_record_shard_entries` 会造成双写和双索引 | PG 列表改直接读分区 `usage_records` |
| 2026-07-05 | 范围窗口改热窗口 + 按需窗口 | 31 天任意范围会生成 496 个窗口，随 scope 数线性放大 | 页面自定义范围需要后台生成状态 |

## 验收标准

- [ ] 高增长 raw 页面和接口都不能默认查询全部用户。
- [ ] 默认使用记录、审计、日志等列表只查询今天或明确日期窗口。
- [ ] 大表索引符合白名单，低频全局索引已删除或有例外登记。
- [ ] SQLite standalone 下 usage 查询按日期 shard cursor 运行。
- [ ] PostgreSQL performance 下 `usage_records` 使用日分区，过期数据可 detach / drop。
- [ ] 范围窗口不再全量生成 31 天任意范围组合。
- [ ] 归档 manifest、手动清空和恢复边界明确。
- [ ] 表监控能展示索引膨胀、分区状态和归档增长。
- [ ] 必要回归、EXPLAIN 门禁和压测记录完成。

## 验证记录

- 类型检查：未执行，命令：`pnpm typecheck`
- 构建：未执行，命令：`pnpm build`
- 单项验证：未执行，说明：当前只落地设计文档。
- 手动验证：未执行，说明：待方案确认后实现。
- 测试项结果：未执行，说明：待代码阶段补充。
- 未验证项：全部代码路径未验证。

## 风险与注意事项

- 删除索引会改变低频筛选体验，需要前端明确日期窗口和按需诊断入口。
- PG 分区涉及主键 / 唯一约束重设计，不能在运行路径保留旧表兜底。
- 归档数据不参与常规页面查询；恢复归档必须是显式维护动作。
- 管理员全局 raw 明细能力会收紧，需用预聚合摘要和离线导出替代。
- 发布异常处理：上线前备份业务库和配置；统计域、usage shard、PG 分区和归档数据按当前 schema 离线重建或回滚到同一数据契约下的已验证构建。

## 完成总结

- 完成时间：待补充
- 实际完成内容：待补充
- 主要改动位置：待补充
- 验证结果：待补充
- 后续建议：待补充

