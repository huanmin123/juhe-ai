# PLAN-0152-20260722T120806000Z 客户端模型目录动态聚合

## 基本信息

- 计划 ID：PLAN-0152-20260722T120806000Z
- 序号：0152
- 创建时间戳（UTC）：20260722T120806000Z
- 状态：已完成
- 创建时间：2026-07-22
- 需求来源：用户反馈公网 `/models` 仅返回 Gemini，并要求 API Key 聚合其全部绑定供应商模型。
- 执行者：Codex
- 工作分支：`codex/model-catalog-fix`
- 独立工作区：`.worktrees/model-catalog-fix-20260722`

## 需求目标

- 修复 `/models` 默认协议误判。
- 公开模型列表聚合全部已启用真实供应商。
- API Key 模型列表聚合该 Key 全部有效分组绑定的供应商。
- 客户端返回作用域内全部可见、启用、可计价模型，不按发布时间过滤。
- 网关移除 `default/codex` 发布快照依赖，保留 AI Chat 专用快照。
- 完成测试、复查、合并 master 和远程推送；禁止生产上线。

## 范围边界

### 本次包含

- 网关模型列表协议识别。
- API Key 供应商作用域聚合。
- 公开全供应商动态目录。
- 客户端目录全量可用模型、可见性和去重。
- 网关发布快照职责收窄。
- SQLite、PostgreSQL、Redis 和必要的真实上游验证。

### 本次不包含

- 删除计价所需的历史模型。
- 修改正常聊天/生成请求的路由层级。
- 生产部署、数据库生产迁移、服务重启或流量切换。

## 关联文档

- 设计：`docs/superpowers/specs/2026-07-22-客户端模型目录动态聚合设计.md`
- 实施：`docs/superpowers/plans/2026-07-22-客户端模型目录动态聚合实施计划.md`
- 架构：`docs/architecture/架构总览.md`
- 后端：`docs/architecture/backend/README.md`
- 功能：`docs/functions/README.md`
- 模型目录指南：`docs/functions/厂商模型目录更新与清洗指南.md`

## 任务清单

- [x] 从最新 `master` 创建独立 worktree 和 `codex/model-catalog-fix` 分支。
- [x] 落地设计文档并对照实现复核作用域、协议和快照边界。
- [x] 创建实施计划、跟踪计划和任务清单。
- [x] 建立协议识别 RED 回归并修复 `/models` 默认形态。
- [x] 建立客户端目录纯筛选与聚合 RED 回归。
- [x] 实现公开全供应商和 API Key 精确供应商聚合。
- [x] 将网关模型响应从发布快照切换为动态目录。
- [x] 收窄快照服务，仅保留 AI Chat variants。
- [x] 固定新旧模型与缺失发布时间模型均保留的全量边界。
- [x] 同步架构、功能和模型目录文档。
- [x] 合并开发期间最新 `master` 并处理冲突（首次检查无新提交）。
- [x] 完成 SQLite、PostgreSQL、Redis、真实 HTTP/必要上游验证。
- [x] 完成类型检查、构建、完整复查和独立代码审查。
- [x] 提交功能分支，合并 master，重新验证并推送远程。
- [x] 保持未上线，等待用户通知。

## 测试项

| 类型 | 测试项 | 验证方式 | 预期 | 状态 |
| --- | --- | --- | --- | --- |
| 协议 | `/models`、`/v1/models`、`/v1beta/models` 与显式客户端 | 网关回归脚本 | 协议形态正确 | 通过 |
| 作用域 | 公开全部启用供应商 | 客户端目录/HTTP 回归 | 无停用或 hybrid，真实供应商齐全 | 通过 |
| 作用域 | API Key 绑定多供应商 | 真实 HTTP 回归 | 返回绑定供应商合集，不泄漏未绑定供应商 | 通过 |
| 边界 | 新旧和缺失发布时间模型 | 纯函数回归 | 全部保留 | 通过 |
| 边界 | 隐藏、停用、无价、自定义 | 纯函数回归 | 按设计筛选 | 通过 |
| 快照 | 网关动态、AI Chat 快照保留 | 契约与存储回归 | 无 default/codex 网关依赖，chat 正常 | 通过 |
| SQLite | 模型目录和网关回归 | pnpm regression scripts | 通过 | 通过 |
| PostgreSQL/Redis | 隔离 smoke | 测试环境命令 | 通过且不污染其他数据 | 通过 |
| 静态 | 后端 typecheck/build | pnpm | 通过 | 通过 |
| 审查 | 自查 + 独立代码审查 | diff/review | 无未解决重要问题 | 通过 |

## 进度记录

- 2026-07-22：线上证据确认 `/models` 为 Gemini 形态、`/v1/models` 已包含多供应商且仍暴露两年前模型。
- 2026-07-22：根因定位为 `/models` 多协议重叠且 Gemini 判定优先；API Key `providerCodes` 已计算但响应层未消费。
- 2026-07-22：从 `origin/master` 对齐的 `master` 创建独立 worktree；不修改原有脏工作区。
- 2026-07-22：完成设计复核，确认动态网关目录与 AI Chat 快照必须拆分职责。
- 2026-07-22：按用户最终“直接返回全部模型列表”的指令，取消早期最近两年筛选设想；发布时间只排序，不过滤。
- 2026-07-22：协议、动态目录、API Key 跨供应商绑定、发布快照职责和真实 SQLite 六供应商目录回归通过；开始集成环境与完整验证。
- 2026-07-22：合并 `origin/master` 的最新 32 个提交并解决计划编号冲突，随后重跑核心模型目录回归、后端类型检查和全量前后端构建均通过。
- 2026-07-22：隔离 PostgreSQL/Redis chat smoke 通过；认证 Redis 限流回归通过；独立审查未发现阻断合并的运行时问题。
- 2026-07-22：补齐认证真实 HTTP 回归，API Key 绑定 GPT + Gemini 时 `/models` 返回二者合集且不包含 Anthropic；未使用真实上游密钥，因为模型目录响应不调用上游。
- 2026-07-22：功能分支已推送，干净集成工作区基于最新 `origin/master` 生成合并提交 `d5442ac87`；合并提交上的核心回归、根级类型检查和全量构建再次通过。

## 验收标准

- 所有完成标准以实施计划为准。
- 必须有失败前证据、修复后证据和最终 fresh verification。
- master 合并和远程推送成功，但没有任何生产上线动作。

## 风险与注意事项

- 原主工作区存在用户未提交改动，最终合并前必须重新检查；不得通过 reset/checkout/stash 丢失或隐藏用户工作。
- 测试凭据只在进程环境中使用，不输出、不写文件、不提交。
- SQLite/PG 清理仅限已确认的测试数据库或本任务专用表/schema。
- 发布时间只用于排序和响应元数据，不参与本次客户端模型过滤。

## 验证记录

- 通过：`test:gateway-models-response-protocol`、`test:gateway-models-dynamic-http`（含公开和 API Key 多分组请求）、`test:client-model-catalog`、`test:authenticated-models-protection`、`test:published-model-catalog-snapshot-contract`、`test:gateway-api-key-route-query-guard` 和直接模型目录回归。
- 通过：认证模型列表 Redis 限流回归、隔离 PostgreSQL/Redis chat smoke、后端及根级 `typecheck`、根级 `build`、`git diff --check`。
- 已知非本次阻断：`pnpm --filter juhe-ai-backend test:model-catalog` 的第一段 Go 目录种子迁移快照校验仅因已存在的 SHA 不一致失败；实际目录数据行数相同，直接 Node 模型目录回归通过。本次未修改该生成迁移或用户主工作区中的相关改动。

## 完成总结

- `/models` 默认恢复 OpenAI-compatible 形态；明确 Gemini/Anthropic 客户端保留原生响应。
- 公开请求动态聚合全部启用真实供应商，API Key 请求只聚合其全部 active 分组供应商；不再按发布时间过滤模型。
- 网关移除 `default/codex` 发布快照依赖，AI Chat 专用快照继续保留。
- 功能分支和干净 master 集成均完成验证并推送远程；未执行任何生产部署，等待用户通知。
