# PLAN-0146 全系统日志治理

## 基本信息

- 状态：实施中，禁止生产上线
- 创建日期：2026-07-20
- 需求来源：生产网关时间线无法精确定位，用户要求全系统统一日志、失败现场保全和异步非阻塞
- 执行分支：`codex/gateway-request-timing-observability-20260720`
- 设计文档：`docs/superpowers/specs/2026-07-20-全系统日志治理设计.md`
- 详细实施计划：`docs/superpowers/plans/2026-07-20-全系统日志治理实施计划.md`

## 需求目标

- Node、Go、worker、ingest-worker、DB service 统一 trace/request/job 关联、event 字段和 failure class。
- 每个关键阶段默认输出结构化 info，可以重建请求时间线。
- 不可预知失败完整保留 Error/cause/stack 和可用上下文；可预知失败保留决策参数和 reason code。
- 日志写入、轮转、索引、搜索、清理和失败保底不得阻塞业务主路径，队列必须有界。
- 原始审计、操作日志、公开接口日志保持各自权限和保留语义，不复制原文到普通日志。

## 执行清单

- [x] 完成 Node/Go 日志入口、角色和读取链路基线验证，记录既有问题。
- [x] 建立共享 event envelope、trace 关联和失败现场契约，完成 Node/Go 契约回归。
- [x] 建立 Node 有界异步 publisher、log worker、轮转、保留和失败保底（文件/控制台写入、轮转清理、ACK drain 与 runtime-log line 回传已完成）。
- [x] 建立 Go 异步 `slog.Handler`、context 关联和 shutdown drain（异步 Handler、HTTP trace/request 关联和服务退出 drain 已完成）。
- [ ] 完成 HTTP、IPC、Redis stream、worker job、DB service 和 runtime log cursor 关联。
- [x] 完成网关、账户、路由、分组、配额、容量、并发和上游流程阶段埋点（入口、分组权限、客户端画像、候选账户、模型能力、账号避让、配额/容量、上游派发、响应处理）。
- [x] 完成 Go management/public API/worker 关键请求与任务埋点（HTTP 入口/完成、DB service IPC 请求、既有 worker 生命周期日志统一异步出口）。
- [x] 完成文件、Redis、SQLite、PostgreSQL 索引、grep 和保留清理的非阻塞改造（沿用有界 cursor/grep/Redis queue，新增 writer worker 与 line 回传）。
- [x] 完成 SQLite + memory + mock 回归、PostgreSQL + Redis 专属 smoke、失败注入和并发性能对照。
- [ ] 完成两名只读 Agent 复核和交叉质疑，关闭高严重问题。
- [ ] 每个里程碑前 fetch/merge 最新 master，不改写历史。
- [ ] 测试通过后合并到 master、反向合并 feature/20260706-go、提交并 push，不执行生产上线。

## 验证门禁

| 类型 | 命令/证据 | 状态 |
| --- | --- | --- |
| Node | `pnpm typecheck` / `pnpm build` | 2026-07-21 最终门禁均通过（exit 0） |
| Go | `go test ./...` / `go vet ./...` | 2026-07-21 最终门禁均通过（exit 0） |
| 契约 | Node/Go event/failure regression | `test:log-event-contract` 与 `go test ./internal/logging/...` 通过 |
| 非阻塞 | writer 延迟、worker 退出、Redis/PG 不可用注入 | Node/Go 阻塞 writer、writer 关闭、Redis 断线/超时有界降级回归通过 |
| 存储 | SQLite + memory、专属 PG + Redis smoke | mock 网关、runtime-log Redis Stream、audit-log PG smoke 通过 |
| 性能 | 开/关日志交错 p50/p95/p99、event-loop/goroutine、queue/drop | A/B 完成；最终 info queue/drop/error 均为 0，详见验证报告 |
| 安全 | 密码文件不进入代码、日志或提交 | 变更差异和受版本控制文件检查未发现密码文件、连接串或真实凭据；测试凭据仅经进程环境临时注入 |
| 发布 | master 合并、feature 反向合并、远端 push | 未执行 |

## 进度记录

- 2026-07-20：基于 `origin/master` `d4443e8f3` 创建独立 worktree `codex/gateway-request-timing-observability-20260720`。
- 2026-07-20：已提交网关子模块设计、失败日志原则和全系统治理总设计；代码尚未开始。
- 2026-07-20：范围包含 Node + Go + worker/DB service + 日志底座，前端暂不采集完整日志。
- 2026-07-20：实施基线 `dfe4108e9`；`pnpm typecheck` 与 `backend-go/go test ./...` 均通过（exit 0），未发现既有阻塞失败。
- 2026-07-20：完成共享日志契约、失败现场裁剪/脱敏、Node 有界异步发布器、Go 有界异步 Handler、Go HTTP trace/request 关联，以及 Node 网关接收、预检、上游派发和响应阶段埋点。
- 2026-07-20：`test:async-log-publisher`、`test:log-event-contract`、`test:logger-redaction`、`test:log-writer-worker`、`go test ./internal/logging/...`、Go HTTP 关联测试和网关语义策略/代理健康回归通过。
- 2026-07-21：合并最新 `origin/master`（`c770350b9`），并完成 writer worker runtime-log line 回传修正；主分支同步点无冲突。
- 2026-07-21：192.168.1.203 测试库旧 schema 缺字段，按授权重建六个项目 schema；当前 DDL 670 条成功，runtime-log Redis Stream 与 audit-log PostgreSQL smoke 通过。
- 2026-07-21：修正逐条 ACK 造成的日志积压，启用批量出队/批量 worker ACK/批量文件写；最终 5 秒并发压测 `pending=0/drop=0/destinationErrors=0`。
- 2026-07-21：完成 `docs/reports/网关阶段日志与异步写入验证报告-2026-07-21.md`；热路径主要稳定耗时为上游派发，本地路由/模型/账号准备 p95 为 0-1ms。
- 2026-07-21：最终门禁 `pnpm typecheck`、`pnpm build`、`go test ./...`、`go vet ./...` 均通过（exit 0）；12 项日志/网关专项回归全部通过。
- 2026-07-20：OpenAI-compatible gateway E2E 的业务断言执行完成，但 Windows 清理 SQLite 文件时报 `EBUSY`；在未改代码的 `origin/master d4443e8f3` 独立 worktree 中同样复现，判定为既有测试清理问题，未将其误报为本分支通过。

## 上线边界

- 本计划不执行生产部署、重启、切流、证书、反向代理或生产库写入。
- 真实 PostgreSQL/Redis 只使用专属测试数据库、命名空间和可清理队列。
- 真实模型只在 mock 无法覆盖时且获得额外确认后执行。
