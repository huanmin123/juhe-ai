# 计划-20260918T142845703Z-调度状态探活核心系统缺陷修复

## 基本信息

- 计划 ID:`PLAN-20260918T142845703Z`
- 状态:已完成(代码级修复与验证完成;真实上游/生产验证不在范围)
- 创建时间:2026-09-18(UTC `20260918T142845703Z`)
- 需求来源:用户要求对调度系统、账户状态系统、探活恢复系统做缺陷复审,并"写一个计划文档,然后根据计划来执行"。
- 执行者:主代理 + ZCode 子代理(写入统一 `glm_5.3_flash_max`,取证/复审只读角色)。
- 证据基线:2026-09-18 三大核心系统复审(四路并行取证 + 两轮独立复核,结论见本计划"证据摘要")。行号以当前工作区(含未提交改动)为准。

## 需求目标

修复复审确认的生产可达缺陷,消除三类客户端可感知风险:

1. 换 Key 重取并发槽失败路径的 nil `Release` panic 与双重释放(P0-1)。
2. 锁死死亡确认写 `temporary_unavailable` 缺 cooldown fence,导致恢复探针永久拒绝接管、账户只能人工恢复(P0-2)。
3. SSE 等待心跳 goroutine 与主流水线对同一 `http.ResponseWriter` 的并发写窄窗数据竞争(P1-1)。

同时完成低风险加固项,并对 P0-3(进程内软阻断/precheck/half-open 写面未接线)做出有据处置:以 Node 归档与历史计划对照判定"迁移遗漏"还是"未完成特性",再决定接线或登记退场。

## 范围边界

### 本次做

- W1 调度派发:`gatewaydispatch` reacquire 失败出口修复 + `attemptedFingerprints` 死代码清理 + 回归测试。
- W2 网关组合根:`chain_account_locks.go` settle 补 fence;`chain_compose.go` 接线 `AccountApiKeyRequestAttemptSafetyLimit` env 覆盖;`chain_ports.go` 失败分类传入 statusCode + 定向测试。
- W3 流式返回:`gatewayresponse/heartbeat.go` Stop 等待 goroutine 退出 + 竞态回归测试。
- W4 账户批量编辑:`accounts/batch.go` 过期停用补清 `cooldown_retest_generation` + 测试。
- W5 P0-3 处置取证(只读):Node 归档与 PLAN-20260718T092139001Z 对照,判定写面缺席的预期语义。
- W6(条件执行,依赖 W5 结论 + W2 完成):若判定为"Node 生产已接线、Go 迁移遗漏",实施 `engine.AccountState` 装配与 `FilterSuppressions` 消费 `AcquirePrecheckHalfOpenLease`;若判定为"Node 侧本就未完成/未启用",不做接线,在本计划登记缺口并同步 `docs/functions/AI账户运行态探针恢复设计` 的当前事实说明,后续另立计划。

### 本次不做(非目标)

- OAuth token 保活的 gateway 侧惰性刷新兜底:涉及部署配置面决策,另立计划。
- outbox 堆积量监控、jobs runner 心跳暴露:observability 特性,另立计划。
- 图像 lane 瞬态失败同 Key 有界重试:与 Node parity 相关,需产品语义确认,不做静默改动。
- DNS/dial 阶段失败与"已到达上游 transport 失败"的拆分:错误语义变更,需独立设计。
- 五项性能优化(热质量 N+1、preparation 重复拉取、latency_degraded 批量读、J1 扫描固定成本、路由状态注册表锁内扫描):归入 `PLAN-20260918T064119294Z` 后端架构与性能优化改革,本计划不改。
- `business/account_runtime` 死代码包删除:单独清理决策,本计划不动。
- 不提交 git、不构建发布产物、不部署、不启动任何生产或共享开发服务。

## 关联文档

- `docs/functions/AI账户错误语义与状态变更边界.md`(状态写入授权边界)
- `docs/functions/AI账户运行态探针恢复设计.md`(precheck/半开/恢复语义)
- `docs/functions/AI账户锁死调度机制设计与实现前复审.md`(锁死字段与运行态契约)
- `docs/plans/计划-20260718T092139001Z-后台探针确认软阻断与受控半开.md`(W5 判据来源之一)
- `docs/plans/计划-20260918T064119294Z-后端架构与性能优化改革.md`(性能项承接方)
- `migration-backup/node/final-archive/`(只读对照,禁止恢复/修改/运行)

## 证据摘要(缺陷定位,行号为复审时点)

| # | 缺陷 | 证据 |
| --- | --- | --- |
| P0-1 | reacquire 双失败出口:err 出口对已释放旧槽二次 Release(非幂等);notAcquired 出口槽 `Release=nil`,循环出口调用 nil 函数 panic | `gatewaydispatch/dispatchsingle.go:199-225,429,579-586,626-629`;生产槽 `cmd/juhe-ai-gateway/chain_dispatch.go:234-258`;首取路径 `dispatchsingle.go:155-161` |
| P0-2 | 锁死 settle 写 `temporary_unavailable` 仅 3 列、cooldown 即时过期、无 fence;jobs J1 强制 fence,失败隔离 5 分钟一轮,无自动恢复路径 | `cmd/juhe-ai-gateway/chain_account_locks.go:527-529`;对照 `chain_error_policy_effects.go:528-546`;`jobs/internal/accounthealth/direct_input.go:168-170`、`scheduler.go:972-980` |
| P1-1 | 心跳 goroutine 在途写与等待恢复后主流水线首写可能并发写同一 writer;`TrackingWriter`/`DownstreamCommitState` 无锁 | `gatewayresponse/heartbeat.go:53-77,179-203`;`pipehelpers.go:47-69`;`gatewaypreauth/responses.go:220-261` |
| P0-3 | `engine.AccountState` 生产未装配(仅测试赋值),`LocalSuppressionStore` 写面无生产调用者,读面(过滤/半开/恢复等待)空转;`AcquirePrecheckHalfOpenLease` 无人消费 | `gatewaydispatch/ports.go:838-849`;`accountpreparation.go:131-158`;`gatewaycircuit/suppression.go:231,310,672-744`;组合根 `chain_compose.go:418-563` |
| P2-a | 批量编辑过期停用漏清 `cooldown_retest_generation`(单账户路径有) | `accounts/batch.go:1281-1287` vs `accounts/patch.go:1288-1293` |
| P2-b | `AccountApiKeyRequestAttemptSafetyLimit` 注释要求组合根用 env 覆盖,实际未做,恒 5000 | `gatewaydispatch/engine.go:44-53`;`chain_compose.go:418-432` |
| P2-c | 失败分类日志只传 Phase,`metricReasonClass` 恒 unknown,statusCode 已可得未传 | `cmd/juhe-ai-gateway/chain_ports.go:359-364,387-389,649-651`;`gatewayresponse/failclass.go:68-98` |
| P2-d | `attemptedFingerprints` 构建后 `_ =` 弃用 | `gatewaydispatch/upstreamdispatch.go:509-531` |

## 执行拆解

- [x] T0 计划文档落盘并更新 `docs/plans/README.md` 索引。
- [x] T1(W1)修复 `dispatchsingle.go` reacquire 失败出口:消除 panic 与双重释放;清理 `upstreamdispatch.go` 死代码;新增回归测试(未获取槽 `Release=nil` 不 panic;旧槽恰好释放一次)。
- [x] T2(W2)锁死 settle 补 fence 三件套(对齐 jobs `validCooldownFence` 判定与生产桥列族);接线 `AccountApiKeyRequestAttemptSafetyLimit`;失败分类传 statusCode;各补定向测试。
- [x] T3(W3)心跳 `Stop` 等待 goroutine 退出;`-race` 回归测试。
- [x] T4(W4)批量编辑过期停用补清 `cooldown_retest_generation`;测试(复用既有 `TestW2BatchUpdateExpiredPackageDisables` 断言)。
- [x] T5(W5)Node 归档 + 历史计划对照,产出 P0-3 处置结论(结论 B,见进度记录)。
- [x] T6(W6)按 T5 结论执行登记路线:不接线,缺口与后续建议落盘"W6 处置决策"。
- [x] T7 集成验证:`backend-go` 三模块 `go build ./...`、四包 `go vet`、改动包测试全绿(见验证记录)。
- [x] T8 复审(Critique):独立复审子代理两次被环境中断,改由主代理按同一清单自查(记录见"T8 复审记录";建议环境稳定后补独立复审)。
- [x] T9 更新计划状态、验证记录与完成总结;P0-3 结论按登记路线落盘本文档,functions 文档经核对无需修改。

## 测试项

| 类型 | 测试项 | 验证方式 | 预期结果 | 状态 |
| --- | --- | --- | --- | --- |
| 基线 | 各改动包修复前测试基线 | 环境多次重启,未单独留基线 | 以修复后全量包测试全绿替代 | 已通过(替代口径) |
| 单元(W1) | reacquire 未获取槽(Release=nil)不 panic | `go test -run TestReacquireNotAcquiredNilReleaseSlotDoesNotPanic` | 优雅返回错误,无 panic | 已通过 |
| 单元(W1) | 旧槽双重释放消除 | `go test -run TestReacquireAcquireErrorReleasesOldSlotExactlyOnce`(计数闭包) | 显式释放后总释放次数恰为 1 | 已通过 |
| 单元(W1) | `attemptedFingerprints` 死代码清理 | `rg` 全仓零残留 + 包构建 + 全量包测试 | 行为无变化 | 已通过 |
| 单元(W2) | 锁死 settle fence 落库 | `TestChainAccountLocksSettleDeadline` 断言 observation/generation/failure_count/3s 退避 | fence 完整,jobs J1 可接管 | 已通过 |
| 单元(W2) | safety limit 随 env 装配 | `chain_compose.go:438-439` 装配 + cmd 定向测试 | env globalMax 生效(>0 时覆盖) | 已通过 |
| 单元(W2) | metricReasonClass 含状态码 | `hasStatus` 时传入 StatusCode;`TestChainFailureDispatch` 族 | 分类不再是恒 unknown | 已通过 |
| 单元(W3) | 心跳 Stop 后无在途写 | `go test -race ./internal/gatewayresponse/ -run TestW3Heartbeat`(3 用例,含并发 Stop) | Stop 返回即 goroutine 已退出,无数据竞争 | 已通过 |
| 单元(W4) | 批量过期停用清 generation | `TestW2BatchUpdateExpiredPackageDisables` 断言两行 generation IS NULL | 与单账户路径列族一致 | 已通过 |
| 集成 | 全量构建 + vet | 三模块 `go build ./...`、四包 `go vet` | 通过 | 已通过 |
| 回归 | 相邻行为不变 | gatewaydispatch(45s)/gatewayresponse(-race,7s)/accounts(36s) 全量 + cmd 定向 | 全部 ok,无新增失败 | 已通过 |
| 不适用 | 真实上游/生产验证 | 本计划为代码级修复 | 未执行,风险见下 | 不适用 |

## 验收标准

1. T1-T4 全部落地且对应测试通过;修复前基线中已有的失败不新增。
2. P0-1 两条失败出口语义与首取路径一致:未获取不调用 nil Release、已显式释放的槽不被二次释放。
3. P0-2 修复后,锁死 DEAD_CONFIRMED 的 `temporary_unavailable` 账户在 jobs J1 侧可通过 `validCooldownFence`(以代码级测试证明,不需要真实 J1 运行)。
4. W3 修复后 `go test -race ./internal/gatewayresponse/` 无竞争报告。
5. W5 产出明确结论(三选一:Node 生产已接线 / Node 未接线 / 证据不足),并按结论执行 W6 或登记;不允许"半装状态"无记录延续。
6. 全部改动不超出各工作包分配文件范围;工作区中他人的未提交改动不被覆盖或回滚。

## 风险与注意事项

- 工作区存在大量既有未提交改动(前序会话产物):所有写入前必须以当前文件为基准做最小编辑,禁止整文件重写式覆盖;禁止 `git add`/`commit`/`reset`。
- `releaseTransientState` 可能承担并发槽之外的清理(W1 实施时必须完整读取该闭包,不能只处理槽;其他清理需保留或证明幂等)。
- P0-2 修复的 fence 值必须满足 jobs `validCooldownFence` 的完整判定(含可能的 config revision 匹配),以 jobs 侧代码为准,不是照抄列名。
- W3 若 Stop 改为同步等待,需确认所有调用方在请求 goroutine 上、可容忍微秒级阻塞;不改 `TrackingWriter` 语义。
- W6 接线路线涉及跨 gateway 运行态装配与并发语义,若 T5 结论为接线但涉及面超过本计划边界,允许收缩为"登记 + 另立计划",不强行实施。
- 本计划不做真实上游、生产或共享开发环境验证;上线路径与部署门禁不在授权范围内。

## 进度记录

- 2026-09-18:复审完成(四路取证 + 两轮复核),计划创建,进入执行。
- 2026-09-18:T1-T4 全部落地(W1 reacquire 出口修复 + `attemptedFingerprints` 死代码删除 + `reacquirepaths_test.go`;W2 锁死 settle 补 fence 三件套 + safety limit env 覆盖 + statusCode 传入分类;W3 心跳 Stop 同步等待退出 + `w3_heartbeat_test.go`;W4 批量过期停用补清 generation,断言由既有 `TestW2BatchUpdateExpiredPackageDisables` 承载)。执行中环境多次重启,子代理中断后由主代理接手完成验证与 W5 取证;写入代理的全部编辑在中断前已落盘,经逐项核对无半成品。
- 2026-09-18:T5/W5 取证结论(主代理执行,子代理两次被环境中断):**结论 B——"请求失败→进程内软阻断/precheck 写面"在 Node 生产链路本就未接线,Go 当前行为与 Node 终态一致,非迁移遗漏**。证据:① `docs/functions/AI账户运行态探针恢复设计.md:74`"普通请求在任何时间都不能直接写 `runtime_degraded`、`precheck_pending` 或 `temporary_unavailable`";② Node 归档 `upstream-dispatch.ts:377`"suppression and health state transitions are reserved for background probes";③ Node `recordGatewayAccountFailureForPrecheck` 生产零调用(仅回归脚本与 ForTest 变体);④ Node 终态归档已删除 `local-suppression-preflight.ts`(`dispatch/preparation.ts:32` 残留悬空导入),`inspection-runtime-effects.ts` 生产零引用——Node 末期正在拆除该机制;⑤ Go 生产已有等价避让层(传输失败 circuit 运行态 `upstreamdispatch.go:787-796`、显式策略 `chain_dispatch.go:686-702`、DB 错误策略、Key 级 guard)。

## W6 处置决策(按 T5 结论执行"登记"路线)

- 不实施 `engine.AccountState` 写面接线。`AccountStateMutations` 端口、`gatewaycircuit.LocalSuppressionStore` 写面(`SuppressForGatewayFailure`/`DegradeForGatewayFailure`)、`AcquirePrecheckHalfOpenLease` 选项、`gatewayaccounteffects.SideEffectsService`、`business/account_runtime` 包共同构成"Node 未接线机制 + Go 迁移残留"的死代码群;接线会违反"普通请求不写 precheck_pending"的现行设计边界。
- 登记缺口(后续另立清理计划,不在本计划执行):① 删除或显式标注上述死端口/死代码群,消除"读面已装、写面静默缺失"的半装状态;② 若未来需要"全池软阻断时放行单个真实请求"的分组级半开,需按探针恢复设计文档重新设计并走独立评审,不得复用本死代码;③ 组合根为"读面已装配"的机制补充一行 startup 启用状态日志,避免静默空转再次不可见。
- `docs/functions/AI账户运行态探针恢复设计.md` 无需修改:其"仅后台探针/显式策略可建立 precheck_pending"的表述与 Go 现状一致;冲突仅在死代码本身,归代码清理。

## 验证记录

全部命令在 `F:\sub2api-lite\backend-go` 下执行:

| 验证 | 命令 | 结果 |
| --- | --- | --- |
| 全量构建 | `go build ./...`(gateway / jobs / maintenance 三模块) | 全部 OK |
| 静态检查 | `go vet` 四个改动包 | OK |
| W1 新测试 | `go test ./internal/gatewaydispatch/ -run TestReacquire -v` | 2 用例 PASS |
| W1 回归 | `go test ./internal/gatewaydispatch/ -count=1` | ok(44.9s) |
| W3 新测试 | `go test -race ./internal/gatewayresponse/ -run TestW3Heartbeat -v` | 3 用例 PASS(含并发 Stop) |
| W3 回归 | `go test -race ./internal/gatewayresponse/ -count=1` | ok(7.2s,无竞争报告) |
| W4 断言 | `go test ./internal/accounts/ -run TestW2BatchUpdateExpiredPackageDisables -v` | PASS(断言两行 generation IS NULL) |
| W4 回归 | `go test ./internal/accounts/ -count=1` | ok(36.2s) |
| W2 定向 | `go test ./cmd/juhe-ai-gateway/ -run 'TestChainFailureDispatch\|TestChainAccountLocks'` | ok(锁死 settle fence 断言含 observation/generation/failure_count/3s 退避) |

说明:各包修复前未单独跑基线(执行窗口环境多次重启);以"修复后全量包测试通过 + 新增测试通过"作为回归证据。gatewaydispatch/gatewayresponse/accounts 三包全量通过本身覆盖了包内既有用例无回归。

## T8 复审记录(Critique)

执行期间环境多次重启,独立复审子代理两次被截断,改由主代理按同一清单逐项自查(已在进度记录如实标注;建议后续环境稳定时补一轮独立复审):

- A1(W1)通过:reacquire 三个出口与首取路径清理语义一致;两处 key-model 拒绝点先中和 Release 再置标记,旧槽恰好一次有效释放;`releaseTransientState` 的 halfOpenLease 清理未受伤;`attemptedFingerprints` 全仓零残留(`rg` exit=1)。
- A2(W2)通过:settle UPDATE 保持 `status='active' AND schedulable=1` WHERE;fence 三要素满足 `validCooldownFence`,SourceConfigRevision 分支读写同源(结算未触碰 `config_revision` 列,探针读同行自然匹配);3 秒初始退避与 `chain_error_policy_effects.go` 生产正路一致;不伪造 `last_error_*` 有注释论证。
- A3(W3)通过:Stop 无锁等待 done,goroutine 退出只依赖 ctx/signal/语义提交/写失败,无死锁;多次/并发 Stop 与交错 Start 语义正确;心跳内容、15s 间隔、首拍立即写出、中止条件全部未变;`-race` 包全量通过。
- A4(W4)通过:过期停用列族与单账户 `expiredByPackage` 臂对齐(generation 补齐,断言在 `TestW2BatchUpdateExpiredPackageDisables`);未见其他列族不对称。
- B 通过:`reacquirepaths_test.go` 的 fake 贴近生产(未获取槽 Release=nil)且以计数闭包断言"恰好一次";`w3_heartbeat_test.go` 断言 Stop 返回后无新写、在途写被排干、并发 Stop 安全。
- C 通过:修复窗口(本地 22:35-22:40)内被触碰文件与授权清单吻合;`w16b_boot_leak_test.go`(22:35)经内容核对为前序会话 w16b 覆盖率批次产物,非本轮,未动。
- D 通过:主代理已实跑全部新测试 + 四包全量(其中 gatewayresponse 带 `-race`)+ 三模块 build + 四包 vet(见验证记录)。

## 完成总结

- 落地修复:P0-1 派发 reacquire nil panic / 双重释放(`dispatchsingle.go`,+2 回归测试);P0-2 锁死 DEAD_CONFIRMED settle 补完整冷却复测 fence,恢复链重新闭合(`chain_account_locks.go`,断言进 `chain_account_locks_test.go`);P1-1 SSE 心跳在途写与主流水线并发写窄窗(`heartbeat.go` Stop 同步化,+3 race 回归测试);P2-a 批量过期停用列族补齐(`batch.go`);P2-b safety limit 按 env 装配(`chain_compose.go`);P2-c 失败分类携带 statusCode,`metricReasonClass` 不再恒 unknown(`chain_ports.go`);P2-d `attemptedFingerprints` 死代码删除(`upstreamdispatch.go`)。
- P0-3 处置:取证结论 B(Node 侧本就未接线、Go 行为与 Node 终态一致),按计划走登记路线,未接线;死代码群清理与"组合根启用状态日志"登记为后续独立计划(见"W6 处置决策")。
- 验证:三模块 `go build` OK;四包 `go vet` OK;gatewaydispatch / gatewayresponse(-race) / accounts 包全量测试 ok;cmd 定向(锁死 + 失败派发)ok;7 个新增回归用例全部通过。
- 未做与后续:真实上游 / 生产验证不在本计划;性能优化五项归 `PLAN-20260918T064119294Z`;OAuth 保活兜底、outbox 堆积监控、图像 lane 同 Key 重试语义、dial 阶段失败拆分、P0-3 死代码群清理均需另立计划;建议环境稳定后补一轮独立复审子代理复核。
