# PLAN-0159-20260722T174032816Z 官方可落地多供应商 OAuth 账户接入

## 基本信息

- 计划 ID：PLAN-0159-20260722T174032816Z
- 序号：0159
- 创建时间戳（UTC）：20260722T174032816Z
- 状态：进行中
- 创建时间：2026-07-22
- 更新时间：2026-07-22
- 需求来源：用户对话
- 执行者：AI
- 工作分支：`codex/oauth-matrix-safe-20260722`
- 独立工作区：`C:\Users\Administrator\.codex\worktrees\sub2api-lite\oauth-matrix-safe-20260722`
- 关联模块：前端、后端、存储、网关、测试、文档

## 需求目标

- 在隔离 worktree 内完成当前可独立落地的 OAuth 账户矩阵收口。
- 保留 OpenAI 现有管理式 OAuth。
- 复用 Gemini 现有导入型 OAuth。
- 新增 Anthropic Bearer Token 型 OAuth 账户支持。
- 完成文档、回归、周期性合并 `master`、最终合并回本地 `master` 的完整闭环。

## 范围边界

### 本次包含

- 新 worktree 开发，不污染当前 `master` 工作区。
- 先落设计文档、计划文档、任务清单，再进入代码实现。
- 后端 `oauth` 能力按供应商 profile 收口。
- 前端账户表单、能力判断、endpoint modes、文案与校验同步收口。
- 使用测试 PostgreSQL / Redis、必要时使用真实模型密钥做最小 E2E。
- 开发期间按阶段合并本地最新 `master`，降低最终冲突成本。
- 完成后把开发分支合并回本地 `master` 并处理冲突。

### 本次不包含

- 复制 CLIProxyAPI / sub2api_source 的私有订阅代理内核。
- 第三方 client identity、cookie/session、TLS 指纹伪装或浏览器绕行。
- 生产部署、线上改配置、连生产库、连生产 Redis。

## 关联文档

- [官方可落地 OAuth 账户接入设计](../functions/官方可落地OAuth账户接入设计.md)
- [官方可落地多供应商 OAuth 账户接入任务清单](2026-07-22-官方可落地多供应商OAuth账户接入任务清单.md)
- [供应商订阅认证接入安全设计](../functions/供应商订阅认证接入安全设计.md)
- [Anthropic 账号接入](../functions/Anthropic账号接入.md)
- [Gemini 账号接入](../functions/Gemini账号接入.md)
- [OpenAI 账号接入](../functions/OpenAI账号接入.md)
- [PLAN-0157-20260722T145435019Z 供应商订阅认证安全扩展](计划-0157-20260722T145435019Z-供应商订阅认证安全扩展.md)
- [PLAN-0158-20260722T155636479Z CLIProxyAPI 本地 Sidecar 接入](计划-0158-20260722T155636479Z-CLIProxyAPI本地Sidecar接入.md)

## 方案概述

- 继续使用统一 `oauth` 账户类型，但以供应商协议档案决定凭据、endpoint modes、测试方式和上游鉴权。
- OpenAI 保持管理式授权流；Gemini 与 Anthropic 使用导入型凭据录入。
- Anthropic 新增 `oauth` 账户可调度能力，上游认证为 `Authorization: Bearer <access_token>`。
- 前端从“是否支持管理式 OAuth”与“是否支持 OAuth 账户类型”两个维度拆开能力判断。

## 任务清单

- [x] 从本地 `master` 创建隔离 worktree 和开发分支。
- [x] 建立 `.local/project-resources/` 私有资源入口并加入 `.gitignore`。
- [x] 新建设计文档。
- [x] 新建需求计划。
- [x] 新建任务清单。
- [ ] 盘点新 worktree 当前前后端与测试现状。
- [ ] 补齐后端供应商级 OAuth 能力和网关鉴权。
- [ ] 补齐前端账户创建 / 编辑 / 校验 / endpoint modes 呈现。
- [ ] 同步长期功能文档。
- [ ] 执行类型检查、专项回归、必要的真实联调。
- [ ] 开发期间合并最新本地 `master` 并处理冲突。
- [ ] 合并回本地 `master`，重新验证并收口结果。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 后端类型检查 | `pnpm --filter juhe-ai-backend typecheck` | 通过 | 未执行 | 待实现后执行 |
| 命令类验证 | 前端类型检查 | `pnpm --filter juhe-ai-frontend typecheck` | 通过 | 未执行 | 待实现后执行 |
| 功能主流程 | 新建 Anthropic OAuth 账户 | 管理端手动创建并保存 | 保存成功，账户保持 `oauth` + Anthropic profile | 未执行 | 待实现后执行 |
| 功能主流程 | Anthropic OAuth 网关调用 | mock 或真实最小请求 | 使用 Bearer token 成功转发 | 未执行 | 待实现后执行 |
| 回归场景 | OpenAI 管理式 OAuth 不回归 | 现有回归脚本 | 现有授权 / 刷新逻辑不受影响 | 未执行 | 待实现后执行 |
| 回归场景 | Gemini OAuth 不回归 | 现有回归脚本 | 现有导入型 OAuth 逻辑不受影响 | 未执行 | 待实现后执行 |
| 环境验证 | PostgreSQL / Redis 测试环境隔离 smoke | 测试脚本 / 手动验证 | 不污染非本任务测试对象 | 未执行 | 使用 `.local/project-resources/test/` 指引 |
| 未覆盖说明 | 供应商新增私有订阅 OAuth | 不执行 | 明确不在本计划范围内 | 不适用 | 非官方独立部署链路不纳入 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-07-22 | 进行中 | AI | 已从本地 `master` 创建隔离 worktree `C:\Users\Administrator\.codex\worktrees\sub2api-lite\oauth-matrix-safe-20260722` 与分支 `codex/oauth-matrix-safe-20260722`。 |
| 2026-07-22 | 进行中 | AI | 已创建私有资源入口 `.local/project-resources/`，并登记开发 / 测试所需的外部密钥文件与测试 PostgreSQL / Redis 主机。 |
| 2026-07-22 | 进行中 | AI | 已落地设计文档、计划文档与任务清单，开始盘点新 worktree 中前后端剩余实现缺口。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-07-22 | 继续使用统一 `oauth` 账户类型，但按供应商 profile 决定语义 | 避免引入新的账户类型与大面积 schema 扩散 | 需要收口所有把 `oauth` 直接等同于 OpenAI 的旧逻辑 |
| 2026-07-22 | Anthropic 本次只做导入型 Bearer Token OAuth | 可独立部署、可直接验证、与现有账户模型兼容 | 不新增项目内 Anthropic 浏览器换码 / 刷新链路 |

## 验收标准

- [ ] 新功能只在隔离 worktree 开发，不污染原 `master` 工作区。
- [ ] OpenAI、Gemini、Anthropic 的 OAuth 账户能力矩阵在前后端一致。
- [ ] Anthropic OAuth 账户可保存、可测试、可参与网关转发。
- [ ] OpenAI / Gemini 既有 OAuth 路径无回归。
- [ ] 设计、计划、长期功能文档和验证记录已同步。
- [ ] 开发分支已合并回本地 `master` 并完成重新验证。

## 验证记录

- 当前仅完成 worktree 基线、文档与私有资源入口落地；代码验证尚未开始。
- 活动目标沿用当前线程既有目标，作为 Anthropic 官方认证落地追踪锚点；本计划将实现范围扩展到当前可独立落地的 OAuth 矩阵。

## 风险与注意事项

- 原 `F:\sub2api-lite` 工作区存在用户既有改动，本计划不得在其上直接开发。
- 真实密钥只从 `.local/project-resources/` 索引的外部文件读取，不回显。
- 开发期间若本地 `master` 前进，必须及时合并，避免最终冲突堆积。
- SQLite / PostgreSQL 的旧测试脏数据只允许按任务专用对象重建，不写入长期兼容代码。

## 完成总结

- 完成时间：待补充
- 实际完成内容：待补充
- 主要改动位置：待补充
- 验证结果：待补充
- 后续建议：待补充
