# PLAN-20260725T085700550Z：AI 健康监控

- 计划 ID：PLAN-20260725T085700550Z
- 状态：已完成（未上线）
- 创建时间：2026-07-25
- 需求来源：用户需求
- 目标：提供最近一个月按小时的 AI 账户健康状态列表，支持账户名搜索和分页。

## 范围

- [x] 统计库增加账户小时健康预聚合，接入 Node SQLite / PostgreSQL stats worker。
- [x] 增加管理 API 和用户 API，限制 31 天窗口并稳定分页。
- [x] 增加菜单、路由、搜索、分页、Canvas 状态条和响应式空态。
- [x] 补充 Mockdata、回归测试和开发文档关联。
- [x] 完成前后端类型检查、前端生产构建、专项回归与 Browser 桌面/移动验收。

## 决策记录

采用使用记录中的 `account_health_check` 作为唯一历史事实，小时槽位最后结果覆盖；使用灰色缺测状态区分“没有执行检查”和“检查失败”。不在接口请求时扫描明细，避免一个月窗口乘账户数造成 shard fan-out。

## 验收标准

1. 菜单可以进入 AI 健康监控，管理/用户作用域符合现有权限。
2. 默认最多展示 31 个统计日、每小时 24 个状态槽位，绿/红/灰含义清晰且有 tooltip。
3. 账户名搜索和分页触发后端查询，前端不二次聚合明细。
4. stats worker 增量消费健康检查记录后，SQLite/PostgreSQL 结果一致且重放不重复计数。
5. 后端类型检查、前端构建、专项回归和 Browser 页面检查通过。

## 验证结果

- `pnpm --filter juhe-ai-frontend typecheck`：通过。
- `pnpm --filter juhe-ai-frontend build`：通过。
- AI 健康监控后端专项回归：通过。
- AI 健康监控前端契约回归：通过。
- Browser：管理/用户入口、账户搜索、分页、31 天范围、桌面与 390px 移动端、控制台均通过；31 天每账户为单 Canvas，未生成 744 个小时 DOM 节点。
- `pnpm --filter juhe-ai-backend typecheck`：通过。

## 关联文档

- `docs/functions/AI健康监控设计.md`
- `docs/functions/账号健康检测设计.md`
- `docs/functions/统计指标与分层聚合设计.md`
- `docs/architecture/frontend/README.md`
- `docs/architecture/backend/README.md`
