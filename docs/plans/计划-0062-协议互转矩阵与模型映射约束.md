# PLAN-0062 协议互转矩阵与模型映射约束

## 基本信息

- 编号：PLAN-0062
- 状态：进行中
- 创建时间：2026-06-24
- 更新时间：2026-06-24
- 需求来源：用户对话
- 执行者：AI
- 关联模块：前端 / 后端 / 网关 / 模型映射 / 协议桥接 / Mock AI / 真实联调 / 文档 / 验证

## 需求目标

- 背景：当前大多数上游模型不原生支持 OpenAI Responses，只有 GPT / OpenAI 原生或少数 OpenAI-compatible 上游能承接 Responses。模型映射如果允许把非原生上游协议选成 `responses`，会让 UI 和运行时把不具备 Responses 能力的模型伪装成可用。用户要求先用 mockai 覆盖各种协议和能力形态，避免直接消耗真实账户排低级错误。
- 目标：建立长期协议互转矩阵，收紧模型映射右侧协议选择和保存校验；右侧 `responses` 只允许“左侧也是 Responses 且当前账号真实接口能力包含 Responses”的直连 / 原生转发场景，非直连桥接不得选择 `responses`。后续协议转换测试必须先走 mockai，再做真实账户探针。
- 交付物：协议矩阵文档、前端 UI 限制、前后端保存校验、mockai 回归、真实账户探针计划和验证记录。

## 范围边界

### 本次包含

- [x] 新增本计划并纳入计划索引。
- [x] 更新模型映射长期文档，明确允许 / 禁止的协议转换矩阵。
- [x] 前端模型映射 UI：右侧 `responses` 只有在左侧为 `responses` 且账号接口能力包含 `responses_json` 或 `responses_sse` 时可选。
- [~] 前端保存校验：`chat_completions -> responses` 仅允许账号真实具备 Responses endpoint mode 的 OpenAI/GPT 原生上游；禁止账号不具备 Responses endpoint mode 时保存 `upstreamEndpointFamily=responses`。
- [x] 后端保存 / 导入校验：同样禁止绕过 UI 保存非原生 Responses 上游映射。
- [x] 解开模型列表选择中过度限制的部分：下游模型从 OpenAI 协议客户端模型池选择；上游模型从当前账号供应商 / 当前账号可用模型池选择，不用前端硬编码成同供应商。
- [~] 扩展 mockai 测试：已覆盖模型映射允许 / 禁止路径、OpenAI -> Anthropic bridge、DeepSeek / GLM Responses -> Chat bridge 和 hybrid mock；工具、thinking、图片、文件和 compact 的跨协议全矩阵仍需继续扩展。
- [~] 真实联调前先用用户提供的上游账户探测可用模型和协议能力，再挑选少量代表模型验证；已完成低成本模型清单和少量入口探针，DeepSeek / GLM 代表请求本轮超时，仍需小批次复测。

### 本次不包含

- 不把 `chat_completions -> responses` 放开给 Chat-only、Messages 或未知兼容上游；该方向只服务真实支持 Responses 的 OpenAI/GPT 原生上游，用于 Chat 客户端访问 Responses-only 模型，并必须由本地桥接把 Chat 响应形态还原。
- 不支持 Anthropic Messages 到 Responses 的反向桥接。
- 不用真实账户替代 mockai 覆盖；真实账户只做 mock 通过后的抽样验收。
- 不把非 Responses 上游伪装成原生 Responses。

## 关联文档

- 模型映射设计：`docs/functions/自定义模型与模型映射设计.md`
- 客户端兼容草案：`docs/functions/客户端兼容与模型映射重设计草案.md`
- OpenAI 到 Anthropic 桥接：`docs/functions/OpenAI到Anthropic协议桥接设计.md`
- 高兼容能力矩阵：`docs/functions/OpenAI到Anthropic高兼容能力矩阵.md`
- 测试手册：`docs/develop/测试与验证说明.md`

## 方案概述

- 方案原则：区分“原生上游协议”和“网关桥接协议”。`responses` 只能在上游真实支持时作为右侧协议；其他转换目标只能落到 `chat_completions` 或 `messages`。
- 数据变化：不新增 schema；收紧现有 `modelMappings` 校验。
- 接口变化：后端账户创建、编辑、导入预览、草稿测试都必须复用同一协议矩阵校验。
- 前端变化：模型映射行的右侧协议选项根据左侧协议和账号 `supportedEndpointModes` 动态禁用；保存时给中文错误。
- 后端变化：normalize / repository 层禁止无真实 Responses 能力的 `upstreamEndpointFamily=responses`。
- 数据处理策略：不做旧映射兼容迁移；当前 schema 下发现非法映射应由用户离线清理，不在运行路径双读兜底。

## 协议互转矩阵

| 下游请求 | 上游协议 | 状态 | 说明 |
| --- | --- | --- | --- |
| Chat Completions | Chat Completions | 支持 | OpenAI-compatible 原生 Chat 或同协议模型映射 |
| Responses | Responses | 支持，但仅原生 | 只有账号真实接口能力包含 Responses 时允许；适用于 GPT / OpenAI 原生或明确支持 Responses 的 OpenAI-compatible 上游 |
| Responses | Chat Completions | 支持 | 当前 `responses -> chat_completions` bridge，面向 DeepSeek / GLM 等 Chat-only 上游 |
| Chat Completions | Responses | 支持范围内 | 仅允许账号真实接口能力包含 Responses 的 OpenAI/GPT 原生上游；本地桥接负责把 Chat 请求转为 Responses，并把 Responses 输出还原为 Chat Completion；Chat-only、Messages 或未知兼容上游仍禁止 |
| Chat Completions | Anthropic Messages | 支持 | `chat_completions -> messages` bridge |
| Responses | Anthropic Messages | 支持 | `responses -> messages` bridge |
| Anthropic Messages | Chat Completions | 支持范围内 | Anthropic-compatible / Claude Code 到 OpenAI Chat 的桥接按独立路径验证；不等价于 Responses |
| Anthropic Messages | Responses | 禁止 | Anthropic 不原生支持 OpenAI Responses，当前不做反向 Responses 合成 |

## 执行拆解

- [x] 创建计划。
- [x] 更新 `docs/functions/自定义模型与模型映射设计.md` 和导入协议说明。
- [x] 更新 `frontend/src/views/accounts/AccountStrategySection.vue` 的右侧协议选项。
- [~] 更新 `frontend/src/views/accounts/accountSavePayload.ts` 保存校验：保留非原生 Responses 禁止，补齐原生 Responses 上游的 `chat_completions -> responses` 例外。
- [x] 更新后端模型映射 normalize / repository 校验。
- [x] 补前端或脚本回归，固定 UI / 保存校验。
- [x] 补后端 `account-model-mapping-regression` 对非法 `responses` 上游的保存和导入拒绝。
- [x] 复跑 mockai 协议转换回归。
- [~] 使用真实账户先探测可用模型，再做抽样真实联调。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 后端类型检查 | `pnpm --dir backend typecheck` | 后端类型检查通过 | 已通过 | 2026-06-24 通过 |
| 命令类验证 | 前端类型检查 | `pnpm --dir frontend typecheck` | 前端类型检查通过或只剩既有无关问题 | 已通过 | 2026-06-24 通过 |
| Mock 回归 | 模型映射保存校验 | `pnpm --dir backend test:account-model-mapping` | 非原生 Responses 上游映射被拒绝；原生 Responses 直连和原生 `chat_completions -> responses` 仍可保存 | 需复跑 | 已有 2026-06-24 基线；需补 Chat -> Responses 原生例外后复跑 |
| Mock 回归 | OpenAI -> Anthropic bridge | `pnpm --dir backend test:openai-anthropic-bridge-mock` | Chat/Responses 到 Messages 继续通过 | 已通过 | 2026-06-24 通过 |
| Mock 回归 | Responses -> Chat bridge | `pnpm --dir backend test:deepseek-gateway-mock-ai`、`pnpm --dir backend test:glm-gateway-mock-ai` | Chat-only 上游继续能承接 Responses bridge | 已通过 | 2026-06-24 通过 |
| Mock 回归 | 混合路由 | `pnpm --dir backend test:hybrid-gateway-mock-ai` | 混合智能路由现有多供应商 mock 不回归 | 已通过 | 2026-06-24 通过 |
| 真实联调 | 模型能力探测 | 临时环境变量调用真实账户 | 得到可用模型和协议能力清单，不写入真实 key | 部分通过 | `/v1/models` 返回 63 个模型；Chat `claude-sonnet-4-6` 200；Responses `gpt-5.5` 200；Anthropic Messages `claude-sonnet-4-6` 200；Responses `claude-sonnet-4-6` 返回 `convert_request_failed not implemented`；DeepSeek / GLM 代表请求本轮 20 秒超时，需分批复测 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-06-24 | 进行中 | AI | 用户补充协议互转和 UI 限制要求：右侧 `responses` 不能在非原生 / 非直连场景选择；真实测试前必须先 mockai。已创建本计划。 |
| 2026-06-24 | 进行中 | AI | 已更新长期模型映射文档、导入协议、前端模型映射 UI、前端保存校验和后端 normalize / create / update / draft / import 校验；右侧 `responses` 只允许账号真实支持 Responses API 的 `responses -> responses` 原生直连。专项回归和 OpenAI -> Anthropic mock 已通过。 |
| 2026-06-24 | 进行中 | AI | 已补跑 DeepSeek、GLM 和 hybrid mock 回归，确认 Chat-only 的 `responses -> chat_completions` bridge 和混合路由不受右侧 Responses 限制误伤。 |
| 2026-06-24 | 进行中 | AI | 已用真实账户做低成本探针：模型清单返回 63 个模型；`/v1/chat/completions`、`/v1/responses` 和 `/v1/messages` 均有代表入口 200；`claude-sonnet-4-6` 走 `/v1/responses` 明确返回未实现转换，验证非原生上游不能泛化选择右侧 Responses。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-06-24 | 右侧 `responses` 只允许原生 Responses 上游 | 大多数非 GPT 上游不支持 Responses；允许选择会造成运行时伪成功或低质量转换；Chat 客户端访问 Responses-only GPT 模型需要单独桥接 | UI、前端保存、后端导入和 API 保存都必须禁止非原生 Responses 上游映射；原生 `chat_completions -> responses` 必须由本地桥接还原 Chat 响应形态 |
| 2026-06-24 | mockai 先于真实账户 | 真实账户消耗额度且错误噪声大；协议字段、SSE 事件和工具格式可由 mockai 先覆盖 | 每个新增协议转换能力先补 mock，再做真实抽样 |

## 验收标准

- [x] 模型映射 UI 不允许在非原生上游选择 `responses`。
- [~] 前后端保存 / 导入校验均不能绕过该限制；同时允许真实 Responses 上游的 `chat_completions -> responses` 例外。
- [x] 允许的转换矩阵在文档中清晰可查。
- [x] mockai 覆盖 Chat、Responses、Anthropic Messages 的核心互转和禁止路径。
- [~] 真实账户探针在 mockai 通过后执行，并记录可用模型 / 协议事实；已完成首轮低成本探针，DeepSeek / GLM 代表模型需分批复测。

## 验证记录

- 类型检查：2026-06-24 执行 `pnpm --dir backend typecheck` 和 `pnpm --dir frontend typecheck`，均通过。
- Mock 回归：2026-06-24 执行 `pnpm --dir backend test:account-model-mapping`，通过；执行 `pnpm --dir backend test:openai-anthropic-bridge-mock`，通过；执行 `pnpm --dir backend test:deepseek-gateway-mock-ai`、`pnpm --dir backend test:glm-gateway-mock-ai`、`pnpm --dir backend test:hybrid-gateway-mock-ai`，均通过。
- 导入协议回归：2026-06-24 执行 `pnpm --dir frontend test:account-import-protocol`，通过。
- 真实联调：已执行低成本模型 / 入口探针。`/v1/models` 返回 63 个模型，包含 GPT / Claude / DeepSeek / GLM / Gemini 等分组；Chat `claude-sonnet-4-6`、Responses `gpt-5.5`、Anthropic Messages `claude-sonnet-4-6` 均返回 200；Responses `claude-sonnet-4-6` 返回 `convert_request_failed not implemented`；DeepSeek / GLM 代表模型本轮 20 秒超时，未作为协议结论。
- 凭据检查：2026-06-24 执行固定真实 key 前缀扫描，未命中仓库内容。
- 未验证项：DeepSeek / GLM 真实代表模型需小批次复测；真实工具、thinking、图片、文件和 compact 的跨协议全矩阵仍需继续扩展；`chat_completions -> responses` 原生上游桥接待实现和 mock 回归。

## 风险与注意事项

- 如果历史数据库已有非法 `upstreamEndpointFamily=responses` 映射，本计划不在运行时代码迁移；由用户按当前 schema 离线清理。
- “Chat -> Responses” 与 “Responses -> Chat” 方向不可混淆；前者只允许真实 Responses 上游并需要本地还原 Chat 响应，后者用于 Chat-only 上游承接 Responses 客户端。
- 发布异常处理：如协议矩阵限制误伤原生 GPT Responses 账号，先检查账号 `supported_endpoint_modes` 是否包含 `responses_json` / `responses_sse`，不要放宽到所有供应商。

## 完成总结

- 完成时间：待补充
- 实际完成内容：待补充
- 主要改动位置：待补充
- 验证结果：待补充
- 后续建议：待补充
