# PLAN-20260821T182741627Z：J3a 代理延迟检测 L3-PG smoke

| 项目 | 内容 |
| --- | --- |
| 状态 | 历史 L3 jobs/reader smoke 计划；当前 Go result projector PostgreSQL 集成改由 `TestPostgresResultProjectorSmoke` 独立验证，现役 Node PG CRUD/CAS smoke 也已通过。当前 Node PG projector 已删除；Node→Go→Node 进程编排、owner handoff、部署与生产门禁仍未完成 |
| 创建时间 | 2026-08-21T18:27:41.627Z |
| 需求来源 | J3a 代理延迟检测完整迁移契约、L2 复审阻塞项与 L3 验收要求 |
| 负责人/执行者 | `/root`（协调与实现）；独立 Sol 复审负责质量门 |

## 0. 当前结论

已交付历史 jobs/reader smoke 入口、隔离配置硬门、权限/fixture/replay/事务断言和验收文档。当前 Go projector PostgreSQL 写回由 `TestPostgresResultProjectorSmoke` 复验，现役 Node PostgreSQL CRUD/CAS smoke 验证微秒版本、并发合并与索引计划；一次性 scratch 与 fixture 已清理。该结果不包含真实 Node→Go→Node 进程编排、owner handoff、生产迁移或 L4 门禁。

## 1. 目标与边界

建立 J3a jobs Store 与 PostgreSQL direct-input reader 的历史隔离验收入口。默认不连接外部系统，不读取 `.local`；本文不改变当前 Go 独占 owner，也不恢复或注册 Node projector/scheduler。

## 2. 关联文档与实现

- 迁移契约：[J3a 代理延迟检测完整迁移契约](../migration/J3a-代理延迟检测完整迁移契约.md)
- 验收方案：[J3a 代理延迟检测 L3 PostgreSQL/PgBouncer 验收方案](../migration/J3a-代理延迟检测L3-PG验收方案.md)
- Smoke 实现：`backend-go/projects/jobs/internal/proxylatency/postgres_integration_test.go`

## 3. 执行拆解

1. [x] opt-in URL、REQUIRED、PgBouncer 端口、固定 scratch database 与 jobs/input 同库预检。
2. [x] jobs role 执行 `Ping`、`EnsureSchema`、owner/proxy lease、IssueInput、AdmitExecution、AppendOutcome/replay 与清理。
3. [x] business reader role 执行只读事务、`CheckContract`、有效 fixture 的 `LoadDue` 与最小权限负向断言。
4. [x] 补充 poison digest、successor replay、权限边界、错误脱敏和清理失败可见性。
5. [x] 经授权 scratch 数据库和 PgBouncer `6432` 执行真实 smoke，并保存脱敏结果摘要。
6. [x] 由独立 Sol 复审确认 PG 基础域 R1-R4；Node→Go→Node、owner handoff 与部署门禁仍作为后续阶段，Node projector 不再是现行路径。

## 4. 测试项

| 类型 | 测试项 | 验证方式 | 预期/状态 |
| --- | --- | --- | --- |
| 命令 | Go 单测 | `go test -count=1 ./internal/proxylatency` | 已通过 |
| 命令 | Go 静态检查 | `go vet ./internal/proxylatency`、`git diff --check` | 已通过 |
| 配置边界 | 默认无 URL | `TestPostgresSmoke` 无环境变量 | 明确 SKIP，已通过 |
| 配置边界 | REQUIRED 缺 URL | `J3A_PG_SMOKE_REQUIRED=1` | 非零失败，已通过 |
| 配置边界 | REQUIRED 未知值 | `J3A_PG_SMOKE_REQUIRED=garbage` | 非零失败，已通过 |
| 配置边界 | jobs/input 不同库 | 两个 scratch URL 指向不同 database | 连接前非零失败，已通过 |
| PG/PgBouncer | 真实 jobs/reader smoke | 两个角色经 `6432` 运行 `TestPostgresSmoke` | 通过；required smoke exit 0 |
| 事务/权限 | 真实隔离、只读、超时和清理 | scratch 角色/fixture/脱敏结果摘要 | 通过；清理和 PgBouncer 认证恢复已核验 |
| 不适用 | Node projector、owner handoff、Redis/Docker、部署 | 本计划明确非目标 | 后续门禁 |

## 5. 进度记录

- 2026-08-21：完成 opt-in smoke 初版；独立复审发现 policy version、scratch/同库/脱敏、权限、replay、fixture 与文档索引缺口。
- 2026-08-21：修复上述本地可验证缺口，补齐真实 UTC 毫秒计划 ID、README 索引、负向测试和清理路径。
- 2026-08-22：默认 SKIP、REQUIRED 缺失/未知值、不同库预检、Go 单测、vet、diff-check 通过；首次真实 smoke 暴露 JSONB digest 键序问题。
- 2026-08-22：修复 PostgreSQL JSONB typed canonical digest 校验；经 dev PgBouncer `6432` 的 required smoke 通过，scratch DB/角色清理及认证恢复完成；独立 Sol 复审 `pass`。

## 6. 验证记录

| 检查 | 当前证据 | 状态 |
| --- | --- | --- |
| `go test -count=1 ./internal/proxylatency` | 本地通过 | 已验证 |
| `go vet ./internal/proxylatency` | 本地通过 | 已验证 |
| 默认 smoke | 明确 `SKIP`，不当作 PG 通过 | 已验证 |
| `J3A_PG_SMOKE_REQUIRED=1` 缺 URL | 非零失败 | 已验证 |
| 真实 PostgreSQL/PgBouncer | 当前一次性 dev scratch 经 PgBouncer `6432` 运行 `TestPostgresResultProjectorSmoke`；Node 现役 PG CRUD/CAS smoke 另行验证微秒版本、并发合并、索引计划；脱敏记录保留，scratch/临时代理行已清理 | 当前通过；不扩展为进程级 handoff 或生产 |

## 7. 风险与注意事项

- 本次仅验证 dev 隔离 scratch 和当前 PgBouncer transaction pooling，不替代生产或 L4 证据；当前五个 `go.work` module 的 Go jobs test/race/vet 已通过。
- 任何 URL 不符合固定 scratch 前缀、`6432`、显式角色或双 URL 同库要求时必须停止，不得 fallback 到主库。
- 失败输出必须保持连接串、密码、payload 和代理凭据脱敏；清理失败保持失败状态。
- owner gate、Node owner、Node projector、Redis、Docker、发布和回滚均保持关闭/非目标。

## 8. 验收结论

真实 scratch 子库、受控 app role、PgBouncer `6432`、事务/清理验证、Go projector 事务路径和现役 Node PG CRUD/CAS smoke 已完成，L3-PG 本地 smoke 门关闭。原始外部日志未入库，仅保留脱敏结果摘要；Node→Go→Node 进程编排、owner handoff、发布/回滚和 active-path-zero 仍是后续门禁。

## 9. 完成总结

完成项：`backend-go/projects/jobs/internal/proxylatency/postgres_integration_test.go`、Go result projector 与脱敏验收报告已更新；本轮 `TestPostgresResultProjectorSmoke` 和现役 Node PG CRUD/CAS smoke 验证 exit 0，scratch/fixture 已清理。Node J3a outcome reader/projector 已删除。残余风险为 Node→Go→Node 进程编排、生产/L4、owner handoff、部署/回滚、Docker/Redis 未验证，后续按迁移路线单独验收。
