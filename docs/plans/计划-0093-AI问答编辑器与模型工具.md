# PLAN-0093 AI 问答编辑器与模型工具实施计划

> **执行要求：** 在隔离分支内按 TDD 顺序逐项实现，每项通过定向回归后提交；完成后执行 Mock、真实模型和 Browser UI 验收。

**目标：** 使用 Tiptap 3 落地可扩展 AIComposer，修复消息方向与发送清空，并让 AI 问答按模型能力接入 Responses 原生工具事件。

**架构：** 编辑器只维护输入文档和附件预览，输出统一内容块；聊天 transport 根据服务端返回的协议能力选择 Chat Completions 或 Responses。模型原生工具由上游执行，本站只声明已支持的原生工具并透传、解析和展示；当前不向模型声明本地 function tool。

**技术栈：** Vue 3、TypeScript、Tiptap 3 / ProseMirror、OpenAI Chat Completions、OpenAI Responses SSE、现有 Node 网关、Browser 插件。

---

## 文件边界

- `frontend/src/views/chat/composer/AIComposer.vue`：编辑器 UI、快捷键、发送/恢复和底部模型能力控件；不显示独立工具栏。
- `frontend/src/views/chat/composer/chatComposerDocument.ts`：Tiptap JSON 到聊天内容块的纯函数转换。
- `frontend/src/views/chat/composer/ChatImageAttachment.ts`：受限图片预览节点，不负责上传。
- `frontend/src/views/chat/composer/chatComposerCommands.ts`：`/` 命令定义和过滤。
- `frontend/src/views/chat/ChatMessageList.vue`：用户右侧、AI 左侧和工具事件展示。
- `backend/src/modules/chat/chat-responses-sse.ts`：Responses SSE 增量解析，不访问存储。
- `backend/src/modules/chat/chat-transport.ts`：协议选择、请求构造和统一流事件。
- `backend/src/storage/chat.repository.ts`：内容块与工具事件的有界持久化。

## Task 1：现有交互缺陷回归

- [x] 新增 `frontend/src/scripts/regression/chat-composer-state-regression.ts`，断言发送快照后草稿立即清空、请求失败恢复快照、成功不恢复。
- [x] 新增 `frontend/src/scripts/regression/chat-message-layout-regression.ts`，固定用户消息右对齐、AI 消息左对齐的 class 契约。
- [x] 运行两个测试并实现状态控制器和布局 class。
- [x] 用户消息整行不使用灰色背景，只保留轻量右侧消息气泡。

## Task 2：Tiptap AIComposer

- [x] 安装并保留实际使用的 Tiptap 3 最小依赖：core、vue-3、starter-kit、placeholder。
- [x] 覆盖段落、marks、标题、引用、列表、代码块、图片顺序和空文档序列化。
- [x] 实现 `input_text | input_image` 转换，文本上限 192 KiB，图片携带本轮受限 Data URL。
- [x] 实现 Enter、Shift+Enter、IME、历史栈、发送清空和失败恢复。
- [x] 替换 `a-textarea`；移除独立工具栏，说明进入 placeholder。

## Task 3：图片粘贴与命令菜单

- [x] 实现图片 MIME、单张 4 MiB、最多 4 张；预览直接使用受限 Data URL，不创建需要手工释放的 Object URL。
- [x] 图片只作为行内节点，不显示独立附件托盘，保持文字/图片顺序。
- [x] `/` 命令支持过滤、上下键、Enter、Escape，并删除命令查询文本后执行。

## Task 4：Responses 协议与工具事件

- [x] 新增 Responses SSE parser 回归，覆盖文本、工具、function 参数、完成、失败和未知事件。
- [x] 实现统一流事件和基于真实候选账户能力的协议选择，不按模型名猜测。
- [x] 实现 Chat/Responses 双 transport；两者都使用会话绑定的真实本地 API Key进入现有网关。
- [x] 服务端只声明已支持的原生 `web_search`，不向模型暴露未实现的本地 function tool。
- [x] 提交 `feat(chat): support native responses tools`。

## Task 5：结构化消息与工具展示

- [x] 扩展 chat schema 与 repository，新增 `content_blocks_json`；路由持久化前按 `192 KiB` 清洗，repository 再以 `256 KiB` 做防御性硬上限。
- [x] 保存工具/思考投影；刷新后可重放，模型上下文只加入完整成功轮次。
- [x] 前端新增 `ChatToolEvent.vue`，工具状态和思考摘要默认折叠、低强调展示。
- [x] 流式事件更新现有消息对象，不刷新整个列表。
- [x] 提交 `feat(chat): persist and render tool events`。

## Task 6：Mock 与真实链路

- [x] 扩展 Mock 上游：先发 web search 工具事件，再发文本回答，断言事件顺序和最终消息正文。
- [x] 覆盖重复 `clientMessageId` 返回冲突且不产生第二次工具调用。
- [x] 使用 Git 忽略的私有凭据文件完成真实账户聊天全链路测试；Mock Responses 单独固定 web search 工具事件验收。
- [x] 使用 Browser 插件测试 Enter 清空、用户右侧、AI 左侧、工具折叠/刷新持久化、Markdown 和图片粘贴顺序。
- [x] 模型能力控件移入输入框；会话列表单行显示并支持右键重命名、置顶、详情和删除。
- [x] 流式滚动状态机支持用户介入、底部跟随、历史锚点和回到底部按钮。
- [x] 增加编辑器、模型控件、响应式布局、滚动策略和原生工具链路回归。

## Task 7：最终验证与文档

- [x] 运行前后端 typecheck、build、全部 chat regression、真实 PostgreSQL / Redis smoke。
- [x] Browser 检查桌面与 390px 移动端，确认无重叠、外层溢出、当前控制台错误和框架错误层；虚拟列表人为滚动的 Browser 元素操作受插件超时限制，由纯策略回归覆盖。
- [x] 更新 `AI问答设计.md`、PLAN-0092 和 PLAN-0093，滚动、模型控件、会话操作等复用规则记录在功能设计中。
- [x] 检查工作树、测试进程、临时文件和敏感凭据，确保没有密钥写入 Git。
- [x] 提交本轮会话操作、模型控件、开发自动登录、滚动和响应式验收改造。

## 验收标准

- 用户消息右侧，AI 和工具事件左侧。
- Enter 发送立即清空；Shift+Enter 换行；失败恢复原草稿；IME 不误发送。
- 编辑器支持撤销/重做、Markdown 结构、图片粘贴预览和 `/` 命令。
- Chat Completions 账户行为不回退；Responses 账户可展示模型原生工具事件。
- 本站不执行未注册本地工具，不绕过网关、代理、额度、审计或使用记录。
- Mock、类型检查、构建、真实模型和 Browser UI 验收通过。
