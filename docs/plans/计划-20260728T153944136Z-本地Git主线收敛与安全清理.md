# PLAN-20260728T153944136Z 本地 Git 主线收敛与安全清理

## 状态

进行中。

设计依据：[本地 Git 主线收敛与安全清理设计](../superpowers/specs/2026-07-28-本地Git主线收敛与安全清理设计.md)。

## 目标

- 保护所有 Git 引用、stash、脏 worktree 和未跟踪产物。
- 把仍适用于当前架构的分支独有修复安全移植到最新 `master`。
- 清理失效 worktree 登记和已经证明冗余的本地分支。
- 让 `master` 的完整性、远端状态和剩余分支用途可以被维护者直接判断。

## 工作分支

- 分支：`codex/git-mainline-consolidation-20260728`
- worktree：`F:\sub2api-lite-worktrees\git-mainline-consolidation-20260728`
- 初始基线：`ed71626396bce56c50499e5aa25bed0f690dd76b`
- 主工作树存在并发修改，本计划不在主工作树编辑、提交或清理。

## 实施清单

### A. 冻结与备份

- [ ] 导出 refs、分支、stash、reflog、worktree 和审计分类清单。
- [ ] 创建并验证仓库外 Git bundle。
- [ ] 为全部有效脏 worktree 导出 tracked/cached patch。
- [ ] 复制未跟踪文件并校验 SHA-256。
- [ ] 验证待删分支 tip 均能从备份恢复。

### B. 7 月 26 日后独有修复

- [ ] 修复 usage spool 停止竞态并补回归。
- [ ] 复核并移植性能拓扑脚本与验收修复。
- [ ] 落地生产 schema 前向升级与业务数据对账工具，或记录不实施依据。
- [ ] 禁止 Nginx 对流式/非幂等网关请求透明重试。
- [ ] 固化 Nginx binary/main config 和服务用户边界。
- [ ] 修正 PostgreSQL smoke 异步 session 接口。
- [ ] 修正模型检测基础连通和可信对比评分。
- [ ] 逐项复核并处置 BUG-0128 至 BUG-0133。

### C. 其余高风险分支

- [ ] 复核 23 条较早高风险分支的唯一代码/测试提交。
- [ ] 合并同主题分支，记录后续演化版本和替代证据。
- [ ] 移植仍有效的独有修复并运行对应回归。
- [ ] 为继续保留的分支记录明确原因和后续门槛。

### D. 清理

- [ ] dry-run 并清理 203 个失效 worktree 登记。
- [ ] 删除不再被有效 worktree 占用的已合并本地分支。
- [ ] 归档并删除已验证 patch-id 等价分支和重复指针。
- [ ] 对疑似 squash/amend、退役 page-data 和历史文档分支完成二次复核后分批处理。
- [ ] 保留所有尚无替代证据的高风险分支。

### E. 验证与收尾

- [ ] 运行所有移植主题的专项回归。
- [ ] 运行后端/前端 typecheck 和受影响构建。
- [ ] 复算分支、worktree、stash、远端和工作区状态。
- [ ] 运行 Git 对象连通性和 bundle 恢复抽样。
- [ ] 完成代码、测试、并发、文档、架构和过度设计复查。
- [ ] 更新计划状态、验证记录和最终剩余风险。

## 完成标准

- 没有已知有效修复只存在于准备删除的分支。
- 删除的每个分支 tip 和脏 worktree 内容都有已验证的恢复路径。
- `master` 在最新远端基线上完成验证，领先/落后状态明确。
- 失效 worktree 登记和确认冗余分支已清理，高风险分支有明确保留原因。
- 未执行生产部署，未删除远端分支，未覆盖其他任务修改。

## 进度记录

| 日期 | 状态 | 记录 |
| --- | --- | --- |
| 2026-07-28 | 进行中 | 完成全分支只读审计，确认 285 条本地分支、152 条未拓扑合并分支、203 个失效 worktree 登记及 25 条高风险分支；从已同步远端的 `master=ed7162639` 创建隔离工作分支。 |
