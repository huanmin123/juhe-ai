# PLAN-20260728T153944136Z 本地 Git 主线收敛与安全清理

## 状态

进行中。

设计依据：[本地 Git 主线收敛与安全清理设计](../superpowers/specs/2026-07-28-本地Git主线收敛与安全清理设计.md)。

## 目标

- 保留 `feature/*`、stash、脏 worktree 和未跟踪产物；按用户要求不生成额外 bundle 备份。
- 把仍适用于当前架构的分支独有修复安全移植到最新 `master`。
- 清理失效 worktree 登记和已经证明冗余的本地分支。
- 让 `master` 的完整性、远端状态和剩余分支用途可以被维护者直接判断。

## 工作分支

- 分支：`codex/git-mainline-consolidation-20260728`
- worktree：`F:\sub2api-lite-worktrees\git-mainline-consolidation-20260728`
- 初始基线：`ed71626396bce56c50499e5aa25bed0f690dd76b`
- 主工作树存在并发修改，本计划不在主工作树编辑、提交或清理。

## 实施清单

### A. 冻结与保护边界

- [x] 导出并统计 refs、分支、stash、reflog、worktree 和审计分类。
- [x] 用户明确选择不生成 bundle 或脏文件副本；清理范围相应收窄。
- [x] `feature/*`、脏 worktree、stash 和高风险分支不进入自动删除候选。

### B. 7 月 26 日后独有修复

- [x] 修复 usage spool 停止竞态并补回归。
- [x] 复核并移植性能拓扑脚本与验收修复，补充 symlink/chown 和回滚失败门禁。
- [x] 保留只读业务数据对账工具；不移植过时 schema 升级方案，不新增 migration 93。
- [x] 禁止 Nginx 对流式/非幂等网关请求透明重试。
- [x] 固化 Nginx binary/main config 和非 root 服务用户边界。
- [x] 修正 PostgreSQL smoke 异步 session 接口。
- [x] 修正模型检测基础连通和可信对比评分，补齐旧记录前端兼容。
- [x] 逐项复核并处置 BUG-0128 至 BUG-0133。

### C. 其余高风险分支

- [x] 完成全部 98 条剩余未合并本地 ref 和 3 条远端跟踪 ref 的逐项语义复核。
- [x] 确认 provider `enabled` PostgreSQL boolean 方言修复尚缺，已选择性移植并加 schema 契约。
- [x] 确认 Go 迁移分支的 7 个已提交改动仍有效，应普通合并；其未提交 W5 工作继续保留。
- [x] 其余旧树均已得到补丁等价、后续替代、明确退役或与权威文档冲突的处置证据。

### D. 清理

- [x] dry-run 并清理 203 个失效 worktree 登记。
- [x] 删除第一批 167 条已合并或 patch-id 等价的本地分支。
- [ ] 将已审计为废弃/替代的独立 tip 分组执行 `git merge -s ours`，每组校验 tree SHA 不变。
- [ ] 对疑似 squash/amend、退役 page-data 和历史文档分支完成二次复核后分批处理。
- [ ] 保留所有尚无替代证据的高风险分支。

### E. 验证与收尾

- [ ] 运行所有移植主题的专项回归。
- [ ] 运行后端/前端 typecheck 和受影响构建。
- [ ] 复算分支、worktree、stash、远端和工作区状态。
- [ ] 运行 Git 对象连通性检查。
- [ ] 完成代码、测试、并发、文档、架构和过度设计复查。
- [ ] 更新计划状态、验证记录和最终剩余风险。

## 完成标准

- 没有已知有效修复只存在于准备删除的分支。
- 删除项均能由拓扑、patch-id 或其他保留 ref 机械证明冗余；不删除脏 worktree。
- `master` 在最新远端基线上完成验证，领先/落后状态明确。
- 失效 worktree 登记和确认冗余分支已清理，高风险分支有明确保留原因。
- 未执行生产部署，未删除远端分支，未覆盖其他任务修改。

## 进度记录

| 日期 | 状态 | 记录 |
| --- | --- | --- |
| 2026-07-28 | 进行中 | 完成全分支只读审计，确认 285 条本地分支、152 条未拓扑合并分支、203 个失效 worktree 登记及 25 条高风险分支；从已同步远端的 `master=ed7162639` 创建隔离工作分支。 |
| 2026-07-28 | 进行中 | 用户明确要求跳过备份并直接清理，同时要求保留全部 `feature/*` 发布分支；自动清理范围收窄为干净且无进程占用的旧 worktree、已合并分支、无独有 merge resolution 的 patch-id 等价分支和由其他保留 ref 承载的重复指针。 |
| 2026-07-29 | 进行中 | 本地分支由 286 收敛至 119；剩余 98 条未合并 ref 已全量复核。确认 Go 迁移已提交 tip 需要普通合并，`feature/20250617` 和性能分支采用选择性重实现，其余旧树仅做内容中性历史闭合。 |
