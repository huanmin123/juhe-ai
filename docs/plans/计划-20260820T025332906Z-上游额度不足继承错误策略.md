# PLAN-20260820T025332906Z 上游额度不足继承错误策略

> 2026-08-24 增量：额度恢复策略已扩展为 Node/Vue 的 `quota_recovery_policy`。API Key 默认 60 分钟，并由账户/Key recovery seed 在恢复间隔的全局窗口内稳定错峰；支持该语义的 API Key 供应商显式 reset 仍最高优先级。OAuth 默认 UTC 每日策略并可按账户类型编辑，且不消费 API Key reset 字段。backend-go 不在本次范围。

## 基本信息

- 计划 ID：PLAN-20260820T025332906Z
- 创建时间戳（UTC）：20260820T025332906Z
- 状态：本次变更已完成
- 创建时间：2026-08-20
- 更新时间：2026-08-22
- 需求来源：上游额度不足错误语义、账户错误策略继承展示与状态恢复边界评审
- 执行者：`/root`（后端、前端与集成）
- 关联模块：Node 网关 / AI 账户错误策略 / 健康探针 / Vue 管理页面 / 权限 / 文档 / 验证

## 范围

### 本次包含

- [x] 形成上游额度不足继承错误策略的功能设计与评审结论。
- [x] 明确静态系统规则 registry、账户规则分层、403 语义匹配、优先级和动作复用边界。
- [x] 明确账户错误策略页面混排、继承 Tag、系统规则只读和保存过滤契约。
- [x] 更新功能文档和计划索引。

### 已实施

- [x] 后端静态 registry、有效规则 DTO 和读取/写入拒绝。
- [x] 网关额度错误匹配、系统优先级、状态写回和低上下文健康探测隔离。
- [x] 前端有效规则混排、继承 Tag、系统规则逐规则只读及保存隔离。
- [x] 授权实例仅投影无敏感系统规则和有效 `quota_recovery_policy`，不返回源账户自定义规则或 `credentials`。
- [x] 完成定向回归与前后端类型检查；未启动隔离页面，原因见验证结果。

### 本次不包含

- 不修改数据库、依赖、生产环境或 Go 迁移代码。
- 不新增 `quota_blocked` 状态；额度专用状态仅作为未来独立计划的可选方向。
- 不增加全局策略页面、全局规则 API 或数据库表。
- 不复制系统规则到每个账户，不暴露授权实例的源 `credentials`。

## 关联文档

- 功能设计：[上游额度不足继承错误策略设计](../functions/上游额度不足继承错误策略设计.md)
- 账户错误边界：[AI 账户错误语义与状态变更边界](../functions/AI账户错误语义与状态变更边界.md)
- 探针恢复：[AI 账户运行态探针恢复设计](../functions/AI账户运行态探针恢复设计.md)
- 网关失败：[网关失败归因与自动探针结果设计](../functions/网关失败归因与自动探针结果设计.md)
- 现有错误策略实现：`backend/src/modules/gateway/policy/account-error-policy.service.ts:228-275`
- 现有前端保存落点：`frontend/src/views/accounts/accountErrorPolicyPayload.ts:288-294`

## 执行清单

1. [x] 固定首批七个错误码、额度文本白名单、大小写规范化及排除项。
2. [x] 实现静态 system registry；不写 `system_settings`、数据库或账户 `credentials`。
3. [x] 扩展有效展示 DTO：`source`、`inherited`、`editable`，保留账户整体只读边界。
4. [x] 实现系统规则先于账户规则的匹配和固定内部顺序；账户规则继续按 `priority`。
5. [x] OAuth 继续使用默认 UTC daily `rate_limited` 场景并忽略 API Key reset 字段；API Key 在支持该语义的供应商上优先遵守 `reset_at` 等恢复信号，否则按 60 分钟并由账户/Key recovery seed 在全局窗口内稳定错峰复测，多 Key 按 fingerprint 隔离，通用模式 30 天后进入人工恢复的 Key `error`。
6. [x] 服务端校验并保存独立 `quota_recovery_policy`，系统规则不复制到 `error_handling_rules`；支持该语义的 API Key 显式 provider reset 不受用户策略缩短，OAuth 不消费 API Key reset 字段。
6. [x] 接入 403 + 稳定错误码/高置信文本匹配，排除裸 403、泛化 `quota`、权限和内容策略错误。
7. [x] 系统命中时不派发低上下文 `request_failure` 健康探测；既有 `error` 恢复门禁保持不变。
8. [x] 前端混排系统/账户规则，同样式显示系统规则并添加“继承” Tag；系统规则逐规则只读。
9. [x] 本地表单数组是唯一保存来源；服务端二次拒绝系统规则写入。
10. [x] 完成授权实例安全投影与定向回归；隔离管理页未启动，原因见验证结果。

## 测试表

| 类别 | 测试项 | 预期结果 | 本计划状态 |
| --- | --- | --- | --- |
| 匹配 | 首批七个稳定错误码 + `403` | 命中系统额度规则一次 | 已通过 |
| 匹配 | `billing_error` 与余额不足关键词共同出现 | 命中；单独 `billing_error` 不命中 | 已通过 |
| 排除 | 裸 `403`、泛化 `quota`、`client_restricted` | 不判为余额不足 | 已通过 |
| 包装兼容 | `type: new_api_error` + 稳定额度码 | 命中系统额度规则 | 已通过 |
| 排除 | `content_policy_violation`、`prompt_guard_blocked` | 保持内容/权限语义 | 已通过 |
| 优先级 | 系统规则与账户规则冲突 | 系统额度规则先执行，账户规则不能绕过 | 已通过 |
| 状态 | 默认系统额度动作 | 使用 `rate_limited`，`cooldown_until` 到期后才进入实际复测 | 已通过（状态守卫覆盖持久化与可调度性） |
| 竞态 | 请求失败、健康探测、人工测试并发 | 无来源成功不覆盖额度保护；旧写回被代次门禁拒绝 | 已通过静态状态守卫与系统命中跳过调度验证 |
| DTO/权限 | 自有与授权实例读取 | DTO 脱敏；授权可见性遵守最终权限契约 | 已通过 |
| 前端 | 编辑、新建、克隆的混排、Tag、只读、排序/删除/启停 | 系统规则同样式但不可变更；新建/克隆无 DTO 时显示只读预览 | 已通过源码回归与类型检查 |
| 保存 | 混排列表提交 | payload 只有账户规则，系统规则不写入 `credentials` | 已通过源码回归与服务端拒绝验证 |

## 进度

| 日期 | 状态 | 记录 | 进展 / 阻塞 |
| --- | --- | --- | --- |
| 2026-08-20 | 已完成 | `/root` | 完成 Node/Vue 继承规则、DTO、保存隔离与定向验证；未修改数据库、Go 或生产。 |
| 2026-08-24 | 本次变更已完成 | `/root` | API Key 额度恢复接入 reset_at 优先、无信号 60 分钟并由账户/Key recovery seed 在全局窗口内稳定错峰、多 Key fingerprint 隔离、30 天 Key error 人工恢复；OAuth 默认 UTC daily，并支持账户级策略配置。 |

## 决策

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-08-20 | 系统规则静态 registry，不入 `system_settings`、数据库或账户凭据 | 系统保护规则应统一生效，避免每账户复制和漂移 | 通过现有账户策略读取接口合成有效 DTO，不增加全局入口 |
| 2026-08-20 | 系统规则先于账户自定义规则 | 防止通用账户规则绕过额度保护 | 账户规则只能补充未覆盖语义，不能覆盖系统规则 |
| 2026-08-20 | 历史决策：默认额度动作使用 `error_disabled`（已由本次变更取代） | 小探测不能证明真实大上下文额度恢复 | 保留历史证据；现行实现改为带每日复测边界的 `rate_limited` |
| 2026-08-24 | API Key 额度恢复采用稳定的全局窗口错峰 | 多 Key 必须按 fingerprint 隔离；供应商 reset 信号优先；无信号才使用 60 分钟基线，并由账户/Key recovery seed 稳定派生偏移 | 通用模式连续 30 天写 Key `error`，人工重新验证后恢复；不新增 `quota_blocked` |
| 2026-08-20 | 不实施 `quota_blocked` | 状态机、恢复、展示和迁移契约尚未确定 | 仅保留为未来独立计划选项 |
| 2026-08-20 | 403 必须叠加稳定错误码或高置信额度文本 | 权限/内容策略 403 仍存在，裸状态码不具备额度语义 | 首批错误码和排除项固定进回归矩阵 |

## 验收

- [x] 功能设计明确区分已核验事实、评审结论和当前实现。
- [x] 账户规则与系统规则同样式混排，系统规则带“继承” Tag 且不可编辑、删除、启停、排序。
- [x] 页面没有全局策略入口，系统规则没有复制到每个账户，授权实例不暴露源 `credentials`。
- [x] 403 只有叠加稳定错误码或高置信额度语义才判定余额不足，并覆盖明确排除项。
- [x] 系统额度命中不派发低上下文失败探测；既有 `error` 状态不被成功观察自动恢复。
- [x] API Key 多 Key 复用 Key runtime `rate_limited`，reset 信号优先、无信号 60 分钟并由账户/Key recovery seed 在全局窗口内稳定错峰复测，通用模式 30 天转 Key `error` 并要求人工恢复；OAuth 默认 UTC daily 且可按账户配置，未新增 `quota_blocked`。
- [x] 有定向回归、失败分发链回归与前后端类型检查证据。

## 验证结果

### 2026-08-24 增量验证

- `pnpm --filter juhe-ai-backend typecheck`：通过。
- `pnpm --filter juhe-ai-backend test:account-error-policy-validation`：通过，覆盖稳定的全局窗口错峰、账户 API Key interval、OAuth UTC daily。
- `pnpm exec tsx src/scripts/regression/account-api-key-quota-retest-regression.ts`（`backend`）：通过。
- `pnpm --filter juhe-ai-backend test:account-api-key-probe-generation-contract`：通过。
- `pnpm --filter juhe-ai-backend test:account-api-key-failure-guard`：通过。
- `pnpm --filter juhe-ai-backend test:account-api-key-runtime-status-sql`：通过。
- `pnpm --filter juhe-ai-backend test:account-api-key-runtime-observation`：通过，覆盖 generic duplicate 未来 due 保留、观察窗口和 explicit reset。
- `pnpm --filter juhe-ai-backend test:account-runtime-mutation-owner-fencing`：通过，覆盖 owner/authorized 账户级额度写回的 provenance 优先级门禁。
- `pnpm --filter juhe-ai-backend test:disabled-account-state-guard`：通过，覆盖单 Key 账户未来显式 reset（含旧显式 provenance）不被迟到通用额度结果覆盖。
- `pnpm --filter juhe-ai-backend test:account-api-key-recovery-parent-status`：通过，覆盖父账户状态参与 Key 恢复探针资格。
- `pnpm --filter juhe-ai-backend test:account-advanced-projection`：通过，覆盖 owner/authorized effective quota policy 脱敏只读投影。
- `pnpm --filter juhe-ai-backend test:account-response-credentials-redaction`：通过，覆盖高级详情策略投影与凭据边界。
- `pnpm --filter juhe-ai-frontend typecheck`、`pnpm --filter juhe-ai-frontend build`：通过。
- `pnpm --filter juhe-ai-frontend test:account-edit-save-flow`：通过，覆盖策略字段保存与增量 PATCH 边界。
- 未执行：真实供应商探测、跨节点容量压测、生产部署和隔离管理页面验证。

- 历史计划待验证清单（本次已由上方增量验证覆盖）：`pnpm --dir backend typecheck`、`pnpm --dir backend test:account-error-policy-validation`、`pnpm --dir backend test:account-advanced-projection`、`pnpm --dir backend test:disabled-account-state-guard`、`pnpm --dir frontend typecheck`。
- 历史已通过：`pnpm --dir backend build`、`pnpm --dir backend test:gateway-upstream-policy-framing-mock-ai`、`pnpm --dir backend test:account-batch-edit`、`pnpm --dir backend test:account-batch-edit-context`、`pnpm --dir backend test:openai-oauth-credentials-patch`、`pnpm --dir frontend build`、`pnpm --dir frontend test:account-edit-save-flow`、`pnpm --dir frontend test:account-batch-edit`。
- `disabled-account-state-guard` 的本次契约：系统 `403` 额度错误持久化 `rate_limited` 且可调度，审计标记 `ruleSource: system` 和 `ruleId`，不投递 `request_failure` 探针；裸 `403` 仍投递普通探针。
- 已修复并通过相邻回归：`pnpm --dir backend test:scope` 已迁移至当前受管理员保护的 `/system-metrics/trend`、`/system-metrics/runtime/summary` 与团队成员分页端点；`pnpm --dir backend test:account-response-credentials-redaction` 已与公开的 `recoveryAt`、`recoveryAtKind: policy_ttl_expiry` 投影契约一致。两项修复未改变本计划的额度策略实现。
- 未执行：未启动隔离管理页，未执行真实上游、生产探针、数据库写入或部署。

## 风险

- 供应商错误字段不一致，若提取契约不统一，可能把权限/内容策略拒绝误判为额度不足；必须保持未知可见并补齐字段矩阵。
- `rate_limited` 的恢复必须受 `cooldown_until`、权限、代次和审计约束；轮询频率不能被误写成实际复测时间。
- 现有 `request_failure` 健康探测与额度状态存在迟到写回竞态，必须使用现有 CAS/generation 或等价条件写入。
- 授权实例投影若复用完整账户 DTO，可能越过源凭据脱敏边界；权限实施前不得默认共享。

## 完成总结

- 本次完成：Node 运行时系统规则改为 `rate_limited`、API Key 默认 60 分钟并由账户/Key recovery seed 在全局窗口内稳定错峰、OAuth UTC daily/账户策略、有效详情 DTO、前端继承混排与保存隔离、账户级显式 reset 优先 CAS、定向回归和类型检查，并同步设计/计划索引。
- 本次未完成：不包含数据库、Go、生产、隔离页面或真实上游验证。
- 交接结论：定向质量门已通过；真实供应商、跨节点容量和生产/隔离页面仍需在对应环境单独验收。
