# PLAN-20260728T141710063Z Node 管理接口按需读写后续分批治理

## 基本信息

- 计划 ID：PLAN-20260728T141710063Z
- 状态：进行中
- 创建时间：2026-07-28
- 需求来源：PLAN-20260727T183737351Z 全站 Node/Vue 调用链复审
- 执行者：Codex
- 范围：Node 后端、Vue 前端、场景 DTO、字段级 PATCH、并发与 Browser 验证；不包含 Go
- 前置计划：[PLAN-20260727T183737351Z](计划-20260727T183737351Z-Node管理接口按需读写重构.md)
- 权威设计：[Node 管理接口按需读写设计](../superpowers/specs/2026-07-28-Node管理接口按需读写设计.md)

## 目标

本计划只承接前置计划已经取得调用链证据、但不适合与账户主链路混成一次高风险重写的剩余项。每批必须先锁定页面消费字段、版本语义和副作用，再拆查询与写命令；不得只在 route 层删响应键，也不得把整份表单继续包装成名义上的 PATCH。

## 当前进度

- [ ] 批次 A1：供应商模型新增 / 编辑按需读取与字段级 PATCH。
- [ ] 批次 A2：授权有效期 / 限额专用投影与 CAS 差量写入。
- [ ] 批次 A3：响应检查策略定点 options、revision 与字段级 PATCH。
- [ ] 批次 A4：模型检测策略 / 计划用途专用候选与字段级 CAS。
- [ ] 批次 B、C：完成 A 批次验收后再逐项排期。

## 审计基线

| 批次 | 页面 / 触发器 | 当前读问题 | 当前写问题 | 目标证据 |
| --- | --- | --- | --- | --- |
| A | 供应商模型新增 / 编辑 | 打开弹窗前读取当前供应商完整详情；模型目录同时承担列表和编辑 DTO | PATCH 全量提交能力、生命周期与价格，隐藏价格还写 `null` | 首开 0 detail；展开协议才读当前供应商能力；单模型 edit 投影；no-op 0 DML |
| A | 授权有效期 / 限额编辑 | 只消费 `expiresAt/limits` 却读取完整授权摘要 | 两字段始终全量回传，无 baseline、revision 和 no-op | 专用 edit 投影；字段差量 + CAS；最小 mutation result |
| A | 响应检查策略编辑 | 已知协议仍读取全部供应商协议候选再由 Vue 过滤 | 完整 PUT、无 CAS；成功后本地 upsert 又重载列表 | protocol 定点 options；PATCH + revision；只保留一种列表收敛方式 |
| A | 模型检测策略 / 计划 | 首屏读取隐藏完整策略；候选先取 50 条再前端过滤，历史 options 过宽 | 策略和计划全量写、重算计划并清 lease | 首屏轻量 profile；用途专用候选；策略 / 计划字段级 CAS |
| B | 代理、系统账户、系统团队、公告编辑 | 部分详情或密文投影仍过宽 | 整表单提交、无统一 no-op 或可靠 CAS | 各资源独立 revision、字段依赖投影与动态 SET |
| B | AI 账户批量编辑、克隆、重新授权 | 批量上下文逐个装配最多 100 个宽账户；克隆并取两份编辑投影 | 批量更新仍需独立字段依赖与 owner 锁范围收敛 | 有界批量 edit DTO；owner 条件进锁行 SQL；逐字段关系表写预算 |
| B | 用量统计 / 使用记录筛选 | owner 切换缺代次围栏；趋势选择后重取账户 options；账户下拉为名称后缀再取 providers | 只读页面，无写问题 | 作用域 fencing；选择 / 移除 0 options；服务端直接返回展示名 |
| C | Chat 上下文、运行 / 审计日志、健康 / 表监控 | 空闲轮询宽上下文、首屏无消费者 snapshot、抽屉级点位提前加载或先取 200 行再前端分页 | 按各域现状评估 | 窄状态快照、可见触发、服务端分页与 Browser 长时间请求预算 |

## 执行规则

1. 每个批次单独建立设计与回归，不能共享一个宽 `Summary` 作为过渡终态。
2. 列表、编辑、options、secret、runtime 和 mutation result 分别定义字段白名单，并用 SQL 捕获证明没有先宽查后裁剪。
3. 写入必须有 baseline、revision / CAS、同值 no-op、动态 `SET`、关系表写入预算和按字段分类的缓存 / worker 副作用。
4. 远程搜索必须有 debounce、取消或代次 fencing；管理 target owner 必须进入缓存键和 SQL owner 条件。
5. 每批至少通过 Node/Vue typecheck、定向 HTTP/repository/竞态回归、生产构建、`git diff --check` 和隔离 Browser Network 验证。

## 非目标

- 不把所有详情请求删除；用户明确点击后、列表确实缺少数据的专用详情是正确边界。
- 不把动态默认资源 ID 写成跨用户编译期常量。
- 不在没有 revision 与关系表回归时顺手改写其他资源。
- 不包含 Go 对齐、生产部署或生产数据迁移。

## 完成标准

每一行审计项只有在“响应字段、实际 SQL、写入列、并发冲突、缓存副作用、前端 Network”六类证据同时闭合后才能从待排期改为完成。前置计划的账户列表 / 单账户编辑 / API Key 结果不能代替本计划其他资源的完成证明。
