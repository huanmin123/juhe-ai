# PLAN-20260722T232105000Z 响应检查策略渐进加载

## 基本信息

- 计划 ID：PLAN-20260722T232105000Z
- 状态：本地实现完成，待 PostgreSQL 与浏览器补证，禁止上线
- 创建时间：2026-07-22
- 需求来源：[全站页面 API 按需加载审计报告](../reports/全站页面API按需加载审计报告-2026-07-20.md) P1
- 执行范围：Node 后端、Vue 前端、测试与文档
- 明确排除：Go、生产、部署、提交和远端状态

## 目标

将响应检查策略管理页拆为 overview、按 ID detail 与 provider-protocol 轻量 options，使首屏不再读取完整 matcher/notes 和 provider definitions，同时保持完整查看、编辑、三协议校验与网关运行时语义。

## 关联文档

- [专项设计](../superpowers/specs/2026-07-22-响应检查策略渐进加载设计.md)
- [全站页面 API 按需加载审计报告](../reports/全站页面API按需加载审计报告-2026-07-20.md)
- [PLAN-20260704T095333001Z 管理接口 DTO 分层与渐进式加载](计划-20260704T095333001Z-管理接口DTO分层与渐进式加载.md)
- [响应语义检查管线设计](../functions/响应语义检查管线设计.md)
- [接口契约与权限矩阵](../functions/接口契约与权限矩阵.md)

## 范围边界

### 本次实施

- Node 管理 overview/detail/provider-options HTTP 与 repository 投影。
- SQLite 主线程、read worker 和 Node PostgreSQL 等价。
- Vue 列表收窄、按点击详情、弹窗按需 options、竞态与重试。
- 管理 UI 补齐现有 Node 已支持的 Gemini 协议。
- 专项回归、PG smoke、文档与复核。

### 本次不做

- 不修改 `backend-go/`。
- 不修改 schema、网关 matcher/action 语义或账户级规则。
- 不缩窄通用 provider options/definitions。
- 不提交、不推送、不部署、不操作生产。

## 执行清单

- [x] 核对审计报告、现有 Node/前端调用链、测试与文档。
- [x] 形成 overview/detail/provider-options 专项设计。
- [x] 用户确认专项设计和 UI 字段取舍。
- [x] 实现 Node overview/detail 类型、窄 SQL、mapper 与 routes。
- [x] 实现 provider-protocol 轻 options 与 SQLite/read-worker/PostgreSQL driver 路径。
- [x] 实现 Vue 窄列表、按需 detail/options、Gemini 与竞态保护。
- [x] 增加 Node HTTP/query/read-worker/PG smoke 回归脚本。
- [x] 增加前端首屏请求矩阵、交互与竞态纯行为回归。
- [x] 同步接口契约、审计报告状态和测试说明。
- [x] 运行 Node/前端专项本地回归、Node typecheck 和 diff-check。
- [ ] 使用真实 PostgreSQL 运行专项 smoke；当前 `driver=sqlite` 且缺少 `JUHE_AI_POSTGRES_URL`。
- [ ] 完成浏览器网络验证与前端全量 typecheck/build；当前浏览器插件/Playwright 不可用，且全量前端门禁被既有账户页类型错误阻塞。
- [x] 双 Agent 独立复核、交叉质疑并闭合本轮发现的问题。

## 测试项

- Overview 精确白名单、detail 完整、默认规则 detail、404、403/认证。
- Overview/options 使用窄查询；detail 单行读取；gateway active 完整查询不变。
- SQLite 同步/read worker/PG 字段与排序一致。
- 首屏 1 overview + 0 detail + 0 provider definitions/options。
- 查看/编辑/创建、失败重试、A→B、关闭、保存、删除竞态。
- PC/移动列表字段正确，详情表单完整，Gemini 可配置。
- 响应字节前后采样、后端 typecheck、前端可运行门禁、diff check。

## 验收标准

- 首屏不读取或返回完整 matcher、notes、provider definitions。
- 查看/编辑仍完整且不会被迟到响应污染。
- provider options 仅在创建/编辑需要时加载且只含三字段。
- 当前三协议、权限、写入、缓存失效和网关行为无回归。
- Node/前端/worker/PG/浏览器证据与双 Agent 复核闭合。

## 风险与注意事项

- 不能为保留旧的“匹配条件搜索”而在 overview 查询中扫描 `match_json/notes`；搜索文案和范围改为概览字段。
- 不能把管理 overview mapper 复用于网关 active policy 热路径。
- 旧列表请求、详情请求和 provider options 请求必须分别有竞态所有权。
- 当前工作树已有大量用户/既有改动，实施时只叠加本计划文件，禁止整理或回退其他差异。

## 进度记录

| 日期 | 状态 | 记录 |
| --- | --- | --- |
| 2026-07-22 | 草稿 | 完成四组只读审计，确认当前首屏读取完整策略与 provider definitions；形成 Node-only 专项设计，等待用户确认。 |
| 2026-07-23 | 实施中 | 用户已确认 overview/detail/provider-options 契约与 UI 字段取舍；开始 Node、Vue、测试和权威文档实施，尚未完成专项验证。 |
| 2026-07-23 | 本地实现完成 | Node/Vue、SQLite HTTP/read-worker、前端纯行为回归、Node typecheck、diff-check 与双 Agent 复核完成；复核发现并修复 provider options 跨 modal 缓存污染和 options 静默 `200` 截断。真实 PostgreSQL 与浏览器证据仍待补。 |

## 验证记录

- 已通过 `pnpm run test:response-inspection-policy-progressive-loading`，覆盖 Node HTTP/query/read-worker 与前端首屏、详情、options、竞态纯行为。
- 已通过 `pnpm --filter juhe-ai-backend typecheck` 和本轮文档/代码 `git diff --check`。
- 双 Agent 复核发现 provider options 缓存在不同 modal 打开周期之间复用旧结果，以及超过响应上限时静默返回 `200` 截断；两项均已修复并复跑本地专项回归。
- 真实 PostgreSQL smoke 未运行：当前运行配置为 `driver=sqlite`，且未提供 `JUHE_AI_POSTGRES_URL`；PG smoke 脚本存在不等于真实证据。
- 浏览器插件和 Playwright 当前不可用，未完成真实页面 Network 验证。前端全量 typecheck/build 仍被既有 `accounts/useAccountListData` 缺少 `accountStatusSnapshotPolling` 等账户页类型错误阻塞；前端专项纯行为回归已通过，但不能替代全量门禁。

## 完成总结

本地 Node/Vue 实现和可用本地证据已完成，首屏 overview、按 ID detail、按需 provider-protocol options 与竞态边界已由专项回归固定。当前仍缺真实 PostgreSQL 和浏览器网络证据，且前端全量 typecheck/build 有既有阻塞，因此计划保持未完成并禁止上线。
