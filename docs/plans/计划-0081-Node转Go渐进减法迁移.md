# PLAN-0081 Node 转 Go 渐进减法迁移

## 基本信息

- 编号：PLAN-0081
- 状态：进行中
- 创建时间：2026-07-06
- 更新时间：2026-07-10
- 需求来源：用户对话
- 执行者：AI / 维护者
- 关联模块：后端 / 存储 / 网关 / 后台 worker / 公开接口 / 管理接口 / 部署 / 文档 / 验证

- 本轮口径更新：Go 已补 `juhe-ai-worker operation-log-retention-cleanup`，W2 的 operation log 保留清理不再是“代码未迁移”；仍未完成的是生产 supervisor 接管、真实 PostgreSQL run-once / 长跑 smoke、完整 data-retention 迁移、生产切流和 Node 删除。

## 需求目标

- 背景：当前 Node 后端为了处理并发、阻塞、事件循环、SQLite 写锁、IPC 和后台任务隔离，已经积累了大量复杂机制。用户已明确决定彻底抛弃 Node 后端，转向 Go，并彻底去掉 SQLite，不再维护 standalone / performance 两套模式。
- 目标：通过渐进式、减法式迁移，把后端逐步迁移到 Go + PostgreSQL + Redis；迁移一个模块就删除一个 Node 旧模块和对应 SQLite / 双模式旧路径，最终只保留前端构建所需的 Node 工具链。当前已完成 M0 文档基线，W0 Go 工程与 PG/Redis/Asynq 常规基线已落地，W1a 公开设置读接口、W1b `/__aipublic__` 四类资源纵切面、W2 管理端只读辅助 / 标签 / operation log 读路径 / operation log 保留清理 worker、W3 captcha / login / current-user / profile update / change-password / logout / session list / session revoke / system account create / full mixed PATCH、自定义模型 `POST/PATCH/DELETE /providers/{code}/models` 补充写接口，以及 W4 system team read/create/update/member、授权列表 / 详情 / team/user usage overview / `:id/usage` 用量明细 / 创建 / 更新 / 有效期更新 / 归还 / 回收、`juhe-ai-worker authorization-expiry-sweep`、`juhe-ai-worker authorization-usage-range-windows-refresh`、`juhe-ai-worker gateway-quota-snapshot-build`、授权来源 / grant / 额度窗口 / 统计脏标记 / usage window schema 和授权缓存失效均为 Go opt-in 灰度能力，默认不生产接管；前端已补 W3 auth / session / 系统账户页和 W4 团队 / 统一授权页本地调用与 UI 边界 smoke。W2 仍不覆盖授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 和其他写接口 operation log；自定义模型 CRUD 仍不代表供应商定义写接口、供应商协议档案写接口、生产切流或 Node 删除；W4 仍不覆盖授权用量窗口真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管、生产 gateway quota snapshot 消费、浏览器连接真实 Go 后端的前端团队 / 统一授权页 smoke、生产切流或 Node 删除；完整会话管理生产接管、真实 Docker/testcontainers integration、浏览器连接真实 Go 后端的前端 smoke、生产切流和 Node 删除仍未完成。系统指标统计已纳入 Go 迁移规划，后续必须从 Node event-loop / DB service / SQLite 文件指标改为 Go runtime、PG、Redis、Asynq、worker lag、stats freshness 和网关 SLI，并作为 W6 / W7 独立迁移分块推进。

> 2026-07-10 更新：gateway quota snapshot 已补 Go Redis runtime state 发布和 Node gateway Redis 模式消费代码路径；历史条目中的“生产 gateway quota snapshot 消费”后续按真实 PG/Redis smoke、生产部署 / supervisor 接管、切流和 Node gateway 删除证据理解。
- 当前 W3 auth / system account 小切片已补 `GET /__aisys__/api/auth/captcha` 验证码发放 / 校验基础、`POST /__aisys__/api/auth/login` 登录 / session 创建小切片、`GET /__aisys__/api/auth/me` 当前用户读取、`PATCH /__aisys__/api/auth/me` 当前用户资料更新、`POST /__aisys__/api/auth/change-password` 当前用户改密、`POST /__aisys__/api/auth/logout` 当前 token hash logout、`GET /__aisys__/api/auth/sessions` 当前用户会话列表、`DELETE /__aisys__/api/auth/sessions/{id}` 当前用户单条会话撤销、`POST /__aisys__/api/system-accounts` 创建子动作和 `PATCH /__aisys__/api/system-accounts/{id}` 完整 mixed partial update；验证码使用 Redis state challenge、`GETDEL` 一次性消费和 60 次 / 分钟生成限流，login 使用 Redis 登录失败锁、PG session token hash / `last_login_at` / 初始 `last_seen_at` 和 Cookie 签发，logout 清 Cookie 已复用 `JUHE_AI_COOKIE_SECURE` / `JUHE_AI_COOKIE_SAME_SITE` 运行时配置，session list 只返回当前系统账户未过期 session 且不读取 token hash、不 touch，session revoke 只按当前系统账户 + session ID 删除且撤销当前 session 时清 Cookie，create 使用 PostgreSQL 事务创建系统账户、7 个默认分组、6 条非 hybrid 默认普通路由 / 默认 API Key 并脱敏记录密码，PATCH 使用 PG 单语句锁定启用超级管理员集合和目标账户、更新资料 / 密码 / 状态 / 图像权限、禁止停用或降级最后一个启用 `super_admin`、提交 `password` 或 `status="disabled"` 时撤销目标全部 session，状态或图像权限真实变更会清理 gateway runtime cache / API Key validation cache，已迁移管理写接口按 Node 写模式使用 60 秒窗口 touch 当前 session，`GET /auth/me`、`GET /auth/sessions` 与 logout 不 touch。这些路径默认关闭，未生产接管；完整 W2 / W3 / W4 管理路径仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 下灰度挂载，`GET /auth/sessions` 和 `DELETE /auth/sessions/{id}` 也可在 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 且 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 时单独灰度挂载。`POST /auth/login` 只证明验证码、Redis 登录失败锁定、PG session 创建和 Cookie 签发的小闭环可验证，`POST /system-accounts` 和 `PATCH /system-accounts/{id}` 只证明系统账户写接口已有 Go opt-in 实现，不代表生产接管，不能删除 Node `/auth` 或 `/system-accounts`。
- 交付物：Go 迁移目录、迁移总计划、Go 架构基线、模块顺序、测试验收策略、开发构建部署调整，以及后续代码迁移计划入口。

## 范围边界

本计划是 Node 后端迁移到 Go + PostgreSQL + Redis 的长期总计划；当前已完成 M0 文档与规则基线，W0 Go 工程与 PG/Redis/Asynq 常规工程基线已落地，W1a 公开设置读接口、W1b 外部维护公开接口和 W2 管理端辅助接口当前已迁移路径都处于 Go 实现中。下面“当前阶段不包含”约束当前未接管范围，不表示长期计划不追踪后续模块迁移。

### 本次包含

- [x] 新增 `docs/migration/` 迁移目录。
- [x] 规划渐进减法迁移原则和阶段。
- [x] 规划 Go 后端目标架构、技术依赖、并发和线程安全边界。
- [x] 规划 Go 系统指标、Prometheus / pprof、内部系统监控契约、worker lag、PG / Redis / Asynq 和网关 SLI 替换口径。
- [x] 规划 PostgreSQL + Redis 单模式存储目标和 SQLite 移除范围。
- [x] 规划模块迁移顺序，明确公开接口、后台接口优先，真实网关最后。
- [x] 规划完整测试与验收策略。
- [x] 规划开发、安装、构建、部署、Docker、服务化和回滚调整。
- [x] 更新文档入口和计划索引。
- [x] 新增 `backend-go/` 最小 Go 工程、命令入口、配置、日志、健康检查、PG store、Redis namespace/cache/state、Asynq inspector/queue 基础封装、baseline migration、sqlc PoC、integration 测试骨架和 W0 live smoke 命令。
- [x] 配置本机 Go 1.26.5、w64devkit、sqlc、goose、golangci-lint 和 govulncheck 工具链。

### 当前阶段不包含

- 不生产接管现有业务接口：当前 W1a、W1b、W2、W3 和 W4 已完成的 Go 路由都只在显式 opt-in 时用于灰度验证，默认生产 server 不注册。W2 已迁移范围仍是管理端只读辅助、标签删除 / 独立 PATCH、operation log 读路径、operation log 保留清理 worker 和少量默认偏好写入，不覆盖授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 或其他写接口 operation log。W3 当前覆盖 captcha、login / session 创建、current-user、profile update、change-password、logout、session list / revoke、`POST /system-accounts` create、`PATCH /system-accounts/{id}` 完整 mixed PATCH 和自定义模型 CRUD 补充写接口；自定义模型 CRUD 不代表供应商定义写接口、供应商协议档案写接口、账号测试任务、前端真实 Go 后端 smoke、生产切流或 Node 删除。W4 当前覆盖 team read/create/update/member、团队状态切换授权副作用、成员维护授权副作用、授权列表 / 详情 / team/user usage overview / `:id/usage` 用量明细 / 创建 / 更新 / 有效期更新 / 归还 / 回收、批量到期扫描 worker、usage range window 刷新 worker 和 gateway quota snapshot 构建 worker，但不覆盖授权用量窗口真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管或生产 gateway quota snapshot 消费；完整 auth 与会话管理生产接管、浏览器连接真实 Go 后端的前端 smoke、生产切流、真实 Docker/testcontainers integration 和 Node 删除仍未完成。`JUHE_AI_PUBLIC_API_ENABLED=true`、`JUHE_AI_MANAGEMENT_API_ENABLED=true` 和 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 只表示可灰度挂载验证，不代表真实端口监听、反向代理切流或 Node 删除；其中 session-only 窄开关只注册 W3 当前用户会话列表 / 撤销两条路径。
- 本轮口径更新：operation log 保留清理 worker 已迁入 Go，但只作为独立 worker 子命令灰度验证，不由管理 HTTP router 暴露，也不代表完整数据保留任务或生产常驻进程已接管。
- W3 当前只允许灰度验证 captcha 发放 / 校验基础、login / session 创建小切片、current-user 读取、profile update、change-password、已迁移管理写接口 session touch、当前 session logout、当前用户 session list / revoke、系统账户 create / 完整 mixed PATCH；session list / revoke 可通过 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 窄开关单独挂载，但不代表完整会话管理生产接管；前端本地调用 / UI 边界 smoke 已补，但完整会话管理生产接管、浏览器连接真实 Go 后端的前端 auth / 系统账户页 smoke、生产切流和 Node 删除仍不包含在当前接管范围。
- 不删除 Node 后端代码：只有某个模块由 Go 接管并通过验收后，才按减法规则删除。
- 不立即修改数据库 schema 或生成离线数据导入脚本：Go 迁移期间如需要 schema 调整或旧 SQLite 数据导入 PostgreSQL，另建具体计划。
- 不迁移真实网关：网关必须在公开接口、后台接口、存储和 worker 迁移成熟后执行。

## 关联文档

- 迁移目录：`docs/migration/README.md`
- 迁移总览：`docs/migration/迁移规划总览.md`
- Go 架构基线：`docs/migration/Go后端架构基线.md`
- Go 技术选型：`docs/migration/Go技术选型与依赖基线.md`
- Go 指标观测：`docs/migration/Go迁移指标与观测规划.md`
- Go 指标字段清单：`docs/migration/Go系统指标字段迁移清单.md`
- 存储目标：`docs/migration/存储目标与SQLite移除.md`
- 模块清单：`docs/migration/模块迁移顺序与减法清单.md`
- W1b 迁移记录：`docs/migration/W1b-外部维护公开接口迁移记录.md`
- W2 迁移记录：`docs/migration/W2-管理端只读辅助接口迁移记录.md`
- W3 迁移记录：`docs/migration/W3-登录与系统账户迁移记录.md`
- W4 迁移记录：`docs/migration/W4-团队与统一授权迁移记录.md`
- 测试策略：`docs/migration/测试与验收策略.md`
- 开发构建部署：`docs/migration/开发构建部署调整.md`
- 公开资源维护接口：`docs/functions/公开资源维护接口设计.md`
- 外部来源系统鉴权：`docs/functions/外部来源系统鉴权设计.md`
- 公开接口日志：`docs/functions/公开接口日志设计.md`
- 架构总览：`docs/architecture/架构总览.md`
- 后端架构入口：`docs/architecture/backend/README.md`
- 开发入口：`docs/develop/README.md`
- 部署入口：`docs/deploy/README.md`

## 方案概述

- 方案原则：渐进式、减法迁移、单 owner、测试先行、Go 原生优先、网关最后。
- 数据变化：本次不改 schema；迁移目标为 PostgreSQL + Redis 单模式，后续 schema 只保留当前最优结构，不在运行路径保留旧 SQLite 结构兼容、双写或双读。
- 接口变化：本次不改接口；后续迁移需保证当前公开契约和管理契约等价，或先更新功能文档和测试预期。
- 前端变化：本次不改前端；后续前端继续以 Vue 3 + TypeScript + Ant Design Vue 维护。
- 后端变化：目标后端从 Node.js + TypeScript 迁移到 Go，逐步删除 Node 后端模块、DB service、SQLite adapter、SQLite 配置和双模式分支。
- 指标变化：系统指标统计必须随 Go 接管同步重建，Node `eventLoopLagMs`、`process_event_loop_*`、`processEventLoop*`、`db-service`、SQLite 文件体积和 usage shard 文件路径不能作为 Go 长期契约。
- 数据处理策略：既有 SQLite 数据如需保留，只通过一次性离线导出、清洗、导入 PostgreSQL 处理，不写入 Go 或 Node 运行路径。

## 执行拆解

### M0 文档基线

- [x] 更新相关长期文档入口。
- [x] 新增迁移目录主文档和示例文档。
- [x] 新增迁移总览、Go 架构基线、模块顺序、测试策略和部署调整。
- [x] 新增 Go 技术选型与依赖基线，固定框架、日志、配置、DB、SQL、job、测试、观测和安全扫描默认依赖。
- [x] 新增 Go 迁移指标与观测规划和 Go 系统指标字段迁移清单，固定 Go runtime、PG、Redis、Asynq、worker lag、stats freshness 和网关 SLI 指标边界，并给出 Node `eventLoopLagMs` / `processEventLoop*` / `db-service` / SQLite 文件指标的字段级删除门禁。
- [x] 明确 PostgreSQL + Redis 单模式和 SQLite 移除边界。

### 后续迁移阶段

- [~] 建立 Go 后端最小工程和 PostgreSQL / Redis / Asynq 基线。
- [~] 固定公开接口契约并迁移公开接口：W1a 公开设置读接口已进入 Go 实现中；W1b `/__aipublic__` 外部维护接口已补 Go catalog/auth、PostgreSQL auth/log adapter、Redis penalty-window helper、公开接口日志 snapshot/log builder、Asynq payload/enqueue/handler、`juhe-ai-worker ingest` 日志消费 runtime、HTTP shell / capture / 499 / ResponseWriter 透传契约、PG foundation smoke 用例、public API log Asynq smoke 用例、public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD 四条真实资源纵切面、默认关闭的 opt-in 生产 router guard，以及 `w1b-public-api-smoke` 独立 maintenance smoke；public group / route strategy / API Key / account 四类完整 Redis limiter + public API log worker shell E2E 测试代码已补；未正式生产接管；当前主线程 Docker 不可用，真实 PG/Redis/Asynq integration、`w1b-public-api-smoke` 真实依赖执行与四类 shell E2E 待复跑。
- [~] 迁移后台管理辅助接口：W2 已补管理端 options / catalog、系统账户列表读 / options、授权候选、providers / models、route strategies、groups、accounts options / tags、标签删除 / 独立 PATCH、operation log 读接口、operation log 保留清理 worker 和默认测试模型偏好写入，并保持 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 默认不接管。W2 仍不覆盖完整 auth 与会话管理生产接管（不含 W3 auth 小切片）、授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 或其他写接口 operation log、前端 smoke、生产切流和 Node 删除；W3 已覆盖系统账户 create / 完整 mixed PATCH，自定义模型 CRUD 已作为后台写接口补充切片进入 Go opt-in，但仍未生产接管。
- 完整会话管理生产接管、浏览器连接真实 Go 后端的前端 smoke、生产切流和 Node 删除仍未完成。
- [~] 迁移后台管理写接口和权限链路：系统账户 create / mixed PATCH、团队 / 授权写接口和自定义模型 CRUD 等子切片已进入 Go opt-in；代理、账号、分组、API Key、策略路由、供应商定义 / 协议档案、设置等写接口仍待迁移，整体未生产接管。
- [ ] 迁移 worker、Node storage 删除收尾和维护脚本。
- [ ] 迁移真实中转网关。
- [ ] 删除 Node 后端发布链路并更新最终部署文档。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 文档结构 | 迁移目录入口 | 检查 `docs/migration/README.md` 和示例文档 | 新目录有 README 和示例文档 | 已通过 | 本次新增 |
| 文档链接 | 文档入口引用 | `rg "migration|迁移" docs` + 相对链接检查 | 入口文档能找到迁移目录和 PLAN-0081，新增链接指向真实文件 | 已通过 | 已执行 |
| 指标规划 | Go 系统指标替换口径 | 检查 `docs/migration/Go迁移指标与观测规划.md`、`docs/migration/Go系统指标字段迁移清单.md`、功能契约和架构入口 | Node event-loop / DB service / SQLite 文件指标被标记为过渡事实，Go 指标口径有明确替代、字段级删除清单和前端契约门禁 | 已通过 | 本次补强 |
| 代码验证 | 当前代码类型检查 | `pnpm typecheck` | 当前前后端类型检查通过 | 不适用 | 本次只改文档 |
| Go 骨架 | Go 单元测试 | `go test ./...` | Go 代码测试通过 | 已通过 | `backend-go` 已通过 |
| Go 骨架 | Race 检测 | `go test -race ./...` | Windows 本机 race 稳定通过 | 已通过 | 已使用 w64devkit 2.8.0 / GCC 16.1.0 修复旧 MinGW 导致的 race 失败 |
| Go 质量 | vet / lint / vuln | `go vet ./...`、`golangci-lint run`、`govulncheck ./...` | vet 无错误、lint 0 issues、无漏洞 | 已通过 | `0 issues`，`No vulnerabilities found` |
| W0 smoke | 命令入口和健康接口 | `go run ./cmd/juhe-ai version`、`go run ./cmd/juhe-ai-worker version`、`go run ./cmd/juhe-ai-maintenance version`、本地临时端口 health / metrics smoke | 命令可运行，health / API health / metrics 正常 | 已通过 | 三个命令均输出 `0.1.0-w0`；health smoke 返回 `ok` |
| W0 integration | testcontainers / goose / sqlc / Redis / Asynq | `go test -tags=integration ./internal/testkit/integration -count=1` | 启动 PG/Redis 并跑 migration、sqlc 查询、Redis TTL/pipeline/counter/fixed-window/penalty-window、Asynq enqueue / consume / retry / archive | 待 Docker 复跑 | 当前主线程 Docker / testcontainers 不可用，容器子测试输出 `SKIP`，不计为真实 PG/Redis 通过 |
| W0 live smoke | 真实 PG/Redis/Asynq URL | `go run ./cmd/juhe-ai-maintenance w0-smoke` | 缺配置 fail-fast；配置真实 URL 时 PG/Redis 均为 `ok`，Asynq 至少完成 enqueue + inspector pending smoke | 部分通过 | 已验证缺少真实 URL 时 fail-fast；本机未配置真实 PG/Redis/Asynq URL |
| W1a 公开设置 | `GET /__aisys__/api/settings/public` | Go API 契约测试 + store 测试 + Redis limiter 测试 + 客户端 IP / trust proxy 测试 + integration seed 测试 + `go run ./cmd/juhe-ai-maintenance w1a-public-settings-smoke` | 匿名 200、`{ data: { appName, appIcon } }`、no-store、只暴露公开字段、Redis state 原子 IP read rate limit、`JUHE_AI_TRUST_PROXY` 默认不信任公网请求头、错误不泄露内部依赖 | Go 实现中 | 已补 Go route/service/store/migration/sqlc/test、Redis 固定窗口原子限流代码、trust proxy 客户端 IP 识别和 W1a 独立 smoke 命令；testcontainers integration 当前主线程因 Docker 不可用输出 `SKIP`，独立真实外部 URL smoke 仍待部署环境执行 |
| W1b `/__aipublic__` | 外部维护公开接口契约 | Node 对照脚本 + Go 契约 / store / Redis / integration 测试 + public group / public route strategy / public API Key / public account 专项矩阵 + router guard 测试 + `w1b-public-api-smoke` + 后续 Go API 契约测试 | Bearer 鉴权、scope、Redis penalty-window 限频、公开接口日志、旧公开路径 404、public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD、默认关闭 / 显式开启 router guard、`w1b-public-api-smoke`、API Key secret 只在新增响应返回且日志脱敏、AI 账户凭据加密落库且不回显、分页、业务响应敏感字段不回显、公开日志快照边界和错误语义不缺失；smoke 输出不能提供生产接管证据 | Go 实现中 | 已新增 W1b 迁移记录并补 Go catalog/auth/store port、PostgreSQL auth/log adapter、Redis penalty-window helper、公开接口日志 snapshot/log builder、Asynq payload/enqueue/handler、`juhe-ai-worker ingest` 日志消费 runtime、HTTP shell / capture / 499 / ResponseWriter 透传契约、W1b PG foundation smoke 用例、public API log Asynq smoke 用例、public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD 四条真实资源纵切面、`JUHE_AI_PUBLIC_API_ENABLED=false` 默认关闭的 opt-in 生产 router guard 和 `w1b-public-api-smoke`；Node 非 PG 对照命令已通过，Go 常规验证矩阵、四类资源 handler/service 专项、router guard 快速测试、maintenance smoke 缺配置 / 输出边界测试和 account shell E2E 编译已通过；account shell E2E 测试代码已补但当前主线程因 Docker 不可用输出 `SKIP`，真实 PG integration、真实 `w1b-public-api-smoke` 与完整 Redis limiter + public API log worker shell E2E 需 Docker/testcontainers 或真实 PG/Redis/worker 环境复跑；生产路由切流和 Node 删除待执行；暂不正式接管 |
| W2 管理辅助 | `GET /__aisys__/api/proxies`、`GET /__aisys__/api/proxies/options`、`GET /__aisys__/api/system-accounts`、`GET /__aisys__/api/system-accounts/options`、`GET /__aisys__/api/authorization-options/grantee-accounts`、`GET /__aisys__/api/my-authorization-options/grantee-accounts`、`GET /__aisys__/api/authorization-options/grantee-teams`、`GET /__aisys__/api/my-authorization-options/grantee-teams`、`GET /__aisys__/api/authorization-options/grantee-groups`、`GET /__aisys__/api/my-authorization-options/grantee-groups`、`GET /__aisys__/api/providers/options`、`GET /__aisys__/api/providers/models/options`、`GET /__aisys__/api/providers/{code}/models`、`GET /__aisys__/api/route-strategies/options`、`GET /__aisys__/api/my-route-strategies/options`、`GET /__aisys__/api/groups/options`、`GET /__aisys__/api/my-groups/options`、`GET /__aisys__/api/groups/account-options`、`GET /__aisys__/api/my-groups/account-options`、`GET /__aisys__/api/accounts/options`、`GET /__aisys__/api/my-accounts/options`、`GET /__aisys__/api/accounts/tags`、`GET /__aisys__/api/my-accounts/tags`、`DELETE /__aisys__/api/accounts/tags/{tagId}`、`DELETE /__aisys__/api/my-accounts/tags/{tagId}`、`PATCH /__aisys__/api/accounts/{id}/tags`、`PATCH /__aisys__/api/my-accounts/{id}/tags`、`GET /__aisys__/api/operation-logs`、`GET /__aisys__/api/operation-logs/{id}`、`GET /__aisys__/api/my-operation-logs` 和 `GET /__aisys__/api/my-operation-logs/{id}` | `go test ./internal/modules/managementauth ./internal/modules/managementproxies ./internal/modules/managementsystemaccounts ./internal/modules/managementauthorizationoptions ./internal/modules/managementproviders ./internal/modules/managementprovidermodels ./internal/modules/managementroutestrategies ./internal/modules/managementgroups ./internal/modules/managementaccounts ./internal/modules/managementoperationlogs ./internal/httpapi ./internal/store/postgres ./internal/config ./internal/app`、`go test ./internal/maintenance ./cmd/juhe-ai-maintenance`、`go run ./cmd/juhe-ai-maintenance w2-operation-logs-smoke`、`go test -tags=integration ./internal/testkit/integration -run TestW2Management -count=1` | options / catalog / 标签删除字段、错误、权限、owner/self 作用域、敏感字段白名单和 router opt-in guard 必须通过；`proxies` 必须 admin-only，返回分页 `items/total/hasMore/page/pageSize`，只按代理名称前缀搜索，固定 `updated_at DESC, id DESC`，包含启用和停用代理，只读取列表摘要字段，不读取或返回 `password_encrypted`、解密密码、`proxyUrl`、`system_account_id`、`created_at` 或 `updated_at`，`total` 只作为窗口上界；`system-accounts` 必须 admin-only，返回分页 `items/total/hasMore/page/pageSize`，只读取列表展示所需非敏感字段，支持 `keyword` 用户名 / 显示名前缀搜索和 `page/pageSize`，不得读取 `password_hash`，`total` 只作为窗口上界；`system-accounts/options` 必须 admin-only，只返回 `id/username/displayName/status`，支持 `ids`、`keyword` 用户名 / 显示名前缀搜索和 `limit=1..50`，不得读取 `password_hash`、`role`、改密状态、最后登录或创建 / 更新时间；`authorization-options/grantee-accounts` 必须 admin-only，`my-authorization-options/grantee-accounts` 允许所有登录用户，二者只返回 `id/username/displayName/status`，支持 `ids`、`keyword` 用户名 / 显示名前缀搜索和 `limit=1..50`；`authorization-options/grantee-teams` 必须 admin-only，`my-authorization-options/grantee-teams` 允许所有登录用户，二者只返回 `id/name/status`，支持 `ids`、`keyword` 团队名前缀搜索和 `limit=1..50`，不得返回团队成员、成员数量、授权来源或 grant；`authorization-options/grantee-groups` 必须 admin-only，`my-authorization-options/grantee-groups` 允许所有登录用户但不改写 `granteeSystemAccountId`，二者必须要求 `granteeSystemAccountId`，只返回目标 active 用户自己的启用分组轻量摘要，支持 `ids`、`providerCode`、`keyword` 分组名前缀搜索、`preferDefault` 和 `limit=1..50`，不得返回分组成员、账号统计、用量、并发、授权来源或 grant；分组 options / account-options 覆盖 owner + 授权组只读 union、`manageableOnly=true` 排除授权组、authorized permissions 和授权组 `accountIds=[]`；`accounts/options` 覆盖 owner + 授权账户只读 union、`accessType=authorized`、授权字段、authorized permissions、owner `tagIds` 命中任一标签过滤和名称包含搜索，`accounts/tags` / `my-accounts/tags` 覆盖 owner 标签字典和未删除账号绑定计数，`DELETE accounts/tags/{tagId}` / `DELETE my-accounts/tags/{tagId}` 覆盖未绑定标签删除 204、绑定中 400、不存在 / 越权 404 和事务行锁绑定复查；operation log 读接口覆盖 admin-only / self viewer scope、summary 搜索倒排、trace 前缀范围、targeted / all_users 合并、个人端脱敏和本地 smoke 输出不声明生产接管；代理创建 / 更新 / 删除 / 检测、系统账户创建 / 更新 / 改密 / 登录 / 会话撤销、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention、其他写接口 operation log、授权来源 / grant / 授权写接口仍未完成；`juhe_ai_session` 会话 cookie、SHA-256 `token_hash`、`system_sessions` join `system_accounts`、过期 / 停用拒绝和普通用户初始密码拦截符合 Node 当前语义；默认生产 router 不注册，显式 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 才允许灰度挂载 | Go 实现中 | 单元 / handler / store / auth / config / app 快速测试已通过；operation log smoke 命令单元测试已通过；真实 `w2-operation-logs-smoke` 需配置 PG / Redis 后执行；`TestW2ManagementAccountOptionsPostgresSmoke` 当前本机 Docker 不可用时执行为 `SKIP`，需在 Docker/testcontainers 健康环境复跑后才能记为真实通过，其余 W2 smoke 待健康环境复跑 |
| W2 操作日志保留清理 worker | `juhe-ai-worker operation-log-retention-cleanup` | `sqlc generate`、`go test ./internal/modules/managementoperationlogs ./internal/store/postgres ./internal/app ./cmd/juhe-ai-worker -count=1`、真实 PG 环境执行 `go run ./cmd/juhe-ai-worker operation-log-retention-cleanup --run-once` | 读取 `system_settings.operationLogRetentionDays`，缺失默认 365 天；每批按 `created_at ASC, id ASC` 删除最多 1000 条 `juhe_dataset.operation_logs`，单轮最多 20 批，满批暂停 25ms，依赖 FK cascade 清理 targets / viewers / summary search terms | Go 实现中 | 已补 targeted Go 测试；真实 PG run-once / 长跑、生产 supervisor、完整 data-retention、生产切流和 Node 删除仍待完成 |
| W5 代理管理写接口 | `POST /__aisys__/api/proxies`、`PATCH /__aisys__/api/proxies/{id}`、`DELETE /__aisys__/api/proxies/{id}` | `sqlc generate`、`go test ./internal/modules/managementproxies ./internal/httpapi ./internal/store/postgres ./internal/config ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run TestW5ManagementProxyCRUDPostgresSmoke -count=1` | admin-only、写鉴权 touch、strict JSON、Node 兼容密码密文、敏感 operation log、连接变更重置检测缓存、固定 4 条绑定账户窗口、`409` 占用冲突、gateway runtime cache 失效和管理 API secret fail-fast | Go 实现中 | integration 用例已补且包可编译；本机 Docker 不运行时实际执行为 `SKIP`，不计真实 PostgreSQL 通过。代理检测、前端真实 Go 后端 smoke、生产切流和 Node `/proxies` 删除仍待完成 |
| W2 管理辅助补充 | `GET /__aisys__/api/providers` | `go test ./internal/modules/managementproviders ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 必须 `requireAuth` + admin-only；返回启用和停用供应商定义，字段对齐 Node `ProviderDefinition`；`systemAccountId` 只影响 `provider_default_test_models` 默认测试模型偏好覆盖；`providers/options` 继续只返回启用供应商；默认生产 router 不注册，显式 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 才允许灰度挂载 | Go 实现中 | 本轮补充 Go opt-in handler / service / PostgreSQL query / router / app 注入和契约测试；真实 PG smoke、前端供应商页 smoke、生产切流和 Node 删除仍待完成 |
| W3 自定义供应商模型 CRUD | `POST /__aisys__/api/providers/{code}/models`、`PATCH /__aisys__/api/providers/{code}/models/{id}`、`DELETE /__aisys__/api/providers/{code}/models/{id}` | `sqlc generate`、`go test ./internal/modules/managementprovidermodels ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`、`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementProviderModelCRUDPostgresSmoke -count=1` | 必须覆盖普通用户个人模型、管理员目标用户个人模型和全局模型；内置模型只读；strict JSON、权限错误、价格校验、int4 上界和 Node 错误优先级一致；`PATCH` 禁止修改模型 ID；停用 / 删除清理默认测试模型偏好；删除检查账户支持模型和同来源供应商模型映射 source/upstream 绑定并返回 `409`；保存 / 删除只发布 gateway runtime cache 失效；默认生产 router 不注册，显式 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 才允许灰度挂载 | Go 实现中 | 已补 port / SQL / migration / store / service / HTTP handler / router / app 注入、专项测试和 integration smoke 代码；真实 `TestW3ManagementProviderModelCRUDPostgresSmoke` 需 Docker/testcontainers 健康环境复跑，当前不代表生产切流或 Node 删除；供应商定义写接口、供应商协议档案写接口和前端真实 Go 后端 smoke仍待迁移 / 验证 |
| W3 auth captcha / session touch / session list-revoke | `GET /__aisys__/api/auth/captcha`、`PATCH /__aisys__/api/auth/me`、`POST /__aisys__/api/auth/change-password`、已迁移管理写接口、`GET /auth/me`、`POST /auth/logout`、`GET /auth/sessions`、`DELETE /auth/sessions/{id}` | `sqlc generate`、`go test ./internal/config ./internal/httpapi ./internal/app -count=1`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/platform/redis ./internal/app -count=1`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app`、`go test ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthPostgresSmoke -count=1`、`pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke`、Node session / auth 回归 | `GET /auth/captcha` 使用 Redis state challenge、PNG data URL、60 次 / 分钟生成限流和 `GETDEL` 一次性消费基础，并跳过 system API IP 读限流；`GET /auth/me`、`GET /auth/sessions` 和 logout 不 touch；`GET /auth/sessions` 只返回当前系统账户未过期 session，不读取 / 返回 token hash，分页 total 只做窗口上界；`DELETE /auth/sessions/{id}` 只撤销当前系统账户下指定 session，撤销当前 session 时清 Cookie；logout / 当前 session revoke 清 Cookie 必须复用运行时 `Secure` / `SameSite` 配置；前端头像菜单必须提供会话管理入口，撤销当前会话后清理本地登录态并返回登录页；写鉴权按 60 秒窗口更新当前 session `last_seen_at`；fresh session 跳过写库；touch 错误返回 500；router 挂载写接口时必须显式注入 touch middleware；`JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 且总管理开关关闭时只注册 session list / revoke，不注册其他管理路径；生产仍默认关闭 | Go 实现中 | captcha targeted Go 测试已通过；logout Cookie 安全属性目标测试和 session list / revoke targeted Go 测试已通过；session-only config / router / app 目标测试已通过；前端本地 auth / session / system-account smoke、typecheck 和 build 已通过；既有 Go 全量测试和 integration 包编译已通过；`TestW2ManagementAuthPostgresSmoke` 因 Docker 未运行按预期 `SKIP`，不计真实 PG integration 通过；Node `test:system-api-db-access`、`test:system-account-session-driver`、`test:auth-must-change-password-boundary` 已通过 |
| W3 system account create | `POST /__aisys__/api/system-accounts` 创建子动作 | `sqlc generate`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountCreatePostgresRedisSmoke -count=1`、`pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke`、后续浏览器连接真实 Go 后端的前端系统账户页 smoke | 仅允许 `super_admin`；严格 JSON object，拒绝未知字段、尾随 JSON、非法枚举、`null` 布尔和用户名 / 用户名称 / 密码空白字符；响应不返回 `password_hash`、默认 API Key 明文或默认资源 ID；事务写入系统账户、7 个默认分组、6 条非 hybrid 默认普通路由、6 条路由分组绑定和 6 个默认 API Key；默认 API Key secret 只以 Node 兼容 AES-GCM 密文落库；operation log 密码字段标记敏感且只写“已设置”；前端系统账户页写入口仅 `super_admin` 可见，创建 payload 不携带默认资源 ID | Go 实现中 | create targeted service / handler / SQL guard / store 常量测试已通过；新增真实 PG+Redis+Asynq operation-log create smoke 代码并通过 integration 包编译，本机 Docker/testcontainers provider 不可用时实际执行为 `SKIP`，不计真实通过；前端本地 auth / session / system-account smoke、typecheck 和 build 已通过；浏览器连接真实 Go 后端的前端系统账户页 smoke、生产切流和 Node 删除仍待完成 |
| W3 system account PATCH | `PATCH /__aisys__/api/system-accounts/{id}` 完整 mixed partial update | `sqlc generate`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountsPatchPostgresRedisSmoke -count=1`、`pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke`、后续真实 mixed PATCH smoke 和浏览器连接真实 Go 后端的前端系统账户页 smoke | 允许 `displayName/description/password/role/status/mustChangePassword/imageGenerationEnabled` 任意组合，禁止 `username` 和未知字段；要求 `super_admin`、写鉴权 touch、响应不返回 `password_hash`；提交 `password` 或 `status="disabled"` 删除目标全部 session 且密码日志脱敏；禁止停用 / 降级最后一个启用 `super_admin`；状态或图像权限真实变化清理 gateway runtime cache / API Key validation cache，状态原因优先；前端编辑和重置密码必须走统一 update API | Go 实现中 | 完整 mixed PATCH targeted service / handler / SQL guard / row mapper 测试已通过；既有真实 PG+Redis+Asynq operation-log smoke 测试代码覆盖 PATCH 真实链路，但本机 Docker/testcontainers provider 不可用时输出 `SKIP`，不计真实通过；前端本地 auth / session / system-account smoke、typecheck 和 build 已通过；真实 mixed PATCH integration、浏览器连接真实 Go 后端的前端系统账户页 smoke、生产切流和 Node 删除仍待完成 |
| W4 system team read/create/update/member | `GET /__aisys__/api/system-teams`、`GET /__aisys__/api/system-teams/{id}`、`GET /__aisys__/api/my-teams`、`GET /__aisys__/api/my-teams/{id}`、`POST /__aisys__/api/system-teams`、`PATCH /__aisys__/api/system-teams/{id}`、`POST /__aisys__/api/system-teams/{id}/members` 和 `DELETE /__aisys__/api/system-teams/{id}/members/{memberId}` | `sqlc generate`、`gofmt`、`go test ./internal/modules/managementsystemteams ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 团队读接口必须 admin / self 作用域正确，支持分页、团队名前缀搜索、详情 active 成员摘要，读接口不 touch session，不读取授权来源 / grant，不按说明字段搜索；团队创建允许 `super_admin/admin`，普通用户 403；严格 JSON object，拒绝未知字段、尾随 JSON、非法状态、空团队名和非法说明；团队名 trim 后全局唯一；只写 `system_teams` 摘要字段；团队更新必须 admin-only、strict JSON、支持 name / description / status partial update，状态从非 disabled 到 disabled 时 revoke 当前 team source 并刷新 effective source，状态从 disabled 到 active 时按 active team grants 展开到 active members，授权变化写全量 stats dirty 标记并发布 `team_authorization_changed` 授权缓存失效；成员新增 / 移除必须 admin-only、strict JSON、写鉴权 touch，新增只允许 active 团队 / active 系统账户，单次最多 20 个且团队 active 成员最多 20 个，新增或重新加入时按既有 active team grants 展开最终用户授权，移除成员时 revoke 该成员 active team source 并刷新 effective source，成员变化写 `team_members_changed` stats dirty 和授权缓存失效；operation log 写 `system_teams.create` / `system_teams.update` / `system_teams.add_members` / `system_teams.remove_member`；默认生产 router 不注册，写接口必须走 touch middleware | Go 实现中 | 本轮已通过 W4 team list/detail/create/update/member service / handler / SQL guard / store / router / app targeted 测试；真实 PG/Redis/Asynq operation-log integration、前端团队页 smoke、生产切流和 Node `/system-teams` / `/authorizations` 删除仍未完成 |
| W4 authorization list/detail | `GET /__aisys__/api/authorizations`、`GET /__aisys__/api/authorizations/{id}`、`GET /__aisys__/api/my-authorizations`、`GET /__aisys__/api/my-authorizations/{id}` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权列表 / 详情必须 admin / self 作用域正确，管理侧 admin-only，个人侧强制当前用户；列表支持 `keyword/resourceType/resourceId/resourceOwnerSystemAccountId/granteeSystemAccountId/teamId/status/direction/sourceType/systemAccountId/startDate/endDate/page/pageSize` 查询参数；管理侧 `direction` 校验但不应用，个人侧应用 inbound / outbound；`startDate/endDate` 仅校验日期格式；PG 以 `resource_authorization_grants` 为分页主表，按 `created_at DESC, id DESC` 稳定排序，关键词使用 `COLLATE "C"` + `starts_with`；列表响应为轻量 DTO 和 `sourceSummary`，不返回 `authorizationSources`、`limits`、`usage` 等详情字段；详情路径 `{id}` 固定为 `resource_authorization_grants.id`，返回授权关系、limits、source 明细、基础 usage 空对象和权限摘要；非资源归属人必须脱敏来源团队和 created/revoked 字段；列表 / 详情不触发到期扫描，不做 usage 聚合 | Go 实现中 | 本轮已通过 service / handler / store query builder / router / app targeted 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| W4 authorization create | `POST /__aisys__/api/authorizations`、`POST /__aisys__/api/my-authorizations` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权创建必须 admin / self 作用域正确，管理侧必须指定授权人，普通用户访问管理侧 403；strict JSON，拒绝未知字段、尾随 JSON、空白必填字段和 `limits:null`；账户个人授权必须指定目标分组，其他组合不能指定目标分组；`expiresAt` 必须是未来时间且账户授权不能晚于源账户到期时间；quota limits 只允许 `hourly/daily/weekly/monthly/total` 且金额最多 6 位小数；个人授权写 manual source、授权账户实例和目标分组绑定；团队授权写 grant 并 fanout 到 active 成员；成功后写 `resource_authorization_created` stats dirty、发布授权缓存失效，并入队 `authorizations.create` operation log；默认生产 router 不注册，写接口必须走 touch middleware | Go 实现中 | 本轮已通过 service / handler / store 编译 / router / app targeted 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| W4 authorization update | `PATCH /__aisys__/api/authorizations/{id}`、`PATCH /__aisys__/api/my-authorizations/{id}` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权更新必须 admin / self 资源归属作用域正确，路径 `{id}` 固定为 `resource_authorization_grants.id`；严格 JSON object 只允许 `status/expiresAt/limits` 且至少一个字段；`status` 只允许 `active/paused`，`expiresAt` 必须是未来时间或 `null`，`limits:null` 清空额度，额度窗口仍按 `hourly/daily/weekly/monthly/total` 和 hourly 1..720 归一化；更新后同步 grant、manual / team source、runtime authorization、`resource_authorization_updated` stats dirty、授权缓存失效和 `authorizations.update` operation log；默认生产 router 不注册，写接口必须走 touch middleware | Go 实现中 | 本轮已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| W4 authorization expire update | `PATCH /__aisys__/api/authorizations/{id}/expire`、`PATCH /__aisys__/api/my-authorizations/{id}/expire` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权有效期更新必须 admin / self 资源归属作用域正确，路径 `{id}` 固定为 `resource_authorization_grants.id`；严格 JSON object 只允许 `expiresAt/limits`，其中 `expiresAt` 必填且可为 `null`，`limits` 可选且可为 `null`，拒绝 `status` 和未知字段；更新后复用 grant、manual / team source、runtime authorization、`resource_authorization_updated` stats dirty、授权缓存失效，并入队 `authorizations.update_expire` operation log；默认生产 router 不注册，写接口必须走 touch middleware；该路由不代表批量到期扫描 worker 已迁移 | Go 实现中 | 本轮已通过 targeted Go 测试、全量 Go 测试、vet、tidy diff、integration 包编译、Markdown 相对链接检查和 `git diff --check`；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| W4 authorization return | `DELETE /__aisys__/api/authorizations/{id}/return`、`DELETE /__aisys__/api/my-authorizations/{id}/return` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`pnpm --filter juhe-ai-backend test:authorization-return-driver`、`pnpm --filter juhe-ai-backend test:authorization-return`、`pnpm --filter juhe-ai-backend typecheck`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权归还必须 admin / self 作用域正确，路径 `{id}` 固定为 `resource_authorization_grants.id`；只允许被授权系统账户归还 direct manual grant，资源 owner 自己、团队授权、已回收 / 已归还 grant、没有 active manual source 的 runtime authorization 返回 404；归还后 grant 标记 `returned`，manual source 撤销，runtime authorization 刷新为 `returned`，写 `resource_authorization_returned` stats dirty，发布授权缓存失效，并入队 `authorizations.return` operation log；默认生产 router 不注册，写接口必须走 touch middleware | Go 实现中 | 本轮已通过 targeted Go 测试、Node grant ID 契约回归、backend typecheck、Go 全量测试、vet、tidy diff 和 integration 包编译；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| W4 resource authorization return | `POST /__aisys__/api/accounts/{id}/return-authorization`、`POST /__aisys__/api/my-accounts/{id}/return-authorization`、`POST /__aisys__/api/groups/{id}/return-authorization`、`POST /__aisys__/api/my-groups/{id}/return-authorization` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/app ./internal/store/postgres -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 资源页归还必须通过账号授权实例 ID / 分组资源 ID 解析 runtime authorization，再查找 direct user grant；只允许 direct manual grant 归还，不允许 team grant / owner self / 已回收 / 已归还 / 无 active manual source；成功复用 grant return 副作用，返回 204，account / group 404 文案区分，operation key 分别为 `accounts.return_authorization` / `groups.return_authorization`；默认生产 router 不注册，写接口必须走 touch middleware | Go 实现中 | 本轮已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端真实 Go 后端 smoke、生产切流和 Node `/accounts` / `/groups` / `/authorizations` 删除仍未完成 |
| W4 authorization revoke | `DELETE /__aisys__/api/authorizations/{id}`、`DELETE /__aisys__/api/my-authorizations/{id}` | `gofmt`、`go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权回收必须 admin / self 资源归属作用域正确，路径 `{id}` 固定为 `resource_authorization_grants.id`；管理侧可用 `systemAccountId` 收窄资源 owner，个人侧忽略 query 并固定当前登录用户；回收后 grant 标记 `revoked`，direct 授权撤销 manual source，team 授权只撤销当前资源 / 当前 team source，runtime authorization 刷新为 `revoked`，写 `resource_authorization_revoked` stats dirty，发布授权缓存失效，并入队 `authorizations.revoke` operation log；默认生产 router 不注册，写接口必须走 touch middleware | Go 实现中 | 本轮已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| W4 authorization state schema | `resource_authorization_sources`、`resource_authorization_grants`、`request_quota_hourly_window_configs`、`group_account_stats_dirty` PostgreSQL schema 基线 | `sqlc generate`、`gofmt -w internal/store/postgres/managementauthorizationschema_test.go`、`go test ./internal/store/postgres -run 'TestW4.*MigrationMatchesCurrentContract' -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff` | Go migration 必须补齐 Node 当前权限链所需 `resource_authorization_sources` / `resource_authorization_grants` 表、manual / team 来源约束、system_account / team grant 约束、JSON object limits、active source 部分唯一索引、active user / team grant 部分唯一索引、owner/resource/grantee 分页索引、grant expiry sweep 索引、team quota snapshot 索引和 `resource_authorizations` quota snapshot 索引；同时补默认额度小时窗口 `1/3/6/12/24/72/168/720` 和 `group_account_stats_dirty` 全量脏标记表；schema 本身不代表 `/authorizations` 接管证据 | Go 实现中 | 本轮已通过 schema guard、`sqlc generate`、`gofmt`、`go test ./internal/store/postgres -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff` 和 integration 包编译；后续仍需实现批量到期扫描 worker、最终用户授权完整 CRUD、operation log 真实消费、真实 PG/Redis/Asynq smoke、生产切流和 Node 删除 |
| W4 authorization cache invalidation | 授权业务变化的 Redis runtime state topic | `gofmt -w internal/modules/gatewaycache/system_account_invalidator.go internal/modules/gatewaycache/system_account_invalidator_test.go`、`go test ./internal/modules/gatewaycache -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff` | Go 必须写 Node 兼容 `gateway_cache_invalidation` runtime state：授权业务变化同时发布 `topic:gateway_runtime_cache` 和 `topic:authorization_quota_cache`，API Key 额度变化发布 `topic:api_key_quota_cache` 并保留 trim 后 `apiKeyId`；系统账户状态 / 图像权限现有 API key validation cache version 清理保持不变；团队 PATCH 状态副作用必须调用 `team_authorization_changed`，成员新增 / 移除必须调用 `team_members_changed`，授权创建必须调用 `resource_authorization_created`，普通授权更新和授权有效期更新必须调用 `resource_authorization_updated`，授权归还必须调用 `resource_authorization_returned`，授权回收必须调用 `resource_authorization_revoked`，但不代表 `/authorizations` 已生产接管 | Go 实现中 | gatewaycache 能力已通过 targeted、全量 Go、vet、tidy、integration 包编译、Markdown 链接和 diff 检查；本轮团队 PATCH 状态副作用、成员维护、授权创建、普通授权更新、授权有效期更新、授权归还和授权回收副作用已接入该失效器；后续仍需补真实 PG/Redis smoke |
| W4 authorization usage overview | `GET /__aisys__/api/authorizations/usage/team-details`、`GET /__aisys__/api/my-authorizations/usage/team-details`、`GET /__aisys__/api/authorizations/usage/user-details`、`GET /__aisys__/api/my-authorizations/usage/user-details` | `gofmt`、`go test ./internal/modules/managementauthorizations -count=1`、`go test ./internal/httpapi -run "TestManagementAuthorization|TestRouterRegistersW4ManagementAuthorization" -count=1`、`go test ./internal/store/postgres -run "TestW4AuthorizationUsageWindow|TestManagementAuthorizationUsageOverview|TestManagementResourceAuthorization" -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 授权 team/user usage overview 必须 admin / self 作用域正确，管理侧 admin-only 且可用 `systemAccountId` 收窄资源归属作用域，个人侧强制当前用户；支持 `resourceType/resourceId/granteeSystemAccountId/teamId/startDate/endDate/page/pageSize` 查询参数；日期窗口默认当天、未来日期夹到当天、最大 31 天；分页使用 `pageSize + 1` 和窗口上界 `total`，不做精确总数；PG 只能读取 `juhe_stats.authorization_team_usage_range_windows` / `authorization_user_usage_range_windows` 预聚合窗口表，并只对当前页 join 系统团队、系统账户、账户和分组摘要；不得读取 `usage_records` 或在请求路径实时 `SUM/GROUP BY` 明细 | Go 实现中 | 已新增 migration `000020_w4_authorization_usage_windows.sql`、service / handler / store / router / app、usage range window 刷新 worker 和 targeted 测试；真实 migration apply、usage range window 真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍未完成 |
| Worker | 后台任务 | Go worker 测试 + 游标验证 | 队列、游标、重试和保留期正确 | 未执行 | 这里指 W7 全量 ingest / stats / ops worker 迁移；W1b public API log 单任务消费 runtime 已在 W1b 行记录，不代表后台 worker 已生产接管 |
| 存储 | PostgreSQL + Redis 单模式 | Go store 测试 + PG/Redis/Asynq smoke + SQLite 引用清理检查 | 新库初始化、Redis 队列和运行态可用，Go 运行路径无 SQLite | 部分通过 | W0 已补 baseline migration、sqlc catalog 查询 PoC、testcontainers integration 和 `w0-smoke`；真实容器 / 真实 URL smoke 因本机环境缺失未执行 |
| 网关 | 真实中转网关 | mock AI / real e2e / 性能压测 | SSE、调度、审计、usage 和失败恢复正确 | 未执行 | 最后执行 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-07-06 | 进行中 | AI | 创建 Go 渐进减法迁移规划，先落文档基线，不改业务代码。 |
| 2026-07-06 | 进行中 | AI | 根据最新决策补充 PostgreSQL + Redis 单模式目标，新增 SQLite 移除范围和旧数据离线处理边界。 |
| 2026-07-06 | 进行中 | AI | 按 10 个 agent 审阅结果补齐 PG/Redis 基线前置、网关 W9/W10 门禁、测试命令矩阵、部署收尾、旧 SQLite 文档过渡说明和历史计划方向收口。 |
| 2026-07-06 | 进行中 | AI | 根据长期维护要求补充 Go 技术选型与依赖基线，默认采用 `net/http` + chi、slog、caarlos0/env、godotenv、pgx、sqlc、goose、go-redis、Asynq、Prometheus、testcontainers、golangci-lint 和 govulncheck，并要求 W0 完成依赖 PoC。 |
| 2026-07-07 | 进行中 | AI | 按系统指标统计 Go 化要求补充 `docs/migration/Go系统指标字段迁移清单.md`，把 Node `eventLoopLagMs`、`processEventLoop*`、`db-service`、V8 heap、SQLite 文件体积和 usage shard 文件指标列为 Go 接管时必须删除或替换的字段，并补充 Go runtime、PG、Redis、Asynq、worker lag、stats freshness 和前端页面迁移门禁。 |
| 2026-07-06 | 进行中 | AI | 创建 `backend-go/` W0 工程骨架，落地 Go 1.26.4 本机工具链、w64devkit 2.8.0 C 工具链、sqlc / goose / golangci-lint / govulncheck CLI，并通过单测、race、vet、build、lint、漏洞扫描、版本命令和 health smoke。2026-07-08 已按官方当前稳定版升级本机 Go 到 1.26.5，并同步开发脚本、安装说明和迁移基线。 |
| 2026-07-06 | 进行中 | AI | 根据 4 个 gpt-5.5 xhigh 子 agent 审阅结果补齐 W0 PoC：Asynq health 改为真实 client ping，`rediss` 传 TLS，diagnostics 限制 loopback，health 错误脱敏，新增 sqlc 生成查询、testcontainers integration 和 `juhe-ai-maintenance w0-smoke`。 |
| 2026-07-06 | 进行中 | AI | 继续按子 agent 审阅结论收紧 W0 基线：PG store 隐藏 pgx/sqlc 类型、Redis 增加 namespace / TTL / pipeline / `IncrWithTTL` 和 DB 去重、Asynq 增加显式 Redis timeout、inspector 与 pending smoke；重新通过常规 Go 验证矩阵和三平台构建。 |
| 2026-07-06 | 进行中 | AI | 启动 W1a 公开设置读接口迁移：用 4 个 gpt-5.5 xhigh 子 agent 梳理 Node 路由、前端契约、存储 schema 和验证门禁；确认 `/__aipublic__` 应拆为 W1b，不与公开设置读接口混同。Go 侧新增 `juhe_business.global_settings` migration / seed、sqlc 查询、PG store 方法、`publicsettings` service 和 `GET /__aisys__/api/settings/public` route。 |
| 2026-07-06 | 进行中 | AI | 继续用 4 个 gpt-5.5 xhigh 子 agent 复核 W1a 限流、Go Redis 封装、文档口径和依赖边界；Go 侧新增 Redis Lua 原子 fixed-window limiter、HTTP 可插拔限流器、真实 server Redis state 注入、跨 client / 并发 integration 断言和 router 缺 limiter fail-fast。 |
| 2026-07-06 | 进行中 | AI | 继续用 3 个 gpt-5.5 xhigh 子 agent 复核 Node trust proxy、Go 当前 IP 识别和文档门禁；Go 侧新增 `JUHE_AI_TRUST_PROXY` 解析、客户端 IP resolver、W1a 限流接入和 diagnostics loopback 接入。 |
| 2026-07-06 | 进行中 | AI | 继续用 3 个 gpt-5.5 xhigh 子 agent 复核 W1a smoke 范围、maintenance 命令结构和文档落点；Go 侧新增 `w1a-public-settings-smoke`，用于真实 PG public settings / system settings 读取、HTTP route 精确响应、Redis state 原子限流和跨 client 共享状态 smoke。 |
| 2026-07-07 | 进行中 | AI | 继续用多个 gpt-5.5 xhigh 子 agent 复核 W1b `/__aipublic__` Node 路由、鉴权、scope、限频、公开日志、存储、资源字段和回归脚本；新增 W1b 单模块迁移记录，当前仍保持契约固定、暂不接管。 |
| 2026-07-07 | 进行中 | AI | 继续用 4 个 gpt-5.5 xhigh 子 agent 复核 W1b Go 骨架落点、auth/scope 常量、store port 和文档措辞；Go 侧新增 `internal/modules/publicapi` catalog、`internal/modules/publicapi/auth` Bearer/token hash/source-token scope 骨架、`internal/store/port/publicapi.go` auth store port 和 router 未接管 guard；仍未挂载 `/__aipublic__` 生产路由。 |
| 2026-07-07 | 进行中 | AI | 继续用 4 个 gpt-5.5 xhigh 子 agent 复核 W1b Node 数据模型、16 个 CRUD 行为、Go PG/sqlc 边界和 Redis/队列日志边界；Go 侧新增 W1b PG migration、sqlc query、PostgreSQL auth/log adapter、Redis penalty-window Lua helper、publicapi rate limiter 和 W1b PG foundation integration smoke；仍未挂载 `/__aipublic__` 生产路由，未实现公开日志 capture/队列和 16 个 CRUD。 |
| 2026-07-07 | 进行中 | AI | 继续用 4 个 gpt-5.5 xhigh 子 agent 复核 W1b 公开接口日志快照语义、Asynq 任务边界、handler 落点和文档状态；Go 侧新增 `internal/modules/publicapilog` snapshot/log builder、`internal/jobs/publicapilog` payload/enqueue/handler 和 W1b public API log Asynq integration smoke；仍未挂载 `/__aipublic__` 生产路由，未接入 HTTP capture middleware、worker runtime 或 16 个 CRUD。 |
| 2026-07-07 | 进行中 | AI | 继续用 4 个 gpt-5.5 xhigh 子 agent 复核 W1b public API log worker runtime、Asynq 边界、Node 队列语义取舍和文档口径；Go 侧新增 `internal/jobs/worker` ingest runtime、`internal/app/ingest_worker.go`、`juhe-ai-worker ingest`、invalid payload `SkipRetry` 映射和 Asynq import guard；W1b public API log integration 改为启动真实 worker runtime。仍未挂载 `/__aipublic__` 生产路由，未接入 HTTP capture middleware 或 16 个 CRUD。 |
| 2026-07-07 | 进行中 | AI | 继续用 3 个 gpt-5.5 xhigh 子 agent 复核 W1b HTTP shell 落点、Node body/auth/capture 顺序和 Go 单测方案；Go 侧新增不挂生产 router 的 `internal/httpapi/public_api_shell.go`，覆盖 16 个 catalog endpoint、旧公开路径 404、JSON body 400、body 413、401/403 auth、429 rate limit、response capture 和 public API log 入队。仍未挂载 `/__aipublic__` 生产路由，未实现真实资源 handler 或 16 个 CRUD。 |
| 2026-07-07 | 进行中 | AI | 继续用 4 个 gpt-5.5 xhigh 子 agent 复核 W1b Node 499 契约、Go `ResponseWriter` 包装、499 单测方案和分组公开接口下一步契约；Go 侧 HTTP shell 补齐客户端提前断开 499 日志、response snapshot `statusCode=499`、写入断连错误识别、`httpsnoop` 保留 `Unwrap` / `Flush` / `ReadFrom` 等底层 writer 能力，并记录分组 CRUD 仍缺业务 schema/query/service/handler。仍未挂载 `/__aipublic__` 生产路由，未实现真实资源 handler 或 16 个 CRUD。 |
| 2026-07-07 | 进行中 | AI | W1b public group CRUD 作为真实资源纵切面之一进入记录范围：Go 侧已补 `publicgroups` service、`public_groups` handler、PublicGroup store port、PostgreSQL `publicgroups` adapter、`w1b_public_groups.sql` query 和 `000004_w1b_public_groups.sql` schema / seed，覆盖目标用户自动创建、provider 启用校验、同名幂等、并发幂等重试、默认分组保护、账号绑定保护、owner 复合 FK 和活跃策略路由行锁保护。handler / service 非容器专项已通过；真实 PG integration 待 Docker 环境复跑。后续记录已补齐完整 Redis limiter + public log shell E2E 用例、public route strategy CRUD、public API Key CRUD、public account CRUD 和 account shell E2E 测试代码，当前剩余生产路由挂载、真实 Docker/testcontainers 复跑、生产切流和 Node 删除证据。 |
| 2026-07-07 | 进行中 | AI | W1b public group 完整 Redis limiter + public API log worker shell E2E 用例已补，覆盖目标为真实 Bearer/auth/scope、Redis penalty-window、public API log 入队 / worker 写 PG、响应脱敏和 HTTP shell 贯穿路径；当前主线程 Docker 不可用，真实 E2E 待复跑；仍未挂生产 router。后续记录已补齐 public route strategy、public API Key、public account 纵切面和 account shell E2E 测试代码，当前剩余生产路由挂载、真实 Docker/testcontainers 复跑、生产切流和 Node 删除证据。 |
| 2026-07-07 | 进行中 | AI | W1b public route strategy CRUD 作为第二条真实资源纵切面已补：Go 侧新增 `publicroutestrategies` service、`public_route_strategies` handler、PublicRouteStrategy store port、PostgreSQL `publicroutestrategies` adapter、`w1b_public_route_strategies.sql` query，并在 `000004_w1b_public_groups.sql` 补 route strategy / binding / api key 约束。handler / service 非容器专项已通过；PostgreSQL store / shell E2E 待 Docker 环境复跑。仍未挂生产 router。后续记录已补齐 public API Key、public account 纵切面和 account shell E2E 测试代码，当前剩余生产路由挂载、真实 Docker/testcontainers 复跑、生产切流和 Node 删除证据。 |
| 2026-07-07 | 进行中 | AI | W1b public API Key CRUD 作为第三条真实资源纵切面已补：Go 侧新增 `publicapikeys` service、`public_api_keys` handler、PublicAPIKey store port、PostgreSQL `publicapikeys` adapter、`w1b_public_api_keys.sql` query，并扩展 `000004_w1b_public_groups.sql` 的 `api_keys` 字段 / 索引 / JSON 约束。handler / service 非容器专项已通过；PostgreSQL store / shell E2E 待 Docker 环境复跑。Go 目标采用 hash-only secret 存储，完整 key 只在新增响应返回且 public API log / query string 脱敏。仍未挂生产 router，后续已补齐 public account 纵切面和 account shell E2E 测试代码，当前剩余生产路由挂载、真实 Docker/testcontainers 复跑、生产切流和 Node 删除证据。 |
| 2026-07-07 | 进行中 | AI | W1b public account CRUD 作为第四条真实资源纵切面已补：Go 侧新增 `publicaccounts` service、`public_accounts` handler、PublicAccount store port、PostgreSQL `publicaccounts` adapter、`w1b_public_accounts.sql` query 和 `000005_w1b_public_accounts.sql` schema / provider profile seed。覆盖目标用户 / 目标分组自动创建、provider/profile 校验、上游凭据 AES-GCM 可逆加密、凭据指纹 hash、响应白名单、pending_test 直启拦截、软删除和绑定清理。已通过 `go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./cmd/...`、`go mod tidy -diff`、`sqlc generate` 和 integration 包编译；真实 Docker/testcontainers integration、account shell E2E 真实复跑、生产路由挂载和 Node 删除仍待完成。已提交 `2c2efe4ec feat(go): add backend migration baseline and W1 public APIs`。 |
| 2026-07-07 | 进行中 | AI | W1b 生产 router opt-in guard 已补：`JUHE_AI_PUBLIC_API_ENABLED=false` 默认不注册 `/__aipublic__`，显式 `true` 时 Go 主 server 装配 Bearer auth、Redis penalty-window、Asynq public API log queue 和四类资源 handler；开启时要求 Redis state、Redis queue 和不少于 32 字符的 `JUHE_AI_SECRET`，AI 账户凭据使用该密钥加密。已通过 `go test ./internal/config ./internal/httpapi ./internal/app`。该 guard 只用于灰度验证，不代表 W1b 正式接管；account shell E2E 测试代码已补，真实 PG/Redis/Asynq integration、account shell E2E 真实复跑、反向代理切流和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W1b public account shell E2E 测试代码已补：新增 `TestW1bPublicAccountsShellE2E`，覆盖 Bearer/auth/scope、Redis limiter、public API log 入队 / worker 写 PG、account add/list/update/delete/limited、source/token last_used、账号凭据加密落库、软删除、请求日志 `apiKey/baseUrl/notes/query` 脱敏和响应白名单。已通过 integration 包编译；当前 `go test -v -tags=integration ./internal/testkit/integration -run TestW1bPublicAccountsShellE2E -count=1` 因 Docker 不可用输出 `SKIP`，不计为真实通过。 |
| 2026-07-07 | 进行中 | AI | W1b 新增 `go run ./cmd/juhe-ai-maintenance w1b-public-api-smoke` 独立灰度验证入口：复用生产 public API handler 装配，内部用本地 `httptest` 验证默认 router guard、显式 opt-in mount、临时内置测试 source/token、真实 PostgreSQL / Redis state / Redis queue、public API log 入队和 external `juhe-ai-worker ingest` 写 PG；输出固定 `takeoverEvidence=false`、`productionTakeoverNotEvaluated=true`。已通过 `go test ./internal/maintenance ./internal/app`；真实依赖执行仍待具备 PostgreSQL / Redis / ingest worker 环境后复跑，不构成生产切流或 Node 删除证据。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第一块进入 Go 实现中：用 4 个 gpt-5.5 xhigh 子 agent 分别梳理 Node 路由 / 权限、前端依赖、Go schema 和文档门禁；确认先迁 `GET /__aisys__/api/proxies/options`，且不能在 Go 管理端会话鉴权未迁移前裸挂生产路由。Go 侧新增 `proxy_profiles` migration、sqlc 查询、Management store port、PostgreSQL adapter、`managementproxies` service、HTTP handler、router 注入 guard 和 W2 integration smoke 代码；已通过 `sqlc generate` 和 `go test ./internal/modules/managementproxies ./internal/httpapi ./internal/store/postgres`，真实 Docker/testcontainers integration、生产切流和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端会话鉴权基线已补：用 3 个 gpt-5.5 xhigh 子 agent 梳理 Node `requireAuth`、Go router 装配和 `system_sessions` schema；Go 侧新增 `000007_w2_management_session_auth.sql`、`w2_management_auth.sql`、`managementauth` module、`NewManagementAPIAuthMiddleware`、PostgreSQL session adapter、`JUHE_AI_MANAGEMENT_API_ENABLED=false` 默认关闭配置和 app 装配。覆盖 `juhe_ai_session` cookie、SHA-256 token hash、过期 / 停用会话 401、普通用户初始密码 403、admin 初始密码有效值归零和 W2 opt-in guard；已通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/modules/managementproxies ./internal/httpapi ./internal/store/postgres ./internal/config ./internal/app`、`go test ./...` 和 integration 包编译。`TestW2ManagementAuthPostgresSmoke` 当前因 Docker/testcontainers 不可用输出 `SKIP`，生产切流、前端 smoke 和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第二块 `GET /__aisys__/api/providers/options` 已补：用 4 个 gpt-5.5 xhigh 子 agent 梳理 ProviderDefinition 契约、前端字段依赖、Go 分层落点和后续 options 顺序；确认本块只迁供应商定义 / 协议画像 / endpoint families / 系统账户默认测试模型偏好，不混入 provider model catalog。Go 侧新增 `000008_w2_management_provider_options.sql`、`w2_management_provider_options.sql`、`managementproviders` module、PostgreSQL provider options adapter、HTTP handler、router/app 注入和 integration smoke 代码。已通过 `sqlc generate`、`go test ./internal/modules/managementproviders ./internal/store/postgres ./internal/httpapi ./internal/app`、`go test ./...` 和 integration 包编译；`TestW2ManagementProviderOptionsPostgresSmoke` 当前因 Docker/testcontainers 不可用输出 `SKIP`，生产切流、前端 smoke、W2 全范围和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第三块 API Key 表单依赖的策略路由 options 已补：用 4 个 gpt-5.5 xhigh 子 agent 梳理 Node `route-strategies/options` 和 `my-route-strategies/options` 契约、前端 selected strategy 回填依赖、Go schema/owner 和文档门禁；确认管理侧必须 admin-only 且可全局 / 指定用户查看，用户侧必须强制 self scope 并忽略 query `systemAccountId`。Go 侧新增 `managementroutestrategies` module、`w2_management_route_strategy_options.sql`、PostgreSQL route strategy options adapter、HTTP handler、router/app 注入和 integration smoke 代码，复用既有 `route_strategies` 表不新增 migration。已通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/modules/managementproxies ./internal/modules/managementproviders ./internal/modules/managementroutestrategies ./internal/httpapi ./internal/store/postgres ./internal/config ./internal/app`、`go test ./...` 和 integration 包编译；`TestW2ManagementRouteStrategiesPostgresSmoke` 当前因 Docker/testcontainers 不可用输出 `SKIP`，生产切流、前端 smoke、W2 全范围和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第四块分组 options owner-only 阶段已补：用 4 个 gpt-5.5 xhigh 子 agent 分别核对 Node `groups/options` / `my-groups/options` 契约、Go schema 能力、现有管理接口模式和文档落点；确认当前 Go schema 尚无 `resource_authorizations` / `group_authorization_settings`，因此本块只迁 owner 自有分组选项，不声明等价 Node 授权组 union。Go 侧新增 `managementgroups` module、`w2_management_group_options.sql`、PostgreSQL group options adapter、HTTP handler、router/app 注入和 integration smoke 代码，复用既有 `groups` / `system_accounts` 表不新增 migration。已通过 `sqlc generate` 和 `go test ./internal/modules/managementgroups ./internal/httpapi ./internal/store/postgres ./internal/app`；真实 Docker/testcontainers integration、授权组 union、group account-options、账户 options owner-only 后续记录、生产切流、前端 smoke、W2 全范围和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第五块账户 options owner-only 阶段已补：用 4 个 gpt-5.5 xhigh 子 agent 分别核对 Node `accounts/options` / `my-accounts/options` 契约、Go schema 能力、前端调用面和现有 W2 实现模式；确认当前 Go schema 尚无 `resource_authorizations`、账号标签表和 `account_name_search_*` 名称检索表，因此本块只迁 owner 自有账户下拉，不声明等价 Node 授权账户 union。Go 侧新增 `managementaccounts` module、`w2_management_account_options.sql`、PostgreSQL account options adapter、HTTP handler、router/app 注入和 integration smoke 代码，复用既有 `accounts` / `group_accounts` / `system_accounts` 表不新增 migration。已通过 `sqlc generate` 和 `go test ./internal/modules/managementaccounts ./internal/httpapi ./internal/store/postgres ./internal/app`；真实 Docker/testcontainers integration、授权账户 union、账号标签、group account-options、生产切流、前端 smoke、W2 全范围和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第六块 group account-options owner-only 阶段已补：用 4 个 gpt-5.5 xhigh 子 agent 分别核对 Node `groups/account-options` / `my-groups/account-options` 契约、Go schema 能力、前端调用面和现有 W2 实现模式；确认该接口不是 `accounts/options?groupId` 别名，而是“分组选项 + accountIds”，当前 Go schema 足以实现 owner 自有分组和启用 owner 绑定账号 ID，不声明等价 Node 授权组 union。Go 侧新增 `w2_management_group_account_options.sql`、PostgreSQL group account options adapter、`managementgroups.Service.AccountOptions`、HTTP handler、router/app 注入和 integration smoke 代码，复用既有 `groups` / `group_accounts` / `accounts` / `system_accounts` 表不新增 migration。已通过 `sqlc generate`、`go test ./internal/modules/managementgroups ./internal/httpapi ./internal/store/postgres ./internal/app`、`go test ./...` 和 integration 包编译；`TestW2ManagementGroupAccountOptionsPostgresSmoke` 因 Docker/testcontainers 不可用输出 `SKIP`，真实 Docker/testcontainers integration、授权组 union、授权账户 union、账号标签、生产切流、前端 smoke、W2 全范围和 Node 删除仍待完成。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第七块账号标签 owner-only 阶段已补：用 3 个 gpt-5.5 xhigh 子 agent 对比账号标签、分组授权 union 和账号授权 union 的范围与风险，确认先迁低风险只读标签字典和 `accounts/options?tagIds` 过滤。Go 侧新增 `000010_w2_management_account_tags.sql`、`w2_management_account_tags.sql`、`ListManagementAccountTags`、`account_tag_bindings` 过滤、`managementaccounts.Service.Tags`、`accounts/tags` / `my-accounts/tags` handler、router/app 注入和 W2 integration smoke 扩展；当时仍保留授权账户 union、主账户写入 `tags` / OAuth 与导入标签写路径 / 完整账号 summary 响应 / operation log owner和名称包含搜索缺口，其中 owner-only 名称包含搜索后续已在第九块补齐。另用子 agent 巡检系统指标统计，补强 Go 指标规划里的 W2 operation 名称和 W6/W7 独立迁移门禁。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第八块授权组只读 union 已补：用子 agent 和本地检索复核 Node `groups/options` / `groups/account-options` 的 owner + authorized union 契约，确认范围只覆盖分组下拉只读可见性，不迁授权写接口、授权来源 / grant、授权账户 union、网关调度或统计写链路。Go 侧新增 `000011_w2_management_group_authorizations.sql`、扩展 `w2_management_group_options.sql` / `w2_management_group_account_options.sql` 为 owner + authorized CTE union，补 `ManageableOnly` 下传、authorized DTO 字段、authorized permissions、授权组 `accountIds=[]` 和 W2 integration smoke 断言；当时仍保留授权账户 union、主账户写入 `tags` / OAuth 与导入标签写路径 / 完整账号 summary 响应 / operation log owner和名称包含搜索缺口，其中 owner-only 名称包含搜索后续已在第九块补齐。 |
| 2026-07-07 | 进行中 | AI | W2 管理端只读辅助接口第九块账户 options owner-only 名称包含搜索已补：用子 agent 和本地检索复核 Node `account_name_search_terms` / `account_name_search_documents` 契约，确认范围只覆盖 `accounts/options` / `my-accounts/options` 的 owner-only 远程搜索，不迁账户主列表、授权账户 union 或账户写接口索引维护。Go 侧新增 `000012_w2_management_account_name_search.sql`、扩展 `w2_management_account_options.sql` 为前缀窗口 + terms/documents contains 候选收敛，补 NFKC/trim/1-3 gram helper、SQL source guard、integration smoke search terms fixture 和中间片段 / 非连续误命中 / owner scope 断言；文档同步保留授权账户 union、授权写接口、账户标签更新 / 主账户标签写入、生产切流和 Node 删除门禁。 |
| 2026-07-08 | 进行中 | AI | W2 管理端只读辅助接口第十块账户 options 授权账户只读 union 已补：用 4 个 gpt-5.5 xhigh 子 agent 分别核对 Node 契约、Go schema、测试缺口和文档门禁；确认范围只覆盖 `accounts/options` / `my-accounts/options` 的 owner + 授权实例下拉，不迁授权来源 / grant / 授权写接口、主账户写入 `tags` / OAuth 与导入标签写路径 / 完整账号 summary 响应 / operation log owner、前端切流或网关调度。Go 侧新增 `000013_w2_management_account_authorization_instances.sql`，扩展 `w2_management_account_options.sql` 为 owner + authorized CTE union，补 `accessType=authorized`、授权字段、authorized permissions、本地授权实例 / 来源账户 / 同一授权 ID 启用分组绑定的 `status` 与 `schedulable` 判定，以及 W2 integration smoke 断言；生产 router 仍默认关闭，真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端只读辅助接口第十一块 `system-accounts/options` 已补：用 3 个 gpt-5.5 xhigh 子 agent 分别核对 Node 契约、Go schema/router 和前端 / 文档调用面；确认 Node 当前 `keyword` 只查 `username` / `displayName` 精确或前缀，ID 通过 `ids` 回填，不搜索角色或说明。Go 侧新增 `managementsystemaccounts` service、`management_system_accounts` handler、`ListManagementSystemAccountOptions` sqlc 查询、PostgreSQL adapter、router/app 注入、SQL 白名单 guard 和 W2 integration smoke，复用既有 `system_accounts` 表，不新增 migration；范围只覆盖 admin-only 轻量下拉；系统账户列表读已由后续切片补齐，创建 / 更新 / 改密 / 登录 / 会话撤销仍未迁移。已通过针对性 Go 测试和 integration 包空编译；真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端只读辅助接口第十二块 `authorization-options/grantee-accounts` 和 `my-authorization-options/grantee-accounts` 已补：用 3 个 gpt-5.5 xhigh 子 agent 分别核对 Node 契约、Go schema 能力和前端 / 文档调用面；确认本块只迁授权对象用户候选轻量只读目录，管理侧必须 admin-only，my 路径允许所有登录用户，`ids` 用于已选对象回填，`keyword` 只查 `username` / `displayName` 精确或前缀。Go 侧新增 `managementauthorizationoptions` service、`management_authorization_options` handler、`ListManagementAuthorizationGranteeAccounts` sqlc 查询、PostgreSQL adapter、router/app 注入、SQL 白名单 guard 和 W2 integration smoke，复用既有 `system_accounts` 表，不新增 migration；该块本身不覆盖 grantee teams、授权来源 / grant / 授权写接口、团队 schema 或前端切流；grantee groups 已由后续第十三块补齐。已通过针对性 Go 测试；真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端只读辅助接口第十三块 `authorization-options/grantee-groups` 和 `my-authorization-options/grantee-groups` 已补：用多个子 agent 核对 Node 契约、Go schema 能力、前端 `GroupOptionSummary` 调用面和文档落点；确认本块只迁个人 AI 账户授权创建时的目标用户启用分组候选，管理侧必须 admin-only，my 路径允许登录用户且不改写 `granteeSystemAccountId`，缺少目标用户返回 400。Go 侧新增 `ListManagementAuthorizationGranteeGroups` sqlc 查询、PostgreSQL adapter、service DTO / authorized permissions、admin / my HTTP handler、router/app 注入、SQL 白名单 guard、handler/service/store 测试和 W2 integration smoke，复用既有 `system_accounts` / `groups` 表，不新增 migration；范围不覆盖 grantee teams、授权来源 / grant / 授权写接口、目标分组写入绑定或前端切流。生产 router 仍默认关闭，真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端只读辅助接口第十四块 `authorization-options/grantee-teams` 和 `my-authorization-options/grantee-teams` 已补：用 4 个 gpt-5.5 xhigh 子 agent 分别核对 Node 契约、Go schema 能力、前端 `SystemTeamPrincipalSummary` 调用面和文档落点；确认本块只迁授权团队候选轻量只读目录，管理侧必须 admin-only，my 路径允许所有登录用户，`ids` 用于已选团队回填，`keyword` 只查团队名前缀且 `%` 按字面量处理。Go 侧新增 `000014_w2_management_system_teams.sql`、`ListManagementAuthorizationGranteeTeams` sqlc 查询、PostgreSQL adapter、service DTO、admin / my HTTP handler、router/app 注入、SQL 白名单 guard、handler/service/store 测试和 W2 integration smoke；范围不覆盖系统团队 CRUD、成员写入、团队授权展开、授权来源 / grant / 授权写接口、前端切流或生产接管。生产 router 仍默认关闭，真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端账号标签删除切片已补：用 4 个 gpt-5.5 xhigh 子 agent 分别核对 Node 删除契约、Go schema / sqlc 落点、前端调用依赖和文档 / 测试边界；确认该删除切片当时只迁 `DELETE /__aisys__/api/accounts/tags/:tagId` 和 `DELETE /__aisys__/api/my-accounts/tags/:tagId`，不覆盖 `PATCH /accounts/:id/tags`、账户创建 / 编辑携带 `tags`、OpenAI OAuth 创建或导入标签写入；后续独立 PATCH opt-in 已在后续记录补齐。Go 侧新增标签删除 store port、PostgreSQL 事务 `FOR UPDATE` 行锁、绑定复查、service 错误映射、admin / self HTTP handler、router/app 注入、handler/service/store/router 测试和 W2 integration smoke 断言；成功返回 `204`，绑定中返回 `400 { message: "标签已绑定账户，不能删除" }`，不存在 / 越权返回 `404 { message: "标签不存在" }`。生产 router 仍默认关闭，真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端默认测试模型偏好写入切片已补：并行子 agent 对比了团队只读、账号标签 PATCH 和 provider 默认测试模型三个候选，最终选择最小写接口 `PUT /__aisys__/api/providers/:code/default-test-model`。Go 侧新增 `UpsertManagementProviderDefaultTestModelPreference` sqlc 查询、store port writer、`managementprovidermodels.Service.SetDefaultTestModel`、HTTP handler、router/app 注入和 integration smoke 扩展；校验模型必须在目标系统账户可见目录中、状态为 `active` 且为文本生成模型，普通用户强制 self scope，管理员可指定 `systemAccountId`。该块当时只写 `provider_default_test_models` 偏好，不迁自定义模型 CRUD、供应商定义写接口、账号测试任务或生产切流；自定义模型 CRUD 已在 2026-07-10 后续补充切片迁入 Go opt-in。Node 删除仍待 W2 全范围和单 owner 证据。 |
| 2026-07-08 | 进行中 | AI | W2 管理端账号标签独立 PATCH opt-in 已补：用 3 个子 agent 复核 Node 契约、Go 实现风险、integration smoke 和文档落点；范围只覆盖 `PATCH /__aisys__/api/accounts/:id/tags` 与 `PATCH /__aisys__/api/my-accounts/:id/tags` 的当前作用域标签集合替换，不迁账户创建 / 编辑请求体 `tags`、OpenAI OAuth 创建、导入标签写入、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 或其他写接口 operation log。Go 侧新增账号标签更新 store port、PostgreSQL 事务锁、`ON CONFLICT DO UPDATE RETURNING` 同名标签复用、授权实例 resource / grantee 约束、service 校验、admin / self HTTP handler、尾随 JSON 拒绝、router/app 注入、handler/service/store/router 测试和 W2 account options integration smoke；已通过 `sqlc generate`、目标 Go 包测试和 integration 包编译；`TestW2ManagementAccountOptionsPostgresSmoke` 需在 Docker/testcontainers 健康环境复跑后才能记为真实通过。生产 router 仍默认关闭，主账户标签写路径、完整账号详情响应、operation log、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端账号标签 PATCH 减法响应切片已补：用 4 个子 agent 复核 Node 完整 summary 契约、Go status/schedulable 折叠风险、测试缺口和前端调用依赖；确认前端对标签 PATCH 响应零字段依赖，提交后会重新拉列表 / 详情，因此本切片不继续追 Node `findAccountSummaryAsync + sanitizeAccountResponse` 完整账号响应，而是把 Go / 前端契约收窄为 `200 { data: { id, tags: [{ id, name }] } }`。Go 侧删除标签 PATCH 后置完整 summary mapper，`GetManagementAccountTagUpdateSummary` 收敛为 `GetManagementAccountTagUpdateAccount`，只读取 `id/systemAccountId/name/ownerSystemAccountId`；SQL guard 禁止该查询重新拼 `source_accounts`、`status/schedulable`、凭据、并发或可用性字段。operation log 仍保留资源名和 before/after 标签值。已通过 `sqlc generate`、`go test ./internal/modules/managementaccounts ./internal/store/postgres ./internal/httpapi ./internal/app`，真实 Docker/testcontainers integration、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 管理端 operation log 读接口切片已补：用 4 个子 agent 分别核对 Node 路由契约、Go operation log schema / store、前端字段依赖和文档 / 测试门禁；范围覆盖 `GET /__aisys__/api/operation-logs`、`GET /__aisys__/api/operation-logs/:id`、`GET /__aisys__/api/my-operation-logs` 和 `GET /__aisys__/api/my-operation-logs/:id`，不迁保留清理或其他写接口 operation log。Go 侧新增 `OperationLogReader` port、PostgreSQL/sqlc 列表与详情查询、summary 倒排搜索、traceId `COLLATE "C"` 前缀范围、targeted/all_users 个人列表窗口合并、`managementoperationlogs` service、admin / self HTTP handler、router/app 注入、SQL guard、service / handler / router 测试。列表保持轻量 DTO 和非精确 `total` 上界，个人详情按 `full/summary/all_users` 脱敏。已通过 `sqlc generate`、目标 Go 包测试、`go test ./...`、`pnpm --dir frontend typecheck` 和 integration 包编译；`TestW2ManagementOperationLogsPostgresSmoke` 已补但在本机 Docker 健康检查下 `SKIP`，前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 operation log 读路径独立 smoke 命令已补：用 3 个子 agent 对比 operation log 保留清理、账户列表详情和前端 smoke 候选后，选择先落可重复的本地灰度证据入口。Go 侧新增 `go run ./cmd/juhe-ai-maintenance w2-operation-logs-smoke`，要求真实 PostgreSQL、Redis state 和 Redis queue，通过本地 `httptest` 验证默认 router guard、显式 opt-in mount、临时 system account / session / operation log fixture、admin / self 列表与详情、个人端脱敏和普通用户访问管理端 403；输出固定 `takeoverEvidence=false` 和 `productionTakeoverNotEvaluated=true`。已通过 `go test ./internal/maintenance ./cmd/juhe-ai-maintenance`；真实依赖执行、反向代理 path-split、生产流量观测、前端 smoke 和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W2 系统账户列表读接口已补：用 5 个 gpt-5.5 xhigh 子 agent 并行评估账户列表 / 详情、系统账户读写、operation log 保留清理和前端 smoke 候选，最终选择 `GET /__aisys__/api/system-accounts` 作为小闭环读切片。Go 侧新增 `ListManagementSystemAccounts` sqlc 查询、PostgreSQL adapter、store port、`managementsystemaccounts.Service.List`、admin-only HTTP handler、router/app 注入和 W2 integration smoke 扩展；列表保持 Node `page/pageSize/total/hasMore` 形态，`total` 是窗口上界，不做全表 `COUNT(*)`，SQL guard 禁止 `password_hash` 和模糊扫描。该块不迁系统账户创建 / 更新 / 改密 / 登录 / 会话撤销，也不迁账户完整列表 / 详情 summary。生产 router 仍默认关闭，真实 Docker/testcontainers、前端 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W3 当前用户读切片已补：根据多个子 agent 对账户列表、系统账户写 / 登录链路、授权写和 operation log 候选的风险梳理，选择先迁 `GET /__aisys__/api/auth/me`。Go 侧在 `managementauth.Authenticator` 中新增 current-user 鉴权分支，保留普通后台 middleware 对普通初始密码用户的 `403 must_change_password` 拦截，同时允许 `/auth/me` 返回 `{ id, username, displayName, role, mustChangePassword }`；router/app 新增 `JUHE_AI_MANAGEMENT_API_ENABLED` opt-in handler，默认生产不注册。新增 W3 迁移记录并同步功能契约、Go 架构基线、测试手册和模块清单。已通过 targeted Go 测试、`go test ./...`、integration 包编译、Node 初始密码边界回归、Markdown 相对链接检查和 `git diff --check`；`TestW2ManagementAuthPostgresSmoke` 已扩展覆盖 admin current-user 和普通初始密码用户 `/auth/me` 200 / 普通管理接口 403，但本机 Docker/testcontainers 不可用时仍为 `SKIP`。本切片当时不迁登录、验证码、登出、改密、session 创建 / revoke、用户资料修改或系统账户写接口；写鉴权 touch 已由后续切片补齐。 |
| 2026-07-08 | 进行中 | AI | W3 当前会话 logout 切片已补：根据 3 个子 agent 对 Node `/auth/logout`、`PATCH /auth/me` 和后续登录 / 改密风险的只读梳理，选择先落当前 session 撤销的小闭环。Go 侧新增 `ManagementSessionRevoker` port、`RevokeManagementSessionByTokenHash` sqlc 查询、PostgreSQL adapter、`managementauth.Authenticator.LogoutCookie`、`NewManagementLogoutHandler`、router/app 注入和 W2 auth integration smoke 扩展；契约保持允许无 Cookie / 无有效登录态调用，有 Cookie 时只按 SHA-256 token hash 删除当前 session，成功后清 `juhe_ai_session` Cookie，撤销失败返回 500 且不清 Cookie。已通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app`、`go test ./...`、integration 包编译、`go mod tidy -diff`、Node `test:system-account-session-driver` 和 `test:auth-must-change-password-boundary`；`TestW2ManagementAuthPostgresSmoke` 因本机 Docker/testcontainers 不可用输出 `SKIP`，不计真实 PG integration 通过。该块只覆盖当前 token hash revoke，当时不迁登录、验证码、改密、session 创建、其他会话撤销、完整 Cookie 安全配置或系统账户写接口；写鉴权 touch 已由后续切片补齐，logout 清 Cookie 安全属性已在 2026-07-09 后续切片补齐。生产 router 仍默认关闭，真实 Docker/testcontainers、前端 logout smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W3 当前用户改密切片已补：按 4 个 gpt-5.5 xhigh 子 agent 对 Node `/auth/change-password` 契约、密码 hash / session repository、Go 落点和测试 / 文档缺口的并行梳理，选择只迁自助改密小闭环。Go 侧新增 Node 兼容 PBKDF2-SHA512 `HashPassword` / `VerifyPassword`、`managementauth.PasswordService`、按用户名读取密码凭据的窄 SQL、当前账号密码更新 SQL、`RevokeOtherManagementSessionsForAccount`、`NewManagementPasswordChangeHandler`、router/app 注入、handler/service/store/router 测试和 W2 auth integration smoke 扩展；普通初始密码用户可只提交新密码，普通改密和管理员改密必须校验旧密码，成功后清除 `mustChangePassword` 并撤销同账号其他 session，响应不返回 `password_hash`，自助改密不写 operation log。已通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app` 和 integration 包编译；真实 `TestW2ManagementAuthPostgresSmoke` 仍需 Docker/testcontainers 健康环境复跑。该块当时不迁登录、验证码、session 创建、完整 Cookie 安全配置、系统账户管理端重置密码或 Node `/auth` 删除；写鉴权 touch 已由后续切片补齐。 |
| 2026-07-08 | 进行中 | AI | W3 session touch 收口切片已补：按 4 个子 agent 分别核对 Node 写模式 touch 契约、Go 落点、测试 / 文档缺口和是否存在未提交 Node 生产补丁；确认当前没有未提交 Node 改动，Node 生产修复已在历史提交中。Go 侧新增 `ManagementSessionToucher` port、`TouchManagementSession` sqlc 查询和 PostgreSQL adapter，`managementauth.Authenticator` 增加写鉴权 touch 分支，按 60 秒最小间隔更新当前 `system_sessions.last_seen_at`；`GET /auth/me` 和 logout 不 touch，`PATCH /auth/me`、`POST /auth/change-password` 和已迁移管理写接口使用 touch middleware / 写鉴权，router 对写接口缺失 touch middleware fail-fast。补充 handler/router/store/integration 测试和迁移文档，已通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app`、`go test ./...`、integration 包编译、Node session/auth 回归、`go mod tidy -diff` 和 `git diff --check`；真实 `TestW2ManagementAuthPostgresSmoke` 因 Docker 未运行输出 `SKIP`，不计真实 PG integration 通过。 |
| 2026-07-08 | 进行中 | AI | W3 captcha 前置切片已补：根据 8 个 gpt-5.5 xhigh 子 agent 对 Node captcha / login / system accounts 写接口 / Go Redis state / 测试文档 / 开源库取舍的并行梳理，选择先迁 `GET /__aisys__/api/auth/captcha` 和 Go 内部验证码校验基础。Go 侧新增 Redis `GetDelete` 原子消费、`managementauth.CaptchaService`、PNG data URL 渲染、Redis state challenge、60 次 / 分钟生成限流、`NewManagementCaptchaHandler`、router/app 注入，以及 system API IP 读限流对 captcha GET 的跳过规则；不引入额外 captcha 库，不退回进程内 Map。该切片只提供登录前置能力，不迁 `POST /auth/login`、登录失败锁定、session 创建、Cookie 安全配置、系统账户写接口或 Node `/auth` 删除。已通过 `gofmt` 和 `go test ./internal/modules/managementauth ./internal/httpapi ./internal/platform/redis ./internal/app -count=1`；真实 PG/Redis integration、前端登录页 smoke、生产单 owner 切流和 Node 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W3 登录 / session 创建小切片已补：根据 6 个 gpt-5.5 xhigh 子 agent 对 Node login 契约、PG session 原子写入、Redis 登录失败锁、测试矩阵和文档落点的并行梳理，迁移 `POST /__aisys__/api/auth/login`。Go 侧新增 Redis failure-lock Lua、`LoginGuardService`、`LoginService`、`NewManagementLoginHandler`、`CompleteManagementLogin` CTE、Cookie `Secure` / `SameSite` 配置、router/app 注入和 W3 PG+Redis login smoke；登录顺序固定为严格 JSON、验证码 `GETDEL`、登录锁、active 账号密码校验、PG session token hash / `last_login_at` / 初始 `last_seen_at`、成功清锁和 Cookie 签发。并发改密窗口通过 `password_hash = verified_password_hash` 二次确认收口；成功响应只返回非敏感用户摘要，不复刻 Node `passwordHash` 泄漏风险。该块仍只是 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 下的 Go opt-in 灰度能力，不代表完整 `/auth` 接管；前端 auth smoke、生产切流、完整 auth / session 生产接管和 Node `/auth` 删除仍待完成；系统账户写接口另随 W3 系统账户写接口切片推进，当前已补 create 和完整 mixed PATCH。 |
| 2026-07-08 | 进行中 | AI | W3 系统账户 reset-password 子动作已补：用 3 个 gpt-5.5 xhigh 子 agent 复核 Go 契约 / SQL、文档一致性和测试风险后，迁移 `PATCH /__aisys__/api/system-accounts/{id}` 中的管理端重置密码子动作。Go 侧新增 `ManagementSystemAccountPasswordResetter` port、`ResetManagementSystemAccountPassword` 单语句 CTE、PostgreSQL adapter、`managementsystemaccounts.Service.ResetPassword`、`NewManagementSystemAccountPasswordResetHandlerWithOperationLog`、router/app 注入和 service / handler / SQL guard / app 测试；仅允许 `super_admin` 提交 `password/mustChangePassword`，拒绝完整 PATCH 字段，拒绝 `mustChangePassword:null`，密码 hash 使用 Node 兼容 PBKDF2-SHA512，成功后删除目标账户全部 session，operation log 只写敏感字段“已重置”并记录撤销 session 数，不记录明文密码。已通过 `sqlc generate`、`gofmt`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app`；真实 PG+Redis+operation-log integration、前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W3 系统账户 status-only 子动作已补：并行使用 4 个 gpt-5.5 xhigh explorer 复核 Node PATCH 契约、Go 扩展点、测试模式和文档落点后，继续迁移 `PATCH /__aisys__/api/system-accounts/{id}` 中的状态更新窄子动作。Go 侧把原 password reset handler 收口为 `ManagementSystemAccountPatchHandler` 分发器，新增 `ManagementSystemAccountStatusUpdater` port、`UpdateManagementSystemAccountStatus` 单语句 CTE、PostgreSQL adapter、`managementsystemaccounts.Service.UpdateStatus`、status-only handler 分支和 service / handler / SQL guard / app 测试；仅允许 `super_admin` 提交 `{ status: "active" | "disabled" }`，拒绝 `password` 混用、`username` 和完整 PATCH 字段，PG 先以 `MATERIALIZED` 锁集合和聚合计数固定启用超级管理员集合，再锁目标账户，禁止停用最后一个启用 `super_admin`，禁用时删除目标账户全部 session，operation log 只记录状态变化和撤销 session 数。已通过 `sqlc generate`、`gofmt`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`；真实 PG+Redis+operation-log integration、前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W3 系统账户 profile-only 子动作已补：并行使用 4 个 gpt-5.5 xhigh explorer 复核 Node 系统账户 PATCH 契约、Go 扩展点、PG 约束和文档门禁后，选择继续迁移 `PATCH /__aisys__/api/system-accounts/{id}` 中不触发网关缓存的资料字段更新，而不是先做默认资源 fanout 更重的 create。Go 侧新增 `ManagementSystemAccountProfileUpdater` port、`UpdateManagementSystemAccountProfile` 单语句 CTE、PostgreSQL adapter、`managementsystemaccounts.Service.UpdateProfile`、profile-only handler 分支和 service / handler / SQL guard / app 测试；仅允许 `super_admin` 提交 `displayName/description/role/mustChangePassword`，拒绝 `password/status/imageGenerationEnabled/username` 和未知字段，PG 先以 `MATERIALIZED` 锁集合和聚合计数固定启用超级管理员集合，再锁目标账户，禁止降级最后一个启用 `super_admin`，显示名唯一冲突由 PG `23505` 最终裁决，operation log 只记录非敏感资料字段变化。已通过 `sqlc generate`、`gofmt`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`；create 默认资源 fanout、`imageGenerationEnabled`、真实 PG+Redis+operation-log integration、前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍待完成。 |
| 2026-07-08 | 进行中 | AI | W3 系统账户 PATCH 真实链路烟测已补：根据 4 个子 agent 和本地复核结果，先补 `TestW3ManagementSystemAccountsPatchPostgresRedisSmoke`，不继续扩大到 `POST /system-accounts`。新增 smoke 通过 HTTP `PATCH /__aisys__/api/system-accounts/{id}` 覆盖 reset-password、status-only、profile-only 和最后一个启用 `super_admin` 降级保护，贯穿真实 PostgreSQL 更新、目标 session 撤销、Redis / Asynq `operation-logs` 入队、`juhe-ai-worker ingest` 写入 `juhe_dataset.operation_logs`、密码日志脱敏和撤销 session 元数据。已通过 `gofmt`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`；`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountsPatchPostgresRedisSmoke -count=1` 在当前 Windows 环境因 Docker / testcontainers provider 不可用输出 `SKIP`，不计真实通过。`POST /system-accounts` 暂缓：Go schema 尚缺默认 API Key `key_secret_encrypted`，也缺默认内置分组 / 默认普通路由 / 默认 API Key fanout 事务和创建日志脱敏策略，不能用单表 insert 冒充创建迁移。 |
| 2026-07-08 | 进行中 | AI | W3 系统账户 create 子动作已补：迁移 `POST /__aisys__/api/system-accounts` 的 Go opt-in 灰度实现。Go 侧新增 `000017_w3_system_account_create.sql` 补 `api_keys.key_secret_encrypted` 当前字段、`w3_management_system_account_create.sql`、`ManagementSystemAccountCreator` port、PostgreSQL 事务 fanout、`managementsystemaccounts.Service.Create`、`NewManagementSystemAccountCreateHandlerWithOperationLog`、router/app 注入和 service / handler / SQL guard 测试；仅允许 `super_admin`，严格拒绝未知字段、尾随 JSON、非法枚举、`null` 布尔和用户名 / 用户名称 / 密码空白字符，事务创建系统账户、7 个默认分组、6 条非 hybrid 默认普通路由、6 条路由分组绑定和 6 个默认 API Key，默认 API Key 明文只以 Node 兼容 AES-GCM 密文落库，响应和 operation log 不回显明文，密码日志标记敏感且只写“已设置”。已通过 `gofmt` 和 `go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`；真实 PG+Redis+Asynq create integration、前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W3 logout Cookie 安全属性收口已补：复核 Node `sessionCookieOptions({ maxAge: 0 })` 和 Go logout 后，确认 Go 登录 Cookie 已使用运行时 `Secure` / `SameSite`，但清 Cookie 未带同样属性。Go 侧将 `NewManagementLogoutHandler` 改为接收 `config.Config`，`clearManagementSessionCookie` 复用 `managementCookieSameSite` 与 `CookieSecure`，同步 app 装配、handler 单测和 W2 auth integration smoke 断言。已通过 `gofmt`、`go test ./internal/httpapi ./internal/app -count=1` 和 `go test ./internal/modules/managementauth ./internal/store/postgres -count=1`；真实 HTTPS、浏览器 Cookie 行为、反向代理和前端 logout smoke 仍待生产切流阶段验证。 |
| 2026-07-09 | 进行中 | AI | W3 系统账户完整 mixed PATCH 已补：将 `PATCH /__aisys__/api/system-accounts/{id}` 从 reset-password / status-only / imageGeneration / profile-only 四个互斥窄子动作收口为完整 partial update。Go 侧新增 `w3_management_system_account_update.sql`、`ManagementSystemAccountUpdater` port、PostgreSQL adapter、`managementsystemaccounts.Service.Update`、统一 PATCH handler 和混合字段 operation log；允许 `displayName/description/password/role/status/mustChangePassword/imageGenerationEnabled` 任意组合，禁止 `username` 和未知字段，提交 `password` 或 `status="disabled"` 撤销目标全部 session，状态或图像权限真实变化清理 gateway runtime cache / API Key validation cache，状态原因优先，密码日志只写“已重置”。已通过 `sqlc generate`、`gofmt`、`go mod tidy -diff`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、integration 包编译、`git diff --check` 和本次改动 Markdown 相对链接检查；真实 mixed PATCH PG+Redis+Asynq integration、前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W3 当前用户会话列表 / 撤销切片已补：迁移 `GET /__aisys__/api/auth/sessions` 和 `DELETE /__aisys__/api/auth/sessions/{id}` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementSessionManager` port、PostgreSQL/sqlc 列表与按当前系统账户撤销查询、`managementauth.SessionService`、HTTP list/revoke handler、router/app 注入和 service / handler / store / app 测试；列表只返回当前系统账户未过期 session，`current` 标记当前 Cookie session，`total` 只做窗口上界，不读取 / 返回 `token_hash`；撤销只允许按当前系统账户 + session ID 删除，撤销当前 session 时清 Cookie，写鉴权按 60 秒窗口 touch 当前 session。已通过 `sqlc generate`、`gofmt`、`go mod tidy -diff`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...` 和 integration 包编译；`TestW2ManagementAuthPostgresSmoke` 已扩展覆盖 list 不 touch 和 revoke 后 current session touch，但当前 Windows Docker / testcontainers provider 不可用时输出 `SKIP`，不计真实 PostgreSQL integration 通过。前端会话管理入口、安全日志、生产单 owner 切流和 Node `/auth` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W3 系统账户 create 真实链路 smoke 代码已补：新增 `TestW3ManagementSystemAccountCreatePostgresRedisSmoke`，通过真实 HTTP `POST /__aisys__/api/system-accounts` 验证 `super_admin` session 写鉴权、PG 系统账户写入、7 个默认分组、6 条非 hybrid 默认普通路由、6 条路由分组绑定、6 个默认 API Key、默认 API Key `key_secret_encrypted` 密文落库、响应不回显默认资源 ID / 明文 API Key / 密码，以及 Asynq `operation-logs` 入队和 `juhe-ai-worker ingest` 写入。已通过 `gofmt`、integration 包编译、targeted Go 测试和 `go test ./... -count=1`；实际运行因当前 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PostgreSQL / Redis / Asynq / operation log worker 通过。前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W3 前端 auth / session / 系统账户页本地 smoke 已补：前端新增当前用户会话摘要 / 列表 / 撤销类型，`api.auth.sessions` 和 `api.auth.revokeSession`，头像菜单“会话管理”入口，`SessionManagementModal` 分页刷新 / 撤销会话 / 撤销当前会话后清本地登录态并返回登录页；系统账户页 smoke 固定 `super_admin` 写入口、create payload 不携带默认资源 ID、编辑和重置密码统一走 update API、前端类型和页面不引用 `passwordHash/tokenHash/key_secret` 等敏感字段。新增 `pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke`，并已通过该 smoke、`pnpm --filter juhe-ai-frontend typecheck` 和 `pnpm --filter juhe-ai-frontend build`；由于当前 pnpm 11 非交互依赖检查要求显式批准构建脚本，本轮同步 `pnpm-workspace.yaml` 的 `allowBuilds` 固定 `core-js` / `esbuild`。该 smoke 不连接真实 Go 后端，不代表浏览器 Cookie / HTTPS / 反向代理 / 生产切流完成。 |
| 2026-07-09 | 进行中 | AI | W4 系统团队 PATCH 已补：迁移 `PATCH /__aisys__/api/system-teams/{id}` 的 Go opt-in 灰度实现，并补齐授权状态基础表。Go 侧新增 `ManagementSystemTeamUpdater` port、`managementsystemteams.Service.Update`、HTTP PATCH handler、router/app 注入、`FindManagementSystemTeamForUpdate` / `UpdateManagementSystemTeam` sqlc 查询和 Postgres 事务状态机；支持 name / description / status partial update，禁用团队时 revoke 当前 active team source 并刷新 runtime authorization effective source，启用团队时按 active team grants 展开到 active members，授权变化写 `group_account_stats_dirty('__all__')` 并发布 `team_authorization_changed` 到 gateway runtime / authorization quota cache topic；新增 `000019_w4_authorization_quota_and_stats_state.sql` 固定默认额度小时窗口和统计脏标记表。已通过 `sqlc generate`、`gofmt`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译、Markdown 链接检查和 `git diff --check`；授权 `/expire` 到期扫描路由 / 到期 worker、真实 PG/Redis/Asynq operation-log integration、前端团队页 smoke、生产单 owner 切流和 Node `/system-teams` / `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 系统团队成员维护已补：迁移 `POST /__aisys__/api/system-teams/{id}/members` 和 `DELETE /__aisys__/api/system-teams/{id}/members/{memberId}` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementSystemTeamMemberManager` port、`managementsystemteams.Service.AddMembers` / `RemoveMember`、HTTP 成员新增 / 移除 handler、router/app 注入、`FindActiveManagementSystemTeamForUpdate` sqlc 查询和 Postgres 事务副作用；新增成员要求 active 团队与 active 系统账户，保持单次最多 20 个和团队 active 成员最多 20 个，已存在 active 成员幂等，removed 成员可重新加入；新增 / 重新加入成员时按当前 active team grants 展开最终用户授权，移除成员时 revoke 该成员 active team source 并刷新 effective source，成员变化写 `group_account_stats_dirty('__all__', 'team_members_changed')` 并发布 `team_members_changed` 到 gateway runtime / authorization quota cache topic；operation log 写 `system_teams.add_members` / `system_teams.remove_member`。已通过 `sqlc generate`、`gofmt` 和 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端团队页 smoke、生产单 owner 切流、授权 `/expire` 到期扫描路由 / 到期 worker 和 Node `/system-teams` / `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权创建已补：迁移 `POST /__aisys__/api/authorizations` 和 `POST /__aisys__/api/my-authorizations` 的 Go opt-in 灰度实现。Go 侧新增 `managementauthorizations.Service`、`ManagementResourceAuthorizationCreator` port、HTTP admin / self 创建 handler、router/app 注入和 PostgreSQL 事务写链路；管理侧必须 admin 且显式指定资源归属人，个人侧强制 self scope；账户个人授权必须指定目标分组，团队授权按 active 成员 fanout；写入或恢复 `resource_authorization_grants`、manual / team source、授权账户实例和目标分组绑定，记录 quota hourly window、`resource_authorization_created` stats dirty 和授权缓存失效；operation log 写 `authorizations.create`。已通过 `gofmt` 和 `go test ./internal/modules/managementauthorizations ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权更新已补：迁移 `PATCH /__aisys__/api/authorizations/{id}` 和 `PATCH /__aisys__/api/my-authorizations/{id}` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementResourceAuthorizationUpdater` port、`managementauthorizations.Service.Update`、HTTP admin / self 更新 handler、router/app 注入和 PostgreSQL 事务写链路；路径 `{id}` 固定为 `resource_authorization_grants.id`，管理侧可用 `systemAccountId` 收窄资源 owner，个人侧强制当前登录用户；严格 JSON 只允许 `status/expiresAt/limits`，支持 `status=active/paused`、未来到期时间或 `null`、额度 object 或 `null` 清空；更新后同步 grant、manual / team source、runtime authorization、`resource_authorization_updated` stats dirty 和授权缓存失效，operation log 写 `authorizations.update`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权有效期更新已补：迁移 `PATCH /__aisys__/api/authorizations/{id}/expire` 和 `PATCH /__aisys__/api/my-authorizations/{id}/expire` 的 Go opt-in 灰度实现。Go 侧复用 `managementauthorizations.Service.Update` 和 PostgreSQL 授权更新事务，新增 HTTP admin / self expire update handler、router/app 注入和 `authorizations.update_expire` operation log；路径 `{id}` 固定为 `resource_authorization_grants.id`，管理侧可用 `systemAccountId` 收窄资源 owner，个人侧忽略 query 并固定当前登录用户；严格 JSON 只允许 `expiresAt/limits`，`expiresAt` 必填且可为 `null`，`limits` 可选且可为 `null`，拒绝 `status`，更新后写 `resource_authorization_updated` stats dirty 并发布授权缓存失效。已通过 targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译、Markdown 相对链接检查和 `git diff --check`；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权归还已补，并同步修复 Node 生产 ID 契约：`DELETE /__aisys__/api/authorizations/{id}/return` 和 `DELETE /__aisys__/api/my-authorizations/{id}/return` 的 Go opt-in 灰度实现已按 grant ID 迁移；Node `returnResourceAuthorizationForGrantee` 同步从按 `runtime_authorization.id` 查找改为按 `resource_authorization_grants.id` 查找，避免授权列表按钮传 grant ID 时归还失败，同时修复 `authorization-return-regression` seed 缺少当前必填 `providerProtocolProfileId` 的回归脚本问题。Go 侧新增 `ManagementResourceAuthorizationReturner` port、`managementauthorizations.Service.Return`、HTTP admin / self 归还 handler、router/app 注入和 PostgreSQL 事务写链路；只允许被授权系统账户归还 direct manual grant，撤销 manual source、刷新 runtime authorization 为 `returned`、写 `resource_authorization_returned` stats dirty 并发布授权缓存失效，operation log 写 `authorizations.return`。已通过 targeted Go 测试、`pnpm --filter juhe-ai-backend test:authorization-return-driver`、`pnpm --filter juhe-ai-backend test:authorization-return`、`pnpm --filter juhe-ai-backend typecheck`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译、本次改动 Markdown 相对链接检查和 `git diff --check`；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-10 | 进行中 | AI | W4 资源页授权归还兼容入口已补：迁移 `POST /__aisys__/api/accounts/{id}/return-authorization`、`POST /__aisys__/api/my-accounts/{id}/return-authorization`、`POST /__aisys__/api/groups/{id}/return-authorization` 和 `POST /__aisys__/api/my-groups/{id}/return-authorization` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementResourceAuthorizationResourceReturner` port、`managementauthorizations.Service.ReturnByResource`、HTTP account/group admin/self resource return handler、router/app 注入和 PostgreSQL 资源解析链路；账户路径通过授权账户实例的 `authorization_instance_authorization_id` 解析 runtime authorization，分组路径按 `resource_type='group'` + grantee 解析 runtime authorization，随后只查 direct system_account grant 并复用 grant return 的 manual source 撤销、runtime refresh、stats dirty 和授权缓存失效；operation log 按 Node 资源页入口使用 `accounts.return_authorization` / `groups.return_authorization`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端真实 Go 后端 smoke、生产切流和 Node `/accounts` / `/groups` / `/authorizations` 删除仍待完成。 |
| 2026-07-10 | 进行中 | AI | W4 资源页归还权限标记已补：`accounts/options`、`my-accounts/options`、`groups/options`、`my-groups/options`、`groups/account-options` 和 `my-groups/account-options` 在 Go opt-in 下会查询授权 runtime 是否存在 active manual source，并据此设置 `permissions.canReturnAuthorization`。该标记对齐 Node 完整摘要里的 `hasActiveManualAuthorizationSource` 规则：owner 资源、纯团队来源授权、非 active manual source 都保持 false；只用于让已迁 `return-authorization` 资源页入口可见，不代表账户 / 分组完整列表、详情、本地调度配置或流量迁移接口已迁移。已通过 `sqlc generate`、`gofmt`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译和 `git diff --check`；真实 PG/Redis/Asynq operation-log integration、浏览器连接真实 Go 后端 smoke、生产切流和 Node `/accounts` / `/groups` 删除仍待完成。 |
| 2026-07-10 | 进行中 | AI | W2 代理管理列表读已补：`GET /__aisys__/api/proxies` 进入 Go opt-in 灰度实现，复用管理端 session 鉴权并要求 admin / super_admin。Go 侧新增 `managementproxies.Service.List`、HTTP admin-only handler、router/app 注入、`ListManagementProxies` sqlc 查询和 PostgreSQL adapter；查询只按代理名称前缀搜索，固定 `updated_at DESC, id DESC`，使用 `pageSize+1` 判断 `hasMore` 且 `total` 只作窗口上界，不读取 `password_encrypted`、解密密码、`proxyUrl` 或 system account / 创建更新时间字段；列表包含启用和停用代理以及测试状态摘要。本切片不覆盖代理创建 / 更新 / 删除 / 检测、真实网络诊断、生产切流或 Node `/proxies` 删除。 |
| 2026-07-10 | 进行中 | AI | W5 代理管理 create/update/delete 已补：`POST /__aisys__/api/proxies`、`PATCH /__aisys__/api/proxies/{id}` 和 `DELETE /__aisys__/api/proxies/{id}` 进入 Go opt-in。Go 侧新增 writer port、`000022_w5_management_proxy_crud.sql`、sqlc CRUD / 固定绑定窗口查询、PostgreSQL adapter、service 校验与 Node 兼容 AES-256-GCM 密文、HTTP strict JSON admin-only handler、敏感 operation log、router/app 注入和 gateway runtime cache invalidation。更新 type/host/port/username/password 时重置检测缓存；删除绑定代理返回 `409`，不做精确 COUNT。管理 API 启用时新增不少于 32 字符的 `JUHE_AI_SECRET` fail-fast。已通过全量 Go 测试、vet、tidy diff 和 Node 代理列表 / 单读对照回归；`TestW5ManagementProxyCRUDPostgresSmoke` 已补，但本机 Docker 不运行时实际执行为 `SKIP`，不计真实 PostgreSQL 通过。代理检测、真实外网诊断、前端真实 Go 后端 smoke、生产切流和 Node `/proxies` 删除仍待完成。 |
| 2026-07-10 | 进行中 | AI | W3 session-only 窄开关已补：新增 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED`，在 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 时只挂载 `GET /__aisys__/api/auth/sessions` 和 `DELETE /__aisys__/api/auth/sessions/{id}`，用于生产问题修复类会话管理灰度，不打开 W2 / W4 或其他 W3 管理接口。配置校验要求 Redis state，不要求 Redis queue；router 会在窄开关下忽略其他管理 handler，撤销路径仍要求 touch middleware；app 层只构造 auth middleware 与 session list / revoke handler。已通过 `gofmt`、`go test ./internal/config ./internal/httpapi ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`、`git diff --check` 和本次改动 Markdown 相对链接检查；真实 PostgreSQL session smoke、生产切流和 Node `/auth` 删除仍待后续执行。 |
| 2026-07-09 | 进行中 | AI | W4 授权回收已补：迁移 `DELETE /__aisys__/api/authorizations/{id}` 和 `DELETE /__aisys__/api/my-authorizations/{id}` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementResourceAuthorizationRevoker` port、`managementauthorizations.Service.Revoke`、HTTP admin / self 回收 handler、router/app 注入和 PostgreSQL 事务写链路；路径 `{id}` 固定为 `resource_authorization_grants.id`，管理侧 admin-only 且可用 `systemAccountId` 收窄资源归属作用域，个人侧 self-only 且忽略 query `systemAccountId`；回收后标记 grant `revoked`，撤销 direct manual source 或当前资源 team source，刷新 runtime authorization 为 `revoked`，写 `resource_authorization_revoked` stats dirty 并发布授权缓存失效，operation log 写 `authorizations.revoke`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权列表已补：迁移 `GET /__aisys__/api/authorizations` 和 `GET /__aisys__/api/my-authorizations` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementResourceAuthorizationLister` port、`managementauthorizations.Service.List`、HTTP admin / self 列表 handler、router/app 注入和 PostgreSQL grant 主表分页读取；支持 Node 当前查询参数、admin / self 作用域、个人侧 `direction`、管理侧 direction 校验但不应用、`sourceType`、状态、team / grantee / owner 过滤、关键词 `COLLATE "C"` + `starts_with` 前缀搜索、窗口上界 total、轻量 DTO、`sourceSummary` 和非资源归属人脱敏。列表只读当前 grant 状态，不触发 Node `expireDueResourceAuthorizations` 到期扫描，不返回 usage / limits / authorizationSources 详情。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权详情已补：迁移 `GET /__aisys__/api/authorizations/{id}` 和 `GET /__aisys__/api/my-authorizations/{id}` 的 Go opt-in 灰度实现。Go 侧新增 `ManagementResourceAuthorizationGetter` port、`managementauthorizations.Service.Get`、HTTP admin / self 详情 handler、router/app 注入和 PostgreSQL grant 主表按 id 读取；路径 ID 固定为 `resource_authorization_grants.id`，应用 admin / self 可见性和非资源归属人 source 明细脱敏，读接口不 touch session、不触发到期扫描、不做 usage 聚合。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-09 | 进行中 | AI | W4 前端团队 / 统一授权页本地 smoke 已补：新增 `pnpm --filter juhe-ai-frontend test:w4-team-authorization-smoke`，通过 mock 前端 HTTP 层固定 `systemTeams` / `myTeams` / `authorizations` / `myAuthorizations` API 路径、团队创建 / 更新 / 成员维护、授权列表 / 详情 / 创建 / 状态更新 / 有效期更新 / 归还 / 回收调用、管理侧 `systemAccountId` 作用域和个人侧 `my-*` 去除管理作用域边界；同步检查授权团队和统一授权菜单路由、页面写入口和敏感字段不暴露。该 smoke 不连接真实 Go 后端，不代表浏览器 Cookie / HTTPS / 反向代理 / 生产切流完成。 |
| 2026-07-09 | 进行中 | AI | W4 授权 team/user usage overview 已补：迁移 `GET /__aisys__/api/authorizations/usage/team-details`、`GET /__aisys__/api/my-authorizations/usage/team-details`、`GET /__aisys__/api/authorizations/usage/user-details` 和 `GET /__aisys__/api/my-authorizations/usage/user-details` 的 Go opt-in 灰度实现。Go 侧新增授权用量 range window schema、`ManagementAuthorizationUsageOverviewReader` port、`managementauthorizations.Service` usage overview 查询、HTTP admin / self handler、router/app 注入和 PostgreSQL 窗口表分页读取；查询只读 `juhe_stats.authorization_team_usage_range_windows` / `authorization_user_usage_range_windows`，先分页窗口表再 join 当前页摘要，不读取 `usage_records`，不做请求路径实时聚合。已通过 targeted Go 测试；真实 migration apply、usage range window 真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管、生产 gateway quota snapshot 消费、生产单 owner 切流和 Node `/authorizations` 删除仍待完成。 |
| 2026-07-10 | 进行中 | AI | W4 网关配额快照构建 worker 已补：新增 `GatewayQuotaSnapshotReader` port、`gatewayquotasnapshot.Service.Build`、`postgres.Store` 网关配额快照读取实现、`app.RunGatewayQuotaSnapshotBuildWorker` 和 `juhe-ai-worker gateway-quota-snapshot-build`。Go 侧按 Node 当前 API Key / direct authorization / team authorization scope 构建 API Key 成本快照、授权成本快照和 quota decision；只读取 `api_keys`、`resource_authorizations`、`resource_authorization_grants`、`accounts`、`system_settings`、`usage_stats_totals`、`usage_stats_daily`、`usage_stats_weekly`、`usage_stats_monthly` 和 `usage_quota_hourly_windows`，不读取 `usage_records`，不实时 `SUM/GROUP BY`，不写 Redis local snapshot，也不接入 Go 或 Node 生产 gateway runtime cache。已通过 targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译、Markdown 相对链接检查和 `git diff --check`；真实 PG smoke、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费、生产切流和 Node gateway 删除仍待完成。 |
| 2026-07-10 | 进行中 | AI | 自定义模型 CRUD 补充写接口已补：`POST /__aisys__/api/providers/{code}/models`、`PATCH /__aisys__/api/providers/{code}/models/{id}` 和 `DELETE /__aisys__/api/providers/{code}/models/{id}` 进入 Go opt-in 灰度实现。Go 侧新增 `ManagementCustomProviderModelWriter` port、PostgreSQL CRUD / 绑定统计 / 默认测试模型清理查询、`000021_w3_custom_provider_model_crud.sql`、service 权限 / 价格 / int4 上界 / 错误优先级校验、HTTP strict JSON handler、router/app 注入和 `TestW3ManagementProviderModelCRUDPostgresSmoke`。创建支持普通用户个人模型、管理员目标用户个人模型和全局模型；更新禁止修改模型 ID，停用会清理默认测试模型偏好；删除会检查账户支持模型和模型映射上下游绑定，绑定中返回 `409`；保存 / 删除只发布 gateway runtime cache 失效，不清 API Key validation cache。已通过 `go test ./internal/modules/managementprovidermodels ./internal/httpapi -count=1`；真实 smoke 当前本机 Docker/testcontainers 不可用时 `SKIP`，生产切流、前端真实 Go 后端 smoke、供应商定义写接口、供应商协议档案写接口和 Node 删除仍待完成。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-07-06 | 后端目标运行时转 Go | 当前 Node 后端围绕事件循环和阻塞规避积累了过多复杂度 | 后续后端模块按 Go 架构迁移，Node 后端逐步删除 |
| 2026-07-06 | 采用渐进减法迁移 | 避免一次性重写风险，同时避免迁移后旧实现残留 | 每个模块 Go 接管后必须删除 Node 旧实现 |
| 2026-07-06 | 公开接口和后台接口优先，真实网关最后 | 公开/后台接口风险相对低，网关是最核心链路 | 网关迁移必须等待测试、存储和 worker 基线成熟 |
| 2026-07-06 | Go 架构优先使用标准库和轻量依赖 | 避免从 Node 复杂度迁移到框架复杂度 | 默认 `net/http`、`chi`、`slog`、`pgx`、`go-redis`；不引入 SQLite driver |
| 2026-07-06 | Go 后端只保留 PostgreSQL + Redis 单模式 | 继续保留 SQLite 会让 schema、repository、测试和部署长期双倍维护 | 不再引入 Go SQLite driver；删除 standalone / performance 模式分支；旧 SQLite 数据只走离线导入 PostgreSQL |
| 2026-07-06 | Go 通用能力优先采用成熟开源库并封装在基础设施层 | 不能为了方便手写长期维护成本高的框架、队列、SQL 映射、迁移和观测能力，也不能让第三方库类型污染业务层 | 新增依赖必须先更新 `Go技术选型与依赖基线.md`；业务模块不得直接 import chi、pgx、redis、asynq、validator、prometheus 等基础设施库 |

## 本阶段验收标准

- [x] `docs/migration/` 有 README 和示例文档。
- [x] 迁移原则、阶段、减法规则和网关最后策略已经写入文档。
- [x] Go 技术基线、目录结构、并发、线程安全、内存和大数据边界已经写入文档。
- [x] Go 框架、日志、配置、DB、SQL、job、测试、观测和安全扫描依赖基线已经写入文档。
- [x] PostgreSQL + Redis 单模式和 SQLite 移除范围已经写入文档。
- [x] 测试与验收策略覆盖公开接口、后台接口、worker、存储、网关、性能和删除验证。
- [x] 开发、构建、部署、Docker、服务化、配置和回滚调整已有规划。

## 后续迁移门禁

- [ ] 每个代码迁移模块都有独立验证记录和删除证据。
- [ ] Go 工程和 PostgreSQL / Redis 基线完成后，再允许模块进入 `已接管`。
- [ ] W0 必须完成依赖 PoC，包括 chi、config、slog、pgx、sqlc、goose、go-redis、Asynq、Prometheus、testcontainers、lint 和 govulncheck。
- [ ] W9 不删除 Node 网关 preflight；W10 整条网关灰度接管后统一删除 Node gateway。
- [ ] W11 发布收尾完成后，开发、部署、Docker、watchdog 和 env 文档不再把 SQLite / standalone 当正式路径。

## 当前 W0 验收状态

- [x] 本机 Go 版本固定到 `go1.26.5 windows/amd64`，`GOROOT=E:\gosdk\go1.26.5`。
- [x] Windows race 所需 C 工具链固定到 `E:\tools\w64devkit-2.8.0\w64devkit\bin`，并在系统 PATH 中排在旧 MinGW 前面。
- [x] `backend-go/` 已包含 Go module、server / worker / maintenance 命令入口、配置解析、slog 日志、chi 路由、健康检查、Prometheus metrics、PG/Redis/Asynq health adapter、goose baseline migration、sqlc 查询和生成包。
- [x] W0 基础设施封装已补：PG store 事务和 catalog 查询不向业务暴露 pgx/sqlc 类型；Redis client 支持 namespace key、TTL set、pipeline、`IncrWithTTL` 原子计数并校验 cache/state/queue 不指向同一 Redis DB；Asynq queue 支持 Redis URL / `rediss` TLS / 显式 timeout / enqueue / inspector / pending smoke。
- [x] W0 基础验证已通过：`go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./...`、`golangci-lint run`、`govulncheck ./...`、命令入口和 health smoke。
- [x] `testcontainers` integration 测试代码已补：PostgreSQL + goose + sqlc baseline schema 查询、W1a public settings smoke、Redis health、fixed-window、penalty-window、Asynq enqueue / consume / retry / SkipRetry archive / retry exhaustion archive、W1b public API foundation smoke 和 W1b public API log Asynq smoke 均已有用例。
- [x] `juhe-ai-maintenance w0-smoke` 已补，缺少真实 PG/Redis/Asynq URL 时会 fail-fast，避免把依赖 `skipped` 当作 smoke 成功；配置真实 queue URL 时会执行 Asynq enqueue + inspector pending smoke。
- [ ] 基于 testcontainers 的 PostgreSQL / Redis / Asynq smoke 当前主线程因 Docker 不可用输出 `SKIP`，需在 Docker 健康环境复跑后再标记真实通过；独立真实外部 URL 的 `w0-smoke` 仍可作为部署环境验证命令保留。
- [ ] Asynq periodic task、worker crash recover、长任务 shutdown drain、队列 metrics 和 Redis 持久化边界仍待后续 W0/W7 专项补齐。

## 当前 W1a 验收状态

- [x] Node / 前端契约已梳理：`GET /__aisys__/api/settings/public` 匿名可读，前端期待 `{ data: { appName, appIcon } }`，失败时登录页回退默认品牌。
- [x] Go schema 已补 `juhe_business.global_settings` 和默认 seed：`appName = 聚合 AI`、`appIcon = /__aisys__/brand-icon.svg`。
- [x] Go route / service / store 已补：`backend-go/internal/modules/publicsettings`、`backend-go/internal/httpapi/public_settings.go`、`backend-go/internal/store/postgres`。
- [x] Go 单元测试已补：公开设置 HTTP envelope / no-store / 错误脱敏，公开设置 JSON 字符串解析。
- [x] Go W1a 已补系统 API IP 读限流基本语义：读取 `system_settings` 的读限流字段，按 IP 做 minute / burst 固定窗口，超限返回 `429`、`Retry-After` 和 `请求过于频繁，请稍后重试`。
- [x] Go W1a 已补 Redis state 原子限流代码：`go-redis` Lua 一次检查 minute / burst 两个 bucket，两个 bucket 都通过才提交，超限不递增；真实 server 启动路径必须配置 `JUHE_AI_REDIS_STATE_URL`，不会静默退回进程内限流。
- [x] Go W1a 已补客户端 IP 识别：`JUHE_AI_TRUST_PROXY=false` 时使用 socket `RemoteAddr`，启用后按可信反代链路解析 `X-Forwarded-For`；Redis state IP read rate limit 和 diagnostics loopback 判断使用解析后的客户端 IP。
- [x] Go W1a 已补独立 smoke 命令：`go run ./cmd/juhe-ai-maintenance w1a-public-settings-smoke`，用于真实 PostgreSQL public settings / rate limit settings、HTTP route 精确响应和 Redis state 跨 client 限流验证；缺少必要 URL 时 fail-fast。
- [ ] `TestW1aPublicSettingsSmoke` 当前主线程因 Docker 不可用输出 `SKIP`，需在 Docker 健康环境复跑后再标记真实通过；独立真实外部 URL 的 `w1a-public-settings-smoke` 仍待部署环境执行。
- [ ] 生产接管、Node 删除证据、反向代理路径切换和前端 smoke 均未执行。

## 验证记录

- 类型检查：不适用，W0 Go 工程使用 Go 验证矩阵替代 Node 类型检查。
- 构建：已执行 `go build ./...`，通过。
- 文档检查：已执行迁移入口检索、相对链接检查和 Go 目标存储方向检查；新增文档命名、入口索引和相对链接检查通过。
- 链接验证：已执行覆盖本次新增和同步修改文档的 Markdown 相对链接检查，结果为 `All checked markdown links resolve.`。
- 方向验证：已检查 `docs/migration/`、PLAN-0081、后端架构入口、存储功能文档和历史计划；旧 SQLite / 双模式内容均已标记为当前 Node 过渡事实或历史记录，Go 目标统一为 PostgreSQL + Redis 单模式。
- 依赖基线验证：已补充 Go 默认依赖、禁用依赖和 W0 PoC 门禁；迁移文档中 Redis Streams 只保留为“不要手写通用队列 / 专项 adapter 例外”的说明，Go 默认队列改为 Asynq。
- 多 agent 审阅：已按 10 个 agent 的审阅意见补齐关键风险，包括 PG/Redis 基线必须先于模块迁移、W9 不能提前删除 Node 网关准备层、可靠队列重试 / 死信 / 幂等边界、PgBouncer / pool / timeout 边界、W0-W11 验收矩阵和部署发布收尾清单。
- Go W0/W1a 验证：已执行 `sqlc generate`、`go mod tidy -diff`、`go test ./...`、`go test -race ./...`、`go vet ./...`、`golangci-lint run`、`govulncheck ./...`、`go build ./...`；均通过，`golangci-lint` 为 `0 issues`，`govulncheck` 无已调用漏洞，`go mod tidy -diff` 无输出。W1a 覆盖公开设置 HTTP envelope / no-store / 错误脱敏、store JSON / 整数设置解析、IP read rate limit `429` / `Retry-After`、Redis limiter 注入、Redis fixed-window 参数校验、`JUHE_AI_TRUST_PROXY` 配置解析、客户端 IP 规范化、限流使用解析后 IP、diagnostics loopback 判断和 `w1a-public-settings-smoke` 缺配置 fail-fast。
- Go W0 smoke：已执行三个命令入口 `version`，均输出 `0.1.0-w0`；本地临时端口已验证 `/__aisys__/health`、`/__aisys__/api/health` 和 `/__aisys__/metrics`。
- Go W0/W1a/W1b integration：已执行 `go test -tags=integration ./internal/testkit/integration -count=1`，当前主线程输出 `SKIP`，原因是 Docker / testcontainers 不可用；不计为真实 PG/Redis 通过。本轮已执行 `go test -tags=integration ./internal/testkit/integration -run '^$'` 证明 integration 包可编译；已执行 `go test -v -tags=integration ./internal/testkit/integration -run TestW1bPublicAccountsShellE2E -count=1`，因 Docker 不可用输出 `SKIP`，不计为真实通过。待 Docker 健康环境复跑时，必须覆盖 `TestW0PostgresMigrationSmoke`、`TestW0RedisAndAsynqSmoke`、`TestW1aPublicSettingsSmoke`、`TestW1bPublicAPIFoundationPostgresSmoke`、`TestW1bPublicAPILogAsynqSmoke`、public group / route strategy / API Key / account PostgreSQL smoke、public group / route strategy / API Key / account shell E2E 和 `TestImagesArePinnedForW0`。
- Go W0 live smoke：已执行 `go run ./cmd/juhe-ai-maintenance w0-smoke` 的缺配置路径，确认缺少 `JUHE_AI_POSTGRES_URL`、`JUHE_AI_REDIS_CACHE_URL`、`JUHE_AI_REDIS_STATE_URL`、`JUHE_AI_REDIS_QUEUE_URL` 时 fail-fast。
- Go W1a smoke：已执行 `go run ./cmd/juhe-ai-maintenance w1a-public-settings-smoke` 的缺配置路径断言，确认缺少 `JUHE_AI_POSTGRES_URL`、`JUHE_AI_REDIS_STATE_URL` 时 fail-fast 且不输出成功 JSON；真实 PG/Redis 执行未完成。
- Go W1b smoke：已新增 `go run ./cmd/juhe-ai-maintenance w1b-public-api-smoke`，并通过单元测试确认缺少 `JUHE_AI_POSTGRES_URL`、`JUHE_AI_REDIS_STATE_URL`、`JUHE_AI_REDIS_QUEUE_URL` 或少于 32 字符的 `JUHE_AI_SECRET` 时 fail-fast 且不输出成功 JSON；默认 router guard 保持关闭；输出固定 `takeoverEvidence=false` 和 `productionTakeoverNotEvaluated=true`。真实 PostgreSQL / Redis / `juhe-ai-worker ingest` 执行未完成。
- 跨平台构建：已执行 `GOOS/GOARCH=windows/amd64`、`linux/amd64`、`darwin/amd64` + `CGO_ENABLED=0` 的 `go build ./...`，均通过。
- Node W1a 对照：已执行 `pnpm --filter juhe-ai-backend test:settings-public-driver`，通过，确认公开设置只暴露 `appName/appIcon`；已执行 `pnpm --filter juhe-ai-backend test:request-context-client-ip`，通过，确认 Node client IP 由 Express trust proxy 后的 `req.ip` 决定，不直接信任转发头；已执行 `pnpm --filter juhe-ai-backend test:system-api-rate-limit`，通过，确认 `/settings/public` 读限流、`429`、`Retry-After`、health bypass 和 IP 白名单语义。`pnpm --filter juhe-ai-backend test:db-service-system-api-http` 当前未通过，复现为 `settings/public` 返回 500；调试日志显示 system API 限流读取系统设置时 SQLite read worker 30s 超时，属于当前 Node 对照链路未稳定通过，不是 Go W1a 代码失败。
- W1b 契约固定：已用多个 gpt-5.5 xhigh 子 agent 做只读交叉梳理，新增 `docs/migration/W1b-外部维护公开接口迁移记录.md`；固定当前 16 个 `/__aipublic__` 公开 CRUD、旧 5 个公开路径继续 404、Bearer-only 鉴权、source/token scope 交集、source 级限频、60 秒 `last_used_at` 节流、公开接口日志 32KB 快照边界、资源字段和 Node 对照命令。已执行并通过 `pnpm test:external-source-auth`、`pnpm test:public-api-logs`、`pnpm --filter juhe-ai-backend test:external-integration-source-expires-at`、`pnpm --filter juhe-ai-backend test:external-integration-source-async-boundary`、`pnpm --filter juhe-ai-backend test:external-public-account-push-async-boundary`、`pnpm --filter juhe-ai-backend test:public-api-log-db-service-ipc` 和 `pnpm test:sqlite-query-regression`；W1b PG/Redis Node 对照 smoke、生产路由切流和 Node 删除仍待执行。
- Go W1b 基础设施与四类公开资源纵切面：已新增 `backend-go/internal/modules/publicapi`、`backend-go/internal/modules/publicapi/auth`、`backend-go/internal/modules/publicapi/ratelimit`、`backend-go/internal/modules/publicapilog`、`backend-go/internal/modules/publicgroups`、`backend-go/internal/modules/publicroutestrategies`、`backend-go/internal/modules/publicapikeys`、`backend-go/internal/modules/publicaccounts`、`backend-go/internal/jobs/publicapilog`、`backend-go/internal/jobs/worker`、`backend-go/internal/app/server.go`、`backend-go/internal/app/ingest_worker.go`、`backend-go/internal/config/config.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/httpapi/public_api_shell.go`、`backend-go/internal/httpapi/public_groups.go`、`backend-go/internal/httpapi/public_route_strategies.go`、`backend-go/internal/httpapi/public_api_keys.go`、`backend-go/internal/httpapi/public_accounts.go`、`backend-go/internal/store/port/publicapi.go`、`backend-go/internal/store/postgres/publicapi.go`、`backend-go/internal/store/postgres/publicgroups.go`、`backend-go/internal/store/postgres/publicroutestrategies.go`、`backend-go/internal/store/postgres/publicapikeys.go`、`backend-go/internal/store/postgres/publicaccounts.go`、`backend-go/internal/store/postgres/queries/w1b_public_groups.sql`、`backend-go/internal/store/postgres/queries/w1b_public_route_strategies.sql`、`backend-go/internal/store/postgres/queries/w1b_public_api_keys.sql`、`backend-go/internal/store/postgres/queries/w1b_public_accounts.sql`、`backend-go/internal/platform/redis` penalty-window helper、`backend-go/internal/maintenance/w1bpublicapismoke.go`、`backend-go/db/migrations/000003_w1b_public_api_foundation.sql`、`backend-go/db/migrations/000004_w1b_public_groups.sql`、`backend-go/db/migrations/000005_w1b_public_accounts.sql` 和 W1b integration smoke；覆盖 16 个 `/__aipublic__` method/path/scope、旧 5 个公开路径不进入 catalog、`Bearer` 解析、token hash namespace、source/token 状态与过期、scope 交集、auth error code/message/status、`last_used_at` 60 秒 touch 判断、source/token rate-limit key、公开接口日志 32KB snapshot / dropped / 499 / error info、Asynq payload/enqueue/handler、`juhe-ai-worker ingest` runtime、invalid payload `SkipRetry` 映射、Asynq import guard、PostgreSQL auth/log adapter、Redis penalty-window 限频、HTTP shell 中 16 个 catalog endpoint 进入 auth / limiter / injected handler、默认关闭 / 显式开启 router guard、`w1b-public-api-smoke` 本地 httptest + 真实依赖验证入口、旧公开路径 404 不鉴权但写日志、JSON body 400、body 413、429 `Retry-After`、response capture、客户端提前断开 499、ResponseWriter 可选接口透传、PublicGroup / PublicRouteStrategy / PublicAPIKey / PublicAccount store port 不暴露 pgx/sqlc/Redis 类型、四类资源 handler/service/store/query/schema 纵切面、AI 账户 provider profile、凭据 AES-GCM 加密、凭据指纹 hash、模型替换、pending_test 直启拦截、软删除和绑定清理，以及完整 key / query string / 来源 token / 上游 apiKey / URL userinfo 日志脱敏。account shell E2E 测试代码已补，覆盖 add/list/update/delete/limited、source/token last_used、账号凭据加密落库、软删除和 public API log 落库脱敏。已执行并通过 `sqlc generate`、`go mod tidy -diff`、`go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./cmd/...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go test ./internal/modules/publicaccounts ./internal/httpapi ./internal/store/postgres ./internal/modules/publicapilog`、`go test ./internal/config ./internal/httpapi ./internal/app` 和本轮 `go test ./internal/maintenance ./internal/app`；真实 Docker/testcontainers integration、`w1b-public-api-smoke` 真实依赖执行、account shell E2E 真实复跑、生产路由切流和 Node 删除仍待完成。
- Go W2 管理辅助当前已迁移路径：已新增 `backend-go/db/migrations/000006_w2_management_proxy_options.sql`、`000007_w2_management_session_auth.sql`、`000008_w2_management_provider_options.sql`、`000009_w2_management_provider_model_catalog.sql`、`000010_w2_management_account_tags.sql`、`000011_w2_management_group_authorizations.sql`、`000012_w2_management_account_name_search.sql`、`000013_w2_management_account_authorization_instances.sql`、`000014_w2_management_system_teams.sql`，以及 management auth/proxy/system account/authorization options/provider/model/route strategy/group/account 的 store port、PostgreSQL adapter、sqlc query、service、handler、router/app 注入和 integration smoke。当前覆盖 `proxies/options`、`system-accounts`、`system-accounts/options`、`authorization-options/grantee-accounts`、`my-authorization-options/grantee-accounts`、`authorization-options/grantee-teams`、`my-authorization-options/grantee-teams`、`authorization-options/grantee-groups`、`my-authorization-options/grantee-groups`、`providers/options`、`providers/models/options`、`providers/{code}/models`、`route-strategies/options`、`my-route-strategies/options`、`groups/options`、`my-groups/options`、`groups/account-options`、`my-groups/account-options`、`accounts/options`、`my-accounts/options`、`accounts/tags`、`my-accounts/tags`、`DELETE accounts/tags/{tagId}`、`DELETE my-accounts/tags/{tagId}`、`PATCH accounts/{id}/tags` 和 `PATCH my-accounts/{id}/tags`；其中系统账户列表读已覆盖 admin-only 分页管理字段白名单，系统账户 options 已覆盖 admin-only 轻量字段白名单，authorization grantee accounts / grantee teams / grantee groups 已覆盖 admin / my 双路径轻量字段白名单，分组 options / account-options 已覆盖 owner + 授权组只读 union、`manageableOnly=true` 排除授权组、authorized permissions、授权组 `accountIds=[]` 和管理员全局不重复授权行，`accounts/options` 已覆盖 owner + 授权账户只读 union、`accessType=authorized`、授权字段、authorized permissions、owner `tagIds` 命中任一标签过滤和名称包含搜索，`accounts/tags` / `my-accounts/tags` 已覆盖 owner 标签字典、未删除账号绑定计数、admin / self 作用域和 router opt-in guard，DELETE 标签已覆盖未绑定标签 204、绑定中 400、不存在 / 越权 404 和事务行锁绑定复查，独立 PATCH 已覆盖 service / HTTP 双路径、admin 指定作用域、my 强制 self scope、授权实例本地标签更新、trim / 去重、绑定表替换和同名标签复用。已通过 `sqlc generate`、`go mod tidy -diff`、管理端相关 Go 快速测试、`go test ./...` 和 integration 包编译；本机执行 `go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAccountOptionsPostgresSmoke -count=1` 因 Docker/testcontainers 不可用按预期 `SKIP`，需健康环境复跑后才能记为真实通过；其他 W2 Docker/testcontainers integration、授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 和其他写接口 operation log、生产路由切流、前端 smoke 和 Node 删除仍待完成。
- Go W2 operation log 读接口当前已覆盖 `operation-logs` / `my-operation-logs` 管理和个人列表 / 详情：管理侧 admin-only，个人侧强制当前 session viewer；列表保持 Node `page/pageSize/total/hasMore` 和轻量 DTO，summary 搜索走倒排表，traceId 走 `COLLATE "C"` 前缀范围，个人列表拆 targeted / all_users 窗口合并，详情按 `full/summary/all_users` 脱敏。已通过 `go test ./internal/modules/managementoperationlogs ./internal/httpapi ./internal/store/postgres ./internal/app` 和 `go test ./internal/maintenance ./cmd/juhe-ai-maintenance`；`go run ./cmd/juhe-ai-maintenance w2-operation-logs-smoke` 已作为真实 PG / Redis 依赖、本地 httptest opt-in 验证入口落地，输出固定不声明生产接管。真实 smoke 执行、Docker/testcontainers integration、前端 operation log 页面 smoke、生产切流和 Node 删除仍待完成。
- Go W2 供应商全量列表：已补 `GET /__aisys__/api/providers` 的 Go opt-in handler、service、PostgreSQL 查询和 router/app 注入；管理侧要求 admin / super_admin，返回启用和停用供应商定义，并按 `systemAccountId` 当前作用域覆盖默认测试模型偏好；`providers/options` 仍只返回启用供应商。已补 targeted Go 测试；真实 PG smoke、前端供应商页 smoke、生产切流和 Node 删除仍待完成。
- Go W2 operation log 读接口当前已覆盖 `operation-logs` / `my-operation-logs` 管理和个人列表 / 详情：管理侧 admin-only，个人侧强制当前 session viewer；列表保持 Node `page/pageSize/total/hasMore` 和轻量 DTO，summary 搜索走倒排表，traceId 走 `COLLATE "C"` 前缀范围，个人列表拆 targeted / all_users 窗口合并，详情按 `full/summary/all_users` 脱敏。已通过 `go test ./internal/modules/managementoperationlogs ./internal/httpapi ./internal/store/postgres ./internal/app` 和 `go test ./internal/maintenance ./cmd/juhe-ai-maintenance`；`go run ./cmd/juhe-ai-maintenance w2-operation-logs-smoke` 已作为真实 PG / Redis 依赖、本地 httptest opt-in 验证入口落地，输出固定不声明生产接管。真实 smoke 执行、Docker/testcontainers integration、前端 operation log 页面 smoke、生产切流和 Node 删除仍待完成。
- Go W2 operation log 保留清理 worker：已补 `juhe-ai-worker operation-log-retention-cleanup`。Go 侧读取 `system_settings.operationLogRetentionDays`，缺失时按 Node 默认 365 天处理；按 `created_at < cutoff`、`created_at ASC, id ASC` 每批最多删除 1000 条 `juhe_dataset.operation_logs`，单轮最多 20 批，满批之间暂停 25ms，并依赖 FK cascade 清理 targets / viewers / summary search terms。已补 service、PostgreSQL/sqlc adapter、worker 命令、参数校验、SQL guard 和 targeted Go 测试；真实 PostgreSQL run-once / 长跑 smoke、生产 supervisor 接管、完整 data-retention、生产切流和 Node 删除仍待完成。
- Go W2 默认测试模型偏好写入：已补 `PUT /__aisys__/api/providers/:code/default-test-model` 的 Go opt-in handler、service 校验、PostgreSQL upsert 和 router/app 注入；已通过 `sqlc generate`、`go mod tidy -diff`、`go test ./internal/modules/managementprovidermodels ./internal/httpapi ./internal/store/postgres ./internal/app`、`go test ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`pnpm --filter juhe-ai-backend test:provider-repository-driver`、`pnpm --filter juhe-ai-backend test:model-catalog` 和 `pnpm --dir frontend test:provider-model-source-operation`。本机执行 `go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementProviderModelsPostgresSmoke -count=1` 因 Windows Docker/testcontainers 健康检查不可用按预期 `SKIP`；该接口只覆盖 `provider_default_test_models` 当前系统账户偏好写入，不覆盖供应商定义写接口或生产单 owner 切流。自定义模型 CRUD 已在后续补充切片进入 Go opt-in，见上方 2026-07-10 进度记录。
- Go 自定义模型 CRUD 补充写接口：已补 `POST/PATCH/DELETE /__aisys__/api/providers/{code}/models` 的 Go opt-in handler、service、PostgreSQL CRUD、绑定删除检查、默认测试模型清理和网关 runtime cache 失效；本轮同时修复 int4 上界校验和 Node 错误优先级。已通过 `go test ./internal/modules/managementprovidermodels ./internal/httpapi -count=1`；真实 `TestW3ManagementProviderModelCRUDPostgresSmoke` 因 Windows Docker/testcontainers 不可用输出 `SKIP`，不计真实 PostgreSQL integration 通过。
- Go W3 session touch：已补 `ManagementSessionToucher` port、PostgreSQL `TouchManagementSession`、写鉴权 touch 分支、router 写接口 touch middleware fail-fast、auth/profile/password/router/store/integration 测试和文档同步。已执行并通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app`、`go test ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go mod tidy -diff`、`git diff --check`、`pnpm --filter juhe-ai-backend test:system-api-db-access`、`pnpm --filter juhe-ai-backend test:system-account-session-driver` 和 `pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary`。`go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthPostgresSmoke -count=1` 因 Docker 未运行输出 `SKIP`，不计真实 PG integration 通过。
- Go W3 captcha：已补 Redis `GetDelete`、`managementauth.CaptchaService`、`GET /__aisys__/api/auth/captcha` handler、router/app 注入和 system API IP 读限流跳过规则；验证码状态使用 Redis state，PNG data URL 不暴露答案，生成限流为 60 次 / 分钟，校验基础通过 `GETDEL` 一次性消费。已执行并通过 `gofmt`、`sqlc generate`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/platform/redis ./internal/app -count=1`、`go test ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go mod tidy -diff`、`git diff --check`、`pnpm --filter juhe-ai-backend test:captcha-rate-limit`、`pnpm --filter juhe-ai-backend test:auth-memory-maintenance-boundary` 和 `pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary`；`TestW2ManagementAuthPostgresSmoke` 因 Windows Docker 不可用输出 `SKIP`，不计真实 PG integration 通过；前端登录页 smoke 和生产切流仍待后续 login 切片一起验证。
- Go W3 login：已补 `POST /__aisys__/api/auth/login` handler、`LoginService`、Redis 登录失败锁、PG `CompleteManagementLogin` CTE、Cookie `Secure` / `SameSite` 配置和 W3 PG+Redis login smoke；验证码失败不计登录失败锁，失败锁 key 使用低泄漏 hash，session token 只以 SHA-256 hash 落库，session 创建 SQL 会用已验证 `password_hash` 二次确认避免并发改密窗口。已在 Go `go1.26.5 windows/amd64` 下执行并通过 `sqlc generate`、`gofmt`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/platform/redis ./internal/store/postgres ./internal/config ./internal/app -count=1`、`go test ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go test -race ./...`、`go vet ./...`、`go build ./...`、`golangci-lint run`、`govulncheck ./...`、`go mod tidy -diff`、`git diff --check` 和本次改动 Markdown 相对链接检查；`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementLoginPostgresRedisSmoke -count=1` 因 Windows Docker 不可用输出 `SKIP`，不计真实 PG+Redis integration 通过。前端登录页 smoke、生产切流、完整 auth / session 生产接管和 Node `/auth` 删除仍待完成；系统账户写接口当前已补 create / full mixed PATCH 的 Go opt-in 实现，但仍未生产接管。
- Go W3 system account create / full mixed PATCH：已补 `POST /__aisys__/api/system-accounts` 创建子动作和 `PATCH /__aisys__/api/system-accounts/{id}` 完整 mixed partial update。create 完成 `super_admin` 权限、严格 JSON 字段集、Node 兼容密码 hash、7 个默认分组、6 条非 hybrid 默认普通路由 / 默认 API Key fanout、默认 API Key `key_secret_encrypted` 密文落库和敏感创建日志；PATCH 完成 `super_admin` 权限、严格 JSON 字段集、允许 `displayName/description/password/role/status/mustChangePassword/imageGenerationEnabled` 任意组合、拒绝 `username` 和未知字段、Node 兼容密码 hash、PG 单语句锁定启用超级管理员集合与目标账户、禁止停用 / 降级最后一个启用 `super_admin`、提交 `password` 或 `status="disabled"` 时删除目标账户全部 session、状态或图像权限真实变化清理 gateway runtime cache / API Key validation cache、混合字段 operation log 和密码脱敏。本轮新增完整 PATCH targeted service / handler / SQL guard / row mapper 测试，并已有 `TestW3ManagementSystemAccountCreatePostgresRedisSmoke` 覆盖 create 真实链路代码、`TestW3ManagementSystemAccountsPatchPostgresRedisSmoke` 覆盖 PATCH 真实链路代码；已执行并通过 `sqlc generate`、`gofmt`、`go mod tidy -diff`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres -count=1`、`go test ./... -count=1`、`go vet ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`git diff --check` 和本次改动 Markdown 相对链接检查，integration smoke 在当前 Windows Docker / testcontainers provider 不可用时输出 `SKIP`，不计真实通过。前端本地 auth / session / system-account smoke、typecheck 和 build 已通过；该路径仍为 opt-in 灰度路径，不代表系统账户写接口生产接管，浏览器连接真实 Go 后端的前端系统账户页 smoke、生产切流和 Node `/system-accounts` 删除仍待完成。
- Go W3 current-user session list / revoke：已补 `GET /__aisys__/api/auth/sessions` 和 `DELETE /__aisys__/api/auth/sessions/{id}` 的 Go opt-in 灰度能力。列表只读当前系统账户未过期 session，返回 `id/current/createdAt/lastSeenAt/expiresAt`，不读取 / 返回 `token_hash`，不 touch session，分页 `total` 只做窗口上界；撤销只允许按当前系统账户 + session ID 删除，撤销当前 session 时清 Cookie，写鉴权按 60 秒窗口 touch 当前 session，暂不写 operation log。前端已补头像菜单“会话管理”、会话分页刷新、撤销其他会话、撤销当前会话后清本地登录态并返回登录页的本地 UI / 调用边界。已执行并通过 `sqlc generate`、`gofmt`、`go mod tidy -diff`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke`、`pnpm --filter juhe-ai-frontend typecheck` 和 `pnpm --filter juhe-ai-frontend build`；`go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthPostgresSmoke -count=1` 因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PostgreSQL integration 通过。
- Go W4 system team read/create/update/member：已补 `GET /__aisys__/api/system-teams`、`GET /__aisys__/api/system-teams/{id}`、`GET /__aisys__/api/my-teams`、`GET /__aisys__/api/my-teams/{id}`、`POST /__aisys__/api/system-teams`、`PATCH /__aisys__/api/system-teams/{id}`、`POST /__aisys__/api/system-teams/{id}/members` 和 `DELETE /__aisys__/api/system-teams/{id}/members/{memberId}` 的 Go opt-in 灰度能力。Go 侧新增 `ManagementSystemTeamUpdater` / `ManagementSystemTeamMemberManager` port、团队 PATCH / 成员维护 service / handler / router / app 注入、PostgreSQL `FOR UPDATE` 查询和状态 / 成员副作用事务；读接口覆盖 admin / self 作用域、分页、团队名前缀搜索和详情 active 成员摘要，不 touch session，不读取授权来源 / grant，不按说明字段搜索；创建接口允许 `super_admin/admin`，普通用户 403，严格 JSON 字段集，团队名 trim 后全局唯一，只写 `system_teams` 摘要字段，operation log 使用 `system_teams.create`；更新接口支持 name / description / status partial update，禁用团队时 revoke active team source 并刷新 effective source，启用团队时按 active team grants 展开到 active members，授权变化写全量 stats dirty 标记并发布 `team_authorization_changed` 授权缓存失效，operation log 使用 `system_teams.update`；成员新增 / 移除要求 admin-only、strict JSON 和写鉴权 touch，新增或重新加入时按既有 active team grants 展开最终用户授权，移除成员时 revoke 该成员 active team source 并刷新 effective source，成员变化写 `team_members_changed` stats dirty / 授权缓存失效和 `system_teams.add_members` / `system_teams.remove_member` operation log。已执行并通过 `sqlc generate`、`gofmt`、`go test ./internal/app ./internal/modules/managementsystemteams ./internal/httpapi ./internal/store/postgres -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`、本次改动 Markdown 相对链接检查和 `git diff --check`；真实 PG/Redis/Asynq operation-log integration、前端团队页 smoke、生产切流和 Node `/system-teams` / `/authorizations` 删除仍待完成。
- Go W4 authorization state schema：已补 `backend-go/db/migrations/000018_w4_resource_authorization_sources_and_grants.sql` 和 `backend-go/db/migrations/000019_w4_authorization_quota_and_stats_state.sql`，把 Node 当前统一授权权限链所需的 `resource_authorization_sources` / `resource_authorization_grants` 表、manual / team 来源约束、system_account / team grant 约束、limits JSON object 约束、active source / grant 部分唯一索引、owner / resource / grantee 分页索引、grant 到期扫描索引、team quota snapshot 索引、`resource_authorizations` quota snapshot 索引、默认额度小时窗口和 `group_account_stats_dirty` 全量脏标记表纳入 Go PostgreSQL migration；`sqlc generate` 已生成对应 models，并新增 schema guard。已执行并通过 `sqlc generate`、`gofmt`、`go test ./internal/store/postgres -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff` 和 `go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`。该块支撑团队 PATCH 状态、成员维护、授权列表 / 详情、授权创建、普通授权更新、授权归还、授权回收和批量到期扫描，不代表批量到期扫描生产周期 runner / 部署调度或生产 gateway quota snapshot 消费已迁移。
- Go W4 authorization cache invalidation：已补 `gatewaycache.SystemAccountInvalidator` 的授权业务缓存失效能力。`InvalidateAuthorizationChanged` 会发布 Node 兼容 `gateway_runtime_cache` 与 `authorization_quota_cache` 两个 Redis runtime state topic；`InvalidateAPIKeyQuotaChanged` 会发布 `api_key_quota_cache` 并保留 trim 后 `apiKeyId`；现有系统账户状态 / 图像权限失效仍清理 `gateway:api-key-validation` cache version 后发布 gateway runtime topic；团队 PATCH 状态副作用已接入 `team_authorization_changed`，成员维护副作用已接入 `team_members_changed`，授权创建已接入 `resource_authorization_created`，授权更新已接入 `resource_authorization_updated`，授权归还已接入 `resource_authorization_returned`，授权回收已接入 `resource_authorization_revoked`，批量到期扫描已接入 `authorization_expired`。已执行并通过 `gofmt`、`go test ./internal/modules/gatewaycache -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`、本次改动 Markdown 相对链接检查和 `git diff --check`。该块仍不代表批量到期扫描生产周期 runner / 部署调度已迁移。
- Go W4 authorization expiry sweep：已补批量授权到期扫描的 Go 后台 service/store 能力和 worker 入口。Go 侧新增 `ManagementResourceAuthorizationExpirySweeper` port、`managementauthorizations.Service.ExpireDue`、`postgres.Store.ExpireDueManagementResourceAuthorizations`、`app.RunAuthorizationExpirySweepWorker` 和 `juhe-ai-worker authorization-expiry-sweep`；默认批量 20，worker 默认 1 分钟间隔、54 秒初始延迟，要求 PostgreSQL、Redis state 和 Redis cache，支持 `--run-once` 本地 smoke；按 `resource_authorization_grants.expires_at ASC, updated_at ASC, id ASC` fixed window 读取 active / paused 到期 grant，使用 `FOR UPDATE SKIP LOCKED`，事务内标记 grant `expired`，复用个人 / 团队 runtime 同步 helper，写 `authorization_expired` stats dirty 并发布授权缓存失效；查询 guard 明确不读取 `usage_records`、不实时 `SUM/GROUP BY`。已通过 targeted Go 测试；真实 PG/Redis 环境复跑、批量到期扫描生产部署接管、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization usage range window refresh：已补授权用量 range window 刷新的 Go 后台 service/store 能力和 worker 入口。Go 侧新增 `ManagementUsageStatsTimezoneReader` / `ManagementAuthorizationUsageRangeWindowRefresher` port、`managementauthorizations.Service.RefreshUsageRangeWindows`、`postgres.Store.RefreshManagementAuthorizationUsageRangeWindows`、`app.RunAuthorizationUsageRangeWindowRefreshWorker` 和 `juhe-ai-worker authorization-usage-range-windows-refresh`；默认每 6 小时刷新，43 分钟初始延迟，只要求 PostgreSQL，支持 `--run-once` 和 `--timezone` 本地 smoke；按 `usageStatsTimezone` 计算当天、昨日、近 7 天、近 31 天和当月 hot ranges，每个 range 单独事务，从 `authorization_team_usage_summary_daily` / `authorization_user_usage_summary_daily` 汇总写入 range window；查询 guard 明确不读取 `juhe_usage.usage_records`，不在请求路径聚合。已通过 targeted Go 测试；真实 PG 环境复跑、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费、生产切流和 Node stats-worker / `/authorizations` 删除仍待完成。
- Go W4 gateway quota snapshot build：已补网关配额快照构建的 Go 后台 service/store 能力和 worker 入口。Go 侧新增 `GatewayQuotaSnapshotReader` port、`gatewayquotasnapshot.Service.Build`、`postgres.Store.ListGatewayQuotaSnapshotAPIKeys` / `ListGatewayQuotaSnapshotAuthorizations` / `ListGatewayQuotaSnapshotTeamAuthorizations` / `LoadGatewayQuotaSnapshotCosts`、`app.RunGatewayQuotaSnapshotBuildWorker` 和 `juhe-ai-worker gateway-quota-snapshot-build`；默认每 1 分钟构建一次，37 秒初始延迟，只要求 PostgreSQL，支持 `--run-once` 和 `--timezone` 本地 smoke；按 Node 当前 API Key / direct authorization / team authorization scope 构建成本快照和 quota decision，只读取管理表、系统设置、`usage_stats_*` 预聚合表与 `usage_quota_hourly_windows`，不读取 `juhe_usage.usage_records`，不在请求路径聚合，不写 Redis local snapshot。已通过 targeted Go 测试；真实 PG 环境复跑、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费、生产切流和 Node gateway 删除仍待完成。
- Go W4 gateway quota snapshot runtime state 发布 / 消费：已补 `gatewayquotasnapshot.RuntimeStatePublisher`、`juhe-ai-worker gateway-quota-snapshot-build --publish-runtime-state` / `--snapshot-ttl`，把构建结果写入 Node 兼容 `gateway_quota_snapshot/current` Redis runtime state；Node gateway Redis 模式已改为 API Key / 授权额度 shared cache miss 后优先读取该共享快照，缺失、截断或授权快照失效时继续回 DB service 精确补判并保留 fail-closed 保护。已通过 Go publisher / worker 测试、Node typecheck、`test:gateway-quota-passive-snapshot` 和 `test:performance-redis-boundary`；真实 PG/Redis 环境 smoke、生产部署 / supervisor 接管、生产切流和 Node gateway 删除仍待完成。
- Go W4 authorization create：已补 `POST /__aisys__/api/authorizations` 和 `POST /__aisys__/api/my-authorizations` 的 Go opt-in 灰度能力。Go 侧新增 `managementauthorizations.Service`、`ManagementResourceAuthorizationCreator` port、HTTP admin / self 创建 handler、router/app 注入和 PostgreSQL 事务写链路；支持账户 / 分组资源授权给系统账户或团队，账户个人授权强制目标分组，团队授权按 active 成员 fanout，写 grant、manual / team source、授权账户实例、目标分组绑定、quota hourly window、`resource_authorization_created` stats dirty 和授权缓存失效，operation log 使用 `authorizations.create`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization update：已补 `PATCH /__aisys__/api/authorizations/{id}` 和 `PATCH /__aisys__/api/my-authorizations/{id}` 的 Go opt-in 灰度能力。Go 侧新增 `ManagementResourceAuthorizationUpdater` port、`managementauthorizations.Service.Update`、HTTP admin / self 更新 handler、router/app 注入和 PostgreSQL 事务写链路；路径 `{id}` 固定为 `resource_authorization_grants.id`，支持普通 PATCH `status=active/paused`、`expiresAt` 和 `limits` 更新，刷新 manual / team runtime source，写 `resource_authorization_updated` stats dirty 和授权缓存失效，operation log 使用 `authorizations.update`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization expire update：已补 `PATCH /__aisys__/api/authorizations/{id}/expire` 和 `PATCH /__aisys__/api/my-authorizations/{id}/expire` 的 Go opt-in 灰度能力。HTTP 层固定 `expiresAt` 必填且可为 `null`，`limits` 可选且可为 `null`，拒绝 `status`；service/store 复用授权更新链路刷新 grant、manual / team runtime source、`resource_authorization_updated` stats dirty 和授权缓存失效，operation log 使用 `authorizations.update_expire`。已通过 targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译、Markdown 相对链接检查和 `git diff --check`；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、批量到期扫描生产周期 runner / 部署调度、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization return：已补 `DELETE /__aisys__/api/authorizations/{id}/return` 和 `DELETE /__aisys__/api/my-authorizations/{id}/return` 的 Go opt-in 灰度能力，并同步修复 Node 授权归还按 grant ID 查找的生产问题。Go 侧新增 `ManagementResourceAuthorizationReturner` port、`managementauthorizations.Service.Return`、HTTP admin / self 归还 handler、router/app 注入和 PostgreSQL 事务写链路；路径 `{id}` 固定为 `resource_authorization_grants.id`，只允许被授权系统账户归还 direct manual grant，撤销 manual source、刷新 runtime authorization 为 `returned`，写 `resource_authorization_returned` stats dirty 和授权缓存失效，operation log 使用 `authorizations.return`。已通过 targeted Go 测试；Node 侧 `test:authorization-return-driver` / `test:authorization-return` 作为 grant ID 契约回归；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 resource authorization return：已补 `POST /__aisys__/api/accounts/{id}/return-authorization`、`POST /__aisys__/api/my-accounts/{id}/return-authorization`、`POST /__aisys__/api/groups/{id}/return-authorization` 和 `POST /__aisys__/api/my-groups/{id}/return-authorization` 的 Go opt-in 灰度能力。Go 侧新增资源归还 port / service / HTTP handler / router / app wiring；账户路径按被授权用户本地账号实例解析 `authorization_instance_authorization_id`，分组路径按 group resource ID + grantee 解析 runtime authorization，再只查 direct `system_account` grant，复用授权归还的 grant returned、manual source revoked、runtime refresh、stats dirty 和授权缓存失效副作用；operation log 使用 `accounts.return_authorization` / `groups.return_authorization`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端真实 Go 后端 smoke、生产切流和 Node 删除仍待完成。
- Go W4 authorization revoke：已补 `DELETE /__aisys__/api/authorizations/{id}` 和 `DELETE /__aisys__/api/my-authorizations/{id}` 的 Go opt-in 灰度能力。Go 侧新增 `ManagementResourceAuthorizationRevoker` port、`managementauthorizations.Service.Revoke`、HTTP admin / self 回收 handler、router/app 注入和 PostgreSQL 事务写链路；路径 `{id}` 固定为 `resource_authorization_grants.id`，管理侧可用 `systemAccountId` 收窄资源 owner，个人侧忽略 query 并固定当前登录用户；回收后标记 grant `revoked`，撤销 direct manual source 或当前资源 team source，刷新 runtime authorization 为 `revoked`，写 `resource_authorization_revoked` stats dirty 和授权缓存失效，operation log 使用 `authorizations.revoke`。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization list：已补 `GET /__aisys__/api/authorizations` 和 `GET /__aisys__/api/my-authorizations` 的 Go opt-in 灰度能力。Go 侧新增 `ManagementResourceAuthorizationLister` port、`managementauthorizations.Service.List`、HTTP admin / self 列表 handler、router/app 注入和 PostgreSQL grant 主表分页读取；列表响应为轻量 DTO 和 `sourceSummary`，不返回 usage / limits / authorizationSources 详情，不触发到期扫描。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization detail：已补 `GET /__aisys__/api/authorizations/{id}` 和 `GET /__aisys__/api/my-authorizations/{id}` 的 Go opt-in 灰度能力。Go 侧新增 `ManagementResourceAuthorizationGetter` port、`managementauthorizations.Service.Get`、HTTP admin / self 详情 handler、router/app 注入和 PostgreSQL grant 主表按 id 读取；路径 `{id}` 固定为 `resource_authorization_grants.id`，详情只读当前授权关系、limits、source 明细和基础 usage 空对象，不触发到期扫描，不做 usage 聚合；非资源归属人 source 内部字段会脱敏。已通过 targeted Go 测试；真实 PG/Redis/Asynq operation-log integration、前端统一授权页 smoke、授权 usage、`/expire` 到期扫描路由 / 到期 worker、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization usage overview：已补 `GET /__aisys__/api/authorizations/usage/team-details`、`GET /__aisys__/api/my-authorizations/usage/team-details`、`GET /__aisys__/api/authorizations/usage/user-details` 和 `GET /__aisys__/api/my-authorizations/usage/user-details` 的 Go opt-in 灰度能力。Go 侧新增 `000020_w4_authorization_usage_windows.sql`、usage overview service / handler / router / app 注入和 PostgreSQL range window 读取；team/user 明细只读预聚合窗口表，先分页再 join 当前页摘要，不在请求路径扫描 `usage_records` 或实时汇总。已通过 targeted Go 测试；真实 migration apply、usage range window 真实 PG smoke / 生产部署接管、批量到期扫描生产部署接管、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- Go W4 authorization usage detail：已补 `GET /__aisys__/api/authorizations/{id}/usage` 和 `GET /__aisys__/api/my-authorizations/{id}/usage` 的 Go opt-in 灰度能力。Go 侧新增 `ManagementResourceAuthorizationUsageReader` port、`managementauthorizations.Service.UsageDetail`、HTTP admin / self handler、router/app 注入和 PostgreSQL range window 读取；路径 `{id}` 固定为 grant ID，direct 授权读 user range window，team 授权读 team range window 并按 runtime authorization 分页 join user range window，不扫描 `usage_records`，不实时 `SUM/GROUP BY`，不触发到期扫描。已通过 targeted Go 测试；真实 PG 环境复跑、usage range window 真实 PG smoke / 生产部署接管、批量到期扫描生产部署接管、生产 gateway quota snapshot 消费、生产切流和 Node `/authorizations` 删除仍待完成。
- W2 / W3 Node 对照：已通过 `pnpm --filter juhe-ai-backend test:route-strategy-list-query-guard`、`pnpm --filter juhe-ai-backend test:api-key-route-validation`、`pnpm --filter juhe-ai-backend test:system-api-read-burst`、`pnpm --filter juhe-ai-backend test:group-options-lightweight`、`pnpm --filter juhe-ai-backend test:system-account-options-lightweight`、`pnpm --filter juhe-ai-backend test:captcha-rate-limit`、`pnpm --filter juhe-ai-backend test:auth-memory-maintenance-boundary`、`pnpm --filter juhe-ai-backend test:system-account-session-driver`、`pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary` 和 `pnpm --filter juhe-ai-backend test:performance-system-api-smoke`；`pnpm --filter juhe-ai-backend test:scope` 当前失败，失败点为“用户 A 应能打开自有账户详情并读取非敏感 Base URL”，属于 Node 旧链路账户详情断言，未作为本轮 Go W2 group account-options 的通过证据。
- Go 系统指标规划：已新增 `docs/migration/Go迁移指标与观测规划.md`，并同步迁移 README、迁移总览、模块减法清单、Go 架构基线、技术选型、测试验收、开发部署、接口契约、统计分层设计和后端架构入口；本轮补充 `docs/migration/Go系统指标字段迁移清单.md`，把 Node `eventLoopLagMs`、`process_event_loop_*`、`processEventLoop*`、`db-service`、V8 heap、SQLite 文件体积和 usage shard 文件路径指标列为 Go 接管时的字段级删除门禁，并明确 Go runtime、PG、Redis、Asynq、worker lag、stats freshness、网关 SLI 和前端系统监控页面替换要求。
- 文档验证：已执行覆盖本次新增 W1b / W2 文档、迁移入口、迁移清单、测试策略和 PLAN-0081 的相对链接检查，结果为 `All checked markdown links resolve.`；本轮 W3 login、Go 1.26.5 工具链、减法迁移原则和 W3 system account full mixed PATCH 文档同步后，已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve.`；本轮 W4 team read/create 文档同步后已执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 33 links.`；本轮 W4 authorization grant/source schema 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 33 links.`；本轮 W4 authorization cache invalidation 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 33 links.`；本轮 W4 team update / authorization state schema 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 33 links.`；本轮 W4 team member 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 33 links.`；本轮 W4 authorization create 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 25 links.`；本轮 W4 authorization return 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 25 links.`；本轮 W4 authorization revoke 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 71 links.`；已执行 `git diff --check`，仅有既有 LF/CRLF 提示，无 whitespace error。
- 本轮 W4 authorization usage overview 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 28 links.`。
- 本轮 W4 authorization usage detail 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 3 links.`。
- 本轮 W4 authorization expiry sweep 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 3 links.`。
- 本轮 W4 authorization usage range window refresh 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 7 links.`。
- 本轮 W4 gateway quota snapshot build 文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve.`。
- 本轮 W4 gateway quota snapshot runtime state 发布 / 消费文档同步后已再次执行本次改动 Markdown 相对链接检查，结果为 `All checked markdown links resolve. Checked 3 links.`；当前代码已通过 Node typecheck、`test:gateway-quota-passive-snapshot`、`test:performance-redis-boundary`、Go targeted 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译和 `git diff --check`。
- 未验证项：独立真实外部 URL 的 `w0-smoke` / `w1a-public-settings-smoke` / `w1b-public-api-smoke` 部署环境执行、W2 / W3 / W4 Docker/testcontainers smoke 真实复跑、W3 `TestW3ManagementLoginPostgresRedisSmoke` 真实 PG+Redis 环境复跑、W3 `TestW3ManagementSystemAccountCreatePostgresRedisSmoke` 和 `TestW3ManagementSystemAccountsPatchPostgresRedisSmoke` 真实 PG+Redis+Asynq operation log 环境复跑、W3 当前用户 session list / revoke 真实 PostgreSQL 环境复跑、W4 team read/create/update/member、authorization list/detail/create/update/return/revoke/expiry sweep、authorization usage overview / usage detail / usage range window refresh / gateway quota snapshot build 真实 PG 环境复跑、W4 authorization state schema / usage window schema 真实 migration apply、W4 authorization cache invalidation 真实 Redis state smoke、反向代理切流 smoke、前端品牌加载 smoke、浏览器连接真实 Go 后端的前端 auth/login / 系统账户页 / 团队页 / 统一授权页 smoke、真实 HTTPS / 浏览器 Cookie 行为验证、Node public settings 删除证据、W1b Node PG/Redis 对照 smoke、Go W1b / W2 / W3 / W4 生产路由挂载、W2 授权来源 / grant / 授权写接口、W3 完整会话管理生产接管 / 安全日志、W4 批量到期扫描真实 PG/Redis smoke / 生产部署接管、W4 usage range window 生产部署接管、W4 gateway quota snapshot build 生产部署接管、生产 gateway quota snapshot 消费、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 和其他写接口 operation log、Node 删除证据和网关测试均待后续代码迁移阶段执行。

- 本轮未验证补充：`juhe-ai-worker operation-log-retention-cleanup --run-once` 真实 PostgreSQL 执行、常驻 worker 长跑、生产 supervisor 配置、完整 data-retention 等价接管和 Node 删除证据仍待后续环境验证。

## 风险与注意事项

- 风险 1：Go 迁移不能误以为“goroutine 等于无限并发”，PostgreSQL 连接池、Redis 队列、上游账号并发和队列容量仍需硬边界。
- 风险 2：双运行时过渡容易残留旧实现，必须坚持单 owner 和删除证据。
- 风险 3：SQLite 数据若需要保留，必须单独离线导入 PostgreSQL，不能把双读双写或启动迁移写进运行路径。
- 风险 4：真实网关迁移风险最高，必须在存储、worker、协议测试、SSE 和性能验证成熟后执行。
- 风险 5：public group、public route strategy、public API Key、public account 四条资源纵切面、`JUHE_AI_PUBLIC_API_ENABLED` opt-in guard 和 `w1b-public-api-smoke` 只是 W1b 灰度入口，容易被误判为 `/__aipublic__` 已接管；必须保持默认关闭，直到 16 个 CRUD、account shell E2E 在真实 Docker/testcontainers 环境通过、PG+Redis smoke、公开日志副作用、反向代理单 owner 切流和 Node 删除证据整体完成。
- 风险 6：W2 管理辅助接口如果绕过 Node 当前 `requireAuth` / `requireAdmin` / `my-*` 作用域，会把后台数据裸露出来；因此当前 `proxies/options`、`system-accounts`、`system-accounts/options`、`authorization-options/grantee-accounts`、`my-authorization-options/grantee-accounts`、`authorization-options/grantee-teams`、`my-authorization-options/grantee-teams`、`authorization-options/grantee-groups`、`my-authorization-options/grantee-groups`、`providers/options`、`providers/models/options`、`providers/{code}/models`、`route-strategies/options`、`my-route-strategies/options`、`groups/options`、`my-groups/options`、`groups/account-options`、`my-groups/account-options`、`accounts/options`、`my-accounts/options`、`accounts/tags`、`my-accounts/tags`、`DELETE accounts/tags/{tagId}`、`DELETE my-accounts/tags/{tagId}`、`PATCH accounts/{id}/tags` 和 `PATCH my-accounts/{id}/tags` 虽已补 Go `requireAuth` 会话鉴权基线、admin-only 管理侧下拉、self-only 用户侧下拉、authorization grantee accounts / grantee teams / grantee groups、分组授权组只读 union、账户授权账户只读 union 和标签删除绑定保护，也仍必须由 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 默认关闭。显式开启只允许灰度验证已迁移路径，不能替代授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 和其他写接口 operation log、前端 smoke、单 owner 切流和 Node 删除证据。
- 风险 6 补充：operation log 保留清理 worker 已补 Go 代码后，仍不能把 W2 标为已接管；该 worker 只覆盖 `operation_logs` 主表按保留期批量删除，不覆盖 public API log、runtime log、model check、usage record 等完整 data-retention，也没有生产 supervisor 和 Node 删除证据。
- 风险 7：系统指标如果只是把 Node 事件循环字段改名为 Go 字段，会造成错误观测和误告警；Go 接管时必须把系统指标统计作为 W6 / W7 独立分块，先落地 Go 原生采样、低基数 Prometheus 标签、PG 窗口表、内部系统监控 DTO、前端契约和旧字段删除证据。
- 风险 6 补充：`GET /__aisys__/api/providers` 虽已补 Go 灰度路径，但只代表供应商定义全量读接口迁移；自定义模型 CRUD 虽已作为补充写接口进入 Go opt-in，也不等于供应商定义写接口、供应商协议档案写接口或生产接管。该路径必须继续保持 admin-only、返回停用供应商、默认测试模型偏好按当前作用域覆盖，且 `/providers/options` 的启用过滤不能被回归。
- 风险 7：系统指标如果只是把 Node 事件循环字段改名为 Go 字段，会造成错误观测和误告警；Go 接管时必须把系统指标统计作为 W6 / W7 独立分块，先落地 Go 原生采样、低基数 Prometheus 标签、PG 窗口表、内部系统监控 DTO、前端契约和旧字段删除证据。
- 发布异常处理：每个模块生产切换前保留上一版发布包、配置和数据备份；schema 变化不依赖运行时自动回滚。

## 完成总结

- 完成时间：待后续迁移全部完成后填写。
- 实际完成内容：当前完成 M0 文档规划，W0 Go 常规工程基线已落地，W1a 公开设置读接口已进入 Go 实现中，W1b 已补 `/__aipublic__` 四类公开资源纵切面和 public API log worker runtime；W2 已补管理端只读辅助、标签删除 / 独立 PATCH、operation log 读接口、operation log 保留清理 worker 和默认测试模型偏好写入的 Go opt-in 灰度能力；W3 已补 captcha、login / session 创建小切片、current-user、profile update、change-password、session touch、logout、logout 清 Cookie 安全属性、current-user session list / revoke 和 system account create / full mixed PATCH 的 Go opt-in 灰度能力；W4 已补团队列表 / 详情读接口、`POST /system-teams` 团队创建、`PATCH /system-teams/{id}` 团队更新、`POST /system-teams/{id}/members` 成员新增、`DELETE /system-teams/{id}/members/{memberId}` 成员移除、`GET /authorizations` / `GET /my-authorizations` 授权列表、`GET /authorizations/{id}` / `GET /my-authorizations/{id}` 授权详情、授权 team/user usage overview、授权 `:id/usage` 用量明细、`POST /authorizations` 和 `POST /my-authorizations` 授权创建、`PATCH /authorizations/{id}` 和 `PATCH /my-authorizations/{id}` 授权更新、`PATCH /authorizations/{id}/expire` 和 `PATCH /my-authorizations/{id}/expire` 授权有效期更新、`DELETE /authorizations/{id}/return` 和 `DELETE /my-authorizations/{id}/return` grant ID 授权归还、`POST /accounts/{id}/return-authorization` / `POST /my-accounts/{id}/return-authorization` / `POST /groups/{id}/return-authorization` / `POST /my-groups/{id}/return-authorization` 账号分组资源页归还、`DELETE /authorizations/{id}` 和 `DELETE /my-authorizations/{id}` 授权回收 Go opt-in 灰度能力、批量授权到期扫描 worker、授权用量 range window 刷新 worker、gateway quota snapshot 构建 worker、授权来源 / grant / 额度窗口 / 统计脏标记 PostgreSQL schema 和授权缓存失效；前端已补 W3 auth / session / 系统账户页和 W4 团队 / 统一授权页本地调用与 UI 边界 smoke。浏览器连接真实 Go 后端的前端 smoke、生产切流和 Node `/auth` / `/system-accounts` / `/system-teams` / `/authorizations` 删除仍未完成；Go 系统指标与观测迁移规划已补齐并明确 W6 / W7 独立分块门禁；业务接口尚未正式生产接管。
- 追加完成内容：W2 已补 `juhe-ai-worker operation-log-retention-cleanup` 操作日志保留清理 worker；该能力仅作为 Go opt-in worker 代码与 targeted 测试完成，不代表生产 supervisor、完整 data-retention 或 Node 删除已完成。
- 追加完成内容：W4 已补授权 team/user usage overview、授权 `:id/usage` 用量明细、批量授权到期扫描 Go worker、授权用量 range window 刷新 Go worker、gateway quota snapshot 构建 Go worker 和 gateway quota snapshot Redis runtime state 发布 / Node Redis 模式消费；usage 读取均只读 range window，批量到期扫描只读 grant 到期索引并刷新 runtime，range window 刷新只从授权日汇总表写 hot ranges，gateway quota snapshot 构建只读预聚合统计与小时窗口，共享快照缺失 / 不完整时继续由 Node gateway 回 DB service 精确补判；批量到期扫描真实 PG/Redis smoke / 生产部署接管、授权用量窗口真实 PG smoke / 生产部署接管、gateway quota snapshot 真实 PG/Redis smoke / 生产部署接管仍未完成。
- 主要改动位置：`docs/migration/`、`docs/plans/计划-0081-Node转Go渐进减法迁移.md`、`backend-go/`。
- 验证结果：W0 常规 Go 验证矩阵、race、lint、漏洞扫描、命令入口、health smoke、W1b/W2/W3 已迁移切片的快速测试和跨平台构建已记录；本轮 W4 team read/create/update/member、authorization list/detail/usage overview/usage detail/create/update/expire/grant-return/resource-return/revoke/expiry sweep、authorization usage range window refresh、gateway quota snapshot build、authorization state schema 和 authorization cache invalidation 已在 Go `go1.26.5 windows/amd64` 下通过 `gofmt`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、integration 包编译、本次改动 Markdown 相对链接检查和 `git diff --check`；Node 授权归还 ID 契约已通过 `pnpm --filter juhe-ai-backend test:authorization-return-driver`、`pnpm --filter juhe-ai-backend test:authorization-return` 和 `pnpm --filter juhe-ai-backend typecheck`。W3 current-user session list / revoke、system account create smoke 代码和前端本地 auth / session / system-account smoke 已在 Go `go1.26.5 windows/amd64` + pnpm 11.7.0 下通过 `sqlc generate`、`gofmt`、`go mod tidy -diff`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、integration 包编译、`pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke`、`pnpm --filter juhe-ai-frontend typecheck` 和 `pnpm --filter juhe-ai-frontend build`；W4 前端团队 / 统一授权页本地调用 smoke 已通过 `pnpm --filter juhe-ai-frontend test:w4-team-authorization-smoke`。真实 `TestW2ManagementAuthPostgresSmoke` 与 `TestW3ManagementSystemAccountCreatePostgresRedisSmoke` 因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PostgreSQL / Redis / Asynq integration 通过。W3 system account full mixed PATCH 既有验证仍成立：已通过 `sqlc generate`、`gofmt`、`go mod tidy -diff`、targeted Go 测试、`go test ./... -count=1`、`go vet ./...`、integration 包编译、`git diff --check` 和本次改动 Markdown 相对链接检查；`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountsPatchPostgresRedisSmoke -count=1` 当前因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PG/Redis/Asynq operation log integration 通过。`TestW3ManagementLoginPostgresSmoke` 同样需 Docker 健康环境复跑；浏览器真实后端前端 smoke、W4 真实 integration 和 Node `test:scope` 账户详情 Base URL 断言需后续单独处理。
- 后续建议：下一步在具备 Docker 或真实 PG/Redis 连接串的环境执行 W0/W1a/W1b integration、W0 live smoke、`w1a-public-settings-smoke`、`w1b-public-api-smoke` 和 W1b account shell E2E 真实复跑；随后评估生产 router 挂载、反向代理切流和 Node 删除证据。
