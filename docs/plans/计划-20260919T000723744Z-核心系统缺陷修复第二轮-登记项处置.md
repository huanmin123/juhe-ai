# 计划-20260919T000723744Z-核心系统缺陷修复第二轮-登记项处置

## 基本信息

- 计划 ID:`PLAN-20260919T000723744Z`
- 状态:进行中
- 创建时间:2026-09-19(UTC `20260919T000723744Z`)
- 需求来源:用户确认"按列出项全部执行,只要有价值、确实是问题"。
- 前序计划:[PLAN-20260918T142845703Z](计划-20260918T142845703Z-调度状态探活核心系统缺陷修复.md)(已完成,提交 `2eb2154e6`)。
- 执行方式说明:工作区存在**并行工作线**(2026-09-19 07:44-08:01 持续修改 `gateway cmd` 的 `main.go`/`compose.go`/`ratelimit` 及配套测试,最后写入距盘点不足 5 分钟)。本轮所有子任务**禁止修改 `backend-go/projects/gateway/cmd/` 下任何文件**;原登记项中依赖 cmd 装配面的部分降级为注记,见"W6 遗留项修正"。

## 需求目标

处置第一轮复审登记的全部"确实成立"项:

1. dial 阶段(建连前)失败与"已到达上游"transport 失败拆分,本机网络故障不再批量污染账户熔断统计(A)。
2. 删除 Node 未接线机制的死代码群:`business/account_runtime` 整包 + `gatewayaccounteffects` 死半区(B);`engine.AccountState` 端口 + 调用点 + `gatewaycircuit` 抑制写面方法(C,内部包范围)。
3. jobs 侧:OAuth token 保活接入 `JUHE_AI_ACCOUNT_HEALTH_ENABLED` 最小装配;outbox drain 有界并发与可配上限;堆积告警;J1 每周期 settings 重读优化(D)。
4. 性能(低风险子集):路由状态注册表锁内 O(n) 扫描(E1);`latency_degraded` 排序 N+1 批量化(E2)。
5. 文档澄清:图像 lane 瞬态失败不触发同 Key 重试的既成语义;`stream_failure_count` 只清不写的历史口径(F)。

## 范围边界

### 本次不做(本轮新登记/维持登记)

- **cmd 装配面注记项**(因并行工作线占用 `gateway cmd`,本轮降级为注记,待其完成后另做):组合根"机制启用状态" startup 日志;`SuppressionWaiter`/`AcquirePrecheckHalfOpenLease` 随 cmd 接线的完整移除;`chain_runtime.go` 抑制构造处 `CanUseProcessLocal` 注释与装配一致化。相关死代码处以显式注释标注"写面已退场,见 PLAN-20260918T142845703Z W6 / 本计划"。
- 热质量 MGET/pipeline 批量化、preparation 快照复用、J1 quota N+1 批量化:需 Redis 模式压测基线或语义评审,维持登记于 `PLAN-20260918T064119294Z`。
- 图像 lane 瞬态失败行为本身不改代码(与 Node parity 一致且换号语义可辩护),仅文档澄清。
- 真实上游 / 生产验证;git push。

## 执行拆解与写入范围(非重叠)

- [ ] A(`gatewaydispatch` 内部):`transport.go` 识别 dial 阶段失败(`url.Error`→`net.OpError.Op=="dial"`/DNS 错误),标记区分;`attemptoutcomes.go` dial 阶段失败仍排除当前候选但**不喂** `ReportTransportFailure` 熔断计数;新增回归测试。禁改 `transport.go`/`attemptoutcomes.go` 之外的既有文件。
- [ ] B(删除死包):先 `rg` 验证零生产引用后删除 `internal/business/account_runtime` 整包;`internal/gatewayaccounteffects` 仅删除无生产调用者的死符号(`NewSideEffectsService`/`ApplyAccountErrorHandlingWithCacheInvalidation`/`ShouldSkipHealthySuccessfulAccountSideEffect`/`RecordGatewayAccountFailureForPrecheck` 等)及其测试,保留生产在用的 `AccountAPIKeyEffects`/`AccountAPIKeyFailureGuard`/KeyModel;若删除被 `cmd` 下测试引用,跳过该符号并报告。
- [ ] C(`gatewaydispatch`+`gatewaycircuit` 内部):删除 `engine.AccountState` 字段、`AccountStateMutations` 端口、`accountpreparation.go` 唯一调用块(79-163 行代理不可用分支内),更新相关测试赋值(`w13g3_dispatch_units_test.go`、`portsandprep_test.go`);删除 `gatewaycircuit` 抑制写面方法(`Suppress`/`ClearSuppression`/`SuppressForGatewayFailure`/`DegradeForGatewayFailure`/`ActivateRuntimeDegradation`/`ClearDegradation`)及专属测试,读面(`FilterSuppressions`/store 构造)保留并加显式"写面已退场"注释。
- [ ] D(`projects/jobs` 全模块):①`oauthrefresh` runner 接入 `JUHE_AI_ACCOUNT_HEALTH_ENABLED` 最小装配路径(复用 worker 装配构造,单 owner 语义不变);②outbox drain 每周期行上限改为可配 env(默认上调)并有界并发(小池,默认 2-4,行间保持 deadline 校验);③drain 结束统计 pending 堆积,超阈值 `slog.Warn`(阈值 env 可配);④`loadDirectSchedule` 每周期重读 `system_settings` 改短 TTL 缓存(或等价安全方案,以代码事实定)。
- [ ] E1(`gatewayrouting`):`routeselector.go` 键注册去 O(n) 线性扫描(map+序表),weighted 状态锁内原位更新替代整 map 拷贝;轮询/权重语义逐位等价,既有测试全绿。
- [ ] E2(`gatewayproxyhealth`):`latencydegradation.go` 排序入口逐账户 `GetJSON` 改 `GetJSONMany` 批量(对齐 `ListNormalRouteLatencyDegradedRuntime` 既有用法);无批量驱动的后端保持回退正确。
- [ ] F(文档,主代理):`架构总览.md` 网关主链路段补"同 Key 瞬态重试仅文本 lane,图像 lane 瞬态失败按统一候选规则继续扫描";`AI账户错误语义与状态变更边界.md`(或就近权威文档)登记 `stream_failure_count` Go 链路只清不写口径。
- [ ] G 集成验证:三模块 build、改动包 vet、定向+相关包全量测试。
- [ ] H 独立复审;I 提交并收尾本计划。

## 测试项

| 类型 | 测试项 | 验证方式 | 预期 | 状态 |
| --- | --- | --- | --- | --- |
| 单元(A) | dial 阶段失败不进熔断计数 | 新增回归(dial 错误注入) | 候选仍排除;`ReportTransportFailure` 未被调用 | 已通过（dialphase_test 5 用例随 R3 提交 d64e51c9b 入库，独立复审 F 节复验全绿）|
| 单元(A) | 非 dial transport 失败行为不变 | 既有 transport 失败测试 | 熔断统计照旧 | 已通过（非 dial 对照用例断言熔断 mutation>0，防回归）|
| 单元(B/C) | 死代码删除后零残留引用 | `rg` 符号全仓 + 构建 | 无引用、编译通过 | 未执行 |
| 回归(B/C) | 删除后包全量测试 | 改动包 `go test -count=1` | 全绿 | 未执行 |
| 单元(D) | oauthrefresh 最小装配可构造 | jobs 构建 + 定向测试 | `ACCOUNT_HEALTH_ENABLED=1` 路径含保活 runner | 未执行 |
| 单元(D) | drain 并发/上限/告警 | 新增定向测试 | 上限可配、并发有界、超阈告警一次 | 未执行 |
| 单元(D) | settings 短 TTL 缓存 | 定向测试 | 周期内不重复读,变更后及时生效 | 未执行 |
| 单元(E1) | 轮询/权重语义等价 | `gatewayrouting` 既有测试全量 + 新增大池排序测试 | 分布不变 | 未执行 |
| 单元(E2) | 批量读等价 | 单/批量结果一致断言 + 包测试 | 排序结果一致 | 未执行 |
| 集成 | 三模块 build + 改动包 vet | 同前轮 | 通过 | 未执行 |
| 不适用 | 真实上游/生产、cmd 装配项 | 见范围边界 | — | 不适用 |

## 风险与注意事项

- 并行工作线占用 `gateway cmd`:任何代理不得写 `cmd/` 下文件;发现删除目标被 cmd 引用时跳过并报告,不强行解耦。
- B/C 删除属不可逆代码移除:以 git 提交 `2eb2154e6` 为恢复基线;删除前逐符号 `rg` 复核。
- D②并发探针会改变 J1 对上游的并发压力:并发默认值保守(≤4),上限 env 可配,行级 deadline 语义不变。
- E1/E2 必须语义等价,禁止借机改排序规则;既有测试是等价性证据。
- 本计划所有代码改动验证仍限于本地构建与测试;上线路径不在授权范围。

## 进度记录

- 2026-09-19:计划创建。发现并行工作线占用 `gateway cmd`(07:44-08:01 共 26 文件),范围调整:cmd 装配面项降级为注记;其余按 A-F 并行派发(写入统一 `glm_5.3_flash_max`,因环境重启频发,写入代理中断后由主代理按已落盘编辑接手验证,与第一轮同口径)。
- 2026-09-19 执行结果:
  - A(dial 拆分)完成:`transport.go` 新增 `DialPhaseTransportError` 包装与 `isPreConnectionDialError` 判据(`url.Error`→`net.OpError.Op=="dial"`/裸 `DNSError`,对原始 client.Do 错误判别以避开 normalizeTransportError 的文本替换丢结构);`attemptoutcomes.go` 两处熔断喂食加 `!dialPhaseTransportFailure` 守卫;5 个新用例(含真实 client.Do 打关闭端口、熔断 mutation=0 断言与非 dial 对照)。包全量绿。
  - B 改由主代理执行(派发遇账户限流):`business/account_runtime` 整包删除(全仓零引用,tracked 无修改,可从 `2eb2154e6` 恢复);`gatewayaccounteffects` 死半区(SideEffectsService/queue/epoch registry/policy/AccountEffects,约 1900 行)确认生产零调用,但 8 个测试文件与活符号混测(约 4000 行需逐函数修剪),按注记路线处置:`doc.go` 登记生产接线边界,整簇删除登记专项清理。
  - C 完成:`engine.AccountState` 字段、`AccountStateMutations` 端口及附属类型、三处调用块(nil 守卫两处 + unsafe URL 一处,生产组合根恒 nil 从未执行)、`accountStateMutationEnabled` 参数删除;接口检查确认六个抑制写方法不被任何 cmd 可达接口声明,4 个零引用已删除,`Suppress`/`SuppressForGatewayFailure` 因 cmd 测试以具体类型直接调用而注记保留;两包测试全绿。**因 gatewaydispatch 包被并行线 codex 重构在途打破而暂缓提交**。
  - D 部分完成:②③④(drain 上限 64→256 且 env 可配、有界并发池、堆积告警 10 分钟频控、settings 60s TTL 缓存)落地并测试绿,已提交;①(oauthrefresh 进最小装配)代码已落盘但依赖 jobs cmd `main.go` 接线,随 jobs-cmd 暂缓提交。
  - E1 完成:键去重 map 化 + weighted 状态锁内原位更新(顺带消除原"拷贝-修改-替换"丢失更新竞争),5000 绑定分布先旧实现录基线再钉值断言,`-race` 绿。
  - E2 完成:排序入口 `GetJSONMany` 批量化,共享 helper 收敛三类无状态回退,等价/失败传播测试绿。
  - F 完成:两处 lane 澄清 + stream_failure_count 口径登记。
- 2026-09-19 提交:无争议范围已提交 `4d47decaa`(23 文件,+1175/-3071);暂缓范围见下节。
- 2026-09-19 09:50 B 收尾完成:gatewayaccounteffects 死半区整簇删除落地并提交 `412f679a4`(16 文件,+129/-9572)。sideeffects.go 收敛为 SideEffectsConfig{RuntimeStateDriver}+IsRedisDriver;accounteffects/sideeffectqueue/sideeffectpolicy 三个生产文件与 sideeffects/sideeffectqueue/we_effects_facade 三个测试文件整删;四个混合测试文件按编译器引导逐函数修剪,活用例全保留;测试专用 helper 迁入使用方文件。验证:生产零引用、cmd 生产+测试编译通过、包全量测试绿(13.1s)、我方触达文件 gofmt 干净。提交同时带入了并行线已暂存的 3 个临时文件删除(go_tests_tmp.txt/gemline.tmp/w14p_prof.out,良性),如实披露。
- 2026-09-19 独立复审(H)完成(`glm_5.3_high`,复审对象 2eb2154e6/4d47decaa/d372ec2fa/412f679a4 共 8 个复审点):**整体判定通过、无 blocker**——六项通过,outbox 治理与死簇删除两项"有保留通过"(前者为跨提交归属需与 d372ec2fa 合并看待 + 同账户并发交错依赖既有 epoch/fence 兜底属观察项;后者为文档残留)。复审提出的两处文档残留(doc.go 过时段落、we_sideeffects_extra_test.go 空壳注释)已随即修正并复测绿。gatewaydispatch/gatewaycircuit 修复(已被 R3 线整合)不在其范围,维持"随 R3 提交"登记。

## 暂缓提交登记(2026-09-19 11:10 销账)

并行工作线身份已确认:它在执行 `PLAN-20260918T064119294Z` 后端架构与性能优化改革(R3 god 包拆分等,工作区可见 accounts/gatewaydispatch/openaicompat 大分解、helpers 下沉 `shared/platform/circuitstate`、`openaicompatcore` 新包等,该计划文档亦在其修改中)。因此:

- **已销账**:该线于 2026-09-19 11:0x 提交 `d64e51c9b`(refactor(gateway): 优化网关链式请求处理和健康检查机制),gatewaycircuit 全部与 gatewaydispatch 的 A/C 修复已随其入库。HEAD 复核:A 的 dial 判定(`gatewayupstream/transport.go` + `attemptoutcomes.go` 三处守卫)与 C 的 `AccountStateMutations` 零残留均健在;四模块构建全绿。
- 顺带修复:该线 shared/platform upstreamhttp 的零值 idle 语义变更导致 jobs 两个传输测试确定性失败(probe/proxylatency),按其文档化新语义对齐测试期望并提交 `94f879229`。
- 该线继续在途的新改动(gateway cmd / gatewayruntimecache / gatewayusage prometheus 等 ~13 文件)属其后续波次,不在本计划范围。

## 验证记录

| 验证 | 结果 |
| --- | --- |
| `go build` gatewaycircuit/gatewayrouting/gatewayproxyhealth/gatewayaccounteffects/business | OK |
| `go vet` gatewaycircuit/gatewayrouting/gatewayproxyhealth/gatewayaccounteffects | OK |
| 包全量:gatewaycircuit 11.3s / gatewayrouting 9.2s / gatewayproxyhealth 3.3s / gatewayaccounteffects 14.0s | 全绿 |
| `go build ./...`(jobs 模块) | OK |
| `go test ./internal/accounthealth/ -count=1 -skip TestProbeTransportEnablesHTTP2ForCustomDialer` | ok 26.8s |
| 新增用例:dialphase 5 / e1 大池 3(先旧实现录基线)/ wh 批量 3 / w19d 12 | 全绿 |
| 已知既有失败(非本计划):jobs `TestProbeTransportEnablesHTTP2ForCustomDialer`(并行线 shared/upstreamhttp 改动所致)、jobs cmd `TestW16G*`(并行线 w16 测试实时编辑中) | 如实登记,不背锅 |
| gatewaydispatch 包构建/测试 | 暂缓(并行线 codex 重构在途) |

主代理自查补充:A 的 dial 判据在 normalizeTransportError 之前对原始错误判定,Unwrap 链与既有 started 分类兼容,候选排除行为保持;E1 消除的丢失更新竞争属等价性增强;D② 并发消费的幂等性以逐行 RequestID + outcome 投影 epoch/fence 兜底论证成立。

## 完成总结

(执行后填写)

## R3 搭车入库补记(2026-09-19 独立复审后)

- 任务 A(dial 拆分)的代码与 `dialphase_test.go` 5 用例最终**随 R3 重构提交 `d64e51c9b` 入库**(transport 迁入 `gatewayupstream/` 子包时一并搬运;`attemptoutcomes.go` 守卫健在),非本计划直接提交——独立复审已追溯授权链(本计划 4d47decaa 的 A 项设计)确认非搬运漂移。
- 复审披露:R3 提交信息仅描述"链式请求处理和健康检查",未声明混入的 dial-phase 熔断计账行为变更与 accounts 等大分解;属该执行线的提交信息卫生问题,已向用户报告,由其后续波次自行改进。
- R3 波次抽样独立复审结论(2026-09-19):五项拆分主张(叶子零回引/搬运纯度/桥完备/测试无净丢失/当前全绿)经抽样全部成立,可保持入库;非代码 follow-up(过时测试表、R3 进度记录补记)中前者已随本次修正,后者属 R3 线文档。

## 真实混合 E2E(2026-09-19 用户追加授权)

按用户提供的真实上游账户(aijh.huanmin.top,glm-5.3-flash,chat 协议;凭据来自当次消息)+ dev PG 临时子库(juhe_ai_sub2api_dev_e2e0919*)+ dev Redis 独立 namespace(juhe-ai:dev:e2e0919*)搭建手工真号混合 E2E(`e2e_manual_real_test.go`,env 门控默认 skip,已提交 `7100d97eb`)。

**测试中发现并修复一个 PG 模式全局回归(7100d97eb)**:`catalog_visible` 列在 R3 提交(d64e51c9b)的 PG schema 中改为 boolean,而 `business/settings` 与 `providers/catalog` 的查询仍用 SQLite 方言 `= 1`,导致 performance 模式下**所有 /v1 请求 preflight.failed 500**。按包内既有 s.pg/s.mode 方言模式修复。该回归为 SQLite 自动化矩阵无法覆盖的盲区——验证了用户坚持真实环境 E2E 的价值。

**场景结果(当前代码,PG+Redis 拓扑)**:
- S1 真号直连 + opencode 真实客户端:通过(真号回复、usage/审计归因、opencode 成功)
- S2 dial 挂死换号:通过(挂死主机优先 + 真号兜底,3 轮全部成功,真号账户保持 active,dial 失败不污染熔断——验证任务 A 修复)
- S3 mock500 熔断换号:通过(熔断确认后 mock 停止被尝试)
- S5 成本护栏:通过(真号可用时 mock 零调用)
- S4 额度 fence 恢复:**部分通过/开放调查项**——403 → rate_limited + fence 写入正确;J1 探针管线确认工作(outcomes 落库、游标推进,已用 SQL tracer + 保留库取证);但复测恢复未在 120 秒观察窗内完成(输入 TTL 已放宽至 10 分钟仍复现)。需要 J1 调度/选择/投影链路的专项排查(输入 TTL、扫描间隔、观察窗最小时长、退避阶梯的交互),SQLite 矩阵等价场景通过,故定性为 PG direct input 链路的专项调查,非本计划范围。

**环境清理**:全部临时子库已删除、scratch 工具已移除;Redis namespace `juhe-ai:dev:e2e0919*` 的残余键需按 runbook 用 SCAN+UNLINK 清理(本机无 redis-cli,遗留待清理)。

**本地提交总计(未 push)**:2eb2154e6 / 4d47decaa / d372ec2fa / ca69a6718 / 412f679a4 / e05128f8d / 3b0001b98 / b680742d3 / 32d7abc9d / e3be55c25 / 94f879229 / 7100d97eb / 4f6c5ca53。

## S4 开放调查项结案(2026-09-19 追加调查)

经多轮取证(保留库诊断 + pgx QueryTracer + 规则路径切换),S4 恢复未完成的根因**不是缺陷**,是 J1 探活的 HTTPS-only 安全策略:

- J1 探针的输入由网关派发,`allow_insecure_base_url` 恒为 false(rg 全仓无 writer);`parseBaseURL` 对 http scheme 一律拒绝("base URL 必须使用 HTTPS")。
- 因此纯 HTTP 的本地 mock 上游**结构性不可被 J1 复测恢复**——探针每次都以 `probe_task_failure / base_url_invalid` 终结,与 fence/调度/投影无关(该链路已由 outcomes + 游标推进证据验证工作正常)。
- 额度类恢复的 `duration_minutes` 有 30 分钟下限、账户规则 `duration_hours` 下限 1 小时,均为产品设计门槛。
- S4 已改用账户级规则(500 → temp_unschedulable)驱动同一 J1 fence 复测链路(`196f53037`);**完整恢复闭环的 E2E 验证需要 HTTPS 测试上游(自签 CA 注入)或 allow_insecure 派发白名单特性,属产品决策,另立专项**。
- 同期发现并修复:并行 R3 会话 catalog 方言回归的修复在其后续编辑中幸存;`chain_catalog.go` 的 CAST 修复健在。

### S4 决策落地(2026-09-19 用户拍板:J1 探活放行 http+https)

- `accounthealth/probe.go` parseBaseURL 放行 http 与 https(旧 allowInsecure 旗标语义保留兼容),`wg_probe_units_test.go` 契约同步更新(`d9e8638dd`)。
- `mockupstream` 新增 `chat_echo` 场景(回显最后一条 user 消息),满足 J1 探活挑战回显校验——固定罐头响应无法通过 containsChallenge,该场景补上了 mock 库的能力缺口。
- **S4 完整恢复闭环已验证**:403 → rate_limited + fence → J1 探针(http mock,chat_echo 回显)→ complete_success → 投影恢复 active → 账户重新服务,全程 7 秒。完整套件 5 场景全 PASS(`e2e-manual-run31.log`)。
- 环境二次清理:临时库 e2e0919j2/m2 已随测试删除,Redis namespace 复验归零。
