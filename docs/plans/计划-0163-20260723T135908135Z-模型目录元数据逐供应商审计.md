# PLAN-0163-20260723T135908135Z 模型目录元数据逐供应商审计

## 基本信息

- 计划 ID：PLAN-0163-20260723T135908135Z
- 序号：0163
- 状态：已完成
- 创建时间：2026-07-23
- 关联模块：模型目录 / 供应商驱动 / Node / SQLite / PostgreSQL / Go migration / 前端
- 设计文档：[模型目录元数据审计设计](../functions/模型目录元数据审计设计.md)

## 目标

- 逐一核对 OpenAI、Anthropic、Gemini、xAI、DeepSeek、GLM 的模型目录字段。
- 补齐可交叉核验的发布时间和其他缺失参数；仍无法补齐的内置条目移出可见目录，确保内置模型零缺失。
- 确认管理页、运行时目录和数据库初始化快照读取同一事实。

## 执行清单

- [x] 定位 6 个供应商驱动、模型事实源、数据库生成链和前端发布时间列。
- [x] 建立字段证据等级、未知值策略与完成条件。
- [x] OpenAI 逐模型审计。
- [x] Anthropic 逐模型审计。
- [x] Gemini 逐模型审计。
- [x] xAI 逐模型审计。
- [x] DeepSeek 逐模型审计。
- [x] GLM 逐模型审计。
- [x] 汇总并修改模型事实、回归断言与新 schema migration。
- [x] 增加内置可见模型必填元数据完整性门禁，自定义模型继续允许可选字段为空。
- [x] 执行模型目录专项测试、类型检查、构建和浏览器验证。
- [x] 复查需求覆盖、字段边界、证据、代码质量、迁移和文档一致性。
- [x] 2026-07-24 复核 Gemini / Grok / GLM 的缓存与容量显示，移除容量推导和重复缺失占位。
- [x] 为 Gemini 增加独立缓存存储时费，修正 GLM 免费缓存能力并移除缺少官方 USD 价格的 `glm-4-flashx-250414`。

## 初始事实

- 前端发布时间列已绑定 `releaseDate`，缺失时显示 `-`。
- DeepSeek 当前 4 条模型均有显式 `release_date`。
- Anthropic 当前 44 条模型仅 5 条有显式 `release_date`；dated ID 之外的大量别名需要判断能否继承官方模型族日期。
- Gemini 14 条、GLM 18 条、xAI 3 条模型没有显式 `release_date`。
- OpenAI 主要通过 `provider-driver.registry.ts` 的映射补发布日期，需要核对映射完整性和准确性。

## 验证记录

- 运行时目录快照：GPT 69、Anthropic 15、Gemini 11、xAI 7、DeepSeek 4、GLM 15，共 121 条；缺少发布日期、协议、输入输出模态或计价事实的内置条目均为 0。无法补齐的 `gpt-5-search-api`、`grok-4.3`、`glm-4-long`、缺少官方 USD 价格的 `glm-4-flashx-250414`，以及非官方 `glm-5.2-free` 已从内置事实源删除，而不是只做前端隐藏。
- 121 条内置事实的 `provider-model-catalog-snapshot-regression`、全内置源 `provider-model-catalog-completeness-regression`、`test:account-model-filter`、`test:provider-auth-account-capability`、xAI 前后端专项、Node/前端生产 build、前端 typecheck、Go public account / migration catalog tests 均通过。
- 后端全量 typecheck 在本计划改动后曾通过；最终复跑时被并行 API Key 列表改动阻断：多个脚本引用当前 `repositories.ts` 尚未导出的 `getApiKeyUsageByIdsAsync`。后端生产 build 仍通过，该阻断不来自模型目录文件。
- `test:model-catalog` 在完成快照门禁、完整性门禁和全部供应商目录断言后，停在并行基线中的“管理员个人模型能否设为系统默认检查模型”HTTP 契约差异：当前接口返回 200，旧断言期望 400；该行为不属于本计划修改范围。
- 隔离 SQLite 后端使用开发自动登录，隔离前端由 Python Playwright 临时启动并自动回收。桌面 GPT 弹窗 20 个当前分页模型、`390x844` 移动端 59 个对话/编码模型的发布时间均为 `YYYY-MM-DD`，控制台无错误，截图位于系统临时目录。
- 2026-07-24 追加浏览器验证：Gemini、Grok、GLM 在 `1440x1000` 与 `390x844` 下均只显示已有容量字段，Gemini 独立显示缓存存储时费，Grok/GLM 不再出现空写入价格；页面无横向溢出、弹窗未越界、控制台无错误。二进制容量使用易读单位并保留精确 Token 数。
- 复查确认：完整性门禁覆盖全部内置事实源，不阻止管理员或用户保存资料尚不完整的自定义模型；内置管理接口不能清空日期、协议、价格或全部容量事实。seed upsert 按 `(provider_code, model)` 识别旧 ID，区分 `manual-override`、`manual-visibility-override` 和普通快照行，既保留明确运营配置，也允许补全后的历史快照行恢复。

待实施后填写。
