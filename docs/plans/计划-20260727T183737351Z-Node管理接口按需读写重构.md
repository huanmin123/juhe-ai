# PLAN-20260727T183737351Z Node 管理接口按需读写重构

## 基本信息

- 计划 ID：PLAN-20260727T183737351Z
- 状态：已完成
- 创建时间：2026-07-28
- 需求来源：用户反馈
- 关联问题：BUG-0137
- 关联重构：REFACTOR-0004
- 范围：Node 后端、Vue 前端、接口契约、回归与 Browser 验证；不包含 Go

## 目标与设计

从“我的 AI 账户”开始，把管理接口从共享宽 `Summary` 改为场景 DTO 与字段级写入；同时修复新增 / 编辑弹窗无交互预加载、API Key 全量表单 PATCH，以及审计确认的其他高收益同类路径。

- 设计文档：[Node 管理接口按需读写设计](../superpowers/specs/2026-07-28-Node管理接口按需读写设计.md)
- 问题记录：[BUG-0137](../bug/问题-0137-管理弹窗无交互预加载与全量读写.md)
- 重构记录：[REFACTOR-0004](../refactors/重构-0004-管理接口场景DTO与字段级写入.md)
- 历史方案：[PLAN-20260704T095333001Z](计划-20260704T095333001Z-管理接口DTO分层与渐进式加载.md) 已提出 DTO 分层，但当前实现仍保留完整 summary 查询、无交互预加载和宽写入，本计划以可执行查询与写入证据重新验收。

## 执行清单

- [x] 读取项目规范并限定 Node + Vue 范围。
- [x] 并行审计 AI 账户、API Key、共享默认资源和其他页面同类调用。
- [x] 固化读写边界、缓存语义、竞态与验收设计。
- [x] 建立登录后用户作用域默认分组 / 默认策略引用缓存。
- [x] 让 AI 账户新增首开不加载分组、标签和模型候选。
- [x] 拆分 AI 账户列表、edit-basic、advanced 和运行态 Node 查询投影。
- [x] AI 账户 Vue 只提交变化字段，Node 只更新相关列 / 关联表 / 缓存。
- [x] 修复 API Key 首开策略 options、列表 usage、secret 查询和字段级 PATCH。
- [x] 修复审计确认的第一批高收益无交互 options / 重复详情读取。
- [x] 完成代理、系统账户、系统团队、供应商模型、标签、外部来源和账户批量编辑的同类问题审计，并把未纳入本轮的高风险宽写入登记为后续分批治理项。
- [x] 增加 HTTP、repository、并发、no-op、查询数和响应体积回归。
- [x] 运行 Node / Vue 类型、定向测试、构建和差异检查。
- [x] 使用隔离开发实例与 Browser 验证 Network 请求矩阵。
- [x] 完成性能、遗漏、并发、边界、过度设计、文档和无关差异复查。

## 验收标准

- 打开 AI 账户测试弹窗不请求模型；展开模型才加载供应商模型，展开请求形态只加载当前模型能力。
- 打开 AI 账户新增弹窗不请求 group / tag / model options；选择供应商也不自动加载模型或完整分组目录。
- 打开 API Key 新增 / 编辑弹窗不请求 route strategy options；展开下拉才请求。
- AI 账户列表和 edit-basic 的 repository 不调用完整 summary 查询后裁剪，响应不含设计明确禁止字段。
- 单字段账户 / API Key PATCH 不覆盖其他字段、不重写无关关联表、不访问 usage 统计；相同值不产生写入。
- 任何归一化后真实发生的业务变化都推进 `configRevision`；只有网关身份、连接、调度、额度或健康相关字段触发运行态、探针、额度窗口或缓存副作用。
- 管理员目标账户与本人默认引用缓存严格隔离；登出、切换用户和相关资源写入后正确失效。
- 已确认的重复详情 / 模拟下拉路径完成修复，确有必要的详情路径保留。
- 固定 20 行列表最多 4 次业务查询、1 次统计查询、64 KiB 未压缩 JSON；`edit-basic` 最多 3 次业务查询、0 次统计查询、0 次 DML、8 KiB JSON；同值 PATCH 为 0 次 DML，单标量 PATCH 最多 1 次账户 DML、0 次统计查询、0 次关联表写入、1 KiB mutation JSON。不得用虚构的改造前基线替代这些绝对预算。
- 所有自动化、类型、构建、Browser 网络验证和性能采样有可复核记录。

## 风险与注意事项

- 默认资源 ID 是用户作用域数据库事实，不是跨用户编译期常量；缓存失效和管理代管 scope 必须准确。
- 列表中 today usage 与并发是可见列，不能为缩包直接删除；应改为专用最小摘要和有界批量查询。
- 账户凭据保存在聚合 JSON 中，字段级 PATCH 必须在 Node 合并后做完整校验，不能在 SQL 层盲目 JSON merge。
- 授权账户的来源字段、权限和绑定语义不同，不能套用 owner 账户写入命令。
- 当前工作区存在其他未提交改动；重叠文件必须保留现有修改，不整理无关差异。
- 代理、系统账户、系统团队、供应商模型和账户批量编辑仍有已证实的宽读写；本轮不能在缺少各自 revision、字段契约和回归夹具时顺手全量改写。审计结果与顺序见设计文档 8.1。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| Node 契约 | 账户 list / edit-basic / advanced 字段与查询边界 | 定向 regression + HTTP smoke | 无禁止字段、无完整 summary mapper | 通过 | list query/projection、status snapshot/presentation、advanced、legacy detail removal、test-options HTTP 和凭据脱敏均通过 |
| Node 写入 | 账户单字段 / no-op / 版本冲突 | `test:account-management-patch`、性能夹具 | 只更新相关字段且不丢更新 | 通过 | owner 条件进入锁行 SQL；revision、局部关系写、同值 0 DML 与无关字段不触发运行态归一化均覆盖 |
| API Key | 列表、secret、PATCH 查询与写入边界 | `test:api-key-demand-write` | secret 不访问统计，额度 / 启停变化才同步窗口 | 通过 | route PATCH、刷新、删除、跨实例 validation cache ACK 和字段级写入均通过 |
| Vue 行为 | 新增 / 编辑 / 下拉请求矩阵 | 组件行为回归 | 首开零 options，首次展开单请求 | 通过 | 账户测试、账户新增、API Key 新增/编辑、owner/scope 迟到隔离和增量 payload 回归均通过 |
| 共享引用 | 登录、切用户、管理 scope、失效 | 单飞与竞态回归 | 不串用户、不阻塞弹窗 | 通过 | 登录后默认分组/路由缓存、scope key、写后失效和竞态回归通过 |
| 性能 | 20 行列表、edit-basic、单字段 PATCH | `test:account-management-performance` | 满足绝对查询 / 字节 / DML 上限 | 通过 | 观测：列表 4+1 查询、26,236 bytes；edit-basic 3+0 查询、911 bytes；备注 PATCH 445 bytes、1 DML、0 关系表写；no-op 0 DML |
| 类型与构建 | Node / Vue typecheck 和 build | workspace scripts | 通过 | 通过 | Node `tsc --noEmit`、Vue `vue-tsc --noEmit`、Node build 和 Vue production build 全部通过 |
| Browser | 隔离自动登录 Network 验证 | Codex Browser | 请求矩阵符合设计 | 通过 | 列表只请求账户列表；新增账户/API Key 首开 0 options；默认分组 0 请求；模型和策略路由仅在展开时各请求对应 options |
| 差异 | 空白和范围检查 | `git diff --check` + scoped diff | 无错误 | 通过 | `master` 工作区干净；最终修正提交为 `ed7162639` |

## 验证记录

2026-07-28 完成最终收口。除表内命令外，账户/API Key/分组/策略路由/授权/响应检查策略/外部来源/使用记录相关定向回归共 65 项通过；隔离实例使用独立 SQLite、日志和 usage 目录，验证后停止。

## 完成总结

账户和 API Key 主链路已从宽 Summary 与全量保存收敛为场景 DTO、按需 options 和字段级 PATCH；默认分组与默认路由通过登录后用户作用域共享引用缓存复用。全站复审发现的其他资源宽读写不混入本阶段，转入 [PLAN-20260728T141710063Z](计划-20260728T141710063Z-Node管理接口按需读写后续分批治理.md) 分批治理。
