# PLAN-0156-20260722T123439000Z 彻底移除页面数据确认机制

## 基本信息

- 计划 ID：PLAN-0156-20260722T123439000Z
- 序号：0156
- 创建时间戳（UTC）：20260722T123439000Z
- 状态：已完成
- 创建时间：2026-07-22
- 更新时间：2026-07-22
- 需求来源：用户确认 page-data confirm 在高频变化账户数据下收益不足，要求彻底删除且无残留
- 执行者：AI
- 开发分支：`codex/remove-page-data-confirm-20260722`
- 发布状态：禁止部署，合并 master 后等待用户另行通知

## 需求目标

- 删除 `/data-changes/confirm` 和整套 page-data domain/revision/change publisher。
- 删除前端 IndexedDB/resource/request cache、activation 和 mutation 自动失效。
- 删除 Node/Go Redis publisher、dirty-domain recovery、IPC、测试和脚本。
- 页面恢复直接调用业务接口，不保留兼容层或替代探针。
- 完整验证后合并并推送 master，不部署。

## 关联文档

- 设计：`docs/superpowers/specs/2026-07-22-彻底移除页面数据确认机制设计.md`
- 实施计划：`docs/superpowers/plans/2026-07-22-彻底移除页面数据确认机制实施计划.md`
- 被替代计划：PLAN-0115、PLAN-0139、PLAN-0141、PLAN-0151

## 任务清单

- [x] 从最新 `origin/master` 建立隔离 worktree 和分支。
- [x] 完成全仓前端、Node、Go、测试与文档依赖盘点。
- [x] 落地删除设计、实施计划和本任务清单。
- [x] 建立“禁止 page-data 残留”门禁。
- [x] 删除前端机制并恢复直接业务请求。
- [x] 删除 Node confirm/runtime/publisher/IPC/dirty-domain。
- [x] 删除 Go publisher/recovery/port/store 接线。
- [x] 删除专项测试、smoke、benchmark、scripts 和当前文档残留。
- [x] 运行专项、typecheck、lint、build、Go 和浏览器验证。
- [x] 完成独立代码复查和残留审计。
- [x] 同步最新 master、复验、合并并推送 origin/master。
- [x] 确认未部署。

## 测试项

| 测试类型 | 验证方式 | 预期 | 状态 |
| --- | --- | --- | --- |
| 残留门禁 | `rg` + `scripts/regression/page-data-removal-gate.ps1` | 生产代码、scripts、package 命令无 page-data confirm/revision/publisher | 通过 |
| 前端 | 账户渐进加载、使用记录 options、认证 session、账户测试模型/按需加载、供应商模型操作 | 直接请求，行为不丢失 | 通过 |
| Node | 账户/分组/系统账户 options、授权 options、System API 限流、storage adapter | 无 confirm 路由和发布副作用 | 通过 |
| Go | `go test ./...` | publisher/recovery 删除后全绿 | 通过 |
| 整合 | 前后端 typecheck/build、根目录 lint | 全部退出码 0 | 通过 |
| 浏览器 | `/my-accounts` 首屏与手动刷新网络、控制台、截图 | 无 confirm；刷新只新增业务列表请求；无 error/warn | 通过 |
| Git | `git diff --check`、新增行敏感信息扫描、最终 master 同步 | 干净、无新增凭据、代码无冲突 | 通过，合并提交 `cc468cff6` 已推送 `origin/master` |

## 验收标准

- [x] `/data-changes/confirm` 不存在。
- [x] 前端不再存在 page-data API、revision token、activation、IndexedDB page-data cache 或 mutation interceptor。
- [x] Node/Go 不再发布或恢复 page-data change，不再读写 page-data Redis key/dirty-domain。
- [x] 当前文档不把该机制描述为有效架构；历史计划有明确退场标记。
- [x] 页面直接请求业务 API，自动化和浏览器验证通过。
- [x] 已合并并推送 master；未部署。

## 决策记录

- 不做“只删聚合层、保留旧 confirm”的折中，因为根因是高频域持续 changed。
- 不做空路由、feature flag 或兼容 shim，避免机制继续被误用。
- 不用新的版本协议替换；未来高频页面优化必须按静态数据与动态快照分层另立计划。
- 不运行生产 Redis 扫描清理；旧 key 按 TTL 过期，本次不部署。

## 验证记录

- 机制门禁：`scripts/regression/page-data-removal-gate.ps1` 通过，并扩展覆盖 `frontend/scripts`、Node、Go、package scripts 和 dirty-domain 当前产物。
- 构建检查：`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir backend typecheck`、`pnpm --dir backend build`、`pnpm lint` 全部通过。
- Go：`go test ./...` 全量通过；迁移目录包含 `000071_w7_drop_page_data_dirty_domains.sql`，历史 `000056` 保留用于已执行数据库的迁移链。
- 前端专项：账户渐进加载、使用记录模型候选、认证 transient session、账户测试模型、账户测试按需加载、供应商模型来源操作均通过。
- Node 专项：账户/分组/系统账户轻量 options、授权 options 查询、System API 限流、storage adapter boundary 均通过。
- 浏览器：Playwright 打开 `http://127.0.0.1:5174/__aisys__/my-accounts`，页面与空状态正常；点击“刷新”后网络中仅新增 `GET /__aisys__/api/my-accounts`，`confirmRequests=[]`，控制台 error/warn 为空。
- 安全与 Git：`git diff --check` 通过；相对 `origin/master` 的新增行无密钥、私钥或含凭据连接串模式。
- 独立复查发现并修复：`frontend/scripts` 旧缓存测试、门禁覆盖盲区、两个空 invalidation 壳、历史计划目录摘要和合并后 Go 测试契约。

## 完成总结

- 通用 page-data confirm/revision 探针机制已从前端、Node、Go、Redis publisher、dirty-domain recovery、IPC、测试、脚本和当前架构入口中彻底删除。
- 页面恢复直接请求业务 API；保留健康探针、冷却重试、网关缓存、业务快照和请求并发合并等独立能力。
- PostgreSQL 通过 `000071_w7_drop_page_data_dirty_domains.sql` 向前删除旧表；历史迁移 `000056` 保留以维持已执行迁移链完整。
- 已完成全量构建、lint、Go 测试、专项回归、浏览器网络验证、门禁、敏感信息扫描和两轮独立复查。
- 合并提交 `cc468cff6` 已推送 `origin/master`；本任务未执行部署，等待用户另行通知上线。
