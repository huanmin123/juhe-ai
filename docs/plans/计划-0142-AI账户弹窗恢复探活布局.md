# PLAN-0142 AI 账户弹窗恢复探活布局

## 基本信息

- 编号：PLAN-0142
- 状态：已完成 / 本地验证通过 / 禁止上线
- 创建时间：2026-07-18
- 关联模块：AI 账户新增、AI 账户编辑、高级配置、恢复探活

## 目标

- “持续恢复探活”使用一行设置项：左侧中文名称与帮助图标，右侧开关。
- 帮助图标悬浮展示现有完整说明，用户无需猜测字段用途。
- 新增与编辑复用同一个弹窗，保存字段、默认开启、授权实例只读边界保持不变。

## 清单

- [x] 增加单行布局、标准帮助图标和移除独立表单行的失败回归。
- [x] 使用 `QuestionCircleOutlined` 替换字符问号。
- [x] 实现左文右开关的紧凑布局。
- [x] 专项回归 `test:temporary-unavailable-probe-toggle` 通过。
- [x] 前端专项回归与 typecheck 通过。
- [x] 根 build 通过。
- [x] 使用真实 `AccountEditModal` 临时验收页完成浏览器视觉与交互检查，临时文件和服务已清理。
- [x] 验证单行 `flex`、左侧名称和帮助图标、右侧开关、垂直居中及开关状态切换。

## 边界

- 不改变探活状态机、后端字段或默认值。
- 不改变授权实例的只读限制。
- 不部署生产。
