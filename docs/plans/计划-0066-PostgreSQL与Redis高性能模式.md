# PLAN-0066 PostgreSQL 与 Redis 高性能模式

## 基本信息

- 编号：PLAN-0066
- 状态：进行中
- 创建时间：2026-06-25
- 需求来源：用户反馈用户量增长后 SQLite + 内存缓存已无法承载，要求新增默认单机模式和 PostgreSQL + Redis 高性能模式，并在 `<测试主机IP>` Linux Docker 主机上搭建测试环境，后续生产复用同一套部署。
- 执行者：AI / 维护者
- 关联模块：后端 / 存储 / DB service / 后台 worker / 网关 / 缓存 / Redis / PostgreSQL / Docker 部署 / 迁移 / 文档 / 验证

## 需求目标

- 新增双运行模式：默认 `standalone` 使用 SQLite + 内存缓存，高性能 `performance` 使用 PostgreSQL + Redis。
- 保持调用方不关心底层缓存实现，统一通过缓存工具和运行态工具访问 memory / Redis。
- PostgreSQL 模式下取消 SQLite 文件级单写者限制，保留 typed command / 队列语义，但写入消费可以并发，最大消费并发为 `100`；使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引队列先用 Redis Streams 承接高性能模式可恢复缓冲。
- 在 `<测试主机IP>` Linux Docker 主机搭建 PostgreSQL、PgBouncer、Redis cache、Redis state 测试栈，生产环境复用该拓扑和调优基线。
- 明确数据切换边界：代码库不提供 SQLite -> PostgreSQL 数据迁移、旧 PostgreSQL 结构迁移、双读双写或旧 schema 兼容；历史数据由上线窗口在代码库外单独处理。

## 范围边界

### 本次包含

- [x] 新增 PostgreSQL 与 Redis 高性能模式长期设计文档。
- [x] 同步架构总览和 functions / plans 索引。
- [x] 新增运行模式配置与启动校验。
- [x] 新增 `DatabaseClient`、`SqlDialect` 和 SQLite adapter，保证现有 SQLite 回归先通过。基础层已完成并接入 PostgreSQL 初始化路径；核心管理链路、网关链路、日志、使用记录、主要统计窗口和外部来源链路已完成双 driver 适配；低频入口、复杂筛选和长周期运维任务按 fail-fast 与专项验证管理。
- [x] 新增 PostgreSQL schema、连接池、PgBouncer 配置和 repository adapter。已完成 PostgreSQL pool 骨架、schema 初始化脚本、默认种子写入、核心 repository adapter、主要 System API / 网关读写路径、统计窗口、外部来源和远端 PG 验证；生产切换前仍需在目标机器完成本机资源安装、schema 初始化、数据导入演练和维护窗口回归。
- [ ] 新增 `AppCache` / `RuntimeStateStore` 抽象，完成 memory driver 和 Redis driver。已完成 `SharedJsonCache` 与 `RuntimeStateStore` 基础 driver；API Key 校验、API Key 额度、授权额度、系统设置、网关分组访问元数据、网关模型目录、管理端模型目录、响应检查策略、授权读资源 lookup 和客户端 IP 封禁策略快照已接入 Redis `SharedJsonCache` 并补源码门禁；登录失败窗口、验证码 challenge / 发放限频、账号并发槽和网关缓存失效版本广播已接入 Redis runtime state，其他调用点仍需继续迁移。
- [ ] 调整 DB service / ingest / stats 写队列，在 PostgreSQL 模式下支持最大消费并发 `100`。已完成使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引 Redis Streams 队列与 PG drain 骨架，账号测试状态表和账户内 API Key runtime state 已补 PG async；账号测试 / 探测执行缓冲是否迁 Redis Streams 待单独评估。
- [x] 编写 Docker Compose 测试栈和服务器部署说明。
- [x] 明确数据切换边界：不在代码库内维护数据迁移脚本、旧结构迁移脚本或运行时兼容分支；高性能模式只维护当前 schema 初始化和当前路径验证。
- [ ] 补充自动化回归、压测、Redis / PostgreSQL 故障演练和备份恢复验证。已完成 System API PG/Redis 压测、真实网关 `/v1/*` 压测、Redis Streams pending 重启恢复、PostgreSQL / PgBouncer 重启恢复、PostgreSQL 备份恢复到临时库演练、SQLite standalone DB service 分支回归和 PG schema 回归；生产规格目标机维护窗口演练仍待执行。

### 本次不包含

- 不把 PostgreSQL / Redis 变成默认依赖。
- 不在常规运行路径做 SQLite 与 PostgreSQL 双读、双写或自动迁移。
- 不引入 Kafka、RabbitMQ、BullMQ、ClickHouse 或其他外部队列 / OLAP。
- 不实现多节点自动故障转移、跨地域复制或自动用户迁移。
- 不把 Redis 作为账务、权限、授权、使用记录或审计事实源；Redis Streams 只作为队列缓冲，数据库落库成功后才 ack。

## 关联文档

- 架构文档：[整体架构设计](../architecture/架构总览.md)
- 功能设计：[PostgreSQL 与 Redis 高性能模式设计](../functions/PostgreSQL与Redis高性能模式设计.md)
- 存储适配：[存储适配接口设计](../functions/存储适配接口设计.md)
- 现有存储说明：[SQLite 存储说明](../functions/SQLite存储说明.md)
- 写队列治理：[SQLite 单写者写队列治理设计](../functions/SQLite单写者写队列治理设计.md)
- 数据集分片：[数据集库分片写入设计](../functions/数据集库分片写入设计.md)
- 统计拆分：[统计数据集与结果库拆分设计](../functions/统计数据集与结果库拆分设计.md)
- 后台任务：[后台任务使用说明](../architecture/backend/后台任务使用说明.md)
- 部署文档：[部署指南](../deploy/部署指南.md)
- 验证文档：[测试与验证说明](../develop/测试与验证说明.md)

## 方案概述

- 模式：`standalone = SQLite + memory`，`performance = PostgreSQL + Redis`，默认仍为 `standalone`。
- 数据库：通过 `DatabaseClient` 和 `SqlDialect` 抽象 SQLite / PostgreSQL 差异；PostgreSQL 按 `juhe_business`、`juhe_dataset`、`juhe_usage`、`juhe_stats`、`juhe_codex_context` schema 承接当前多库职责。
- 缓存：通过 `AppCache` 承接可丢弃缓存，通过 `RuntimeStateStore` 承接原子计数、锁、并发占用、限流和短 TTL 调度态。
- 队列：typed command 不推翻；SQLite 模式继续按 owner 串行，PostgreSQL 模式入队即触发 drain，默认最大消费并发 `100`，并受连接池和同 key 顺序限制；`redis_stream` 先覆盖使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引队列，其他队列按同一边界后续评估。
- 部署：测试主机 `<测试主机IP>` 使用 Docker Compose；生产同构，至少包含 PostgreSQL、PgBouncer、Redis cache、Redis state 和应用服务。

## 执行拆解

- [x] 阶段 0：设计文档落地，明确双模式、PG schema 映射、Redis 抽象、队列并发和部署边界。
- [x] 阶段 1：配置和启动校验，新增 runtime mode、database driver、cache driver、runtime state driver。
- [x] 阶段 2：数据库抽象，SQLite 先跑在统一 `DatabaseClient` / `SqlDialect` 下，现有测试不变。基础层和主要回归已完成，核心管理链路、网关链路、日志、使用记录、主要统计窗口和外部来源链路已接入。
- [x] 阶段 3：PostgreSQL schema 和 repository adapter，完成业务库、dataset、usage、stats、codex context 的表结构映射。schema 初始化、默认种子、核心 repository adapter、主要 System API / 网关读写路径、统计窗口和外部来源 PG 路径已完成；低频入口、复杂筛选和长周期运维任务继续按 fail-fast 与专项验证管理。
- [ ] 阶段 4：Redis cache / runtime state driver，迁移网关运行态、缓存失效、限流和会话亲和。基础 driver、登录失败窗口、验证码 challenge / 发放限频、账号并发槽、网关缓存失效 runtime state 版本广播、API Key 校验缓存、API Key 额度缓存、授权额度缓存、系统设置、网关设置、网关分组访问元数据、网关模型目录、模型路由索引、管理端模型目录、响应检查策略、授权读资源 lookup、客户端 IP 封禁策略快照和混合路由评分结果 Redis shared cache 已完成。
- [ ] 阶段 5：队列并发改造，PostgreSQL 模式下写队列入队即消费，最大并发 `100`，增加背压指标。使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引 Redis Streams 队列已完成，账号测试状态表和账户内 API Key runtime state 已补 PG async；账号测试 / 探测执行缓冲待按真实积压和幂等成本单独评估。
- [x] 阶段 6：Docker 测试栈，在 `<测试主机IP>` 搭建 PostgreSQL、PgBouncer、Redis cache、Redis state。中间件容器已 healthy，PG schema 与默认种子已在远端库验证。
- [x] 阶段 7：数据切换边界收口，不提供 SQLite -> PostgreSQL 数据迁移脚本、旧 PostgreSQL 结构迁移脚本、启动期自动迁移或双读双写；历史数据处理不进入代码库。
- [ ] 阶段 8：压测与故障演练，覆盖高并发写入、连接池耗尽、Redis 重启、PostgreSQL 重启、备份恢复。System API、真实网关、Redis pending 接管和 PG/Redis 重启恢复已通过；生产规格维护窗口演练仍待补。
- [ ] 阶段 9：同步部署和验证文档，形成生产可复用配置、备份、恢复和监控说明。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 文档验证 | 设计文档和索引 | 人工检查 docs 链接 | 新设计、计划、架构入口和索引互相链接正确 | 已通过 | 当前只落地文档 |
| 配置回归 | standalone 默认启动 | `pnpm --filter juhe-ai-backend test:runtime-config-env-override` | 行为与当前 SQLite + memory 一致 | 已通过 | 已覆盖默认 standalone 和 performance 配置读取 |
| 配置边界 | performance 缺少依赖 | runtime config regression / 启动校验 | 快速失败，错误可读 | 已通过 | 配置层已校验 PG / Redis URL；PG repository 仍 fail-fast |
| SQL 抽象 | SQLite / PostgreSQL DatabaseClient 基础回归 | `pnpm --filter juhe-ai-backend test:database-client` | SQLite 真库 CRUD / upsert / transaction 正常；PostgreSQL 占位符、execute、transaction、多语句 DDL result 归一化正常 | 已通过 | 基础层已落地；`bindPlaceholders()` 固定调用层继续写 `?`，PG driver 统一转 `$1...` |
| PG 主流程 | PostgreSQL schema 初始化 | `pnpm --filter juhe-ai-backend postgres:init-schema-only`、`postgres:init-schema`、`test:postgres-seed-defaults` | 当前 schema 创建成功，默认数据写入成功 | 已通过 | `<测试主机IP>` 最新完整 `postgres:init-schema` 验证 5 个 schema、594 条 schema 语句、147 条默认种子语句。默认 provider/profile seed 已覆盖全部内置分组引用，旧 provider 级默认分组唯一索引已显式 drop，避免多 protocol profile 默认分组冲突 |
| PG repository | provider 只读 repository 双 driver | `pnpm --filter juhe-ai-backend test:provider-repository-driver` | SQLite 和 PostgreSQL 供应商、协议档案、endpoint family 读取语义一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；`/__aisys__/api/providers` 和 `/__aisys__/api/providers/options` 已切到 async repository |
| PG repository | 登录 / 会话最小 repository 双 driver | `pnpm --filter juhe-ai-backend test:system-account-session-driver` | 默认管理员凭据校验、session 创建 / 查询 / touch / revoke、最近登录时间更新一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；登录、登出、`requireAuth` 和 `/auth/me` 会话读取已切到 async repository |
| PG repository | 系统账户管理读写双 driver | `pnpm --filter juhe-ai-backend test:system-account-management-driver` | 系统账户分页 / 选项 / 创建 / 默认分组 / 更新 / 禁用 / 唯一性校验一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；`/__aisys__/api/system-accounts` 列表、选项、创建和更新已切到 async repository |
| PG repository | 分组管理读写双 driver | `pnpm --filter juhe-ai-backend test:group-management-driver` | 分组分页 / 选项 / 账户组选项 / 创建 / 高并发调度策略 / 更新 / 删除 / 唯一性校验一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；`/__aisys__/api/groups` 列表、选项、账户组选项、创建、更新和删除已切到 async repository；PG 模式下分组列表已读取真实分组和绑定账户 ID，统计聚合仍待账号 / 统计 repository 迁移 |
| PG repository | API Key 管理读写与网关 Key 校验双 driver | `pnpm --filter juhe-ai-backend test:api-key-management-driver` | API Key 分页 / secret / 创建 / 分组绑定 / 更新 / 刷新密钥 / 删除 / 唯一性校验 / 网关 Key 校验 / `read_gateway_runtime` Key、分组和最小账号候选读取入口一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；`/__aisys__/api/api-keys` 列表、secret、创建、更新、刷新密钥和删除已切到 async repository；回归通过 `createAccountAsync` 创建带支持模型和模型映射的最小候选账号并绑定分组；PG 模式下 `read_gateway_runtime` 已能读取有效 Key、绑定、分组访问元数据、默认响应检查策略、最小可调度账号候选、账号支持模型和模型映射；混合路由模型目录 HTTP 校验已由 `test:performance-system-api-smoke` 覆盖；删除后的 dataset/stats 清理目标登记仍待清理仓储迁移 |
| PG query guard | API Key 列表 keyword / 分组筛选 PG explain | `pnpm --filter juhe-ai-backend test:api-key-list-query-guard`、`test:api-key-list-postgres-smoke`、`test:api-key-management-driver`、`test:postgres-schema-sql` | PG API Key 列表名称搜索不使用 `ILIKE` 或长文本扫描；keyword 先通过名称索引 CTE 锁定匹配 ID，再按原列表排序输出；分组和 active 筛选命中 route strategy group / API Key 索引 | 已通过 | 本地类型检查、SQLite query guard、API Key 管理回归和 PG schema SQL 通过；2026-06-28 远端 PostgreSQL / Redis URL 下 `test:api-key-list-postgres-smoke` 通过，覆盖名称精确 / 前缀、`%` 字面量、分组 + active 筛选、列表不返回完整密钥和 explain `explainIndexed=true` |
| PG repository | 公共设置和系统 API 限流设置双 driver | `pnpm --filter juhe-ai-backend test:settings-public-driver` | 公共全局设置、全局设置列表和系统 API 限流设置读取一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；`/__aisys__/api/settings/public` 和系统 API 限流中间件已切到 async repository |
| PG repository | 系统设置管理读写双 driver | `pnpm --filter juhe-ai-backend test:settings-management-driver` | 全局设置和系统设置的局部更新、恢复、缓存失效、非法字段拒绝一致 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；`/__aisys__/api/settings/global` 和 `/__aisys__/api/settings` 已切到 async GET / PATCH；PG 模式暂不支持在线修改统计时区 |
| PG repository | 响应检查策略管理端 CRUD | `pnpm --filter juhe-ai-backend test:response-inspection-policy`、`test:response-inspection-policy-postgres-smoke` | 管理端列表 / 创建 / 更新 / 删除在 standalone 和 PG 模式语义一致；provider scoped 校验走 async provider 查询；网关 active 策略 PG 查询命中索引；操作日志不回退同步设置读取 | 已通过 | 本地 SQLite 回归通过并固定路由层使用 async repository / async 操作日志入口；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖 protocol active、provider active、禁用排除、删除清理和 PG explain `explainIndexed=true` |
| PG repository | 代理管理 / 检测状态 CRUD | `pnpm --filter juhe-ai-backend test:proxy-single-read`、`test:proxy-postgres-smoke` | 管理端代理列表 / options / 创建 / 更新 / 删除、代理测试配置读取、检测状态写回和代理 URL 解密在 standalone 和 PG 模式语义一致 | 已通过 | 本地 SQLite 回归通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖临时代理创建、列表、options、更新用户名 / 密码、URL 解密、检测状态写回、启用候选读取和删除清理 |
| PG repository | 外部来源系统 source/token 管理与公开鉴权 | `pnpm --filter juhe-ai-backend test:external-integration-source-async-boundary`、`test:external-integration-source-expires-at`、`test:external-source-auth`、`test:external-integration-source-postgres-smoke` | 来源系统列表 / 创建 / 更新 / 删除、token 创建 / 查看 / 更新、内置测试 token 重置和公开 Bearer token 鉴权在 standalone 和 PG 模式语义一致 | 已通过 | 本地边界 / expiresAt / 完整外部来源公开接口回归通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖临时来源系统创建、前缀列表、token 计数、secret 解密、scope 校验、token 更新、source 禁用同步 token、禁用后鉴权拒绝、删除清理 |
| PG repository | 外部来源公开账号 / 分组 / API Key 推送 | `pnpm --filter juhe-ai-backend test:external-public-account-push-async-boundary`、`test:external-source-auth`、`test:external-public-account-push-postgres-smoke` | 公开账号新增 / 列表 / 修改 / 删除、公开分组新增 / 列表 / 修改 / 删除、公开 API Key 新增 / 列表 / 修改 / 删除在 standalone 和 PG 模式语义一致，公开账号写入操作日志不回退同步入口 | 已通过 | 本地完整外部公开接口回归和源码门禁通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖公开账号、分组、API Key 全链路和临时目标用户清理。PG 模式下 API Key 删除关联 usage / audit 明细清理暂不投递本地 dataset 任务，返回 `postgres_record_cleanup_not_supported`，后续需补 PG 版清理 worker |
| PG repository | 外部来源公开账号用量读路径 | `pnpm --filter juhe-ai-backend test:external-public-account-usage-query-guard`、`test:external-public-account-usage-postgres-smoke` | `/__aipublic__/account/usage` 在 standalone 和 PG 模式均只读预聚合范围窗口，关键词先解析账号 ID，不在请求链路聚合明细或日表 | 已通过 | 本地 query guard 固定窗口表读取、索引排序和 async 路由边界；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖全局 account 范围窗口排序、账号名前缀搜索、账号元数据补齐、owner 字段不外露、mock 分支和 PG explain `explainIndexed=true` |
| PG repository | 系统团队管理端 CRUD | `pnpm --filter juhe-ai-backend test:system-team-query-guard`、`test:system-team-single-read`、`test:system-team-fanout-boundary`、`test:system-team-postgres-smoke` | 团队列表 / 创建 / 更新 / 添加成员 / 移除成员在 standalone 和 PG 模式语义一致；成员变更触发的团队授权来源展开 / 撤销不回退同步仓储；操作日志不回退同步入口 | 已通过 | 本地 query guard、单条读取和授权展开边界回归通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖团队列表关键词、成员作用域列表、团队授权创建、添加成员自动展开授权、移除成员撤销来源、停用 / 启用团队撤销与恢复来源，并对列表排序、名称前缀、成员窗口和成员作用域查询执行 PG explain，输出 `explainIndexed=true` |
| PG repository | 账户流量迁移 async 写路径 | `pnpm --filter juhe-ai-backend test:account-traffic-migration-query-guard`、`test:account-traffic-migration-postgres-smoke` | `POST /accounts/:id/traffic-migration` 在 PG 模式下不回退同步账号迁移仓储或同步操作日志；源账号状态 / 冷却写入、目标账号可用性校验和网关运行态迁移输入保持原语义 | 已通过 | 本地 typecheck 和 query guard 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖同组源 / 目标账号迁移、源账号 `temporary_unavailable` 与冷却时间写入、目标账号保持 active / schedulable，并对账号读取和同组绑定读取执行 PG explain，输出 `explainIndexed=true`。同时修复 PG 写路径误触 SQLite 分组账号统计脏标记 |
| PG repository | AI 账户导入 HTTP 路径 | `pnpm --filter juhe-ai-backend test:account-import-availability-schedule`、`test:account-import-postgres-smoke` | `POST /accounts/import/preview` 和 `POST /accounts/import/confirm` 在 PG 模式下不回退同步导入服务或同步操作日志，自动建分组和账户创建落到 PG | 已通过 | 本地导入契约和 async direct-call 回归通过；2026-06-28 `<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖 System API 登录、导入 preview / confirm、自动创建临时分组、导入账号落库、分组绑定、标签绑定和 availabilitySchedule JSON 保存 |
| PG repository | OpenAI OAuth 管理端 async repository | `pnpm --filter juhe-ai-backend test:openai-oauth-token-response-boundary`、`test:openai-oauth-credentials-patch`、`test:openai-oauth-refresh-activation`、`test:openai-oauth-access-token-refresh` | OAuth 创建、Refresh Token 创建、手动刷新和重新授权不再导入同步 repository / 操作日志入口；OAuth 凭据补丁、草稿激活和后台刷新语义不变 | 已通过 | 本地回归和源码门禁通过；受真实 OpenAI OAuth code / refresh token 限制，未做远端 PG 下真实 token endpoint 交换 smoke |
| PG repository | 账户单独绑定分组路由 async repository | `pnpm --filter juhe-ai-backend test:account-single-read`、`test:account-single-group-binding-postgres-smoke` | `POST /accounts/:id/group` 不再导入同步账户读取 / 分组绑定 / 操作日志入口，PG 模式下能把账户从 A 分组唯一切换到 B 分组 | 已通过 | 本地 SQLite 回归和源码门禁通过；2026-06-28 `<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖 System API 登录、临时分组 / 账户创建、单独绑定分组 HTTP 调用和 PG 绑定唯一性校验 |
| PG repository | 账户标签管理端 CRUD | `pnpm --filter juhe-ai-backend test:account-tags`、`test:account-tags-postgres-smoke` | 标签列表 / 删除 / 独立更新在 standalone 和 PG 模式语义一致；绑定标签删除约束和绑定计数正确 | 已通过 | 本地 SQLite 回归通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖创建账户带标签、列表计数、替换标签、删除约束和清理 |
| PG repository | 账户导出 async 读路径 | `pnpm --filter juhe-ai-backend test:account-tags`、`test:account-export-postgres-smoke` | 按 ID / 按筛选导出在 PG 模式下不回退同步账号列表、账号摘要、代理读取或同步操作日志；导出内容保留凭据、代理、标签、支持模型和 summary | 已通过 | 本地账户标签 / 导出格式回归和源码门禁通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖临时账号、代理和分组创建后按 ID 导出密钥 / 接口能力 / 标签 / 支持模型 / 分组名 / 代理密码，以及按筛选导出 matchedAccounts 和凭据回填 |
| PG repository | 认证资料更新 / 改密 async repository | `pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary`、`test:system-account-session-driver`、`test:auth-profile-postgres-smoke` | `/auth/me` 显示名称更新和 `/auth/change-password` 撤销其他会话不再导入同步系统账户 / 会话入口；PG 模式下强制改密、资料更新、普通改密旧密码校验和旧会话撤销语义一致 | 已通过 | 本地回归和源码门禁通过；2026-06-28 `<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖临时用户创建、初始改密前资料修改拦截、强制改密、显示名称更新、普通改密旧密码校验、旧会话撤销和 PG 字段校验 |
| PG repository | 公告管理端 / 公开公告 CRUD | `pnpm --filter juhe-ai-backend test:announcement-single-read`、`test:announcement-postgres-smoke` | 公开公告列表 / 已读、管理端列表 / 详情 / 创建 / 更新 / 发布 / 下线 / 删除在 standalone 和 PG 模式语义一致 | 已通过 | 本地回归和源码门禁通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖草稿创建、管理端列表 / 详情、更新、发布、公开列表、已读去重、重复发布、归档后重发清理已读、下线隐藏和删除清理 |
| PG repository | API Key / 账户时间计划与授权过期后台一致性 | `pnpm --filter juhe-ai-backend test:schedule-authorization-postgres-smoke` | 时间计划边界同步和授权到期扫描在 PG 模式下不回退 SQLite，重复边界事件幂等，到期 grant 能同步运行态授权 | 已通过 | 2026-06-28 `<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖 API Key / 账户时间计划激活、重复 event 跳过、changedIds 返回、授权 grant 到期、运行态授权同步为 `expired`，并验证授权到期扫描 explain 命中 `idx_resource_authorization_grants_expiry_sweep` 且无 Seq Scan |
| PG repository | 表监控管理端快照读取 | `pnpm --filter juhe-ai-backend test:table-monitor-default-scan`、`test:table-monitor-dbstat-unavailable`、`test:table-monitor-postgres-smoke` | 表监控 overview、单表 history 和 database-history 在 PG 模式下读取 `juhe_stats` 快照表，不回退同步 SQLite；查询计划命中快照索引 | 已通过 | 本地 SQLite 采样 / dbstat 不可用回归通过；2026-06-28 `<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖 overview、单表 history、三库 database-history PG async 读取，并验证快照查询 explain `explainIndexed=true` |
| PG repository / ops-worker | 过期逻辑删除账户 PG 清理与调度 | `pnpm --filter juhe-ai-backend test:deleted-account-related-cleanup`、`test:expired-deleted-account-cleanup-postgres-smoke`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role` | DB service 在 PG 模式下通过 async repository 清理过期逻辑删除账户；ops-worker 高性能模式恢复已迁移的删除清理调度；SQLite 同步清理语义不变 | 已通过 | 本地类型检查、删除账户关联清理回归、PG 高数据量调度守卫和后台 worker 角色守卫通过；2026-06-28 `<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过，覆盖无历史阻塞账号的逻辑删除老化、DB service PG async 清理、账号物理删除、分组绑定删除和账号子表清理。PG 历史明细 / 统计清理仍返回 deferred，待单独 worker 设计 |
| PG repository / ops-worker | 账号健康检测 / 冷却复测 PG 探测数据库事实链路 | `pnpm --filter juhe-ai-backend test:account-probe-postgres-smoke`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role`、`test:cooldown-retest-recovery`、`test:background-db-service-write-boundary`、`test:postgres-schema-sql` | 健康检测和冷却复测候选读取 / 单账号读取 / 状态写回在 PG 模式下不回退 SQLite；ops-worker 高性能模式恢复健康检测、账号冷却复测和账户内 API Key 冷却复测调度；候选查询命中 profile 前导 due 索引 | 已通过 | 本地类型检查、SQLite 高数据量守卫、worker 角色、冷却复测恢复、DB service 写边界和 PG schema SQL 回归通过；2026-06-28 远端 PostgreSQL / Redis URL 下 `postgres:init-schema-only` 输出 `schemaStatements=596`，`test:account-probe-postgres-smoke` 通过，覆盖健康检测 success/failure、冷却复测 failure backoff 和 explain `explainIndexed=true`。真实上游 HTTP 探测不在该 smoke 内执行 |
| PG repository / ops-worker | 代理延迟刷新 PG 调度与候选索引 | `pnpm --filter juhe-ai-backend test:proxy-postgres-smoke`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role`、`test:proxy-single-read`、`test:postgres-schema-sql` | 代理延迟刷新在 PG 高性能 ops-worker 下恢复调度；启用代理候选读取和检测状态写回不回退 SQLite；候选查询命中最久未检测窗口索引 | 已通过 | 本地类型检查、schema SQL、SQLite 高数据量守卫、worker 角色和代理单条读取回归通过；2026-06-28 远端 PostgreSQL / Redis URL 下 `postgres:init-schema-only` 输出 `schemaStatements=597`，`test:proxy-postgres-smoke` 通过，覆盖代理 CRUD、测试配置解密、检测状态写回、启用代理候选读取和 `idx_proxy_profiles_latency_refresh_due` explain |
| PG repository / ops-worker | OpenAI OAuth access token 自动刷新 PG 调度 | `pnpm --filter juhe-ai-backend test:openai-oauth-access-token-refresh`、`test:openai-oauth-access-token-refresh-postgres-smoke`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role`、`test:postgres-schema-sql` | OAuth 自动刷新 due 候选、设置读取、凭据写回、连续失败异常标记和失败恢复在 PG 模式下不回退 SQLite；ops-worker 高性能模式恢复自动刷新调度 | 已通过 | 本地类型检查、OAuth SQLite 回归、schema SQL、SQLite 高数据量守卫和 worker 角色回归通过；2026-06-28 远端 PostgreSQL / Redis URL 下 `postgres:init-schema-only` 输出 `schemaStatements=598`，`test:openai-oauth-access-token-refresh-postgres-smoke` 通过，覆盖测试替身 token 成功刷新、连续失败 3 次标记异常、错误脱敏和 `idx_accounts_openai_oauth_refresh_pg_due` explain。真实 OpenAI token endpoint 不在该 smoke 内执行 |
| System API | performance 最小 HTTP 烟测 | `pnpm --filter juhe-ai-backend test:performance-system-api-smoke` | public settings、captcha、login、me、settings GET/PATCH、system-accounts GET/POST/PATCH、providers、provider options、provider models GET/POST/PATCH/DELETE、groups GET/POST/PATCH/DELETE、authorization-options GET、authorizations GET、my-authorizations GET、authorizations `POST` / `GET /:id/usage` / `PATCH /:id` / `PATCH /:id/expire` / `DELETE /:id`、accounts POST/GET 列表/详情/options/PATCH/clearFailureState/DELETE、授权实例 `/authorized-dispatch` clearFailureState、api-keys GET/POST/PATCH/secret/refresh-key/DELETE、混合路由 API Key 创建 / 更新 / 删除、logout 链路在 SQLite 和 PG/Redis 下可用 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；只覆盖登录前后、设置管理、系统账户管理、分组管理、自定义模型管理、授权列表 / options / usage 详情、授权创建、现有授权更新 / 有效期和额度更新 / 回收、AI 账户创建 / 普通 owner 列表 / 详情 / options / 常规更新 / 异常状态恢复 / 删除、授权实例详情读取 / 异常状态恢复、API Key 管理和混合路由模型目录校验最小链路，不代表完整管理端 PG-ready |
| PG repository | 授权个人归还双 driver | `pnpm --filter juhe-ai-backend test:authorization-return-driver` | 账户授权实例归还、授权列表个人归还和分组个人归还后，grant 为 `returned`，manual source 撤销，runtime authorization 为 `returned`，重复归还返回空结果 | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；只覆盖个人归还仓储，团队授权归还仍待完整场景回归 |
| PG repository | 其他 CRUD / upsert / 分页 / 事务 | 双 driver 回归脚本 | SQLite 和 PostgreSQL 语义一致 | 未执行 | 完整授权实例列表视图、统计等 repository 待迁移；团队授权创建 fanout 已按同步逻辑迁移，仍建议补独立回归 |
| Redis cache | memory / Redis 缓存一致 | `pnpm --filter juhe-ai-backend test:runtime-state-cache-driver`、`test:runtime-state-cache-redis` | TTL、delete、clear、版本失效一致 | 已通过 | memory driver 已通过；2026-06-27 `<测试主机IP>` 真实 redis-cache 下 `SharedJsonCache` TTL / delete / clear 通过。注意现有生产 `createAppCache` 调用点仍主要是进程内 LRU，迁移到 `SharedJsonCache` 前不等于全量共享缓存 |
| Redis cache | API Key 校验 / API Key 额度 / 授权额度 / 网关设置 / 网关分组访问 / 网关模型目录 / 模型路由索引 / 管理端模型目录 / 响应检查策略 / 客户端 IP 封禁策略快照 / 混合路由评分结果共享缓存门禁 | `pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-index`、`test:gateway-runtime-cache`、`test:api-key-group-route-cache`、`test:api-key-group-route-capability`、`test:client-ip-policy-shared-cache-redis`、`test:hybrid-scoring-shared-cache-redis` | 异步判定、网关设置、模型 / 策略读取、模型路由索引、IP 封禁策略快照和混合路由评分结果复用优先读取 Redis shared cache，写入同步更新 shared cache，全量失效清 shared cache，反向索引清理保持常量级 | 已通过 | 2026-06-27 已补源码门禁；2026-06-28 补客户端 IP 封禁策略快照 Redis 冒烟、混合路由评分 Redis 变体、网关设置和模型路由索引 shared cache；本地 LRU 仍保留为进程内热缓存，Redis shared cache 承接跨进程共享；API Key 校验、网关设置、分组访问、模型目录、模型路由索引、响应检查策略、IP 封禁策略快照和混合路由评分缓存不保存上游账号密钥 |
| Redis cache | 授权读资源 lookup 共享缓存门禁 | `pnpm --filter juhe-ai-backend test:authorization-list-team-usage-batch` | 授权列表和授权用量读路径复用统一 `repository-lookups` async helper，资源名称 lookup 先读本地 LRU，再读 Redis shared cache，未命中才读 PG | 已通过 | 2026-06-27 已补源码门禁；覆盖系统账户、AI 账户、分组、API Key、团队和团队成员名称 lookup，不保存上游账号 credentials；`test:performance-system-api-smoke` 已在远端 PG/Redis 下复跑通过 |
| Redis cache | 系统设置 / 全局设置共享缓存门禁 | `pnpm --filter juhe-ai-backend test:settings-management-driver`、`test:settings-public-driver` | async 设置读取优先读取 Redis shared cache，更新后绕过旧共享缓存从数据库重读，全量失效清 shared cache | 已通过 | 本地 SQLite 通过；`<测试主机IP>` 远端 PostgreSQL / Redis URL 下通过；PG 子进程按可变配置校验字段类型和公开边界 |
| Redis state | 原子计数 / 锁 / 并发占用 / 缓存失效版本 / 验证码 challenge | `pnpm --filter juhe-ai-backend test:runtime-state-cache-driver`、`test:runtime-state-cache-redis`、`test:auth-memory-maintenance-boundary`、`test:account-concurrency-lane-index`、`test:gateway-concurrency-preparation`、`test:gateway-cache-invalidation-runtime-state` | 不突破上限，不误释放锁，跨进程失效版本能触发本地 handler，一次性 challenge 不能重复消费 | 已通过 | memory / 默认链路通过；网关缓存失效 runtime state 源码边界通过，`<测试主机IP>` redis-state 已通过登录失败锁定、`RuntimeStateStore` `incr` / lock、账号并发 Redis Lua 并发争抢和跨进程版本同步烟测；验证码 challenge / 发放限频已补 runtime state 源码门禁，并已用远端 redis-cache / redis-state 复跑 `test:runtime-state-cache-redis` 通过，用时 665ms |
| DB service | SQLite standalone 异步 dispatch 路径 | `pnpm --filter juhe-ai-backend test:db-service-sqlite-dispatch-standalone` | SQLite standalone 模式下全局设置、网关设置、分组访问、候选账号、模型目录等读操作不返回 `undefined` | 已通过 | 2026-06-27 修复非 PG 分支 `break` 导致的 standalone 路径丢失；新增临时 SQLite 真库回归 |
| 队列并发 | 最大消费并发 100 | `pnpm --filter juhe-ai-backend test:usage-record-writer-pool`、`test:usage-record-byte-batch`、`test:redis-stream-queue`、`test:account-test-task-boundary` | 入队即消费，连接池不爆，队列指标可观测 | 部分通过 | 使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引 Redis Streams 已落地；账号测试状态表、状态变更操作日志和账户内 API Key runtime state 已补 PG async，执行缓冲持久化待按真实证据单独评估 |
| Redis Streams 队列 | 使用记录 / 审计日志 / 操作日志 / 公开接口日志 / 数据维护 / 运行日志索引高性能队列缓冲 | `pnpm --filter juhe-ai-backend test:redis-stream-queue`、`test:record-maintenance-redis-stream-smoke`、`test:runtime-log-redis-stream-smoke`、`test:operation-log-queue`、`test:public-api-logs`、`test:public-api-log-db-service-ipc`、`test:audit-log-settings-contract`、`test:performance-reliability-drill` | performance 默认 `redis_stream`，非 ingest 写入 Stream，consumer 成功落库后 ack，失败保持 pending，Redis 重启后 pending 可接管，压测同时检查 pending 与 consumer group lag | 已通过 | 本地回归覆盖 Stream 解析、配置、usage / audit / operation / public API / record maintenance / runtime log index 源码边界、审计采样契约；`<测试主机IP>` redis-state 已通过 record maintenance 和 runtime log 真实 Stream 烟测；最新真实网关压测下 usage / audit / operation / public API / record maintenance / runtime log pending 和 lag 均为 0；2026-06-27 可靠性演练中 Redis state 重启前 3 条 pending，重启后全部接管并 ACK |
| 账户内 API Key runtime state | 多 API Key 故障隔离状态读写 | `pnpm --filter juhe-ai-backend test:account-api-key-failure-guard`、`test:account-api-key-runtime-state-postgres-smoke`、`test:background-db-service-write-boundary` | PG 模式下 failure / success 写回不回退 SQLite，冷却复测候选查询走索引，写回后触发分组账号统计 dirty 和网关缓存失效 | 已通过 | 真实 PG/Redis smoke 覆盖 failure 写入、due-for-probe 候选读取、success 恢复、dirty 标记和 `idx_account_api_key_runtime_probe` explain；本地回归固定 DB service 写边界和短避让语义 |
| PG stats | usage 派生统计 PG 聚合 | `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness` | `aggregateUsageStatsBatchAsync` 在 PG 下写核心 usage stats，同时写 usage model / error / latency 和 account quality 派生表 | 已通过 | 2026-06-27 已补批量 PG upsert；远端 PG/Redis readiness 全绿：`usage_records_write` 36.84ms、`audit_logs_write` 40.40ms、`operation_logs_write` 61.41ms、`public_api_logs_write` 11.67ms、`usage_stats_aggregate` 173.05ms。报告文件为 `reports/performance-gateway-persistence-readiness-20260626T211555Z.json` |
| PG stats | 用量概览窗口 PG 刷新 / 读路径 | `pnpm --filter juhe-ai-backend test:usage-overview-postgres-smoke`、`test:usage-overview-window-atomic`、`test:usage-rank-staged-refresh`、`test:background-worker-performance` | PG 模式下刷新 `usage_overview_summary_windows` / `usage_overview_trend_windows` / `usage_model_rank_windows` / `usage_error_rank_windows`，统计首页读取 async PG 窗口结果 | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过：写入最小 usage stats 源数据，刷新 `usage_overview_windows` 后读取 requestCount=7、趋势桶 1、模型排行 1、错误排行 1，并验证 source watermark 不变时 skipped=true；SQLite 原子发布、staged refresh 和后台性能回归均通过 |
| PG stats | 用量范围窗口 PG 刷新 / 读路径 | `pnpm --filter juhe-ai-backend test:usage-scope-range-postgres-smoke`、`test:usage-rank-staged-refresh`、`test:background-worker-performance` | PG 模式下按 `(start_date,end_date)` 小事务刷新 `usage_scope_range_windows`，账号 / 分组 / API Key 等范围用量摘要读取 async PG 窗口结果 | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过：写入 1 条 `usage_stats_daily` 源数据，刷新 `usage_scope_range_windows` 后窗口表返回 requestCount=11、activeDays=1、totalCost=0.321，`loadUsageRangeSummaryForScopeAsync` 读回一致结果，并验证 source watermark 不变时 skipped=true；本地 staged refresh 和后台性能回归均通过 |
| PG stats | 授权范围窗口 PG 刷新 / 读路径 | `pnpm --filter juhe-ai-backend test:authorization-usage-range-postgres-smoke`、`test:authorization-usage-pagination`、`test:usage-rank-staged-refresh`、`test:background-worker-performance` | PG 模式下按 `(start_date,end_date)` 小事务刷新 `authorization_team_usage_range_windows` / `authorization_user_usage_range_windows`，授权团队 / 用户消耗明细读取 async PG 窗口结果 | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过：写入最小 lookup 和授权 daily 源数据，刷新 `authorization_usage_range_windows` 后团队 summary requestCount=13、用户 summary requestCount=17，团队 / 用户明细各 1 行，并验证 source watermark 不变时 skipped=true；本地授权分页、staged refresh 和后台性能回归均通过 |
| PG stats | TopN 排行快照与 AI 性能窗口 PG 刷新 | `pnpm --filter juhe-ai-backend test:usage-rank-snapshots-postgres-smoke`、`test:usage-rank-staged-refresh`、`test:background-worker-performance` | PG 模式下刷新 account / caller_account last7d 请求排行、API Key / account authorization / group authorization current month 成本排行，并同步刷新 `ai_performance_summary_windows` | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过 5 个 TopN stage；2026-07-01 已补齐 `ai_performance_summary_windows` PG staged refresh，并在 Mac PostgreSQL / Redis 环境复跑 smoke 通过，执行 6 个 stage 且第二轮 source watermark 不变时 skipped=true；本地 staged refresh 和 typecheck 通过 |
| PG stats | AI 性能监控 async 读路径 | `pnpm --filter juhe-ai-backend test:ai-performance-account-options-query-guard`、`test:ai-performance-hourly-query-guard`、`test:ai-performance-postgres-smoke` | PG 模式下 `/ai-performance` 和 `/ai-performance/accounts` 不回退 SQLite，读取 TopN、hourly 和 summary 预聚合窗口 | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过：创建临时系统账户、分组和账号，写入最小 `usage_rank_snapshots` / `usage_stats_hourly` / `ai_performance_summary_windows`，验证账号名前缀搜索、显式选中回填、summary 和小时趋势；本地 query guard 固定账号选项和小时趋势查询边界 |
| PG stats | 账号用量统计 async 读路径 | `pnpm --filter juhe-ai-backend test:account-usage-query-guard`、`test:account-usage-postgres-smoke` | PG 模式下 `/account-usage` 不回退 SQLite，读取 `usage_scope_range_windows`、`usage_stats_daily`、`usage_rank_snapshots` 和业务账号元数据 | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过：创建临时系统账户、分组和两个账号，写入 caller_account / system_account 范围窗口、daily 和 TopN rank，验证默认排序、summary、默认趋势账号、账号名前缀搜索、日趋势和显式选中回填；本地 query guard 固定关键词预解析、手动选中补入和窗口排序索引边界 |
| PG stats | 客户端 IP 统计 / 封禁策略 PG 聚合、窗口和读写路径 | `pnpm --filter juhe-ai-backend test:client-ip-stats-postgres-smoke`、`test:client-ip-stats`、`test:client-ip-stats-shard-fanout-boundary`、`test:client-ip-policy-hit-buffer`、`test:client-ip-policy-shared-cache-redis` | PG 模式下从 `juhe_usage.usage_records` 聚合到 `juhe_stats.client_ip_*` daily / range window，管理端 IP 统计、公开 IP 用量接口和 IP 封禁策略读写读取 async PG 结果；IP 封禁策略快照通过 Redis shared cache 支持跨节点重载；搜索 keyword 使用 BTree 前缀范围条件并纳入 PG explain 门禁；封禁 / 解封操作日志不回退同步设置读取 | 已通过 | 2026-06-28 远端 PG/Redis smoke 通过：写入 3 条 usage record，PG 聚合消费 3 条，窗口刷新后列表返回 2 个 IP 汇总，详情返回 1 个账号窗口，并覆盖封禁策略创建、active 列表 / hash 精确读取、命中计数写入 3 次和解封；同一 smoke 对客户端 IP 搜索前缀、列表排序、详情排序和封禁筛选执行 PG explain，输出 `explainIndexed=true`；SQLite IP 统计、shard fan-out 边界、封禁命中缓冲、async 操作日志源码门禁和真实 Redis 快照回归均通过 |
| PG quota | API Key / 授权 quota snapshot | `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`、`test:gateway-quota-passive-snapshot` | PG 聚合后刷新 `usage_quota_hourly_windows` 并构建网关 quota snapshot，server 仍只读被动快照 | 已通过 | 2026-06-27 已补 `refreshUsageQuotaHourlyWindowsCacheAsync` 和 `buildGatewayQuotaSnapshotAsync`；远端 PG/Redis readiness 覆盖 API Key、直接账户 / 分组授权和团队账户 / 分组授权 quota snapshot，最新 `gateway_quota_snapshot_pg` 97.05ms。报告文件为 `reports/performance-gateway-persistence-readiness-20260626T225814Z.json` |
| PG audit read | 审计日志列表 / 错误组 / 详情 / payload 窗口 | `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`、`test:log-list-query-guard`、`test:log-list-page-window` | performance 模式审计日志路由不回退 SQLite，同步保留结构化筛选和固定页窗 | 已通过 | 2026-06-27 已补 `listAuditLogsAsync`、`listAuditLogsByIdsAsync`、`listAuditErrorGroupsAsync`、`listAuditErrorGroupEventsAsync`、`getAuditLogDetailAsync` 和 PG payload blob 窗口读取；远端 PG/Redis readiness 覆盖写入后读取列表、错误组、详情和 payload，最新 `audit_logs_write` 140.46ms。报告文件为 `reports/performance-gateway-persistence-readiness-20260626T225814Z.json` |
| PG operation read | 操作日志列表 / 搜索 / viewer / 详情 | `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`、`test:log-list-query-guard`、`test:log-list-page-window` | performance 模式操作日志路由不回退 SQLite，summary 搜索继续走倒排词项，viewer 可见性和详情裁剪保持一致 | 已通过 | 2026-06-27 已补 `listOperationLogsAsync`、`listOperationLogsForViewerAsync`、`getOperationLogDetailAsync` 和 `getOperationLogDetailForViewerAsync`；远端 PG/Redis readiness 覆盖 trace 列表、summary 搜索、viewer 搜索列表、管理员详情和 viewer full 详情读取，最新 `operation_logs_write` 191.00ms。报告文件为 `reports/performance-gateway-persistence-readiness-20260626T225814Z.json` |
| PG public API read | 公开接口日志列表 / 详情 | `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`、`test:public-api-logs` | performance 模式公开接口日志路由不回退 SQLite，列表保留 trace/path/result/status/client IP 结构化筛选，详情读取有界 request/response 快照 | 已通过 | 2026-06-27 已补 `listPublicApiLogsAsync` 和 `getPublicApiLogDetailAsync`；远端 PG/Redis readiness 覆盖 trace/path/result 列表和详情 request/response 读取，最新 `public_api_logs_write` 26.61ms。报告文件为 `reports/performance-gateway-persistence-readiness-20260626T225814Z.json` |
| PG runtime read | 运行日志列表 / 搜索 / 详情 | `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`、`test:runtime-log-search-guard`、`test:log-list-page-window` | performance 模式运行日志 DB service 读路径不回退 SQLite，keyword 仍只搜索 message，详情按 ID 窗口读取 raw_json | 已通过 | 2026-06-27 已补 `listRuntimeLogsAsync`、`getRuntimeLogDetailAsync` 和 `getRuntimeLogFacetsAsync`；远端 PG/Redis readiness 覆盖 trace 列表、keyword 搜索和详情 raw_json 读取，最新 `runtime_logs_read` 14.87ms。报告文件为 `reports/performance-gateway-persistence-readiness-20260626T225814Z.json` |
| PG usage read | 使用记录列表 / 详情 PG async 读路径 | `pnpm --filter juhe-ai-backend test:usage-record-list-postgres-smoke`、`test:usage-record-list-query-guard` | performance 模式使用记录管理页不回退 SQLite；列表先读 `usage_record_shard_entries` 1001 行以内 catalog 窗口，再回 `usage_records` 补详情；trace / client IP 前缀、model 筛选和耗时排序有真实 PG explain 门禁 | 已通过 | 2026-07-03 远端 PG/Redis smoke 通过：写入 5 条 usage record，确认主表和 catalog 各 5 条，trace 前缀列表 5 条、model+failed+duration 1 条、client IP 前缀 2 条、详情读取正确，并覆盖音频 token、输出图片张数、异步自定义模型补价和列表 / 详情成本拆解，PG explain 输出 `explainIndexed=true`；本地 `typecheck`、`test:usage-record-list-query-guard` 和 `test:usage-record-list-catalog-window` 通过 |
| System API 压测 | PG/Redis 管理链路 100 并发 | `pnpm --filter juhe-ai-backend test:performance-system-api-load` | HTTP 0 错误，deadlocks delta 0，无大事务 / 长阻塞，压测夹具清理干净 | 已通过 | `<测试主机IP>` 远端 PG/Redis 下 100 并发、25 个账号分摊写入、15% 写请求通过：最终 20373 请求全 200，端到端脚本 RPS 455.09，总体 p95 335.60ms，PATCH p95 451.17ms，deadlocks delta 0，最大事务 0.38s，最大 active query 0.35s；详见 `docs/reports/高性能模式压测报告-2026-06-26.md` |
| 网关压测 | PG/Redis 网关链路 100 并发复测 | `pnpm --filter juhe-ai-backend test:performance-gateway-load` | Chat / Responses 全成功，Redis Stream backlog 0，deadlocks delta 0，无长期阻塞 | 已通过 | 2026-06-27 `<测试主机IP>` 远端 PG/Redis 下补充 runtime log Redis Stream 和第六条 Stream backlog 门禁后，100 并发、20 秒、64 个账号、60 秒 settle 通过：10269 请求全成功，p95 222.48ms，p99 301.48ms，deadlocks delta 0，最大 lock waiters 0，usage / audit / operation / public API / record maintenance / runtime log pending 和 lag 均为 0，total backlog 0。最新报告文件为 `reports/performance-gateway-load-20260627-runtime-log-stream-100c.json` |
| 网关回归 | API Key 校验与调度 | `pnpm --filter juhe-ai-backend test:account-api-key-gateway-mock-ai`、`test:gateway-runtime-cache`、`test:gateway-cache-invalidation-index` | 真实 `/v1/chat/completions` 入口校验 API Key、按策略路由读取分组和候选账号，账号内 Key 轮询 / 权重 / 失败切换、授权实例、超级优先、故障回退和流量迁移覆盖；缓存 miss / hit / 失效均正确 | 已通过 | 2026-06-28 本地 mock 上游回归通过：单账户多 API Key 轮询、权重、OpenAI-compatible Key 池、坏 Key 短避让、授权来源 Key runtime、全部坏 Key 逐步摘除、超级优先、故障回退和流量迁移覆盖均符合预期；同时复跑网关运行态缓存和缓存定点失效门禁 |
| 使用记录 | 高并发 usage 写入 | `pnpm --filter juhe-ai-backend test:performance-gateway-load` | 明细无丢失，Redis Stream 消化完成，无死锁 / 长阻塞 | 已通过 | 2026-06-26 远端 PG/Redis 100 并发、20 秒、24 个账号通过：11192 请求全成功，usage records 12493，Redis pending 0，deadlocks delta 0，最大事务 1.85s，报告 `reports/performance-gateway-load-final-100c-20s.json` |
| 网关压测 | 真实 `/v1/responses` / `/v1/chat/completions` | `pnpm --filter juhe-ai-backend test:performance-gateway-load` | p95 可接受，usage / audit / operation / public API / record maintenance / runtime log 队列跟上，PG 无 deadlock、无长期锁等待 | 已通过 | 最新 100 并发 p95 229.37ms，p99 295.28ms，audit 215，高于 2% 抽样预期下限 145；`pg_stat_statements` 慢项仍集中在 usage catalog / shard entries 批量 INSERT，未观察到网关热读慢 SELECT |
| 部署验证 | Docker 测试栈 | `<测试主机IP>` compose 启动 / config 校验 | PG / PgBouncer / Redis / 应用健康，当前仓库 compose 可解析 | 部分通过 | PostgreSQL / PgBouncer / Redis cache / Redis state 已在 `<远端Docker目录>` 启动并 healthy；PG schema 和默认种子已初始化；当前仓库 standalone / performance compose 已在远端 `/tmp` 临时目录通过 `docker compose config --quiet`，临时目录已删除；本机连接远端验证需使用宿主机发布端口：PgBouncer `6432`、redis-cache `6379`、redis-state `6380`；应用容器 performance 模式和生产规格维护窗口演练仍待完成 |
| 故障演练 | Redis / PG 重启 | `pnpm --filter juhe-ai-backend test:performance-reliability-drill` | 可读错误、自动重连、Redis Streams pending 可恢复 | 已通过 | 2026-06-27 远端 Docker 栈通过：Redis state 重启后 PING 约 1.03s 恢复，3 条 pending 全部接管并 ACK；PostgreSQL / PgBouncer 重启后健康检查约 41ms 恢复；演练后四个中间件容器均 healthy |
| 备份恢复 | PostgreSQL 备份恢复 | `pnpm --filter juhe-ai-backend test:performance-reliability-drill` | 业务事实可恢复，统计可重建 | 已通过 | 2026-06-27 使用 remote_docker 模式在 `juhe-ai-postgres` 容器内 `pg_dump`，恢复到临时库并校验 schema 表数量一致：business 47、dataset 19、usage 5、stats 59、codex context 3；临时库和 dump 已清理 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-06-25 | 默认保留 SQLite + memory，显式开启 PG + Redis | 保持轻量部署和已有用户使用习惯 | 新配置必须不破坏默认启动 |
| 2026-06-25 | PostgreSQL 模式仍保留 typed command / 队列语义 | 队列不仅解决 SQLite 写锁，也承接背压、重试、优先级和可观测性 | 只调整消费并发，不把所有调用改成直接写库 |
| 2026-06-25 | PostgreSQL 写队列最大消费并发设为 100 | 用户明确要求高性能模式下入队即可异步消费并发处理 | 必须配套连接池、PgBouncer 和背压指标 |
| 2026-06-25 | Redis cache 与 Redis runtime state 拆分 | cache 可以淘汰，调度运行态和原子计数不能被随机淘汰 | 生产推荐两个 Redis 实例或容器 |
| 2026-06-26 | 暂不引入 RabbitMQ，先用 Redis Streams 承接高性能队列缓冲 | Redis 已是高性能模式必选依赖，先减少新中间件数量；Redis Streams 可以满足使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引队列的持久化缓冲、consumer group、pending 重投和容量控制 | 已覆盖使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引队列；账号测试 / 探测仍按后续迁移计划推进 |
| 2026-06-26 | 网关缓存失效先用 Redis runtime state 版本同步，不引入 Redis pub/sub 常驻连接 | 当前需要的是低频管理写后的跨 server 失效，不需要亚毫秒广播；版本号轮询能复用 runtime state driver，避免新增连接生命周期和订阅重连复杂度 | 写路径发布 gateway runtime / authorization quota / API key quota 版本，server 读取入口按 1 秒节流同步并触发本地 handler；后续如多实例规模继续扩大再评估 pub/sub |
| 2026-06-28 | 验证码 challenge / 发放限频先迁 Redis runtime state | 验证码是短 TTL、无凭据、登录前跨 server 一致性状态，适合 Redis state；校验必须原子消费，避免 get 后 del 的并发重复使用窗口。客户端 IP 并发和错误熔断仍在每个网关请求同步热路径，直接 Redis 化会增加 RTT 并改变排队 / 取消语义，需要真实问题证据后单独设计 | `RuntimeStateStore` 新增 `getDeleteJson`；`auth/captcha` 路由改用 async challenge / 限频 / 校验入口；Redis 模式下 challenge 写入 state 并用 `GETDEL` 一次性消费，发放限频用 `incr` + TTL；standalone 仍复用原内存行为 |
| 2026-06-28 | 测试基线跟随当前模型目录和策略路由契约 | 代理负向回归还硬编码旧 `gpt-4o-mini` 和 API Key `groupBindings`，会在进入代理负向语义前失败；System API smoke 业务链路已通过但 teardown 未清默认 API Key / 策略路由和授权实例派生，容易把测试清理失败误判成业务失败 | `test:proxy-negative` 改用 `DEFAULT_OPENAI_SUPPORTED_MODELS[0]` 并通过策略路由创建 API Key；`test:performance-system-api-smoke` 清理测试系统账号前先清 API Key / route strategy、授权实例账号、authorization sources / runtime / grants 和公告非级联引用；不改变生产删除逻辑 |
| 2026-06-25 | SQLite 到 PostgreSQL 历史数据处理不进入代码库 | 项目默认不做运行时旧结构兼容、双读双写或自动迁移；用户明确不考虑代码内数据 / 结构迁移成本 | 高性能模式只维护当前 PostgreSQL schema 初始化和当前路径验证 |
| 2026-06-25 | PostgreSQL schema 使用 `juhe_*` 独立 schema 并从 SQLite DDL 生成 | 保留当前多库职责边界，同时避免业务代码直接感知 PostgreSQL schema 名 | 初始化脚本负责 SQL 方言转换、外键依赖排序和默认种子写入 |
| 2026-06-25 | 先落地 `DatabaseClient` / `SqlDialect` 基础层，再逐个迁移 repository | 当前同步 repository 数量大，直接批量切 PG 风险高 | `postgres:init-schema` 已接入基础层，provider 只读 repository 已作为第一条 PG 读路径验证，登录 / 会话最小 repository 已作为第一条 PG 认证路径验证，公共设置和系统 API 限流设置已作为登录前读链路验证，系统设置管理读写、系统账户管理读写、分组管理读写、API Key 管理读写与网关 Key 校验入口已作为管理端和网关认证入口验证，后续按模块迁移和验证 |
| 2026-06-26 | 网关运行态接入 PG 最小候选账号读取 | 已补齐账号候选读取依赖的分组访问元数据、候选窗口、账号授权、支持模型、模型映射、代理、API Key 运行态、质量分和响应检查策略 PG 读取，并用本地 SQLite 与远端 PostgreSQL / Redis 回归验证 | `read_gateway_runtime` 在 PG 模式下可读取有效 API Key、绑定分组、网关设置、分组访问元数据和最小可调度账号候选；完整 `/v1` PG-ready 仍需账号管理写路径、模型目录、usage / stats、授权额度和审计链路继续迁移 |
| 2026-06-26 | 账号模型配置附属表先补 async replace 写入 helper | 账号创建 / 更新完整迁移前，需要先解除 `account_supported_models` 和 `account_model_mappings` 的 SQLite 写入依赖 | PG 模式下可通过事务写入候选账号支持模型和模型映射；已纳入 `test:api-key-management-driver` 的本地 SQLite 与远端 PostgreSQL / Redis 验证 |
| 2026-06-26 | 先落地 AI 账户最小创建 repository，不直接切账号管理路由 | 账号 HTTP 路由仍依赖同步 provider / group / list / detail / operation log 周边路径，直接切路由会牵出更大迁移面 | `createAccountAsync` 已支持 PG 事务创建账号、绑定分组、写名称搜索词、标签、支持模型和模型映射；模型目录读取已补 async PG 路径，当前回归创建时直接携带模型配置 |
| 2026-06-26 | AI 账户详情先支持普通 owner 回看，不一次性迁移列表和授权实例详情 | 管理端创建后的回看是当前 System API smoke 缺口；完整列表和授权视图依赖统计、授权、质量和运行态多张表，迁移面更大 | `findAccountSummaryAsync` / `findAccountForTestAsync` 已支持 PG 读取普通 owner 账号详情、分组绑定、标签、支持模型和模型映射；usage、账号质量、API Key 运行态明细暂不在详情请求里实时聚合 |
| 2026-06-26 | AI 账户列表先支持普通 owner 分页，不做授权实例和精确计数 | 管理端自有账号列表是创建后继续管理的基本链路；授权实例和统计质量维度依赖更多 repository，不能在列表请求里临时扫明细 | `listAccountsPageAsync` 已支持 PG 普通 owner 列表、名称词项搜索、分组 / 标签 / 状态 / 调度状态过滤和基础排序；`test:performance-system-api-smoke` 已覆盖按 keyword + groupId 查回新建账号 |
| 2026-06-26 | AI 账户 options 先支持普通 owner 轻量下拉 | 账号选择器只需要轻量字段，适合先解除管理端常用筛选入口对 SQLite 同步仓储的依赖；授权实例 options 仍涉及授权视图边界 | `listAccountOptionsAsync` 已支持 PG 普通 owner ID、名称、供应商、协议档案、分组、标签、类型、状态和调度状态过滤；`test:account-options-lightweight` 与 `test:performance-system-api-smoke` 已覆盖 |
| 2026-06-26 | AI 账户更新先支持普通 owner 常规字段和分组重绑 | 更新路径需要复用创建时的模型、标签、代理、时间计划和名称搜索校验；授权实例运行态 mutation 单独迁移更稳 | `updateAccountAsync` / `setAccountGroupAsync` 已支持 PG 普通 owner 常规 PATCH 和分组绑定更新；`test:performance-system-api-smoke` 已覆盖 |
| 2026-06-26 | AI 账户异常恢复分两层支持 owner 和授权实例 | 普通 owner 恢复只需要更新账号失败、冷却和 stream failure 字段；授权实例恢复必须校验被授权账号、目标分组和运行态授权绑定上下文 | `clearAccountFailureStateAsync` 已支持 PG 普通 owner `clearFailureState`；`updateAuthorizedAccountBindingDispatchAsync` 已支持 PG 授权实例 `/authorized-dispatch` 异常恢复；`test:performance-system-api-smoke` 已覆盖 SQLite 与远端 PG/Redis |
| 2026-06-26 | AI 账户删除先支持普通 owner 逻辑删除 | 删除会影响源账号、授权实例、授权状态、名称搜索和缓存失效，适合先迁移业务库内一致性；dataset / stats 历史清理目标后续随清理仓储迁移 | `deleteAccountWithRelatedCleanupAsync` 已支持 PG 撤销账号资源授权、逻辑删除源账号和授权实例、清理标签 / 搜索索引并失效网关与授权运行态缓存；`test:performance-system-api-smoke` 已覆盖 DELETE 后详情 404 |
| 2026-06-26 | 授权个人归还先补仓储级 PG 事务路径 | HTTP 完整授权管理仍依赖大量同步授权仓储；个人归还自身可以先按最小事务闭环解除 SQLite 写入依赖 | `returnAccountAuthorizationInstanceForGranteeAsync`、`returnResourceAuthorizationForGranteeAsync` 和 `returnGroupAuthorizationForGranteeAsync` 已支持 PG 更新 direct grant、manual source 和 runtime authorization；账户 / 授权列表 / 分组归还路由已切 async；`test:authorization-return-driver` 已覆盖本地 SQLite 和远端 PG |
| 2026-06-26 | 授权列表 / options 先切 PG async 读路径 | 管理端需要先解除授权列表和授权候选项对 SQLite 同步读库的依赖；授权创建涉及 runtime authorization、source 刷新、额度窗口、授权实例绑定和缓存失效，适合单独迁移 | `listResourceAuthorizationsPageAsync`、`usageStatsTimezoneAsync` 和授权 grantee options 已支持 PG；`/__aisys__/api/authorizations`、`/__aisys__/api/my-authorizations`、`/__aisys__/api/authorization-options/*` 已纳入 `test:performance-system-api-smoke` 本地 SQLite 与远端 PostgreSQL / Redis 验证 |
| 2026-06-26 | 授权 usage 详情切 PG async 读路径 | 详情只需要读取授权业务表、运行态授权和预聚合 usage 窗口，适合先解除管理端详情查看对 SQLite 同步仓储的依赖 | `getResourceAuthorizationUsageAsync` 和 `GET /__aisys__/api/authorizations/:id/usage` 已支持 PG 读取个人 / 团队授权范围窗口；`test:authorization-usage-pagination`、`test:base-list-page-window` 和 `test:performance-system-api-smoke` 已覆盖 |
| 2026-06-26 | 授权创建切 PG async 写路径 | 个人账号授权创建必须同时写 grant、runtime authorization、source、额度窗口、授权实例账号和目标分组绑定；团队授权创建还要 fanout 到成员运行态授权 | `createResourceAuthorizationAsync` 和 `/authorizations` POST 已支持 PG；`test:performance-system-api-smoke` 已覆盖个人账号授权创建 / 回收的本地 SQLite 与远端 PostgreSQL / Redis HTTP 链路，团队 fanout 仍建议补独立回归 |
| 2026-06-26 | 授权更新 / 回收支持已有 grant 的 PG async 写路径 | 已有授权的暂停 / 恢复 / 有效期 / 额度 / 回收需要闭环 grant、runtime authorization、source、额度窗口和缓存失效 | `updateResourceAuthorizationAsync`、`revokeResourceAuthorizationAsync` 和 `/authorizations/:id` PATCH / expire / DELETE 已支持 PG；`test:performance-system-api-smoke` 已覆盖本地 SQLite 与远端 PostgreSQL / Redis |
| 2026-06-26 | 自定义模型管理 CRUD 切到 async 仓储和路由 | API Key 混合路由、账号模型配置和管理端模型维护都依赖同一模型目录，继续同步 SQLite 路由会阻塞 performance 管理链路 | `custom_provider_models` 已支持 PG find / upsert / delete / 绑定计数，`/providers/models/options` 和 `/providers/:code/models` 已切 async；`test:model-catalog` 覆盖 SQLite 详细契约，`test:performance-system-api-smoke` 覆盖本地 SQLite 与远端 PostgreSQL / Redis HTTP CRUD |
| 2026-06-26 | System API DB 在途请求上限改为可配置并按模式默认 | 64 的管理端 DB 在途请求保护会在 100 并发高性能压测中提前返回 `system_api_busy` 503；但普通 standalone 节点不应默认继承高性能阈值 | 新增 `JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT`，standalone 默认 64、performance 默认 256；`<测试主机IP>` 100 并发压测使用 256 通过 |
| 2026-06-26 | 账号 PATCH 写路径跳过未变化的子写入 | 压测写请求会重复提交相同 `groupId`、`supportedModels` 和 `modelMappings`；继续删重插分组和模型附属表会放大行锁、WAL 和事务耗时 | 路由层跳过相同分组重绑，仓储层跳过相同支持模型 / 模型映射替换，名称未变化时跳过查重；优化后 100 并发压测总请求 15216、0 错误、总体 p95 645.22ms、PATCH p95 765.13ms、deadlocks delta 0 |
| 2026-06-26 | 账号 PATCH 相同分组不再重复做分组校验 | 最新压测慢 SQL 显示相同 `groupId` 仍会触发分组详情校验；这条查询对相同分组 PATCH 没有业务价值 | 保留低风险查询收敛；单独压测未稳定改善 p95，不能把它当作主要性能收益来源 |
| 2026-06-26 | 网关运行态缓存失效按 reason 控制系统设置缓存清理 | `account_updated` 原先会清网关运行态缓存，同时误清 `system_settings` 仓储缓存，导致高并发账号 PATCH 下系统设置白名单查询被反复打回 PostgreSQL | 网关运行态 invalidator 保留 `reason`；只有 `settings_updated` 和无 reason 的兼容清理会清系统设置缓存。最终压测 17967 请求、0 错误、总体 p95 527.59ms、PATCH p95 660.05ms、deadlocks delta 0 |
| 2026-06-26 | 账号 PATCH 相同模型配置短路 provider catalog 校验 | 压测请求会重复提交相同 `supportedModels` / `modelMappings`；虽然已跳过删重插，但仍会反复查询 provider catalog 和跨协议模型池 | 仓储先用本地归一化结果与当前配置比较；完全一致时复用当前模型配置，真实变化仍走原 provider catalog 校验。最终压测 20373 请求、0 错误、总体 p95 335.60ms、PATCH p95 451.17ms、deadlocks delta 0 |
| 2026-06-26 | 本轮停止继续优化账号主表 UPDATE | 最新慢 SQL 已只剩 `UPDATE juhe_business.accounts` 明显突出，继续优化要改变主表 UPDATE 形态、锁行为或字段写入策略；此前动态省略凭据列已经证明这类方向容易负优化 | 本轮停止继续做代码优化，只保留已由压测证明有效的缓存失效粒度和相同模型配置短路；下一阶段如继续，应单独设计运行态字段拆分或写入合并并压测 |
| 2026-06-26 | 动态省略账号凭据列 UPDATE 被压测否决 | 尝试只在凭据变化时写 credential 字段，功能回归可通过，但真实压测变差，说明动态 SQL 形态可能破坏 PostgreSQL statement 复用和执行稳定性 | 该改动已回滚；对比本轮基线，总体 p95 648.22ms 变为 743.47ms，PATCH p95 777.49ms 变为 883.68ms，端到端脚本 RPS 341.17 降到 284.11 |
| 2026-06-26 | 热点账号写入不能无限并发 | PostgreSQL 没有 SQLite 文件锁，但同一账号 PATCH 仍会竞争同一行；探索压测中单账号写入触发 21s 级锁等待和大量 503 | 正式压测改为 25 个账号分摊写入通过；后续队列高频写同一资源时必须做按资源串行、写入合并或 Redis 运行态拆分 |
| 2026-06-26 | 新增网关事实表 PG readiness 门禁 | System API 压测不能证明 `/v1/*` 网关事实表已经 PG-ready，必须单独验证 usage、audit、operation/public logs 和 stats | 新增 `test:performance-gateway-persistence-readiness`；初始远端结果五个入口均缺 PG adapter，本轮迁移后 usage、audit、operation log、public API log 和核心 usage stats 聚合全部通过 |
| 2026-06-26 | usage / audit / operation / public / 核心 stats 先迁 PG，完整网关压测进入下一步 | usage 是网关最高频事实写入，audit 是排障核心；operation/public logs 会影响后台队列，stats 聚合会影响额度和统计读表 | `createUsageRecordsBatchAsync` 已写入 `juhe_usage` 主表、catalog 和账号侧效应；`createAuditLogsBatchAsync` 已写入 audit metadata、attempt、payload ref、payload blob 表、文件和审计错误分组；`createOperationLogsBatchAsync` / `createPublicApiLogsBatchAsync` 已写 PG，operation search terms 改批量插入；`aggregateUsageStatsBatchAsync` 已写核心统计桶 |
| 2026-06-27 | PG usage 派生统计不能只验证函数不报错 | readiness 原先只调用 `aggregateUsageStatsBatchAsync`，无法发现 usage model / error / latency / account quality 漏写；统计页和热点分析依赖这些派生表，不能回扫明细表补口径 | `aggregateUsageStatsBatchAsync` PG 分支补齐 usage model / error / latency / account quality 批量 upsert；readiness 改为写成功+失败两条用量记录并断言派生表行数。远端 PG/Redis 复测通过，`usage_stats_aggregate` 173.05ms |
| 2026-06-27 | performance 模式聚合后也必须推送 quota snapshot | stats-worker 原先在 PG 模式下聚合用量后直接跳过 quota hourly window 和网关额度快照推送，可能导致高性能节点额度判断滞后 | 新增 PG 版 `refreshUsageQuotaHourlyWindowsCacheAsync`、`buildGatewayQuotaSnapshotAsync` 和成本批量读取；stats-worker 在 PG 模式下聚合到用量后刷新 hourly window 并推送快照。readiness 覆盖 API Key、直接账户 / 分组授权和团队账户 / 分组授权 quota snapshot |
| 2026-06-27 | PG 审计写入必须保留错误分组 | 管理端错误分析、排障聚合和保留清理都依赖 `audit_error_groups`，不能只写原始 `audit_logs` | `createAuditLogsBatchAsync` PG 分支新增 `audit_error_groups` 原子 upsert，并在同事务回填 `audit_logs.error_group_id`；readiness 写入失败审计并断言错误分组存在 |
| 2026-06-27 | PG 审计读路径必须跟写入一起迁移 | 只写审计和错误组但列表 / 详情仍走同步 SQLite，会导致 performance 模式管理端审计页 fail-fast | 新增审计日志 async PG 读接口，路由切到 async；readiness 写入后验证列表、按 ID、错误组、事件、详情和 payload 窗口读取 |
| 2026-06-27 | PG 操作日志读路径必须跟写入一起迁移 | 操作日志写入已 PG-ready，但列表、搜索、viewer 可见性和详情仍走同步 SQLite 会导致管理端操作日志页在 performance 模式 fail-fast | 新增操作日志 async PG 读接口，路由切到 async；readiness 写入后验证 trace 列表、summary 搜索、viewer 搜索列表、管理员详情和 viewer full 详情 |
| 2026-06-27 | PG 公开接口日志读路径必须跟写入一起迁移 | 公开接口日志写入已 PG-ready，但管理端列表 / 详情仍同步 SQLite，会导致 performance 模式公开接口日志页 fail-fast | 新增公开接口日志 async PG 读接口，路由切到 async；readiness 写入后验证 trace/path/result 列表和详情 request/response 读取 |
| 2026-06-27 | PG 运行日志读路径先补 list/detail，写入索引单独评估 | 运行日志管理页读取经 DB service 触发同步 SQLite；但写入索引、文件 cursor 和 facet 维护牵涉本地日志流与 worker IPC，不适合和读路径一次性混改 | 新增运行日志 async PG 读接口，DB service 切到 async；readiness 写入临时 PG 行后验证 trace 列表、keyword 搜索和详情 raw_json。运行日志写入索引队列、cursor / facet 维护和尾部压力仍作为后续项 |
| 2026-06-28 | 使用记录管理列表必须补 PG async 读路径和真实 explain | 网关压测证明 usage 写入已 PG-ready，但管理端使用记录列表 / 详情仍调用同步 repository，会在 performance 模式下回退 SQLite 或 fail-fast；本地 SQLite query guard 不能证明真实 PG collation 和索引计划 | 新增 `listUsageRecordsAsync` / `getUsageRecordDetailAsync`，路由改为 async；PG 下继续先读 catalog 窗口再回主表补详情；`test:usage-record-list-postgres-smoke` 覆盖 trace / model / client IP / duration 查询 explain，且把前缀上界从 `prefix + \uffff` 修正为 next-prefix，上界对 `.` / `:` 结尾前缀退一位计算 |
| 2026-06-28 | 账户内 API Key runtime state 必须补 PG async 读写，不先迁探测执行队列 | 冷却复测候选来自 `account_api_key_runtime_states`，状态读写是数据库事实；执行队列只是短窗口去重和并发控制。先修 PG 模式同步 SQLite fail-fast，比直接引入 Redis Streams 更小且收益明确 | 新增 `listAccountApiKeyRuntimeStatesDueForProbeAsync`、`recordAccountApiKeyRuntimeFailureAsync`、`recordAccountApiKeyRuntimeSuccessAsync`；DB service PG 分支调用 async 写回；真实 PG smoke 覆盖 failure / due probe / success / dirty / explain。探测执行缓冲是否持久化继续按真实积压和幂等成本评估 |
| 2026-06-28 | 账号健康检测 / 冷却复测先补 PG DB facts，不先迁探测执行队列 | 健康检测和冷却复测的候选、单账号读取、成功 / 失败写回是数据库事实；执行队列只负责 ops-worker 本地短窗口去重和外部 I/O 并发。没有真实积压证据前迁 Redis Streams 会扩大幂等和取消复杂度 | 新增健康检测 / 冷却复测 PG async repository 和 DB service 分支，新增 `idx_accounts_health_check_pg_due` / `idx_accounts_cooldown_retest_pg_due`，恢复 PG ops-worker 的 `account-health-check`、`cooldown-account-retest` 和 `account-api-key-cooldown-retest` 调度；真实 PG smoke 覆盖候选、写回和 explain |
| 2026-06-28 | 代理延迟刷新可恢复 PG ops-worker 调度，不需要 Redis 化执行缓冲 | 代理候选读取、测试配置解密和检测状态写回已经走 PG async / DB service；周期任务本身只串行处理一批代理网络探测，没有必须进入 Redis Streams 的事实数据 | 恢复 PG ops-worker 的 `proxy-latency-refresh` 调度，新增 `idx_proxy_profiles_latency_refresh_due` 固定启用代理最久未检测候选窗口；真实 PG smoke 覆盖候选、写回和 explain。真实代理网络质量继续由手动检测和运行环境验证 |
| 2026-06-28 | OAuth 自动刷新先补 PG due / 写回链路，不把 OAuth token 放 Redis | 自动刷新事实在 PostgreSQL，执行入口是 ops-worker 周期任务；OAuth access token、refresh token 和代理 URL 都是敏感运行数据，不适合进入 Redis shared cache | 新增 OAuth due PG async 查询、`idx_accounts_openai_oauth_refresh_pg_due`、DB service clear / mark exception PG 分支和测试 requester；恢复 PG ops-worker 的 `openai-oauth-access-token-refresh` 调度。远端 smoke 用测试替身 token endpoint，真实上游 token endpoint 仍按真实账号单独验证 |
| 2026-06-28 | API Key 列表 PG keyword 不能只靠排序索引过滤 | 直接 `ILIKE` 不匹配 `lower(name)` 表达式索引；改成 range 后，真实列表排序仍会让 PG 选择 `idx_api_keys_system_account_default_updated` 后过滤名称，用户量上来后关键词搜索可能成为慢点 | PG keyword 场景改成 `matched_api_key_ids` materialized CTE：先用 `idx_api_keys_system_account_name_lookup` 找匹配 ID，再按原列表排序输出；新增 `test:api-key-list-postgres-smoke` 固定 keyword、分组筛选和默认排序 explain |
| 2026-06-28 | PostgreSQL 前缀查询必须显式固定 C collation | 真实远端 PG explain 发现，默认 collation 下 `prefix + \uffff` 和简单 next-prefix 都可能与预期排序不一致，导致 trace / client IP / API Key 名称前缀查询命不中预期索引或漏匹配 | 使用记录 trace / client IP、API Key 名称 keyword 改为 `COLLATE "C"` + 二进制上界，schema 增加对应 C collation 表达式索引，`test:usage-record-list-postgres-smoke`、`test:api-key-list-postgres-smoke` 和 `test:postgres-schema-sql` 固定真实 explain |
| 2026-06-28 | Redis Stream 压测积压按本轮 delta 判断 | 测试环境 Redis Stream 可能存在历史 pending / backlog，如果直接看 total pending / backlog，会把本轮前遗留消息误判为本轮压测新增积压 | `performance-gateway-load` 报告新增 positive pending / backlog 和 net backlog delta；2026-06-28 网关 128 并发压测中 positivePending=0、positiveBacklog=0、netBacklogDelta=-828，历史积压单独记录为清理项 |
| 2026-06-28 | 收口为统一回归与压测报告 | 按用户要求减少逐点测试节奏，先集中修代码，再统一跑核心回归、PG/Redis smoke、System API 压测和网关压测 | 新增 `docs/reports/高性能模式压测报告-2026-06-28.md`；System API 100 并发 30 秒 15078 请求全成功，p95 466ms；网关 128 并发 30 秒 19360 请求全成功，QPS 415.7，p95 242.91ms；两者 deadlocks delta 均为 0 |
| 2026-06-29 | 移除 SQLite -> PostgreSQL 旧数据处理实现 | 用户明确要求任何情况下不在代码里考虑数据迁移 / 结构迁移成本；旧数据处理实现会把历史成本固化进仓库 | 删除相关实现、package 入口和 smoke；部署 / 设计 / 测试文档改为只说明当前 schema 初始化与当前路径验证 |
| 2026-06-28 | PostgreSQL 默认 seed 必须与 SQLite 默认 seed 共用 provider / profile 列表 | `DEFAULT_BUILT_IN_GROUPS` 已包含 hybrid 默认分组，但 PG seed 漏掉 hybrid provider 和 3 个 hybrid profile 时，System API smoke 在 `system-accounts` POST 阶段触发 `groups_provider_code_fkey`，生产新建系统账户也会失败 | `schema-defaults.ts` 新增共享默认 provider / protocol / endpoint family / profile seed 列表，SQLite 和 PG seed 共用；新增 `test:postgres-seed-defaults` 固定所有默认分组引用。远端执行完整 `postgres:init-schema` 后 hybrid provider=hybrid、hybridProfiles=3，`test:performance-system-api-smoke` 复跑通过 |
| 2026-06-28 | 默认分组唯一约束按 provider protocol profile，不按 provider code | 当前产品模型允许同一 provider 下多个 protocol profile 各有默认分组；provider 级唯一默认分组约束与当前模型冲突 | schema 初始化只生成 `idx_groups_owner_protocol_profile_default_unique`，并用 `test:postgres-schema-sql` 固定不再创建 provider 级默认唯一索引；测试库按当前 schema 重建后验证通过 |
| 2026-06-26 | 高性能模式成功审计先降为 5% 稳定抽样，失败审计保持全量 | 真实网关 100 并发下 10% 成功审计会让 audit payload / blob 写入尾部积压；把 PG flush 批次从 25 提到 50 未提升吞吐，反而把最大事务拉到 3.67s | 该策略已被 2026-06-27 的 2% 抽样取代，历史结果保留用于说明 10% 和大批次不可取 |
| 2026-06-27 | 新增高性能可靠性演练脚本 | 生产切换前必须验证 Redis Streams pending 接管、Redis/PG 重启恢复和 PostgreSQL 备份恢复，不只看吞吐压测 | 新增 `test:performance-reliability-drill`；支持本机 pg_dump/pg_restore 和远端 Docker 容器内备份恢复，支持可选外部重启命令 |
| 2026-06-27 | 新增真实 Redis cache/state driver 回归 | 原 `runtime-state-cache-driver` 只覆盖 memory driver，不能证明 Redis TTL、lock、登录锁和账号并发 Lua 路径可用 | 新增 `test:runtime-state-cache-redis`，已在 `<测试主机IP>` redis-cache / redis-state 通过；同时补充 `closeRedisClients()` 避免测试进程因共享 Redis client 未关闭而卡住 |
| 2026-06-27 | API Key 校验缓存先迁 Redis shared cache，不迁带上游凭据的网关运行态快照 | API Key 校验值不包含上游账号密钥，命中面在每次网关请求前；网关运行态快照含账号 credentials，直接放 Redis 会扩大敏感数据面 | `validateGatewayApiKeyAsync` 先读本地 LRU，再读 Redis `SharedJsonCache`，DB 命中后写双层缓存；按 ID 失效仍保留本地反向索引，Redis 共享命名空间全量换版本；`test:gateway-cache-invalidation-index` 已覆盖源码门禁和本地定点失效行为 |
| 2026-06-27 | 系统设置和全局设置 async 路径迁 Redis shared cache | 设置读取不含上游密钥，System API、公共配置和限流读取频繁，适合先降低多节点重复 PG 查询 | `getSettingsAsync` / `listGlobalSettingsAsync` 先读本地 LRU，再读 Redis `SharedJsonCache`，更新后绕过旧共享缓存从数据库重读；`test:settings-management-driver` 和 `test:settings-public-driver` 已在本地 SQLite 与远端 PG/Redis 下通过 |
| 2026-06-27 | 授权读资源 lookup 迁入 Redis shared cache | 授权列表、授权 usage 详情和授权摘要会反复读取账号、分组、API Key、系统账户和团队名；这些显示型 lookup 不含上游账号 credentials，适合先共享，避免多 server 节点重复打 PG | `repository-lookups` 暴露 async helper，先读本地 LRU，再读 Redis `SharedJsonCache`，未命中才读 PG，并在授权读仓储删除重复私有 helper；`test:authorization-list-team-usage-batch`、`test:authorization-options-query-guard`、`test:authorization-usage-pagination`、`test:resource-authorization-single-read` 和远端 PG/Redis `test:performance-system-api-smoke` 已通过 |
| 2026-06-27 | 本轮不把带账号凭据的网关运行态缓存整体共享到 Redis | 共享运行态快照会把 OpenAI API Key / OAuth 凭据放入 Redis cache，安全边界和失效粒度需要单独设计 | 先只迁不含上游密钥的 API Key 校验、额度和设置缓存；后续若要迁网关运行态，只评估不含 credentials 的子集或加密字段方案 |
| 2026-06-27 | 压测父子进程必须使用同一个 `JUHE_AI_SECRET` | 账号凭据加密密钥不一致时，子进程无法解密压测脚本创建的 mock 上游账号，表现为所有请求 503 “没有可用的上游账户”，但 PG / Redis 本身无锁等待或错误 | `test:performance-gateway-load` 子进程改为继承 `runtimeConfig.secret`；修复后 smoke 和 100 并发复测均能打到 mock upstream |
| 2026-06-27 | PG schema 初始化必须直接包含 usage_records 当前运行态字段 | 缺少 `failure_attribution` 等 usage 运行态字段时，网关事实表 readiness 会在写入或统计聚合阶段失败 | 当前 schema 建表语句必须一次性包含这些字段和协议档案索引；运行时代码不生成补列语句，测试库或开发库按当前 schema 重建 |
| 2026-06-27 | PG schema 初始化必须直接包含 API Key 当前路由策略字段 | `api_keys` 缺 `route_strategy_id` 等路由列时，创建索引和 System API smoke 都会失败 | 当前 schema 建表语句必须一次性包含路由策略字段并先于索引输出；运行时代码不生成补列语句，历史库由上线窗口离线处理或重建 |
| 2026-06-27 | 真实网关压测的 audit 判断要留足异步 settle | 高性能模式成功审计是抽样且异步批量 flush，10 秒 settle 可出现业务全成功但审计样本或 Redis Stream lag 尚未完全消化的误判 | 最新正式复测使用 60 秒 settle，通过：10269 请求全成功、audit 238 高于 2% 抽样预期下限 143，usage / audit / operation / public API / record maintenance / runtime log pending 和 lag 均为 0 |
| 2026-06-27 | 审计日志迁入 Redis Streams，但剩余本地队列不一次性全迁 | 真实网关压测发现只看 usage Stream 会漏掉 audit tail，且审计 payload 具备重启后不丢的价值；operation / public / runtime log 和账号测试 / 探测队列当时未成为当前瓶颈，一次性全迁会扩大幂等和恢复复杂度 | audit queue 已接入 Redis Streams，payload Buffer 通过 base64 标记安全恢复，PG consumer 单批限制为 25，失败不 ACK 保持 pending；operation、public API log、record maintenance 和 runtime log index 已在后续补迁，账号测试 / 探测队列继续记录为后续 P1，按积压证据单独迁移 |
| 2026-06-27 | 操作日志迁入 Redis Streams，并把压测门禁升级为 pending + lag | 操作日志属于管理端审计追溯事实，重启丢缓冲风险高于 public/runtime 普通观测日志；同时 `pending` 只表示已投递未 ACK，不能代表 Stream 尾部没有未读消息 | operation queue 已接入 `juhe-ai:queue:operation-logs` / `juhe-ai:operation-log-writers`；压测脚本通过 `XINFO GROUPS` 采样 usage / audit / operation lag，最终门禁使用 backlog=pending+lag |
| 2026-06-27 | 公开接口日志迁入 Redis Streams | 公开接口日志保存外部接入原文请求 / 响应，与 operation 一样属于可追溯事实；重启丢缓冲风险高于普通运行日志 | public API log queue 已接入 `juhe-ai:queue:public-api-logs` / `juhe-ai:public-api-log-writers`；压测脚本同步采样 public API Stream lag，最终六条 Stream backlog 均为 0 |
| 2026-06-27 | 数据维护队列迁入 Redis Streams | 数据维护任务属于清理和后台修复类副作用，高性能模式下 server / DB service 只靠 IPC 缓冲会在重启时丢任务；任务本身已有类型和执行边界，适合按失败 pending 重投 | record maintenance queue 已接入 `juhe-ai:queue:record-maintenance` / `juhe-ai:record-maintenance-writers`；真实 Redis smoke 验证入队、ACK、pending / lag 归零，最新 100 并发压测六条 Stream backlog 均为 0 |
| 2026-06-27 | 运行日志索引迁入 Redis Streams | 高性能模式下运行日志索引原本主要依赖 IPC 和本地队列，server / DB service 重启时有丢缓冲风险；运行日志写入具备稳定 source ID，适合按 Redis Stream at-least-once + PG `ON CONFLICT` 幂等落库 | runtime log index queue 已接入 `juhe-ai:queue:runtime-log-index` / `juhe-ai:runtime-log-index-writers`；真实 Redis smoke 验证入队、PG 写入、ACK、pending / lag 归零，最新 100 并发压测六条 Stream backlog 均为 0 |
| 2026-06-27 | 审计保持单路小批次，成功抽样降为 2% | 2 路 audit consumer 能让落盘条数更快追上，但真实压测出现 `maxLockWaiters=1`；50 / 75 条批次只能在 pending 和 lag 之间挪动，total backlog 仍在 110 左右 | 最终保留 audit 单路 consumer、25 条批次、`successSampleRate=0.02`、`successHotRetentionHours=0`，失败审计仍全量；最新 100 并发 60 秒 settle 后六条 Stream backlog 均为 0 |
| 2026-06-27 | PG 模式 DB service 网关热读允许并发 dispatch，SQLite 继续串行保护 | 100 并发网关冷启动曾出现 503 “本地数据库服务请求超时”，根因是 PostgreSQL 模式下 `read_gateway_runtime` 等热读仍被单队列串行压住，不是 PG 死锁 | 新增 PG-only 并发白名单，覆盖 API Key 校验、网关设置、分组访问、候选账号、quota 检查、模型目录和策略读取等热读；SQLite standalone 分支行为由 `test:db-service-sqlite-dispatch-standalone` 固定，PG performance 模式未迁移入口必须 fail-fast，不回退 SQLite；源码门禁由 `test:db-service-runtime-metrics` 固定 |
| 2026-06-28 | 客户端 IP 统计和封禁策略必须整体迁移 PG，不能只改列表读 | IP 统计同时服务管理端统计页、公开公益接口、后台派生窗口和网关封禁策略缓存；只把列表读切 PG 会导致 performance 模式聚合仍写 SQLite、窗口仍从 SQLite 刷新，封禁策略和命中计数也会落回 SQLite | 新增 `aggregateClientIpStatsBatchAsync`、`refreshClientIpUsageRangeWindowsAsync`、`listClientIpStatsAsync`、`getClientIpStatsDetailAsync`、IP policy async 读写和公开接口 async 包装；远端 `test:client-ip-stats-postgres-smoke` 验证 PG 聚合、窗口、列表、详情、封禁策略创建 / 读取 / 命中 / 解封闭环 |
| 2026-06-28 | 客户端 IP 统计搜索 / 筛选 / 排序必须有真实 PG explain 门禁 | 功能 smoke 只能证明结果正确，不能证明大数据量下 keyword 搜索、列表排序、详情排序和封禁筛选不会成为长尾短板；`LIKE 'prefix%'` 在 PG 下也不如 BTree 范围谓词稳定 | keyword 搜索改为 `>= prefix AND < nextPrefix` 前缀范围条件；`test:client-ip-stats-postgres-smoke` 新增 PG `EXPLAIN (COSTS OFF)` 断言，覆盖 registry 前缀索引、range request sort 索引、account range request sort 索引和 active policy 索引 |
| 2026-06-28 | 客户端 IP 封禁策略快照使用 Redis shared cache，并由失效路径绕过旧快照回源刷新 | IP 封禁策略快照不含上游凭据，适合放入 Redis shared cache；跨 worker / DB service 重载需要共享一致的 active snapshot，同时失效通知不能继续读取旧 shared cache | Redis cache driver 下 `reloadClientIpPolicyCacheLocal` 可读取 shared snapshot，`replaceClientIpPolicySharedSnapshotAsync` 负责写 Redis shared cache，DB service / background 失效路径使用 `bypassSharedCache` 回源刷新；`test:client-ip-policy-shared-cache-redis` 覆盖旧 shared snapshot、绕过重载和清理后 cacheOnly 不回源，`test:gateway-cache-invalidation-index` 固定源码门禁 |
| 2026-06-28 | 混合路由评分结果可共享到 Redis，但不共享请求正文和自由文本 reason | 混合路由评分是异步路径，Redis 命中可以省掉上游评分模型调用；缓存键已是摘要，缓存值不含上游账号凭据。自由文本 `reason` 可能带有评分模型对请求的概括，不适合放大到 Redis shared cache | `scoreHybridGatewayRequest` 本地 LRU miss 后读取 Redis shared cache，成功评分后写 shared cache；Redis value 只写 `level`、`confidence`、`factors`。新增 `test:hybrid-scoring-shared-cache-redis`，首次请求写 Redis 后清空本地 LRU，第二次相同请求仍不重复调用评分模型 |
| 2026-06-28 | 剩余同步调度缓存暂不继续 Redis 化 | session affinity、IP 错误熔断、账号规避、hybrid route affinity 都在每次调度同步读取；完整网关运行态和候选账号缓存含上游凭据；proxy agent 不是 JSON 数据。继续强行迁移会增加 Redis RTT 或扩大敏感数据面，收益不确定 | 本轮缓存优化停止在不含凭据且异步或可重载的 shared cache。后续只在真实慢 SQL、跨节点命中率问题、队列积压或长周期压测证明收益时，再单独设计对应缓存方案 |
| 2026-06-28 | 用量概览窗口先单独迁移 PG，不一次性迁移全部排行 / 范围窗口 | 统计首页 summary / trend / model / error 读路径直接依赖 overview window；PG 高性能 stats-worker 此前只调 usage 聚合，窗口不刷新会导致统计页只有明细聚合但没有概览窗口 | 新增 `refreshUsageOverviewWindowSnapshotsAsync`、`getUsageStatsOverviewAsync` 和 `test:usage-overview-postgres-smoke`；PG stats-worker 恢复 `client-ip-stats-aggregation` 和 `usage-overview-windows-refresh` 调度。当时账号排行、授权排行、系统指标趋势、usage scope range 和 authorization range window 继续作为独立 stage 补 PG smoke |
| 2026-06-28 | 用量范围窗口作为独立 PG stage 迁移 | 账号、分组、API Key 等范围用量摘要依赖 `usage_scope_range_windows`，如果只补 overview window，统计首页以外的范围查询仍可能缺数据或回退旧路径 | 新增 `refreshUsageScopeRangeWindowSnapshotsAsync` 和 `test:usage-scope-range-postgres-smoke`；PG stats-worker 新增 `usage-scope-range-windows-refresh` 调度。授权范围窗口、账号排行、授权排行和系统指标趋势仍按独立 stage 后续补齐 |
| 2026-06-28 | 授权范围窗口跟随范围窗口独立迁移 PG | 授权团队 / 用户消耗明细依赖 `authorization_team_usage_range_windows` 和 `authorization_user_usage_range_windows`，如果只补 usage scope range，授权统计页仍可能回退 SQLite 或缺窗口数据 | 新增 `refreshAuthorizationUsageRangeWindowSnapshotsAsync`、授权用量 async overview 和 `test:authorization-usage-range-postgres-smoke`；PG stats-worker 新增 `authorization-usage-range-windows-refresh` 调度。账号排行、授权排行和系统指标趋势仍按独立 stage 后续补齐 |
| 2026-06-28 | 核心 TopN 排行快照整体迁移 PG | 账号、调用方、API Key 和授权排行共用 `usage_rank_snapshots`，分开迁移会让高性能 stats-worker 的核心排行定时任务长期无法恢复 | 新增 `refresh*RankSnapshotAsync` helper 和 `test:usage-rank-snapshots-postgres-smoke`；PG stats-worker 新增 `usage-rank-snapshots-refresh` 调度。系统指标趋势仍按独立 stage 后续补齐 |
| 2026-06-28 | 系统指标趋势必须补 PG 采样、窗口、读路径和 explain 门禁闭环 | performance stats-worker 此前没有调度 `system-metrics-sample` / `system-metrics-trend-windows-refresh`，且 `/stats/system-metrics` 仍读同步 SQLite；只补窗口刷新会出现 PG 无源数据或接口读错库，缺 PG explain 又无法证明窗口查询不会退化为全表扫描 | 新增系统指标采样 / 进程事件循环采样 PG async 写入、`refreshSystemMetricsTrendWindowSnapshotsStageAsync`、`getSystemMetricsOverviewAsync` 和 `test:system-metrics-postgres-smoke`；PG stats-worker 恢复系统指标采样和趋势窗口刷新，smoke 同时验证系统指标 latest / trend 和进程事件循环 latest / peak / trend 查询命中索引 |
| 2026-06-26 | 修正分组候选调度索引方向 | PG explain 发现 `idx_group_accounts_dispatch_priority` 的 `local_super_priority_enabled` 为 ASC，与候选窗口 ORDER BY DESC 不一致，会导致额外 Sort | 当前 schema 索引方向改为 DESC；测试库重建后分组候选查询为 Index Scan 且无 Sort |
| 2026-06-25 | 宿主机验证不能直接复用容器内 DNS 连接串 | `pgbouncer`、`redis-cache`、`redis-state` 只在 Compose 网络内可解析，宿主机应走发布端口 | 文档和验证脚本必须区分容器内 URL 与宿主机 URL；redis-state 容器内端口是 `6379`，宿主机默认发布为 `6380` |
| 2026-06-25 | PostgreSQL 模式下网关运行态缓存失效不走 SQLite after-commit 队列 | 已迁移 PG 写路径提交后再通知当前进程 invalidator，继续调用 SQLite after-commit 会被 fail-fast 拦截 | `runGatewayCacheInvalidatorsAfterCommit()` 在 PG driver 下直接执行 effect；跨进程缓存失效已补 Redis runtime state 版本同步 |

## 验收标准

- [ ] 默认 standalone 不配置 PostgreSQL / Redis 也能启动并通过现有回归。
- [ ] performance 模式缺少 PostgreSQL / Redis 配置时快速失败。
- [x] PostgreSQL schema 能初始化空库，并写入默认数据。
- [ ] 主要 repository 在 SQLite 和 PostgreSQL 下语义一致。
- [x] Redis cache / runtime state driver 支持 TTL、版本失效、原子计数、原子消费和锁；API Key 校验、API Key 额度、授权额度、系统设置、网关分组访问元数据、分组绑定账号 ID、授权统计与授权来源 lookup、网关模型目录、管理端模型目录、响应检查策略、授权读资源 lookup、客户端 IP 封禁策略快照和混合路由评分结果已接入 Redis shared cache；登录失败窗口、验证码 challenge / 发放限频、账号并发槽和网关缓存失效版本广播已接入 Redis runtime state，多数生产 `createAppCache` 调用点仍是后续迁移项。
- [ ] PostgreSQL 模式写队列入队即触发消费，最大并发 `100`，且连接池和队列背压可观测；使用记录、审计日志、操作日志、公开接口日志、数据维护和运行日志索引 Redis Streams 链路已完成，usage / audit / 审计错误分组 / operation / public API / 核心 usage stats、usage model / error / latency / account quality、客户端 IP 统计窗口和封禁策略、用量概览窗口、用量范围窗口、授权范围窗口、TopN 排行快照、AI 性能监控读路径、账号用量统计读路径、系统指标趋势、quota snapshot、账户内 API Key runtime state、外部来源公开推送、操作日志读路径、公开接口日志读路径和运行日志读写主要路径已具备 PG readiness / smoke，账号测试 / 探测执行缓冲持久化待按真实证据评估。
- [x] `<测试主机IP>` Docker 测试栈可以启动、健康检查、备份和恢复。
- [ ] 网关主链路、使用记录、审计、统计聚合、授权额度和缓存失效在 performance 模式下通过验证；当前网关事实表 readiness、完整网关吞吐压测、usage / audit / operation / public API / record maintenance / runtime log Redis Streams、Redis Streams 积压恢复、usage model / error / latency / account quality 派生统计、客户端 IP 统计窗口和封禁策略、用量概览窗口、用量范围窗口、授权范围窗口、TopN 排行快照、AI 性能监控读路径、账号用量统计读路径、系统指标趋势、quota snapshot 主要路径、外部来源公开推送、审计错误分组、操作日志读路径、公开接口日志读路径、运行日志读写路径 readiness 和 PG DB service 热读并发白名单已通过。
- [ ] 部署、备份恢复和故障演练文档同步完成；高性能部署、当前 schema 初始化、备份恢复和故障演练文档已更新，真实生产规格维护窗口演练仍待补。

## 验证记录

- 类型检查：2026-06-27 新增真实 Redis cache / state 回归、SQLite standalone DB service 分支回归、PG DB service 热读并发白名单、PG schema 回归、压测 fixture 修复、API Key / 设置 / 网关分组访问 / 网关模型目录 / 管理端模型目录 / 响应检查策略 / 授权读资源 lookup 共享缓存和审计 Redis Streams 后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐客户端 IP 统计 / 封禁策略 PG async 链路后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐用量概览窗口 PG 刷新 / 读路径和 PG stats-worker 调度后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐用量范围窗口 PG 刷新 / 读路径和 PG stats-worker 调度后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐授权范围窗口 PG 刷新 / 读路径和 PG stats-worker 调度后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐 TopN 排行快照 PG 刷新和 PG stats-worker 调度后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐系统指标趋势 PG 采样 / 刷新 / 读路径和 PG stats-worker 调度后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐客户端 IP 封禁策略快照 Redis shared cache 后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 类型检查：2026-06-28 补齐混合路由评分结果 Redis shared cache 后，`pnpm --filter juhe-ai-backend typecheck` 已通过。
- 账户内 API Key runtime state PG smoke：2026-06-28 远端 PostgreSQL / Redis performance 配置运行 `test:account-api-key-runtime-state-postgres-smoke` 通过；覆盖 failure 写入、due-for-probe 候选读取、success 恢复、分组账号统计 dirty 标记和 `idx_account_api_key_runtime_probe` explain。本地 `typecheck`、`test:background-db-service-write-boundary` 和 `test:account-api-key-failure-guard` 已通过。
- PostgreSQL 默认 seed 回归：2026-06-28 远端 PG/Redis `test:performance-system-api-smoke` 曾在 `system-accounts` POST 阶段暴露 `groups_provider_code_fkey`，已修复 PG seed 漏 hybrid provider/profile 的问题。`test:postgres-seed-defaults`、`test:postgres-schema-sql`、`typecheck` 通过；远端完整 `postgres:init-schema` 后确认 `hybridProvider="hybrid"`、`hybridProfiles=3`，`test:performance-system-api-smoke` 复跑通过。
- 网关事实表 readiness 复测：2026-06-28 远端 PG/Redis 运行 `test:performance-gateway-persistence-readiness` 通过；usage 64.39ms、audit 153.11ms、operation 198.27ms、public API 17.65ms、runtime logs 26.33ms、usage stats 120.28ms、quota snapshot 69.67ms，未报告缺失 PG adapter。报告文件 `reports/performance-gateway-persistence-readiness-20260627T210033Z.json`。
- 真实网关压测复测：2026-06-28 远端 PG/Redis 运行 `test:performance-gateway-load` 通过；64 并发、30 秒、32 个账号，15694 请求全成功，成功 QPS 381.73，p95 134.69ms，deadlocks delta 0，max lock waiters 0，max transaction 0.73s，max active query 0.01s，六条 Redis Stream pending / lag 均为 0。报告文件 `reports/performance-gateway-load-1782594606889-9b0959.json`。
- OpenAI OAuth 管理端 async 补充：2026-06-28 本地运行 `typecheck`、`test:openai-oauth-token-response-boundary`、`test:openai-oauth-credentials-patch`、`test:openai-oauth-refresh-activation`、`test:openai-oauth-access-token-refresh` 通过；源码门禁固定 OAuth 管理端不再导入同步账号 / 分组 / 供应商 / 代理 / 操作日志入口。
- 账户单独绑定分组 async 补充：2026-06-28 本地运行 `typecheck`、`test:account-single-read` 通过；远端 PostgreSQL / Redis performance 配置运行 `test:account-single-group-binding-postgres-smoke` 通过；覆盖 System API 登录、临时分组 / 账户创建、`POST /accounts/:id/group` 从 A 分组切到 B 分组和 PG 绑定唯一性校验。
- 账户标签管理端 PG smoke：2026-06-28 远端 PostgreSQL / Redis performance 配置运行 `test:account-tags-postgres-smoke` 通过；覆盖创建账户带标签去重、标签列表绑定计数、绑定标签删除约束、独立替换标签、解除绑定后删除和清理。本地 `typecheck`、`test:account-tags` 已通过。
- 认证资料更新 / 改密 async 补充：2026-06-28 本地运行 `typecheck`、`test:auth-must-change-password-boundary`、`test:system-account-session-driver` 通过；远端 PostgreSQL / Redis performance 配置运行 `test:auth-profile-postgres-smoke` 通过，覆盖临时用户创建、初始改密前资料修改拦截、强制改密、显示名称更新、普通改密旧密码校验、旧会话撤销和 PG 字段校验。
- 公告管理端 / 公开公告 PG smoke：2026-06-28 远端 PostgreSQL / Redis performance 配置运行 `test:announcement-postgres-smoke` 通过；覆盖草稿创建、管理端列表 / 详情、更新、发布、公开列表、已读去重、重复发布保留已读状态、归档后重发清理已读、下线隐藏和删除清理。本地 `typecheck`、`test:announcement-single-read` 已通过。
- 时间计划 / 授权过期 PG smoke：2026-06-28 本地 `typecheck`、`test:postgres-schema-sql` 通过；远端 PostgreSQL / Redis performance 配置运行 `postgres:init-schema-only` 通过，输出 `schemas=5, schemaStatements=593, seedStatements=0`；随后运行 `test:schedule-authorization-postgres-smoke` 通过，覆盖 API Key / 账户时间计划边界激活、重复 event 跳过、授权 grant 到期、运行态授权同步和授权过期扫描索引计划。
- 表监控管理端快照读取 PG smoke：2026-06-28 本地 `typecheck`、`test:table-monitor-default-scan`、`test:table-monitor-dbstat-unavailable` 通过；远端 PostgreSQL / Redis performance 配置运行 `test:table-monitor-postgres-smoke` 通过，覆盖 overview、单表 history、三库 database-history PG async 读取和快照索引 explain。2026-06-30 已补齐 performance 模式表监控 PG 采样写入：`table-storage-monitor` 在 PostgreSQL driver 下只采 `juhe_business`、`juhe_dataset`、`juhe_usage`、`juhe_stats` 四个 schema，写入 `juhe_stats.database_storage_snapshots` / `table_storage_snapshots`，不再 fail-fast 或回退 SQLite；本地 `typecheck`、`test:table-monitor-default-scan`、`test:table-monitor-dbstat-unavailable`、`test:postgres-schema-sql` 已通过，远端 `test:table-monitor-postgres-smoke` 待具备 PG/Redis 环境后复跑。
- 过期逻辑删除账户 PG 清理：2026-06-28 本地 `typecheck`、`test:deleted-account-related-cleanup`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role` 通过；远端 PostgreSQL / Redis performance 配置运行 `test:expired-deleted-account-cleanup-postgres-smoke` 通过，覆盖临时账户逻辑删除、`deleted_at` 老化、DB service 分发到 PG async 清理、账号物理删除、分组绑定删除和账号业务子表清理；ops-worker 高性能模式已恢复 `expired-deleted-account-cleanup` 调度。有关联 usage / audit / stats 历史时仍返回 deferred，不在本轮删除历史明细。
- 账号健康检测 / 冷却复测 PG smoke：2026-06-28 本地 `typecheck`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role`、`test:postgres-schema-sql`、`test:cooldown-retest-recovery`、`test:background-db-service-write-boundary` 通过；远端 PostgreSQL / Redis performance 配置运行 `postgres:init-schema-only` 通过，输出 `schemas=5, schemaStatements=596, seedStatements=0`；随后运行 `test:account-probe-postgres-smoke` 通过，覆盖健康检测候选 / 单账号读取、success 写回、failure 写回、冷却复测候选 / 单账号读取、failure backoff 写回和 `idx_accounts_health_check_pg_due` / `idx_accounts_cooldown_retest_pg_due` explain。
- 代理延迟刷新 PG smoke：2026-06-28 本地 `typecheck`、`test:postgres-schema-sql`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role`、`test:proxy-single-read` 通过；远端 PostgreSQL / Redis performance 配置运行 `postgres:init-schema-only` 通过，输出 `schemas=5, schemaStatements=597, seedStatements=0`；随后运行 `test:proxy-postgres-smoke` 通过，覆盖代理 CRUD、测试配置解密、检测状态写回、启用代理候选读取和 `idx_proxy_profiles_latency_refresh_due` explain。
- OpenAI OAuth access token 自动刷新 PG smoke：2026-06-28 本地 `typecheck`、`test:openai-oauth-access-token-refresh`、`test:postgres-schema-sql`、`test:sqlite-high-volume-guards`、`test:background-metrics-worker-role` 通过；远端 PostgreSQL / Redis performance 配置运行 `postgres:init-schema-only` 通过，输出 `schemas=5, schemaStatements=598, seedStatements=0`；随后运行 `test:openai-oauth-access-token-refresh-postgres-smoke` 通过，覆盖测试替身 token 成功刷新、连续失败 3 次标记异常、错误脱敏和 `idx_accounts_openai_oauth_refresh_pg_due` explain。
- API Key 列表 PG smoke：2026-06-28 本地 `typecheck`、`test:api-key-list-query-guard`、`test:api-key-management-driver`、`test:postgres-schema-sql` 通过；远端 PostgreSQL / Redis performance 配置运行 `test:api-key-list-postgres-smoke` 通过，覆盖 keyword 精确 / 前缀、`%` 字面量、分组 + active 筛选、列表密钥脱敏和默认排序 / keyword CTE / 名称索引窗口 / 分组筛选 explain。
- AI 账户导入 HTTP PG smoke：2026-06-28 本地 `typecheck`、`test:account-import-availability-schedule` 通过；远端 PostgreSQL / Redis performance 配置运行 `test:account-import-postgres-smoke` 通过，覆盖 System API 登录、导入 preview / confirm、自动创建临时分组、导入账号落库、分组绑定、标签绑定和 availabilitySchedule JSON 保存，临时账号 / 分组已清理。
- 构建：未执行，命令待实现后执行 `pnpm build`。
- 单项验证：已通过 `test:runtime-config-env-override`、`test:runtime-state-cache-driver`、`test:runtime-state-cache-redis`、`test:database-client`、`test:provider-repository-driver`、`test:system-account-session-driver`、`test:system-account-management-driver`、`test:group-management-driver`、`test:api-key-management-driver`、`test:authorization-return-driver`、`test:settings-public-driver`、`test:settings-management-driver`、`test:settings-query-boundary`、`test:gateway-cache-invalidation-runtime-state`、`test:redis-stream-queue`、`test:audit-log-async-flush`、`test:gateway-audit-payload-storage`、`test:db-service-runtime-metrics`、`test:performance-system-api-smoke`、`test:authorization-options-query-guard`、`test:authorization-list-team-usage-batch`、`test:authorization-usage-pagination`、`test:base-list-page-window`、`test:log-list-query-guard`、`test:log-list-page-window`、`test:scope`、`test:authorization-return`、`test:gateway-storage-cache-invalidation`、`test:gateway-runtime-cache`、`test:gateway-cache-invalidation-index`、`test:account-api-key-gateway-mock-ai`、`test:schedule-authorization-postgres-smoke`、`test:table-monitor-postgres-smoke`、`test:expired-deleted-account-cleanup-postgres-smoke`、`test:account-probe-postgres-smoke`、`test:proxy-postgres-smoke`、`test:proxy-single-read`、`test:openai-oauth-access-token-refresh-postgres-smoke`、`test:api-key-list-postgres-smoke`、`test:account-import-postgres-smoke`、`test:account-single-group-binding-postgres-smoke`、`test:auth-profile-postgres-smoke`、`test:gateway-quota-passive-snapshot`、`test:postgres-schema-sql`、`test:auth-memory-maintenance-boundary`、`test:auth-must-change-password-boundary`、`test:system-api-rate-limit`、`test:system-account-list-query-guard`、`test:system-account-whitespace-boundary`、`test:system-account-options-lightweight`、`test:account-options-lightweight`、`test:account-model-mapping`、`test:deleted-account-related-cleanup`、`test:group-options-lightweight`、`test:group-scheduling-policy`、`test:default-group-current-contract`、`test:api-key-route-validation`、`test:api-key-availability-schedule`、`test:route-strategy-group-bindings`、`test:account-concurrency-lane-index`、`test:gateway-concurrency-preparation`、`test:usage-record-writer-pool`、`test:usage-record-byte-batch`。
- 高性能压测：2026-06-26 相同模型配置短路优化后，`<测试主机IP>` 远端 Docker 栈在 `JUHE_AI_DB_POOL_MAX=150`、`JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT=256`、100 并发、25 个账号、15% 写请求下通过 `test:performance-system-api-load`；总请求 20373，HTTP 失败 0，端到端脚本 RPS 455.09，整体 p95 335.60ms，PATCH p95 451.17ms，deadlocks delta 0，最大事务 0.38s，最大 active query 0.35s。报告文件为 `reports/performance-system-api-model-config-skip-20260626-213152.json`，对比数据见 `docs/reports/高性能模式压测报告-2026-06-26.md`。
- 网关事实表 readiness：2026-06-26 远端 Docker 栈运行 `test:performance-gateway-persistence-readiness`。初始结果五个入口均 fail-fast；迁移 usage / audit / operation / public API / 核心 stats 后最终全绿：`usage_records_write` 33.30ms、`audit_logs_write` 62.22ms、`operation_logs_write` 60.06ms、`public_api_logs_write` 17.70ms、`usage_stats_aggregate` 79.98ms。2026-06-27 远端 schema-only 同步补齐 `usage_records.failure_attribution` 等增量列后复测仍全绿：`usage_records_write` 31.00ms、`audit_logs_write` 64.38ms、`operation_logs_write` 58.29ms、`public_api_logs_write` 13.04ms、`usage_stats_aggregate` 42.47ms。2026-06-27 补齐 usage model / error / latency / account quality PG 派生聚合后再次复测全绿：`usage_records_write` 36.84ms、`audit_logs_write` 40.40ms、`operation_logs_write` 61.41ms、`public_api_logs_write` 11.67ms、`usage_stats_aggregate` 173.05ms。补齐 PG quota snapshot 后复测全绿：`usage_records_write` 43.37ms、`audit_logs_write` 52.88ms、`operation_logs_write` 62.43ms、`public_api_logs_write` 12.81ms、`usage_stats_aggregate` 170.89ms、`gateway_quota_snapshot_pg` 143.82ms。扩展直接和团队授权 quota snapshot 后复测全绿：`usage_records_write` 51.10ms、`audit_logs_write` 50.15ms、`operation_logs_write` 61.07ms、`public_api_logs_write` 9.70ms、`usage_stats_aggregate` 160.78ms、`gateway_quota_snapshot_pg` 68.35ms。扩展 PG 审计错误分组后复测全绿：`usage_records_write` 61.39ms、`audit_logs_write` 81.52ms、`operation_logs_write` 38.24ms、`public_api_logs_write` 9.87ms、`usage_stats_aggregate` 150.37ms、`gateway_quota_snapshot_pg` 94.36ms。扩展 PG 审计读路径后复测仍全绿：`usage_records_write` 55.20ms、`audit_logs_write` 150.46ms、`operation_logs_write` 61.09ms、`public_api_logs_write` 11.57ms、`usage_stats_aggregate` 166.59ms、`gateway_quota_snapshot_pg` 96.97ms。扩展 PG 操作日志读路径后复测仍全绿：`usage_records_write` 46.71ms、`audit_logs_write` 138.89ms、`operation_logs_write` 199.23ms、`public_api_logs_write` 12.52ms、`usage_stats_aggregate` 166.50ms、`gateway_quota_snapshot_pg` 106.93ms。扩展 PG 运行日志读路径后复测仍全绿：`usage_records_write` 57.31ms、`audit_logs_write` 162.36ms、`operation_logs_write` 182.93ms、`public_api_logs_write` 10.64ms、`runtime_logs_read` 17.27ms、`usage_stats_aggregate` 147.80ms、`gateway_quota_snapshot_pg` 92.67ms。扩展 PG 公开接口日志读路径后复测仍全绿：`usage_records_write` 65.18ms、`audit_logs_write` 149.05ms、`operation_logs_write` 187.80ms、`public_api_logs_write` 17.95ms、`runtime_logs_read` 13.15ms、`usage_stats_aggregate` 119.90ms、`gateway_quota_snapshot_pg` 77.73ms。补充 API Key 校验 / 系统设置 Redis shared cache 后复测仍全绿：`usage_records_write` 58.41ms、`audit_logs_write` 140.46ms、`operation_logs_write` 191.00ms、`public_api_logs_write` 26.61ms、`runtime_logs_read` 14.87ms、`usage_stats_aggregate` 142.41ms、`gateway_quota_snapshot_pg` 97.05ms。补充网关模型目录 / 响应检查策略 Redis shared cache 后复测仍全绿：`usage_records_write` 53.71ms、`audit_logs_write` 152.89ms、`operation_logs_write` 185.36ms、`public_api_logs_write` 25.43ms、`runtime_logs_read` 20.80ms、`usage_stats_aggregate` 176.00ms、`gateway_quota_snapshot_pg` 100.21ms。补充审计 Redis Streams 和 PG DB service 热读并发后复测仍全绿：`usage_records_write` 62.81ms、`audit_logs_write` 251.93ms、`operation_logs_write` 295.97ms、`public_api_logs_write` 29.28ms、`runtime_logs_read` 19.50ms、`usage_stats_aggregate` 247.80ms、`gateway_quota_snapshot_pg` 97.30ms。最新报告文件为 `reports/performance-gateway-persistence-readiness-20260627T000410Z.json`。
- 真实网关压测：2026-06-26 远端 Docker 栈运行 `test:performance-gateway-load`。最终 100 并发、20 秒、24 个账号、35 秒沉淀窗口通过：11192 请求全成功，端到端脚本 RPS 200.34，p95 232.02ms，p99 275.32ms，usage records 12493，audit logs 615，Redis pending 0，deadlocks delta 0，最大 lock waiters 0，最大事务 1.85s，最大 active query 0.03s。2026-06-27 修复压测子进程 `JUHE_AI_SECRET` fallback 后复测通过：100 并发、20 秒、64 个账号、20 秒沉淀窗口，10561 请求全成功，成功 QPS 257.18，p95 227.75ms，p99 252.20ms，usage records 11682，audit logs 450，Redis pending 0，deadlocks delta 0，最大 lock waiters 0，最大事务 2.14s，最大 active query 0.02s。2026-06-27 补齐 PG 派生统计和 API Key quota snapshot 后再次复测通过：10479 请求全成功，成功 QPS 256.09，p95 219.37ms，p99 237.76ms，usage records 11490，audit logs 525，Redis pending 0，deadlocks delta 0，最大 lock waiters 0，最大事务 1.71s，最大 active query 0.02s。2026-06-27 补充网关模型目录 / 响应检查策略 Redis shared cache 后使用 3 秒 warmup 再次复测通过：10485 请求全成功，成功 QPS 255.26，p95 227.24ms，p99 272.70ms，usage records 11526，audit logs 500，Redis pending 0，deadlocks delta 0，最大 lock waiters 0，最大事务 2.10s，最大 active query 0.03s。补充管理端模型目录 Redis shared cache 后再次复测通过：10262 请求全成功，成功 QPS 249.80，p95 234.95ms，p99 304.58ms，usage records 11162，audit logs 475，Redis pending 0，deadlocks delta 0，最大 lock waiters 0，最大事务 1.86s，最大 active query 0.02s。补充审计 Redis Streams、审计 PG 单批 25 和 DB service PG 热读并发后再次复测通过：10703 请求全成功，成功 QPS 260.94，p95 211.37ms，p99 320.89ms，usage records 11703，audit logs 454，usage stream pending 0，audit stream pending 25，total pending 25，deadlocks delta 0，最大 lock waiters 0，最大事务 1.73s，最大 active query 0.03s。补充 operation / public API Redis Streams、pending+lag backlog 门禁和 2% 成功审计抽样后，60 秒 settle 复测通过：10776 请求全成功，p95 225.63ms，p99 356.13ms，usage records 11776，audit logs 236，usage / audit / operation / public API pending 和 lag 均为 0。补充 record maintenance Redis Stream 和第五条 Stream backlog 门禁后，60 秒 settle 复测通过：7535 请求全成功，p95 354.06ms，p99 410.80ms，usage records 8647，audit logs 186，usage / audit / operation / public API / record maintenance pending 和 lag 均为 0，total backlog 0，deadlocks delta 0，最大 lock waiters 0，最大事务 1.01s。补充 runtime log Redis Stream 和第六条 Stream backlog 门禁后，60 秒 settle 复测通过：10269 请求全成功，p95 222.48ms，p99 301.48ms，usage records 11274，audit logs 238，usage / audit / operation / public API / record maintenance / runtime log pending 和 lag 均为 0，total backlog 0，deadlocks delta 0，最大 lock waiters 0，最大事务 1.87s。补充网关分组访问 Redis shared cache 后，60 秒 settle 复测通过：10434 请求全成功，p95 229.37ms，p99 295.28ms，usage records 11469，audit logs 215，六条 Stream pending 和 lag 均为 0，total backlog 0，deadlocks delta 0，最大 lock waiters 0，最大事务 1.72s。最新报告文件为 `reports/performance-gateway-load-20260627-group-usage-access-cache-100c.json`。
- 可靠性演练：2026-06-27 远端 Docker 栈运行 `test:performance-reliability-drill`。最终全项通过：PG health 57.85ms，PG 阻塞快照 deadlocks 0 / lock waiters 0 / idle in transaction 0，生产 usage stream pending 0，临时 Redis Stream 3 条 pending 可接管并 ACK，PostgreSQL 备份恢复到临时库 14.03s 且 schema 表数量一致，Redis state 重启后 3 条 pending 全部恢复并 ACK，PostgreSQL / PgBouncer 重启后健康检查恢复约 41ms。报告文件为 `reports/performance-reliability-drill-20260627-022408.json`。
- 真实 Redis cache / state 回归：2026-06-27 远端 `<测试主机IP>` redis-cache / redis-state 运行 `test:runtime-state-cache-redis`，通过且脚本正常退出。覆盖 `SharedJsonCache` TTL / delete / clear，`RuntimeStateStore` JSON / `incr` 上限 / lock token 释放，登录失败锁定、验证码 challenge `GETDEL` 一次性消费、验证码发放限频和账号并发 Redis Lua 并发争抢上限；最新复跑耗时 665ms，测试 key 已清理。
- SQLite standalone DB service 分支回归：2026-06-27 本地运行 `test:db-service-sqlite-dispatch-standalone`，通过。覆盖 SQLite standalone 模式下 `list_public_global_settings`、`read_gateway_settings`、`resolve_group_usage_access`、`list_openai_accounts_for_group`、`list_openai_accounts_for_group_result`、`list_recoverable_unavailable_openai_accounts_for_group` 和 `list_provider_model_catalog` 经异步 dispatch 后调用当前同步实现。
- 共享缓存门禁：2026-06-27 本地运行 `test:gateway-cache-invalidation-index`，通过；2026-06-28 补客户端 IP 封禁策略快照、混合路由评分结果、网关设置和模型路由索引后复跑通过。确认 API Key 校验、API Key 额度、授权额度、网关设置、网关分组访问元数据、网关模型目录、模型路由索引、管理端模型目录、响应检查策略、客户端 IP 封禁策略快照和混合路由评分结果异步判定 / 读取 / 写入 / 清理均包含 Redis `SharedJsonCache` 路径，且 API Key 定点失效不扫描全部本地缓存；客户端 IP 封禁策略在 Redis cache driver 下读取 shared snapshot，失效重载使用 `bypassSharedCache` 回源刷新，真实 Redis 回归覆盖旧 shared snapshot 和清理后 cacheOnly 不回源。`test:settings-management-driver`、`test:settings-public-driver` 已确认系统设置 / 全局设置 async 路径包含 Redis shared cache，更新后绕过旧共享缓存从数据库重读。`test:authorization-list-team-usage-batch` 已确认授权读资源 lookup 复用统一 Redis shared cache helper，不再在授权读仓储重复私有 lookup。`test:model-catalog`、`test:gateway-runtime-cache`、`test:api-key-group-route-cache`、`test:api-key-group-route-capability` 和 `test:hybrid-gateway-mock-ai` 已确认模型目录语义、网关运行态软过期、动态模型路由索引、未知模型缓存行为、请求级路由能力和混合路由评分缓存语义未被破坏。
- 客户端 IP 统计 / 封禁策略 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:client-ip-stats-postgres-smoke` 通过；脚本写入 3 条 usage record，`aggregateClientIpStatsBatchAsync` 消费 3 条，`refreshClientIpUsageRangeWindowsAsync` 刷新后 `listClientIpStatsAsync` 返回 2 个 IP 汇总，`getClientIpStatsDetailAsync` 返回 1 个账号窗口，并验证封禁策略创建、active 列表、hash 精确读取、命中计数写入 3 次和解封；同一 smoke 对客户端 IP 搜索前缀、列表 `request_count` 排序、详情账号 `request_count` 排序和 active 封禁策略筛选执行 PG explain，断言不出现 Seq Scan 且命中对应索引，输出 `explainIndexed=true`；finally 清理 smoke 数据、policy 数据并恢复 IP 统计 job state。2026-06-28 补充封禁 / 解封操作日志 async 设置读取后，本地 `typecheck` 和 `test:client-ip-stats` 通过。
- 使用记录列表 / 详情 PG smoke：2026-07-03 远端 PG/Redis 运行 `test:usage-record-list-postgres-smoke` 通过；脚本写入 5 条 usage record，确认 `usage_records=5`、`usage_record_shard_entries=5`，`listUsageRecordsAsync` 按 trace 前缀返回 5 条、按 model+failed+duration 返回 1 条、按 client IP 前缀返回 2 条，`getUsageRecordDetailAsync` 按 ID 读取详情正确，并覆盖音频 token、输出图片张数、异步自定义模型补价和列表 / 详情成本拆解；同一 smoke 对 trace 前缀、model 筛选、client IP 前缀和 duration 排序执行 PG explain，断言不出现 Seq Scan 且命中对应 catalog 索引，输出 `explainIndexed=true`。同时本地 `typecheck`、`test:usage-record-list-query-guard` 和 `test:usage-record-list-catalog-window` 通过。
- 客户端 IP 封禁策略快照 Redis shared cache：2026-07-03 本地运行 `typecheck`、`test:gateway-cache-invalidation-index`、`test:performance-redis-boundary`、`test:client-ip-policy-shared-cache-redis` 均通过；随后使用测试 Redis 复跑 `test:client-ip-policy-shared-cache-redis` 通过，脚本覆盖普通判定回源写入 shared cache、`cacheOnly` 命中 shared cache、禁用策略后旧 shared snapshot 命中、绕过 shared cache 重载刷新 Redis snapshot，以及清理 shared cache 后 `cacheOnly` 不回源数据库。
- 混合路由评分结果 Redis shared cache：2026-06-28 本地运行 `typecheck`、`test:hybrid-gateway-mock-ai` 和 `test:gateway-cache-invalidation-index` 均通过；随后使用测试 Redis 运行新增 `test:hybrid-scoring-shared-cache-redis` 通过。脚本复用 hybrid mock 上游，首次完全相同请求写入 shared cache 后清空本地评分 LRU，第二次请求仍只命中 1 次入口评分模型、目标请求执行 2 次，证明 shared cache 能省重复评分调用且不省略真实目标请求。
- 用量概览窗口 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:usage-overview-postgres-smoke` 通过；脚本写入最小 `juhe_stats.usage_stats_*` 源数据，`refreshUsageRankSnapshotsInStages({ stageNames: ['usage_overview_windows'] })` 刷新后 `getUsageStatsOverviewAsync` 读取 requestCount=7、趋势桶 1、模型排行 1、错误排行 1，并验证第二轮 source watermark 不变时 `skipped=true`，finally 清理 smoke 源数据、窗口数据和 job state。同时本地 `test:usage-overview-window-atomic`、`test:usage-rank-staged-refresh` 和 `test:background-worker-performance` 通过。
- 用量范围窗口 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:usage-scope-range-postgres-smoke` 通过；脚本写入最小 `juhe_stats.usage_stats_daily` 源数据，`refreshUsageRankSnapshotsInStages({ stageNames: ['usage_scope_range_windows'] })` 刷新后窗口表返回 requestCount=11、activeDays=1、totalCost=0.321，`loadUsageRangeSummaryForScopeAsync` 读回一致结果，并验证第二轮 source watermark 不变时 `skipped=true`，finally 清理 smoke 源数据、窗口数据和 job state。同时本地 `test:usage-rank-staged-refresh` 和 `test:background-worker-performance` 通过，性能回归覆盖 39680 行窗口发布，durationMs=31517.3，maxEventLoopGapMs=246.1。
- 授权范围窗口 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:authorization-usage-range-postgres-smoke` 通过；脚本写入最小 `juhe_business` lookup 数据和 `juhe_stats.authorization_*_usage_summary_daily` 源数据，`refreshUsageRankSnapshotsInStages({ stageNames: ['authorization_usage_range_windows'] })` 刷新后 `getAuthorizationTeamUsageOverviewAsync` 读取团队 summary requestCount=13、`getAuthorizationUserUsageOverviewAsync` 读取用户 summary requestCount=17，团队 / 用户明细各 1 行，并验证第二轮 source watermark 不变时 `skipped=true`，finally 清理 smoke business、source、window 和 job state。同时本地 `test:authorization-usage-pagination`、`test:usage-rank-staged-refresh` 和 `test:background-worker-performance` 通过。
- TopN 排行快照 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:usage-rank-snapshots-postgres-smoke` 通过；脚本写入 `juhe_stats.usage_stats_daily` / `usage_stats_monthly` 源数据，`refreshUsageRankSnapshotsInStages` 刷新 5 个 TopN stage 后校验 account、caller_account、api_key、account_authorization 和 group_authorization 的 rank=1 与 metric_value。2026-07-01 补齐 `ai_performance_summary_windows` PG staged refresh 后，在 Mac PostgreSQL / Redis 环境复跑通过，执行 6 个 stage，并验证第二轮 source watermark 不变时 `skipped=true`。同时本地 `test:usage-rank-staged-refresh` 和 `typecheck` 通过。
- AI 性能监控 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:ai-performance-postgres-smoke` 通过；脚本创建临时系统账户、分组和账号，写入最小 TopN / hourly / summary 窗口，`listAiPerformanceAccountOptionsAsync` 验证账号名前缀搜索和显式选中回填，`getAiPerformanceOverviewAsync` 验证 summary requestCount=42 与小时趋势 requestCount=42，finally 清理业务和统计 smoke 数据。同时本地 `typecheck`、`test:ai-performance-account-options-query-guard` 和 `test:ai-performance-hourly-query-guard` 通过。
- 账号用量统计 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:account-usage-postgres-smoke` 通过；脚本创建临时系统账户、分组和两个账号，写入 caller_account / system_account 范围窗口、daily 和 TopN rank，`getAccountUsageStatsOverviewPageAsync` 验证默认窗口排序、summary requestCount=38、默认趋势账号、账号名前缀搜索、日趋势 requestCount=31 和关键词未命中时显式选中回填，finally 清理业务和统计 smoke 数据。同时本地 `typecheck` 和 `test:account-usage-query-guard` 通过。
- 外部来源系统 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:external-integration-source-postgres-smoke` 通过；脚本创建临时来源系统和生产 token，验证列表前缀搜索、tokenCount / activeTokenCount、token secret 解密、`validateExternalIntegrationSourceTokenAsync` 授权成功 / scope 不足 / source 禁用拒绝、token 更新、source 禁用同步 token 状态和删除清理。同时本地 `typecheck`、`test:external-integration-source-async-boundary`、`test:external-integration-source-expires-at` 和 `test:external-source-auth` 通过。
- 外部来源公开推送 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:external-public-account-push-postgres-smoke` 通过；脚本覆盖公开账号新增 / 列表 / 修改 / 删除、公开分组新增 / 列表 / 修改 / 删除、公开 API Key 新增 / 列表 / 修改 / 删除和临时目标用户清理。本地 `typecheck`、`test:external-public-account-push-async-boundary` 和 `test:external-source-auth` 通过；PG 模式下 API Key 删除后的本地 dataset 清理投递已显式跳过，后续需补 PG 版关联明细清理 worker。
- 外部来源公开账号用量 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:external-public-account-usage-postgres-smoke` 通过；脚本创建临时系统账户、分组和两个账号，写入全局 account 范围窗口，验证公开账号用量排序、账号名前缀搜索、账号元数据补齐、内部 owner 字段不外露和 mock 分支，并对账号名称前缀解析、窗口请求数排序和关键词窗口读取执行 PG explain，输出 `explainIndexed=true`。本地 `typecheck` 和 `test:external-public-account-usage-query-guard` 通过；公开账号 usage 路由已改为 async 服务，PG 下使用 `usageStatsTimezoneAsync`、`latestUsageStatsLagSecondsForRuntime`、`juhe_stats.usage_scope_range_windows` 和 `juhe_business.accounts`。
- 系统团队 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:system-team-postgres-smoke` 通过；脚本创建临时团队、两个成员和一个分组授权，验证团队列表关键词、成员作用域列表、团队授权创建、添加新成员后授权运行态自动展开、移除成员后团队来源撤销、停用团队撤销全部团队来源、重新启用团队后恢复来源，并对团队列表排序、名称前缀搜索、成员窗口和成员作用域查询执行 PG explain，输出 `explainIndexed=true`。本地 `typecheck`、`test:system-team-query-guard`、`test:system-team-single-read` 和 `test:system-team-fanout-boundary` 通过。
- 账户流量迁移 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:account-traffic-migration-postgres-smoke` 通过；脚本创建临时分组和同组源 / 目标账号，验证迁移后源账号 `temporary_unavailable`、冷却时间写入、目标账号仍 active / schedulable，并对账号管理读取和同组绑定读取执行 PG explain，输出 `explainIndexed=true`。本地 `typecheck` 和 `test:account-traffic-migration-query-guard` 通过；同时修复 PG 模式下流量迁移写入后误触 SQLite 分组账号统计脏标记的问题。`test:authorized-account-admin-dispatch` 本轮在账号测试任务 queued 等待处超时，未作为该改动通过依据。
- 账户导出 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:account-export-postgres-smoke` 通过；脚本创建临时分组、代理和账号，`exportAccountsForRequestAsync` 按 ID 导出账号密钥、接口能力、标签、支持模型、分组名称、代理引用和代理密码，按筛选导出验证 `matchedAccounts=1` 与凭据回填，finally 清理临时账号、代理、分组和标签。同时本地 `typecheck`、`test:account-tags` 通过；`test:scope` 当前因 API Key `groupBindings` 契约差异在导出前失败，未作为本轮导出验证依据。
- 系统指标趋势 PG smoke：2026-06-28 远端 PG/Redis 运行 `test:system-metrics-postgres-smoke` 通过；脚本在最近 31 天内找到空闲小时桶 `2026-05-29T00`，通过 `insertSystemMetricsSampleAsync` / `insertProcessEventLoopSampleAsync` 写入 `sampledAt=2026-05-28T16:00:00.000Z` 的系统和进程采样并生成 hourly 源数据，`refreshUsageRankSnapshotsInStages({ stageNames: ['system_metrics_trend_windows'] })` 刷新后 `getSystemMetricsOverviewAsync` 读回系统趋势 1 条和进程事件循环趋势 1 条，并验证最新系统采样、系统指标趋势窗口、进程事件循环 latest / peak / trend 查询的 PG explain 命中索引且不出现 Seq Scan，输出 `explainIndexed=true`；第二轮 source watermark 不变时 `skipped=true`，finally 删除源数据并重刷窗口恢复。同时本地 `test:system-metrics-window-query-guard`、`test:system-metrics-event-loop-average`、`test:background-metrics-worker-role` 和 `test:background-worker-performance` 通过。
- 授权读资源 lookup 补充验证：2026-06-27 本地运行 `test:authorization-list-team-usage-batch`、`test:authorization-options-query-guard`、`test:authorization-usage-pagination`、`test:resource-authorization-single-read` 均通过；随后固定 API Key 路由策略字段必须由当前 schema 建表直接提供，`test:postgres-schema-sql` 通过；最后用远端 PostgreSQL / Redis URL 运行 `test:performance-system-api-smoke` 通过，覆盖授权列表、options、usage 详情、授权创建、已有授权更新 / 有效期和额度更新 / 回收的最小 HTTP 链路。后续复查确认此前 redis-state 阻塞来自宿主机连接串使用了 redis-cache 密码，账号文件补齐 redis-state 独立密码后，真实 Redis state 回归已复跑通过。
- 最新 readiness / 网关压测：2026-07-03 远端 PG/Redis 运行 `test:performance-gateway-persistence-readiness` 通过，报告 `reports/performance-gateway-persistence-readiness-20260702T182822Z.json`：usage 468.00ms、audit 143.91ms、operation 217.19ms、public API 24.58ms、runtime logs 47.54ms、usage stats 410.03ms、quota snapshot 112.50ms。此前 2026-06-28 运行 `test:performance-system-api-load`，50 并发、30 秒、17188 请求全成功，QPS 410.33，p95 214.8ms，p99 305.01ms，deadlocks delta 0，报告 `reports/performance-system-api-load-20260628T1710Z-codex.json`；同日 `test:performance-gateway-load`，64 并发、30 秒、32 账号、50ms mock upstream、10 秒 settle，14932 请求全成功，成功 QPS 359.73，p95 161.98ms，p99 261.71ms，deadlocks delta 0，最大 lock waiters 0，最大 active query 0.29s，本轮 positive pending / backlog 均为 0，历史 backlog 净减少 800，报告 `reports/performance-gateway-load-1782666536194-26ffae.json`。
- 数据切换边界收口：2026-06-29 删除 SQLite -> PostgreSQL 旧数据处理实现、测试入口和文档 runbook。后续验证只覆盖当前 PostgreSQL schema 初始化、当前 repository / cache / queue 路径和压测；历史数据保留 / 导入不进入代码库测试矩阵。
- 手动验证：`<测试主机IP>` 已完成中间件实启，PostgreSQL / PgBouncer / Redis cache / Redis state 均 healthy，`pg_stat_statements` 已创建。当前仓库 standalone / performance compose 已在远端 `/tmp` 临时目录通过 `docker compose config --quiet`，远端 Docker Compose 版本 `2.40.3+ds1-0ubuntu1~22.04.1`，临时目录已删除且未启动新容器。2026-07-03 远端 PostgreSQL / Redis performance 配置执行完整 `postgres:init-schema` 通过，最新初始化结果为 5 个 schema、594 条 schema 语句、147 条默认种子语句；随后使用同一套中间件完成使用记录 PG smoke、客户端 IP 策略 shared cache Redis 回归、runtime state Redis 回归、record maintenance / runtime log Redis Stream smoke、客户端 IP 统计 PG smoke 和 readiness 门禁。`test:provider-repository-driver`、`test:system-account-session-driver`、`test:system-account-management-driver`、`test:group-management-driver`、`test:api-key-management-driver`、`test:authorization-return-driver`、`test:settings-public-driver`、`test:settings-management-driver` 和 `test:performance-system-api-smoke` 已使用远端 PostgreSQL / Redis URL 验证 provider 只读 repository、登录 / 会话最小 repository、系统账户管理读写、分组管理读写、API Key 管理读写与网关 Key 校验入口、混合路由模型目录校验、自定义模型管理 CRUD、授权列表 / options 读取、授权 usage 详情读取、授权创建、现有授权更新 / 有效期和额度更新 / 回收、AI 账户最小创建与分组绑定 repository、AI 账户创建和普通 owner 列表 / 详情 / options / 常规更新 / 异常状态恢复 / 删除 HTTP 链路、账户授权实例归还、授权列表个人归还、分组个人归还、网关运行态最小账号候选读取、账号支持模型 / 模型映射附属表 replace 写入、公共设置读取、系统 API 限流设置读取、系统设置管理读写和最小 HTTP 链路；2026-06-28 已复跑远端 PG/Redis System API smoke 确认系统账户创建不再触发默认分组外键失败。redis-state 已通过临时 Stream 的 XADD / XREADGROUP / XACK 烟测并删除临时 key，也已通过跨进程 API Key quota cache invalidation 版本同步烟测并删除测试 key。宿主机连接远端 Docker 栈时已确认必须使用发布端口：PgBouncer `6432`、redis-cache `6379`、redis-state `6380`，不能把 redis-state 的容器内 `6379` 误当宿主机端口。
- 未验证项：应用容器 performance 模式和生产规格机器上的最终维护窗口演练；历史数据保留 / 导入由上线窗口在代码库外单独处理。

## 风险与注意事项

- PostgreSQL 没有 SQLite 文件级全局写锁，但仍有行锁、唯一索引冲突、长事务、连接池耗尽和 autovacuum 压力。
- 最大并发 `100` 必须配套 PgBouncer、连接池上限和队列背压；不能让每个 Node 进程都创建 100 个数据库连接。
- Redis cache 与 runtime state 如果共用 LRU 实例，可能导致限流、并发占用或短 TTL 屏蔽被意外淘汰。
- Redis 8 授权需要生产前确认；如果授权不可接受，要另起决策评估替代方案。
- PostgreSQL adapter 会触及大量 repository 和 SQL 差异，必须先做 SQLite adapter 回归，再接 PostgreSQL。
- 服务器凭据不能写入仓库文档、日志或计划，只能保存在部署环境安全位置。

## 完成总结

- 完成时间：2026-07-01 阶段收口；生产上线窗口仍需单独执行。
- 实际完成内容：核心 PostgreSQL repository / DB service、Redis cache/state/Streams、网关事实写入、System API 高频管理链路、主要统计窗口、readiness、压测、故障演练和备份恢复验证已完成；`ai_performance_summary_windows` 已补齐 PostgreSQL staged refresh 支持，避免 stats-worker 调度该 stage 时 fail-fast。
- 主要改动位置：`backend/src/storage/` 的 PostgreSQL async repository / 统计窗口实现、`backend/src/modules/background/` 的高性能 worker 调度、`backend/src/scripts/regression/` 的 PG/Redis smoke 与 readiness、`docker/` 的 performance 配置、`docs/deploy/高性能模式部署指南.md` 和本计划。
- 验证结果：截至 2026-07-03，远端 PG/Redis smoke、`performance-gateway-persistence-readiness`、System API load、Gateway load、Redis / PG 重启演练和备份恢复均有通过记录；本轮已使用远端 PostgreSQL / Redis 完整复跑 `postgres:init-schema`、使用记录 PG smoke、客户端 IP 策略 shared cache Redis 回归、runtime state Redis 回归、record maintenance / runtime log Redis Stream smoke、客户端 IP 统计 PG smoke 和 readiness 门禁。2026-07-01 本地补丁后 `pnpm --filter juhe-ai-backend typecheck`、`test:usage-rank-staged-refresh` 通过，并通过 SSH 隧道在 Mac PostgreSQL / Redis 环境执行 `postgres:init-schema`、`test:postgres-seed-defaults`、`test:runtime-state-cache-redis`、`test:usage-rank-snapshots-postgres-smoke` 和 `test:ai-performance-postgres-smoke`。
- 后续建议：生产 Mac 目标机 PostgreSQL / Redis 已安装并完成当前 schema 初始化；正式切换前按上线计划重新导出最终 SQLite 业务 JSON，完成离线转换 / 导入演练、PG HTTP readiness、维护窗口切换和回滚演练。
