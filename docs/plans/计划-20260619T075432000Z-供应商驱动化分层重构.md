# PLAN-20260619T075432000Z 供应商驱动化分层重构

## 基本信息

- 计划 ID：PLAN-20260619T075432000Z
- 状态：进行中
- 创建时间：2026-06-19
- 更新时间：2026-06-19
- 需求来源：用户对话
- 执行者：AI
- 关联模块：后端 / 前端 / 存储 / 网关 / 模型目录 / 使用记录 / 文档 / 验证

## 需求目标

- 背景：Anthropic、GPT 和 OpenAI-compatible 逻辑在网关、账号、计费、统计和前端中多处混写，后续新增供应商会继续放大维护成本。
- 目标：建立供应商驱动化标准接口，让通用层只关心认证、分组、调度、审计和记录；供应商只实现自己的协议、凭据、模型、计费和前端能力。
- 交付物：架构规范、计划文档、后端 driver registry 第一批落地、分组 profile 校验修复、受影响回归验证记录。

## 范围边界

### 本次包含

- [x] 新增供应商驱动分层规范文档。
- [x] 新增 `ProviderDriver` / `ProtocolDriver` / usage-cost-model 标准接口骨架。
- [x] 将账号请求能力过滤切到 provider driver registry。
- [x] 将模型计费目录的供应商候选、协议推断、价格来源和 cache-read 计费口径切到 pricing provider driver。
- [x] 将账号凭据 endpoint modes 写入归一、usage 记账模型和 GPT OAuth dispatch 准备生命周期切到 driver / registry。
- [x] 将上游错误 payload 解析切到 protocol driver，错误策略、使用记录和响应审计按账号协议读取 code / type / message。
- [x] 将前端账户表单、测试入口、测试展示、OAuth 用量条和模型加载默认供应商判断切到 account provider capability adapter。
- [x] 将模型检测账户选择和后端检测协议边界切到 model-check provider capability helper，OpenAI-compatible 不再被前端 GPT 供应商名挡住。
- [x] 将网关账号副作用的合并、取消和健康成功跳过策略从队列服务拆到独立 policy 文件。
- [x] 复核 usage-stats 统计展示供应商口径，并补静态回归防止统计页直接依赖 GPT / OpenAI / Anthropic 常量。
- [x] 修复账号绑定分组时只校验 `providerCode`、未校验 `providerProtocolProfileId` 的缺口。
- [x] 补充本轮最小回归测试和验证记录。
- [x] 更新计划进度与阶段总结。

### 后续阶段

- OpenAI 历史命名中性化：`OpenAIAccountSecret`、`openai-account-selector`、runtime cache、DB service IPC 和最终诊断响应的 `UpstreamAttempt` 协议档案另开专项处理。

### 本次不包含

- 不一次性重命名所有 OpenAI 历史文件：先建立新入口并切高价值调用点，避免大规模 diff 难以验证。
- 不新增数据库迁移逻辑：本轮不改 schema。
- 不实现新供应商真实接入：只为后续接入建立标准和第一批隔离点。

## 关联文档

- 架构文档：`docs/architecture/架构总览.md`
- 架构规范：`docs/architecture/供应商驱动分层规范.md`
- 请求分层：`docs/functions/请求处理分层设计.md`
- Anthropic 接入：`docs/functions/Anthropic账号接入.md`
- 对比调研：`docs/functions/Anthropic与GPT全链路能力对比.md`
- 验证手册：`docs/develop/测试与验证说明.md`

## 方案概述

- 方案原则：通用层依赖 registry，具体供应商落到独立 driver 文件；协议、供应商、客户端画像和 usage/cost/model 分开。
- 数据变化：本轮不改 schema；只修复已有 `provider_protocol_profile_id` 校验使用方式。
- 接口变化：不改外部 HTTP API；内部新增 driver 类型和 registry。
- 前端变化：账户页、模型检测页和 provider 模型 formatter 已切到 capability adapter / helper / 分类规则文件。
- 后端变化：新增 provider driver 骨架，账号请求能力过滤先通过 driver 判定。
- 数据处理策略：不处理旧数据；如果已有混绑数据，后续按用户要求提供离线检查 SQL。

## 执行拆解

- [x] 新增 `docs/architecture/供应商驱动分层规范.md`
- [x] 更新 `docs/architecture/README.md` 和 `docs/plans/README.md`
- [x] 新增 provider driver 类型、registry 和内置 OpenAI-compatible / GPT / Anthropic driver
- [x] 新增 protocol driver 类型、registry 和内置 OpenAI v1 / Anthropic v1 driver
- [x] 修改 `account-capability-filter` 通过 driver 判断请求能力
- [x] 修改 `/models` 请求识别通过 protocol driver registry
- [x] 修改主调度上游 URL 构造通过 provider driver registry
- [x] 修改主调度上游请求头 / 请求体构造通过 provider driver registry
- [x] 修改返回侧 JSON 语义帧、usage 解析和流式 usage 兜底通过 protocol driver registry
- [x] 修改本地网关错误响应协议和 endpoint capability failure 判断通过 protocol driver registry
- [x] 修改请求 / 路由层本地错误响应协议、`/models` sender 选择和 provider driver native request 识别通过 protocol driver registry
- [x] 修改 `response/stream.ts` 的 stream inspector factory、SSE 语义帧提取、失败事件注入策略和 terminal drain 策略通过 protocol driver registry
- [x] 补齐 Anthropic 当前有效模型 / Claude Code 别名 / Antigravity 隐藏计价别名的回归约束，并按官方 retirement date 过滤 Mythos preview。
- [x] 新增 `model-pricing` provider driver 类型和 registry，并把内置 OpenAI-compatible / GPT / Anthropic 计价数据、模型候选、release date、协议推断和 cache-read 口径收敛到 driver。
- [x] 修改 `model-pricing.service` / `model-catalog.service`，通用计算层不再直接导入 Anthropic / OpenAI pricing data 或 provider 常量。
- [x] 新增 provider account credential driver registry，账号凭据写入归一不再由 storage 直接判断 Anthropic / OpenAI endpoint modes。
- [x] 扩展 provider driver 的 usage 记账接口，`gateway/usage/records.ts` 不再直接调用 OpenAI 模型映射或 Anthropic/GPT 常量。
- [x] 新增 GPT provider dispatch 准备钩子，OpenAI OAuth access token 热路径刷新 / 预热不再写在通用 dispatch 准备层。
- [x] 扩展 protocol driver 的错误 payload 解析接口，`failure-dispatch`、`finalization`、`usage/records` 和 `account-error-policy` 不再直接解析供应商错误结构。
- [x] 新增前端 `accountProviderCapabilities` adapter，并让账户表单、保存校验、草稿测试、测试弹窗、测试输出和 OAuth 用量展示通过 adapter 判断 provider/profile 能力。
- [x] 新增前后端模型检测 provider capability helper，并让模型检测账户选项、可信对比协议边界和文案通过能力 helper 表达。
- [x] 新增 provider 模型分类规则文件，`providerModelFormatters` 不再内联 GPT / Claude / 图像 / 音频模型名前缀规则。
- [x] 新增账号副作用 policy 文件，合并、取消和健康成功跳过策略不再内联在 side-effect queue service。
- [x] 补 usage-stats 供应商展示静态回归，统计页不直接导入供应商常量或协议判断。
- [x] 修复 `setAccountGroup` 的 profile 校验
- [x] 补充 / 更新回归脚本
- [x] 运行后端类型检查和关键回归
- [x] 回填验证记录与阶段总结

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 后端类型检查 | `pnpm --filter juhe-ai-backend typecheck` | 新增 driver 类型和引用无 TS 错误 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 分组 profile 绑定校验 | `pnpm --filter juhe-ai-backend test:group-scheduling-policy` | 不同 `providerProtocolProfileId` 的账号不能绑定到当前分组 | 已通过 | 2026-06-19 通过，已补 synthetic profile 回归 |
| 回归场景 | API Key 分组能力过滤 | `pnpm --filter juhe-ai-backend test:api-key-group-route-capability` | 请求能力过滤和跨分组 fallback 行为不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | OpenAI endpoint mode 能力 | `pnpm --filter juhe-ai-backend exec tsx src/scripts/regression/openai-endpoint-mode-capability-regression.ts` | OpenAI-compatible 能力矩阵、默认值和候选账号过滤不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | OpenAI API Key 透传和 URL 构造 | `pnpm --filter juhe-ai-backend test:openai-api-key-passthrough` | OpenAI URL normalization、Codex compact URL 和请求体透传不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | GPT OAuth Codex adapter | `pnpm --filter juhe-ai-backend test:openai-oauth-codex-adapter` | OAuth Codex 请求构造、compact 判断和 header/body 归一不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | Anthropic 网关能力 | `pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai` | Anthropic Messages / count_tokens / usage 语义不回退 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 账户 API Key 调度 | `pnpm --filter juhe-ai-backend test:account-api-key-gateway-mock-ai` | OpenAI-compatible / GPT API Key 调度不受 driver registry 改造影响 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 账户内 API Key 失败保护 | `pnpm --filter juhe-ai-backend test:account-api-key-failure-guard` | 账户内 Key 短避让和全局状态写入边界不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 账号错误策略解析 | `pnpm --filter juhe-ai-backend test:account-error-policy-validation` | Anthropic 官方 `error.type` 可通过 protocol driver 参与错误策略匹配 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 上游失败切号 | `pnpm --filter juhe-ai-backend test:upstream-request-failure` | 非成功响应、原地重试、切号和运行态屏障不受错误解析驱动化影响 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 模型目录 | `pnpm --filter juhe-ai-backend test:model-catalog` | 新增模型 / 计费驱动类型不影响当前 catalog 行为 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 用量计价 | `pnpm --filter juhe-ai-backend test:usage-pricing` | Claude / Antigravity 别名计价、退休模型过滤和 pricing driver 静态约束不回退 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 使用记录目录窗口 | `pnpm --filter juhe-ai-backend test:usage-record-list-catalog-window` | 使用记录列表只按当前页窗口补取 catalog 明细 | 已通过 | 2026-06-19 通过 |
| 回归场景 | endpoint modes 写入归一 | `pnpm --filter juhe-ai-backend test:openai-endpoint-modes` | OpenAI-compatible / GPT / Anthropic 默认接口能力由 credential driver 输出且行为不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | OAuth 热路径刷新 | `pnpm --filter juhe-ai-backend test:openai-oauth-refresh-hot-path-cache` | GPT OAuth 临期预热、过期阻塞刷新和并发缓存不回退 | 已通过 | 2026-06-19 通过 |
| 回归场景 | OAuth 后台保活 | `pnpm --filter juhe-ai-backend test:openai-oauth-access-token-refresh` | 后台保活刷新、连续失败异常标记和单进程 worker 回归路径不回退 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 账号副作用队列上限 | `pnpm --filter juhe-ai-backend test:gateway-account-side-effect-queue-limit` | 副作用 policy 拆分后合并、取消、健康成功跳过和队列上限行为不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 账号副作用优先队列 | `pnpm --filter juhe-ai-backend test:gateway-account-side-effect-queue` | 副作用队列堆序语义不受 policy 拆分影响 | 已通过 | 2026-06-19 通过 |
| 命令类验证 | 前端类型检查 | `pnpm --filter juhe-ai-frontend typecheck` | account provider capability adapter 类型无错误 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端账户测试协议 | `pnpm --filter juhe-ai-frontend test:account-test-protocols` | OpenAI / Anthropic 单协议测试入口不回退，跨供应商协议选择不加载单一模型目录 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端账户测试模型 | `pnpm --filter juhe-ai-frontend test:account-test-models` | 测试模型加载不再在混合供应商选择时回落到默认 GPT 供应商 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端账户用量展示 | `pnpm --filter juhe-ai-frontend test:account-usage-formatters` | OAuth 用量条按能力展示且格式不变 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端账户保存流程 | `pnpm --filter juhe-ai-frontend test:account-edit-save-flow` | 保存 / OAuth / API Key 分流不受 capability adapter 影响 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端账户测试展示 | `pnpm --filter juhe-ai-frontend test:account-test-display-formatters` | 单账号 / 批量测试输出文案不回退 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端模型检测供应商能力 | `pnpm --filter juhe-ai-frontend test:model-check-provider-capabilities` | OpenAI-compatible 可进入 OpenAI v1 模型检测，Anthropic 原生仍隔离 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端供应商模型 formatter | `pnpm --filter juhe-ai-frontend test:provider-model-formatters` | 模型分类规则拆分；自定义文本模型默认协议优先来自当前供应商协议档案 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端供应商模型目录操作 | `pnpm --filter juhe-ai-frontend test:provider-model-source-operation` | 聚合目录自定义模型编辑 / 删除仍按来源供应商路由 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端用量统计供应商展示 | `pnpm --filter juhe-ai-frontend test:usage-stats-provider-display` | 用量统计页不直接依赖 GPT / OpenAI / Anthropic 常量，只通过 providerDisplayName 展示供应商 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 前端账号基础与 OAuth 菜单能力 | `pnpm --filter juhe-ai-frontend test:account-basic-formatters` | OAuth 刷新 / 重新授权菜单通过通用 OAuth 管理能力判断，不继续使用 GPT 命名 | 已通过 | 2026-06-19 通过 |
| 静态边界 | 供应商边界源码回归 | `pnpm --filter juhe-ai-backend test:provider-boundary-source` | 账号 OAuth 管理、用量断言和模型检测错误文案不回退到旧 GPT 口径 | 已通过 | 2026-06-19 通过 |
| 回归场景 | OpenAI Responses 兼容恢复 | `pnpm --filter juhe-ai-backend test:gateway-compatibility-recovery` | encrypted reasoning 清洗重试仍正常，且恢复策略已有 OpenAI v1 协议门禁 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 模型检测可信对比能力边界 | `pnpm --filter juhe-ai-backend test:model-check-trusted-comparison` | 可信对比协议边界通过 model-check provider capability helper 表达，文案不再绑定 GPT 供应商名 | 已通过 | 2026-06-19 通过 |
| 回归场景 | 模型检测不可用边界 | `pnpm --filter juhe-ai-backend test:model-check-unavailable-boundary` | 无效目标、不可检测账户、上游失败和代理失败边界不回退 | 已通过 | 2026-06-19 通过 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-06-19 | 进行中 | AI | 创建计划；确定采用 `ProtocolDriver + ProviderDriver + Usage/Cost/Model driver + Frontend capability adapter`，第一批先落文档、registry、请求能力过滤和分组 profile 校验。 |
| 2026-06-19 | 进行中 | AI | 第一批代码落地：新增 provider driver registry 和接口骨架，`account-capability-filter` 改为通过 driver 判断，`setAccountGroup` 补齐 `providerProtocolProfileId` 硬边界；关键回归通过。 |
| 2026-06-19 | 进行中 | AI | 第二批代码落地：主调度 `upstream-dispatch` 改用 `buildGatewayUpstreamUrlsForAccount`，OpenAI route helper 不再承载 Anthropic URL 分支；OpenAI passthrough、Anthropic mock 和 API Key group capability 回归通过。 |
| 2026-06-19 | 进行中 | AI | 第三批代码落地：主调度 `account-preparation` 改用 `buildGatewayUpstreamRequestParts`，OpenAI-compatible / GPT / Anthropic 请求头和请求体构造下沉到 provider driver；删除 `gateway/upstream/request.ts` 旧混写 `buildUpstreamRequestParts`。 |
| 2026-06-19 | 进行中 | AI | 第四批代码落地：新增 OpenAI v1 / Anthropic v1 protocol driver registry，`preflight` 的 `/models` 请求识别改走 `isGatewayProtocolModelsRequest`；模型目录、Anthropic mock 和 API Key group capability 回归通过。 |
| 2026-06-19 | 进行中 | AI | 第五批代码落地：`ProtocolDriver` 增加响应协议、默认客户端 profile、JSON 语义帧、usage 解析、流式 usage 兜底、本地错误响应协议和 endpoint capability failure 能力；`finalization`、`non-stream-json-inspection`、`failure-dispatch`、`failure-response` 切到 registry。 |
| 2026-06-19 | 进行中 | AI | 第六批代码落地：核对 Anthropic 官方模型 / Claude Code 别名，补 `claude-mythos-preview` retirement date，强化模型目录和 usage pricing 回归；`claude-opus-4-6-antigravity` 等 Antigravity 兼容别名继续作为隐藏计价模型直接命中但不进入公开模型发现目录。 |
| 2026-06-19 | 进行中 | AI | 第七批代码落地：`request/error-response`、`request/pre-auth`、`routes`、`preflight`、OpenAI-compatible / GPT provider driver 的本地协议判断切到 protocol registry；剩余 Anthropic direct helper 只保留在 Anthropic provider / protocol 实现边界。 |
| 2026-06-19 | 进行中 | AI | 第八批代码落地：`ProtocolDriver` 增加 stream inspector、SSE semantic frames、response inspection failure event 和 terminal drain 配置；`response/stream.ts` 不再直接 import OpenAI / Anthropic stream inspector；顺手修复 OAuth 路由 helper 作用域导致的后端 typecheck 失败。 |
| 2026-06-19 | 进行中 | AI | 第九批代码落地：新增 `model-pricing` provider driver registry，把 OpenAI-compatible / GPT / Anthropic 的 raw pricing、模型候选、release date、API 协议推断和 cache-read 口径下沉到 driver；`model-pricing.service` / `model-catalog.service` 仅保留通用计费计算。 |
| 2026-06-19 | 进行中 | AI | 第十批代码落地：新增 account credential driver registry，`account-credentials-normalization` 只通过 registry 归一 endpoint modes；OpenAI-compatible / GPT / Anthropic 默认能力行为保持不变。 |
| 2026-06-19 | 进行中 | AI | 第十一批代码落地：扩展 provider driver usage 记账接口，`gateway/usage/records.ts` 的 usage semantic、upstream model、model mapping source 和 local failure 默认 provider 改走 provider driver facade；`fixed-responses` 的 `/models` 记录同步补 usage semantic。 |
| 2026-06-19 | 进行中 | AI | 第十二批代码落地：新增 GPT provider `prepareAccountBeforeDispatch` 钩子，把 OpenAI OAuth access token 热路径刷新 / 预热从通用 dispatch 准备层迁出；同时修复单进程 worker 回归环境下 OAuth 后台保活 DB service fallback。 |
| 2026-06-19 | 进行中 | AI | 第十三批代码落地：扩展 protocol driver 的错误 payload 解析能力，OpenAI / Anthropic 分文件实现 code / type / message 提取；`failure-dispatch`、`finalization`、`usage/records` 和账号错误策略改走 registry。 |
| 2026-06-19 | 进行中 | AI | 第十四批代码落地：新增前端 account provider capability adapter，账户表单 OAuth 判断、endpoint modes 默认值、保存校验、草稿测试、测试弹窗、测试输出、OAuth 用量条和测试模型默认供应商回落不再直接散落 GPT / Anthropic 判断。 |
| 2026-06-19 | 进行中 | AI | 第十五批代码落地：新增模型检测 provider capability helper，前端账户选项不再用 GPT 供应商名过滤 OpenAI v1 账户；后端检测协议边界和可信对比文案通过 helper 表达。 |
| 2026-06-19 | 进行中 | AI | 第十六批代码落地：新增 provider 模型分类规则文件，`providerModelFormatters` 不再内联 GPT / Claude / 图像 / 音频模型名前缀分类规则，保留原有 formatter 导出兼容调用方。 |
| 2026-06-19 | 进行中 | AI | 第十七批代码落地：新增账号副作用 policy 文件，把错误副作用 LWW 合并、成功取消和健康成功跳过策略从 side-effect queue service 中拆出；队列服务只保留排队、重试和执行。 |
| 2026-06-19 | 进行中 | AI | 第十八批复核：usage-stats 统计页只通过通用 `providerDisplayName` 展示供应商，不直接持有 GPT / OpenAI / Anthropic 常量；新增静态回归锁住该边界。 |
| 2026-06-19 | 进行中 | AI | 第十九批深度复查修复：账号 OAuth 刷新 / 重新授权能力函数从 `canManageGptOAuth` 中性化为 `canManageOAuthAccount`，用户提示不再绑定 GPT；模型检测可信对比错误改写去除硬编码 OpenAI v1 文案；新增 `provider-boundary-source` 静态边界回归。 |
| 2026-06-19 | 进行中 | AI | 第二十批双 Agent 深度复查修复：OpenAI Responses 兼容恢复重试增加 OpenAI v1 账户门禁；前端跨供应商协议批量测试改为拦截且模型加载不再回落默认 GPT；自定义模型默认接口协议优先使用当前 provider profile；OAuth 创建能力收紧为显式 GPT OAuth flow。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-06-19 | 不做单一巨大 `ProviderDriver` | 协议形态、供应商账号、模型计费和前端展示变化频率不同，强行合并会产生新耦合 | 拆成多个 registry，通用层只通过稳定接口编排 |
| 2026-06-19 | 第一批不做全量重命名 | 现有工作区改动多，且网关主链路影响面大 | 先加 facade 和新入口，再逐步迁移调用点 |

## 验收标准

- [x] 新供应商接入标准有长期文档承接。
- [x] 后端存在可复用 provider driver registry，至少覆盖当前三个内置档案。
- [x] 后端存在可复用 protocol driver registry，至少覆盖当前 OpenAI v1 和 Anthropic v1。
- [x] 请求能力过滤不再直接在核心 filter 中手写 OpenAI / Anthropic endpoint mode 分支。
- [x] `/models` 请求识别不再由 preflight 直接手写 OpenAI / Anthropic helper 分支。
- [x] 主调度上游 URL 构造不再通过 OpenAI route helper 混合处理 Anthropic。
- [x] 主调度上游 header/body 构造不再通过通用 upstream 文件混合处理 OpenAI OAuth、OpenAI API Key 和 Anthropic API Key。
- [x] 返回侧 JSON 响应检查和 usage 解析不再由 response 层手写 OpenAI / Anthropic 解析分支。
- [x] 流式 usage 缺失兜底和本地网关错误响应协议不再由 response 层直接依赖 Anthropic helper。
- [x] endpoint capability failure 判断下沉到 protocol driver，避免失败重生 / 切号副作用误污染账号状态。
- [x] 请求 / 路由层本地错误响应协议选择不再直接导入 Anthropic route helper。
- [x] OpenAI-compatible / GPT provider driver 不再直接导入 Anthropic route helper，只通过 protocol registry 判断 native request。
- [x] `response/stream.ts` 不再直接导入 `OpenAIStreamInspector`、`AnthropicStreamInspector` 或 Anthropic SSE semantic helper。
- [x] Anthropic 模型目录覆盖当前有效 Claude / Claude Code 别名；兼容代理 / Antigravity 别名可计价但不污染公开模型发现目录。
- [x] `model-pricing.service` 和 `model-catalog.service` 不再直接导入 Anthropic / OpenAI pricing data 或 Anthropic provider 常量，模型候选、协议推断、价格来源和 cache-read 口径通过 pricing provider driver 获取。
- [x] `account-credentials-normalization` 不再直接导入 OpenAI / Anthropic endpoint mode 写入 helper 或 Anthropic 协议判断。
- [x] `gateway/usage/records.ts` 不再直接导入 `resolveOpenAIAccountModelMapping`、`ANTHROPIC_PROVIDER_CODE` 或 `GPT_VENDOR_CODE`。
- [x] `gateway/dispatch/account-preparation.ts` 不再直接导入 OpenAI OAuth refresh service，GPT OAuth 生命周期通过 provider driver hook 接入。
- [x] 上游失败响应、使用记录失败项和账号错误策略的错误 payload 解析通过 protocol driver 完成，通用错误策略服务不再直接 `JSON.parse` 供应商错误结构。
- [x] 前端账户表单、测试入口、测试展示、OAuth 用量条和测试模型默认供应商回落通过 account provider capability adapter 判断，不再在这些文件里散落 GPT / Anthropic 分支。
- [x] 模型检测页面通过 model-check provider capability helper 判断 OpenAI v1 检测能力，OpenAI-compatible 不再被 GPT 供应商名过滤，Anthropic 原生仍按当前能力边界隔离。
- [x] Provider 模型 formatter 不再内联模型名前缀分类规则，分类规则集中在独立文件，调用方保留原导出入口。
- [x] 账号副作用队列服务不再内联合并、取消和健康成功跳过策略，相关策略集中在 `account-side-effect-policy.ts`。
- [x] Usage 统计展示页不直接依赖 GPT / OpenAI / Anthropic 常量或协议判断。
- [x] 账号 OAuth 刷新 / 重新授权菜单不再使用 GPT 命名能力函数或 GPT 绑定提示。
- [x] OAuth 创建流程不会因 OpenAI-compatible 供应商声明 `oauth` 账户类型而误调用 GPT / OpenAI OAuth API。
- [x] 前端批量账户测试不会把 GPT 默认模型用于 Anthropic 等其他供应商协议账户。
- [x] 自定义模型默认接口协议优先来自当前供应商协议档案，Anthropic 文本模型不再默认填 OpenAI Responses / Chat。
- [x] OpenAI Responses encrypted reasoning 兼容恢复重试只对 OpenAI v1 协议账户生效。
- [x] 账号分组绑定必须以 `provider_protocol_profile_id` 为硬边界。
- [x] 第一批关键回归通过。
- [ ] Provider request capability mismatch 的特殊原因从中心 registry 下沉到 provider / protocol hook。
- [ ] GPT / OpenAI-compatible provider driver 不再直接知道 Anthropic native request 排除规则。
- [ ] 账号测试 / 草稿测试模块的 OpenAI / Anthropic 请求生成、响应解析和草稿 runtime secret 构造下沉到 account-test provider driver。
- [ ] 前端 endpoint modes 枚举、表单选项和保存校验进一步改为 provider profile / capability 驱动。
- [ ] 模型检测探针体系从 OpenAI v1 固定实现拆为 probe driver。
- [ ] API Key 池能力从 storage 层供应商判断迁到 provider credential capability。
- [ ] OpenAI 历史命名中性化后续阶段完成。

## 验证记录

- 类型检查：已执行，命令：`pnpm --filter juhe-ai-backend typecheck`，结果通过。
- 单项验证：已执行，命令：`pnpm --filter juhe-ai-backend test:group-scheduling-policy`，结果通过。
- 网关回归：已执行，命令：`pnpm --filter juhe-ai-backend test:api-key-group-route-capability`，结果通过。
- OpenAI endpoint mode 回归：已执行，命令：`pnpm --filter juhe-ai-backend exec tsx src/scripts/regression/openai-endpoint-mode-capability-regression.ts`，结果通过。
- OpenAI API Key 透传回归：已执行，命令：`pnpm --filter juhe-ai-backend test:openai-api-key-passthrough`，结果通过。
- 返回侧 protocol driver 化回归：已执行，命令：`pnpm --filter juhe-ai-backend typecheck`、`pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai`、`pnpm --filter juhe-ai-backend test:openai-api-key-passthrough`、`pnpm --filter juhe-ai-backend test:api-key-group-route-capability`，结果通过。
- Stream protocol driver 化回归：已执行，命令：`pnpm --filter juhe-ai-backend typecheck`、`pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai`、`pnpm --filter juhe-ai-backend test:response-inspection-policy`、`pnpm --filter juhe-ai-backend test:openai-api-key-passthrough`、`pnpm --filter juhe-ai-backend test:claude-code-client-strategy`、`pnpm --filter juhe-ai-backend test:stream-event-token-estimate-boundary`、`pnpm --filter juhe-ai-backend test:usage-pricing`，结果通过。
- GPT OAuth Codex adapter 回归：已执行，命令：`pnpm --filter juhe-ai-backend test:openai-oauth-codex-adapter`，结果通过。
- Anthropic 回归：已执行，命令：`pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai`，结果通过。
- API Key 调度回归：已执行，命令：`pnpm --filter juhe-ai-backend test:account-api-key-gateway-mock-ai`，结果通过。
- API Key 失败保护回归：已执行，命令：`pnpm --filter juhe-ai-backend test:account-api-key-failure-guard`，结果通过。
- 账号错误策略回归：已执行，命令：`pnpm --filter juhe-ai-backend test:account-error-policy-validation`，结果通过；已覆盖 Anthropic 官方 `error.type` 通过 protocol driver 参与 error code 匹配。
- 上游失败切号回归：已执行，命令：`pnpm --filter juhe-ai-backend test:upstream-request-failure`，结果通过；确认非成功响应、原地重试、切号和运行态屏障不回退。
- Claude Code 客户端画像回归：已执行，命令：`pnpm --filter juhe-ai-backend test:claude-code-client-strategy`，结果通过。
- 模型目录回归：已执行，命令：`pnpm --filter juhe-ai-backend test:model-catalog`，结果通过；已覆盖当前有效 Claude / Claude Code 别名和 Anthropic hidden pricing alias 边界。
- 用量计价回归：已执行，命令：`pnpm --filter juhe-ai-backend test:usage-pricing`，结果通过；已覆盖 Claude / Antigravity 兼容别名直接计价、退休模型过滤、response driver 化和 pricing driver 化静态约束。
- 使用记录目录窗口回归：已执行，命令：`pnpm --filter juhe-ai-backend test:usage-record-list-catalog-window`，结果通过；确认使用记录列表只读取固定 entry 窗口并按当前页补取 catalog shard 明细。
- endpoint modes 写入归一回归：已执行，命令：`pnpm --filter juhe-ai-backend test:openai-endpoint-modes`，结果通过；已覆盖 account credential driver 默认能力和 storage 归一边界。
- OAuth 热路径刷新回归：已执行，命令：`pnpm --filter juhe-ai-backend test:openai-oauth-refresh-hot-path-cache`，结果通过；已覆盖 GPT OAuth dispatch 准备钩子的临期预热、过期阻塞刷新和并发缓存。
- OAuth 后台保活回归：已执行，命令：`pnpm --filter juhe-ai-backend test:openai-oauth-access-token-refresh`，结果通过；已覆盖单进程 worker 回归环境 fallback 和连续失败异常标记。
- 账号副作用队列上限回归：已执行，命令：`pnpm --filter juhe-ai-backend test:gateway-account-side-effect-queue-limit`，结果通过；已覆盖副作用 policy 拆分后的合并、取消、健康成功跳过和队列上限行为。
- 账号副作用优先队列回归：已执行，命令：`pnpm --filter juhe-ai-backend test:gateway-account-side-effect-queue`，结果通过。
- 前端类型检查：已执行，命令：`pnpm --filter juhe-ai-frontend typecheck`，结果通过。
- 前端账户测试协议回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:account-test-protocols`，结果通过；已确认 OpenAI / Anthropic 单协议测试入口不回退，跨供应商协议选择不会加载单一模型目录。
- 前端账户测试模型回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:account-test-models`，结果通过；已确认混合供应商选择不再回落默认 GPT 模型目录。
- 前端账户用量展示回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:account-usage-formatters`，结果通过。
- 前端账户保存流程回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:account-edit-save-flow`，结果通过。
- 前端账户测试展示回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:account-test-display-formatters`，结果通过。
- 前端账户基础 formatter 回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:account-basic-formatters`，结果通过；已确认基础文案、门面导出和 OAuth 刷新 / 重新授权菜单能力判断不回退。
- 前端模型检测供应商能力回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:model-check-provider-capabilities`，结果通过；已确认 OpenAI-compatible 可进入 OpenAI v1 检测，Anthropic 原生仍隔离。
- 前端供应商模型 formatter 回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:provider-model-formatters`，结果通过；已确认模型分类规则从 formatter 主文件拆出，且 Anthropic 文本自定义模型默认协议来自 provider profile。
- 前端供应商模型目录操作回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:provider-model-source-operation`，结果通过。
- 前端用量统计供应商展示回归：已执行，命令：`pnpm --filter juhe-ai-frontend test:usage-stats-provider-display`，结果通过；已确认 usage-stats 统计页不直接依赖 GPT / OpenAI / Anthropic 常量。
- 供应商边界源码回归：已执行，命令：`pnpm --filter juhe-ai-backend test:provider-boundary-source`，结果通过；已确认账号 OAuth 管理、用量断言和模型检测错误改写不回退到旧 GPT / OpenAI 硬编码口径。
- OpenAI Responses 兼容恢复回归：已执行，命令：`pnpm --filter juhe-ai-backend test:gateway-compatibility-recovery`，结果通过；已确认 encrypted reasoning 清洗重试不回退，且恢复策略已有 OpenAI v1 协议门禁。
- 模型检测可信对比回归：已执行，命令：`pnpm --filter juhe-ai-backend test:model-check-trusted-comparison`，结果通过；已确认检测协议边界和可信对比文案不再绑定 GPT 供应商名。
- 模型检测不可用边界回归：已执行，命令：`pnpm --filter juhe-ai-backend test:model-check-unavailable-boundary`，结果通过。

## 风险与注意事项

- 网关请求能力过滤位于调度前，任何默认值变化都会影响跨分组 fallback。
- `providerProtocolProfileId` 修复可能暴露既有错误数据；本项目默认不在运行路径写旧数据兼容。
- 上游请求和响应拆分必须分阶段推进，避免在一次改动中同时改变 URL、header、SSE、usage 和错误副作用。

## 完成总结

- 完成时间：整体计划仍进行中；2026-06-19 已推进至第二十批。
- 实际完成内容：第一批完成文档标准、provider driver registry、请求能力过滤 driver 化、账号分组 profile 校验和回归验证；第二批完成主调度上游 URL provider driver 化；第三批完成主调度上游请求头 / 请求体构造 provider driver 化，并删除旧混写入口；第四批完成 protocol driver registry 和 `/models` 请求识别 driver 化；第五批完成返回侧 JSON 语义、usage 解析、流式 usage 兜底、本地错误响应协议和 endpoint capability failure 的 protocol driver 化；第六批补齐 Anthropic 当前模型 / Claude Code 别名 / Antigravity 隐藏计价别名的测试边界和 Mythos preview 到期过滤；第七批把 request / routes / preflight / OpenAI-compatible / GPT provider driver 的 Anthropic native 判断进一步收敛到 protocol registry；第八批把 `response/stream.ts` 的 stream inspector factory 和 SSE semantic frames 提取下沉到 protocol driver；第九批把 `model-pricing` / `model-catalog` 的供应商计价差异下沉到 pricing provider driver；第十批把账号凭据 endpoint modes 写入归一下沉到 account credential driver；第十一批把 usage 记录的语义和模型记账下沉到 provider driver facade；第十二批把 GPT OAuth dispatch 准备生命周期下沉到 GPT provider driver hook；第十三批把上游错误 payload 解析下沉到 protocol driver；第十四批把前端账户页核心 provider 判断下沉到 account provider capability adapter；第十五批把模型检测页面和后端检测协议边界下沉到 model-check provider capability helper；第十六批把 provider 模型分类规则从 formatter 主文件拆到独立规则文件；第十七批把账号副作用合并、取消和健康成功跳过策略从队列服务拆到独立 policy 文件；第十八批复核 usage-stats 统计展示并补静态回归；第十九批深度复查修复账号 OAuth 管理菜单命名、模型检测错误文案硬编码和供应商边界源码回归；第二十批修复 OpenAI 兼容恢复误作用到非 OpenAI 协议、跨供应商批量测试模型错配、Anthropic 自定义文本模型默认协议和 OAuth flow 能力误开放风险。
- 主要改动位置：`docs/architecture/供应商驱动分层规范.md`、`backend/src/modules/providers/drivers/`、`backend/src/modules/providers/drivers/account-credentials.registry.ts`、`backend/src/modules/providers/drivers/gpt/oauth-dispatch-preparation.ts`、`backend/src/modules/gateway/protocols/registry.ts`、`backend/src/modules/gateway/protocols/_shared/types.ts`、`backend/src/modules/gateway/protocols/_shared/error-payload.ts`、`backend/src/modules/gateway/protocols/openai-v1/driver.ts`、`backend/src/modules/gateway/protocols/openai-v1/error-payload.ts`、`backend/src/modules/gateway/protocols/anthropic-v1/driver.ts`、`backend/src/modules/gateway/protocols/anthropic-v1/error-payload.ts`、`backend/src/modules/gateway/dispatch/account-capability-filter.ts`、`backend/src/modules/gateway/dispatch/upstream-dispatch.ts`、`backend/src/modules/gateway/dispatch/account-preparation.ts`、`backend/src/modules/gateway/upstream/request.ts`、`backend/src/modules/gateway/upstream/error-helpers.ts`、`backend/src/modules/gateway/request/preflight.ts`、`backend/src/modules/gateway/request/pre-auth.ts`、`backend/src/modules/gateway/request/error-response.ts`、`backend/src/modules/gateway/routes.ts`、`backend/src/modules/gateway/client-profiles/strategy.ts`、`backend/src/modules/gateway/usage/records.ts`、`backend/src/modules/gateway/policy/account-error-policy.service.ts`、`backend/src/modules/gateway/response/fixed-responses.ts`、`backend/src/modules/gateway/response/stream.ts`、`backend/src/modules/gateway/response/finalization.ts`、`backend/src/modules/gateway/response/non-stream-json-inspection.ts`、`backend/src/modules/gateway/response/failure-dispatch.ts`、`backend/src/modules/gateway/response/failure-response.ts`、`backend/src/modules/gateway/runtime/account-side-effect-policy.ts`、`backend/src/modules/gateway/runtime/account-side-effects.service.ts`、`backend/src/modules/model-pricing/provider-driver.types.ts`、`backend/src/modules/model-pricing/provider-driver.registry.ts`、`backend/src/modules/model-pricing/model-pricing.service.ts`、`backend/src/modules/model-pricing/model-catalog.service.ts`、`backend/src/modules/model-pricing/anthropic-model-pricing.data.ts`、`backend/src/modules/model-checks/model-checks.provider-capabilities.ts`、`backend/src/modules/model-checks/model-checks.service.ts`、`backend/src/modules/openai-oauth/openai-oauth.routes.ts`、`backend/src/modules/openai-oauth/openai-oauth-access-token-refresh.service.ts`、`backend/src/scripts/regression/model-catalog-regression.ts`、`backend/src/scripts/regression/usage-pricing-regression.ts`、`backend/src/scripts/regression/account-error-policy-validation-regression.ts`、`backend/src/scripts/regression/openai-endpoint-mode-capability-regression.ts`、`backend/src/scripts/regression/model-check-trusted-comparison-regression.ts`、`backend/src/scripts/regression/gateway-account-side-effect-queue-limit-regression.ts`、`backend/src/storage/account-credentials-normalization.ts`、`backend/src/storage/account-group-binding-write.repository.ts`、`backend/src/scripts/regression/group-scheduling-policy-regression.ts`、`backend/src/scripts/regression/openai-api-key-passthrough-regression.ts`、`frontend/src/views/accounts/accountProviderCapabilities.ts`、`frontend/src/views/accounts/accountEndpointModes.ts`、`frontend/src/views/accounts/accountFormDefaults.ts`、`frontend/src/views/accounts/accountEditFormLoaders.ts`、`frontend/src/views/accounts/accountSavePayload.ts`、`frontend/src/views/accounts/useAccountEditForm.ts`、`frontend/src/views/accounts/useAccountEditSaveFlow.ts`、`frontend/src/views/accounts/useAccountEditTestAction.ts`、`frontend/src/views/accounts/useAccountTestModal.ts`、`frontend/src/views/accounts/useAccountTestModels.ts`、`frontend/src/views/accounts/AccountEditModal.vue`、`frontend/src/views/accounts/AccountStrategySection.vue`、`frontend/src/views/accounts/AccountTestModal.vue`、`frontend/src/views/accounts/accountTestFlow.ts`、`frontend/src/views/accounts/accountTestDisplayFormatters.ts`、`frontend/src/views/accounts/accountUsageFormatters.ts`、`frontend/src/views/model-checks/modelCheckProviderCapabilities.ts`、`frontend/src/views/model-checks/useModelCheckAccountOptions.ts`、`frontend/src/views/providers/providerModelCategoryRules.ts`、`frontend/src/views/providers/providerModelFormatters.ts`、`frontend/scripts/regression/account-test-models-regression.ts`、`frontend/scripts/regression/model-check-provider-capabilities-regression.ts`、`frontend/scripts/regression/provider-model-formatters-regression.ts`、`frontend/scripts/regression/usage-stats-provider-display-regression.ts`。
- 验证结果：已记录的后端类型检查和关键网关回归通过。
- 后续建议：Provider capability mismatch hook、OpenAI 系 driver native protocol 排除矩阵、账号测试 / 草稿测试 provider driver 化、endpoint modes 前端 profile 化、模型检测 probe driver 化、API Key 池能力上移和 OpenAI 历史命名中性化建议分别单独立项处理，避免在本轮高价值隔离改造里引入大范围队列、任务和文件重命名风险。
