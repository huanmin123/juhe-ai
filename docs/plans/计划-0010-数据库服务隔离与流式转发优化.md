# PLAN-0010 数据库服务隔离与流式转发优化

## 基本信息

- 编号：PLAN-0010
- 状态：进行中
- 创建时间：2026-05-08
- 更新时间：2026-05-08
- 需求来源：用户对话
- 执行者：AI
- 关联模块：后端 / 存储 / 网关 / 后台任务 / 文档 / 验证

## 需求目标

- 背景：网关转发本身是异步 I/O，但当前主 Web 进程仍有部分 SQLite 同步读写和流式响应全量 Buffer 累积，随着并发、长流式输出或后台写入增长，可能出现事件循环短暂被占用。
- 目标：保留 SQLite 和本地轻量部署，不引入 PostgreSQL 等第三方数据库；通过独立数据库服务进程减少 Web/API/网关主进程被同步 SQL 阻塞的概率，并把流式响应处理改为增量解析、尊重 backpressure。
- 交付物：长期架构文档、第一阶段计划、SQLite 存储说明、本计划、后端数据库服务能力、流式转发优化和验证记录。

## 范围边界

### 本次包含

- [x] 同步长期架构、阶段计划和存储说明，明确数据库服务进程与流式转发优化边界。
- [ ] 新增本地数据库服务进程或等价 DB service，主 Web 进程通过异步 IPC 使用高频网关读写能力。
- [ ] 将网关主链路高频 SQLite 读写优先迁移到 DB service：API Key 校验、网关运行态缓存未命中读取、配额缓存读取和账号运行态副作用。
- [ ] 保留后台 worker 执行统计、审计、运行日志索引、清理和上游探测；后台任务不回到主进程执行。
- [ ] 流式响应改为增量解析 SSE 状态和 usage，避免成功流全量 `Buffer.concat`。
- [ ] 处理 `res.write()` backpressure，客户端慢读时等待 `drain`。
- [ ] 客户端断开时尽量关闭上游异步迭代器，释放连接和并发槽。

### 本次不包含

- 请求体上限调整：保持 `/v1` 网关 raw body 上限 `64mb` 不变，后续如需配置化再单独处理。
- 更换数据库：不引入 PostgreSQL、Redis、Kafka、BullMQ 等第三方存储或队列。
- 多实例全局一致性：第一阶段仍默认单实例；多实例部署需要后续设计共享锁、全局并发和队列归属。
- 全站所有管理 API 一次性异步化：本次优先处理客户请求链路和高频网关路径，管理面 CRUD 可继续使用现有 repository。

## 关联文档

- 架构文档：`docs/architecture/架构总览.md`
- 阶段计划：`docs/plans/第一阶段计划.md`
- 存储说明：`docs/functions/SQLite存储说明.md`
- 后台任务说明：`docs/architecture/backend/后台任务使用说明.md`
- 运行手册：`docs/develop/运行说明.md`
- 验证手册：`docs/develop/测试与验证说明.md`

## 方案概述

- 方案原则：SQLite 继续作为单文件事实存储；DB service 只隔离主 Web 事件循环，不把 SQLite 变成多写者数据库。
- 数据变化：不新增业务表，不修改已有字段；可能新增进程运行态和 IPC 消息类型。
- 接口变化：对外 HTTP API 和 `/v1` 协议不变。
- 后端变化：新增数据库服务进程 / IPC 客户端，网关高频读写改为异步调用；流式转发维护小窗口状态和有限诊断片段。
- 数据处理策略：预上线阶段不做版本化迁移；如本地运行进程异常，回退到主进程同步 repository 或停止数据库服务进程即可恢复原行为。

## 执行拆解

- [x] 更新相关长期文档。
- [ ] 新增 DB service 进程、supervisor、IPC 请求和超时降级。
- [ ] 接入网关 API Key 校验、运行态账号读取、设置读取和配额读取。
- [ ] 接入账号状态、OAuth 快照和 token 刷新写入的异步副作用边界。
- [ ] 优化流式 SSE 增量解析、usage 汇总和失败诊断片段。
- [ ] 补充 backpressure 和客户端断开清理。
- [ ] 运行类型检查、构建和网关回归脚本。
- [ ] 更新验证记录和完成总结。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 后端类型检查 | `pnpm --filter juhe-ai-backend typecheck` | 后端 TypeScript 类型检查通过 | 未执行 | 待实现后执行 |
| 命令类验证 | 全量类型检查 | `pnpm typecheck` | 前后端类型检查通过 | 未执行 | 待实现后执行 |
| 命令类验证 | 后端构建 | `pnpm --filter juhe-ai-backend build` | 生成 `dist/server.js`、后台 worker 和 DB service 相关构建产物 | 未执行 | 待实现后执行 |
| 功能主流程 | 网关正常流式转发 | `pnpm test:smoke` 或真实 `/v1/responses stream:true` | 流式响应包含完成事件，使用记录可入队，客户响应不中断 | 未执行 | 依赖真实账号和网络 |
| 功能主流程 | DB service 存活 | 启动后端并检查日志 / 运行态 | Web 主进程、background worker、DB service 进程均存活，PID 不同 | 未执行 | 待实现后执行 |
| 异常与边界 | DB service 不可用降级 | 手动终止 DB service 后访问 `/health` 和网关缓存路径 | Web 主进程不退出；按设计重启或降级，不产生未处理异常 | 未执行 | 待实现后执行 |
| 异常与边界 | 慢客户端 backpressure | 使用慢速读取客户端请求流式接口 | 后端等待 `drain`，内存不因成功流持续累积完整响应 | 未执行 | 可用本地假上游验证 |
| 回归场景 | 作用域和授权边界 | `pnpm test:scope` | 用户侧、管理侧、授权和分页边界不变 | 未执行 | 待实现后执行 |
| 回归场景 | 代理负向链路 | `pnpm test:proxy-negative` | 停用代理仍阻断账户测试和网关请求，不静默直连 | 未执行 | 待实现后执行 |
| 未覆盖说明 | 生产级压测 | 后续用 10 / 20 / 50 并发 `/v1` 请求压测 | 观察事件循环延迟、DB service 队列和流式内存曲线 | 不适用 | 本轮先完成结构改造，压测可在可用上游环境补测 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-05-08 | 进行中 | AI | 用户确认保持 `/v1` raw body 上限 `64mb`；不引入 PostgreSQL；直接推进本地 DB service 和流式 Buffer 优化，并要求先落文档计划再实现。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-05-08 | 保持 SQLite，不引入第三方数据库 | 项目定位是轻量本地部署，用户明确不考虑 PostgreSQL 等第三方数据库 | 优先通过本地 DB service 隔离主事件循环，仍需接受 SQLite 单写者边界 |
| 2026-05-08 | `/v1` 请求体上限保持 `64mb` | 兼容 OpenAI 图片 / 文件类请求 payload 上限，用户明确要求不改 | 本轮只记录风险，不调整配置 |
| 2026-05-08 | 流式响应改为增量解析 | 当前成功流全量累积和反复 concat 会在长输出时放大内存和 CPU 压力 | 减少长流式响应对主进程的内存和 CPU 占用 |

## 验收标准

- [ ] DB service 启动、退出和重启不影响 `/health` 与主 Web 进程存活。
- [ ] 网关高频数据库读取在 DB service 可用时通过异步 IPC 完成，主 Web 进程不直接等待同步 SQL。
- [ ] DB service 不可用时有明确降级或可读错误，不产生未处理异常。
- [ ] 流式成功响应不再保存完整响应体用于常规解析，只保留必要 usage 状态和有限诊断片段。
- [ ] 慢客户端 backpressure 被处理，`res.write()` 返回 `false` 时等待 `drain`。
- [ ] 请求体上限保持 `64mb`。
- [ ] 必要文档、类型检查、构建和网关回归验证完成或说明未验证原因。

## 验证记录

- 类型检查：未执行，命令：`pnpm typecheck`
- 构建：未执行，命令：`pnpm --filter juhe-ai-backend build`
- 单项验证：未执行，说明：待实现后补充。
- 手动验证：未执行，说明：待实现后补充。
- 测试项结果：未执行，说明：待实现后补充。
- 未验证项：生产级压测需可用真实上游账号、代理和 API Key，后续单独补测。

## 风险与注意事项

- SQLite 仍是单写者数据库；DB service 只能避免主 Web 事件循环被同步 SQL 占用，不能消除所有写锁等待。
- 如果后台 worker 与 DB service 仍各自持有 SQLite 连接，必须保持 WAL、短事务、批量上限和 busy timeout。
- 将账号错误状态写入改为异步后，需要保证网关运行态缓存及时失效或本地短期标记生效，避免短窗口内重复命中故障账号。
- 流式响应如果不保存完整成功体，审计日志的成功流完整原文捕获需要另行受采样和活跃捕获上限约束，不能无限累积。
- 回滚建议：如 DB service 引发运行问题，可临时关闭 DB service 接入并回退到现有同步 repository；流式优化可退回旧 `pipeUpstreamStream` 实现。

## 完成总结

- 完成时间：待补充
- 实际完成内容：待补充
- 主要改动位置：待补充
- 验证结果：待补充
- 后续建议：待补充
