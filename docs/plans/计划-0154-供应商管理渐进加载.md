# PLAN-0154 供应商管理渐进加载

## 基本信息

- 编号：`PLAN-0154`
- 状态：本地实现完成，待真实 PostgreSQL 与浏览器补证，禁止上线
- 创建时间：2026-07-23
- 需求来源：[全站页面 API 按需加载审计报告](../reports/全站页面API按需加载审计报告-2026-07-20.md) 剩余 P1
- 执行范围：Node 后端、Vue 前端、测试与文档
- 明确排除：Go、生产、部署、提交、推送和远端状态

## 目标

将供应商页面拆为管理 `ProviderListItem`、SQL 真正轻量的启用 provider options、保持完整的 definitions，以及点击后按 code 加载的单 provider detail/model 目录；同时为只需判断启用 code 的 Node 路径提供窄查询，避免整批协议定义组装。

## 关联文档

- [专项设计](../superpowers/specs/2026-07-23-供应商管理渐进加载设计.md)
- [全站页面 API 按需加载审计报告](../reports/全站页面API按需加载审计报告-2026-07-20.md)
- [PLAN-0077 管理接口 DTO 分层与渐进式加载](计划-0077-管理接口DTO分层与渐进式加载.md)
- [供应商驱动分层规范](../architecture/供应商驱动分层规范.md)
- [接口契约与权限矩阵](../functions/接口契约与权限矩阵.md)

## 已确认现状

- 管理 `GET /providers` 返回完整 `ProviderDefinition[]`，首屏读取全部协议档案、能力、默认支持模型和 scoped 默认检查模型。
- `/providers/options` 的响应已是 `id/code/name/enabled`，但仓储仍先构建完整 definitions 后过滤映射，尚未做到 SQL 轻量。
- `/providers/definitions` 被账户等表单真实依赖，不能通过破坏该契约完成瘦身。
- 个人模型页首屏读取完整 definitions；点击模型目录又先重拉整批 scoped definitions，再读取单 provider 模型目录。
- 多个仅需判断 provider code 是否启用的供应商模块路径仍使用完整列表 `.find/.filter`。

## 范围边界

### 本次拟实施

- Node `ProviderListItem`、`ProviderOption` 窄 SQL、单 provider detail 和启用 code lookup。
- `/providers/list` 轻首屏权限、detail admin/普通用户停用可见性、现有 scope 权限；旧 `/providers` 完整管理契约保留兼容。
- SQLite 主线程/read worker/PostgreSQL 等价实现。
- Vue 管理列表与个人模型页首屏轻 DTO，点击后并行 detail/models，scope 与请求代次保护。
- `providers.catalog` route/scope cache key、auth revision、写后失效与缓存竞态。
- HTTP/query/worker/PG/前端行为专项回归、权威文档和独立复核。

### 本次不做

- 不修改 `backend-go/`。
- 不修改 schema、供应商驱动、协议桥接、网关调度、计费或账户凭据语义。
- 不新增供应商创建/编辑/启停管理接口。
- 不缩窄 `/providers/definitions`，不迁移真实依赖完整 definitions 的账户表单。
- 不改变模型目录、模型能力、模型写入和默认检查模型写入 DTO。
- 不提交、不推送、不部署、不操作生产。

## 已确认契约

- 新增 `GET /providers/list` 返回 `ProviderListItem[]`，字段固定为 `id/code/name/parentCode/description/enabled/accountTypes/capabilities/baseUrl/defaultHealthCheckModel/defaultSupportedModels`；管理页面使用该入口，旧 admin-only `GET /providers` 完整 definition 契约保留兼容。
- 管理首屏保持现有账户类型、接口能力、Base URL、默认检查模型和默认支持模型列；后端只读取 provider base、每个 provider 被选中的默认 protocol profile 摘要及 scoped 默认检查模型，不加载全部 profiles 或 endpoint families。
- 个人模型页首屏改用启用 `/providers/options`，只展示名称与模型目录操作。
- 新增 `GET /providers/:code` 完整 detail；admin 可读停用项，普通用户只能读启用项。
- 点击模型目录并行加载单 provider detail 与 models，不再重拉整批 `/providers/definitions`。
- `/providers/options` HTTP DTO 不变，但改为直接窄 SQL；完整 `/providers/definitions` 不变。

上述 Node/Vue 契约已获本轮剩余按需加载实施授权；是否满足完成标准仍以 repository 查询、权限和专项证据为准。

## 执行清单

- [x] 核对审计报告、Provider route/repository/worker、Vue 页面、缓存与现有测试。
- [x] 形成 list/options/definitions/detail/enabled-code 专项设计。
- [x] 用户已授权实施 `ProviderListItem`、首屏 UI 取舍、detail 权限和点击时序。
- [x] 实现 Node DTO、list/options/single detail/enabled code 窄仓储；options 与 provider-code lookup 已迁移到窄查询，完整 definitions 保持兼容。
- [ ] 实现 SQLite read worker 与 PostgreSQL 等价查询。
- [x] 接入 provider list/detail routes、权限和静态路由顺序；不恢复已退场的通用 page-data 机制。
- [x] 将供应商模块内纯 code/enabled 校验迁移到窄 lookup，保留完整 definition 校验边界。
- [x] 实现 Vue 管理/个人首屏轻 DTO 与点击后并行 detail/models。
- [x] 实现 list/modal generation、scope/auth revision、关闭与 A→B→A 竞态保护。
- [ ] 增加 Node HTTP/query/worker/PG smoke 回归。
- [ ] 增加前端请求矩阵、缓存、失败重试和竞态回归。
- [x] 同步供应商专项设计、接口矩阵、PLAN-0077 后续决策、测试说明与审计报告状态。
- [ ] 运行 Node/前端专项门禁及可用的浏览器网络验证。
- [ ] 至少两名只读 Agent 独立复核并交叉质疑，闭合全部高严重度问题。

## 测试项

- `/providers/list` 精确 `ProviderListItem` 字段、管理/个人 scope、启用与停用项、稳定排序；当前可见五类摘要字段保留，但无完整 profiles、families 或未展示 definition 深字段。
- `/providers/options` 精确四字段、只含启用项、`id === code`、SQL 无 profile/default model/full loader。
- `/providers/definitions` 完整协议档案与 scoped 默认模型不回归。
- `/:code` admin/普通用户、停用/不存在、非法 code、scope 越权和静态路由优先级。
- list 的 base + 选中 profile + scoped default model 固定批量查询，以及 options/enabled lookup 的 SQLite 主线程、worker、PG 字段及整数 enabled 语义一致。
- 单 detail 只读取目标 code，模型 options/capabilities 的启用判断不再组装整批 definitions。
- 管理与个人首屏均零 definitions/detail/models；点击后 detail/models 各一次并行且不重拉整批。
- A→B、A→B→A、目标用户切换、刷新、关闭、登出、失败重试和缓存确认竞态。
- 模型创建/编辑、默认检查模型、capabilities 和账户表单 definitions 现有回归继续通过。

## 验收标准

- 供应商管理和个人模型页首屏不读取完整 ProviderDefinition。
- `/providers/options` 从 HTTP 到 SQL 都是轻量接口。
- `/providers/definitions` 保持完整且真实依赖方无回归。
- 单 provider detail/models 只在点击后加载，且 code/scope/身份/竞态正确。
- 纯 code/enabled 校验不再触发完整 definitions，完整协议校验不被误删。
- Node、SQLite worker、真实 PostgreSQL、前端时序、缓存失效、浏览器和双 Agent 复核证据闭合。

## 风险与注意事项

- 轻首屏通过新增 `/providers/list` 完成，旧 `/providers` 保留完整兼容；不得让新 Vue 页面回退到旧宽入口。
- 不能只裁剪 HTTP 响应而保留完整 loader；SQL/query guard 是验收必需证据。
- 不能以接口瘦身为由删除 ProvidersView 当前真实展示的账户类型、接口能力、Base URL、默认检查模型或默认支持模型；应收窄为选中 profile 摘要而非改变 UI。
- 不能为了复用 options 而让账户创建/编辑失去 protocol profiles、account types 或 capabilities。
- detail cache 必须包含 target system account，否则默认检查模型偏好会跨用户串值。
- enabled lookup 不能替代允许管理停用供应商的存在性校验，也不能替代完整协议能力校验。
- 当前工作树存在大量并行改动；实施时必须按文件职责协调，禁止整理、回退、提交或覆盖无关差异。

## 进度记录

| 日期 | 状态 | 记录 |
| --- | --- | --- |
| 2026-07-23 | 草稿 | 完成当前 Node/Vue/SQL/缓存/测试只读审计，形成 `ProviderListItem + 轻 options + 单 detail/models + enabled code lookup` 设计，等待用户确认。 |
| 2026-07-23 | 本地实现完成 | ProviderListItem、窄 options、单 provider detail、按 code lookup、管理/个人作用域和 Vue 点击后 detail/models 已落地；本地专项与代码复查通过，真实 PostgreSQL 和浏览器 Network 待补。 |

## 验证记录

- 已通过供应商 repository/模型目录相关本地专项与前端 provider catalog 行为回归；真实 PostgreSQL、浏览器 Network 未执行。完整 `/providers/definitions` 仍保留作为协议表单契约，不计入首屏轻查询。

## 完成总结

本地 Node/Vue 实现与专项复查已完成：首屏 list/options 不再依赖完整 definitions，点击后按 code 读取 detail/models，完整 definitions 兼容保留。真实 PostgreSQL 与浏览器证据仍待补，计划不标最终完成。
