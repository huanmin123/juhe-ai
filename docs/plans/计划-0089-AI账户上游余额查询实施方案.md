# AI 账户上游余额查询 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为物理单 API Key AI 账户提供可配置的上游余额自动/手动查询，并在账户列表安全展示当前结果。

> 2026-07-13 补充：多 Key 是核心凭据轮换能力。账户最终有效 Key 数量大于 1 时必须允许保存，并自动关闭余额查询；余额查询不能反向阻断多 Key 创建或编辑。

**Architecture:** 账户业务表保存启用、配置与下次到期时间，`account_usage_snapshots(kind='relay_balance')` 保存唯一当前结果；内置适配器和受限自定义适配器统一返回 decimal string 结果。`ops-worker` 每分钟按业务索引领取到期账户，手动刷新与自动刷新复用 `background_job_leases` 去重，列表只按当前页 ID 批量补齐快照。

**Tech Stack:** Node.js 22、TypeScript、Express、Zod、SQLite/PostgreSQL、Vue 3、Ant Design Vue、现有代理与后台 worker 基础设施。

---

## 文件结构

新增文件：

- `backend/src/modules/accounts/account-balance.types.ts`：余额配置、状态和结果领域类型。
- `backend/src/modules/accounts/account-balance-config.ts`：配置归一化与校验纯函数。
- `backend/src/modules/accounts/account-balance-adapters.ts`：内置/自定义适配器注册与响应解析。
- `backend/src/modules/accounts/account-balance-query.service.ts`：安全请求、代理、租约和快照状态编排。
- `backend/src/modules/background/account-balance-refresh.job.ts`：有界到期刷新任务。
- `backend/src/storage/account-balance.repository.ts`：业务配置、到期候选与当前快照读写。
- `backend/src/scripts/regression/account-balance-config-regression.ts`：配置与权限契约。
- `backend/src/scripts/regression/account-balance-adapters-regression.ts`：适配器 fixture 回归。
- `backend/src/scripts/regression/account-balance-refresh-regression.ts`：快照、租约、手动与自动刷新回归。
- `frontend/src/views/accounts/AccountBalanceQuerySection.vue`：账户编辑余额配置区。
- `frontend/src/views/accounts/accountBalanceQuery.ts`：表单、DTO、展示状态纯函数。
- `frontend/src/scripts/regression/account-balance-query-regression.ts`：前端配置和展示回归。

修改文件：

- `backend/src/storage/schema/business-schema.ts`、`repository-row-types.ts`、`account-read.repository.ts`、`account-summary.repository.ts`、`repositories.ts`：当前 schema 与读取映射。
- `backend/src/storage/schema/stats-schema.ts`、`account-usage-snapshot.repository.ts`：`relay_balance` 当前快照契约。
- `backend/src/domain/types.ts`：账户 DTO。
- `backend/src/modules/accounts/accounts.routes.ts`、`accounts-list.routes.ts`：写入校验、列表快照和手动刷新接口。
- `backend/src/modules/background/background-jobs.ts`、`background-job-registry.entries.ts`：ops-worker 注册。
- `backend/src/scripts/maintenance/mockdata/business/foundation.ts`、`core/accounts.ts`：可验收样本。
- `backend/package.json`、`frontend/package.json`：定向回归命令。
- `frontend/src/types/domain/accounts.ts`、`api/accounts.ts`：前端类型与请求。
- `frontend/src/views/accounts/accountFormTypes.ts`、`accountFormDefaults.ts`、`accountEditFormLoaders.ts`、`accountSavePayload.ts`、`AccountEditModal.vue`：表单闭环。
- `frontend/src/views/accounts/AccountUsageCell.vue`、`AccountsView.vue`：余额状态与手动刷新。
- `docs/functions/Mockdata造数设计.md`、`接口契约与权限矩阵.md`、`SQLite存储说明.md`、`docs/develop/测试与验证说明.md`：事实同步。

## Task 1：领域类型与当前 schema

**Files:**
- Create: `backend/src/modules/accounts/account-balance.types.ts`
- Modify: `backend/src/storage/schema/business-schema.ts`
- Modify: `backend/src/storage/schema/stats-schema.ts`
- Modify: `backend/src/domain/types.ts`
- Test: `backend/src/scripts/regression/account-balance-config-regression.ts`

- [ ] **Step 1: 写失败的 schema 与类型回归**

断言 PostgreSQL 生成 SQL 包含三个账户字段、到期部分索引，并断言 `account_usage_snapshots` 接受 `relay_balance`：

```ts
assert.match(schemaSql, /balance_query_enabled integer NOT NULL DEFAULT 0/)
assert.match(schemaSql, /balance_query_config_json text NOT NULL DEFAULT '\{\}'/)
assert.match(schemaSql, /balance_query_next_refresh_at text/)
assert.match(schemaSql, /idx_accounts_balance_query_due/)
```

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-backend test:account-balance-config`

Expected: FAIL，缺少账户余额字段或测试脚本命令。

- [ ] **Step 3: 增加最小领域类型与 schema**

```ts
export type AccountBalanceAdapter = 'sub2api' | 'newapi' | 'litellm' | 'custom'
export type AccountBalanceStatus = 'pending' | 'refreshing' | 'fresh' | 'unlimited' | 'unsupported' | 'failed'

export interface AccountBalanceQueryConfig {
  adapter: AccountBalanceAdapter
  intervalMinutes: number
  custom?: AccountBalanceCustomConfig
}
```

账户新增 `balance_query_enabled`、`balance_query_config_json`、`balance_query_next_refresh_at`，并增加设计文档指定的部分索引。

- [ ] **Step 4: 运行 GREEN**

Run: `pnpm --filter juhe-ai-backend test:account-balance-config && pnpm --filter juhe-ai-backend typecheck`

Expected: PASS。

## Task 2：配置校验与账户读写

**Files:**
- Create: `backend/src/modules/accounts/account-balance-config.ts`
- Modify: `backend/src/storage/repository-row-types.ts`
- Modify: `backend/src/storage/account-read.repository.ts`
- Modify: `backend/src/storage/account-summary.repository.ts`
- Modify: `backend/src/storage/repositories.ts`
- Modify: `backend/src/modules/accounts/accounts.routes.ts`
- Test: `backend/src/scripts/regression/account-balance-config-regression.ts`

- [ ] **Step 1: 写失败的配置矩阵**

覆盖：默认关闭、默认 5 分钟、1/10 合法、0/11/小数非法、OAuth 非法、多 Key 自动关闭、授权实例非法、自定义同源相对路径、JSON Pointer 两种模式、正 divisor。

```ts
assert.deepEqual(normalizeAccountBalanceConfig({ adapter: 'sub2api' }), {
  adapter: 'sub2api', intervalMinutes: 5
})
assert.throws(() => validateAccountBalanceCapability(oauthAccount, true), /仅支持 API Key/)
```

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-backend test:account-balance-config`

Expected: FAIL，归一化函数尚不存在。

- [ ] **Step 3: 实现当前结构读写**

路由 Zod 契约增加：

```ts
balanceQueryEnabled: z.boolean().optional(),
balanceQueryConfig: accountBalanceQueryConfigSchema.optional()
```

开启或改变适配器时 `nextRefreshAt=now`；关闭时 `nextRefreshAt=null`。AccountSummary 只对物理所有者/管理员返回配置字段，授权实例不映射。

- [ ] **Step 4: 运行 GREEN**

Run: `pnpm --filter juhe-ai-backend test:account-balance-config && pnpm --filter juhe-ai-backend test:account-response-credentials-redaction`

Expected: PASS，API Key 明文不进入 DTO。

## Task 3：快照和到期候选 repository

**Files:**
- Create: `backend/src/storage/account-balance.repository.ts`
- Modify: `backend/src/storage/account-usage-snapshot.repository.ts`
- Modify: `backend/src/storage/repositories.ts`
- Test: `backend/src/scripts/regression/account-balance-refresh-regression.ts`

- [ ] **Step 1: 写失败的 SQLite 行为回归**

创建 active/disabled/OAuth/multi-key/授权实例/未到期 fixture，断言只返回 active、启用、物理单 Key API Key 到期账户，且顺序为到期时间后 ID。

```ts
assert.deepEqual(due.map((item) => item.id), ['acct_due_a', 'acct_due_b'])
```

失败快照断言：

```ts
assert.equal(snapshot.status, 'failed')
assert.equal(snapshot.remainingUsd, undefined)
assert.equal(snapshot.lastErrorMessage, '上游鉴权失败（HTTP 401）')
```

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-backend test:account-balance-refresh`

Expected: FAIL，repository 尚不存在。

- [ ] **Step 3: 实现有界查询和快照替换**

```sql
SELECT ... FROM accounts
WHERE status = 'active'
  AND balance_query_enabled = 1
  AND balance_query_next_refresh_at <= ?
  AND deleted_at IS NULL
  AND authorization_instance_authorization_id IS NULL
ORDER BY balance_query_next_refresh_at ASC, id ASC
LIMIT ?
```

快照写入 `failed` 时以新 JSON 整体替换旧 JSON，不能 `COALESCE` 保留旧金额。

- [ ] **Step 4: 运行 GREEN 与 PG SQL 门禁**

Run: `pnpm --filter juhe-ai-backend test:account-balance-refresh && pnpm --filter juhe-ai-backend test:postgres-schema-sql`

Expected: PASS。

## Task 4：余额适配器

**Files:**
- Create: `backend/src/modules/accounts/account-balance-adapters.ts`
- Test: `backend/src/scripts/regression/account-balance-adapters-regression.ts`

- [ ] **Step 1: 写真实响应 fixture 的失败测试**

每个适配器至少覆盖：金额成功、无限或未提供、鉴权失败响应不进入解析器、字段缺失、数字字符串、非法负值。自定义 JSON Pointer 覆盖余额直取和总额减已用。

```ts
assert.deepEqual(parseNewApiBalance({ data: { total_available: 3655000 } }, { quotaPerUnit: 500000 }), {
  status: 'fresh', remainingUsd: '7.310000', rawRemaining: '3655000', rawUnit: 'quota', basis: 'api_key_quota'
})
```

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-backend test:account-balance-adapters`

Expected: FAIL，解析器尚不存在。

- [ ] **Step 3: 实现纯解析器与注册表**

所有金额计算使用 decimal string helper，不通过 JavaScript 浮点直接减除。接口没有明确金额口径时返回：

```ts
{ status: 'unsupported', basis: 'subscription' }
```

- [ ] **Step 4: 运行 GREEN**

Run: `pnpm --filter juhe-ai-backend test:account-balance-adapters`

Expected: PASS，测试输出不包含 fixture API Key。

## Task 5：安全查询服务与跨进程去重

**Files:**
- Create: `backend/src/modules/accounts/account-balance-query.service.ts`
- Modify: `backend/src/storage/background-task-runs.repository.ts`（仅在现有 lease API 无法接受业务 key 时扩展类型）
- Test: `backend/src/scripts/regression/account-balance-refresh-regression.ts`

- [ ] **Step 1: 写失败的请求边界测试**

本机 mock HTTP 服务断言：GET、正确鉴权头、账户代理参数传递、8 秒 AbortSignal、256 KiB 上限、redirect error、custom 不同源拒绝、错误摘要无响应正文/Key。

并发发起两次同账户查询，断言上游只命中一次。

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-backend test:account-balance-refresh`

Expected: FAIL，查询服务尚不存在。

- [ ] **Step 3: 实现租约编排**

```ts
const leaseKey = `account-balance:${account.id}`
const acquired = await tryAcquireBackgroundJobLeaseAsync({
  leaseKey, jobName: 'account-balance-refresh', leaseUntil: addSeconds(now, 15)
})
```

查询开始写 `refreshing`，最终在 `finally` 释放租约；失败用分类后的中文摘要覆盖快照并更新下次到期时间，绝不调用账户错误策略。

- [ ] **Step 4: 运行 GREEN**

Run: `pnpm --filter juhe-ai-backend test:account-balance-refresh`

Expected: PASS，上游命中次数为 1。

## Task 6：ops-worker 与手动刷新 API

**Files:**
- Create: `backend/src/modules/background/account-balance-refresh.job.ts`
- Modify: `backend/src/modules/background/background-jobs.ts`
- Modify: `backend/src/modules/background/background-job-registry.entries.ts`
- Modify: `backend/src/modules/accounts/accounts.routes.ts`
- Modify: `backend/src/modules/accounts/accounts-list.routes.ts`
- Test: `backend/src/scripts/regression/account-balance-refresh-regression.ts`

- [ ] **Step 1: 写失败的调度和权限测试**

断言 ops-worker 每分钟注册、单轮 limit=100、小并发；手动接口允许物理 owner/admin，拒绝被授权人、未开启、disabled、OAuth 和多 Key。

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-backend test:account-balance-refresh`

Expected: FAIL，任务登记或路由缺失。

- [ ] **Step 3: 实现任务和接口**

```ts
scheduler.schedule({
  name: backgroundScheduledJobName('account-balance-refresh'),
  intervalMs: minuteMs,
  initialDelayMs: 20 * secondMs,
  task: runAccountBalanceRefresh
})
```

列表 repository 接受当前页物理账户 ID，一次批量读取 `relay_balance`，按权限映射 `balanceSnapshot`，禁止 `map` 内查询。

- [ ] **Step 4: 运行 GREEN**

Run: `pnpm --filter juhe-ai-backend test:account-balance-refresh && pnpm --filter juhe-ai-backend test:account-list-query-guard`

Expected: PASS，无 N+1 源码门禁失败。

## Task 7：前端配置和余额单元格

**Files:**
- Create: `frontend/src/views/accounts/AccountBalanceQuerySection.vue`
- Create: `frontend/src/views/accounts/accountBalanceQuery.ts`
- Create: `frontend/src/scripts/regression/account-balance-query-regression.ts`
- Modify: `frontend/src/types/domain/accounts.ts`
- Modify: `frontend/src/api/accounts.ts`
- Modify: `frontend/src/views/accounts/accountFormTypes.ts`
- Modify: `frontend/src/views/accounts/accountFormDefaults.ts`
- Modify: `frontend/src/views/accounts/accountEditFormLoaders.ts`
- Modify: `frontend/src/views/accounts/accountSavePayload.ts`
- Modify: `frontend/src/views/accounts/AccountEditModal.vue`
- Modify: `frontend/src/views/accounts/AccountUsageCell.vue`
- Modify: `frontend/src/views/accounts/AccountsView.vue`
- Modify: `frontend/package.json`

- [ ] **Step 1: 写失败的表单与展示回归**

```ts
assert.equal(formatAccountBalance({ status: 'fresh', remainingUsd: '7.310000' }).text, '$7.31')
assert.equal(formatAccountBalance({ status: 'failed', remainingUsd: '7.31', errorMessage: '超时' }).text, '查询失败')
assert.equal(buildBalancePayload(oauthForm), undefined)
```

源码门禁断言使用 `ReloadOutlined`、无文本按钮、失败 tooltip、图标 loading class。

- [ ] **Step 2: 运行 RED**

Run: `pnpm --filter juhe-ai-frontend test:account-balance-query`

Expected: FAIL，helper/component 尚不存在。

- [ ] **Step 3: 实现最小 UI**

配置区只在物理单 API Key 账户显示。余额行：

```vue
<span>剩余：{{ display.text }}</span>
<ReloadOutlined
  class="balance-refresh-icon"
  :class="{ spinning: display.refreshing, disabled: !canRefresh }"
  @click="canRefresh && emit('refresh-balance', account.id)"
/>
```

AccountsView 使用按账户 ID 的 pending Set，A/B 账户刷新状态隔离；请求完成后只替换目标账户的余额快照。

- [ ] **Step 4: 运行 GREEN 与构建**

Run: `pnpm --filter juhe-ai-frontend test:account-balance-query && pnpm --filter juhe-ai-frontend typecheck && pnpm --filter juhe-ai-frontend build`

Expected: PASS，仅允许既有 Vite chunk warning。

## Task 8：Mockdata、文档与最终验证

**Files:**
- Modify: `backend/src/scripts/maintenance/mockdata/core/accounts.ts`
- Modify: `docs/functions/Mockdata造数设计.md`
- Modify: `docs/functions/接口契约与权限矩阵.md`
- Modify: `docs/functions/SQLite存储说明.md`
- Modify: `docs/develop/测试与验证说明.md`
- Modify: `docs/plans/计划-0089-AI账户上游余额查询.md`

- [ ] **Step 1: 增加可视化样本**

Mockdata 至少包含 fresh、failed、unlimited、unsupported、disabled 和功能关闭账户；清理沿用 `mockdata_` ID，不留下外部请求任务。

- [ ] **Step 2: 运行定向回归合集**

```powershell
pnpm --filter juhe-ai-backend test:account-balance-config
pnpm --filter juhe-ai-backend test:account-balance-adapters
pnpm --filter juhe-ai-backend test:account-balance-refresh
pnpm --filter juhe-ai-backend test:postgres-schema-sql
pnpm --filter juhe-ai-frontend test:account-balance-query
pnpm typecheck
pnpm build
git diff --check
```

Expected: 全部 PASS。

- [ ] **Step 3: 浏览器验收**

流程：本地登录 -> 我的 AI 账户 -> 打开 API Key 账户编辑 -> 开启余额查询并保存 -> 列表手动刷新 -> 验证成功/失败 tooltip -> 停用账户验证图标禁用 -> 移动端验证不重叠。检查 URL、页面非空、无框架 overlay、控制台无相关错误并保存桌面/移动截图证据。

- [ ] **Step 4: 最终代码审查**

检查：API Key 不出现在响应/日志；授权实例无配置/快照；失败快照无旧金额；到期查询有索引/limit/稳定排序；手动与自动查询共享租约；余额不进入调度、健康或本地 usage。

- [ ] **Step 5: 更新计划状态**

全部验证通过后将 PLAN-0089 标记为“已完成，待提交评审”。按用户当前要求保持不提交、不推送、不上线，等待与 PLAN-0088 一并复查。

## Task 9：余额查询共享总超时调整为 15 秒

**Files:**
- Modify: `backend/src/scripts/regression/account-balance-refresh-regression.ts`
- Modify: `backend/src/modules/accounts/account-balance-query.service.ts`
- Modify: `docs/plans/计划-0089-AI账户上游余额查询.md`

- [x] **Step 1: 写失败的超时契约回归**

在现有源码契约断言中加入：

```ts
assert.match(
  balanceServiceSource,
  /const requestTimeoutMs = 15_000/,
  '余额查询的全部内置适配器应共享 15 秒总 deadline'
)
```

- [x] **Step 2: 运行 RED，确认仍为 8 秒时失败**

Run:

```powershell
pnpm --filter juhe-ai-backend test:account-balance-refresh
```

Expected: FAIL，实际源码仍为 `const requestTimeoutMs = 8_000`，不匹配 `15_000`。

- [x] **Step 3: 实现最小生产代码变更**

把余额查询服务中的单一总预算常量改为：

```ts
const requestTimeoutMs = 15_000
```

不修改 `accountBalanceRequestContext()`、`requestJson()`、`refreshConcurrency = 4` 或 `balanceRefreshLeaseMs = 30_000`；所有候选适配器继续通过同一个 `deadlineAtMs` 消费剩余预算。

- [x] **Step 4: 运行 GREEN 和专项回归**

Run:

```powershell
pnpm --filter juhe-ai-backend test:account-balance-refresh
pnpm --filter juhe-ai-backend test:account-balance-adapters
pnpm --filter juhe-ai-backend test:account-balance-auto-detect
```

Expected: 全部 PASS；租约去重、适配器偏好回退、代理传递和非阻塞自动探测语义保持不变。

- [x] **Step 5: 完成后端和全项目验证**

Run:

```powershell
pnpm --filter juhe-ai-backend typecheck
pnpm typecheck
pnpm build
git diff --check
```

Expected: 全部 PASS，只允许既有构建 warning，不允许新增类型错误或构建失败。

- [x] **Step 6: 更新计划并提交功能分支**

将 PLAN-0089 的 15 秒调整任务和验证记录标记为完成，然后提交：

```powershell
git add backend/src/modules/accounts/account-balance-query.service.ts backend/src/scripts/regression/account-balance-refresh-regression.ts docs/functions/AI账户上游余额查询设计.md docs/plans/README.md docs/plans/计划-0089-AI账户上游余额查询.md docs/plans/计划-0089-AI账户上游余额查询实施方案.md
git commit -m "fix: extend account balance query timeout"
```

- [x] **Step 7: 合并、发布和生产验证**

把功能分支合并到 `master` 并推送，构建发布包后依次完成临时环境验证、生产原子切换和健康检查。普通代码发布必须保持“重建非业务数据库”和“清理 Redis namespace”两个开关均为 `0`。生产验证至少包括：

```text
/__aisys__/health = 200
/__aisys__/api/health = 200
1 server + 1 DB service + 3 workers
Redis Stream pending = 0, lag = 0
无新增 worker crash、DB service unavailable 或孤儿进程
```

发布成功后把最新 `master` 合并到 `feature/20260706-go` 并推送，确认 `feature/20260706-go...master` 的右侧计数为 `0`。

## Task 10：后台连续失败阈值与人工刷新隔离

**Files:**
- Modify: `backend/src/modules/accounts/account-balance.types.ts`
- Modify: `backend/src/modules/accounts/account-balance-query.service.ts`
- Modify: `backend/src/modules/accounts/account-balance.routes.ts`
- Modify: `backend/src/scripts/regression/account-balance-refresh-regression.ts`
- Modify: `frontend/src/types/domain/accounts.ts`
- Modify: `frontend/src/views/accounts/accountBalanceQuery.ts`
- Modify: `frontend/src/views/accounts/AccountsView.vue`
- Modify: `frontend/src/scripts/regression/account-balance-query-regression.ts`
- Modify: `docs/plans/计划-0089-AI账户上游余额查询.md`

- [x] **Step 1: 写后台三次临时失败和确定性失败的 RED 回归**

在余额刷新回归中分别构造独立账户：已有 `$7.31` 成功快照后连续抛出 `UpstreamRequestTimeoutError`，断言第 1、2 次仍为 `fresh` 且保留金额和 `consecutiveTransientFailures=1/2`，第 3 次为 `failed` 且金额为空；再用 `上游鉴权失败（HTTP 401）` 断言第一次即为 `failed`。成功查询必须删除失败序列字段。

```ts
await refreshAccountBalanceCandidate(candidate, {
  query: async () => { throw new UpstreamRequestTimeoutError('上游余额查询超时') }
})
const firstFailureSnapshot = balanceRepository.loadAccountBalanceSnapshotsByAccountIds([candidate.id]).get(candidate.id)
assert.equal(firstFailureSnapshot?.consecutiveTransientFailures, 1)
assert.equal(firstFailureSnapshot?.remainingUsd, '7.310000')
```

- [x] **Step 2: 写人工刷新不持久化失败的 RED 回归**

调用 `refreshAccountBalanceCandidate(candidate, { mode: 'manual', query })`，断言返回本次 `failed` DTO，但数据库快照和 `balance_query_next_refresh_at` 均保持原值；人工成功则写入新金额并清零失败序列。路由源码必须显式传入 `mode: 'manual'`。

- [x] **Step 3: 运行后端 RED**

```powershell
pnpm --filter juhe-ai-backend test:account-balance-refresh
```

Expected: FAIL，当前快照类型没有连续失败字段，第一次超时会直接写 `failed`，人工失败也会持久化。

- [x] **Step 4: 实现错误分类和后台失败状态机**

快照增加可选元数据：

```ts
consecutiveTransientFailures?: number
lastTransientErrorMessage?: string
lastTransientFailureAt?: string
```

余额服务增加 `mode?: 'automatic' | 'manual'`，默认 `automatic`。错误分类规则固定为：超时、网络异常、HTTP `408/429/5xx` 属于 transient；`401/403`、本地凭据/Base URL/自定义配置错误和其他确定性 `4xx` 属于 deterministic。自动 transient 通过旧快照生成 1/3、2/3 或第 3 次 `failed`；deterministic 立即 `failed`；成功快照不复制失败元数据。

- [x] **Step 5: 实现人工失败隔离**

`mode='manual'` 的失败分支只返回：

```ts
{
  status: 'failed',
  errorMessage: accountBalanceErrorMessage(error),
  lastAttemptAt: completedAt
}
```

不调用 `persistBalanceRefreshIfCurrent()`。人工成功继续持久化并更新下次刷新时间；账户余额刷新路由显式使用 manual mode，草稿测试逻辑保持不变。

- [x] **Step 6: 运行后端 GREEN**

```powershell
pnpm --filter juhe-ai-backend test:account-balance-refresh
pnpm --filter juhe-ai-backend test:account-balance-adapters
pnpm --filter juhe-ai-backend test:account-balance-auto-detect
```

Expected: 全部 PASS。

- [x] **Step 7: 写前端 RED 并实现提示**

前端快照类型同步三个可选字段。格式化规则：已有成功结果并处于 1/3、2/3 时保持金额/无限/未提供文本，tooltip 追加暂时失败说明；`pending` 且有失败次数时显示 `待重试（1/3）`。列表人工刷新先判断失败，失败只 `message.error()`，不能调用 `updateLoadedAccountBalance()`；成功才替换当前行。

```ts
assert.equal(formatAccountBalance({
  status: 'pending',
  consecutiveTransientFailures: 1,
  lastTransientErrorMessage: '上游余额查询超时'
}).text, '待重试（1/3）')
```

- [x] **Step 8: 运行前端 GREEN 和全项目验证**

```powershell
pnpm --filter juhe-ai-frontend test:account-balance-query
pnpm --filter juhe-ai-backend typecheck
pnpm typecheck
pnpm build
git diff --check
```

Expected: 全部 PASS，只允许既有 Vite chunk warning。

- [x] **Step 9: 更新计划、提交、合并和发布**

把 PLAN-0089 的后台阈值、人工刷新隔离和生产验证状态更新为完成，提交功能分支，合并到本地 `master` 后重新运行余额专项回归与构建。推送 master，按普通代码发布流程部署，非业务数据库重建和 Redis namespace 清理均保持关闭；生产检查健康、进程拓扑、队列和最近错误后，再把 master 合并到 `feature/20260706-go` 并推送。

## Task 11：能力暂停、NewAPI 语义修正与紧凑布局

**Files:**
- Modify: `backend/src/modules/accounts/account-balance-adapters.ts`
- Modify: `backend/src/modules/accounts/account-balance-query.service.ts`
- Modify: `backend/src/storage/account-balance.repository.ts`
- Modify: `backend/src/scripts/regression/account-balance-adapters-regression.ts`
- Modify: `backend/src/scripts/regression/account-balance-refresh-regression.ts`
- Modify: `frontend/src/views/accounts/AccountBalanceQuerySection.vue`
- Modify: `frontend/src/views/accounts/accountBalanceQuery.ts`
- Modify: `frontend/src/views/accounts/AccountsView.vue`
- Modify: `frontend/src/scripts/regression/account-balance-query-regression.ts`
- Modify: `docs/plans/计划-0089-AI账户上游余额查询.md`

- [x] **Step 1: 写 NewAPI 与能力暂停 RED 回归**

断言 `unlimited_quota=true` 归一化为 `unsupported`，内置规则继续尝试后续适配器；全部规则确定不支持时后台保存 `unsupported`、保持用户开关开启并把 `balance_query_next_refresh_at` 置空。人工失败或人工 `unsupported` 都不落库，人工成功恢复用户刷新周期。

- [x] **Step 2: 写 15/30/60 退避 RED 回归**

固定时钟后连续触发三次临时错误，分别断言下次刷新约为 15、30、60 分钟；第四次仍为 60 分钟。确定性不支持必须无下次刷新时间，成功后重新使用账户配置周期并清零失败元数据。

- [x] **Step 3: 写前端暂停展示与紧凑布局 RED 回归**

断言 `unsupported` 显示“已暂停”并保留解释性 tooltip；列表人工返回 `unsupported` 时只顶部提示、不替换当前单元格。源码门禁断言测试按钮位于刷新周期控件容器内，旧 `.balance-query-test` 独立行已删除，窄屏样式允许同一表单项换行且不溢出。

- [x] **Step 4: 实现最小后端状态机**

NewAPI 令牌不限额返回 `unsupported`；适配器解析只在 `fresh/unlimited` 时记录偏好。自动确定性不支持写暂停快照和空下次刷新时间，临时错误按 15/30/60 分钟退避；人工模式只在真实成功时保存和恢复周期。

- [x] **Step 5: 实现最小前端交互**

列表将暂停状态显示为“已暂停”；人工刷新 `failed/unsupported` 只通过顶部消息反馈。编辑弹窗把“测试查询”按钮移到刷新周期输入框右侧，删除独立验证行，标题帮助提示保留“不保存”说明。

- [x] **Step 6: 运行专项与全量验证**

```powershell
pnpm --filter juhe-ai-backend test:account-balance-adapters
pnpm --filter juhe-ai-backend test:account-balance-refresh
pnpm --filter juhe-ai-backend test:account-balance-auto-detect
pnpm --filter juhe-ai-frontend test:account-balance-query
pnpm typecheck
pnpm build
git diff --check
```

浏览器验收桌面和窄屏布局、人工提示与单元格不整表刷新；只允许既有 Vite chunk warning。

- [x] **Step 7: 合并、发布、重扫和生产观察**

提交功能分支并合并到 `master`，推送后按临时环境、生产原子切换、90 秒稳定门禁执行。普通发布固定传入“非业务数据库不重建、Redis 不清理”。上线后游标重扫所有系统账户，纠正历史 NewAPI “无限”快照，并检查健康端点、1 server + 1 DB service + 3 workers、队列 pending/lag、内存、孤儿进程和最近错误。最后把 `master` 合入并推送 `feature/20260706-go`，验证该分支不缺少 master 提交。

## Task 12：多 Key 保存时自动关闭余额查询

**目标：** 多 Key 账户始终可以创建和编辑；余额查询作为单 Key 附属能力，在最终有效 Key 数量大于 1 时自动关闭，不再产生保存错误。

**Files:**

- Modify: `frontend/src/views/accounts/accountBalanceQuery.ts`
- Modify: `frontend/src/views/accounts/accountSavePayload.ts`
- Modify: `frontend/src/views/accounts/AccountBalanceQuerySection.vue`
- Modify: `frontend/src/views/accounts/AccountApiKeySection.vue`
- Modify: `frontend/src/views/accounts/useAccountEditSaveFlow.ts`
- Modify: `frontend/src/scripts/regression/account-balance-query-regression.ts`
- Modify: `backend/src/modules/accounts/account-balance-config.ts`
- Modify: `backend/src/modules/accounts/accounts.routes.ts`
- Modify: `backend/src/storage/account-balance.repository.ts`（仅在现有关闭写入与条件保护不足时调整）
- Modify: `backend/src/scripts/regression/account-balance-config-regression.ts`
- Modify: `backend/src/scripts/regression/account-balance-refresh-regression.ts`
- Modify: `docs/functions/AI账户上游余额查询设计.md`
- Modify: `docs/plans/计划-0089-AI账户上游余额查询.md`

- [ ] **Step 1: 写前端 RED 回归**

覆盖单 Key 已开启后增加第二个有效 Key：表单保存校验不再返回“仅支持单 API Key”，构建出的保存 payload 必须显式包含 `balanceQueryEnabled: false`，不能返回 `undefined` 让后端继承旧开启状态。多 Key 配置区展示中文说明，不弹阻断式确认框。

```ts
assert.equal(validateAccountBalanceForm(multiKeyBalanceEnabledForm), undefined)
assert.deepEqual(buildAccountBalancePayload(multiKeyBalanceEnabledForm), {
  balanceQueryEnabled: false
})
```

- [ ] **Step 2: 写后端 RED 回归**

至少覆盖：

- 已开启余额查询的单 Key 账户更新为两个 Key，保存成功。
- 创建多 Key 账户并显式提交 `balanceQueryEnabled=true`，创建成功且最终关闭。
- 请求省略余额字段但现有账户已开启时，最终多 Key 状态仍自动关闭。
- 多 Key 减少为单 Key 后保持关闭。
- 自动关闭后 `balance_query_next_refresh_at` 为空，余额配置 JSON 保留。

旧行为应在 RED 阶段分别暴露前端限制错误或后端 `上游余额查询仅支持单 API Key 账户`。

- [ ] **Step 3: 实现前端非阻断收敛**

以最终非空、去重并通过凭据校验的 Key 数量为准。Key 数量大于 1 时：

- 保存 payload 明确提交 `balanceQueryEnabled=false`。
- 不继续校验余额适配器和自定义字段。
- API Key 区域提示“多 Key 账户不支持余额查询，保存后将自动关闭余额查询”。
- 保留表单里的余额配置草稿；账户实际保存为多 Key 后，后续再减少为单 Key 时开关仍为关闭，用户可自行重新开启。

- [ ] **Step 4: 实现后端唯一事实规则**

在余额配置纯规则中增加“根据最终凭据解析有效 Key 数量并收敛写入状态”的入口。账户创建和更新必须先完成 credentials 合并，再调用该规则；多 Key 时返回关闭状态而不是抛错。完整编辑、基础编辑、管理端和直接 API 共用同一规则，不能散落多组条件分支。

业务写入固定为：

```text
balance_query_enabled = false
balance_query_next_refresh_at = NULL
balance_query_config_json = 保留现有或本次合法配置
```

- [ ] **Step 5: 收敛快照和并发结果**

自动关闭成功后通过现有 stats-writer 幂等删除 `relay_balance` 快照。删除不要求与业务库跨库强事务：关闭状态和空调度时间先阻止展示与领取。余额请求在保存前已经开始时，完成阶段必须重新核对配置版本、启用状态和单 Key 资格，条件不满足就丢弃结果，不能重新安排下次刷新。

- [ ] **Step 6: 运行专项 GREEN**

```powershell
pnpm --filter juhe-ai-frontend test:account-balance-query
pnpm --filter juhe-ai-backend test:account-balance-config
pnpm --filter juhe-ai-backend test:account-balance-refresh
```

Expected: 多 Key 创建和编辑全部成功；余额关闭、调度停止、旧结果丢弃；单 Key余额查询原有路径继续通过。

- [ ] **Step 7: 运行相邻回归与全项目验证**

```powershell
pnpm --filter juhe-ai-backend test:account-api-key-gateway-mock-ai
pnpm --filter juhe-ai-backend test:account-api-key-pool-test-mock-ai
pnpm --filter juhe-ai-backend test:account-api-key-draft-activation
pnpm --filter juhe-ai-backend test:account-balance-auto-detect
pnpm typecheck
pnpm build
git diff --check
```

Expected: 多 Key 轮换、草稿激活、余额自动探测、前后端类型和生产构建均通过，只允许既有构建 warning。

- [ ] **Step 8: 手动验收**

依次验证：新建多 Key、单 Key 开启余额后新增 Key、多 Key 删除到单 Key、管理端编辑和普通用户编辑。所有多 Key 保存都必须成功并展示中文自动关闭说明；单 Key 恢复后余额开关保持关闭，重新手动开启才能出现余额行和后台刷新。
