# PLAN-20260722T232105008Z AI 性能与统计窗口渐进加载

## 基本信息

- 计划 ID：PLAN-20260722T232105008Z
- 状态：进行中（usage-window/AI-IP、table history 六字段和 AI base/series 已完成，真实 PostgreSQL/浏览器证据待补），禁止上线
- 创建时间：2026-07-23
- 需求来源：[全站页面 API 按需加载审计报告](../reports/全站页面API按需加载审计报告-2026-07-20.md) P2
- 执行范围：Node 后端、Vue 前端、测试与文档
- 明确排除：Go、提交、推送、部署、生产和远端状态

## 目标

把 AI 性能拆为“默认前 10 base + `series?accountIds` 追加序列”，让 AI/IP 共用的 usage-window 去掉页面级强制刷新并以服务端时区建立首次隐式窗口，同时把表监控 database history 收窄为专用六字段投影。

## 关联文档

- [专项设计](../superpowers/specs/2026-07-23-AI性能与统计窗口渐进加载设计.md)
- [全站页面 API 按需加载审计报告](../reports/全站页面API按需加载审计报告-2026-07-20.md)
- [AI 性能监控设计](../functions/AI性能监控设计.md)
- [SQLite 存储说明](../functions/SQLite存储说明.md)
- [页面数据缓存与增量更新设计](../functions/页面数据缓存与增量更新设计.md)

## 范围边界

### 确认后实施

- Node 将 AI 性能 overview 拆为默认最多 10 个账户的 base 和按 `accountIds` 返回可见追加账户的 series。
- 为 base/series 增加独立 DTO、repository 读取边界、SQLite read-worker operation 和 PostgreSQL 路径；不恢复已退场的通用 page-data 缓存。
- Vue 首屏只读 base，新增账户仅补 series，并实现 scope/range/ID/generation 合并与局部错误。
- AI/IP 改用按身份和 admin/self 隔离的 usage-window singleflight/短缓存；首次隐式范围必须由服务端窗口锚定。
- 表监控 database history 改用专用窄 row/DTO/worker/API/前端类型，保留视口加载和现有图表语义。
- 补齐 HTTP、query guard、worker、真实 PG、前端请求矩阵、时区和竞态测试，并修复发现的测试夹具漂移。

### 本次不做

- 不修改 Go、schema、统计 worker、刷新频率、统计口径或预聚合表。
- 不改变 IP 列表/详情业务字段，不重做表监控 overview、单表 history 或清理功能。
- 不新增实时明细扫描、Redis 聚合、长期缓存或用户偏好持久化。
- 不提交、不推送、不部署、不操作生产。

## 已确认的设计候选

- AI base：保留现有管理/个人 `/ai-performance` 路径，但只接受 scope/range，旧 `accountIds` 返回 400；响应为 `range + summary + 默认 accounts(<=10) + 默认 hourlySeries`。
- AI series：新增管理/个人 `/ai-performance/series`，要求 1..20 个重复裸键 `accountIds`，返回 `range + 可见 accounts + hourlySeries`，不可见/不存在 ID 静默省略。
- 前端：首屏不请求 series；搜索新增仅补缺失 ID，删除不请求，日期/owner/身份变化后按新范围重载 base 和仍需的追加 IDs。
- usage-window：admin/self + 用户 + 角色 + auth revision 隔离，精确 singleflight、约 60 秒短缓存；AI/IP 页面加载不再传 `force`。
- 首次时区：没有显式日期时，AI 三日与 IP 快捷范围必须等待真实服务端窗口；浏览器 fallback 不得驱动首个业务请求。
- table history：严格 `databaseRole/sampledAt/fileBytes/walBytes/freeBytes/tableCount` 六字段，SQL 和 mapper 不读取 `shmBytes` 或其他宽快照列；history 使用专用主库加 WAL 计算，不复用 overview 宽 helper。
- 预聚合：AI 仅读排行/小时/摘要窗口，IP 仅读现有 IP 窗口，table history 仅读数据库快照；禁止请求时扫描明细或实时采样降级。

以上内容在用户明确确认前只作为设计草稿，不授权修改运行时代码。

## 执行清单

- [x] 核对审计报告、AI 性能 overview、usage-window composable、IP 统计和表监控 history 当前调用链。
- [x] 形成 base/series、首次服务端时区窗口和 history 窄投影专项设计。
- [x] 用户确认 AI DTO 变更、series 参数、窗口失败边界和 history 字段。
- [x] 实现 Node AI base/series parser、DTO、repository 和独立读取边界。
- [x] 实现 AI base/series SQLite worker 与 PostgreSQL 等价路径。
- [x] 实现 Vue AI base/series 合并、局部错误和完整竞态保护。
- [ ] 实现 usage-window 身份 key、非 force 复用和 AI/IP 首次服务端时区边界。
- [x] 实现 table-monitor history 专用 DTO、显式列、mapper、worker 和前端类型。
- [ ] 增加 HTTP、query guard、worker、真实 PostgreSQL smoke 和 fixture 漂移回归。
- [ ] 增加前端请求次数、缓存、时区、scope/range/generation 与 A→B→A 回归。
- [ ] 同步 AI 性能、统计窗口、IP、表监控、接口矩阵和审计报告权威文档。
- [ ] 运行 typecheck/build/diff check、浏览器网络验证和双 Agent 复核门禁。

## 独立验收切片

1. AI Node 切片：base 默认 10、series IDs、权限省略、SQL/缓存、SQLite worker/PG。
2. AI Vue 切片：首屏 base、追加 series、合并去重、局部失败和 scope/range/generation 竞态。
3. 窗口切片：AI/IP 去 force、admin/self 身份缓存、首次服务端时区边界和失败重试。
4. 表监控切片：history 六字段投影、视口行为、worker/PG 和图表语义。
5. 整合切片：fixture 漂移修复、既有回归、浏览器网络矩阵、文档与交叉复核。

每个切片可单独验证，但只有五个切片全部完成并形成权威证据后，PLAN-0160 才能标记完成。

## 测试项

- AI parser/HTTP：base 无追加 IDs；series 重复裸键/bracket、去重、1/20/21、非法形状和范围。
- AI DTO/query：base 最多 10、series 只返请求可见项、无重复 summary/rank、无 overview 裁剪、无 N+1/明细扫描。
- 权限：管理全局/owner、个人 self、自有/授权/授权分组、撤销授权、伪造 scope、不可见/不存在省略。
- 窗口：admin/self/身份 revision 隔离、singleflight、TTL、失败不伪装服务端成功、AI/IP 首次跨时区日界。
- 表 history：精确六字段、窄 SQL/mapper、每角色 limit、稳定排序、主库加 WAL 曲线与 tooltip 不变。
- 存储：SQLite 同步/read worker 与真实 PostgreSQL 逐字段等价；PG SKIP 不计通过。
- 前端：首屏请求矩阵、新增/删除/重置、日期/owner 切换、A→B→A、迟到成功/失败、卸载、局部重试。
- fixture：旧 overview、账户/profile、统计窗口和快照 fixture 对齐当前生产构造；不得放宽生产约束掩盖漂移。
- 回归：AI options/小时图、IP list/detail、usage-window、table overview/history 和 page-data revision 不退化。

## 验收标准

- AI 首屏最多返回并渲染默认 10 个账户；增加账户不重取 base，只按 ID 请求 series。
- AI series 对不可见 ID 静默省略，管理/个人统计作用域与摘要口径保持不变。
- AI/IP 普通加载、筛选、分页、排序和刷新不再强制请求 usage-window；缓存未过期时窗口网络请求为 0。
- 首次隐式日期范围有服务端时区窗口作为证据，窗口失败时不按浏览器本地日界误发业务请求。
- table history 从 SQL 到 Vue 严格只传六字段，同时主库加 WAL 曲线、空闲和表数 tooltip 与专用 history 口径一致。
- 三类页面均不退化到明细扫描、实时聚合或实时数据库采样。
- SQLite worker、真实 PostgreSQL、前端竞态、浏览器请求矩阵、fixture 和独立复核全部闭合。

## 风险与注意事项

- base DTO 删除 `defaultAccounts/selectedAccounts` 是显式契约变更，必须同步所有 Node/Vue 类型和回归，不能保留宽数组只为少改代码。
- series 的可见省略会使请求数与响应数不同，前端必须按 ID 合并，不能依赖数组位置补占位。
- usage-window 的浏览器 fallback 不能被误认为服务端统计日界；错误状态必须可见并可重试。
- history 不返回 `shmBytes`，前端不能再复用 overview 的 `totalDatabaseBytes`；`tableCount` 参与 tooltip，不能从六字段投影删除。
- fixture 修复必须证明是旧假设漂移，不能用测试修改掩盖真实权限、SQL 或时区回归。
- 当前工作树已有大量既有改动，禁止整理、回退或提交无关差异。

## 进度记录

| 日期 | 状态 | 记录 |
| --- | --- | --- |
| 2026-07-23 | 进行中 | AI/IP 已改为按 admin/self 复用 usage-window，普通筛选/分页不再强制刷新，窗口失败不会驱动业务请求；AI base/series 已拆分为独立 Node/Vue 契约，新增账户只请求 series，删除零请求，身份/KeepAlive/owner 竞态门和 options 失效已补。 |
| 2026-07-23 | 进行中 | table-monitor database history 已从 SQL、mapper、worker result 到 Vue 收窄为六字段，历史曲线只计算主库文件与 WAL；SQLite/query/display/视口回归通过。AI base/series 与前端专项回归通过；真实 PG/浏览器仍待补。 |

## 验证记录

- 已通过 stats-date-range-window、usage-stats progressive loading、授权 usage-window、table-monitor default-scan/display/progressive-loading、AI base/series HTTP/worker/query guard、前端 AI 请求矩阵与 Node typecheck。真实 PG、浏览器和完整前端 typecheck仍待验证。

## 完成总结

待实施与验收后填写。
