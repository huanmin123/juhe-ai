# 网关审计、健康检查协议与管理筛选修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复超限请求审计缺失、跨协议上下文污染、AI 账户健康检查协议不可配置、管理员使用记录空列表和操作日志单字搜索失效，并完成生产发布与反向合并。

**Architecture:** 审计超限只落无正文元数据；健康检查持久化精确 endpoint mode，后台直接使用保存值；Codex 内部上下文先解析为协议无关状态，再在每次选中真实账号后渲染；管理员全用户使用记录只开放当天有界窗口；操作日志搜索索引增加单字词项。所有实现从 `master` 的独立 worktree 开发，测试通过后汇总到集成分支。

**Tech Stack:** Node.js 22、TypeScript、Express、Vue 3、Ant Design Vue、SQLite、PostgreSQL、Go、pnpm workspace。

---

## 基本信息

- 计划 ID：PLAN-20260713T102225000Z
- 状态：已完成
- 创建时间：2026-07-13
- 完成时间：2026-07-14
- 需求来源：Codex 会话 `019f59f5-0408-7483-af0a-0fbb781d0484`、`019f5a36-3a17-76a3-9692-a8c736c07b1b`、`019f5a09-1775-7751-a04e-8bcf828f9bc1`、`019f5a43-7451-7ee1-bc85-e54b38591848`
- 执行方式：独立 worktree + TDD + 并行子代理 + 集成复审

## 范围边界

- 超限审计不保存请求正文，不新增审计表字段，不提高正文大小限制。
- 健康检查保存精确 JSON / Streaming mode；GPT 默认 `responses_sse`，其他档案默认 JSON，手工测试仍允许临时选择其他已启用 mode。
- 内部 `juhecmp.v1/v2` 和内部 `previous_response_id` 不得发送给原生上游；外部原生 `previous_response_id` 未命中内部索引时保持透传。
- 管理员未选择用户时，使用记录只允许当天、创建时间倒序和现有 1001 行有界窗口；业务筛选和手工日期必须先选择用户。
- 操作日志保持全用户筛选能力，仅修复单字搜索并补离线词项回填。
- 不增加 Redis 内存上限，不把大正文或大上下文放入 Redis Stream 消息。

## 设计契约

### 超限审计

`recordDroppedAuditCapture` 在 `gateway_body_rejected` 时写入一条 `client_request` payload：

```ts
{
  partType: 'client_request',
  rawBodySizeBytes: input.bytes,
  contentType: input.contentType,
  captureStatus: 'overflow'
}
```

该 payload 不含 `body` 和 `headers`。审计错误信息记录实际大小、限制值和限制范围，客户端 413 响应契约不变。

### 健康检查请求形态

新增不可空字段：

```ts
type AccountHealthCheckEndpointMode =
  | 'chat_json' | 'chat_sse'
  | 'responses_json' | 'responses_sse'
  | 'messages_json' | 'messages_sse'
  | 'generate_content_json' | 'generate_content_sse'
```

默认值由供应商协议档案和已启用请求形态决定：OpenAI 官方 GPT 使用 `responses_sse`，通用 OpenAI 兼容使用 `chat_json`，Anthropic 使用 `messages_json`，Gemini Native 使用 `generate_content_json`；首选不可用时优先使用第一个已启用 JSON mode，再使用第一个可检查 mode。后台探针直接使用保存值，不保留协议族映射。

### 跨协议上下文

请求预处理阶段只生成不可变的协议无关上下文：

```ts
interface CodexResponsesMaterializedContext {
  canonicalBody: JsonRecord
  resolvedInput: unknown[]
  previousResponseKind: 'none' | 'internal' | 'external'
  internalState?: CodexResponsesChatBridgeRequestState
}
```

`buildPreparedUpstreamRequestParts` 每次 dispatch attempt 根据实际选中的账号和模型映射重新克隆并渲染请求。显式 Responses-to-Chat bridge 使用桥接状态；原生 Responses 使用普通 Responses message；其他协议适配器只接收普通输入项。completion handler 只绑定实际显式 bridge 账号。

### 管理员列表

管理员全用户使用记录请求必须满足：

```ts
systemAccountId === undefined
startAt === 当天开始
endAt === 次日开始
sortBy === 'createdAt'
sortOrder === 'desc'
无 accountKeyword/result/statusCode/clientIp/groupId/model/traceId/trafficSource
```

后端对不满足契约的请求返回 400。前端使用 `dateRangeMode: 'auto' | 'manual'`，自动模式在零点或页面恢复可见时更新当天并重新加载。

## Task 1：超限审计无正文元数据

**Files:**
- Modify: `backend/src/modules/audit-logs/audit-log-queue.service.ts`
- Modify: `backend/src/modules/gateway/request/body-middleware.ts`
- Modify: `backend/src/scripts/regression/audit-log-retention-regression.ts`
- Modify: `frontend/src/views/audit-logs/AuditLogDetailDrawer.vue`
- Modify: `frontend/scripts/regression/audit-log-detail-layout-regression.ts`

- [x] **Step 1: 写失败回归**

在现有 413 回归中断言 `payloadCount=1`、`rawPayloadBytes` 等于实际正文大小、payload 无正文且状态为 `overflow`；前端回归断言无正文 payload 仍显示“超出捕获上限”。

- [x] **Step 2: 验证红灯**

```powershell
pnpm --filter juhe-ai-backend test:audit-log-retention
pnpm --filter juhe-ai-frontend test:audit-log-detail-layout
```

预期：现有空 `payloads` 和“未捕获”分支导致断言失败。

- [x] **Step 3: 最小实现并验证绿灯**

按设计契约写入无正文 payload，透传限制元数据，只调整详情状态展示条件。

```powershell
pnpm --filter juhe-ai-backend test:audit-log-retention
pnpm --filter juhe-ai-frontend test:audit-log-detail-layout
pnpm --filter juhe-ai-backend typecheck
pnpm --filter juhe-ai-frontend typecheck
```

预期：全部通过。

## Task 2：账户健康检查精确请求形态

**Files:**
- Modify: `backend/src/domain/types.ts`
- Modify: `backend/src/domain/provider-protocol.ts`
- Modify: `backend/src/storage/schema/business-schema.ts`
- Modify: `backend/src/storage/repositories.ts`
- Modify: `backend/src/modules/accounts/account-test.service.ts`
- Modify: `backend/src/modules/background/account-health-check.service.ts`
- Modify: `backend/src/modules/accounts/account-health-check-dispatch.service.ts`
- Modify: `backend-go/db/migrations/000005_w1b_public_accounts.sql`
- Modify: `backend-go/internal/modules/managementaccounts/*`
- Modify: `backend-go/internal/store/postgres/*accounts*`
- Modify: `frontend/src/types/domain.ts`
- Modify: `frontend/src/views/accounts/*`
- Modify: `backend/src/scripts/maintenance/mockdata/*`
- Modify: `docs/functions/AI账户检查模型与人工测试设计.md`
- Modify: `docs/functions/账号健康检测设计.md`

- [x] **Step 1: 写精确 mode、供应商默认值和未启用 mode 失败测试**

覆盖 OpenAI 官方 API Key/OAuth、通用 OpenAI、Anthropic、Gemini Native、能力回退、禁用能力拒绝、后台健康检查和账户默认手工测试。

- [x] **Step 2: 验证红灯**

```powershell
pnpm --filter juhe-ai-backend test:account-health-check
pnpm --filter juhe-ai-backend test:account-test-responses-contract
go test ./internal/modules/managementaccounts ./internal/store/postgres
```

预期：字段缺失或现有 SSE/Chat 默认顺序导致断言失败。

- [x] **Step 3: 按当前 schema 完整同步**

新增不可空数据库列和 API 字段，同步 CRUD、批量编辑、导入导出、授权实例、外部推送、Go 管理接口、Mockdata 和前端表单。运行时代码不读取旧字段。

- [x] **Step 4: 前端交互**

在“健康检查配置”中把“检查模型”和“检查协议”同排展示；移动端自然换行；检查协议选项只展示账户已启用的 JSON / Streaming 生成请求形态，工具、计数和嵌入接口不展示。

- [x] **Step 5: 验证绿灯**

```powershell
pnpm --filter juhe-ai-backend test:account-health-check
pnpm --filter juhe-ai-backend test:account-health-check-dispatch
pnpm --filter juhe-ai-backend test:account-test-responses-contract
pnpm --filter juhe-ai-backend test:account-batch-edit
pnpm --filter juhe-ai-backend test:account-tags
pnpm --filter juhe-ai-backend test:postgres-schema-sql
go test ./internal/modules/managementaccounts ./internal/store/postgres ./internal/httpapi
pnpm typecheck
pnpm --filter juhe-ai-frontend build
```

预期：全部通过。

## Task 3：按选中账号渲染 Codex 内部上下文

**Files:**
- Modify: `backend/src/modules/gateway/codex-responses/chat-bridge-state.ts`
- Modify: `backend/src/modules/gateway/request/preflight.ts`
- Modify: `backend/src/modules/gateway/dispatch/account-preparation.ts`
- Modify: `backend/src/modules/gateway/dispatch/upstream-dispatch.ts`
- Create: `backend/src/scripts/regression/codex-cross-protocol-context-regression.ts`
- Modify: `backend/package.json`
- Modify: `docs/functions/Codex-Responses转Chat状态保持设计.md`

- [x] **Step 1: 写失败回归**

覆盖纯原生候选、纯 bridge 候选、混合候选、bridge 到原生重试、原生到 bridge 重试、内部 previous response、外部 previous response、真实原生 encrypted content，以及任何非 Chat 上游都不出现 `juhecmp`。

- [x] **Step 2: 验证红灯**

```powershell
pnpm --filter juhe-ai-backend test:codex-cross-protocol-context
```

预期：当前候选集合预处理把内部结构发送给原生 Responses，测试失败。

- [x] **Step 3: 实现不可变解析和 attempt 级渲染**

预处理阶段只解析并缓存；账号准备阶段按实际映射克隆请求；所有内部状态保存和 completion handler 绑定都使用实际选中账号。

- [x] **Step 4: 验证绿灯和相邻回归**

```powershell
pnpm --filter juhe-ai-backend test:codex-cross-protocol-context
pnpm --filter juhe-ai-backend test:deepseek-gateway-mock-ai
pnpm --filter juhe-ai-backend test:codex-context-state-sharding
pnpm --filter juhe-ai-backend test:codex-context-state-writer-pool
pnpm --filter juhe-ai-backend test:codex-turn-switch-e2e
pnpm --filter juhe-ai-backend typecheck
```

预期：全部通过。

## Task 4：管理员使用记录和操作日志筛选

**Files:**
- Modify: `backend/src/storage/usage-records.repository.ts`
- Modify: `backend/src/storage/usage-record-list-query.ts`
- Modify: `backend/src/modules/usage-records/usage-records.routes.ts`
- Modify: `frontend/src/views/usage-records/UsageRecordsView.vue`
- Modify: `frontend/src/views/usage-records/UsageRecordsFilterToolbar.vue`
- Modify: `frontend/src/views/usage-records/usageRecordPageState.ts`
- Modify: `backend/src/storage/operation-log-search.ts`
- Modify: `backend-go/internal/store/postgres/operationlogs.go`
- Create: `backend/src/scripts/maintenance/rebuild-operation-log-search-terms.ts`
- Modify: `backend/src/scripts/regression/usage-record-list-query-guard-regression.ts`
- Modify: `backend-go/internal/store/postgres/operationlogs_test.go`

- [x] **Step 1: 写全用户当天窗口、强制选用户和午夜滚动失败测试**

断言管理员默认能看到当天最新记录；全用户附带任一业务筛选返回 400；选择用户后筛选正常；自动日期跨日更新而手工日期不变。

- [x] **Step 2: 写操作日志单字搜索失败测试**

Node 和 Go 同时断言中文、英文和数字单字能生成词项并命中；离线重建按游标分批处理，不把全表读入内存。

- [x] **Step 3: 验证红灯**

```powershell
pnpm --filter juhe-ai-backend test:usage-record-list-query-guard
go test ./internal/store/postgres ./internal/httpapi
```

预期：现有管理员选择 guard 和最小 2 字搜索规则导致失败。

- [x] **Step 4: 最小实现并验证绿灯**

复用日期分片/分区裁剪和现有 1001 行窗口，不新增统计扫描；前端引入自动日期模式和 visibility/零点刷新；搜索索引增加去重单字词项。

```powershell
pnpm --filter juhe-ai-backend test:usage-record-list-query-guard
pnpm --filter juhe-ai-backend test:usage-record-list-catalog-window
pnpm --filter juhe-ai-backend test:operation-log-queue
go test ./internal/store/postgres ./internal/httpapi ./internal/modules/managementoperationlogs
pnpm typecheck
pnpm --filter juhe-ai-frontend build
```

预期：全部通过。

## Task 5：集成、真实测试环境和上线

- [x] **Step 1: 逐分支规格复审和代码质量复审**
- [x] **Step 2: 将已审核提交 cherry-pick 到集成分支并解决文档索引冲突**
- [x] **Step 3: 执行完整定向回归、类型检查、构建、打包和生产候选 release 健康预检**
- [x] **Step 4: 在 `192.168.1.203` 测试 PostgreSQL/Redis 执行 schema 同步、字段回填和 PG smoke**
- [x] **Step 5: 在部署文档新增上线计划、迁移 SQL、回滚 SQL和问题记录**
- [x] **Step 6: 合并并推送 `master`，按生产临时服务接管流程上线**
- [x] **Step 7: 验证内网、本机、公网 health、PG、Redis Streams，并以专项自动化覆盖管理筛选、审计详情和跨协议会话**
- [x] **Step 8: 将 `master` 反向合并到 `feature/20260706-go` 并推送远程**

## 验收标准

- 413 审计显示实际大小和 overflow，不保存正文。
- 健康检查协议可配置、持久化并在所有后台探针使用 JSON。
- bridge 和原生账号互相切换时内部压缩标记不泄漏，内部历史连续可用。
- 管理员默认看到当天使用记录，筛选前选择用户，跨日自动刷新。
- 操作日志单字搜索可用，原有多字和精确筛选不回归。
- `master`、生产 release 和 `feature/20260706-go` 最终包含同一修复提交；部署问题进入上线文档。

## 进度记录

- 2026-07-13：完成四个会话、源码、Git、生产只读数据和部署链路复核；方案经用户确认。
- 2026-07-13：创建四个独立 worktree，进入并行 TDD 实现。
- 2026-07-13：四项实现与三轮独立复审完成并合入隔离集成分支；补齐原生 Responses 零重写、健康协议 JSON 默认和测试夹具交叉契约。
- 2026-07-13：`192.168.1.203` 一次性隔离 PostgreSQL 数据库完成 613 条 schema、166 条 seed，模型映射异步 smoke 与账户导出 smoke 通过；临时数据库已删除。
- 2026-07-14：修复代码以 `20617dda7` 推送 `master`，生产 release `20260713-2356-four-fixes-20617dda7` 完成迁移、切流、临时资源清理和最终单主服务稳定性验收。
- 2026-07-14：代码反向合并提交 `18b1cb69c` 已将 `master` 纳入 `feature/20260706-go`；另以 `12fdcc9e0` 修复 Windows CRLF 导致的源码文本断言假失败，最终文档随收口提交再次同步。

## 验证记录

- 定向审计、健康检查协议、Codex 跨协议上下文、管理筛选、操作日志搜索、上下文分片/写入器和切号 E2E 回归通过；`pnpm typecheck`、`pnpm build`、`go test ./...`、`git diff --check` 通过。
- 测试 PostgreSQL 使用一次性隔离数据库初始化 613 条 schema 和 166 条 seed，真实异步模型映射与账户导出 smoke 通过，临时数据库已删除。共享测试库的历史 DDL 漂移不作为当前 release 证据。
- 候选服务预检 180 秒、正式服务 direct 60 秒、公网 180 秒、临时资源清理后 60 秒和最终单主服务 180 秒均通过；最终为 1 个 server、1 个 DB service、3 个 worker，watchdog 正常，临时端口、临时数据库和 Redis 临时 namespace 均已清理。
- 生产 PostgreSQL 的目标字段存在，非法协议族和授权实例错配均为 0。迁移 SQL 累计影响 2165 行；离线脚本扫描 964 个唯一物理账户，实际修正协议族 10 个并补齐 1 个同族 JSON 凭据能力。
- 操作日志重建执行时处理 995 条日志、生成 224109 个词项和 11989 个单字符词项；上线期间新增日志后，最终在线为 1022 条日志、228147 个词项和 12288 个单字符词项。
- Redis cache 为 6.67 MB / 768 MB，state/queue 为 6.16 MB / 2 GB；Stream 最大长度 2、pending 1、最大 lag 1，容量与消费状态正常，本次不提高内存上限。
- 真实流量 A/B 中候选服务 220 次命中、7 个 5xx（3.2%），旧主服务 142 次命中、5 个 5xx（3.5%）；新版本未放大既有上游业务失败。

## 风险与注意事项

- 生产浏览器没有现成认证会话，未执行登录后的管理筛选、审计详情和跨协议会话人工页面复验；功能语义由专项自动化覆盖，构建和生产验收仅覆盖发布完整性与服务稳定性，不能描述为已人工验证。
- 首次正式主进程在候选与正式两套 worker 重叠期间被 launchd 拉起一次；清理候选后固定 PID 连续 180 秒稳定。发布流程已调整为切回主服务后立即清理候选，再做长观察。
- PostgreSQL 临时建库只能使用本机管理角色；应用账号继续只负责业务 dump、restore、迁移和校验，不扩大其权限。

## 完成总结

- 四项修复已进入 `master` 和生产 release，`feature/20260706-go` 已完成反向合并；生产代码提交为 `20617dda7`，代码反向合并提交为 `18b1cb69c`。
- 生产 current 指向 `20260713-2356-four-fixes-20617dda7`，最终单主服务拓扑、PostgreSQL、Redis、Nginx 和 watchdog 均稳定。
- 发布中发现的数据库权限、双 worker 重叠、无认证浏览器会话、Windows CRLF 测试假失败等问题均已修正或记录到上线问题文档，后续按更新后的脚本和门禁执行。
