# PLAN-0076 管理接口读写隔离与前台防阻塞

## 基本信息

- 编号：`PLAN-0076`
- 状态：`已完成`
- 创建时间：`2026-07-04`
- 需求来源：用户反馈本地 `AI 账户管理` 管理接口被后台 worker / SQLite 锁等待拖到秒级，并要求读请求不排队、不被后台写阻塞。
- 执行者：Codex 与并行 gpt-5.5 agents
- 关联模块：后端 / DB service / System API / SQLite / PostgreSQL 高并发模式 / 后台 worker / 管理列表 / 统计窗口 / 前端接口契约

## 需求目标

- 管理端和网关业务读请求不得被后台 worker 写入、维护任务或统计刷新无意义阻塞。
- 前端接口契约保持稳定：前端请求什么，后端仍返回对应真实数据；不通过返回空数据、删字段或弱化页面能力来掩盖性能问题。
- SQLite 单机模式下，只对写入和锁等待做必要治理；读请求不进入写队列，遇到写锁时不能长时间同步卡死 DB service 事件循环。
- PostgreSQL / 高并发模式下，读写默认依赖数据库连接池、MVCC、事务和行锁并发能力；应用层不继续用 SQLite 单写队列思维串行纯读。
- 后台 worker、网关 side effects、维护任务等非前台关键路径写入必须让位于前台业务读。

## 问题定义

- 现象：本地打开 `AI 账户管理` 时，`/accounts` 约 `1.4s`，并导致 `providers/options`、`proxies/options`、`accounts/tags` 等轻量接口在浏览器 Network 中一起显示秒级。
- 已确认根因：
  - `accounts` 列表在请求链路同步读取统计装饰字段，可能等待 stats SQLite 写锁。
  - SQLite 默认 `busy_timeout=5000ms`，同步等待会卡住 DB service 事件循环。
  - 后台 worker DB service typed operation 过去默认 high priority，可能和前台业务抢队列。
  - System API HTTP 路由虽然不走 IPC typed operation 队列，但在 DB service 进程内直接执行 repository；统一 admission、同步 SQLite 调用和认证 `touchSession` 仍会把读请求变成被动阻塞或隐藏写。
- 扩展风险：
  - `groups`、`api-keys`、`stats/usage-overview`、运行日志 facets/runtime snapshot、网关 side effects 也存在读写职责混杂或装饰字段阻塞风险。
  - 高并发模式消除了 SQLite 文件级写锁，但仍可能被应用层 DB service 串行队列、连接池耗尽、行锁、Redis、实时聚合和前端 fanout 拖慢。

## 范围边界

### 本次做

- 建立 DB service typed operation 读写分类：`read`、`write`、`maintenance`、`runtime`、`ambiguous`。
- 让纯读 operation 在 PostgreSQL 模式下直接并发执行，不进入写队列。
- 让 SQLite 模式下纯读不进入写队列；写操作继续集中治理。
- 后台 worker typed DB service 请求默认低优先级；网关非关键 side effects 写入不抢前台读。
- System API HTTP 路由补显式读写元数据，避免按 HTTP 方法误判；认证 touch、操作日志、last_used 等副作用拆入低优先级写路径或合并写路径。
- 修正管理列表和统计接口中的请求链路重活：
  - `accounts` 统计装饰锁等待治理。
  - `groups` 统计 / runtime 装饰读写隔离。
  - `api-keys` usage 装饰读写隔离。
  - `stats/usage-overview` 不在请求链路临时聚合明细或日表。
- 补充回归测试覆盖“后台写不堵前台读”“纯读并发”“前端契约不变”。

### 本次不做

- 不删减前端字段，不让接口返回空装饰数据作为长期方案。
- 不用 `items: []`、空 stats Map、空 usage Map 这类伪成功响应掩盖 SQLite busy、锁等待、慢查询或后台任务延迟。
- 不把前端首屏请求拆掉作为主要修复手段；前端体验和契约优先。
- 不引入运行时旧 schema 兼容或自动迁移逻辑。
- 不把 PostgreSQL 真实服务器凭据写入文档、日志或代码。

## 关键设计

### 读写隔离原则

- 纯读：
  - 不进入写队列。
  - PostgreSQL 模式直接走 async pool 并发。
  - SQLite 模式不得长时间同步等待写锁；当前通过 query-only 只读连接和 SQLite read worker pool，把 busy wait/retry 放到 worker 子进程内，避免卡死 DB service 主事件循环。
- 写入：
  - SQLite 模式继续受写队列、批次和优先级保护。
  - PostgreSQL 模式依赖事务、唯一约束、行锁和连接池；只对确有资源顺序要求的写引入 keyed 串行。
- 后台维护：
  - 默认低优先级。
  - 必须小批次、可续跑、可超时。
- 不确定读写属性的 operation：
  - 先按写处理，避免把隐藏副作用放入并发读通道。

### System API 路由分类原则

- 不只按 HTTP 方法判断读写属性，路由需要显式标注：`noDb`、`read`、`readWithSideEffect`、`write`、`longRead`。
- `GET /auth/me`、所有经过 session touch 的已认证 GET、公开接口鉴权成功后的 `last_used_at` 更新，属于读请求夹带副作用，需要拆副作用或进入低优先级写路径。
- `POST /accounts/import/preview` 是预览读，不应因为 POST 自动进写队列。
- `POST /accounts/export` 如果记录操作日志，就是读 + 写副作用；不能按纯读处理。
- `GET /audit-logs/search-hot`、`GET /runtime-logs/grep` 属于长 IO / 长查询读，需要独立并发限制和 deadline，但不能降级为空结果。

### 前端契约原则

- 管理页面已有字段保持返回。
- 如果某字段成本高，应在后端用缓存、预聚合、读通道并发或后台刷新解决。
- 只有明确是“运行态不可观测”的接口才允许返回 `available:false` 之类状态；业务数据不能无提示返回空。

### SQLite 与 PostgreSQL 差异

- SQLite：
  - 文件级写锁和同步 `node:sqlite` 调用会放大阻塞。
  - 写必须集中治理；读要避免长 busy wait 卡住事件循环。
  - 读通道必须返回真实数据或明确错误；不能因为锁忙返回空统计、空列表或缺字段。
- PostgreSQL：
  - 不存在 SQLite 同类全库文件写锁。
  - 仍需关注连接池、热点行锁、长事务和慢查询。
  - 应用层不要无意义串行纯读。

## 执行拆解

- [x] 先行止血：`accounts` 列表统计装饰读取增加短 SQLite busy timeout，避免 stats 写锁拖死业务列表。
- [x] 替换临时止血：移除 stats 锁忙返回空 Map 的伪成功路径，改为真实读取，锁忙时明确返回错误。
- [x] 先行止血：`background worker -> DB service` typed operation 显式低优先级。
- [x] 建立 `DbServiceOperation` 读写分类工具，并用回归锁住分类。
- [x] 改造 DB service typed operation 调度：
  - [x] PostgreSQL operation 不进入 SQLite 式串行队列。
  - [x] SQLite 纯读 / runtime 不进入写队列。
  - [x] 写 / maintenance 保留受控调度。
- [x] 梳理 gateway side effects：
  - [x] 关键业务读保持 direct / high。
  - [x] 非关键状态写、成功快照、后台探针写降为 low。
- [x] 梳理 System API HTTP 路由：
  - [x] 增加路由读写元数据，不按 HTTP method 粗判。
  - [x] 认证 touch 按 route access mode 控制，显式纯读不再 touch session。
  - [x] 去掉对显式纯读的统一 DB admission 阻塞；长读单独限并发和 deadline。
- [x] 增加 SQLite 只读通道：
  - [x] `accounts` 管理列表接入 query-only SQLite read worker pool。
  - [x] 非 SQLite 写 owner 进程使用只读 SQLite handle，并叠加 `PRAGMA query_only=ON` 防止隐藏写。
  - [x] `accounts` / `accounts/options` / 账号详情读路径移除过期账号自动停用副作用；过期 sweep 保留给后台任务。
  - [x] 账号列表 worker 读返回真实统计；锁忙 / 读失败不返回伪造空数据。
  - [x] `groups`、`api-keys`、`route-strategies`、`proxies`、`system-accounts`、`providers`、`settings` 管理端主读路径接入 query-only read worker。
  - [x] `api-keys`、`route-strategies` 列表 / 选项 read worker 路径拆成 read-only 版本，不在读请求里创建默认数据。
  - [x] `runtime-logs` 列表 / 详情 / facets SQLite 读接入 query-only read worker，关键词搜索仍返回真实结果，不降级为空。
  - [x] typed DB service 中确认安全的仓储纯读接入 read worker pool：公开全局设置、供应商模型目录、OpenAI-compatible files / vector stores list / get / search / chunks。
  - [x] gateway runtime / cache / quota / health / cooldown / account-test 运行态读取保留在原 owner 边界，不误放入 query-only read worker。
- [x] 梳理管理列表接口：
  - [x] `api-keys` usage 装饰保持真实读取和字段契约。
  - [x] `groups` stats / runtime 装饰保持真实读取和字段契约。
  - [x] 保持返回字段和前端契约。
- [x] 梳理统计接口：
  - [x] `usage-overview` summary 由预聚合窗口提供，未发现请求链路实时扫描明细聚合。
  - [x] `account-usage`、`ai-performance` 读取窗口 / rank / summary 表，不在请求链路实时扫明细。
- [x] 补充回归测试：
  - [x] DB service 读写分类测试。
  - [x] 后台 / 网关写副作用低优先级测试。
  - [x] SQLite 锁忙不返回伪造空统计测试。
  - [-] PostgreSQL 模式真实服务器并发 smoke：本轮未改 PG 执行路径，不作为验收必需；PG 不套 SQLite 全局队列由调度回归固定，真实凭据不落文档。
- [x] 更新长期架构 / 高性能模式说明。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 类型检查 | 后端类型 | `pnpm --filter juhe-ai-backend typecheck` | 无类型错误 | 已通过 |  |
| 类型检查 | 全仓类型 | `pnpm typecheck` | 前后端类型通过 | 已通过 |  |
| 回归 | 账户列表 stats 锁等待 | `pnpm --filter juhe-ai-backend test:account-list-stats-lock` | stats 忙锁明确失败，不返回伪造空统计 | 已通过 |  |
| 回归 | SQLite 管理端 read worker | `pnpm --filter juhe-ai-backend test:sqlite-read-worker` | DB service SQLite 管理端主读进入 query-only 子进程，返回真实数据且不触发隐藏写 | 已通过 | 覆盖 accounts / groups / api-keys / route-strategies / proxies / system-accounts / providers / settings / model catalog / runtime-logs / OpenAI-compatible files / vector stores |
| 回归 | 账号列表查询 / 防隐藏写 | `pnpm --filter juhe-ai-backend test:account-list-query-guard` | 搜索 / 分组筛选走索引，账号读路径不更新过期账号 | 已通过 | 原“读路径小批量清理过期账号”已改为“读路径不写” |
| 回归 | 后台 DB service 请求低优先级 | `pnpm --filter juhe-ai-backend test:db-service-request-priority` | 纯读不强制 low，写副作用 low | 已通过 |  |
| 回归 | DB service 读写分类 | `pnpm --filter juhe-ai-backend test:db-service-operation-access-mode` | 纯读和写入分类稳定 | 已通过 |  |
| 回归 | DB service 读写调度模拟 | `pnpm --filter juhe-ai-backend test:db-service-read-write-dispatch-simulation` | SQLite 只排写 / 维护，PostgreSQL 不套 SQLite 全局队列 | 已通过 | 模拟前台读、runtime、后台写、维护任务 |
| 回归 | 前台读不被后台写阻塞 | `pnpm --filter juhe-ai-backend test:gateway-background-side-effect-priority` | 后台 / 网关写副作用不抢读路径 | 已通过 |  |
| 回归 | 管理列表契约 | `pnpm --filter juhe-ai-backend test:management-list-contract` | 字段契约不变，统计字段真实返回 | 已通过 | 不返回空数据 |
| 回归 | System API 路由读写分类 | `pnpm --filter juhe-ai-backend test:system-api-db-access` | 纯读不被统一 admission 拦截，副作用写被识别 | 已通过 | 覆盖 session touch / import preview / export / accounts 首屏 GET |
| 回归 | API Key 列表查询与密钥契约 | `pnpm --filter juhe-ai-backend test:api-key-list-query-guard` | 列表不返回完整密钥字段，复制 / reveal 走明确 secret 接口 | 已通过 | 防止敏感字段进入列表 |
| 回归 | 策略路由列表查询 | `pnpm --filter juhe-ai-backend test:route-strategy-list-query-guard` | 搜索仅按名称精确 / 前缀，列表筛选命中索引 | 已通过 |  |
| 回归 | 分组选项轻量读 | `pnpm --filter juhe-ai-backend test:group-options-lightweight` | options / account-options 不读取统计库 | 已通过 |  |
| 回归 | 运行日志搜索保护 | `pnpm --filter juhe-ai-backend test:runtime-log-search-guard` | 关键词仅匹配 message，不扫 rawJson | 已通过 |  |
| 回归 | 运行日志 keyword SQL | `pnpm --filter juhe-ai-backend test:runtime-log-keyword-only-sql` | 纯 keyword 查询保持 message 模糊匹配语义 | 已通过 |  |
| 回归 | DB service System API HTTP | `pnpm --filter juhe-ai-backend test:db-service-system-api-http` | 内部 health、公开设置、请求体边界可用 | 已通过 | 真实 HTTP 路径 |
| 真实压测 | SQLite worker real load | `JUHE_AI_WORKER_LOAD_DURATION_SECONDS=15; JUHE_AI_WORKER_LOAD_CONCURRENCY=12; pnpm --filter juhe-ai-backend test:worker-real-load` | 请求全成功，无 SQLite read error / lock signal | 已通过 | total=1453 success=1453 failed=0 p95=218.333ms readErrors=0 lockSignals=0 |
| 真实 HTTP smoke | SQLite System API performance smoke | `pnpm --filter juhe-ai-backend test:performance-system-api-smoke` | 登录、设置、系统账户、供应商、分组、授权、账号、API Key HTTP 链路通过 | 已通过 | 修正 smoke 分组创建 payload 以匹配当前 strict schema |
| 真实环境 | PostgreSQL 高并发 smoke | 远程测试 PostgreSQL / Redis 重建后运行完整 `test:*postgres-smoke` 矩阵 | 纯读并发、写入事务、统计窗口、记录清理、导入、表监控和公告链路正常 | 已通过 | 33 个 PG smoke 全部通过；凭据不落文档 |
| 真实环境 | PostgreSQL System API smoke | 远程测试 PostgreSQL / Redis 运行 `pnpm --filter juhe-ai-backend test:performance-system-api-smoke` | 管理端 HTTP 主链路在 SQLite 与 PG 子分支均通过 | 已通过 | 覆盖登录、设置、系统账户、供应商、分组、授权、账号、API Key |
| 回归 | Redis Stream 状态 smoke | `pnpm --filter juhe-ai-backend test:record-maintenance-redis-stream-smoke`；`pnpm --filter juhe-ai-backend test:runtime-log-redis-stream-smoke` | Stream 读状态失败不伪装为空；消息确实入队并 drain | 已通过 | 仅对 Redis NOGROUP / no such key 初始化态做显式 0 pending/lag 处理 |
| 回归 | 审计热搜失败语义 | `pnpm --filter juhe-ai-backend test:audit-log-hot-search` | 热搜正常命中；目录读取失败不返回假空列表 | 已通过 | 目录不存在仍可作为无热搜文件处理 |

## 验收标准

- 前端管理页首屏接口不再因为后台 worker 写入或 stats SQLite 锁等待整体进入秒级。
- 纯读 DB service typed operation 不被写队列串行拖住。
- System API HTTP 纯读不被统一 DB admission 或隐藏副作用写拖住。
- PostgreSQL 高并发模式下，纯读使用连接池并发，不受 SQLite 单写策略限制。
- 后台 worker 和网关非关键 side effects 不抢前台业务读。
- `accounts`、`groups`、`api-keys` 等管理列表保持原字段语义和真实数据返回。
- 关键回归和 `pnpm typecheck` 通过。

## 进度记录

- `2026-07-04`：确认本地 `AI 账户管理` 秒级加载根因来自账户列表同步统计装饰等待 SQLite stats 写锁，轻量接口被 DB service 事件循环同步等待连带拖慢。
- `2026-07-04`：完成先行止血：账户列表 stats 装饰短 busy timeout；后台 worker DB service typed operation 显式 low priority；相关回归和 typecheck 已通过。
- `2026-07-04`：用户明确要求：读不应排队，不应被后台写堵；前端接口契约不能通过返回空数据破坏。计划据此调整为读写隔离方案。
- `2026-07-04`：并行审计补充结论：System API HTTP 路由不走 IPC 队列但仍会被统一 admission、同步 SQLite busy wait、session touch / last_used 等隐藏写拖慢；SQLite 纯读需要只读连接或 read worker pool，而不是只绕过队列。
- `2026-07-04`：完成本轮代码落地：DB service operation 读写分类与调度骨架、System API route access mode、管理页首屏 GET 纯读 admission 绕过、网关 / 后台写副作用低优先级、管理列表假空兜底移除。
- `2026-07-04`：修复当前工作树中阻塞 typecheck 的 Gemini stream inspector 类型缺口和 stream read-plan regression 参数缺口；该修复不改变本计划的读写隔离边界。
- `2026-07-04`：新增 DB service 读写调度模拟回归，并把入队决策抽成可测试 helper；复查修正旧静态断言，使其跟随 access mode 派生策略。
- `2026-07-04`：落地 SQLite 账号列表 read worker pool：DB service 进程下 `listAccountsPageAsync` 投递 query-only 子进程，read worker 使用只读 SQLite handle，返回真实账号统计并通过回归确认不会触发过期账号隐藏写。
- `2026-07-04`：调整账号读路径边界：`listAccounts`、`listAccountsPage`、`findAccountSummary`、`listAccountOptions` 不再调用 `disableExpiredAccounts`；过期停用由后台健康检查 / 冷却重试 / 维护 sweep 承担。
- `2026-07-04`：扩展 SQLite read worker 覆盖范围：`groups`、`api-keys`、`route-strategies`、`proxies`、`system-accounts`、`providers`、`settings`、`runtime-logs` 主读路径进入 query-only 子进程；`api-keys` / `route-strategies` 默认数据创建副作用从 read worker 路径剥离。
- `2026-07-04`：修复 API Key 列表 DTO 契约，列表仍不返回完整密钥，但稳定保留 `key: ""`，避免前端字段消失。
- `2026-07-04`：真实 SQLite worker 压测通过：15 秒请求窗口、并发 12，`total=1453`、`success=1453`、`failed=0`、`p95=218.333ms`、`SQLite readErrors=0`、`lockSignals=0`。
- `2026-07-04`：真实 SQLite System API performance smoke 通过；同步修正 smoke 中已过期的分组创建 payload，去掉当前 strict schema 不接受的 `providerProtocolProfileId`。
- `2026-07-04`：收口 SQLite query-only read worker 覆盖：DB service 公开全局设置、供应商模型目录、OpenAI-compatible files / vector stores list / get / search / chunks 纯读进入 read worker；明确 runtime / cache / quota / health / cooldown / account-test 运行态读取不属于 query-only DB 读通道。
- `2026-07-04`：远程测试 PostgreSQL / Redis 已重建：删除项目 5 个 schema，flush 测试 Redis DB，重新初始化当前 schema 和默认种子；`postgres:init-schema` 输出 `schemas=5`、`schemaStatements=593`、`seedStatements=147`，`test:postgres-schema-sql` 和 `test:postgres-seed-defaults` 通过。
- `2026-07-04`：补齐真实 PG 深测中暴露的当前契约漂移：系统团队新增 `idx_system_teams_name_c_lookup` C collation 前缀索引；API Key / 系统团队列表 smoke 对齐当前 `name COLLATE "C"` 查询；账号导入和单分组绑定 smoke 补当前必需的 `providerProtocolProfileId`；表监控 smoke 对齐 5 个 PG 逻辑 schema；记录清理 smoke 自包含准备并恢复安全游标。
- `2026-07-04`：修复不应伪装为空的测试和读取路径：审计热搜目录读取失败返回 unavailable，不再作为空结果；record-maintenance / runtime-log Redis Stream smoke 不再吞 Redis / PG 读取错误，并断言测试消息确实进入 Stream。
- `2026-07-04`：完整远程 PG smoke 矩阵复跑通过：从 `test:auth-profile-postgres-smoke` 到 `test:announcement-postgres-smoke` 共 33 个脚本全部 PASS；远程 System API performance smoke 通过。

## 验证记录

- `pnpm --filter juhe-ai-backend test:db-service-operation-access-mode`：通过。
- `pnpm --filter juhe-ai-backend test:db-service-read-write-dispatch-simulation`：通过。
- `pnpm --filter juhe-ai-backend test:system-api-db-access`：通过。
- `pnpm --filter juhe-ai-backend test:gateway-background-side-effect-priority`：通过。
- `pnpm --filter juhe-ai-backend test:management-list-contract`：通过。
- `pnpm --filter juhe-ai-backend test:account-list-stats-lock`：通过。
- `pnpm --filter juhe-ai-backend test:sqlite-read-worker`：通过。
- `pnpm --filter juhe-ai-backend test:account-list-query-guard`：通过。
- `pnpm --filter juhe-ai-backend test:api-key-list-query-guard`：通过。
- `pnpm --filter juhe-ai-backend test:route-strategy-list-query-guard`：通过。
- `pnpm --filter juhe-ai-backend test:group-options-lightweight`：通过。
- `pnpm --filter juhe-ai-backend test:runtime-log-search-guard`：通过。
- `pnpm --filter juhe-ai-backend test:runtime-log-keyword-only-sql`：通过。
- `pnpm --filter juhe-ai-backend test:runtime-log-grep-path`：通过。
- `pnpm --filter juhe-ai-backend test:sqlite-writer-boundary`：通过。
- `pnpm --filter juhe-ai-backend test:db-service-request-priority`：通过。
- `pnpm --filter juhe-ai-backend test:background-db-service-write-boundary`：通过。
- `pnpm --filter juhe-ai-backend test:storage-adapter-boundary`：通过。
- `pnpm --filter juhe-ai-backend test:db-service-system-api-boundary`：通过。
- `pnpm --filter juhe-ai-backend test:db-service-system-api-http`：通过。
- `pnpm --filter juhe-ai-backend test:performance-system-api-smoke`：通过。
- 远程 PostgreSQL / Redis：`postgres:init-schema`：通过，结果 `schemas=5`、`schemaStatements=593`、`seedStatements=147`。
- 远程 PostgreSQL / Redis：`pnpm --filter juhe-ai-backend test:postgres-schema-sql`：通过。
- 远程 PostgreSQL / Redis：`pnpm --filter juhe-ai-backend test:postgres-seed-defaults`：通过。
- 远程 PostgreSQL / Redis：完整 `test:*postgres-smoke` 矩阵 33 个脚本：通过。
- 远程 PostgreSQL / Redis：`pnpm --filter juhe-ai-backend test:record-maintenance-redis-stream-smoke`：通过。
- 远程 PostgreSQL / Redis：`pnpm --filter juhe-ai-backend test:runtime-log-redis-stream-smoke`：通过。
- 远程 PostgreSQL / Redis：`pnpm --filter juhe-ai-backend test:api-key-management-driver`：通过。
- 远程 PostgreSQL / Redis：`pnpm --filter juhe-ai-backend test:performance-system-api-smoke`：通过。
- `pnpm --filter juhe-ai-backend test:audit-log-hot-search`：通过。
- `JUHE_AI_WORKER_LOAD_DURATION_SECONDS=15; JUHE_AI_WORKER_LOAD_CONCURRENCY=12; pnpm --filter juhe-ai-backend test:worker-real-load`：通过。
- `pnpm --filter juhe-ai-backend test:db-service-runtime-metrics`：通过。
- `pnpm --filter juhe-ai-backend exec tsx src/scripts/regression/stream-max-lifetime-read-plan-regression.ts`：通过。
- `pnpm --filter juhe-ai-backend typecheck`：通过。
- `pnpm typecheck`：通过。

## 风险与注意事项

- 部分 GET / 读函数可能隐含写入、缓存刷新或默认数据创建，分类时必须标为 `ambiguous` 或拆出副作用。
- SQLite 纯读即使不进写队列，也可能碰到文件锁；处理方式必须是不长时间同步卡住事件循环，而不是返回假数据。
- PostgreSQL 并发不是无限并发；连接池、行锁和慢查询仍需监控。
- 前端 fanout 可以后续优化体验，但不能作为替代后端读写隔离的主方案。
- 路由和 operation 分类必须保守，发现隐藏写先标 `readWithSideEffect` / `ambiguous`，再拆副作用；不能把会写状态的请求放入纯读通道。

## 完成总结

- DB service typed operation 已有显式读写分类，SQLite 只对写入和维护任务排队，纯读不进入写队列；PostgreSQL 不套 SQLite 全局队列。
- SQLite standalone 模式下，管理端主读、运行日志索引读、公开设置、模型目录和 OpenAI-compatible files / vector stores 安全纯读已进入 query-only read worker 子进程；读失败返回真实错误，不返回空列表、空统计或缺字段伪成功。
- 前端管理列表 DTO 契约保持稳定，API Key 列表不返回完整密钥字段，账号 / 分组 / API Key / 策略 / 代理 / 系统账户 / 供应商 / 设置 / 运行日志 / OpenAI-compatible 读路径都用回归验证真实数据返回。
- runtime / cache / quota / health / cooldown / account-test、Codex context writer pool 等 owner 明确的运行态路径未混入 read worker，避免破坏缓存失效、运行态一致性或任务状态语义。
- 必要验证已通过：后端 typecheck、全仓 typecheck、SQLite read worker、DB service 读写分类、读写调度模拟、System API 读写元数据、管理列表契约、SQLite worker real load、System API performance smoke、远程 PG/Redis schema 重建和 33 个 PG smoke 矩阵。
