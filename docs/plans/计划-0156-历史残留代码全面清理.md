# 计划-0156-历史残留代码全面清理

## 状态

- 状态：实施中
- 创建日期：2026-07-22
- 基线：`origin/master@438fec969`
- 分支：`codex/dead-code-cleanup-20260722`
- 工作空间：`F:\sub2api-lite\.worktrees\dead-code-cleanup-20260722`
- 设计：`docs/superpowers/specs/2026-07-22-历史残留代码全面清理设计.md`
- 实施计划：`docs/superpowers/plans/2026-07-22-历史残留代码全面清理实施计划.md`
- 重构记录：完成时写入 `docs/refactors/重构-0004-历史残留代码全面清理.md`

## 目标

删除当前没有真实生产、命令、动态、迁移、维护或负向契约消费者的前端、Node、Go、测试脚本、配置、生成物和依赖；建立可重复的死代码门禁；全量验证后合并到本地 master 并推送远程，但不执行上线。

## 已完成准备

- [x] 从 `origin/master` 创建独立 worktree 和 `codex/dead-code-cleanup-20260722` 分支。
- [x] 完成前端、Node、Go、测试脚本、构建依赖、文档历史六域并行审计。
- [x] 基线通过 `pnpm typecheck`、`pnpm build`、`go test ./...`。
- [x] 设计文档已批准并提交：`4052643d0`。
- [x] 落地详细实施计划。

## 任务清单

- [x] PLAN-0156-T01：同步最新 master，重跑扫描并冻结删除候选。
- [x] PLAN-0156-T02：建立 TS 未使用、动态入口、Go cmd 闭包和脚本入口门禁。
- [x] PLAN-0156-T03：删除前端孤立文件、旧账户 bind-group 事件链和测试替身。
- [x] PLAN-0156-T04：收敛前端未使用声明，修正 Vite TS 配置并删除生成物。
- [ ] PLAN-0156-T05：删除 Node 高置信孤立文件和无消费者私有声明。
- [ ] PLAN-0156-T06：验证账户清理、Chat 初始化等疑似漏接线代码，修复缺陷或删除脚手架。
- [ ] PLAN-0156-T07：删除 Go 9 个未注册业务包及专用 port/store/test。
- [ ] PLAN-0156-T08：删除 Go 可达包中只被测试调用且已有正式替代的旧 API。
- [ ] PLAN-0156-T09：删除孤儿脚本，接入或合并仍有价值的漏挂回归。
- [ ] PLAN-0156-T10：清理真正无引用依赖，更新事实文档和重构记录。
- [ ] PLAN-0156-T11：同步 master，执行全量验证、浏览器 smoke 和两级独立复查。
- [ ] PLAN-0156-T12：在不覆盖用户未提交修改的前提下合并本地 master、复验并推送远程。
- [ ] PLAN-0156-T13：保持未上线状态，等待用户明确通知。

## 删除证据记录格式

每个删除簇必须在本文件或重构记录中留下：

1. 普通引用与字符串路径扫描结果。
2. Worker/fork/spawn/动态 import/registry/队列/环境开关检查结果。
3. package script、Go cmd、maintenance、smoke、migration 和发布入口检查结果。
4. 当前事实文档与 Git 历史判断。
5. 数据 owner 与生产 owner 判断。
6. 删除前失败门禁、删除后定向测试和提交 hash。
7. 若保留候选，写明具体真实消费者或风险，不使用“以后可能用”作为理由。

## 必须保留的当前边界

- Node 当前生产 owner，不能仅因 Go 有同名代码而删除。
- Node `worker.ts`、`db-service.ts`、`temporary-maintenance-worker.ts` 及审计/SQLite/usage/Codex/json/model-check 动态 worker。
- Go 已注册 cmd、正式 opt-in 路由、maintenance smoke、migration、sqlc、testkit 和 bridge。
- 当前仍保护已删除功能不复活的负向契约测试；如需变更，先迁移守卫。
- 原始 master 工作区的用户未提交文档改动。

## 同步策略

- 候选冻结前同步一次。
- 每个主要删除批次开始前检查 `origin/master` 差异。
- master 新增约 20 个提交或开发跨过一个工作日时立即同步。
- 最终验证前再次同步。
- 每次同步后重跑受影响的入口扫描、typecheck 和定向测试。

## 测试矩阵

- TypeScript：`pnpm typecheck`、严格未使用门禁、相关 regression。
- Frontend：Vite build、关键页面浏览器 smoke、console error 检查。
- Node：动态入口门禁、变更域 regression、release source preflight、owner manifest。
- Go：`go test ./...`、`go test -race ./...`、`go vet ./...`、`go mod tidy -diff`、cmd 闭包差集。
- PG/Redis：仅在触及真实边界时使用唯一 namespace/schema，清理本任务数据，秘密不回显。
- 真实模型：仅在触及真实协议或模型行为时执行；否则记录无需执行。

## 验收标准

- 所有批准删除项完成，所有保留项有实证消费者。
- 死代码门禁通过，不依赖简单 import 次数误判动态入口。
- 全量 typecheck/build/Node regression/Go test-race-vet/tidy/发布门禁通过。
- 文档、计划、重构记录与代码事实一致。
- 两级独立复查无未解决高风险问题。
- feature 分支合并到本地 master 并推送远程 master。
- 未部署、未上线，等待用户通知。

## 进度与证据

- 2026-07-22：设计批准；独立 worktree 基线测试通过；六域审计完成。
- 2026-07-22：详细实施计划落地，准备创建持续目标并开始 Task 1。
- 2026-07-22：候选冻结前执行 `git fetch origin master`；`HEAD...origin/master` 为 `1 0`，远程 `master` 仍为 `438fec96984080277bf05c1378390abf8e978f23`，当前分支只领先已批准设计提交，无需额外合并。
- 2026-07-22：前端严格未使用门禁预演 `pnpm --filter juhe-ai-frontend exec vue-tsc --noEmit --noUnusedLocals --noUnusedParameters` 以退出码 1 结束，共 35 条诊断；主要集中在 `src/views/accounts/accountFormatters.ts`（6）、`src/views/audit-logs/AuditLogsView.vue`（3）、`src/views/usage-stats/UsageStatsView.vue`（3），以及 `src/api/contracts.ts`、`src/views/accounts/accountErrorPolicyPayload.ts`、`src/views/accounts/AccountsView.vue`、`src/views/model-checks/ModelChecksView.vue`（各 2）。这是严格门禁收敛前的预演结果，不是现有基线测试失败。
- 2026-07-22：Node 严格未使用门禁预演 `pnpm --filter juhe-ai-backend exec tsc -p tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters` 以退出码 1 结束，共 229 条诊断；主要集中在 `src/storage/repositories.ts`（54）、`src/scripts/regression/gemini-gateway-mock-ai-regression.ts`（14）、`src/modules/db-service/db-service-handlers.ts`（11）、`src/modules/providers/drivers/_shared/openai-anthropic-bridge.ts`（9）、`src/storage/account-summary.repository.ts` 与 `src/storage/usage-records.repository.ts`（各 6）。这是严格门禁收敛前的预演结果，不是现有基线测试失败。
- 2026-07-22：Go 候选冻结扫描中，`go list -f '{{.ImportPath}}' ./...` 得到 93 个项目包，`go list -deps -f '{{.ImportPath}}' ./cmd/...` 的正式命令闭包包含 83 个项目包；差集为 10 个，其中 9 个未注册业务包为 `internal/jobs/accountbalanceautodetect`、`internal/jobs/accountbalancerefresh`、`internal/jobs/accountbalancesnapshotcleanup`、`internal/jobs/accounthealthcheck`、`internal/jobs/cooldownaccountretest`、`internal/modules/accountbalanceautodetect`、`internal/modules/accountbalancesnapshotcleanup`、`internal/modules/accounthealthcheck`、`internal/modules/cooldownaccountretest`，另一个是明确保留的测试资产 `db/migrationtests`。
- 2026-07-22：高风险保留项再次冻结：Node `worker.ts`、`db-service.ts`、`temporary-maintenance-worker.ts` 及审计、SQLite、usage、Codex、JSON、model-check 动态 worker；Go 已注册 cmd、正式 opt-in 路由、migration、sqlc、maintenance smoke、testkit 与 bridge；仍用于阻止已删除功能复活的负向契约测试。后续不得仅凭普通 import 次数或与 Go 同名而删除这些资产。
- 2026-07-22：完成 PLAN-0156-T02 门禁基础设施。新增前后端 `tsconfig.unused.json` 与 `typecheck:unused`，实际严格检查分别以退出码 2 产生 35 条前端、229 条 Node 诊断；新增动态入口回归后，临时注入不存在入口时按预期 RED 并精确列出该入口，移除临时项后 9 个真实动态入口 GREEN；新增 Go cmd 闭包测试，默认 `-count=1` 模式精确接受 9 个已知未注册业务包、`db/migrationtests` 与门禁自身并 GREEN，`-args -deadcode-strict` 模式按预期 RED 并逐项列出 9 个业务包。`pnpm --filter juhe-ai-backend typecheck`、`go -C backend-go test -count=1 ./...` 和 `git diff --check` 保持 GREEN；严格门禁的 35 / 229 / 9 是后续清理任务的验收目标，不是本次提交质量失败。
- 2026-07-22：完成 PLAN-0156-T03。按 TDD 先收紧页面缓存、账户编辑保存、网关 formatter、Chat 移动交互、Chat composer 和用量统计回归，分别因旧文件或旧 `bind-group` UI 透传仍存在而 RED；随后删除 `frontend/src/composables/usePageDataCache.ts`、`frontend/src/views/accounts/AccountBindGroupModal.vue`、`frontend/src/views/accounts/useAccountBindGroup.ts`、`frontend/src/views/api-keys/useApiKeyGroupOptions.ts`、`frontend/src/views/chat/chatConversationDrawer.ts`、`frontend/src/views/chat/composer/chatImagePreparationState.ts`、`frontend/src/views/usage-stats/UsageStatCell.vue`，并移除 `accountRowActions.ts`、`AccountRowActions.vue`、`AccountTableCell.vue`、`AccountMobileCard.vue`、`AccountList.vue` 的旧账户 bind-group UI event chain 以及 `accountDerivedState.ts` 的 `bindGroupOptionsForAccount` / `bindGroupTip`。保留 `useAccountEditSaveFlow.ts` 的 `api.accounts.bindGroup` / `api.myAccounts.bindGroup` 正式编辑调用、账户 `groupName` 显示链、`defaultGroupForProvider`、`ChatImagePreparationQueue` 与 AIComposer 当前队列接线、API Key `routeStrategyId` owner；账户渐进加载回归的供应商测试桩同步到真实 `providers.definitions` owner。上述专项回归、前端 `typecheck`、后端 `test:dead-code-entrypoints` 均 GREEN；前端严格 `typecheck:unused` 仍为 Task 4 的 35 条诊断，本任务候选相关诊断为 0。
- 2026-07-22：合并 `origin/master@0bcea242d` 后完成 PLAN-0156-T04。按 TDD 先记录 `pnpm --filter juhe-ai-frontend typecheck:unused` 精确 RED 35，再删除无真实消费者的前端 import、type、local helper、wrapper 和未读解构，并清理由删除链暴露的二级死依赖，最终 strict 35→0；未使用 `_` / `void` 假消费，也未新增业务接线。收紧 `test:frontend-build-info` 后因旧 `dev` 命令未显式指定 TypeScript 配置而 RED，随后把 `dev`、`build`、`typecheck` 收敛到 `vite.config.ts` / `tsconfig.node.json` 真实 owner，移除 app project references、Node composite，启用双侧 `noEmit`，并删除 tracked `vite.config.js` / `vite.config.d.ts`；构建后回归再次 GREEN，仓库扫描确认生成物未重生，`dist/build-info.json.buildId` 等于当前 HEAD。前端 strict、普通 typecheck、lint、build、Build ID 回归、账户 formatter、审计 payload、授权、模型检测 / 供应商模型、使用记录、用量统计定向回归及后端动态入口门禁均 GREEN。`test:audit-log-detail-layout` 与 `test:log-progressive-loading` 仍因 `0bcea242d` 已存在的过期全文件源码形状断言失败，相关失败文件不在本任务 diff 中，本任务不扩范围修复。
- 2026-07-22：完成 PLAN-0156-T05 的高置信子批。按 TDD 先把动态入口、存储边界、后台指标、进程 fatal、Codex 客户端策略、usage pricing 和 provider repository 回归收紧到当前正式 owner，分别因 `web-search-executor.ts`、`process-fatal.ts`、`mock-background-runtime.ts`、`codex-switch-probe.ts` 仍存在而 RED；随后删除这四个孤立文件，并删除 `db-service.ts`、账户测试、后台 IPC、record maintenance、client IP policy、proxy health、account options / summary / usage repository 中经全仓引用和动态入口审计确认无消费者的私有函数与二级死链。Node 严格未使用诊断从 229 降到 207，本子批候选相关诊断归零；权限上下文参数、Express arity、网关响应、usage writer 等不确定边界保留到后续判定。动态入口、Codex diagnostic、usage pricing、provider repository、storage adapter、background metrics、process fatal、proxy health、普通 typecheck 和 build 均 GREEN。`account-diagnostic-mock-ai` 暴露父进程运行时改 secret 与 SQLite read worker 启动 secret 不一致的基线初始化缺陷，回归改为 env-first + deferred import 后连续两次 GREEN，`test:sqlite-read-worker` 同时 GREEN；未修改生产加密实现。`test:usage-record-writer-pool` 在本分支和独立 `2751d238c` baseline worktree 均稳定失败，证明当前 master 的 shutdown drain 未等待账户副作用完成，本子批未触及该写入链，后续按独立问题修复，不能删除断言掩盖。
