# PLAN-20260727T153818508Z Anthropic 官方 OAuth 接入复用实现

## 基本信息

- 计划 ID：PLAN-20260727T153818508Z
- 创建时间戳（UTC）：20260727T153818508Z
- 状态：进行中
- 创建时间：2026-07-27
- 更新时间：2026-07-27
- 需求来源：用户对话
- 执行者：AI
- 关联模块：前端、后端、网关、账户、文档、回归

## 需求目标

- 在当前 `F:\sub2api-lite` 工作区内完成 Anthropic 官方 OAuth 接入。
- 优先直接复用现有 OpenAI OAuth 账户接入骨架与开源项目里已验证的 Anthropic OAuth 换码思路，避免重新发明流程。
- 保持 Anthropic API Key、Anthropic 直贴 Access Token、OpenAI OAuth、Gemini OAuth 既有能力不回归。

## 范围边界

### 本次包含

- 新增 Anthropic OAuth 授权链接生成与 code 换 token 服务。
- 新增 Anthropic OAuth 创建账户接口与前端账户表单交互。
- 让 Anthropic OAuth 账户支持三种创建方式：站内 OAuth 换码、Refresh Token 创建、直接录入 Access Token。
- 同步更新导入协议、长期设计文档和任务清单。
- 执行类型检查与最小专项回归。

### 本次不包含

- 不原样复制 `sub2api` / `new-api` / `cap` 全部代码与全部供应商逻辑。
- 不接入 Claude 订阅代理、cookie/session、TLS 指纹、浏览器劫持或 sidecar 依赖。
- 不改变当前 Anthropic 网关主转发方式；上游运行时仍使用 `Authorization: Bearer <access_token>`。

## 方案概述

- 后端新增独立 `anthropic-oauth` 模块，结构参考现有 `openai-oauth`，但只保留 Anthropic 需要的最小闭环：`auth-url`、`create-from-code`、`create-from-refresh-token`、`refresh-token` 与重新授权接口。
- 前端把 OAuth 表单抽成供应商可配置模式：OpenAI 继续支持手动回调 URL 与 Refresh Token；Anthropic 新增手动回调 URL、Refresh Token 与直接 Access Token 三种模式。
- 账户保存链路继续统一走 `accounts.type = 'oauth'`，具体校验按供应商协议档案决定。

## 任务清单

- [x] 盘点现有 OpenAI OAuth、Anthropic OAuth 和前端账户表单差异。
- [x] 创建本计划文档。
- [x] 新增 Anthropic OAuth 后端服务与路由。
- [x] 新增前端 Anthropic OAuth 多模式表单与保存逻辑。
- [x] 更新导入协议与长期设计文档。
- [ ] 执行类型检查与专项回归。
- [ ] 完成一次代码复查并记录结果。

## 验收标准

- [ ] Anthropic OAuth 可生成授权链接。
- [ ] Anthropic OAuth 可通过回调 URL 创建账户。
- [ ] Anthropic OAuth 可通过 Refresh Token 创建或重新授权账户。
- [ ] Anthropic OAuth 仍可通过直接录入 Access Token 创建账户。
- [ ] Anthropic OAuth 创建后的账户走现有 Anthropic Bearer 转发链路。
- [ ] OpenAI OAuth、Gemini OAuth、Anthropic API Key 不回归。
- [ ] 长期文档、导入示例和验证记录已同步。

## 风险与注意事项

- 当前工作树存在大量用户既有修改，改动必须限制在 OAuth 相关文件与必要文档内。
- 参考开源实现时只复用可证明必要的流程与字段，不把无关运行时、代理、风控绕行逻辑带入当前项目。
- 若 Anthropic 官方 token 响应和参考项目假设不一致，以 Anthropic 当前真实接口响应为准。
