# PLAN-0161 任务清单

## 文档与基线

- [x] 从最新 `master` 创建独立 worktree 和功能分支。
- [x] 落地 Responses 防火墙设计文档。
- [x] 落地阶段计划文档。
- [x] 更新目录索引和架构关联。
- [ ] 在每次远程 `master` 变化后先同步功能分支。

## P0 源头与历史

- [x] 建立版本化 Responses item contract registry。
- [x] 按目标类型直接生成 `fc_*`、`ctc_*` 等合法 ID。
- [x] 实现历史 sanitizer 的 copy-on-write、重复 ID 和禁止字段边界。
- [x] 覆盖 `store=false`、不可持久化 `rs_*` 和未知 item 的测试。

## P1 响应防火墙

- [x] 实现 JSON/SSE guard 和显式 provenance marker。
- [x] 统一 R0/R2/unknown/late violation 分类。
- [x] 固定 SSE identity 映射、completed 终态和语义提交边界。
- [x] 将 safe repair 写回真实 JSON/SSE 下游。
- [x] 接线 `off/shadow/safe_repair/strict_intercept` 运行模式。
- [x] 增加性能、诊断上限和 dispose 回归。

## P2 账户策略与可观测性

- [x] 增加 Node 账户 safe repair 开关，默认开启。
- [x] 增加 Node 账户 strict intercept 开关，默认关闭。
- [x] 校验 Node API/前端默认值和互斥关系。
- [x] 严格拦截触发切号、异常状态和说明原因。
- [x] 修复/未知在使用记录显示黄色 tag，记录具体原因。

## P3 转换和环境验证

- [ ] 完成 Node OpenAI/Anthropic/Gemini Responses 转换矩阵审计（当前已有 bridge checkpoint 回归，完整矩阵仍待 P3）。
- [ ] 增加 mock gateway 全链路 E2E 和协议矩阵。
- [ ] 使用本机 SQLite 测试库，必要时删除重建当前 schema。
- [ ] 使用本机 PG/Redis 凭据执行边界 smoke，不提交密码。
- [ ] 真实模型测试仅在用户明确允许时读取 `D:\模型密钥.txt`，限制日志和费用。
- [ ] 形成性能与灰度回退报告。

## 发布

- [ ] 功能分支先合并最新 `origin/master` 并解决冲突。
- [ ] 在功能分支跑完整验证。
- [ ] 合并到本地 `master`，保留 master 工作树用户改动。
- [ ] 再次 fetch，若远程变化则先合并并解决冲突。
- [ ] 推送 `master:master` 并验证远程 tip。
