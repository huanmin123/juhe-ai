# PLAN-0092 AI 问答 MVP

## 基本信息

- 编号：PLAN-0092
- 状态：进行中
- 创建时间：2026-07-12
- 更新时间：2026-07-12
- 需求来源：Codex 会话 `019f543d-eece-74f1-b588-304b5cc7db3e` 及当前会话确认
- 执行者：AI / 维护者
- 关联模块：前端 / Node 后端 / 独立聊天存储 / DB service / 网关 / API Key / 后台 worker / 使用记录 / 原始审计 / 文档 / 验证

## 需求目标

- 为登录用户提供使用自己 API Key 的站内 AI 问答工作台，并将它作为平台内置 OpenAI Chat 客户端接入现有网关。
- 模型调用必须经过现有 `/v1/chat/completions` 网关和完整调度链路。
- 服务端管理最近 7 天完整成功轮次，前端以动态虚拟列表展示富文本流式回答。
- 第一版保持纯文本问答，不引入 Agent、MCP、Skill、附件或图片生成运行时。

## 范围边界

### 本次包含

- [ ] 新增用户侧“AI 问答”菜单和 `/my-chat` 页面。
- [ ] 新增会话、消息、上下文、流式发送和 7 天清理能力。
- [ ] 会话固定绑定自己的 API Key，模型允许逐轮切换。
- [ ] 所有模型请求重新进入真实 API Key 网关入口。
- [ ] 支持安全 Markdown、表格、代码、LaTeX、Mermaid 和 HTTPS 图片展示。
- [ ] 支持动态高度虚拟列表、游标分页、停止生成和中文错误状态。
- [ ] 支持 SQLite standalone 与 PostgreSQL performance 同一契约。
- [ ] 补齐 Mockdata、权限、网关、流式、安全、性能和浏览器验证。

### 本次不包含

- 图片上传、视觉问答、图片生成和产物下载。
- MCP、Skill、Function Calling、工具审批和工具执行 worker。
- 文件、知识库、联网搜索、语音和长期记忆。
- 消息编辑、分支、分享、多人会话和管理员读取他人聊天正文。
- Go 后端实现或 Node / Go 双写。

## 关联文档

- 功能设计：`docs/functions/AI问答设计.md`
- 架构总览：`docs/architecture/架构总览.md`
- 接口与权限：`docs/functions/接口契约与权限矩阵.md`
- SQLite 存储：`docs/functions/SQLite存储说明.md`
- PostgreSQL 模式：`docs/functions/PostgreSQL与Redis高性能模式设计.md`
- 安全与日志：`docs/functions/安全与日志策略.md`
- 原始审计：`docs/functions/原始审计日志设计.md`
- 测试说明：`docs/develop/测试与验证说明.md`

## 方案概述

- 主 Node 进程承载 AI 问答 HTTP / SSE 路由；数据库读写通过 DB service typed operations。
- AI 问答从网关视角属于普通客户端，loopback 只是传输实现，不产生内部特权。
- 会话保存固定 `api_key_id`，服务端读取真实 Key 调用本机 `/v1/models` 和 `/v1/chat/completions`。
- 网关继续负责额度、路由策略、分组、账户、代理、切号、计费、使用记录和审计。
- standalone 新增独立 `juhe-ai-chat.sqlite3`；performance 新增独立 `juhe_chat` schema，消息表按 UTC 日分区；上下文只读取最近 7 天完整成功轮次。
- 新增按用户、按 UTC 日增量维护的容量窗口，默认每用户最近 7 天 2 GiB；发送门禁不得扫描消息明细。
- 前端使用 `@tanstack/vue-virtual` 和安全 Markdown 渲染栈，不复制参考项目的重型 Agent 结构。
- 聊天记录保留与原始审计保全保持独立，删除聊天不级联删除审计和使用记录。
- 当前项目不做旧 schema 兼容；上线前按目标业务 schema 单独完成结构同步和数据处理。

## 执行拆解

### 第一阶段：契约与存储

- [ ] 同步接口契约、权限矩阵和存储文档。
- [ ] 新增独立 SQLite / PostgreSQL `juhe_chat` 当前完整 schema、日分区和索引。
- [ ] 新增 AI 问答 repository、mapper 和 DB service typed operations。
- [ ] 实现会话 CRUD、消息事务、幂等和同会话活动轮次门禁。

### 第二阶段：上下文与后台任务

- [ ] 实现 7 天完整轮次查询和模型预算。
- [ ] 实现用户 7 天容量窗口与 2 GiB 发送门禁。
- [ ] 实现按完整轮次游标清理、空会话清理和标题重算。
- [ ] 实现异常 `streaming` 恢复并接入 `ops-worker`。

### 第三阶段：真实网关调用

- [ ] 在主服务 System API 代理前挂载 Chat Router。
- [ ] 实现主进程登录 session typed 校验。
- [ ] 实现 API Key 模型 options 和 loopback gateway client。
- [ ] 实现受签名原始 IP / trace 元数据与 `ai_chat` 使用记录来源。
- [ ] 实现 OpenAI Chat SSE 解析、内部事件、取消和错误映射。

### 第四阶段：前端基本问答

- [ ] 新增菜单、路由、会话栏、Key / 模型选择和输入区。
- [ ] 实现发送、停止、流式纯文本、历史分页和会话删除。
- [ ] 实现加载态、空态、失败态和移动端抽屉。

### 第五阶段：富文本与虚拟列表

- [ ] 引入动态虚拟列表和滚动锚定。
- [ ] 实现 Markdown、代码、表格、KaTeX、Mermaid 和 HTTPS 图片。
- [ ] 实现 DOMPurify、URL policy、Mermaid strict 和异步渲染防覆盖。
- [ ] 完成长消息、5000 条合成消息和移动端布局验证。

### 第六阶段：全链路验收

- [ ] 扩展 Mockdata 和覆盖检查。
- [ ] 完成 SQLite 与 PostgreSQL Mock AI 回归。
- [ ] 验证 API Key、路由、代理、额度、使用记录和审计链路。
- [ ] 完成类型检查、构建、浏览器交互和安全回归。
- [ ] 更新文档、验证记录和发布说明。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 前后端类型检查 | `pnpm typecheck` | TypeScript 类型检查通过 | 未执行 | 待实现后填写 |
| 命令类验证 | 完整构建 | `pnpm build` | 前后端构建通过 | 未执行 | 待实现后填写 |
| 存储 | SQLite / PostgreSQL schema 与 repository | 新增定向回归 | 两种模式契约一致，索引和约束生效 | 未执行 | 待实现 |
| 权限 | 用户会话隔离 | scope 回归与 Mock AI | 用户和管理员个人入口都不能读取他人会话 | 未执行 | 待实现 |
| 主流程 | 新建会话并流式问答 | Mock AI 全链路 | 固定 Key、可切模型、消息终态正确 | 未执行 | 待实现 |
| 网关回归 | 路由、账户、代理、额度与记录 | Mock AI / 真实网关烟测 | 不绕过现有调度和计费 | 未执行 | 待实现 |
| 上下文 | 7 天与完整轮次 | 定向 repository / service 回归 | 失败和取消轮次不进入上下文 | 未执行 | 待实现 |
| 容量 | 用户 7 天日窗口与 2 GiB 门禁 | 定向 repository / service 回归 | 不扫描消息表；超限只阻止发送 | 未执行 | 待实现 |
| 流式 | 跨 chunk、断流、取消、大小上限 | SSE parser 回归 | 无乱码、无误完成、内存有界 | 未执行 | 待实现 |
| 安全 | Markdown XSS、URL、Mermaid SVG | 前端 renderer 回归 | 恶意内容被清洗，失败有源码回退 | 未执行 | 待实现 |
| 性能 | 5000 条消息虚拟化 | 浏览器自动化与 DOM 计数 | DOM 有界，滚动和流式更新稳定 | 未执行 | 待实现 |
| 前端体验 | 桌面 / 移动端与中文文案 | Browser 自动化和截图 | 不重叠、不溢出、文案全中文 | 未执行 | 待实现 |
| 清理 | 过期消息、空会话和异常流 | worker 定向回归 | 游标有界、幂等、标题正确重算 | 未执行 | 待实现 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-07-12 | 第一版只实现 Node | Go 迁移尚未达到可用阶段 | 不等待 Go，不做双写兼容 |
| 2026-07-12 | 会话固定 API Key，模型逐轮可切换 | 保持调度归属稳定，同时满足用户模型选择 | 换 Key 必须新建会话 |
| 2026-07-12 | 第一版只做纯文本问答 | 控制 MVP 复杂度 | MCP、Skill、图片生成进入后续计划 |
| 2026-07-12 | 上下文按完整成功轮次 | 防止失败回答留下孤立用户消息 | failed / canceled 轮次只回看不续传 |
| 2026-07-12 | 不关闭 AI 问答原始审计正文 | 聊天保留期不能暗中改变全局审计政策 | 7 天仅约束聊天表、页面和上下文 |
| 2026-07-12 | AI 问答按平台内置客户端处理 | 聊天与审计是独立模块，模型调用仍是普通网关调用 | 不免鉴权、限流、计费、使用记录或审计 |
| 2026-07-12 | 使用 TanStack Virtual，参考但不复制自研列表 | 降低耦合和维护成本 | 自行补动态高度、流式和 prepend 锚定 |
| 2026-07-12 | 聊天正文从业务库存储拆分 | 7 天内仍可能达到数十 GiB，不能挤压核心业务表 | 独立 SQLite / `juhe_chat` schema；PostgreSQL 消息日分区 |
| 2026-07-12 | 默认每用户 7 天 2 GiB 容量门禁 | 防止异常长输出或滥用耗尽共享磁盘 | 超限只阻止新发送，读取、停止、删除保持可用 |

## 验收标准

- [ ] 用户可以选择自己的 API Key 新建会话，并在 Key 支持范围内逐轮切换模型。
- [ ] 所有模型请求真实经过现有网关，代理、路由、额度、记录和审计保持生效。
- [ ] 最近 7 天完整成功轮次被正确组装，失败、取消和过期内容不进入上下文。
- [ ] 流式发送、停止、断流和重复提交不会产生重复模型调用或卡死会话。
- [ ] Markdown、代码、表格、LaTeX、Mermaid 和 HTTPS 图片安全稳定展示。
- [ ] 大量消息下 DOM、内存和滚动保持有界，桌面与移动端无重叠溢出。
- [ ] SQLite、PostgreSQL、权限、安全、worker 和浏览器验证全部通过或明确记录未覆盖风险。
- [ ] 相关架构、接口、存储、Mockdata、测试和发布文档同步完成。

## 风险与注意事项

- loopback 网关调用必须保留真实 API Key 鉴权，内部签名元数据不能覆盖调用身份。
- 动态虚拟列表最容易在图片 / Mermaid 异步增高和 prepend 历史时跳动，必须有浏览器级回归。
- 流式回答不能逐 token 写库，最终正文与中间 buffer 都必须有字节上限。
- AI 问答新增独立聊天 schema / SQLite 文件；上线只创建或同步聊天存储，不重建无关业务、数据集、统计、上下文数据库或 Redis 数据。
- 删除聊天不会删除原始审计；若未来改变该产品承诺，需要单独立项。
- 发布异常时先关闭 AI 问答菜单和路由入口，保持现有公开网关继续服务；回滚代码不能回滚或删除已有 API Key、使用记录和审计事实。

## 验证记录

- 类型检查：未执行，当前仅完成设计文档。
- 构建：未执行，当前未修改业务代码。
- 单项验证：未执行。
- 手动验证：未执行。
- 未验证项：全部实现与发布验证等待后续开发阶段。

## 完成总结

- 完成时间：待实现后填写。
- 实际完成内容：当前只完成设计和实施计划。
- 主要改动位置：`docs/functions/AI问答设计.md`、`docs/plans/计划-0092-AI问答MVP.md`。
- 验证结果：文档结构与仓库规则检查待本轮完成后记录；业务验证尚未开始。
- 后续建议：用户确认本文后进入实现，不在实现中扩大到工具或多模态范围。
