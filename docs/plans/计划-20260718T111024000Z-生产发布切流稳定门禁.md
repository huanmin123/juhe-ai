# PLAN-20260718T111024000Z 生产发布切流稳定门禁

## 基本信息

- 计划 ID：PLAN-20260718T111024000Z
- 状态：已完成
- 创建时间：2026-07-18
- 更新时间：2026-07-18
- 需求来源：生产上线期间 `/responses` 出现 Nginx 502 和本地 DB service 503
- 执行者：AI
- 关联模块：部署、Nginx、launchd、DB service、运行日志

## 需求目标

- Nginx 只切换到已经连续稳定、完整可承流的目标服务。
- 主服务切回后保留 temporary 至少 60 秒，失败自动恢复 temporary。
- 没有与当前 release/PID 绑定的稳定标记时，清理脚本拒绝删除临时兜底。
- Redis 日志链路故障时，未捕获异常仍同步写入 launchd stderr。

## 事故证据与边界

- 2026-07-18 18:06:48 至 18:06:57，主 HTTP 服务已经接收网关请求，但 DB service 尚未记录 `db_service_started`，产生“本地数据库服务暂时不可用”503。
- 主 PID 65241 于 18:06:33 以 `exit(1)` 退出；PID 67585 于 18:07:10 再次 `exit(1)`，Nginx 已指向 `3001/3002` 且 temporary 已清理，因此产生 502。
- 当前日志只能确认进程退出，无法确认具体未捕获异常；Redis runtime log enqueue 当时持续 timeout/disconnected，故本次不伪造异常根因，只补可靠 stderr 诊断。
- 本次不引入长期双活、数据回灌或常驻自动流量控制器。

## 执行拆解

- [x] 汇总 Nginx、launchd、应用和 DB service 时间线
- [x] 新增切回前固定 PID、完整拓扑和双 health 稳定门禁
- [x] 把切回后 60 秒观察放进 Nginx 原子 rollback trap
- [x] 新增生产稳定 marker 和 completed 清理门禁
- [x] 新增未捕获异常同步 stderr 脱敏诊断
- [x] 更新私有部署方案、README 和上线计划模板
- [x] 完成脚本语法、回归、类型检查和 Mac 端只读验证

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 回归 | 致命诊断脱敏与大小边界 | `pnpm --filter juhe-ai-backend test:process-fatal-diagnostic` | 单行 JSON、完整载荷有界、所有字段不含 secret，undefined/Symbol 不抛错 | 已通过 | 2026-07-18 通过 |
| 静态门禁 | 发布流程契约 | Mac `/bin/bash ./test-deployment-schema-gates.sh` | 新增与原有门禁全部匹配 | 已通过 | 输出 `DEPLOYMENT_SCHEMA_GATES_OK` |
| 回归 | Nginx 原子回滚 | Mac `/bin/bash ./test-switch-nginx-rollback.sh` | reload 失败恢复旧 upstream | 已通过 | 输出 `NGINX_SWITCH_ROLLBACK_TEST_OK` |
| 回归 | 无稳定标记拒绝清理 | Mac `/bin/bash ./test-cleanup-production-stable-marker.sh` | 退出非零且临时 sentinel 保留 | 已通过 | 输出 `CLEANUP_PRODUCTION_STABLE_MARKER_TEST_OK` |
| 语法 | 三份变更生产脚本 | Mac `/bin/bash -n` | 全部通过 | 已通过 | pre-switch、switch、cleanup 均通过 |
| 类型/构建 | 后端 TypeScript | `pnpm --filter juhe-ai-backend typecheck`、`build` | 通过 | 已通过 | 两项退出码均为 0 |
| 生产只读 | launchd PID、3001/3002、Nginx active | SSH 只读检查 | 当前服务稳定，不重启、不切流 | 已通过 | PID 67705、runs=3、active=main、双 health=ok |

## 验收标准

- [x] 主服务未完成切回前 60 秒稳定观察时，切换脚本拒绝 main。
- [x] 切回后 60 秒内任一主 PID/拓扑/health 失败进入原子 rollback trap 恢复 temporary。
- [x] completed 清理没有有效稳定标记时拒绝运行。
- [x] 权威上线文档与模板不再出现“先清理兜底、后稳定观察”的顺序。
- [x] 必要自动化和 Mac 端脚本语法验证完成。

## 验证记录

- 后端：fatal diagnostic 回归、logger 原文回归、typecheck、build 均通过。
- 部署：Mac 原生 Bash syntax、schema gates、Nginx rollback、missing-marker cleanup 均通过。
- 生产：三份脚本以 `.next -> bash -n -> chmod 700 -> mv` 原子同步；同步过程未执行脚本、未 reload Nginx、未重启 launchd。
- 生产稳定采样：13 个样本、实际 81 秒，PID 始终为 67705，1 server + 1 DB service + 3 worker、3001/3002、3099 与公网 health 全部通过；launchd `runs=3` 未增加。
- 远端最终脚本 SHA256：switch `ef8a5cc253e7ab58d2eb455a43768c4b4909969538f8814cc895c8aecb362f9e`，cleanup `2549460a6be496f67c30cf9bd6e3b304928daa959b58ce5f114606b2488a6e55`，pre-switch `b72ab205c069342b6e47fcb0621f550c3b82c623b3e4b326d7ca1e37b5d4451c`。
- 既有环境限制：Windows 轻量 Bash 无法完整模拟 macOS BSD `stat` 与 Bash 数组；权威结果采用目标 Mac 原生 `/bin/bash`。

## 完成总结

- 完成时间：2026-07-18
- 发布流程已从“切主后先清理兜底、再观察”改为“切前固定拓扑观察、切后保留 temporary 观察并自动回退、成功 marker 后清理、最后残留终验”。
- 本次 502/503 已定位为发布窗口内主应用/DB service 尚未完整稳定以及随后主进程连续退出，非账户探针导致。
- 发布门禁脚本已经无重启同步到 Mac，立即约束下一次上线。
- fatal stderr 应用代码已完成回归、类型检查和构建；为避免仅为诊断能力再次触发生产切流，将随下一次正式应用 release 生效。

## 风险与注意事项

- 当前 fatal 具体异常尚未被历史日志保存；stderr 兜底需要随下一次应用 release 生效。
- 新脚本同步到 Mac 只影响后续发布，不应重启当前服务或修改当前 Nginx upstream。
- 切回前和切回后各 60 秒会延长临时双 worker 窗口，但这是避免客户端 502/503 的必要保护时间。
