# PLAN-0161-20260723T055453949Z 发布模型快照完整退场

## 基本信息

- 计划 ID：PLAN-0161-20260723T055453949Z
- 序号：0161
- 状态：已完成
- 创建时间：2026-07-23
- 关联模块：AI 问答 / 模型目录 / Node / SQLite / PostgreSQL / 前后端契约
- 关联问题：BUG-0122

## 目标

- 完整删除已无消费者的最终发布模型快照与 dirty rebuild 基础设施。
- 验证 AI 问答前端调用均有真实后端实现，避免保留前端空壳。

## 执行清单

- [x] 穷举生产、脚本、测试和文档引用。
- [x] 删除 Node 快照生产、预热、reconcile、内部接口与 DB service operation。
- [x] 明确 Go runtime 不在本次关注范围，PostgreSQL 派生表暂留供其使用。
- [x] 清理 SQLite 当前 schema；Node 不再访问 PostgreSQL 快照表。
- [x] 更新聊天 HTTP golden、退场契约和权威文档。
- [x] 完成类型检查、构建、Node 回归、migration catalog 和复查。
- [x] 同步远程、提交并推送。

## 验证记录

- `pnpm typecheck`、`pnpm build` 通过。
- AI Chat HTTP golden 通过：21 条路由、19 个 DTO；前端 Chat 调用均命中后端契约。
- Chat route、模型懒加载和发布快照退场契约回归通过。
- `go test ./db/migrationtests` 通过，确认 schema 版本保持 74。

## 完成总结

- Node 发布快照生产、读取、预热、reconcile、内部 endpoint、DB service 空壳和维护脚本已删除。
- SQLite 启动幂等删除废弃表；PostgreSQL 表为 Go runtime 保留，Node 已完全脱离。
- Go runtime 按用户要求不纳入本次改造。
