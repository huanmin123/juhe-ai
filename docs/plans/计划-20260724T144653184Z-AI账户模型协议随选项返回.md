# PLAN-20260724T144653184Z AI 账户模型协议随选项返回

> 后续纠正：供应商模型目录 options 携带模型能力的契约仍有效；本计划把账户人工测试批量 `test-options` 扩成能力全集的部分已被 [PLAN-20260727T183737351Z](计划-20260727T183737351Z-Node管理接口按需读写重构.md) 废止。当前测试弹窗打开零 options 请求，模型下拉只读取 `{ id, name }`，请求形态下拉只读取当前模型 capabilities。本文件其余账户测试内容仅保留历史记录，不是当前契约。

## 基本信息

- 计划 ID：PLAN-20260724T144653184Z
- 状态：已完成
- 创建时间：2026-07-24
- 需求来源：用户反馈
- 关联问题：BUG-0125
- 关联模块：Node / Vue / AI 账户 / 模型目录 / 人工测试 / 文档 / 验证

## 目标

- 供应商模型选项在同一响应中返回协议、服务等级和思考能力，不再由前端按模型追加能力请求。
- 历史目标：账户测试模型选项曾在同一响应中返回模型协议和账户可用测试请求形态；当前已改为批量 `{ id, name }` 与单模型 capabilities 分离。
- 纯图片模型在新增、编辑和人工测试界面显示 `Images API`，不再显示误导性的 Responses 文本协议。

## 范围

- [x] 扩展 `/providers/models/options` 响应能力字段。
- [x] 历史实现曾扩展账户 `test-options` 响应协议与 `testEndpointModes`；该响应扩展现已废止。
- [x] 历史实现曾删除测试弹窗逐模型能力请求；当前请求形态下拉已恢复按当前模型定点读取。
- [x] 补充后端 HTTP、前端状态与界面联动回归。
- [x] 更新权威设计、接口和验证文档。
- [x] 使用隔离开发实例与 Browser 验证真实交互。

不包含数据库迁移、生产部署、删除兼容能力详情接口或改变网关运行时协议校验。

## 设计与安全边界

- 权威设计沿用并更新 `docs/functions/AI账户检查模型与人工测试设计.md` 与 `docs/functions/AI账户上游接口能力设计.md`。
- 列表返回的能力用于前端即时联动；测试提交和账户保存仍由后端按当前目录与账户能力重新校验。
- 搜索与 `limit <= 50` 边界保持不变，不返回价格、凭据或其他账户敏感字段。
- 管理端代用户作用域和个人端作用域保持现有权限隔离。

## 验收标准

以下勾选项是 2026-07-24 的历史验收，不作为当前账户测试接口证据；当前验收见 PLAN-20260727T183737351Z。

- [x] `gpt-image-2` 模型选项直接携带 `supportedApiProtocols: ['images']`。
- [x] 账户测试模型选项直接携带 `testEndpointModes: ['images_json']`。
- [x] 前端选择 `gpt-image-2` 后右侧立即显示 `Images API`，且不请求模型能力详情端点。
- [x] 文本及混合协议模型仍显示其与账户能力的正确交集。
- [x] 定向回归、前后端 typecheck、前端 build 和 Browser 验证通过。

## 验证记录

下表只记录历史实现当时的结果，其中账户 `test-options` 与测试弹窗请求矩阵已经过时；不得据此恢复批量能力响应。

| 验证项 | 命令 / 步骤 | 状态 | 结果 |
| --- | --- | --- | --- |
| 后端模型目录 | 定向模型选项断言；`pnpm.cmd --filter juhe-ai-backend test:model-catalog` | 通过（聚合套件有无关失败） | 本次新增的能力字段断言通过；聚合套件随后被工作区既有 GLM 目录快照和个人默认模型断言阻断。 |
| 后端测试选项 | `pnpm.cmd --filter juhe-ai-backend test:account-test-options-http-contract`、`test:account-test-task-boundary` | 通过 | `test-options` 一次返回模型协议与账户可用测试形态。 |
| 前端账户协议 | `pnpm.cmd --filter juhe-ai-frontend test:account-test-protocols`、`test:account-health-check-endpoint-mode` | 通过 | 纯图片模型联动为 `Images API`，文本及混合模型保持正确交集。 |
| 前端人工测试 | `pnpm.cmd --filter juhe-ai-frontend test:account-test-models`、`test:account-test-lazy-loading` | 通过 | 打开时加载一次选项，切换模型不追加能力详情请求。 |
| 类型与构建 | 前后端 typecheck、前端 build | 通过 | 类型检查通过；生产构建通过，仅保留既有 chunk 体积警告。 |
| 浏览器 | 隔离实例新增账户与手动测试 | 通过 | `gpt-image-2` 在新增表单和人工测试中均显示 `Images API`；控制台无错误，网络仅请求 `test-options`。 |
