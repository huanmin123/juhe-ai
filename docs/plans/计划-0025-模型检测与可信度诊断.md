# PLAN-0025 模型检测与可信度诊断

## 基本信息

- 编号：PLAN-0025
- 状态：进行中
- 创建时间：2026-05-20
- 更新时间：2026-05-21
- 需求来源：用户对话 / 调研
- 执行者：AI / 维护者
- 关联模块：前端 / 后端 / 存储 / 网关 / 权限 / 文档 / 验证

## 需求目标

- 背景：用户希望新增一个独立菜单，让管理员和普通用户都能自行检查当前链路是否可信地提供目标模型；第一版只支持 `gpt-5.5` 和 `gpt-5.4`。
- 目标：提供内置自托管的模型可信度检测能力，供用户在 `我的 AI 账户` 中检查自己的 AI 账户是否像目标模型，同时把检测请求纳入现有权限、额度、使用记录、审计和脱敏边界；当前优先保证检测准度，成本和耗时由发起用户承担。
- 交付物：长期设计文档、调研结论、接口契约、后端检测服务、记录库摘要表、前端中文页面、专项回归和验证记录。

## 范围边界

### 本次包含

- [x] 落地长期设计文档，固定功能口径、检测层次、结果等级和安全边界。
- [x] 完成第一轮官方文档、开源项目、学术安全研究和第三方检测网站调研。
- [x] 同步架构总览、功能目录和接口权限矩阵。
- [x] 实现管理侧 `/__aisys__/api/model-checks/*` 接口。
- [x] 实现用户侧 `/__aisys__/api/my-model-checks/*` 接口。
- [x] 新增记录库 `model_check_runs`、`model_check_items` 及稳定分页索引。
- [x] 实现轻量 `probe runner + deterministic checker`。
- [x] 实现管理侧和用户侧独立菜单 `模型检测`。
- [x] 补充自动回归、类型检查、构建和真实 / mock 链路验证记录。

### 本次不包含

- 绝对模型真伪证明：黑盒 API 无法在软件层证明上游物理模型身份，页面和接口只输出可信度等级。
- 第三方检测站跳转：不推荐用户把 API Key、base URL 或检测内容交给外部站点。
- 非 OpenAI 供应商：第一版只覆盖 OpenAI 供应商下的 `gpt-5.5` 和 `gpt-5.4`。
- 研究级评测平台：不引入 OpenAI Evals、promptfoo、HELM、lm-evaluation-harness 作为运行时依赖。
- TEE、logprobs 统计证明、完整模型指纹分类器：作为后续研究项，不进入第一版。

## 关联文档

- 架构文档：`docs/architecture/架构总览.md`
- 功能设计：`docs/functions/模型检测设计.md`
- 接口与权限：`docs/functions/接口契约与权限矩阵.md`
- 功能目录：`docs/functions/README.md`
- OpenAI 账号接入：`docs/functions/OpenAI账号接入.md`
- 存储说明：`docs/functions/SQLite存储说明.md`
- 验证手册：`docs/develop/测试与验证说明.md`

## 方案概述

- 方案原则：内置检测、自托管执行、复用本地 OpenAI 网关链路、按真实使用计入额度和记录、只保存脱敏有界摘要。
- 数据变化：新增检测运行表和检测项表，目标字段固定记录 AI 账户目标、目标 ID、目标模型、内部完整检测 profile、等级、分数、traceId、调用方、耗时、错误摘要和脱敏证据。
- 接口变化：新增管理侧和用户侧两套 model-checks 接口，普通用户接口固定当前登录用户作用域，管理员接口支持管理筛选。
- 前端变化：新增独立菜单 `模型检测`，提供 AI 账户选择、模型选择、可信对比账户选择、结果总览、证据列表和历史记录；不展示检测档位。
- 后端变化：新增模型检测模块，复用账号测试中的内存网关请求能力和网关实际转发路径，不在检测服务里另写直连上游。
- 数据处理策略：检测历史属于记录库排障数据，遵守保留期和分页窗口规则；检测产生的真实模型请求继续进入既有使用记录、统计和审计流程。

## 执行拆解

- [x] 更新 `docs/functions/模型检测设计.md`。
- [x] 更新 `docs/functions/README.md`。
- [x] 更新 `docs/architecture/架构总览.md`。
- [x] 更新 `docs/functions/接口契约与权限矩阵.md`。
- [x] 创建本计划并加入计划索引。
- [x] 明确记录库表结构、索引、保留策略和写入方式。
- [x] 实现后端 domain 类型、repository、service 和 routes。
- [x] 实现 AI 账户目标的检测执行路径。
- [x] 实现完整 profile，并固定使用准确优先的完整检测；快速 / 标准 profile 不开放且不在页面展示。
- [x] 实现可信对比账户显式选择：默认关闭，用户选择可信账户后必须校验账户可用性，不可用则开启失败并返回中文提示。
- [x] 实现前端页面、路由、菜单、API client 和中文交互。
- [x] 补充回归脚本、mock 上游场景和真实链路手动验证。
- [x] 更新完成总结和验证记录。

## 测试项

| 测试类型 | 测试项 | 验证方式 / 命令 | 预期结果 | 状态 | 实际结果或备注 |
| --- | --- | --- | --- | --- | --- |
| 命令类验证 | 后端类型检查 | `pnpm --filter juhe-ai-backend typecheck` | 后端 TypeScript 类型检查通过 | 通过 | 2026-05-20 已通过 |
| 命令类验证 | 前端类型检查 | `pnpm --filter juhe-ai-frontend typecheck` | 前端 TypeScript 类型检查通过 | 通过 | 2026-05-20 已通过 |
| 命令类验证 | 构建验证 | `pnpm build` | 前后端构建通过 | 通过 | 2026-05-20 已通过 |
| 后端回归 | 准确优先多轮检测 | mock 上游 + 多轮隐藏探针 | 多轮稳定、长上下文和辅助模型对照通过后允许 `high_confidence` | 通过 | `pnpm test:model-check-full-profile` |
| 后端回归 | 辅助模型对照 | 同一目标分别检测 `gpt-5.5` 和 `gpt-5.4` | 报告记录模型差异证据，完全同质时降低可信度 | 通过 | `pnpm test:model-check-full-profile` |
| 后端回归 | 分布相似度对照 | 目标账户与可信对比账户返回明显不同隐藏分布 | 生成 `distribution_similarity` 检测项，扣分降级但不误报为模型字段不一致 | 通过 | `pnpm test:model-check-distribution-similarity` |
| 后端回归 | 可信对比默认关闭 | 不传 `trustedComparisonAccountId` 发起完整检测 | 不消耗可信对比账户，不写入可信对比检测项 | 通过 | `pnpm test:model-check-trusted-comparison` 验证 options 默认关闭 |
| 后端回归 | 可信对比开启失败 | 显式传 `trustedComparison = true` 且未选择可信对比账户 | 接口返回中文错误并拒绝本次检测，不创建成功报告 | 通过 | `pnpm test:model-check-trusted-comparison` |
| 后端回归 | 严格模型匹配 | mock 上游请求 `gpt-5.4` 但响应 `gpt-5.4-mini-*` | 关键探针失败，整体结论为 `suspicious`，不误判为 `likely` | 通过 | `pnpm test:model-check-strict-model-match` |
| 后端回归 | 可信对比开启成功 | 显式传 `trustedComparisonAccountId` 且账户可用 | 同一探针集同时跑目标链路和可信对比链路，报告记录差异摘要 | 通过 | `pnpm test:model-check-trusted-comparison-success` |
| 后端回归 | 存储脱敏防泄露 | 构造含 API Key、Bearer token、代理密码、raw body 和超长文本的报告摘要 | 检测详情和历史列表均只保留脱敏有界摘要 | 通过 | `pnpm test:model-check-storage-sanitizer` |
| 后端回归 | AI 账户模型检测 | 复用单账号候选路径 | 只命中被测账号，不静默切到同分组其他账号 | 通过 | `pnpm test:model-check-full-profile` |
| 权限边界 | 用户侧检测授权资源 | 普通用户通过 `my-model-checks` 检测被授权账户 | 授权账户可检测，消费计入当前用户，详情不暴露所有者内部作用域 | 通过 | `pnpm test:model-check-user-authorized-resource` |
| 异常与边界 | 旧目标类型 / 账户不可检测 / 代理失败 / 上游不可用 | 非账户目标 + 未绑定分组 AI 账户 + 停用代理 + 持续 503 mock 上游 | 前置失败返回中文错误且不建报告；代理失败和上游失败报告只落 `unavailable`，不误判为假模型 | 通过 | `pnpm test:model-check-unavailable-boundary` |
| 前端体验 | 菜单、空态、进度和结果展示 | 浏览器手动验证 | 页面文案、提示、表格和错误均为中文 | 部分验证 | 本地前后端服务可启动并返回 HTTP 200；内置浏览器连接两次超时，未完成登录后点按验收 |
| 安全检查 | 敏感信息脱敏 | 检查报告详情和日志 | 不展示上游 API Key、OAuth token、代理密码、完整敏感请求头、完整探针题库 | 通过 | `pnpm test:model-check-storage-sanitizer` 与完整 profile 回归覆盖报告脱敏 |
| 未覆盖说明 | 绝对真伪证明 | 不执行 | 黑盒 API 不具备证明条件，只输出可信度诊断 | 不适用 | 固定边界 |

## 进度记录

| 日期 | 状态 | 记录人 | 进展 / 决策 / 阻塞 |
| --- | --- | --- | --- |
| 2026-05-20 | 进行中 | AI | 已落地长期设计文档、架构入口、接口权限矩阵和功能目录；完成第一轮官方、开源、学术与第三方检测站调研。 |
| 2026-05-20 | 进行中 | AI | 复核项目现有 `/v1/models` 行为后，决定把模型目录检测降为低权重证据，真实可信度主要依赖实际 Responses 请求、协议能力和行为探针。 |
| 2026-05-20 | 进行中 | AI | 根据用户补充“成本由用户承担，只考虑能否准”，将默认方案调整为准确优先：完整检测默认启用，多轮探针、长上下文、辅助模型对照和可选可信对比。 |
| 2026-05-20 | 进行中 | AI | 历史方案曾补充“官网对照默认不开启，用户需要时再开启；开启时没有可用官网账户则提示并开启失败”，后续已在 2026-05-21 调整为可信对比账户由用户选择。 |
| 2026-05-20 | 进行中 | AI | 已实现后端 `model-checks` / `my-model-checks` 接口、记录库表、完整 profile 探针执行器、历史官网对照失败语义、前端独立菜单和中文页面；后续已调整为可信对比。 |
| 2026-05-20 | 进行中 | AI | 真实账户验证发现 `gpt-5.4` 可被上游降级返回 `gpt-5.4-mini-*`；已收紧模型字段匹配规则，并补严格模型匹配回归，避免变体模型被误判为本体模型。 |
| 2026-05-20 | 进行中 | AI | 使用本地真实 OpenAI API Key 账户复测：`gpt-5.5` 报告 `mcr_1779297722674_d2f9e2b2` 为 `likely / 100`；`gpt-5.4` 报告 `mcr_1779297772928_0638ba2f` 为 `likely / 100`，后续连续 3 次 `gpt-5.4` 复测均返回精确 `gpt-5.4` 且无模型不匹配。 |
| 2026-05-20 | 进行中 | AI | 继续完善完整 profile：新增 `long_context` 中等长上下文找针和 `cross_model` 辅助模型对照；mock 回归覆盖 AI 账户目标、单账号不静默切号、历史官网对照成功路径和报告脱敏。 |
| 2026-05-21 | 进行中 | AI | 根据用户反馈“不要自动判断官网账户，应由用户选择可信对比账户”，将历史官网对照改为可信对比账户选择：前端下拉选择账户，后端按账户权限、分组和调度状态校验，不再按 API Key / OAuth、账户名或 base URL 猜测。 |
| 2026-05-21 | 进行中 | AI | 继续完善前端检测体验：目标选择改为可搜索的 `我的 AI 账户` 账户选择器，降低用户自行检测填错目标的概率。 |
| 2026-05-21 | 进行中 | AI | 补强模型检测记录脱敏：顶层 message / errorMessage 与 JSON 摘要统一识别 `sk-*`、Bearer / Basic 凭据和带密码代理 URL，并新增存储脱敏专项回归。 |
| 2026-05-22 | 进行中 | AI | 根据继续调研结论补强可信对比：开启可信对比时额外执行隐藏分布相似度对照，多次采样比较成功率、约束满足率、输出长度、usage 和文本相似度。 |
| 2026-05-21 | 进行中 | AI | 补齐用户侧授权资源检测回归：普通用户可检测被授权账户，实际上游命中所有者账户但使用记录、额度归属和详情可见范围按当前用户处理。 |
| 2026-05-21 | 进行中 | AI | 补齐异常边界回归：旧目标类型会被拒绝且不创建报告，AI 账户未绑定可用分组会在目标解析阶段中文失败，代理失败与上游持续 503 时报告只落 `unavailable`，避免把运维不可用误判成模型不符。 |

## 决策记录

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-05-20 | 功能命名为 `模型检测`，结论使用可信度等级 | 黑盒链路不能证明物理模型身份 | UI 和接口禁止输出“已证明为真模型” |
| 2026-05-20 | 第一版只支持 `gpt-5.5` 和 `gpt-5.4` | 用户当前范围明确，便于维护探针基线 | 后续新增模型必须先补目录、探针和评分基线 |
| 2026-05-20 | 检测请求复用本地网关链路 | 保持权限、额度、审计、代理和调度一致 | 不新增独立直连上游实现 |
| 2026-05-20 | 不推荐第三方检测网站作为产品入口 | 用户需要提交 API Key 和 base URL，安全边界不可控 | 只把外部项目作为调研参考 |
| 2026-05-20 | 不引入外部评测框架作为运行依赖 | 框架偏重，且不符合本项目轻量定位 | 内部实现轻量探针执行器和确定性判分 |
| 2026-05-20 | 固定使用准确优先完整检测 | 成本和耗时由用户承担，目标是尽量减少误判 | 页面不展示检测档位，`high_confidence` 必须多轮稳定通过 |
| 2026-05-20 | 历史官网对照默认关闭且开启失败不降级 | 避免默认消耗官网账号，也避免用户以为已对照但实际未对照 | 历史决策，2026-05-21 已被“可信对比账户由用户选择”替代 |
| 2026-05-21 | 可信对比账户由用户选择 | OAuth 账户也可能是用户信任的对比链路，系统不应按类型、名称或 base URL 猜测“官网账户” | `trustedComparisonAccountId` 指向的账户必须可访问、可调度、绑定可用分组且不能等于检测目标 |

## 验收标准

- [x] 管理员和普通用户都有独立 `模型检测` 菜单，且权限作用域正确。
- [x] 支持检测 AI 账户，目标模型限定为 `gpt-5.5`、`gpt-5.4`。
- [ ] 检测报告只输出 `高可信`、`较可信`、`不确定`、`疑似不符`、`不可检测` 等结论。
- [x] 默认检测使用完整 profile，覆盖多轮行为探针、长上下文、辅助模型对照和稳定性判断；开启可信对比时额外覆盖分布相似度对照。
- [x] 可信对比默认不开启；用户显式选择可信对比账户后才执行，同一账户不可同时作为目标和对比账户。
- [x] 检测请求走本地网关并进入使用记录、统计、额度和审计流程。
- [x] 检测历史分页读取检测记录摘要，不扫描使用记录或审计 payload。
- [x] 报告详情严格脱敏，不泄露凭据、代理密码、完整敏感头、完整响应体和隐藏题库。
- [x] 类型检查、构建、专项回归和必要手动验证完成或明确记录未覆盖原因。

## 验证记录

- 文档检索：已执行，命令：`rg -n "模型检测|my-model-checks|model-checks" docs`
- 官方资料核对：已执行，来源：OpenAI GPT-5.5 指南、Responses API OpenAPI、Models API OpenAPI、RBAC 权限文档。
- 开源资料核对：已执行，来源：OpenAI Evals、promptfoo、lm-evaluation-harness、HELM、Veridrop、LLMmap、LLM API Audit。
- 后端类型检查：已执行，命令：`pnpm --filter juhe-ai-backend typecheck`，结果通过。
- 前端类型检查：已执行，命令：`pnpm --filter juhe-ai-frontend typecheck`，结果通过。
- 可信对比专项回归：已执行，命令：`pnpm test:model-check-trusted-comparison`，结果通过。
- 可信对比成功回归：已执行，命令：`pnpm test:model-check-trusted-comparison-success`，结果通过。
- 分布相似度回归：已执行，命令：`pnpm test:model-check-distribution-similarity`，结果通过。
- 完整 profile 回归：已执行，命令：`pnpm test:model-check-full-profile`，结果通过。
- 严格模型匹配回归：已执行，命令：`pnpm test:model-check-strict-model-match`，结果通过。
- 异常边界回归：已执行，命令：`pnpm test:model-check-unavailable-boundary`，结果通过。
- 存储脱敏回归：已执行，命令：`pnpm test:model-check-storage-sanitizer`，结果通过。
- 用户侧授权资源回归：已执行，命令：`pnpm test:model-check-user-authorized-resource`，结果通过。
- 真实账户检测：已执行，命令为本地系统 API 登录后调用 `/__aisys__/api/model-checks/run`，目标为本地真实 OpenAI API Key 类型 AI 账户，未选择可信对比账户；`gpt-5.5` 和 `gpt-5.4` 最新复测均通过完整 profile。
- 构建：已执行，命令：`pnpm build`，结果通过。
- 前端服务访问：已执行，本地启动后 `http://127.0.0.1:3000/__aisys__/health` 和 `http://127.0.0.1:5174/__aisys__/` 均返回 HTTP 200；内置浏览器连接两次超时，未完成登录后页面点按验收，随后已停止本轮启动的本地服务。
- 未验证项：登录后浏览器页面点按仍可继续补强。

## 风险与注意事项

- 模型检测是概率诊断，不能用于法律、财务或官方认证结论。
- 探针题库必须隐藏和版本化；公开题面容易被中转服务针对适配。
- 检测会真实消耗 token、成本和授权额度，当前成本由发起用户承担；页面仍必须提前给出成本、次数和预计耗时提示。
- 可信对比会额外消耗用户选择的对比账户额度，必须由用户显式选择；未选择或账户不可用时不能自动降级为普通检测。
- OAuth / Codex adapter 与公开 OpenAI API Key 行为不同，报告必须展示 adapter 能力边界。
- 本地 `/v1/models` 当前来自模型目录，不能当作上游账号可见模型的强证据。
- 回滚建议：实现后如检测误报，可先隐藏菜单或禁用运行接口，保留历史记录只读查询。

## 完成总结

- 完成时间：2026-05-21 阶段性完成。
- 实际完成内容：已完成第一版模型检测闭环，并继续补强完整 profile、严格模型匹配、长上下文、辅助模型对照、可信对比成功路径、分布相似度对照、目标选择体验、检测记录脱敏、用户侧授权资源检测和不可用边界回归。
- 主要改动位置：`backend/src/modules/model-checks/`、`backend/src/storage/model-checks.repository.ts`、`backend/src/scripts/regression/model-check-*.ts`、`frontend/src/views/model-checks/`、`frontend/src/composables/useScopedDomainApi.ts`、`docs/functions/模型检测设计.md`。
- 验证结果：前后端类型检查、完整构建、可信对比、可信对比成功、分布相似度、完整 profile、严格模型匹配、存储脱敏、用户侧授权资源和不可用边界回归已通过；真实账户 `嘻嘻嘻嘻嘻嘻` 已完成 `gpt-5.5` / `gpt-5.4` 检测。
- 后续建议：继续补浏览器页面验收，并根据真实检测样本持续校准探针权重。
