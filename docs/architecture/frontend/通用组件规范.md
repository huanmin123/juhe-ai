# 前端通用组件规范

> 面向 AI 与维护者。
> 本文记录 `frontend/src/components/` 下可跨页面复用的组件边界。新增页面或改造页面前，应先确认这里是否已有组件可以承接，不要在单个页面重复手写同类下拉、状态标签或响应式列表逻辑。
> 前端架构入口见 [README.md](README.md)，通用样式原则见 [样式规范.md](样式规范.md)，列表响应式规则见 [响应式列表规范.md](响应式列表规范.md)。

## 1. 文件定位

- 本文件回答这些问题：
  - 哪些组件是前端全局通用组件
  - 新页面应该优先复用哪些组件
  - 组件的默认业务语义是什么
  - 什么场景允许继续使用 Ant Design Vue 原生组件
- 本文件只记录跨页面组件约定，不记录单个业务页面的临时实现细节。

## 2. 通用组件目录

- 通用组件统一放在 `frontend/src/components/`。
- 页面私有组件放在对应 `frontend/src/views/<module>/` 目录内。
- 当同一类控件在两个及以上页面重复出现，并且包含相同筛选、状态、权限或响应式规则时，应优先抽到 `frontend/src/components/`。
- 通用组件应保持轻量，不引入全局状态；页面负责加载数据并通过 props 传入。

## 3. 系统主体选择器

### 3.1 组件定位

- 组件：`frontend/src/components/SystemPrincipalSelect.vue`
- 用途：统一选择系统账户、系统团队，或同时选择二者。
- 适用场景：
  - 系统账户筛选
  - 授权对象选择
  - 团队成员添加
  - 后续任何“选择系统账户 / 选择团队 / 选择系统主体”的表单或筛选

涉及系统账户或系统团队选择时，优先使用 `SystemPrincipalSelect`，不要在页面里重新手写 `a-select` options、停用过滤、停用标记或账号展示名格式。

### 3.2 核心 props

| prop | 说明 |
| --- | --- |
| `accounts` | 系统账户列表，由页面调用 `api.systemAccounts.list()` 后传入 |
| `teams` | 系统团队列表，由页面调用 `api.systemTeams.list()` 后传入 |
| `scope` | 选择范围；`system_account`、`team`、`all`，默认 `system_account` |
| `activeOnly` | 是否只显示启用对象，默认 `true` |
| `includeAll` | 是否展示“全部系统账户”选项，常用于筛选 |
| `allLabel` / `allValue` | 自定义全部选项文案和值，默认 `全部系统账户` / `all` |
| `excludedIds` | 需要排除的对象 ID，例如团队成员添加时排除已有成员 |
| `mode` | 透传给 `a-select` 的模式，例如 `multiple` |

### 3.3 启停语义

- 功能性选择默认使用 `activeOnly=true`，只显示启用对象。
  - 示例：新增授权的被授权用户 / 团队。
  - 示例：系统团队添加成员。
- 筛选性选择可以使用 `activeOnly=false`，保留停用对象并在选项文案后显示 `（停用）`。
  - 示例：账户、分组、API Key、使用记录、用量统计里的系统账户筛选。
  - 示例：授权管理按团队来源或被授权用户筛选历史记录。
- 页面不应自行判断“停用对象是否可选”；如果确有例外，先评估是否应扩展 `SystemPrincipalSelect` 的 props。
- 前端过滤只改善体验，后端仍必须校验停用系统账户、停用团队、成员失效和授权失效。

### 3.4 使用示例

筛选系统账户，保留停用对象：

```vue
<SystemPrincipalSelect
  v-model:value="systemAccountFilter"
  :accounts="systemAccounts"
  :active-only="false"
  include-all
  class="toolbar-select responsive-list-inline-filter"
  @change="loadData"
/>
```

新增授权时选择启用团队：

```vue
<SystemPrincipalSelect
  v-model:value="createForm.granteeId"
  :teams="teams"
  scope="team"
  placeholder="选择一个团队"
/>
```

添加团队成员，排除已有成员：

```vue
<SystemPrincipalSelect
  v-model:value="memberForm.systemAccountIds"
  :accounts="systemAccounts"
  :excluded-ids="usedMemberIds"
  mode="multiple"
  placeholder="选择一个或多个系统账户"
/>
```

### 3.5 实体下拉与标签缓存

- 组件：`frontend/src/components/CachedSelect.vue`
- 组件：`frontend/src/components/AccountSelect.vue`
- 组件：`frontend/src/components/GroupSelect.vue`
- 组件：`frontend/src/components/ProxySelect.vue`
- 用途：实体类下拉统一保持 `id + 名称` 成对缓存，避免候选列表尚未加载时把 ID 直接显示给用户。
- 适用场景：
  - AI 账户下拉、账户筛选、检测目标账户选择。
  - 代理、分组、系统主体等以 ID 提交、以名称展示的实体下拉。
- 默认语义：
  - 页面查询和提交仍只传 ID。
  - 页面内选中态和缓存必须保留名称。
  - 未拿到名称时不回退显示 ID；这类问题应从数据回填、接口返回或组件选中态修正。
  - 搜索候选由页面在用户打开下拉或输入搜索词后加载；页面初始化不主动预加载候选列表。
  - 远程候选默认最多展示 50 条；如果需要保留当前已选中项，应通过 `selectedIds` / `selectedOptions` 让组件只补齐已选中的展示标签。
  - `CachedSelect` 会按当前登录用户在浏览器本地记录“该下拉里被选中过的频率”，后续静默把常用项排到前面；排序只影响前端展示，不改变后端查询、权限或业务口径。
  - 需要隔离不同场景的选择习惯时，专用实体组件可以传 `preferenceKey`，例如列表筛选、表单选择、批量操作各自使用独立 key；不传时使用组件默认实体 key。
  - 需要查库的下拉应优先读本地 50 条候选窗口并立即展示，再在后台请求远程候选补差异；本地窗口只缓存 ID、标签和下拉展示所需的轻量字段，不缓存凭据、密钥、审计内容或大 payload。
  - 缓存候选如果经远程 `ids` 补查确认已经不存在或当前用户无权访问，必须从本地候选窗口和选择频率里移除；如果该值正被选中，应清空当前选择并用中文提示用户重新选择。

涉及 AI 账户选择时优先使用 `AccountSelect`；涉及分组选择时优先使用 `GroupSelect`；涉及代理选择时优先使用 `ProxySelect`。业务页面不要直接用裸 `CachedSelect` 承接实体下拉，除非正在新增专用实体选择组件。

## 4. 模型筛选选择器

### 4.1 组件定位

- 组件：`frontend/src/components/ModelFilterSelect.vue`
- 用途：统一按系统支持的模型名称筛选列表。
- 适用场景：
  - 使用记录里的模型筛选。
  - 后续按模型精确筛选的审计、日志或统计明细页面。

模型筛选应优先使用 `ModelFilterSelect`，由页面加载后端模型选项并传入组件；组件只负责模型名展示、清空和本地下拉输入匹配。

### 4.2 默认语义

- 选项来源使用后端本地供应商模型目录，例如 `/providers/models/options`。
- `value` 直接使用模型名并保留原始大小写；组件不额外拼接供应商前缀，也不把仅大小写不同的模型折叠成同一项。
- 输入匹配只发生在下拉选项内，页面提交给后端的仍是完整模型名。
- 使用记录等大表筛选不得把输入关键字传给后端做 `LIKE`；后端仍按 `model = ?` 精确查询。
- 组件不直接请求接口，不判断权限，不扫描使用记录明细。

### 4.3 使用示例

```vue
<ModelFilterSelect
  v-model:value="modelFilter"
  :loading="modelOptionsLoading"
  :models="modelOptions"
  placeholder="全部模型"
  @change="loadData"
/>
```

## 5. 响应式列表组件

- 管理列表优先组合使用：
  - `ResponsiveListToolbar.vue`
  - `ResponsiveDataList.vue`
- 页面、弹窗、抽屉和业务组件里需要表格展示时，统一使用 `ResponsiveDataList`；只有 `ResponsiveDataList` 内部可以直接封装 Ant Design Vue 的 `a-table`。
- 不要在业务页面里直接写 `a-table`，避免固定列、滚动、空态、手机形态和操作列样式各自定制导致不一致。窄例外：导入预览弹窗、配置指南弹窗、固定小数据量且无分页 / 排序 / 响应式卡片诉求的只读说明表，可以直接使用 `a-table`，但不能把这个例外扩展到业务主列表或可增长明细。
- 详情弹窗里的小表格如果不需要内部滚动，应使用 `ResponsiveDataList` 并传入 `pagination=false`、`tableScrollEnabled=false`、`lockBodyScroll=false`，让内容随弹窗或抽屉主体自然滚动。
- 详细规则见 [响应式列表规范.md](响应式列表规范.md)。
- 新列表不要复制断点监听、滚动锁、手机筛选抽屉、下拉刷新或加载更多逻辑。
- 需要服务端排序时，优先使用 `ResponsiveDataList` 的 `sort-change` 事件接收标准化排序数组；页面负责把列 key 映射成业务 API 白名单字段，后端负责按同一字段白名单生成查询排序，避免在页面层把完整数据拉到内存后再排序。

## 6. 状态标签组件

- 状态展示优先使用 `StatusTag.vue` 或现有模块内状态标签组件。
- 状态颜色必须配合中文文本，不要只用颜色表达状态。
- 复杂状态如 AI 账户状态，可以继续使用模块内封装，例如账户页的 `AccountStatusTag.vue`。

## 7. 表格列管理组件

### 7.1 组件定位

- 组件：`frontend/src/components/TableColumnManager.vue`
- 工具：`frontend/src/components/tableColumnSettings.ts`
- 用途：统一管理表格列的显示 / 隐藏、拖拽排序和左 / 右固定。
- 适用场景：
  - 使用记录、API Key、AI 账户、运行日志、审计日志等列数量较多的列表。
  - 管理视图和用户视图列不同，但需要分别保存列偏好的页面。

### 7.2 默认语义

- 页面负责提供原始 `columns`，并通过 `useTableColumnSettings` 得到实际传给 `ResponsiveDataList` 的 `managedColumns`。
- `TableColumnManager` 只负责配置 UI，不直接请求接口，不判断业务权限。
- 列偏好默认写入浏览器本地 `localStorage`，按页面传入的 `storageKey` 隔离；管理侧和用户侧列不同的页面应使用不同 key。
- 至少保留一列显示；确认为核心识别字段的列应通过 `requiredKeys` 禁止隐藏，例如使用记录的 `AI账户名称`。
- 固定列必须按表格渲染规则自动归位：左固定列连续靠左，普通列居中，右固定列连续靠右；切换固定位置时同步移动到对应分组，拖拽和上 / 下移只在当前固定分组内调整顺序。
- 拖拽使用浏览器原生能力，不额外引入拖拽依赖；同时保留上移 / 下移按钮作为兜底。

### 7.3 使用要求

- 新增列管理时优先复用 `TableColumnManager`，不要在页面内重复实现拖拽、显隐和本地存储。
- 列 key 必须稳定，优先使用显式 `key`；不能依赖展示文案作为长期配置标识。
- 列管理只影响表格展示，不改变后端查询、排序、权限或导出语义；如果隐藏了当前排序列，排序状态仍按页面原有逻辑执行。

## 8. 只读代码查看器

- 组件：`frontend/src/components/ReadonlyCodeViewer.vue`
- 用途：使用 CodeMirror 展示只读 JSON 或文本内容，内置行号、JSON 格式化、大小提示和复制入口。
- 适用场景：审计日志原始内容，以及后续排障类只读 payload。
- 页面负责传入已经按权限处理后的文本；组件不请求接口，不自行解密，不绕过后端脱敏边界。
- 需要标题时使用 `title`，标题会展示在组件工具栏内。
- 如果页面已有自定义外层工具栏，可设置 `showToolbar=false` 和 `attachedToolbar=true`，保持编辑器边框与外层工具栏拼接一致。

## 9. 列表操作组件

### 9.1 组件定位

- 组件：`frontend/src/components/RowActions.vue`
- 类型：`frontend/src/components/rowActions.ts`
- 用途：统一列表操作列和移动端卡片底部操作的图标、颜色、更多菜单和删除确认表现。
- 适用场景：
  - 表格操作列中的编辑、删除、测试、更多操作。
  - 移动端列表卡片底部操作。
  - 弹窗内小表格的轻量操作入口。

### 9.2 默认语义

- 桌面表格默认使用图标按钮，编辑、删除、更多等动作通过 tooltip 展示中文说明。
- 移动端卡片默认使用图标加中文文字，保证按钮面积和语义可见。
- 危险动作必须通过 `confirmTitle` 接入二次确认，不允许绕过确认直接删除。
- `more` 菜单项必须带图标和语义色；颜色只辅助表达，中文文案仍是主语义。

### 9.3 动作色彩

| tone | 用途 |
| --- | --- |
| `primary` | 编辑、主要配置 |
| `success` | 启用、恢复、通过 |
| `warning` | 停用、重置、需要注意 |
| `info` | 测试、检测、查看详情 |
| `purple` | 迁移、切换、调整绑定 |
| `danger` | 删除、移除 |
| `default` | 复制、普通辅助操作 |

### 9.4 使用要求

- 新增列表页必须优先使用 `RowActions`，不要重复手写纯文字操作列。
- 已有页面改造时，优先把常用动作放在 `actions`，低频动作放在 `moreActions`。
- 操作列宽度应随图标化收敛；只有移动端卡片或需要长文案时才保留完整文字按钮。
- 权限隐藏由页面负责判断；组件只负责展示和触发动作。

## 10. 新增通用组件规则

- 先确认是否已有组件能通过 props 扩展解决问题。
- 新通用组件要保持输入清晰，不直接请求接口，不隐藏页面级权限逻辑。
- 新通用组件需要同步更新本文，并说明：
  - 组件路径
  - 适用场景
  - 默认语义
  - 关键 props
  - 不适用场景
- 修改通用组件后至少运行：

```powershell
pnpm --filter juhe-ai-frontend typecheck
```
