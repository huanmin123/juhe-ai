# 开发测试与验证说明

> 面向 AI 与维护者。
> 本文件承接代码级检查、接口验证、网关联通、账号测试和自动烟测；启动方式和运行注意事项见 [开发运行说明](运行说明.md)。

## 1. 适用场景

- 需要确认代码改动没有破坏类型、构建或关键路径。
- 需要验证后端接口、管理面页面、账号测试、网关转发或使用记录。
- 需要执行自动烟测、回归验证或问题修复后的复现检查。

## 2. 代码级验证

在项目根目录执行：

```powershell
pnpm typecheck
pnpm build
```

如果只改了某一端，也可以先跑对应 workspace 的命令，再按需要补全全量验证。

本地业务库缺少发布模型目录快照，或模型 / 账户路由变更后需要离线重建全部系统账户快照时，使用：

```powershell
pnpm --filter juhe-ai-backend maintenance:rebuild-published-model-catalog
pnpm --filter juhe-ai-backend test:published-model-catalog-snapshot-contract
```

维护命令会在独立进程内显式切换到 `db-service` 可写角色，完成后依次关闭 SQLite 读 worker、Redis、SQLite 和 PostgreSQL 连接；不得以默认 `server` 只读连接执行，也不能遗漏读 worker 清理而让命令完成后继续挂起。该命令会重建当前本地或明确配置环境的全部发布模型目录快照，禁止未经部署授权指向生产数据库执行。

涉及前端 Build ID、Vite 静态清单、发布注入、动态路由资源加载失败提示或自动刷新时，至少运行：

```powershell
pnpm --filter juhe-ai-frontend test:frontend-build-info
pnpm --filter juhe-ai-frontend test:route-load-recovery
pnpm --filter juhe-ai-frontend typecheck
pnpm --filter juhe-ai-frontend build
```

第一条覆盖完整 40 位 Build ID 规范化、变化 / 相同 / 未知分类、清单禁用缓存、异常响应和 1500ms 超时；第二条固定 Vite preload error 复用统一恢复流程、确认版本前不声称系统更新、首次自动恢复和 30 秒内重复失败改为手动刷新。构建完成后还必须核对 `frontend/dist/build-info.json.buildId` 等于当前构建提交；正式发布包则必须等于 `RELEASE_SOURCE_COMMIT`。

涉及开发免登录、登录路由或鉴权中间件时，至少运行：

```powershell
pnpm test:development-auto-login
pnpm --filter juhe-ai-backend test:runtime-development-security
pnpm test:auth-must-change-password-boundary
pnpm --filter juhe-ai-backend typecheck
```

第一条固定普通开发不默认注入免登录身份、AI 测试显式设置时原样透传，并继续禁止生产环境启用；第二条固定非生产环境安全策略冲突不阻止启动、生产环境仍然 fail-fast；第三条继续固定普通运行时未登录返回 401、初始密码和管理员边界，防止开发测试通道扩散到生产鉴权。

## 2.0 高性能模式基础回归

涉及 `JUHE_AI_RUNTIME_MODE`、PostgreSQL / Redis 配置、缓存工具、运行态工具、登录限流、账号并发槽或记录类队列时，至少先跑：

```powershell
pnpm --filter juhe-ai-backend test:runtime-config-env-override
pnpm --filter juhe-ai-backend test:runtime-state-cache-driver
pnpm --filter juhe-ai-backend test:runtime-state-cache-redis
pnpm --filter juhe-ai-backend test:client-ip-policy-shared-cache-redis
pnpm --filter juhe-ai-backend test:hybrid-scoring-shared-cache-redis
pnpm --filter juhe-ai-backend test:database-client
pnpm --filter juhe-ai-backend test:provider-repository-driver
pnpm --filter juhe-ai-backend test:system-account-session-driver
pnpm --filter juhe-ai-backend test:system-account-management-driver
pnpm --filter juhe-ai-backend test:group-management-driver
pnpm --filter juhe-ai-backend test:api-key-management-driver
pnpm --filter juhe-ai-backend test:authorization-return-driver
pnpm --filter juhe-ai-backend test:settings-public-driver
pnpm --filter juhe-ai-backend test:settings-management-driver
pnpm --filter juhe-ai-backend test:settings-query-boundary
pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-runtime-state
pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-index
pnpm --filter juhe-ai-backend test:db-service-sqlite-dispatch-standalone
pnpm --filter juhe-ai-backend test:db-service-runtime-metrics
pnpm --filter juhe-ai-backend test:redis-stream-queue
pnpm --filter juhe-ai-backend test:runtime-log-hot-path-boundary
pnpm --filter juhe-ai-backend test:runtime-log-file-consumer-contract
pnpm --filter juhe-ai-backend test:runtime-log-file-consumer
pnpm --filter juhe-ai-backend test:runtime-log-file-rotation
pnpm --filter juhe-ai-backend test:runtime-log-file-consumer-pressure
pnpm --filter juhe-ai-backend test:runtime-log-redis-removal
使用测试环境 `JUHE_AI_POSTGRES_URL` 和 `JUHE_AI_REDIS_QUEUE_URL` 运行 `pnpm --filter juhe-ai-backend test:runtime-log-postgres-consumer-smoke`
pnpm --filter juhe-ai-backend test:performance-boundary
pnpm --filter juhe-ai-backend test:usage-hot-window-refresh
pnpm --filter juhe-ai-backend test:usage-overview-window-incremental
pnpm --filter juhe-ai-backend test:performance-redis-boundary
pnpm --filter juhe-ai-backend test:performance-no-sqlite-fallback
pnpm --filter juhe-ai-backend test:operation-log-queue
pnpm --filter juhe-ai-backend test:public-api-logs
pnpm --filter juhe-ai-backend test:public-api-log-db-service-ipc
pnpm --filter juhe-ai-backend test:audit-log-settings-contract
pnpm --filter juhe-ai-backend test:audit-log-transport-worker
pnpm --filter juhe-ai-backend test:gateway-response-lifecycle
pnpm --filter juhe-ai-backend test:gateway-response-lifecycle-http
pnpm --filter juhe-ai-backend test:server-audit-shutdown
使用下文 `JUHE_PERFORMANCE_SYSTEM_API_*` 专用环境变量运行 `pnpm --filter juhe-ai-backend test:performance-system-api-smoke`
pnpm --filter juhe-ai-backend test:authorization-list-team-usage-batch
pnpm --filter juhe-ai-backend test:authorization-options-query-guard
pnpm --filter juhe-ai-backend test:authorization-usage-pagination
pnpm --filter juhe-ai-backend test:resource-authorization-single-read
pnpm --filter juhe-ai-backend test:performance-system-api-load
pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness
pnpm --filter juhe-ai-backend test:performance-reliability-drill
pnpm --filter juhe-ai-backend test:postgres-schema-sql
pnpm --filter juhe-ai-backend test:postgres-seed-defaults
pnpm --filter juhe-ai-backend test:auth-memory-maintenance-boundary
pnpm --filter juhe-ai-backend test:account-concurrency-lane-index
pnpm --filter juhe-ai-backend test:gateway-concurrency-snapshot
pnpm --filter juhe-ai-backend test:gateway-concurrency-preparation
pnpm --filter juhe-ai-backend test:authorized-account-admin-dispatch
pnpm --filter juhe-ai-backend test:usage-record-writer-pool
pnpm --filter juhe-ai-backend test:usage-record-byte-batch
pnpm --filter juhe-ai-backend test:performance-mode-isolation
pnpm --filter juhe-ai-backend test:storage-adapter-boundary
pnpm --filter juhe-ai-backend typecheck
```

这些命令只验证默认单机模式、新增抽象的内存 driver、真实 Redis cache / runtime state driver、客户端 IP 策略单 IP Redis shared cache、混合路由评分结果 Redis shared cache、`DatabaseClient` 基础层、provider 只读 repository 的本地 SQLite 分支、登录 / 会话最小 repository 的本地 SQLite 分支、系统账户管理读写的本地 SQLite 分支、分组管理读写的本地 SQLite 分支、API Key 管理读写与网关 Key 校验入口的本地 SQLite 分支、账户授权实例归还 / 授权列表个人归还 / 分组个人归还 / 现有授权更新和回收的本地 SQLite 分支、公共设置读取、系统设置管理读写和系统设置 SQL 白名单查询边界的本地 SQLite 分支、网关缓存失效 runtime state 版本广播源码边界、API Key 校验 / API Key 额度 / 授权额度 / 系统设置 / 网关设置 / 网关分组访问元数据 / 分组绑定账号 ID / 授权统计与授权来源 lookup / 授权读资源 lookup / 网关模型目录 / 模型路由索引 / 客户端 IP 策略单 IP 条目 / 混合路由评分结果 Redis shared cache 源码门禁、高性能模式缓存 / 运行态 / 队列 Redis 事实源全局静态门禁、SQLite standalone 模式 DB service 异步 dispatch 分支、PG 模式 DB service 热读并发白名单、Redis Streams 队列解析 / 配置 / 使用记录 / 审计日志 / 操作日志 / 公开接口日志 / 数据维护 / 运行日志索引接入边界、审计 payload Buffer 安全编码、审计高性能采样契约、最小 System API HTTP 链路、网关 usage / audit / operation / public API / usage stats 核心事实表、审计错误分组、审计读路径、操作日志读路径、公开接口日志读路径和运行日志读路径 PG readiness、usage model / error / latency / account quality 派生统计 PG readiness、PostgreSQL DDL 生成、默认 seed 与内置分组引用一致性、账户列表并发读数契约、高性能隔离静态门禁、存储适配直接调用点清单和高性能可靠性演练入口。Docker compose、Redis Streams 重启后积压恢复、record maintenance / runtime log 真实 Stream 已有专项验证，结果以 `docs/reports/` 为准；完整真实 PostgreSQL repository 行为、备份恢复和未迁移 PG repository 行为仍需按功能在 Linux 测试栈补做。

上一段汇总中“运行日志索引接入边界”和“runtime log 真实 Stream”是历史错误表述，不属于当前契约，也不得作为后续实现依据。运行日志专项已从 Redis Stream 接入边界中移除：高性能模式验证不得期待 `runtime-log-index` Stream、consumer group 或 XADD；应运行 `test:runtime-log-file-consumer*` 和 `test:runtime-log-redis-removal`，确认角色 JSONL 文件 backlog 完整追平。下文历史命令清单中凡提到“运行日志索引 Redis Stream”的描述均以此当前规则为准。

高性能模式中间件已启动后，PostgreSQL 初始化验证使用：

```powershell
pnpm --filter juhe-ai-backend postgres:init-schema-only
pnpm --filter juhe-ai-backend postgres:init-schema
```

`postgres:init-schema-only` 只验证当前版本 DDL；`postgres:init-schema` 会继续写入默认管理员、内置供应商 / 协议 / 协议档案 / 分组和系统设置。默认内置分组只绑定 provider，不引用 provider protocol profile；该契约必须由 `test:postgres-seed-defaults` 固定，避免 PostgreSQL seed 缺项导致创建系统账户时触发 `groups_provider_code_fkey`。完整初始化后必须在开放公网前轮换默认管理员密码。宿主机运行时如果 `JUHE_AI_POSTGRES_URL` / Redis URL 来自 Docker Compose 内部 DNS，需要临时改为宿主机发布端口，具体见 `../deploy/高性能模式部署指南.md`。

PostgreSQL 验证只支持当前 schema 初始化。已有历史测试库不要沿用原结构；需要保留数据时由运维在代码库外离线重建为当前 schema，再运行 `pnpm --filter juhe-ai-backend test:postgres-schema-sql` 固定当前 DDL 边界。

从宿主机或开发机验证 Docker 栈时，不要直接复用应用容器内 URL。容器内可用 `pgbouncer:5432`、`redis-cache:6379`、`redis-state:6379`、`redis-queue:6379`；宿主机需要使用测试主机 IP 和发布端口，例如 `<测试主机IP>:6432`、`<测试主机IP>:6379`、`<测试主机IP>:6380`、`<测试主机IP>:6381`。其中 redis-state / redis-queue 容器内端口仍是 `6379`，但默认宿主机发布端口分别是 `6380` / `6381`。

如果 `redis-cache` 可连接但 `redis-state` 宿主机发布端口超时，登录保护、运行态锁和账号并发 Redis Lua 等路径会卡在 runtime state 连接，`test:performance-system-api-smoke` 可能在登录阶段超时。此时先恢复 redis-state 发布端口或容器，再复跑真实 state 验证；不得为了让测试继续而临时让 cache/state URL 指向同一个 Redis 进程，即使使用不同 DB 也会被 Go 配置校验拒绝。

Redis 密码包含 `@`、`:`、`/`、`#` 等特殊字符时，写入 `JUHE_AI_REDIS_CACHE_URL`、`JUHE_AI_REDIS_STATE_URL`、`JUHE_AI_REDIS_QUEUE_URL` 或各测试脚本专用 Redis URL 前必须 URL 编码，否则 Node URL 解析会把密码切断，表现为健康检查失败或子进程连错地址。

真实 Redis cache / runtime state driver 回归使用：

```powershell
$env:JUHE_AI_REDIS_CACHE_URL = "redis://:<Redis缓存密码URL编码>@<测试主机IP>:6379/0"
$env:JUHE_AI_REDIS_STATE_URL = "redis://:<Redis运行态密码URL编码>@<测试主机IP>:6380/0"
pnpm --filter juhe-ai-backend test:runtime-state-cache-redis
Remove-Item Env:\JUHE_AI_REDIS_CACHE_URL, Env:\JUHE_AI_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖 `SharedJsonCache` 的 TTL / delete / clear 版本失效、`RuntimeStateStore` 的 JSON 读写 / `incr` 上限 / lock token 释放、登录失败锁定和账号并发 Redis Lua 原子占用；脚本只写入带 `regression_*` 后缀的临时 key，并在结束时按 pattern 清理。

客户端 IP 策略单 IP Redis shared cache 回归使用：

```powershell
$env:JUHE_CLIENT_IP_POLICY_SHARED_CACHE_REDIS_URL = "redis://:<Redis缓存密码URL编码>@<测试主机IP>:6379/0"
$env:JUHE_AI_ALLOW_CLIENT_IP_POLICY_SHARED_CACHE_CLEANUP = "1" # 仅确认该 Redis 是测试实例时设置
pnpm --filter juhe-ai-backend test:client-ip-policy-shared-cache-redis
Remove-Item Env:\JUHE_CLIENT_IP_POLICY_SHARED_CACHE_REDIS_URL, Env:\JUHE_AI_ALLOW_CLIENT_IP_POLICY_SHARED_CACHE_CLEANUP -ErrorAction SilentlyContinue
```

该命令在临时统计库创建 active policy，通过普通判定按 `ip_hash` 从数据库索引回源并写入单 IP Redis shared cache，随后验证 `cacheOnly` 能命中该单 IP 条目；禁用策略后先确认未失效的旧单 IP 条目仍会被 `cacheOnly` 命中，再通过绕过 shared cache 的重载清理旧条目；清理 shared cache 后，`cacheOnly` 不应回源数据库，普通判定应再次回源并重建单 IP 条目。该脚本会切换 / 清理固定 cache namespace；非本机 Redis 必须显式设置 `JUHE_AI_ALLOW_CLIENT_IP_POLICY_SHARED_CACHE_CLEANUP=1`，且只允许指向测试 Redis 或维护窗口内实例，不能直接打正在承载生产网关的 Redis。它同时固定 Redis cache driver 下 IP 策略判定不加载全量 active 策略快照的边界。

混合路由评分结果 Redis shared cache 回归使用：

```powershell
$env:JUHE_AI_REDIS_CACHE_URL = "redis://:<Redis缓存密码URL编码>@<测试主机IP>:6379/0"
pnpm --filter juhe-ai-backend test:hybrid-scoring-shared-cache-redis
Remove-Item Env:\JUHE_AI_REDIS_CACHE_URL -ErrorAction SilentlyContinue
```

该命令复用本机 mock hybrid 上游。第一次完全相同请求会调用入口评分模型并写入 Redis shared cache，测试随后清空本地评分 LRU 但保留 shared cache，第二次请求仍应只产生 1 次入口评分模型命中、2 次目标模型请求。该脚本会切换 / 清理 `gateway:hybrid-scoring-result` cache namespace，只允许在测试 Redis 或维护窗口内运行，不能直接打正在承载生产网关的 Redis。

provider 只读 repository 的远端 PostgreSQL 验证使用同一条回归命令，但运行前需要临时设置：

```powershell
$env:JUHE_PROVIDER_REPOSITORY_POSTGRES_URL = "postgresql://..."
$env:JUHE_PROVIDER_REPOSITORY_REDIS_CACHE_URL = "redis://..."
$env:JUHE_PROVIDER_REPOSITORY_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:provider-repository-driver
Remove-Item Env:\JUHE_PROVIDER_REPOSITORY_POSTGRES_URL, Env:\JUHE_PROVIDER_REPOSITORY_REDIS_CACHE_URL, Env:\JUHE_PROVIDER_REPOSITORY_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令会先跑本地 SQLite，再在子进程里用 `JUHE_AI_RUNTIME_MODE=performance`、`JUHE_AI_DATABASE_DRIVER=postgres`、`JUHE_AI_CACHE_DRIVER=redis`、`JUHE_AI_RUNTIME_STATE_DRIVER=redis` 跑 PostgreSQL 分支。连接串不要写入仓库文档或提交记录。

登录 / 会话最小 repository 的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_SYSTEM_ACCOUNT_SESSION_POSTGRES_URL = "postgresql://..."
$env:JUHE_SYSTEM_ACCOUNT_SESSION_REDIS_CACHE_URL = "redis://..."
$env:JUHE_SYSTEM_ACCOUNT_SESSION_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:system-account-session-driver
Remove-Item Env:\JUHE_SYSTEM_ACCOUNT_SESSION_POSTGRES_URL, Env:\JUHE_SYSTEM_ACCOUNT_SESSION_REDIS_CACHE_URL, Env:\JUHE_SYSTEM_ACCOUNT_SESSION_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖默认管理员凭据校验、session 创建 / 查询 / touch / 撤销和最近登录时间更新，只代表登录 / 会话最小仓储已具备双 driver。

系统账户管理读写的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_SYSTEM_ACCOUNT_MANAGEMENT_POSTGRES_URL = "postgresql://..."
$env:JUHE_SYSTEM_ACCOUNT_MANAGEMENT_REDIS_CACHE_URL = "redis://..."
$env:JUHE_SYSTEM_ACCOUNT_MANAGEMENT_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:system-account-management-driver
Remove-Item Env:\JUHE_SYSTEM_ACCOUNT_MANAGEMENT_POSTGRES_URL, Env:\JUHE_SYSTEM_ACCOUNT_MANAGEMENT_REDIS_CACHE_URL, Env:\JUHE_SYSTEM_ACCOUNT_MANAGEMENT_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖系统账户分页、选项、创建、默认分组补齐、更新、禁用和唯一性校验；远端 PostgreSQL 分支会清理测试创建的临时系统账户和默认分组。

分组管理读写的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_GROUP_MANAGEMENT_POSTGRES_URL = "postgresql://..."
$env:JUHE_GROUP_MANAGEMENT_REDIS_CACHE_URL = "redis://..."
$env:JUHE_GROUP_MANAGEMENT_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:group-management-driver
Remove-Item Env:\JUHE_GROUP_MANAGEMENT_POSTGRES_URL, Env:\JUHE_GROUP_MANAGEMENT_REDIS_CACHE_URL, Env:\JUHE_GROUP_MANAGEMENT_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖分组分页、选项、账户组选项、创建、高并发调度策略、更新、删除和同供应商名称唯一性校验；PG 模式下分组列表会读取真实绑定账户 ID，并通过 `lookup:group-account-ids` Redis shared cache 复用不含凭据的分组账号 ID 列表；授权分组摘要会通过 `lookup:resource-authorization-sources` Redis shared cache 装配授权来源，但用量 / 状态聚合仍等待账号、usage 和 stats repository 迁移。

API Key 管理读写与网关 Key 校验入口的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_API_KEY_MANAGEMENT_POSTGRES_URL = "postgresql://..."
$env:JUHE_API_KEY_MANAGEMENT_REDIS_CACHE_URL = "redis://..."
$env:JUHE_API_KEY_MANAGEMENT_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:api-key-management-driver
Remove-Item Env:\JUHE_API_KEY_MANAGEMENT_POSTGRES_URL, Env:\JUHE_API_KEY_MANAGEMENT_REDIS_CACHE_URL, Env:\JUHE_API_KEY_MANAGEMENT_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖 API Key 分页、secret 查询、创建、路由策略绑定、更新、刷新密钥、删除、同账户名称唯一性校验、网关 API Key 校验和 DB service `read_gateway_runtime` 的 Key / 路由策略 / 最小可调度账号候选读取入口；回归会通过 `createAccountAsync` 创建带支持模型和模型映射的候选账号并绑定到策略路由分组，再断言网关运行态读回。PG 模式下 API Key 摘要会读取真实绑定路由策略，网关运行态会按路由策略分组读取可调度账号、账号支持模型、模型映射、API Key 运行态、代理和响应检查默认策略。混合路由模型目录 HTTP 校验由 `test:performance-system-api-smoke` 覆盖；usage 聚合和删除后的 dataset / stats 清理目标登记仍等待后续仓储收敛。

授权个人归还的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_AUTHORIZATION_RETURN_POSTGRES_URL = "postgresql://..."
$env:JUHE_AUTHORIZATION_RETURN_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AUTHORIZATION_RETURN_REDIS_STATE_URL = "redis://..."
$env:JUHE_AUTHORIZATION_RETURN_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:authorization-return-driver
Remove-Item Env:\JUHE_AUTHORIZATION_RETURN_POSTGRES_URL, Env:\JUHE_AUTHORIZATION_RETURN_REDIS_CACHE_URL, Env:\JUHE_AUTHORIZATION_RETURN_REDIS_STATE_URL, Env:\JUHE_AUTHORIZATION_RETURN_REDIS_QUEUE_URL -ErrorAction SilentlyContinue
```

该命令覆盖个人直授权账户实例归还、授权列表个人归还和分组个人归还：授权业务记录标记为 `returned`、manual source 撤销、runtime authorization 刷新为 returned，且重复归还返回空结果。授权列表个人归还固定路径 ID 为 `resource_authorization_grants.id`，用于防止前端授权列表按钮传 grant ID 时被仓储误按 runtime authorization ID 查找。该命令只代表个人归还仓储已具备双 driver；授权实例异常状态恢复、授权列表 / options、授权 usage 详情、授权创建、现有授权更新 / 有效期和额度更新 / 回收的 PG HTTP 路径由 `test:performance-system-api-smoke` 覆盖；其中授权创建 smoke 覆盖个人账号授权，团队授权创建仍建议补独立场景回归。

公共设置和系统 API 限流设置读取的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_SETTINGS_PUBLIC_POSTGRES_URL = "postgresql://..."
$env:JUHE_SETTINGS_PUBLIC_REDIS_CACHE_URL = "redis://..."
$env:JUHE_SETTINGS_PUBLIC_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:settings-public-driver
Remove-Item Env:\JUHE_SETTINGS_PUBLIC_POSTGRES_URL, Env:\JUHE_SETTINGS_PUBLIC_REDIS_CACHE_URL, Env:\JUHE_SETTINGS_PUBLIC_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖公共全局设置、全局设置列表和系统 API 限流设置读取；系统设置管理端写入、批量维护和审计链路仍需单独迁移验证。

系统设置管理读写的远端 PostgreSQL 验证同理：

```powershell
$env:JUHE_SETTINGS_MANAGEMENT_POSTGRES_URL = "postgresql://..."
$env:JUHE_SETTINGS_MANAGEMENT_REDIS_CACHE_URL = "redis://..."
$env:JUHE_SETTINGS_MANAGEMENT_REDIS_STATE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:settings-management-driver
Remove-Item Env:\JUHE_SETTINGS_MANAGEMENT_POSTGRES_URL, Env:\JUHE_SETTINGS_MANAGEMENT_REDIS_CACHE_URL, Env:\JUHE_SETTINGS_MANAGEMENT_REDIS_STATE_URL -ErrorAction SilentlyContinue
```

该命令覆盖全局设置和系统设置的局部更新、恢复、缓存失效、非法字段拒绝，以及 PostgreSQL 模式下禁止在线修改统计时区；async 设置读取已接入 Redis `SharedJsonCache`，更新后会绕过旧共享缓存从数据库重读。该命令与 `pnpm --filter juhe-ai-backend test:performance-system-api-smoke` 共同作为 W5 Go `GET/PATCH /__aisys__/api/settings/global` 和 `GET/PATCH /__aisys__/api/settings` 的 Node 对照。两组 Go 路径已经进入 opt-in，但 Node driver 通过不代表 Go 真实依赖 smoke、前端真实后端联调或生产接管通过。

账号测试任务、账户内 API Key runtime state、审计 HTTP 完成时间、模型检测记录、响应检查策略管理端 CRUD、代理管理 CRUD、账户标签管理端 CRUD、账户导出、账户流量迁移、公告管理端 / 公开公告、AI 性能监控、账号用量统计、外部来源系统 source/token 管理与公开鉴权、外部来源公开资源维护的远端 PostgreSQL smoke 使用完整 performance runtime 配置：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
$env:JUHE_AI_ALLOW_ACCOUNT_MODEL_MAPPING_POSTGRES_SMOKE = "1"
pnpm --filter juhe-ai-backend test:account-test-task-postgres-smoke
pnpm --filter juhe-ai-backend test:account-api-key-runtime-state-postgres-smoke
pnpm --filter juhe-ai-backend test:audit-log-postgres-smoke
pnpm --filter juhe-ai-backend test:account-model-mapping-postgres-smoke
pnpm --filter juhe-ai-backend test:model-checks-postgres-smoke
pnpm --filter juhe-ai-backend test:response-inspection-policy-postgres-smoke
pnpm --filter juhe-ai-backend test:proxy-postgres-smoke
pnpm --filter juhe-ai-backend test:account-tags-postgres-smoke
pnpm --filter juhe-ai-backend test:account-export-postgres-smoke
pnpm --filter juhe-ai-backend test:account-traffic-migration-postgres-smoke
pnpm --filter juhe-ai-backend test:announcement-postgres-smoke
pnpm --filter juhe-ai-backend test:ai-performance-postgres-smoke
pnpm --filter juhe-ai-backend test:account-usage-postgres-smoke
pnpm --filter juhe-ai-backend test:external-integration-source-postgres-smoke
pnpm --filter juhe-ai-backend test:external-public-account-push-postgres-smoke
pnpm --filter juhe-ai-backend test:system-team-postgres-smoke
Remove-Item Env:\JUHE_AI_RUNTIME_MODE, Env:\JUHE_AI_DATABASE_DRIVER, Env:\JUHE_AI_CACHE_DRIVER, Env:\JUHE_AI_RUNTIME_STATE_DRIVER, Env:\JUHE_AI_QUEUE_DRIVER, Env:\JUHE_AI_POSTGRES_URL, Env:\JUHE_AI_REDIS_CACHE_URL, Env:\JUHE_AI_REDIS_STATE_URL, Env:\JUHE_AI_REDIS_QUEUE_URL, Env:\JUHE_AI_ALLOW_ACCOUNT_MODEL_MAPPING_POSTGRES_SMOKE -ErrorAction SilentlyContinue
```

`test:account-test-task-postgres-smoke` 只验证账号测试 session / task 状态表 PG async 读写、清理，以及 running 任务被取消后 complete/fail 不能覆盖为 success/failed；`test:account-api-key-runtime-state-postgres-smoke` 验证多 API Key 账号 runtime state failure / success PG 写回、due-for-probe 候选读取、分组账号统计 dirty 标记和 `idx_account_api_key_runtime_probe` explain，不执行真实上游探针，也不把探测执行队列迁 Redis Streams；`test:audit-log-postgres-smoke` 最小写入并读回 `http_completed_at/http_duration_ms`，验证审计固化耗时与客户端完成耗时独立后清理测试行；`test:model-checks-postgres-smoke` 只验证模型检测 run/items PG async 读写、列表 / 详情和系统账户作用域，不执行真实上游模型探针；`test:response-inspection-policy-postgres-smoke` 验证响应检查策略管理端 list/create/update/delete、provider scoped async 校验、网关 active 查询和 PG explain 索引门禁；`test:proxy-postgres-smoke` 验证代理管理 list/options/create/update/delete、代理 URL 解密、检测状态写回和启用检测候选读取，不执行真实外网代理连通探测；`test:account-tags-postgres-smoke` 验证账户标签创建、列表计数、独立替换、删除约束和清理；`test:account-export-postgres-smoke` 验证账户导出按 ID / 按筛选读取 PG 账号摘要、密钥凭据、标签、支持模型、代理引用和代理密码；`test:account-traffic-migration-postgres-smoke` 验证同组账号手动流量迁移、源账号状态 / 冷却写入、目标账号保持可调度和 PG explain `explainIndexed=true`；`test:announcement-postgres-smoke` 验证公告草稿创建、管理端列表 / 详情、更新、发布、公开列表、已读去重、归档后重发清理已读、下线隐藏和删除清理；`test:ai-performance-postgres-smoke` 验证 AI 性能监控账号选项和概览读取 TopN、hourly、summary 预聚合窗口；`test:account-usage-postgres-smoke` 验证账号用量统计读取范围窗口、daily、TopN、账号名前缀搜索和显式选中回填；`test:external-integration-source-postgres-smoke` 验证外部来源系统 source/token 管理、token secret 解密、Bearer token 鉴权、scope 不足拒绝、source 禁用同步 token 状态和删除清理；`test:external-public-account-push-postgres-smoke` 验证外部来源公开账号、分组、路由策略和 API Key 的新增 / 列表 / 修改 / 删除，并清理临时目标用户，不做吞吐压测；`test:system-team-postgres-smoke` 验证系统团队 CRUD、成员作用域列表、团队授权来源展开 / 撤销、停用 / 启用团队恢复来源和 PG explain `explainIndexed=true`。以上脚本都依赖 Redis cache 配置满足高性能 runtime 校验，连接串不要写入仓库文档或提交记录。

`test:account-model-mapping-postgres-smoke` 验证上游接口能力变更触发的 PG 异步账户更新回滚、异步导入预览和异步草稿测试校验，固定启用映射拒绝、停用映射保留语义。

在 Windows PowerShell 中，如果外层脚本依赖 `try/finally` 删除临时 PostgreSQL 数据库或 Redis namespace，必须显式调用 `pnpm.cmd` 或另起受控子进程，并在结束后独立复查临时资源。不要在同一 PowerShell 进程中直接调用可能执行 `exit` 的 `pnpm.ps1`，否则包装器退出可能跳过外层 `finally`。Redis namespace 清理使用 node-redis 5 时，`unlink` 应传入字符串数组，不能把数组展开为多个参数；游标扫描期间直接删除可能漏项，随机且有界的 smoke namespace 可在确认模式足够窄后列出匹配键并一次删除。

项目代码库不提供 SQLite -> PostgreSQL 数据转换脚本、历史 PostgreSQL 结构转换脚本、启动期自动转换或双读双写回归。高性能模式验证只覆盖当前 schema 初始化和当前 repository / cache / queue 路径；历史数据只允许在上线窗口于代码库外离线重建到当前 schema。

Redis 三角色、故障边界与排空门禁使用：

```powershell
pnpm --filter juhe-ai-backend test:runtime-config-env-override
pnpm --filter juhe-ai-backend test:redis-role-config
pnpm --filter juhe-ai-backend test:redis-role-live-smoke
pnpm --filter juhe-ai-backend test:redis-queue-fence
pnpm --filter juhe-ai-backend test:redis-enqueue-retry
pnpm --filter juhe-ai-backend test:redis-runtime-failure-boundary
pnpm --filter juhe-ai-backend test:redis-stream-drain
pnpm --filter juhe-ai-backend test:redis-stream-drain-cli
pnpm test:redis-role-operations
```

其中 macOS 脚本在 Windows 只完成静态和可用 shell 的语法检查；system LaunchDaemon、端口/PID、state 冷启动、queue AOF 与 rewrite 必须在真实 macOS 隔离端口演练，不能用 Windows 或 Docker 结果替代。
`test:redis-role-live-smoke` 会写入并清理当前 namespace，必须使用隔离三实例、随机 namespace，并显式设置 `JUHE_AI_ALLOW_REDIS_ROLE_LIVE_SMOKE=1`；脚本验证真实 CONFIG、fence、Stream 消费删除和 state 无 XADD 增量。

当前 PostgreSQL schema 初始化与 seed 的固定门禁使用：

```powershell
pnpm --filter juhe-ai-backend test:postgres-schema-sql
pnpm --filter juhe-ai-backend test:postgres-seed-defaults
```

OpenAI OAuth 管理端创建 / 刷新 / 重新授权涉及真实外部 OpenAI OAuth code / refresh token，默认不放入远端 PG smoke；改动该路由时至少运行 `typecheck`、`test:openai-oauth-protocol-contract`、`test:openai-oauth-token-response-boundary`、`test:openai-oauth-credentials-patch`、`test:openai-oauth-refresh-activation` 和 `test:openai-oauth-access-token-refresh`。其中 protocol contract 回归不访问外网，固定 authorize 参数、authorization code 表单编码、refresh JSON 编码和 token 请求头；token response boundary 回归同时固定 OAuth 管理端不重新导入同步账号 / 分组 / 供应商 / 代理 / 操作日志入口。

Codex 兼容头或 GPT-5.6 Responses Lite 调整使用 `pnpm --filter juhe-ai-backend test:codex-latest-compatibility`；该回归同时固定当前 UA、HTTP 头、会话头，保证 Lite header 与 `reasoning.context=all_turns`、`parallel_tool_calls=false` 同步，并保证 `/models` 不按 `client_version` 过滤模型目录。软件账户测试请求还必须执行 `pnpm --filter juhe-ai-backend test:account-test-request`，确认 Lite 测试 payload 与真实客户端契约一致。

performance 最小 System API HTTP 链路验证使用：

```powershell
$env:JUHE_PERFORMANCE_SYSTEM_API_POSTGRES_URL = "postgresql://..."
$env:JUHE_PERFORMANCE_SYSTEM_API_REDIS_CACHE_URL = "redis://..."
$env:JUHE_PERFORMANCE_SYSTEM_API_REDIS_STATE_URL = "redis://..."
$env:JUHE_PERFORMANCE_SYSTEM_API_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:performance-system-api-smoke
Remove-Item Env:\JUHE_PERFORMANCE_SYSTEM_API_POSTGRES_URL, Env:\JUHE_PERFORMANCE_SYSTEM_API_REDIS_CACHE_URL, Env:\JUHE_PERFORMANCE_SYSTEM_API_REDIS_STATE_URL, Env:\JUHE_PERFORMANCE_SYSTEM_API_REDIS_QUEUE_URL -ErrorAction SilentlyContinue
```

该命令覆盖 public settings、captcha、login、me、settings GET / PATCH、system-accounts GET / POST / PATCH、providers、provider options、provider models GET / POST / PATCH / DELETE、groups GET / POST / PATCH / DELETE、authorization-options GET、authorizations GET、my-authorizations GET、authorizations `POST` / `GET /:id/usage` / `PATCH /:id` / `PATCH /:id/expire` / `DELETE /:id`、accounts POST、accounts GET 列表 / 详情 / options、accounts PATCH、accounts DELETE、授权实例 `/authorized-dispatch` 异常恢复、api-keys GET / POST / PATCH / secret / refresh-key / DELETE 和 logout，只代表登录前后、设置管理、系统账户管理、分组管理、自定义模型管理、授权列表 / options / usage 详情、授权创建、现有授权更新 / 有效期和额度更新 / 回收、AI 账户创建 / 普通 owner 列表 / 详情 / options / 常规更新 / 异常状态恢复 / 删除、授权实例详情读取 / 异常状态恢复和 API Key 管理的最小 HTTP 链路可用，不代表完整管理端授权实例列表视图和统计已 PG-ready。授权读资源 lookup Redis shared cache 补充后需要额外结合 `test:authorization-list-team-usage-batch`、`test:authorization-options-query-guard`、`test:authorization-usage-pagination` 和 `test:resource-authorization-single-read` 固定源码门禁与分页边界；授权个人归还不在该 HTTP smoke 覆盖范围内，需额外跑 `test:authorization-return-driver` 验证仓储级 PG 事务路径。该 smoke 现在作为 W5 品牌设置和系统运行设置的 Node 对照证据；Go 两组 settings 路径虽已进入 opt-in，仍不能据此宣称真实依赖或生产接管通过。

注意：`test:performance-system-api-smoke` 的远端 PG/Redis 分支应使用 `JUHE_PERFORMANCE_SYSTEM_API_POSTGRES_URL`、`JUHE_PERFORMANCE_SYSTEM_API_REDIS_CACHE_URL`、`JUHE_PERFORMANCE_SYSTEM_API_REDIS_STATE_URL` 和 `JUHE_PERFORMANCE_SYSTEM_API_REDIS_QUEUE_URL` 传参，不要在父 PowerShell 会话里额外设置 `JUHE_AI_QUEUE_DRIVER=redis_stream` 等全局 performance 变量。该 smoke 会自行 fork PostgreSQL 子进程；父进程强行切到 performance 队列模式会让 standalone 分支误用 Redis Stream 配置，导致不必要的失败。

> 历史记录：页面数据 v2 confirm 已于 2026-07-22 退役；下列命令和说明不得执行或作为当前验证依据，保留仅用于历史排查。

页面数据 v2 confirm 的历史 PostgreSQL / Redis HTTP smoke 曾使用专用变量和显式写入许可：

```powershell
$env:JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_ALLOW = "1"
$env:JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_REDIS_QUEUE_URL = "redis://..."
pnpm --dir backend run smoke:page-data-confirm-http
Remove-Item Env:\JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_ALLOW, Env:\JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_POSTGRES_URL, Env:\JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_REDIS_CACHE_URL, Env:\JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_REDIS_STATE_URL, Env:\JUHE_AI_PAGE_DATA_CONFIRM_SMOKE_REDIS_QUEUE_URL -ErrorAction SilentlyContinue
```

该命令不进入普通回归聚合，并设置 `JUHE_AI_DISABLE_BASE_ENV=true`，连接信息只从上述专用环境变量注入。它要求 cache/state/queue 使用三个不同的 Redis `host:port`，为本次运行生成唯一 namespace，直接在当前 PostgreSQL schema 中创建两个唯一系统账户和 session fixture；账户布尔字段依赖 schema 默认值，兼容历史 integer 与当前 PostgreSQL boolean 定义。脚本随后启动当前 Node System API。基线请求固定以一次 v2 `POST /data-changes/confirm` 确认 `/my-accounts` manifest 的 `accounts.static`、`accounts.options`、`providers.catalog` 三域；其余请求验证未登录 401、普通用户 admin scope 403、账户 scope 隔离、epoch reload、全 scope reset、session `last_seen_at` no-touch。进入限流验证前会精确删除本次随机 namespace 下测试 IP 和 fixture 账户的 read/write bucket，确保从零计数实际触发 HTTP 429，再核对 Redis read bucket 达到阈值且不存在 write bucket。测试库关闭全部 read 限流、测试 IP 位于白名单、最小 read 阈值超过 255，或当前 schema 缺表时都会明确失败，不会伪造通过。脚本不初始化或迁移 PostgreSQL schema；`finally` 先停止 HTTP 接收并有界关闭 runtime Redis，server 超时后强制断开连接，再按本次 fixture ID 删除 PG 行；cache/state/queue Redis 的 SCAN、DEL、QUIT 均有 deadline 且逐角色独立尝试，单个角色失败不会跳过其余清理。所有清理错误统一汇总；输出只包含布尔结果、计数和耗时，不包含 URL、Cookie、token 或密码。

performance System API 压测使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
$env:JUHE_AI_DB_POOL_MAX = "150"
$env:JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT = "256"
$env:JUHE_PERFORMANCE_LOAD_DURATION_MS = "30000"
$env:JUHE_PERFORMANCE_LOAD_CONCURRENCY = "100"
$env:JUHE_PERFORMANCE_LOAD_ACCOUNT_COUNT = "25"
$env:JUHE_PERFORMANCE_LOAD_WRITE_RATIO = "0.15"
$env:JUHE_PERFORMANCE_LOAD_RESET_PG_STAT_STATEMENTS = "true"
pnpm --filter juhe-ai-backend test:performance-system-api-load
```

`JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT=256` 是 performance 压测值；standalone 普通节点默认仍为 `64`，不要把高性能压测参数长期写入通用 `backend/.env`。

该命令会创建临时压测分组、多个 AI 账号和 API Key，混合管理端读请求与账号 PATCH 写请求，并采样 PostgreSQL deadlocks、lock waiters、not granted locks、idle in transaction、最长事务和最长 active query。2026-06-26 远端测试栈的 100 并发结果见 `../reports/高性能模式压测报告-2026-06-26.md`。脚本结束后应复查 `压测分组%`、`压测AI账户%` 和 `压测APIKey%` 计数为 0。

performance 网关事实表 readiness 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness
```

该命令不是吞吐压测，而是完整网关压测前置门禁：直接验证 usage、audit、operation log、public API log、usage stats 及审计错误分组 / 审计读路径 / 操作日志读路径 / 公开接口日志读路径 / 运行日志读路径在 PostgreSQL 模式下是否可写、可聚合或可读取。远端复测必须使用当前 schema 初始化出的测试库，或由运维在代码库外离线重建为当前 schema 后再运行；`postgres:init-schema-only` 不是历史库维护入口。2026-06-27 当前 schema 复测全绿：`usage_records_write` 31.00ms、`audit_logs_write` 64.38ms、`operation_logs_write` 58.29ms、`public_api_logs_write` 13.04ms、`usage_stats_aggregate` 42.47ms。2026-06-27 补齐 usage model / error / latency / account quality 派生统计 PG 聚合后再次复测全绿：`usage_records_write` 36.84ms、`audit_logs_write` 40.40ms、`operation_logs_write` 61.41ms、`public_api_logs_write` 11.67ms、`usage_stats_aggregate` 173.05ms。补齐 PG quota snapshot 后复测全绿：`usage_records_write` 43.37ms、`audit_logs_write` 52.88ms、`operation_logs_write` 62.43ms、`public_api_logs_write` 12.81ms、`usage_stats_aggregate` 170.89ms、`gateway_quota_snapshot_pg` 143.82ms。扩展直接账户授权和直接分组授权 quota snapshot 后复测仍全绿：`usage_records_write` 51.51ms、`audit_logs_write` 56.21ms、`operation_logs_write` 56.26ms、`public_api_logs_write` 13.30ms、`usage_stats_aggregate` 133.42ms、`gateway_quota_snapshot_pg` 96.24ms。扩展团队账户授权和团队分组授权 quota snapshot 后复测仍全绿：`usage_records_write` 51.10ms、`audit_logs_write` 50.15ms、`operation_logs_write` 61.07ms、`public_api_logs_write` 9.70ms、`usage_stats_aggregate` 160.78ms、`gateway_quota_snapshot_pg` 68.35ms。扩展 PG 审计错误分组后复测仍全绿：`usage_records_write` 61.39ms、`audit_logs_write` 81.52ms、`operation_logs_write` 38.24ms、`public_api_logs_write` 9.87ms、`usage_stats_aggregate` 150.37ms、`gateway_quota_snapshot_pg` 94.36ms。扩展 PG 审计读路径后复测仍全绿：`usage_records_write` 55.20ms、`audit_logs_write` 150.46ms、`operation_logs_write` 61.09ms、`public_api_logs_write` 11.57ms、`usage_stats_aggregate` 166.59ms、`gateway_quota_snapshot_pg` 96.97ms。扩展 PG 操作日志读路径后复测仍全绿：`usage_records_write` 46.71ms、`audit_logs_write` 138.89ms、`operation_logs_write` 199.23ms、`public_api_logs_write` 12.52ms、`usage_stats_aggregate` 166.50ms、`gateway_quota_snapshot_pg` 106.93ms。扩展 PG 运行日志读路径后复测仍全绿：`usage_records_write` 57.31ms、`audit_logs_write` 162.36ms、`operation_logs_write` 182.93ms、`public_api_logs_write` 10.64ms、`runtime_logs_read` 17.27ms、`usage_stats_aggregate` 147.80ms、`gateway_quota_snapshot_pg` 92.67ms。扩展 PG 公开接口日志读路径后复测仍全绿：`usage_records_write` 65.18ms、`audit_logs_write` 149.05ms、`operation_logs_write` 187.80ms、`public_api_logs_write` 17.95ms、`runtime_logs_read` 13.15ms、`usage_stats_aggregate` 119.90ms、`gateway_quota_snapshot_pg` 77.73ms，报告为 `reports/performance-gateway-persistence-readiness-20260626T224149Z.json`。补充 API Key 校验 / 系统设置 Redis shared cache 后复测仍全绿：`usage_records_write` 58.41ms、`audit_logs_write` 140.46ms、`operation_logs_write` 191.00ms、`public_api_logs_write` 26.61ms、`runtime_logs_read` 14.87ms、`usage_stats_aggregate` 142.41ms、`gateway_quota_snapshot_pg` 97.05ms，报告为 `reports/performance-gateway-persistence-readiness-20260626T225814Z.json`。补充网关模型目录 / 响应检查策略 Redis shared cache 后复测仍全绿：`usage_records_write` 53.71ms、`audit_logs_write` 152.89ms、`operation_logs_write` 185.36ms、`public_api_logs_write` 25.43ms、`runtime_logs_read` 20.80ms、`usage_stats_aggregate` 176.00ms、`gateway_quota_snapshot_pg` 100.21ms，报告为 `reports/performance-gateway-persistence-readiness-20260626T231415Z.json`。补充审计 Redis Streams 与 DB service PG 热读并发后复测仍全绿：`usage_records_write` 62.81ms、`audit_logs_write` 251.93ms、`operation_logs_write` 295.97ms、`public_api_logs_write` 29.28ms、`runtime_logs_read` 19.50ms、`usage_stats_aggregate` 247.80ms、`gateway_quota_snapshot_pg` 97.30ms，报告为 `reports/performance-gateway-persistence-readiness-20260627T000410Z.json`。补充网关设置 Redis shared cache 后复测仍全绿：`usage_records_write` 64.39ms、`audit_logs_write` 153.11ms、`operation_logs_write` 198.27ms、`public_api_logs_write` 17.65ms、`runtime_logs_read` 26.33ms、`usage_stats_aggregate` 120.28ms、`gateway_quota_snapshot_pg` 69.67ms，报告为 `reports/performance-gateway-persistence-readiness-20260627T210033Z.json`。

performance 使用记录列表 / 详情 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:usage-record-list-postgres-smoke
```

该命令不是吞吐压测，只验证使用记录管理页的 PG 读路径和查询计划：写入 5 条临时 usage record，确认 `juhe_usage.usage_records` 主表和 `usage_record_shard_entries` catalog 都写入，再通过 `listUsageRecordsAsync` 验证 trace 前缀、model+failed+duration 排序、client IP 前缀筛选，通过 `getUsageRecordDetailAsync` 验证详情读取，并覆盖音频 token、输出图片张数、异步自定义模型补价和列表 / 详情成本拆解。脚本还会对 trace 前缀、model 筛选、client IP 前缀和 duration 排序执行 `EXPLAIN (COSTS OFF)`，在 `enable_seqscan=off` 下断言不出现 Seq Scan 且命中 catalog 索引。2026-07-03 远端 PG/Redis 复测通过：`records=5`、`entries=5`、`traceItems=5`、`failedItems=1`、`clientIpItems=2`、`explainIndexed=true`；finally 删除 smoke 主表、catalog 数据和临时自定义模型。

performance 客户端 IP 统计 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
$env:JUHE_AI_ALLOW_CLIENT_IP_STATS_POSTGRES_SMOKE = "1" # 仅确认该 PG 是测试实例时设置
pnpm --filter juhe-ai-backend test:client-ip-stats-postgres-smoke
```

该命令不是吞吐压测，只验证客户端 IP 统计和策略的 PG 闭环：写入 `juhe_usage.usage_records`、执行 `aggregateClientIpStatsBatchAsync`、刷新 `client_ip_usage_range_windows` / `client_ip_account_usage_range_windows`、读取 IP 列表和详情，并覆盖封禁策略创建、active 列表、hash 精确读取、命中计数写入和解封。脚本还会在真实 PG 上对客户端 IP 搜索前缀、列表 `request_count` 排序、详情账号 `request_count` 排序和 active 封禁策略筛选执行 `EXPLAIN (COSTS OFF)`，在 `enable_seqscan=off` 下断言不出现 Seq Scan 且命中对应索引。脚本会保存并恢复 `client_ip_stats_aggregation` / `client_ip_range_window_refresh` job state，finally 删除 smoke usage、daily、window、dirty、registry、policy 和 policy hit 数据；非本机 PG 必须显式设置 `JUHE_AI_ALLOW_CLIENT_IP_STATS_POSTGRES_SMOKE=1`，且只允许指向测试库。2026-06-28 远端 PG/Redis 复测通过：消费 3 条使用记录，列表返回 2 个 IP 汇总，详情返回 1 个账号窗口，policy hit 写入 3 次，`explainIndexed=true`。客户端 IP 策略单 IP shared cache 另由 `test:client-ip-policy-shared-cache-redis` 验证，Redis cache driver 下的策略读取边界由 `test:performance-redis-boundary` 和 `test:gateway-cache-invalidation-index` 固定。

performance 用量概览窗口 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:usage-overview-postgres-smoke
```

该命令不是吞吐压测，只验证统计首页用量概览窗口的 PG 闭环：写入最小 `juhe_stats.usage_stats_*` 源数据、执行 `refreshUsageRankSnapshotsInStages({ stageNames: ['usage_overview_windows'] })`、读取 `getUsageStatsOverviewAsync`，验证 source watermark 不变时第二轮刷新会跳过，并验证只更新今日源表时不会重写昨日 `usage_overview_summary_windows` / `usage_overview_trend_windows` / `usage_model_rank_windows` / `usage_error_rank_windows`。SQLite 侧同一增量边界由 `pnpm --filter juhe-ai-backend test:usage-overview-window-incremental` 固定。

performance 热用量窗口 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:usage-hot-window-postgres-smoke
```

该命令不是吞吐压测，只验证 stats-worker 在线聚合后使用的热窗口刷新入口：写入最小 `juhe_stats.usage_stats_*` 源数据和一条非今日范围窗口，执行 `refreshHotUsageWindowSnapshots`，验证只发布 `end_date = today` 的概览窗口和 `usage_scope_range_windows`、保留非今日窗口，并验证 source watermark 不变时第二轮跳过。

performance 用量范围窗口 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:usage-scope-range-postgres-smoke
```

该命令不是吞吐压测，只验证账号 / 分组 / API Key 等范围用量窗口的 PG 闭环：写入最小 `juhe_stats.usage_stats_daily` 源数据、执行 `refreshUsageRankSnapshotsInStages({ stageNames: ['usage_scope_range_windows'] })`、读取 `loadUsageRangeSummaryForScopeAsync`，验证 source watermark 不变时第二轮刷新会跳过，并验证只更新今日源表时不会重写昨日 `usage_scope_range_windows`。SQLite 侧同一边界由 `pnpm --filter juhe-ai-backend test:usage-rank-snapshot-skip` 固定。

performance 授权范围窗口 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:authorization-usage-range-postgres-smoke
```

该命令不是吞吐压测，只验证授权团队 / 用户范围用量窗口的 PG 闭环：写入最小 `juhe_business` lookup 数据和 `juhe_stats.authorization_*_usage_summary_daily` 源数据、执行 `refreshUsageRankSnapshotsInStages({ stageNames: ['authorization_usage_range_windows'] })`、读取 `getAuthorizationTeamUsageOverviewAsync` / `getAuthorizationUserUsageOverviewAsync`，并验证 source watermark 不变时第二轮刷新会跳过。2026-06-28 远端 PG/Redis 复测通过：`teamRequestCount=13`、`userRequestCount=17`、团队 / 用户明细各 1 行、`skipped=true`。

performance TopN 排行快照 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:usage-rank-snapshots-postgres-smoke
```

该命令不是吞吐压测，只验证账号 / 调用方 / API Key / 授权 TopN 排行快照的 PG 闭环：写入最小 `juhe_stats.usage_stats_daily` / `usage_stats_monthly` 源数据、执行 `refreshUsageRankSnapshotsInStages` 的 5 个 TopN stage、校验 `usage_rank_snapshots` 中 rank=1 与 metric value，并验证 source watermark 不变时第二轮刷新会跳过。2026-06-28 远端 PG/Redis 复测通过：5 个 stage 执行，`accountTop` 和 `callerTop` 正确，`skipped=true`。

performance 系统指标趋势 PG smoke 使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
pnpm --filter juhe-ai-backend test:system-metrics-postgres-smoke
```

该命令不是吞吐压测，只验证系统指标趋势窗口的 PG 闭环：在最近 31 天内寻找空闲小时桶，通过 `insertSystemMetricsSampleAsync` / `insertProcessEventLoopSampleAsync` 写入最小系统和进程采样并生成 hourly 源数据、执行 `refreshUsageRankSnapshotsInStages({ stageNames: ['system_metrics_trend_windows'] })`、通过 `getSystemMetricsOverviewAsync` 读取系统趋势和进程事件循环趋势，并验证 source watermark 不变时第二轮刷新会跳过。脚本还会在远端 PG 上对最新系统采样、系统指标趋势窗口、进程事件循环 latest / peak / trend 查询执行 `EXPLAIN (COSTS OFF)`，在 `enable_seqscan=off` 下断言不出现 Seq Scan 且命中对应索引。2026-06-28 远端 PG/Redis 复测通过：测试桶 `2026-05-29T00`，`sampledAt=2026-05-28T16:00:00.000Z`，系统趋势 1 条，进程事件循环趋势 1 条，`explainIndexed=true`，`skipped=true`；finally 删除源数据并重刷窗口恢复。

该 smoke 属于当前 Node 过渡体系验证，只能作为迁移前基线。Go W6 / W7 接管系统指标统计时，必须按 [Go 迁移指标与观测规划](../migration/Go迁移指标与观测规划.md) 和 [Go 系统指标字段迁移清单](../migration/Go系统指标字段迁移清单.md) 改跑 Go runtime、PG、Redis、Asynq、worker lag 和 stats freshness 验收；不得把 `eventLoopLagMs`、`processEventLoop*`、`process_event_loop_*` 或 `db-service` 当作 Go 长期通过标准。

performance 真实网关压测使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."
$env:JUHE_AI_GATEWAY_LOAD_ALLOW_SETTINGS_WRITE = "1"
$env:JUHE_AI_GATEWAY_LOAD_ALLOW_PRIVATE_UPSTREAM = "1"
$env:JUHE_AI_GATEWAY_LOAD_CONCURRENCY = "100"
$env:JUHE_AI_GATEWAY_LOAD_DURATION_SECONDS = "20"
$env:JUHE_AI_GATEWAY_LOAD_WARMUP_SECONDS = "3"
$env:JUHE_AI_GATEWAY_LOAD_SETTLE_SECONDS = "60"
$env:JUHE_AI_GATEWAY_LOAD_ACCOUNT_COUNT = "64"
$env:JUHE_AI_GATEWAY_LOAD_ACCOUNT_CONCURRENCY = "10000"
$env:JUHE_AI_GATEWAY_LOAD_ASSERT_ACCOUNT_CONCURRENCY = "1"
$env:JUHE_AI_GATEWAY_LOAD_REPORT_PATH = "reports/performance-gateway-load-100c-settle60-latest.json"
pnpm --filter juhe-ai-backend test:performance-gateway-load
```

该命令会启动真实后端、DB service、worker、Redis Streams 队列和本地 mock OpenAI 上游，覆盖 `/v1/responses` 与 `/v1/chat/completions`，并采样 PG deadlocks、lock waiters、最长事务、最长 active query、`pg_stat_statements` 慢 SQL、usage / audit / operation / public API / record maintenance / runtime log stream 的 pending 与 consumer group lag、total backlog、usage / audit / operation / public API / stats 落库量。设置 `JUHE_AI_GATEWAY_LOAD_ASSERT_ACCOUNT_CONCURRENCY=1` 时，压测还会采样 Redis state 中的账号并发槽，并把 Redis 账号槽峰值与 mock 上游活跃流做对账；如果出现上游仍有活跃流但 Redis 账号并发长期低于活跃流，或 Redis 槽位超过账号硬并发总容量，报告必须失败。脚本会临时写入压测系统设置，并只在显式配置 `JUHE_AI_GATEWAY_LOAD_ALLOW_SETTINGS_WRITE=1` 时运行；本地 mock 上游豁免也必须通过 `JUHE_AI_GATEWAY_LOAD_ALLOW_PRIVATE_UPSTREAM=1` 显式开启。该豁免只用于本脚本进程，不代表生产账号 Base URL 可以指向 localhost / 内网。`JUHE_AI_GATEWAY_LOAD_CLEANUP=true` 会在报告生成后清理本次夹具和 trace 明细；如果 cleanup 报错，需要按报告的 `tracePrefix` 补清理。

2026-06-28 最新补测：64 并发、30 秒、32 账号、50ms mock upstream、10 秒 settle 通过，14932 请求全成功，成功 QPS 359.73，p95 161.98ms，p99 261.71ms，deadlocks delta 0，最大 lock waiters 0，最大 active query 0.29s，本轮 positive pending / backlog 均为 0，历史 backlog 净减少 800，报告为 `reports/performance-gateway-load-1782666536194-26ffae.json`。历史 100 并发和 128 并发结果见 `../reports/高性能模式压测报告-2026-06-28.md` 和旧版报告。高性能 / PostgreSQL 模式下成功审计策略必须和单机模式使用同一组实际配置，默认完全成功请求最近 1 小时全量热保留，超过热窗口后只保留 10% 稳定采样，问题审计仍全量；压测脚本按当前审计设置计算 `expectedAuditLogsMinimum`。`JUHE_AI_GATEWAY_LOAD_SETTLE_SECONDS` 建议不低于 `60`，短 settle 可能导致业务全成功但审计异步写入或 Redis Stream lag 尚未完全消化。判断队列是否异常必须看 total backlog，即 pending + lag，而不是只看 pending。

performance 可靠性与灾备演练使用：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_POSTGRES_URL = "postgresql://..."
$env:JUHE_AI_REDIS_CACHE_URL = "redis://..."
$env:JUHE_AI_REDIS_STATE_URL = "redis://..."
$env:JUHE_AI_REDIS_QUEUE_URL = "redis://..."

# 本机已安装 pg_dump / pg_restore 时可不设置 remote_docker。
# Windows 开发机没有 PostgreSQL client 时，使用远端 Docker 容器内工具：
$env:JUHE_AI_RELIABILITY_PG_BACKUP_RESTORE_MODE = "remote_docker"
$env:JUHE_AI_RELIABILITY_REMOTE_SSH_TARGET = "user@<测试主机IP>"
$env:JUHE_AI_RELIABILITY_REMOTE_DOCKER_DIR = "<远端Docker目录>"
$env:JUHE_AI_RELIABILITY_POSTGRES_CONTAINER = "juhe-ai-postgres"
$env:JUHE_AI_RELIABILITY_POSTGRES_DB = "juhe_ai"
$env:JUHE_AI_RELIABILITY_POSTGRES_USER = "juhe_ai"

# 以下两个命令会重启测试栈中间件；生产只能在维护窗口执行。
$env:JUHE_AI_RELIABILITY_REDIS_RESTART_COMMAND = 'ssh user@<测试主机IP> "docker restart juhe-ai-redis-queue >/dev/null"'
$env:JUHE_AI_RELIABILITY_POSTGRES_RESTART_COMMAND = 'ssh user@<测试主机IP> "docker restart juhe-ai-postgres >/dev/null && docker restart juhe-ai-pgbouncer >/dev/null"'

pnpm --filter juhe-ai-backend test:performance-reliability-drill
```

该命令覆盖 PostgreSQL 健康和阻塞快照、Redis queue PING、生产 usage stream length / pending 快照、临时 Redis Stream pending 接管、PostgreSQL `pg_dump -> 临时库 pg_restore -> schema 表数量校验 -> 删除临时库`、Redis queue 重启后 pending 消息保留 / 接管 / ACK，以及 PostgreSQL + PgBouncer 重启后的连接恢复。2026-06-27 远端测试栈完整演练通过：备份恢复 14.03s，Redis 重启后 3 条 pending 全部恢复并 ACK，Redis PING 约 1.03s 恢复，PostgreSQL 重启健康检查约 41ms 恢复；报告见 `reports/performance-reliability-drill-20260627-022408.json`。

## 2.1 SQLite 写入压测

涉及业务库、数据集目录库、使用记录目录库、统计结果库、usage shard 或 Responses 桥接状态索引时，除回归测试外需要跑真实 SQLite 写入压测：

```powershell
pnpm test:sqlite-database-pressure
```

该命令使用临时真实 SQLite 文件覆盖：

- business / dataset / usage-catalog / stats 原始事务批量写。
- usage shard direct 写入、可选 usage writer pool 写入和 usage shard catalog 批量写入。
- Responses bridge state direct 写入与 child process writer pool 写入。
- 默认 shard 矩阵为 `8,16,32`，可用 `JUHE_AI_SQLITE_PRESSURE_SHARDS` 调整。

本机小矩阵参考结果：`usage-catalog_raw_sqlite_writes` 1000 行约 `34690 ops/s`；`usage` 8 / 16 shard direct 与 pool 模式均无失败或拒绝；Responses bridge state direct 与 pool 模式均无失败。历史小样本中 usage writer pool 在现有批量模型下没有稳定提升，容易被 IPC 与 catalog 单写者瓶颈抵消，因此 usage writer pool 当前只作为显式压测开关，默认关闭，不能因为有多 shard 就盲目开启。Responses 桥接状态索引更适合继续保留 writer pool 默认路径。所有数字只作为当前机器证据，变更 shard 数、pool size 或批量策略前必须重新跑压测；样本太小时可能被 worker 启动成本、JIT 和临时文件初始化放大影响，只能证明脚本可运行，不能作为容量结论。

快速自检可降低样本量：

```powershell
$env:JUHE_AI_SQLITE_PRESSURE_RAW_ROWS = "1000"
$env:JUHE_AI_SQLITE_PRESSURE_USAGE_ROWS = "1000"
$env:JUHE_AI_SQLITE_PRESSURE_CODEX_ROWS = "500"
$env:JUHE_AI_SQLITE_PRESSURE_BATCH_SIZE = "100"
$env:JUHE_AI_SQLITE_PRESSURE_SHARDS = "8,16,32"
pnpm test:sqlite-database-pressure
Remove-Item Env:\JUHE_AI_SQLITE_PRESSURE_RAW_ROWS, Env:\JUHE_AI_SQLITE_PRESSURE_USAGE_ROWS, Env:\JUHE_AI_SQLITE_PRESSURE_CODEX_ROWS, Env:\JUHE_AI_SQLITE_PRESSURE_BATCH_SIZE, Env:\JUHE_AI_SQLITE_PRESSURE_SHARDS -ErrorAction SilentlyContinue
```

## 2.2 Worker 真实多进程压测

涉及后台 worker 拓扑、队列、统计聚合、SQLite 单写者或 worker 内并发策略时，可以跑真实多进程 worker 压测：

```powershell
pnpm --filter juhe-ai-backend test:worker-real-load
```

该命令会使用临时库启动真实 `server`、DB service、`ingest-worker`、`stats-worker` 和 `ops-worker`，创建 mock 上游、临时账号 / 分组 / API Key，然后通过真实 `/v1/chat/completions`、`/v1/responses` 和流式 Responses 发压测请求。默认压测 `600` 秒，结束后等待 `20` 秒观察消化情况，并输出请求吞吐、延迟分位、usage / audit 落库量、统计 shard cursor lag、各进程事件循环和 SQLite lock / busy 信号。

常用参数：

```powershell
$env:JUHE_AI_WORKER_LOAD_DURATION_SECONDS = "600"
$env:JUHE_AI_WORKER_LOAD_SETTLE_SECONDS = "30"
$env:JUHE_AI_WORKER_LOAD_CONCURRENCY = "10"
$env:JUHE_AI_WORKER_LOAD_SAMPLE_INTERVAL_SECONDS = "30"
pnpm --filter juhe-ai-backend test:worker-real-load
Remove-Item Env:\JUHE_AI_WORKER_LOAD_DURATION_SECONDS, Env:\JUHE_AI_WORKER_LOAD_SETTLE_SECONDS, Env:\JUHE_AI_WORKER_LOAD_CONCURRENCY, Env:\JUHE_AI_WORKER_LOAD_SAMPLE_INTERVAL_SECONDS -ErrorAction SilentlyContinue
```

快速冒烟可把 `JUHE_AI_WORKER_LOAD_DURATION_SECONDS` 调到 `30`。报告默认写入 `backend/tmp/worker-real-load-*.json`；`usageStats*ScopeRequests` 是按 system / account / group / API Key 等统计 scope 展开的聚合请求数，不与 usage record 一一对应，统计消化应优先看 `statsUsageShardStates`、`statsUsageMaxLagSeconds` 和 `statsUsageLastSuccessAt`。

## 3. 后端接口快速检查

项目已启动后，可先用以下免登录接口确认服务和公开配置可访问：

```powershell
curl.exe http://127.0.0.1:3000/__aisys__/health
curl.exe http://127.0.0.1:3000/__aisys__/api/health
curl.exe http://127.0.0.1:3000/__aisys__/api/settings/public
```

账户、分组、API Key、代理和系统设置等管理接口需要登录会话 Cookie。手工检查时可以先在前端登录，再从浏览器请求里复制 `juhe_ai_session=...` 到当前 PowerShell 会话；不想手工处理 Cookie 时，优先使用前端页面或 `pnpm test:smoke` 验证关键链路。

```powershell
$AdminCookie = "juhe_ai_session=..."

curl.exe http://127.0.0.1:3000/__aisys__/api/providers -H "Cookie: $AdminCookie"
curl.exe http://127.0.0.1:3000/__aisys__/api/accounts -H "Cookie: $AdminCookie"
curl.exe http://127.0.0.1:3000/__aisys__/api/groups -H "Cookie: $AdminCookie"
curl.exe http://127.0.0.1:3000/__aisys__/api/api-keys -H "Cookie: $AdminCookie"
curl.exe http://127.0.0.1:3000/__aisys__/api/proxies -H "Cookie: $AdminCookie"
curl.exe http://127.0.0.1:3000/__aisys__/api/settings -H "Cookie: $AdminCookie"
```

## 4. 本地网关验证

客户端接入时使用 OpenAI 兼容协议：

- Base URL：`http://127.0.0.1:3000` 或 `http://127.0.0.1:3000/v1`
- API Key：`API Key 管理` 或 `我的 API Key` 页面生成的本地网关密钥
- 通用入口：`/models`、`/responses`；`/chat/completions` 只在当前 API Key 绑定的路由策略分组能选到 OpenAI API Key / OpenAI-compatible 上游账号时验证。
- OAuth / Codex 边界：GPT OAuth 账号只覆盖 `POST /responses` 和 `POST /responses/compact`（含 `/v1` 前缀），不会把 `/chat/completions` 自动翻译到 Responses。

`$LiteKey` 使用 API Key 页面新建后返回的明文密钥，或同步脚本首次输出的 `sync.apiKey`。

本机 `backend/data/juhe-ai.sqlite3`（或 `JUHE_AI_DATABASE_PATH` 指向的业务库）可能已经保存真实 GPT 账户、本地网关 API Key 和分组绑定；`JUHE_AI_DATASET_DATABASE_PATH` 指向的数据集目录库保存审计、日志索引、操作日志、公开接口日志和模型检测事实；`JUHE_AI_USAGE_CATALOG_DATABASE_PATH` 指向的使用记录目录库保存 usage shard 注册表和筛选目录；`JUHE_AI_USAGE_SHARD_ROOT` 指向的目录保存新写入的使用记录；`JUHE_AI_STATS_DATABASE_PATH` 指向的统计结果库保存统计缓存、额度窗口和监控历史。四个主 SQLite 文件路径必须互不相同，usage shard 根目录也必须与这些文件路径区分。需要真实联通验证或烟测时，可以优先复用本地业务库里的数据，不必重复创建；但不要把真实密钥写入文档、提交记录、Issue、截图或聊天内容，只在本机命令会话里临时赋值。

本地网关 API Key 的列表和更新响应只返回前缀、掩码或空 key，不从管理面列表接口批量取回完整密钥；新建成功响应、页面弹窗和单条复制接口可按资源权限返回完整密钥。需要手工验证时，使用刚创建后保存到当前 PowerShell 会话的 `$LiteKey`、在 API Key 列表复制完整密钥，或使用 `pnpm test:smoke` 自动创建并清理临时本地网关 API Key：

```powershell
if (-not $LiteKey) {
  throw "请先在 API Key 页面新建密钥并把一次性明文保存到当前 PowerShell 会话，或改用 pnpm test:smoke 自动创建临时密钥"
}
```

真实上游账户只用于本地联通验证；优先通过账户测试接口、网关请求或 `pnpm test:smoke` 验证可用性，不要直接导出或打印上游账户凭据。账户测试和后台复测必须走本地 OpenAI 网关处理链路，不能在测试服务里单独直连上游；测试产生的请求、token、成本、错误和账号命中记录必须和客户请求一样进入使用记录、审计和必要统计，不隐藏成本、不丢弃账户配置。统计、质量和授权消耗口径通过 `traffic_source` 区分普通 `gateway`、手动 `manual_account_test` 和恢复探活 `cooldown_retest`；未产生 token / cost 的失败按 0 token、0 cost 计入请求和错误次数。人工测试产生使用记录和审计事实，但不得修改账户状态、账户内 API Key 状态、健康检查信号、冷却字段、最近错误或调度可用性。

```powershell
$LiteKey = "你的本地网关 API Key"

Invoke-RestMethod http://127.0.0.1:3000/v1/models `
  -Headers @{ Authorization = "Bearer $LiteKey" }

$payload = @{
  model = "gpt-5.4-mini"
  input = "只输出 OK"
  max_output_tokens = 16
  stream = $false
} | ConvertTo-Json -Compress

Invoke-RestMethod -Method Post `
  -Uri "http://127.0.0.1:3000/v1/responses" `
  -Headers @{ Authorization = "Bearer $LiteKey" } `
  -ContentType "application/json" `
  -Body $payload
```

流式响应可用 `curl.exe -N` 检查：

```powershell
$streamBody = '{"model":"gpt-5.4-mini","input":"只输出 OK","max_output_tokens":16,"stream":true}'
$bodyFile = New-TemporaryFile
Set-Content -LiteralPath $bodyFile -Value $streamBody -NoNewline -Encoding utf8

curl.exe -sS -N -X POST "http://127.0.0.1:3000/v1/responses" `
  -H "Authorization: Bearer $LiteKey" `
  -H "Content-Type: application/json" `
  --data-binary "@$bodyFile" `
  --max-time 60
```

检查点：`/v1/models` 返回模型列表；非流式 `/v1/responses` 返回完成状态；流式响应能看到完成事件；使用记录能看到 endpoint、token 和成本字段。如果额外验证 `/v1/chat/completions`，确认本地 API Key 绑定的路由策略分组内有 API Key 类型或其他 OpenAI-compatible 透传账号可调度。

发布前真实链路验证不要复用随手抓取的旧 Key；优先由 `pnpm test:smoke` 为目标账户所在分组创建临时路由策略和本地 API Key，完成 `/v1` 请求后自动清理，避免误判账户、分组、路由策略、API Key 和用量链路。

如果本地没有真实账户，`pnpm test:smoke` 会自动启动本机 mock OpenAI 上游，并通过 Mockdata 共享夹具生成临时 GPT API Key 类型账号、临时 GPT 分组和临时本地 API Key，再按同一套正规网关流程完成 `/v1/models`、`/v1/responses` 和使用记录校验，结束后清理临时数据。该 fallback 覆盖的是 API Key / mock OpenAI-compatible 透传路径，不代表 OAuth Codex adapter 支持 `/chat/completions`；它不消耗真实 OpenAI 额度，也不污染真实账号。如果设置了 `JUHE_AI_SMOKE_ACCOUNT_NAME`，脚本会严格测试指定账号，不自动 fallback。通用造数职责见 [Mockdata 造数设计](../functions/Mockdata造数设计.md)。

需要额外覆盖 Codex turn 切号策略时，运行 mock smoke：

```powershell
pnpm test:smoke:mock
```

该命令会启动临时 SQLite 业务库、统计数据集目录库、使用记录目录库、usage shard 目录和统计结果库、本机 mock OpenAI 上游和临时网关实例，自动创建 GPT 分组、两个 GPT API Key 类型假账号和本地网关 API Key，覆盖 Codex turn 前 3 次失败、第 4 次切到备用账号、非 Codex 不切号、Codex 建流前 HTTP 非 `2xx` 账号耗尽时收口为可重试 SSE，以及 Codex profile 下 `context_length_exceeded`、`cyber_policy` 等当前策略边界。它不消耗真实 OpenAI 额度，也不污染 `backend/data/`。

需要验证新版本响应语义检查对广告污染、JSON / SSE 传输形态和服务端切号的真实网关链路时，运行：

```powershell
pnpm test:response-inspection-gateway-e2e
```

该命令会启动临时 SQLite、本机 mock OpenAI-compatible 上游和临时网关实例，自动创建响应检查策略、污染账号、干净账号和本地网关 API Key，覆盖 Chat JSON、Chat SSE、Responses JSON、Responses SSE，以及客户端请求 `stream: true` 但上游仍返回 JSON 的场景。断言重点是污染内容不会写给客户端、命中策略后会从污染账号切到干净账号、客户端非流式请求仍返回 JSON、流式请求仍保持可读 SSE 或上游 JSON 回退形态。

需要验证 Anthropic 官方 API Key 直连、native Messages JSON / SSE、本地 `x-api-key` 认证和协议隔离时，运行：

```powershell
pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai
```

该命令会启动临时 SQLite、本机 mock Anthropic 上游和临时网关实例，自动创建 Anthropic 分组、Anthropic API Key 类型假账号和本地网关 API Key，覆盖 `POST /v1/messages` JSON、`POST /v1/messages` SSE、`POST /v1/messages/count_tokens`、`GET /v1/models` 本地响应、Claude Code 本地画像 header 不透传上游、`anthropic-version` 缺省补齐、`anthropic-beta` 客户端 header 透传、通用 Anthropic 客户端对污染文本 / JSON error object / SSE `event:error` 保持 opaque 直通，以及 OpenAI-compatible 分组不能接收 `/v1/messages` 的隔离边界。它不消耗真实 Anthropic 额度，也不污染 `backend/data/`。当前已存在 Bearer Token 型 Anthropic OAuth 的凭据 / 表单 / 驱动自动回归，但仍没有 Anthropic 订阅网页登录或真实 Bearer Token E2E 自动命令；真实验证通过前不要新增这类私有登录运行时路径。

当前 API Key 和策略路由只负责入口、额度、状态、路由模式和分组调度，不保存客户端画像或显式跨协议桥接规则。跨协议转换通过混合供应商账户配置；普通供应商账号只承接其协议档案支持的原生请求或同协议模型别名。

需要验证普通 Anthropic 账号不会被旧 API Key 显式规则误用来承接 OpenAI Chat / Responses 下游请求时，运行：

```powershell
pnpm --filter juhe-ai-backend test:protocol-boundary-openai-anthropic
```

该命令会启动临时 SQLite，创建 Anthropic 分组、Anthropic API Key 类型假账号和普通策略路由 API Key，断言 API Key 创建入口拒绝 `explicitHybridRouteRules`，并断言普通 Anthropic 账号不会为 `/v1/chat/completions` 构造上游 URL，也不会通过 capability check。它不消耗真实上游额度，也不污染 `backend/data/`。

需要验证 Anthropic Messages 到 OpenAI Chat 的纯转换 helper，以及普通 OpenAI-compatible 账号不会被旧 API Key 显式规则误用来承接 Anthropic Messages 请求时，运行：

```powershell
pnpm --filter juhe-ai-backend test:anthropic-openai-chat-bridge-mock
pnpm --filter juhe-ai-backend test:protocol-boundary-anthropic-openai
```

第一条命令只验证共享 bridge helper 的纯转换逻辑，覆盖 Anthropic JSON / SSE、文本、工具调用、图片输入、usage、SSE error 和不支持能力的 guidance。第二条命令会启动临时 SQLite，创建 OpenAI-compatible 分组、OpenAI-compatible API Key 类型假账号和普通策略路由 API Key，断言账户创建入口拒绝 `clientCompatibility`、API Key 创建入口拒绝 `explicitHybridRouteRules`，并断言普通 OpenAI-compatible 账号不会为 `/v1/messages` 构造上游 URL，也不会通过 capability check。

真实跨协议转换不保留旧 API Key 显式桥接入口。跨协议请求由混合供应商账户表达下游协议、真实上游协议、模型映射和转换策略，API Key 只绑定对应策略路由入口；本地 mock 回归应覆盖混合供应商账户命中、上游请求转换、错误切号和普通账户拒绝跨协议。

如果需要抓取官方 Claude Code CLI 通过本地网关发出的请求形态，可在 mock 链路上开启可选捕获：

```powershell
$env:JUHE_RUN_CLAUDE_CODE_CLI_MOCK = "1"
pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai
Remove-Item Env:\JUHE_RUN_CLAUDE_CODE_CLI_MOCK -ErrorAction SilentlyContinue
```

该可选项会通过 `npx @anthropic-ai/claude-code@latest --print --bare --setting-sources local --no-session-persistence` 启动官方 CLI，设置 `ANTHROPIC_BASE_URL` 指向临时本地网关，并使用临时本地 API Key。它不消耗真实 Anthropic 额度，但依赖当前机器上的 Node / npx / Claude Code CLI 能正常以非交互方式发起请求。`--setting-sources local` 用于避免用户级 Claude settings 覆盖临时 `ANTHROPIC_BASE_URL` / key；如果 CLI 已能输出版本但仍没有命中本地 `/v1/messages`，应记录为本机 CLI 运行环境阻断，不能把它当成网关协议失败。稳定的画像和 header 过滤边界仍由 `test:claude-code-client-strategy` 和默认 Anthropic mock 回归覆盖。

需要同时验证本机真实 CLI 客户端经本地临时网关的全链路兼容时，运行：

```powershell
pnpm --filter juhe-ai-backend test:cli-local-gateway
```

该命令会启动临时 SQLite、本机 mock Anthropic 上游、本机 mock OpenAI-compatible 上游和临时网关实例，自动创建 Anthropic native、DeepSeek Chat-only、GLM Chat 三组账号和本地 API Key，然后实际调用本机 `claude`、`codex`、`opencode`。覆盖重点是 Claude Code 命中 `/v1/messages`、Codex 命中本地 `/v1/responses` 但被 DeepSeek Chat-only 路由受控拒绝且不命中上游、opencode 使用一次性 `OPENCODE_CONFIG_CONTENT` 自定义 provider 命中 GLM Chat、上游只收到账号凭据而不是本地 API Key；Claude/opencode 链路写入成功使用记录，Codex/DeepSeek Chat-only 链路不写成功记录。该命令不消耗真实上游额度，不写用户全局 Codex / Claude / opencode 配置；Windows 下脚本会直接启动各 CLI 的 Node 入口，避免 npm `.cmd` 包装转发复杂参数时造成误判。

需要验证本机 opencode 通过本地临时网关访问真实 OpenAI-compatible 上游时，运行：

```powershell
$env:JUHE_REAL_OPENCODE_OPENAI_COMPATIBLE_API_KEY = "sk-..."
$env:JUHE_REAL_OPENCODE_OPENAI_COMPATIBLE_BASE_URL = "https://vsllm.com"
$env:JUHE_REAL_OPENCODE_MODEL = "gpt-5.4-mini"
pnpm --filter juhe-ai-backend test:opencode-openai-compatible-real-e2e
Remove-Item Env:\JUHE_REAL_OPENCODE_OPENAI_COMPATIBLE_API_KEY, Env:\JUHE_REAL_OPENCODE_OPENAI_COMPATIBLE_BASE_URL, Env:\JUHE_REAL_OPENCODE_MODEL -ErrorAction SilentlyContinue
```

该命令会创建临时 SQLite、本地网关、临时 OpenAI-compatible 账号和临时本地 API Key，通过一次性 `OPENCODE_CONFIG_CONTENT` 自定义 provider 调用本机 opencode，验证 opencode 命中本地 `/v1/chat/completions`、本地 API Key 不透传上游、真实上游返回可被 opencode 消费，并写入成功使用记录。它不写用户全局 opencode 配置。

真实 Codex CLI 到 Chat-only 上游的跨协议 E2E 暂不保留旧入口。当前边界下，普通 OpenAI-compatible / DeepSeek / GLM / Gemini OpenAI Chat 账号只验证自身真实 Chat JSON / SSE 能力；Codex `/v1/responses` 不能靠 API Key 显式规则或账号客户端画像改写到 Chat 上游。混合供应商账户落地后，再新增对应真实 E2E，用混合供应商账户表达下游协议、真实上游协议、模型映射和不可保真能力处理。

需要使用真实 Anthropic 或 Anthropic-compatible 上游做联网 E2E 时，运行：

```powershell
$env:JUHE_REAL_ANTHROPIC_API_KEY = "sk-..."
$env:JUHE_REAL_ANTHROPIC_BASE_URL = "https://api.anthropic.com"
$env:JUHE_REAL_ANTHROPIC_MODEL = "claude-haiku-4-5"
$env:JUHE_REAL_REQUEST_TIMEOUT_MS = "180000"
pnpm --filter juhe-ai-backend test:anthropic-real-gateway-e2e
Remove-Item Env:\JUHE_REAL_ANTHROPIC_API_KEY -ErrorAction SilentlyContinue
```

该命令会创建临时 SQLite、本地网关、临时 Anthropic 分组和临时本地 API Key，默认覆盖真实 `/v1/models`、本地 `/v1/models`、网关 runtime 选号、Messages JSON / SSE、Claude Code 画像 header、工具调用、故意错误请求和并发 JSON 请求。`JUHE_REAL_RUN_COUNT_TOKENS` 默认是 `auto`：脚本会先探测 `/v1/messages/count_tokens`，只有真实上游可用时才把 `message_token_counting` 写入临时账号能力；设置为 `1` / `required` 可强制验证官方 Count Tokens，设置为 `0` 可跳过。默认不启动官方 Claude Code CLI；如需真实 CLI 捕获，额外设置 `$env:JUHE_REAL_RUN_CLAUDE_CODE = "1"`，脚本会使用临时 HOME / APPDATA / XDG 配置隔离本机用户级 Claude 配置，Windows 下优先直接启动 CLI 的 Node 入口。真实联网结果受上游账号额度、模型可用性、限流和第三方兼容网关行为影响；如果上游返回余额不足、额度不足或 403，说明真实模型覆盖被上游账户状态阻断，不能改成按状态码或错误类型判死账号。

对 Anthropic-compatible 第三方上游做轻量稳定性验证时，可以只覆盖客户端关键能力并关闭不适用项，例如：

```powershell
$env:JUHE_REAL_ANTHROPIC_BASE_URL = "https://vsllm.com"
$env:JUHE_REAL_ANTHROPIC_MODEL = "claude-fake-5"
$env:JUHE_REAL_RUN_INVALID_REQUEST_CASE = "0"
$env:JUHE_REAL_RUN_INVALID_MODEL_CASE = "0"
$env:JUHE_REAL_RUN_PARALLEL_CASE = "0"
$env:JUHE_REAL_REQUEST_INTERVAL_MS = "45000"
pnpm --filter juhe-ai-backend test:anthropic-real-gateway-e2e
```

这种口径适合验证 JSON、SSE、工具调用、Claude Code 画像和 header 过滤是否能稳定工作。部分兼容网关没有 `/v1/messages/count_tokens`，默认自动探测后不会声明该能力；关闭故意错误和并发项是为了避免把上游宽松校验、限流或余额状态误判成本地协议缺陷。若兼容上游偶发返回 HTTP 200 但 Anthropic `content: []`、空 `model` 或零 usage，网关应将其转成受控 `upstream_protocol_error`，不能把空成功原样暴露给客户端。

需要验证 DeepSeek 独立供应商、OpenAI-compatible Chat JSON / SSE、模型映射、usage cache hit、reasoning 语义和 `/responses` 隔离时，运行：

```powershell
pnpm --filter juhe-ai-backend test:deepseek-gateway-mock-ai
```

该命令会启动临时 SQLite、本机 mock DeepSeek 上游和临时网关实例，自动创建 DeepSeek 分组、DeepSeek API Key 类型假账号和本地网关 API Key。它覆盖本地 `/v1/models`、`POST /v1/chat/completions` JSON / SSE、同协议模型映射、`prompt_cache_hit_tokens`、`reasoning_content`、`insufficient_system_resource`、DeepSeek 不承接 `/v1/responses`、旧显式混合映射和 Codex 客户端画像不会把 Responses 改写到 Chat，以及上游 JSON 请求体必须带准确 `Content-Length`。它不消耗真实 DeepSeek 额度，也不污染 `backend/data/`。

需要验证 GLM 通用 / Coding 档案、Chat-only 协议隔离、模型目录、价格目录、导入导出和错误恢复时，运行：

```powershell
pnpm --filter juhe-ai-backend test:glm-gateway-mock-ai
```

该命令会启动临时 SQLite、本机 mock GLM 上游和临时网关实例，覆盖 GLM 通用 Chat JSON、GLM Coding Chat SSE、普通 GLM 账号不承接 Codex Responses、旧显式混合映射不会把 Responses 改写到 Chat、截断 / error SSE 失败事件、JSON 错误避让恢复、模型目录 / 价格目录和账号导入导出。它不消耗真实 GLM 额度，也不污染 `backend/data/`。

需要验证 Gemini native、Gemini OpenAI Chat 兼容档案、普通供应商跨协议隔离和 Anthropic 映射禁用时，运行：

```powershell
pnpm --filter juhe-ai-backend test:gemini-gateway-mock-ai
pnpm --filter juhe-ai-backend test:gemini-interactions
pnpm --filter juhe-ai-backend test:postgres-schema-sql
```

第一条命令会启动临时 SQLite、本机 mock Gemini 上游和临时网关实例，覆盖本地 `/v1beta/models`、`generateContent` JSON、`streamGenerateContent` SSE、`countTokens`、本地 `Authorization` / `x-goog-api-key` / query `key` 认证、上游 `x-goog-api-key` 替换、OpenAI Chat 路径不进入 Gemini native、Gemini OpenAI Chat 直连、普通 Gemini / Anthropic / OpenAI-compatible 账号不再通过旧显式混合映射承接跨协议请求，以及 `gemini_cli` 专属响应策略写出前换账号和 `generic_gemini` 隔离。第二条固定 Interactions JSON `event_type`、`metadata.total_usage` 累计快照、POST body stream + Accept、GET `?stream=true`、DELETE 2xx 空 body 与 cancel 契约；第三条用 fresh SQLite 插入两个 Interactions 检查 mode，并核对生成 PG SQL 与 Go migration。三条命令都不消耗真实 Gemini 额度，也不污染 `backend/data/`。

需要对 OpenAI Chat / Responses、Anthropic Messages、Gemini native 三类协议互转做复杂交叉 mock 回归时，运行：

```powershell
pnpm --filter juhe-ai-backend test:protocol-cross-matrix-mock-ai
```

该命令会启动临时 SQLite、本机多协议 mock 上游和临时网关实例，自动创建 OpenAI-compatible Chat、Anthropic Messages、Gemini native 三组目标账号和本地 API Key。当前版本用于协议边界负向回归：普通账户只承接自身真实协议和同协议模型别名，`responses -> chat_completions`、`messages -> chat_completions`、`generate_content|stream_generate_content -> chat_completions`、`chat_completions|responses|generate_content -> messages`、`chat_completions|responses|messages -> generate_content` 这类旧 API Key 显式跨协议规则都应被拒绝或不命中上游；账号模型映射保存层继续禁止跨协议写入。它不消耗真实上游额度，也不污染 `backend/data/`。

需要使用同一批真实 NewAPI / OpenAI-compatible 聚合账户验证多协议、混合供应商跨协议转换和故障 mock 账户救回时，运行：

```powershell
$env:JUHE_REAL_NEWAPI_CHANNELS_JSON = '[{"key":"sk-...","url":"https://vsllm.com"},{"key":"sk-...","url":"https://vsllm.com"}]'
pnpm --filter juhe-ai-backend test:newapi-real-multi-protocol-e2e
Remove-Item Env:\JUHE_REAL_NEWAPI_CHANNELS_JSON -ErrorAction SilentlyContinue
```

该命令会启动临时 SQLite、本机故障 mock 上游和临时网关实例，把每个真实 channel 同时按 OpenAI Chat、OpenAI Responses、Anthropic Messages、Gemini native 和 Hybrid OpenAI Chat 档案写入临时账户；故障 mock 至少应被命中并写入失败使用记录，后续请求如果因同一上游桶短期避让而直接走真实账号，也按自救成功处理。默认 OpenAI 兼容 / Hybrid 上游模型是 `gpt-5.5`，Gemini native 默认使用 `gemini-3.5-flash` 并带短文本 `thinkingConfig`，可通过 `JUHE_REAL_NEWAPI_OPENAI_CHAT_MODEL`、`JUHE_REAL_NEWAPI_OPENAI_RESPONSES_MODEL`、`JUHE_REAL_NEWAPI_HYBRID_UPSTREAM_MODEL`、`JUHE_REAL_NEWAPI_ANTHROPIC_MODEL` 和 `JUHE_REAL_NEWAPI_GEMINI_MODEL` 覆盖。覆盖 `/v1/chat/completions` JSON / SSE、`/v1/responses` JSON / SSE、`/v1/messages` JSON / SSE、`/v1beta/models/*:generateContent` / `streamGenerateContent`，以及 Hybrid `messages -> chat_completions`、`generate_content -> chat_completions` 两类真实跨协议转换。真实联网结果受上游账号额度、模型可用性、限流和第三方兼容实现影响；不要把真实 Key 写入文档、提交记录或截图。

需要只验证 Gemini 客户端画像识别、OpenAI / Anthropic 协议隔离和普通 SDK 不误判时，运行：

```powershell
pnpm --filter juhe-ai-backend test:gemini-client-strategy
```

该命令覆盖显式 `x-juhe-client-profile: gemini_cli`、`GeminiCLI` / `proxy_client=geminicli` User-Agent 签名、普通 `@google/genai` SDK header 不升级、通用 Gemini native 默认 `generic_gemini`，以及 Gemini header 不影响 OpenAI / Anthropic 请求。

需要使用真实 Gemini / NewAPI 聚合上游验证 Gemini OpenAI Chat 和 `gemini-cli` 时，运行：

```powershell
$env:JUHE_REAL_GEMINI_OPENAI_CHAT_API_KEY = "sk-..."
$env:JUHE_REAL_GEMINI_OPENAI_CHAT_BASE_URL = "https://vsllm.com"
$env:JUHE_REAL_GEMINI_OPENAI_CHAT_MODEL = "gemini-3.5-flash"
pnpm --filter juhe-ai-backend test:gemini-openai-chat-real

$env:JUHE_REAL_GEMINI_API_KEY = "sk-..."
$env:JUHE_REAL_GEMINI_BASE_URL = "https://vsllm.com"
$env:JUHE_REAL_GEMINI_CLI_MODEL = "gemini-3.5-flash"
pnpm --filter juhe-ai-backend test:gemini-cli-real-gateway

Remove-Item Env:\JUHE_REAL_GEMINI_OPENAI_CHAT_API_KEY, Env:\JUHE_REAL_GEMINI_OPENAI_CHAT_BASE_URL, Env:\JUHE_REAL_GEMINI_OPENAI_CHAT_MODEL, Env:\JUHE_REAL_GEMINI_API_KEY, Env:\JUHE_REAL_GEMINI_BASE_URL, Env:\JUHE_REAL_GEMINI_CLI_MODEL -ErrorAction SilentlyContinue
```

`test:gemini-openai-chat-real` 使用临时本地网关创建 Gemini OpenAI Chat 账号，真实探测 `/v1/models`，并覆盖 Chat JSON 和 Chat SSE；不再覆盖 Codex Responses 到 Chat 的旧 API Key 显式桥接。第三方 NewAPI 如果只提供根地址，例如 `https://vsllm.com`，该脚本会验证本地档案能正确拼接到 `/v1/chat/completions`。`test:gemini-cli-real-gateway` 使用临时 Gemini native 账号和临时 HOME 调用本机 `gemini` CLI，不写用户全局配置；脚本会在临时 `.gemini/settings.json` 中选择 `gemini-api-key`，同时通过 `GOOGLE_GEMINI_BASE_URL` 指向本项目，验证 CLI 命中本地 `/v1beta/models/{model}:streamGenerateContent?alt=sse`。

跨供应商检查失败恢复、短期避让、恢复命中和 usage/audit 供应商归属时，运行：

```powershell
pnpm --filter juhe-ai-backend test:provider-failure-recovery-matrix
```

该命令覆盖 GPT、OpenAI-compatible、DeepSeek、GLM 和 Anthropic 的失败换号矩阵，重点断言 `provider_code`、`provider_protocol_profile_id`、`usage_semantic`、group/account attempt 归属，不替代各供应商专项协议回归。

需要使用真实 DeepSeek 或第三方 DeepSeek OpenAI-compatible 上游做联网 E2E 时，运行：

```powershell
$env:JUHE_REAL_DEEPSEEK_API_KEY = "sk-..."
$env:JUHE_REAL_DEEPSEEK_BASE_URL = "https://api.deepseek.com"
$env:JUHE_REAL_DEEPSEEK_MODELS = "deepseek-v4-flash,deepseek-v4-pro"
pnpm --filter juhe-ai-backend test:deepseek-real-gateway-e2e
Remove-Item Env:\JUHE_REAL_DEEPSEEK_API_KEY, Env:\JUHE_REAL_DEEPSEEK_BASE_URL, Env:\JUHE_REAL_DEEPSEEK_MODELS -ErrorAction SilentlyContinue
```

该命令会创建临时 SQLite、本地网关、临时 DeepSeek 分组和临时本地 API Key，覆盖本地模型目录、Chat JSON 和 Chat SSE。真实联网结果受上游账号额度、模型可用性、限流和第三方兼容网关行为影响；如果上游返回模型配额超限或余额不足，应记录为上游账户状态阻断，不要改成按状态码或错误类型判死账号。第三方代理如果暴露别名模型，可以通过 `JUHE_REAL_DEEPSEEK_MODELS` 指定，例如 `deepseek-ai-v4-flash,deepseek-ai-v4-pro`。

需要验证 Claude Code 客户端画像识别、OpenAI 协议隔离和未知流形态不升级时，运行：

```powershell
pnpm --filter juhe-ai-backend test:claude-code-client-strategy
```

账户标签功能改动后运行：

```powershell
pnpm test:account-tags
```

该命令使用临时 SQLite 数据库覆盖标签创建、更新、删除约束、导入导出和授权实例可见性边界，不消耗真实 OpenAI 额度，也不污染 `backend/data/`。

Go W1b 公开账户默认模型语义改动后，在 `backend-go/` 下运行：

```powershell
go test ./internal/httpapi -run TestPublicAccount -count=1
go test ./internal/modules/publicaccounts -run TestServiceAdd -count=1
go test ./internal/store/postgres -run TestPublicAccount -count=1
go test ./internal/httpapi ./internal/modules/publicaccounts ./internal/store/postgres -count=1
```

该矩阵用于固定 [BUG-0035 Go 公开账户默认模型语义漂移](../bug/问题-0035-Go公开账户默认模型语义漂移.md) 的非 Docker 门禁：

- HTTP `StringListValue` 必须区分 `supportedModels` 字段省略、显式 `[]` 和显式非空数组，不能在 handler 进入 service 前折叠。
- service 在字段省略时继承 provider 的 `default_supported_models_json`；显式空数组和 provider 默认值最终为空时都返回 `账户支持模型不能为空，请至少选择一个该 Base URL 支持的模型`。
- 同目标分组、同供应商、同协议档案、同名账户的重复名称冲突必须保持在支持模型非空校验之前。
- PostgreSQL provider profile 查询必须联表读取默认模型 JSON；解码异常应 fail-fast；fresh Go seed 中 `gpt` / `openai` 必须保留 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna` 静态 guard。

真实 PostgreSQL 和 Docker/testcontainers 目标测试：

```powershell
go test -v -tags=integration ./internal/testkit/integration -run TestW1bPublicAccountsPostgresSmoke -count=1
go test -v -tags=integration ./internal/testkit/integration -run TestW1bPublicAccountsShellE2E -count=1
```

真实环境必须确认：省略 `supportedModels` 后 `account_supported_models` 写入 provider 默认模型；显式 `[]` 返回 HTTP `400` 且不创建账户；重复名称和显式空数组同时出现时仍先返回名称冲突；公开日志和业务响应不泄露凭据。缺少 Docker 时输出 `SKIP` 只能记为未执行。`go run ./cmd/juhe-ai-maintenance w1b-public-api-smoke` 当前只通过 `group/list` 验证公开 shell、鉴权、限频和日志链路，不能替代本次 `account/add` 默认模型专项。

本轮非 Docker 目标测试、全量 Go、目标包 race、vet、tidy diff 和 integration 包编译已通过。两个真实 PostgreSQL / shell E2E 目标测试因本机 Docker 未运行输出 `SKIP`，不计真实环境通过。

Go W2 账号标签下拉、未绑定标签删除、独立标签 PATCH 和 PATCH operation log 写入基线改动后，在 `backend-go/` 下运行：

```powershell
go test ./internal/jobs/operationlog ./internal/jobs/worker ./internal/modules/managementaccounts ./internal/store/postgres ./internal/httpapi ./internal/config ./internal/app
go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAccountOptionsPostgresSmoke -count=1
```

第一条覆盖 Go handler / service / store / router、标签删除 204 / 400 / 404、标签 PATCH 事务 SQL guard、PATCH `id/tags` 窄响应、operation log before/after payload、Asynq `operation-logs` 入队参数、worker `operation-log:write` handler、HTTP 成功入队和入队失败不影响业务响应；第二条需要 Docker / testcontainers 可用，缺少 Docker 时只记录为 `SKIP`，不能算真实 PostgreSQL 集成通过。该 integration smoke 代码已通过 `WithOperationLog` handler 断言成功 PATCH 生成 `operation-log:write` / `operation-logs` payload、越权失败 PATCH 不入队；只有在 Docker/testcontainers 健康环境实际运行且不是 `SKIP` 时，才算真实 PostgreSQL integration 通过。Go W2 目前覆盖 `GET accounts/tags` / `GET my-accounts/tags`、`DELETE accounts/tags/{tagId}` / `DELETE my-accounts/tags/{tagId}`、`PATCH accounts/:id/tags` / `PATCH my-accounts/:id/tags` 和 PATCH 成功后的 operation log 写入基线；不覆盖账户创建 / 编辑携带 `tags`、OAuth 创建、导入标签写入、账号列表 / 详情完整 summary、operation log 保留清理生产接管 / 完整 data-retention 或其他写接口 operation log。

操作日志保留清理已由 `juhe-ai-worker operation-log-retention-cleanup` 独立 worker 覆盖，见下方 W2 operation log worker 验证段；它不属于账号标签 PATCH 切片的接管证据。

Go W2 系统账户列表读和 options 改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app
go test -tags=integration ./internal/testkit/integration -run '^$'
go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementSystemAccountOptionsPostgresSmoke -count=1
```

第二条覆盖 `GET /__aisys__/api/system-accounts` 列表读和 `GET /__aisys__/api/system-accounts/options` 的 service / handler / store / router 快速验证，包括 admin-only、分页窗口上界、`total` 非精确上界、敏感字段 SQL 白名单、普通用户 403 和默认关闭的 router opt-in guard。integration 包编译只证明测试代码可编译；`TestW2ManagementSystemAccountOptionsPostgresSmoke` 需要 Docker / testcontainers 可用，缺少 Docker 时只记录为 `SKIP`，不能算真实 PostgreSQL integration 通过。该切片不覆盖系统账户创建、更新、禁用、管理端重置密码、登录、登出、验证码、默认资源补齐或会话撤销。

Go W3 当前用户读取 / 当前用户资料更新 / 当前用户改密 / 当前会话登出接口改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app
go test ./...
go test -tags=integration ./internal/testkit/integration -run '^$'
go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthPostgresSmoke -count=1
```

第一条覆盖 `GET /__aisys__/api/auth/me` handler / router / app wiring、`AuthenticateCookieForCurrentUser` 允许普通初始密码用户返回 `mustChangePassword=true`、普通后台 middleware 仍拦截初始密码用户、`PATCH /__aisys__/api/auth/me` 严格 JSON / 空白字符 / 重复名 / no-op / operation log payload / 入队失败不影响成功、`POST /__aisys__/api/auth/change-password` 严格 JSON / Node 兼容 PBKDF2-SHA512 hash / 初始密码免旧密码 / 普通改密校验旧密码 / 成功清除 `mustChangePassword` / 只撤销同账号其他 session、已迁移管理写接口 `last_seen_at` 60 秒节流 touch、`GET /auth/me` 与 logout 不 touch、`POST /__aisys__/api/auth/logout` 按当前 token hash 删除 session、无 Cookie 仍返回已登出、撤销失败不清 Cookie、默认关闭的 router opt-in guard 和错误脱敏。`TestW2ManagementAuthPostgresSmoke` 名称沿用 W2 session 鉴权基线 smoke，本轮已扩展覆盖 current-user 读响应、profile update 真实 PG 更新、初始密码用户 PATCH 403、change-password 真实 PG 密码 hash 与其他 session 撤销、`last_seen_at` 读写差异、operation log payload、logout 清 Cookie 和登出后旧 Cookie 401；它需要 Docker / testcontainers 可用，缺少 Docker 时只记录为 `SKIP`，不能算真实 PostgreSQL integration 通过。该切片不覆盖登录、验证码、session 创建或完整系统账户写接口。

Go W3 前端 auth / session / 系统账户页本地 smoke 改动后，在仓库根目录运行：

```powershell
$env:CI = 'true'
pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke
pnpm --filter juhe-ai-frontend typecheck
pnpm --filter juhe-ai-frontend build
```

第一条固定前端 `auth` / `systemAccounts` 当前契约，并负向断言登录会话管理 API、头像菜单入口和弹窗组件不存在；同时覆盖系统账户页 `super_admin` 写入口和敏感字段不暴露。它使用本地 mock HTTP，不连接真实 Go 后端；浏览器 Cookie、HTTPS、反向代理和生产单 owner 切流仍需真实环境 smoke。

Go W3 系统账户 `PATCH /__aisys__/api/system-accounts/{id}` reset-password / status-only 子动作改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test ./... -count=1
go test -race ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
```

第二条覆盖 reset-password / status-only 的 service / handler / store / router 快速验证，包括 `super_admin` 权限、严格 JSON 字段集、`password` 与 `status` 混用拒绝、`username` 特殊错误、`mustChangePassword:null` 拒绝、Node 兼容密码 hash、状态枚举、最后一个启用 `super_admin` 保护、禁用时删除目标账户全部 session、operation log 脱敏和默认关闭的 router opt-in guard。SQL guard 必须固定 status-only 先 `MATERIALIZED` 锁定并聚合启用 `super_admin` 集合，再锁目标账户，避免并发停用不同超级管理员时退化成互锁或绕过保护。integration 包编译只证明测试代码可编译；真实 PG+Redis+operation-log integration、gateway cache invalidation / API Key validation cache 清理、浏览器连接真实 Go 后端的前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍未完成。

Go W3 自定义供应商模型 CRUD 补充写接口改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementprovidermodels ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementProviderModelCRUDPostgresSmoke -count=1
```

第二条覆盖 `POST/PATCH/DELETE /__aisys__/api/providers/{code}/models` 的 service / handler / store / router 快速验证，包括个人 / 管理员目标用户 / 全局模型权限、价格校验、strict JSON、int4 上界、Node 错误优先级、禁止修改模型 ID、停用 / 删除清理默认健康检查模型偏好、账户支持模型和同来源供应商模型映射绑定删除保护，以及保存 / 删除只发布 gateway runtime cache 失效。integration 包编译只证明测试代码可编译；`TestW3ManagementProviderModelCRUDPostgresSmoke` 需要 Docker / testcontainers 可用，缺少 Docker 时只记录为 `SKIP`，不能算真实 PostgreSQL integration 通过。该切片不覆盖供应商定义写接口、供应商协议档案写接口、前端真实 Go 后端 smoke、生产单 owner 切流或 Node 供应商自定义模型写路径删除。

Go W4 系统团队列表 / 详情读接口、`POST /__aisys__/api/system-teams` 创建子动作、`PATCH /__aisys__/api/system-teams/{id}` 更新子动作、`POST /__aisys__/api/system-teams/{id}/members` 成员新增、`DELETE /__aisys__/api/system-teams/{id}/members/{memberId}` 成员移除、`GET /__aisys__/api/authorizations` / `GET /__aisys__/api/my-authorizations` 授权列表、`GET /__aisys__/api/authorizations/{id}` / `GET /__aisys__/api/my-authorizations/{id}` 授权详情、`POST /__aisys__/api/authorizations` / `POST /__aisys__/api/my-authorizations` 授权创建、`PATCH /__aisys__/api/authorizations/{id}` / `PATCH /__aisys__/api/my-authorizations/{id}` 授权更新、`PATCH /__aisys__/api/authorizations/{id}/expire` / `PATCH /__aisys__/api/my-authorizations/{id}/expire` 授权有效期更新、`DELETE /__aisys__/api/authorizations/{id}/return` / `DELETE /__aisys__/api/my-authorizations/{id}/return` 授权归还、`DELETE /__aisys__/api/authorizations/{id}` / `DELETE /__aisys__/api/my-authorizations/{id}` 授权回收、授权来源 / grant / 额度窗口 / 统计脏标记 schema 基线、授权缓存失效和 gateway quota snapshot 构建 worker 改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/gatewaycache -count=1
go test ./internal/modules/managementsystemteams ./internal/modules/managementauthorizations ./internal/modules/gatewayquotasnapshot ./internal/httpapi ./internal/store/postgres ./internal/app ./cmd/juhe-ai-worker -count=1
go test ./... -count=1
go vet ./...
go mod tidy -diff
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
```

第二条覆盖授权缓存失效能力，第三条覆盖团队列表 / 详情 / 创建 / 更新 / 成员维护、授权列表 / 详情 / 创建 / 更新 / 有效期更新 / 归还 / 回收的 service / handler / store / router 快速验证，以及授权来源 / grant、额度窗口和统计脏标记 migration schema guard。团队接口验证包括 admin-only 管理列表、普通用户访问管理列表 403、`my-teams` 强制 self 作用域、团队名前缀搜索、分页窗口上界、详情 active 成员摘要、读接口不 touch session、strict JSON、`systemAccountId` query 合法性、团队名称全局唯一、只写团队摘要字段、PATCH partial update、禁用团队 revoke team source、启用团队按 active grants 展开到 active members、成员新增 active 团队 / active 系统账户 / 批量上限 / 团队 active 成员上限、成员新增按 active team grants 展开最终用户授权、成员移除 revoke 该成员 active team source、effective source 刷新、全量 stats dirty 标记、`system_teams.create` / `system_teams.update` / `system_teams.add_members` / `system_teams.remove_member` operation log payload 和默认关闭的 router opt-in guard；授权列表验证包括 admin / self 作用域、管理侧 direction 校验但不应用、个人侧 direction 应用、grant 主表分页、关键词前缀搜索、轻量 DTO、`sourceSummary`、非资源归属人脱敏、读接口不 touch session 和不触发到期扫描；授权详情验证包括 admin / self 作用域、路径 ID 固定为 grant ID、读接口不 touch session、非资源归属人 source 明细脱敏、返回基础 usage 空对象且不做 usage 聚合、不触发到期扫描；授权创建验证包括 admin / self 作用域、账户个人授权目标分组必填、团队授权 fanout、quota limits 归一化、授权账户实例创建 / 目标分组绑定、`resource_authorization_created` 缓存失效和 `authorizations.create` operation log payload；授权更新验证包括 admin / self 资源归属作用域、grant ID path 契约、strict JSON、`status=active/paused`、`expiresAt`、`limits` 归一化、manual / team source runtime 刷新、`resource_authorization_updated` 缓存失效和 `authorizations.update` operation log payload；授权有效期更新验证包括 admin / self 资源归属作用域、grant ID path 契约、`expiresAt` 必填、`limits` 可选 / 可清空、不接受 `status`、复用 `resource_authorization_updated` 缓存失效和 `authorizations.update_expire` operation log payload；授权归还验证包括 admin / self 作用域、grant ID path 契约、manual source 撤销、runtime authorization `returned` 状态、`resource_authorization_returned` 缓存失效和 `authorizations.return` operation log payload；授权回收验证包括 admin / self 资源归属作用域、grant ID path 契约、manual source / 当前资源 team source 撤销、runtime authorization `revoked` 刷新、`resource_authorization_revoked` 缓存失效和 `authorizations.revoke` operation log payload；schema guard 固定 `resource_authorization_sources` / `resource_authorization_grants`、来源 / grant 状态约束、active 部分唯一索引、到期扫描索引、quota snapshot 索引、默认额度小时窗口和 `group_account_stats_dirty`；cache guard 固定 Node 兼容 `gateway_runtime_cache`、`authorization_quota_cache` 和 `api_key_quota_cache` runtime state topic、TTL 和 `apiKeyId` 字段；gateway quota snapshot guard 固定 API Key / direct authorization / team authorization scope、`LIMIT 5001` 完整性窗口、只读 `usage_stats_*` 预聚合表和 `usage_quota_hourly_windows`，不读取 `usage_records`，不在请求路径实时汇总。integration 包编译只证明测试代码可编译；真实 PG+Redis+Asynq operation-log integration、浏览器连接真实 Go 后端的前端团队 / 统一授权页 smoke、授权 usage、批量到期扫描 worker、gateway quota snapshot 生产部署 / 消费、生产单 owner 切流和 Node `/system-teams` / `/authorizations` 删除仍未完成。

Go W4 授权 team/user usage overview 当前同属上方 `backend-go/` 验证矩阵；新增 route / service / store / schema guard 必须证明只读取 `juhe_stats.authorization_team_usage_range_windows` / `authorization_user_usage_range_windows` 预聚合窗口表，先分页再 join 当前页摘要，不读取 `usage_records`，不在请求路径做实时 `SUM/GROUP BY`。授权 `:id/usage` 明细、usage range window 刷新 worker 和 gateway quota snapshot 构建 worker 也必须在 Go 侧验证：usage range window worker 只从 `authorization_team_usage_summary_daily` / `authorization_user_usage_summary_daily` 汇总写入 range window，不读取 `juhe_usage.usage_records`；gateway quota snapshot worker 只读取 `usage_stats_*` 预聚合表和 `usage_quota_hourly_windows`，不写 Redis local snapshot；真实 PostgreSQL smoke 和生产部署接管另行记录，不得用本地单元测试替代。

Go W4 前端团队 / 统一授权页本地 smoke 改动后，在仓库根目录运行：

```powershell
$env:CI = 'true'
pnpm --filter juhe-ai-frontend test:w4-team-authorization-smoke
pnpm --filter juhe-ai-frontend typecheck
pnpm --filter juhe-ai-frontend build
```

第一条固定前端 `systemTeams` / `myTeams` / `authorizations` / `myAuthorizations` API 路径、团队创建 / 更新 / 成员维护入口、授权列表 / 详情 / 创建 / 状态更新 / 有效期更新 / 归还 / 回收调用、管理侧 `systemAccountId` 作用域和个人侧 `my-*` 去除管理作用域边界。它使用本地 mock HTTP，不连接真实 Go 后端；浏览器 Cookie、HTTPS、反向代理、真实 Go 后端页面 smoke 和生产单 owner 切流仍需真实环境验证。

Node 对照基线可继续运行：

```powershell
pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary
pnpm --filter juhe-ai-backend test:system-account-session-driver
pnpm --filter juhe-ai-backend test:auth-profile-postgres-smoke
```

Go W2 operation log 管理 / 个人读接口改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementoperationlogs ./internal/httpapi ./internal/store/postgres ./internal/app
go test ./internal/maintenance ./cmd/juhe-ai-maintenance
go test ./...
go test -tags=integration ./internal/testkit/integration -run '^$'
go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementOperationLogsPostgresSmoke -count=1
go run ./cmd/juhe-ai-maintenance w2-operation-logs-smoke
```

第一条覆盖 `GET /__aisys__/api/operation-logs`、`GET /__aisys__/api/operation-logs/{id}`、`GET /__aisys__/api/my-operation-logs` 和 `GET /__aisys__/api/my-operation-logs/{id}` 的 service / handler / store / router 快速验证，包括 admin-only、self viewer scope、分页窗口上界、轻量列表 DTO、summary 搜索倒排、traceId `COLLATE "C"` 前缀范围、个人端脱敏和详情 404。`go test ./internal/maintenance ./cmd/juhe-ai-maintenance` 覆盖 `w2-operation-logs-smoke` 缺配置 fail-fast、默认 router guard 和输出不声明生产接管；`go run ./cmd/juhe-ai-maintenance w2-operation-logs-smoke` 需要真实 `JUHE_AI_POSTGRES_URL`、`JUHE_AI_REDIS_STATE_URL` 和 `JUHE_AI_REDIS_QUEUE_URL`，会用本地 `httptest` 创建临时 fixture 验证显式 opt-in operation log 读路径，但不启动生产 HTTP 端口。integration 包编译只证明测试代码可编译；`TestW2ManagementOperationLogsPostgresSmoke` 需要 Docker / testcontainers 可用，缺少 Docker 时只记录为 `SKIP`，不能算真实 PostgreSQL integration 通过。该切片不覆盖 operation log 保留清理、其他写接口 operation log、前端页面 smoke、生产切流、反向代理 path-split 或 Node 删除。

Go W2 operation log 保留清理 worker 改动后，在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementoperationlogs ./internal/store/postgres ./internal/app ./cmd/juhe-ai-worker -count=1
go run ./cmd/juhe-ai-worker operation-log-retention-cleanup --run-once
```

第二条覆盖 service / store / worker 命令参数、默认保留天数、批量删除窗口和 SQL guard。第三条需要真实 `JUHE_AI_POSTGRES_URL`，会按 `operationLogRetentionDays` 计算 cutoff，缺失时使用 365 天默认值；每批最多删除 1000 条 `juhe_dataset.operation_logs`，单轮最多 20 批，满批之间暂停 25ms，并依赖 FK cascade 清理 operation log 子表。缺少真实 PostgreSQL 时不能把 run-once / 长跑、生产 supervisor、完整 data-retention 或 Node 删除记为已验证。

## 5. GPT OAuth 账户验证

前端路径：

```text
http://127.0.0.1:5173/__aisys__/accounts
```

新增 GPT OAuth 账户时，可选两种 OpenAI OAuth 授权方式：

- `手动授权`：生成授权链接，登录完成后复制浏览器地址栏里的 `http://localhost:1455/auth/callback?code=...&state=...` 回调 URL，粘贴回弹窗创建账户。
- `Refresh Token`：直接粘贴已有 OpenAI `refresh_token`，后端刷新成功后创建 OAuth 账户。

已有 GPT OAuth 账户在账户列表“更多”菜单中提供两个排障入口：

- `刷新令牌`：强制使用当前账户保存的 Refresh Token 换取一次 Access Token；成功后账户会刷新凭据，限流中、临时不可调用等软失败状态会恢复可调度，停用和异常不会被这个入口恢复。异常账户前端隐藏刷新入口，后端也会拒绝直接刷新，需要先走“异常恢复”或通过编辑修正凭据。失败会直接显示 OAuth 或代理错误。如果遇到 OpenAI 返回 `refresh_token_reused` 或 “Refresh Token 已被使用”一类提示，但账户仍能继续调用，通常表示另一个请求或后台任务已经先完成刷新并写入了新凭据；后端会在锁内重读账户并优先采用最新凭据。
- `重新授权`：打开独立弹窗复用新增账户的授权流程，可通过授权回调 URL 或新的 Refresh Token 覆盖当前账户 OAuth 凭据；名称、分组、代理和并发不随重新授权修改。异常账户前端隐藏重新授权入口，后端也会拒绝直接重新授权。

后端授权链接接口可用以下命令做冒烟检查：

```powershell
$AdminCookie = "juhe_ai_session=..."

Invoke-RestMethod -Method Post `
  -Uri "http://127.0.0.1:3000/__aisys__/api/openai-oauth/auth-url" `
  -Headers @{ Cookie = $AdminCookie } `
  -ContentType "application/json" `
  -Body "{}"
```

OAuth token 交换、刷新、重新授权和账户测试会使用账户绑定代理；网关真实转发也会使用账号绑定代理。OAuth token 交换、刷新和重新授权属于授权协议请求，不计入模型使用统计；账户测试会通过同一套网关模型请求链路计入使用记录。后台 Access Token 预刷新只做授权保活，会扫描所有仍存在、未删除、有 `refresh_token` 且快过期的 GPT OAuth 账户，不因停用、异常、限流或临时不可调用状态跳过；预刷新成功只写回凭据，不自动恢复普通账户状态；连续 3 次预刷新失败会把账户标记为 `status = error`，异常类型为 `oauth_token_refresh_failed`，后续后台刷新成功只自动恢复这一类刷新异常。真实网关请求只在 Access Token 缺失、过期、过期时间无效或距离过期不超过 5 秒时阻塞刷新；距离过期小于 60 秒但仍超过 5 秒时只触发账号级后台预热并继续当前请求。后台冷却复测固定启用，但只会复测冷却时间到期的 `temporary_unavailable` 和 `rate_limited` 账号；临时状态下的 `cooldown_until` 表示下次后台复测计划时间，不作为账户套餐到期时间展示。账号质量主动测速能力已删除；真实质量样本触发的频繁失败后台确认仍保留，确认探针使用 `traffic_source = cooldown_retest`，不进入账号质量统计。OAuth 额度快照不再主动探测，只从真实网关请求响应头被动更新；人工测试不得更新额度快照。

## 6. 账户与管理面验证

账户列表“更多”菜单里的“测试”会调用后端 `POST /__aisys__/api/accounts/:id/test`；用户侧 `我的 AI 账户` 调用 `POST /__aisys__/api/my-accounts/:id/test`，固定使用当前登录用户作用域。创建 / 编辑账户弹窗底部的“测试”使用当前表单快照，并固定以表单中的 `healthCheckModel` 执行，不开放临时模型选择；新增自有账户调用 `POST /__aisys__/api/accounts/test-draft` 或 `POST /__aisys__/api/my-accounts/test-draft`，编辑已保存自有账户把未保存表单快照提交到单账户测试接口。保存新增或导入账户时，即使请求状态为 `active`，也必须先落为 `pending_test`，由后台激活检查成功后改为 `active`；关键连接字段编辑保存后同样交后台复检，不以人工测试结果激活或恢复账户。授权账户仍按已保存账户测试。管理员在管理侧可通过 `systemAccountId` 代某个用户作用域测试其可见账户。

测试内部按账户保存的精确 `healthCheckEndpointMode` 构造最小本地网关请求，并复用正常网关链路验证账户是否可用；允许 Chat Completions、Responses、Anthropic Messages 和 Gemini Generate Content 的 JSON / SSE 生成形态，前提是账户上游接口能力已启用，工具、计数、嵌入、上传和下载接口不作为检查协议。人工测试、后台激活检查、周期健康检查、冷却复测和事前确认探针都不根据历史流量切换 endpoint / stream；测试不过即表示该账户不能承接本次测试形态。GPT OAuth 账户会在网关准备上游账号时按正常请求规则刷新 token。系统探针必须严格使用账户 `health_check_model`；账户创建时按“当前所有者供应商个人默认 > 管理员系统默认 > 协议档案默认”初始化一次，后续默认变化不回写已有账户，也不回退到账户支持模型首项。账户列表测试先按需调用 `GET /__aisys__/api/accounts/:id/test-options` 或 `GET /__aisys__/api/my-accounts/:id/test-options`，可选择该账户供应商、协议档案与所有者作用域内可见且启用的文本目录模型，不受 `supportedModels` 限制；所选模型只用于本次诊断，不保存到账户。诊断重试的所有 attempt 使用同一轮开始时确定的模型。OpenAI API Key 账户的 Responses 测试按当前契约不发送 `max_output_tokens`。账户测试和后台复测结果携带本地 `traceId`，上游错误文本中的 `request id` 必须保留；排障时本地链路用 `traceId` 查询运行日志、使用记录和审计日志，上游链路用 `request id` 查询供应商日志。测试输入由后端使用默认探活输入生成。测试只允许命中当前被测账户，不会静默切到同分组其他账号。

手动账户测试的真实请求等待策略为 `10s + 20s + 30s`。产品不提供多账户批量测试；后端账号测试 worker 只异步消费用户显式发起的单账户测试任务，并按系统设置 `accountTestTaskConcurrency` 控制全站并发。单个任务的 60s 运行超时只从后台任务进入 `running` 并写入 `startedAt` 后开始计算；`queued` / 等待后台接收的任务也必须记录 `queuedAt`、等待时长和最近投递 / 接收状态，超过队列等待上限后自动收口为失败或取消，避免后台异常时长期卡住并持续堆积。worker 从数据库补充 runnable 测试任务时必须按固定窗口读取，不能一次性把所有 queued 任务灌入内存队列。每次单账户测试创建独立 `testSessionId`，同一用户的 A/B 两次测试互不阻塞、互不覆盖、互不取消；“停止测试”只取消当前会话。关闭弹窗、切换菜单、刷新页面或浏览器会话丢失只中断当前前端接收，后端任务继续执行。前端用 `sessionStorage` 保存 12 小时终端快照，重新进入页面后按对应 `testSessionId` 恢复接收；若浏览器会话已丢失，则通过后端任务列表和检测结果查看最终结果。

模型检测和代理检测仍共享 DB service 诊断任务 in-flight 上限；超过上限会快速返回 `503` 和 `Retry-After`，不会在 DB service 事件循环内排队等待外部探测。账户测试和模型检测展示用响应体只保留 `256KB` 有界预览，异常大响应会标记 `responseTruncated` 并追加截断提示，避免管理接口因为上游异常响应占用过多内存或同步解析大文本。自有账户包含停用和异常状态都可以手动测试，但成功或失败都只形成诊断结果，不得清理或写入 `pending_test`、`temporary_unavailable`、`rate_limited`、`error`、冷却、最近错误、健康检查信号、账户内 API Key 运行态或调度状态。只有后台激活检查、周期健康检查、冷却复测和事前确认探针可以按各自策略推进运行态。授权账户测试额外要求已绑定到当前用户分组、授权仍有效、额度未耗尽、账户未到期；授权用户只看到脱敏后的测试结果，完整上游响应头、响应体、上游地址、代理、请求体和无状态码内部异常文本等诊断字段仅所有者或管理员可见。授权实例不提供批量编辑；显式异常账户仍通过专用恢复动作或后台策略处理，不能借人工测试恢复调度。

账号测试取消去放大验证：

```powershell
pnpm --filter juhe-ai-backend test:account-test-cancel-rpc-deamplification
pnpm --filter juhe-ai-backend test:account-test-cancel-postgres-redis-smoke
```

第一条回归覆盖：队列正常路径源码不得轮询取消状态；SQLite 下取消与 complete/fail 竞态最终保持 `canceled`；100 个正常完成任务的取消读 operation 计数为 0。第二条使用 fresh 当前 schema PostgreSQL 和三套物理隔离 Redis，验证 100 个任务并发 claim/complete、取消先于 complete/fail、cache/state/queue 三个 Redis 分别 PING，并只清理随机 marker 对应 task 行。测试数据库和连接串不得写入仓库；临时数据库必须在 `finally` 中删除。

周期健康检查始终由 `ops-worker` 分批执行，不提供账户级 `health_check_enabled` 开关；候选窗口、检查频率和失败阈值由后台策略控制。系统探针严格使用账户 `health_check_model`，缺失、不可见、停用或协议不匹配时必须显式报配置错误，不能临时选择其他模型。后台激活检查负责把新建 / 导入时请求为 `active` 的 `pending_test` 账户在成功后切为 `active`；关键凭据、Base URL、代理、供应商、协议档案或类型等连接字段变更后同样由后台复检决定后续运行态。健康检测字段、候选查询、近期成功顺延、激活检查和失败阈值变更后运行：

```powershell
pnpm test:account-health-check
```

该命令使用临时 SQLite 覆盖 `health_check_model` 字段创建、候选筛选、严格模型校验、真实成功请求刷新健康信号、后台探活流量不刷新真实健康成功信号、失败计数、激活状态推进和阈值判断；还应验证个人默认、管理员系统默认、协议档案默认只在新账户初始化时按优先级生效。

```powershell
$AdminCookie = "juhe_ai_session=..."

$account = (Invoke-RestMethod http://127.0.0.1:3000/__aisys__/api/accounts -Headers @{ Cookie = $AdminCookie }).data |
  Where-Object { $_.providerCode -eq "gpt" -and $_.status -eq "active" } |
  Select-Object -First 1

$testOptions = (Invoke-RestMethod `
  -Uri "http://127.0.0.1:3000/__aisys__/api/accounts/$($account.id)/test-options" `
  -Headers @{ Cookie = $AdminCookie }).data

$testBody = @{
  model = $testOptions.healthCheckModel
} | ConvertTo-Json -Compress

Invoke-RestMethod -Method Post `
  -Uri "http://127.0.0.1:3000/__aisys__/api/accounts/$($account.id)/test" `
  -Headers @{ Cookie = $AdminCookie } `
  -ContentType "application/json" `
  -Body $testBody
```

管理面重点检查：

- 账户列表只显示名称、类型、供应商、并发、状态、用量、优先级、最近使用时间和操作。
- `Access/API Key` 与 `Refresh Token` 不在列表展示，只在编辑弹窗展示和修改。
- API Key 页面只在新建成功弹窗中一次性展示完整本地网关密钥；列表、详情和后续更新只能看到前缀、掩码或空 key。
- OAuth 账户如需代理，可在账户编辑弹窗中绑定代理配置；代理不可用时刷新 token 和测试会失败。

### 6.1 PLAN-0081 W1b 账户健康任务 bridge

Node/Go 专项分别运行：

```powershell
pnpm --filter juhe-ai-backend test:account-health-check-dispatch
pnpm --filter juhe-ai-backend test:account-health-check-dispatch-http

Set-Location backend-go
go test ./internal/platform/accounthealthcheckdispatch ./internal/modules/publicaccounts ./internal/config ./internal/app -count=1
```

真实跨运行时测试命令：

```powershell
Set-Location backend-go
go test -v -tags=integration ./internal/testkit/crossruntime -run '^TestAccountHealthCheckDispatchNodeE2E$' -count=1 -timeout=30s
```

该测试位于独立 `crossruntime` 包，不依赖 Docker/testcontainers 的 package gate；`JUHE_AI_REQUIRE_INTEGRATION` 或 `JUHE_AI_REQUIRE_NODE_GO_BRIDGE` 任一开启时，Node/tsx 缺失都会失败而不是跳过。它已在 Windows 本机以强制依赖模式执行，普通目标测试和 `-race` 均通过。该测试只验证真实 Go client -> Node 生产 router，包括 loopback、HMAC-SHA256、`activation/configuration` 和仅 `202` 成功；不验证真实 public HTTP 写入、worker 入队或最终状态。现有 `w1b-public-api-smoke` 注入 no-op dispatcher，不验证 bridge。

#### 6.1.1 PLAN-0081 模型目录 bridge 真实服务门禁

使用健康 Docker / testcontainers 环境强制运行：

```powershell
Set-Location backend-go
$env:JUHE_AI_REQUIRE_INTEGRATION = '1'
go test -v -tags=integration ./internal/testkit/integration -run '^TestW3ManagementProviderModelSnapshotNodeBridgePostgresRedisSmoke$' -count=1 -timeout=5m
```

该用例创建 fresh PostgreSQL、三套隔离 Redis 并执行当前全部 Goose migration，然后导入真实 `backend/src/server.ts`。Node 使用生产 schema gate、strict internal route、模型目录 snapshot reconciler 和优雅退出钩子；测试 process role 固定为 `worker`，不启动 DB service / background worker supervisor。用例再启动真实 Go `app.RunServer` listener，确认正确 secret 时 `/__aisys__/readyz` 报告 `nodeModelCatalogBridge=ok`，错误 secret 在监听前以 `unauthorized` 失败，并检查失败端口从未打开。

2026-07-20 已通过 SSH loopback Docker socket tunnel 在远端 Docker `29.1.3`、PostgreSQL `18-bookworm`、Goose `63`、Redis `8.2.7-bookworm` 上以强制模式执行为 `PASS`，不是 `SKIP`；临时容器、SSH tunnel 和远端 `socat` 均已清理。该证据不覆盖完整 Node supervisor 拓扑、前端真实 management smoke、生产 single-owner、反向代理切流、回滚或 Node 删除。

### 6.2 PLAN-0081 真实 Go 管理 smoke

W5 系统运行设置当前 catalog 以 53 项和 `000043` 删除历史倍率设置为准；本节其他旧批次记录中的 55 key / 旧倍率只代表当时 schema 事实，不得作为当前 API 或 migration 验收口径。

该 smoke 面向已经启动并显式挂载管理 API 的真实 Go HTTP 服务，同时覆盖 W2 外部来源列表 / 详情 / 可选 Token secret / 可选 PATCH / 可选 DELETE、W2 账户 `test-options` 可选只读、W5 API Key 管理 / 自助列表只读、W5 分组只读 / 可选 mutation、W5 策略路由列表 / 详情只读，以及 W6 公开接口日志列表 / 可选详情和客户端 IP 列表只读。命令如下：

```powershell
# 仓库根目录代理
pnpm smoke:plan0081-real-go-management
pnpm test:plan0081-real-go-management-smoke

# 前端 workspace 原始命令
pnpm --filter juhe-ai-frontend smoke:plan0081-real-go-management
pnpm --filter juhe-ai-frontend test:plan0081-real-go-management-smoke
```

`smoke:plan0081-real-go-management` 是真实 URL / Cookie / listener HTTP smoke；`test:plan0081-real-go-management-smoke` 是本地 mock regression。根目录和前端 workspace 命令代理均已落地。

W5 系统运行设置写入使用独立的 loopback listener smoke，不复用远程管理 smoke：

```powershell
# 先启动仅用于本地隔离验证的 Go server，并确认管理端已显式 opt-in
$env:JUHE_REAL_GO_SETTINGS_WRITE_BASE_URL = 'http://127.0.0.1:8080'
$env:JUHE_REAL_GO_SETTINGS_WRITE_COOKIE = 'juhe_ai_session=...'
$env:JUHE_REAL_GO_SETTINGS_WRITE_ALLOW = '1'
$env:JUHE_REAL_GO_SETTINGS_WRITE_CONFIRMATION = 'plan0081-settings-write-v1:<base-url-sha256-prefix>'
pnpm smoke:plan0081-real-go-settings-write
pnpm test:plan0081-real-go-settings-write-smoke
```

该写 smoke 只允许 loopback（`localhost`、`127.0.0.0/8` 或 `::1`），并在客户端进程 `NODE_ENV=production` 时拒绝执行；这两个条件不能识别端口转发或本地 listener 背后的数据库，不能替代隔离环境确认。脚本先调用 Go `/__aisys__/api/readyz` 并校验服务身份，readyz 不发送管理 Cookie。它只临时修改 `systemMetricsHourlyRetentionDays`；当前值仍等于 smoke 临时值时才在 `finally` 恢复，检测到第三方并发值会失败且不覆盖。真实执行必须使用可销毁、独占写入的隔离 PostgreSQL / Redis、当前 Goose schema 和单独 Go listener；该脚本不会启动 ingest worker，因此不能单独证明 operation log 入库、浏览器页面或生产切流。

真实 smoke 必须显式提供以下环境变量，不从浏览器地址、Vite 配置、默认端口或本地登录态猜测：

```powershell
$env:JUHE_REAL_GO_MANAGEMENT_BASE_URL = 'http://127.0.0.1:8080'
$env:JUHE_REAL_GO_MANAGEMENT_COOKIE = 'juhe_ai_session=...'

# 可选：管理员代某个系统账户验证；省略时使用当前 session 的默认管理作用域
$env:JUHE_REAL_GO_MANAGEMENT_SYSTEM_ACCOUNT_ID = 'sys_...'

# 可选：只读模式选择既有 owner 非默认分组；绝不作为写入目标
$env:JUHE_REAL_GO_MANAGEMENT_GROUP_ID = 'grp_...'

# 可选：固定只读策略路由详情目标；空列表时可不设置
$env:JUHE_REAL_GO_MANAGEMENT_ROUTE_STRATEGY_ID = 'route_...'

# 可选：验证已保存账户的只读 test-options；不会提交人工测试
$env:JUHE_REAL_GO_MANAGEMENT_ACCOUNT_ID = 'acc_...'

# 可选：固定公开接口日志详情目标；省略时只验证列表，不从列表自动选择敏感 payload
$env:JUHE_REAL_GO_MANAGEMENT_PUBLIC_API_LOG_ID = 'publog_...'

# 可选：必须同时提供，才 reveal 指定外部来源的指定 Token secret；只提供一个会在 HTTP 前失败
$env:JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_ID = 'source_...'
$env:JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_TOKEN_ID = 'token_...'

# 可选写入：只能使用外部预置、专属且可恢复的临时来源 fixture；三项必须同时提供
$env:JUHE_REAL_GO_MANAGEMENT_ALLOW_EXTERNAL_INTEGRATION_SOURCE_MUTATIONS = '1'
$env:JUHE_REAL_GO_MANAGEMENT_TEMP_EXTERNAL_INTEGRATION_SOURCE_ID = 'source_plan0081_fixture_...'
$env:JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_MUTATION_FIXTURE_CONFIRMATION = 'plan0081-external-source-fixture-v1:source_plan0081_fixture_...'

# 可选不可恢复写入：只能删除外部预置、专属、tokenless 且带精确 marker 的临时来源；三项必须同时提供
$env:JUHE_REAL_GO_MANAGEMENT_ALLOW_EXTERNAL_INTEGRATION_SOURCE_DELETES = '1'
$env:JUHE_REAL_GO_MANAGEMENT_TEMP_EXTERNAL_INTEGRATION_SOURCE_DELETE_ID = 'source_plan0081_delete_fixture_...'
$env:JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_DELETE_FIXTURE_CONFIRMATION = 'plan0081-external-source-delete-fixture-v1:source_plan0081_delete_fixture_...'

# 可选：写入模式指定临时分组供应商，以及覆盖单请求超时
$env:JUHE_REAL_GO_MANAGEMENT_GROUP_PROVIDER_CODE = 'openai'
$env:JUHE_REAL_GO_MANAGEMENT_TIMEOUT_MS = '15000'

pnpm smoke:plan0081-real-go-management
```

默认同时关闭分组 mutation、外部来源 PATCH 和外部来源 DELETE；只提供任一临时来源 ID 或确认值但未显式启用对应开关时，脚本会在首个 HTTP 请求前失败。

默认不设置任何 mutation / delete 开关时，smoke 只能执行只读检查：外部来源第一页和可选非内置来源详情、公开接口日志 `page=1&pageSize=20` 列表、API Key 管理 / 自助第一页列表、分组列表、所选 owner 非默认分组详情、策略路由列表和可选详情、供应商 options、模型 options、可选账户 `test-options`，以及 `GET /__aisys__/api/ip-stats?page=1&pageSize=20&sortField=requestCount&sortOrder=desc`。API Key 管理列表在配置 `JUHE_REAL_GO_MANAGEMENT_SYSTEM_ACCOUNT_ID` 时附带该 owner query，自助列表永不附带；两者校验 progressive `total/hasMore`、字段白名单和敏感字段递归禁入，管理列表必须包含 owner，自助列表必须省略 owner，仅允许列表项根级 `keyPrefix/keySuffix`。smoke 不请求 Node / Go 均不存在的 API Key 详情，也不调用 API Key secret。默认不会 reveal 外部来源 Token secret；只有同时配置 `JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_ID` 与 `JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_TOKEN_ID` 才发送 `GET /external-integration-sources/{id}/tokens/{tokenId}/secret`，任一变量单独存在都会在首个 HTTP 请求前失败。前端 UI 使用的 detail / `tokenSecret` / update / delete API 都编码动态 source / token 路径段。成功摘要只输出计数和 `externalIntegrationSourceTokenSecretChecked`、`externalIntegrationSourcePatchChecked`、`externalIntegrationSourceDeleteChecked` 等布尔状态，不输出明文密钥、Token 或 fixture ID；失败时也不把 raw Axios error 写到控制台。公开日志列表校验摘要 DTO、progressive `total/hasMore` 和 `no-store`，不接受 `requestData/responseData`；只有配置 `JUHE_REAL_GO_MANAGEMENT_PUBLIC_API_LOG_ID` 才发送详情 GET，并校验 ID、两个 payload 对象和 `no-store`。配置 `JUHE_REAL_GO_MANAGEMENT_ACCOUNT_ID` 后只允许发送 `GET /accounts/{id}/test-options`，并校验账户 ID、默认模型、模型协议、精确请求形态和首项默认值；禁止调用 `POST /accounts/{id}/test`。客户端 IP 列表必须校验 `rangeReady`、`pageUpperBound`、`hasMore`、规范化日期范围和行 DTO；`rangeReady=false` 时要求空列表、上界 0、`hasMore=false`。`JUHE_REAL_GO_MANAGEMENT_GROUP_ID` 只用于分组只读选择；无论是否启用 mutation，都禁止对该既有分组执行 `PATCH` 或 `DELETE`，客户端 IP 部分始终不执行策略 mutation。

只有显式设置下列门禁时才允许写入：

```powershell
$env:JUHE_REAL_GO_MANAGEMENT_ALLOW_GROUP_MUTATIONS = '1'
pnpm smoke:plan0081-real-go-management
```

mutation 模式必须使用 `randomUUID()` 生成唯一临时标识，创建名称带该标识的 `personal` 分组，并只把创建响应返回的服务端分组 ID 作为后续写目标。完整顺序固定为：

1. `POST` 创建临时 `personal` 分组。
2. `GET` 列表确认临时分组可见。
3. `GET` 详情确认 owner、provider 和 `groupType=personal`。
4. `PATCH` 修改临时分组名称和描述，将其切换为 `high_concurrency`，并写入 `defaultSoftConcurrency`、`maxQueueWaitMs`、`clientIpConcurrencyLimit`、`clientIpConcurrencyOverflowMode`、`imageLaneMaxConcurrency` 五项可写调度字段。
5. 校验 PATCH 响应，并再次 `GET` 详情确认名称、描述、分组类型和五项调度字段。
6. `DELETE` 删除该临时分组。
7. 再次 `GET` 详情确认返回 `404`。
8. `finally` 只按本次创建响应返回的临时 ID 做安全清理；已删除后的 `404` 视为清理完成，不能扩大到名称搜索结果、`JUHE_REAL_GO_MANAGEMENT_GROUP_ID` 或任何既有分组。

外部来源 PATCH 使用独立三重门禁，不从列表、只读来源 ID 或 Token secret 目标推断写入对象。目标详情必须同时满足 `isBuiltIn=false`、`tokenCount=0`、`activeTokenCount=0`、`tokens=[]`、名称以 `PLAN-0081 external source smoke fixture ` 开头、notes 精确等于 `PLAN-0081 external source smoke fixture`；否则首个 PATCH 前失败。空 Token 是硬门禁，因为来源更新会同步 Token 字段，而 smoke 无法恢复每个 Token 的独立历史值。脚本只对该显式 ID 写入带 `randomUUID()` 的临时 name/notes，校验 PATCH 响应和 GET 回读后，在同一次执行中恢复原 name/status/scopes/rateLimits/expiresAt/notes，并再次 GET 确认恢复。首个 PATCH 返回不确定错误时仍尝试恢复，主错误与恢复错误同时保留；fixture 必须由外部流程为本次运行唯一创建或独占，不能复用共享或生产来源。

外部来源 DELETE 使用另一组独立三重门禁，目标 ID 必须与 PATCH fixture ID、显式只读 / Token secret 来源 ID 不同，自动只读详情选择也会排除该 DELETE ID。首个 DELETE 前必须 GET 并确认 `isBuiltIn=false`、`tokenCount=0`、`activeTokenCount=0`、`tokens=[]`、名称以 `PLAN-0081 external source delete fixture ` 开头、notes 精确等于 `PLAN-0081 external source delete fixture`；任一不匹配都禁止 DELETE。成功必须返回 204、空 body、`Cache-Control: no-store`，随后同 ID GET 必须为 404。DELETE 不可恢复且不重试；若 DELETE 返回错误，脚本只做一次有界 GET 诊断并同时保留主错误与诊断结果，不把 404 伪造成成功。该 fixture 必须由外部流程为本次运行唯一创建，禁止共享或生产来源。

未设置 `JUHE_REAL_GO_MANAGEMENT_GROUP_PROVIDER_CODE` 时，脚本只能从真实 `providers/options` 响应中选择一个启用供应商；未设置 `JUHE_REAL_GO_MANAGEMENT_TIMEOUT_MS` 时使用脚本内默认超时。Cookie、Base URL、供应商代码、系统账户 ID、分组 ID、策略路由 ID、账户 ID、公开接口日志 ID、外部来源 ID、Token ID 和 Token 明文不得出现在成功摘要或错误日志中。

真实运行前必须具备可用的 Go HTTP 服务、fresh/current PostgreSQL、Redis state/cache/queue 依赖，以及与目标 Go 服务匹配的有效管理员 session；Go server 还必须实际启用 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 并挂载 W2 外部来源五条只读 GET、PATCH、DELETE，W5 `GET /api-keys` / `GET /my-api-keys`、分组 create/list/detail/update/delete、策略路由 list/detail、W6 `GET /public-api-logs`、可选 `GET /public-api-logs/{id}`、`GET /ip-stats`，以及配置账户 ID 时所需的 W2 `GET /accounts/{id}/test-options`。客户端 IP 预聚合窗口仍由 Node production writer / worker 生产；公开接口日志必须已按 fresh Goose 结构准备并由单一 writer 产生，不能把 Node integer/text 旧表直接交给 Go。当前本机未执行该真实 URL / Cookie / listener 命令，因此不记录真实 Go listener / Cookie / session 通过，也不构成生产流量切换、生产单 owner、回滚演练或 Node 路由删除证据。独立 Testcontainers PostgreSQL DELETE smoke 已真实通过，只证明隔离 PG / HTTP / session / 行锁 / 级联与日志保留边界，不替代目标部署 listener。

默认模式只验证读取结果；显式开启临时分组或外部来源 PATCH 时，只验证对应已迁移写路由及安全恢复。显式开启外部来源 DELETE 时只验证专属 tokenless fixture 的不可恢复删除边界。三者都不表示接管其他写接口或生产 owner。

本地非真实验证使用：

```powershell
pnpm test:plan0081-real-go-management-smoke
pnpm --filter juhe-ai-frontend typecheck
```

本地 regression 已使用 mock HTTP 固定外部来源列表 / 详情、默认不 reveal、双变量 reveal 门禁、单变量 HTTP 前失败、encoded source / token segment、精确 secret DTO、布尔摘要和敏感值 / raw Axios secret error 不进控制台，同时固定 API Key 管理 / 自助列表 URL、owner 显隐、空列表、progressive pagination、根级预览字段和嵌套敏感 key 禁入，公开接口日志列表摘要 / `no-store` / payload 隔离、可选详情 ID 与 GET-only 边界、策略路由列表 / 详情、可选账户 `test-options` 精确 DTO、客户端 IP 列表 ready / not-ready DTO、progressive `pageUpperBound`、HTTP 错误 / 超时和敏感值不进入错误文本；分组部分继续固定 mutation opt-in、`randomUUID` 临时资源、完整 `high_concurrency` PATCH、更新后回读、DELETE 后 404、`finally` 清理、清理 404 幂等、主错误与清理错误聚合，以及禁止写入既有 `JUHE_REAL_GO_MANAGEMENT_GROUP_ID`。上述 regression 和前端 typecheck 已通过，但不连接真实 Go、PostgreSQL、Redis 或 session，只能作为非真实验证。

外部来源 mutation regression 另固定默认零 PATCH、三重 fixture 门禁、编码 ID、PATCH 响应与 GET 回读、首 PATCH 失败后恢复、恢复后回读和 fixture marker 拒绝；它同样不替代真实 listener / Cookie / PostgreSQL 验证。

外部来源 DELETE regression 固定默认零 DELETE、独立三门禁、DELETE ID 与 PATCH / 显式只读目标隔离、自动只读详情排除、编码 ID、built-in / token-bearing / name / notes marker 拒绝、204 空 body / no-store、删除后 404、错误状态 / 缺 no-store / 删除后仍存在反例，以及 DELETE 失败后的单次 GET 诊断和敏感值禁入。它不连接真实 listener；真实 PostgreSQL 证据由 `TestW2ManagementExternalIntegrationSourceDeletePostgresSmoke` 单独提供。

#### 6.2.1 外部来源创建独立真实 Go smoke

外部来源创建是一次性返回明文 Token 的写接口，使用独立命令和目标绑定确认，不并入默认只读 management smoke：

```powershell
# 无真实依赖的 mock regression
pnpm test:plan0081-real-go-external-source-create-smoke

# 真实 listener 写入；Base URL 可带或不带 /__aisys__/api
$env:JUHE_REAL_GO_MANAGEMENT_BASE_URL = 'http://127.0.0.1:8080'
$env:JUHE_REAL_GO_MANAGEMENT_COOKIE = 'juhe_ai_session=...'
$env:JUHE_REAL_GO_MANAGEMENT_ALLOW_EXTERNAL_INTEGRATION_SOURCE_CREATES = '1'
$target = [Uri]($env:JUHE_REAL_GO_MANAGEMENT_BASE_URL.Trim())
$path = $target.AbsolutePath.TrimEnd('/')
if (-not $path.EndsWith('/__aisys__/api', [StringComparison]::Ordinal)) { $path += '/__aisys__/api' }
$builder = [UriBuilder]$target
$builder.Path = $path
$builder.Query = ''
$builder.Fragment = ''
$normalizedTarget = $builder.Uri.AbsoluteUri.TrimEnd('/')
$digest = [Security.Cryptography.SHA256]::HashData([Text.Encoding]::UTF8.GetBytes($normalizedTarget))
$targetHash = [Convert]::ToBase64String($digest).TrimEnd('=').Replace('+', '-').Replace('/', '_').Substring(0, 16)
$confirmation = "plan0081-external-source-create-v1:$targetHash"
if ([string]::IsNullOrWhiteSpace($confirmation) -or $confirmation -notmatch '^plan0081-external-source-create-v1:[A-Za-z0-9_-]{16}$') { throw '生成目标绑定确认值失败' }
$env:JUHE_REAL_GO_MANAGEMENT_EXTERNAL_INTEGRATION_SOURCE_CREATE_CONFIRMATION = $confirmation
pnpm smoke:plan0081-real-go-external-source-create
```

确认值绑定规范化后的 management API 目标，格式为 `plan0081-external-source-create-v1:` 加目标 SHA-256 的前 16 位 base64url；缺少 opt-in、缺少确认值或目标不匹配都会在 HTTP 前失败。脚本只创建本次 `randomUUID()` 命名且 `status=disabled` 的 marker 来源，不使用既有 fixture；POST 永不重试。POST 出现 `500`、非法 JSON / DTO 或传输错误时按提交结果不确定处理，通过精确 name + notes marker 有界恢复目标。

清理前每次都严格 GET 回读并核对 source ID、token ID、唯一名称、marker notes、disabled、非内置和 Token 关系；DELETE 传输错误或“204 后仍存在”只在再次通过同一回读门禁后有界重试。多条恢复匹配、marker 不符或关系不符均拒绝删除并报告清理失败。成功摘要和错误文本不输出 Cookie、Base URL、来源 / Token ID 或明文 Token；主错误与清理错误使用聚合错误同时保留。

`TestW2ManagementExternalIntegrationSourceCreatePostgresSmoke` 已在隔离 PostgreSQL 18、fresh schema `54` 下以强制 integration 模式真实通过，覆盖 201/no-store/Pragma、来源与首 Token 原子写入、生产 hash / AES-GCM 密文和三次真实 hash 唯一冲突零孤儿行。上述 mock regression 与前端 typecheck 已通过；当前未执行带真实目标 URL / Cookie 的 listener 命令，因此不记录真实部署 session、operation log ingest、生产单 owner、回滚或 Node 删除通过。

#### 6.2.2 内置测试 Token reset 独立真实 Go smoke

reset 不可回滚，只允许单实例操作可销毁隔离 fixture。该 smoke 强制 Base URL 使用 `localhost`、`127.0.0.0/8` 或 `::1`，远程 HTTP/HTTPS 地址都会在任何请求前失败；先运行无真实依赖回归：

```powershell
pnpm test:plan0081-real-go-external-source-reset-smoke
```

真实 listener 必须显式提供 loopback Base URL 和隔离管理员 Cookie。先运行只读 confirmation 命令；它只读取固定 `extsrc_builtin_test / exttok_builtin_test` 的 detail 与 secret，按 `plan0081-external-source-reset-v1`、规范化 management API URL、固定 source/token ID、旧 `tokenPrefix/tokenSuffix` 以换行连接并计算小写 SHA-256，只输出 `confirmationHash=...`，不会发起 POST：

```powershell
pnpm smoke:plan0081-real-go-external-source-reset-confirmation
```

确认目标确实是当前独立 Go listener 和可销毁 fixture 后，将上一步输出的 64 位 hash 写入 `JUHE_REAL_GO_MANAGEMENT_EXTERNAL_SOURCE_RESET_CONFIRMATION`，再显式设置 `JUHE_REAL_GO_MANAGEMENT_EXTERNAL_SOURCE_RESET_ENABLED=1` 并执行：

```powershell
pnpm smoke:plan0081-real-go-external-source-reset
```

confirmation 不匹配时允许读取旧 detail/secret 以计算当前事实，但必须在 POST 前失败。成功链固定为旧 detail/secret -> 空对象 reset POST -> 新 detail/secret，校验 48 字符 `juis_` 明文、preview、`no-store` / `Pragma` 与固定内置 ID；输出只包含布尔结果和 confirmation hash，不输出 Base URL、Cookie、prefix/suffix 或新旧 Token。同一 fixture 禁止并发运行两个 reset smoke；当前 API 没有 expected-preview compare-and-reset 契约。真实 PG/Redis/Asynq integration 命令为：

```powershell
Push-Location backend-go
try {
  $env:JUHE_AI_REQUIRE_INTEGRATION = '1'
  go test -v -tags=integration ./internal/testkit/integration -run '^TestW2ManagementExternalIntegrationSourceBuiltInResetPostgresRedisAsynqSmoke$' -count=1 -timeout=6m
  if ($LASTEXITCODE -ne 0) { throw "reset integration 失败：$LASTEXITCODE" }
} finally {
  Remove-Item Env:\JUHE_AI_REQUIRE_INTEGRATION -ErrorAction SilentlyContinue
  Pop-Location
}
```

#### 6.2.3 外部来源 Token 创建真实 PostgreSQL smoke

该测试使用已有来源新增 Token，完整执行 Go router、管理 session、handler、service、PostgreSQL 事务和凭据 codec：

```powershell
Set-Location backend-go
$env:JUHE_AI_REQUIRE_INTEGRATION = '1'
go test -v -tags=integration ./internal/testkit/integration -run '^TestW2ManagementExternalIntegrationSourceTokenCreatePostgresSmoke$' -count=1 -timeout=6m
$testExitCode = $LASTEXITCODE
Remove-Item Env:\JUHE_AI_REQUIRE_INTEGRATION -ErrorAction SilentlyContinue
if ($testExitCode -ne 0) { exit $testExitCode }
```

强制门禁下 Docker / Testcontainers 不可用必须失败，不能把 `SKIP` 记录为通过。测试覆盖 HTTP `201`、`no-store`、`Pragma: no-cache`、完整来源详情和 Token 计数、一次性 48 字符 `juis_` 明文、生产 domain-separated hash、Node 兼容 AES-GCM 密文回解、scope 排序、原来源与既有 Token 不变、missing / built-in 精确 `400` 无副作用，以及连续三次真实 `token_hash` 唯一冲突后目标来源和全库均无孤儿新增。

该测试已通过 SSH Docker tunnel 在隔离 PostgreSQL 18 容器中真实执行，fresh Goose schema 到 `54`。当前未覆盖来源删除与 Token 创建的并发序列化，也未执行真实部署 URL / Cookie listener、operation log ingest 或生产切流，因此这些门禁仍记为未完成。

### 6.3 PLAN-0081 W6 客户端 IP 白名单真实 Go smoke

该 smoke 只验证已迁移的 `allowlist` / `unallowlist` 两条真实 Go HTTP 写路径，只允许外部加锁、专属且可重建的隔离 fixture，禁止使用共享环境或现有生产 IP registry hash。当前服务端没有 expected-none / policy-ID CAS 和可查询幂等结果，脚本的目标确认不能证明 hash 无业务策略；默认 CI / 发布门禁只运行本地 regression。根目录命令如下：

```powershell
pnpm test:plan0081-real-go-client-ip-allowlist-smoke
pnpm smoke:plan0081-real-go-client-ip-allowlist
pnpm smoke:plan0081-real-go-client-ip-allowlist-cleanup-only
```

真实命令没有默认目标，必须显式提供完整管理 API Base URL、短期管理员 Cookie、专属 lowercase 64 位 registry hash、外部 fixture lease ID，以及绑定规范化 Base URL / hash / lease 的确认摘要。HTTP 只允许 loopback，其他目标强制 HTTPS。示例：

```powershell
$env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_BASE_URL = 'http://127.0.0.1:8080/__aisys__/api'
$env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_MANAGEMENT_COOKIE = 'juhe_ai_session=...'
$env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_DISPOSABLE_IP_HASH = '0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef'
$env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_EXTERNAL_FIXTURE_LEASE_ID = 'ci-lease-20260714-001'

$confirmationPayload = @(
  'plan0081-external-client-ip-allowlist-fixture-v1'
  $env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_BASE_URL
  $env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_DISPOSABLE_IP_HASH
  $env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_EXTERNAL_FIXTURE_LEASE_ID
) -join "`n"
$confirmationDigest = [Convert]::ToHexString(
  [Security.Cryptography.SHA256]::HashData([Text.Encoding]::UTF8.GetBytes($confirmationPayload))
).ToLowerInvariant()
$env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_EXTERNAL_FIXTURE_CONFIRMATION = "plan0081-external-fixture-v1:$confirmationDigest"
$env:JUHE_REAL_GO_CLIENT_IP_ALLOWLIST_TIMEOUT_MS = '15000' # 可选
pnpm smoke:plan0081-real-go-client-ip-allowlist
```

`allowlist` 会停用该 hash 的全部 active policy，因此外部 lease 必须保证 fixture 独占，且目标数据库可以直接重建或废弃。脚本只尝试一次 allowlist，严格校验 active allowlist DTO，要求第一次 unallowlist 的 `disabledCount=1`、第二次 `=0`。普通明确响应和 cleanup-only 使用 135 秒清理窗口；allowlist 发生传输错误、HTTP `408` 或任意 `5xx` 时视为提交不确定，至少守候 130 秒 mutation horizon，并把有效 deadline 延伸到尝试开始后至少 160 秒，再重新取得连续两次 `disabledCount=0`。cleanup 每次使用不同 reason、遵守 `Retry-After`，任何 `>1` 都标记 fixture 污染并失败。主错误和清理错误同时保留，每个业务成功响应必须为 `no-store`，反向代理错误先按状态分类，成功摘要不打印 Cookie、Base URL、hash、policy ID 或响应 body。

恢复时只运行 `pnpm smoke:plan0081-real-go-client-ip-allowlist-cleanup-only`，它复用相同目标绑定但不会再次调用 allowlist。由于当前服务端无法限定“只清理本次 policy ID”，cleanup-only 同样只能用于该隔离 fixture，不能用于共享或生产 hash。

本地 `pnpm test:plan0081-real-go-client-ip-allowlist-smoke` 使用 mock HTTP 固定 lease / 目标绑定、lowercase hash、非 loopback HTTP 拒绝、方法 / 路径 / body / header、唯一 mutation fingerprint、真实 AbortSignal 超时、cleanup 超时 / `429` 恢复、污染计数、连续两次零确认、cleanup-only、strict DTO、`no-store`、错误聚合和敏感值不进入错误文本；该回归与前端 typecheck 已通过。当前没有提供真实隔离 fixture 参数，因此真实 smoke 与 cleanup-only 都未执行，不记录真实 Go HTTP / PostgreSQL / Redis / session 通过，也不构成生产切流或 Node 路由删除证据。

## 7. 性能综合测试

需要评估本机模拟链路的 QPS、延迟、错误率和资源占用时，可以运行：

```powershell
pnpm test:perf
```

该命令会启动临时 SQLite 业务库、统计数据集目录库、使用记录目录库、usage shard 目录和统计结果库、本机模拟 OpenAI 上游和临时网关实例，并通过 Mockdata 共享夹具自动创建压测 GPT 分组、GPT API Key 类型账户和本地网关 API Key；测试结束后删除临时数据目录，不消耗真实 OpenAI 额度，也不污染 `backend/data/`。

默认覆盖 `/v1/models`、非流式 `/v1/responses` 和 `/v1/chat/completions` 三类 API Key / mock OpenAI-compatible 场景，按多个并发档位输出：

- `QPS` / `成功QPS`：总吞吐和成功请求吞吐。
- `错误率` / `statusCounts` / `errorCounts`：失败比例、HTTP 状态码和错误分类。
- `latencyMs`：`min`、`avg`、`p50`、`p90`、`p95`、`p99`、`max`。
- `eventLoopDelayMs`：事件循环延迟均值和分位，用于观察 Node 主线程压力。
- `cpu` / `memory`：本进程 CPU 时间、单请求 CPU 均值和内存变化。
- `usageRecordsDelta` / `auditLogsDelta`：本次压测写入临时 usage shard 的使用记录数量，以及写入临时统计数据集目录库的审计日志数量。
- `usageRecordQueueLength` / `auditLogQueueLength`：结束时队列是否清空。

常用调参示例：

```powershell
$env:JUHE_AI_PERF_SCENARIOS = "models,responses,chat,responses_stream"
$env:JUHE_AI_PERF_CONCURRENCY = "25,50,100"
$env:JUHE_AI_PERF_DURATION_SECONDS = "15"
$env:JUHE_AI_PERF_WARMUP_SECONDS = "3"
$env:JUHE_AI_PERF_UPSTREAM_LATENCY_MS = "20"
$env:JUHE_AI_PERF_UPSTREAM_BODY_BYTES = "1024"
$env:JUHE_AI_PERF_REPORT_PATH = "perf-report.json"
pnpm test:perf
```

`JUHE_AI_PERF_REPORT_PATH` 的相对路径按发起 `pnpm` 命令的目录解析，父目录会自动创建。例如在仓库根目录设置为 `reports/perf-limit-report-2026-05-17.json` 时，报告会写入 `<项目根目录>\reports\perf-limit-report-2026-05-17.json`。

可用环境变量：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `JUHE_AI_PERF_SCENARIOS` | `models,responses,chat` | 逗号分隔场景；可选 `models`、`responses`、`chat`、`responses_stream`，其中 `chat` 对应 API Key / mock OpenAI-compatible 的 `/v1/chat/completions` |
| `JUHE_AI_PERF_CONCURRENCY` | `10,25,50` | 逗号分隔并发档位 |
| `JUHE_AI_PERF_DURATION_SECONDS` | `6` | 每个并发档位正式压测秒数 |
| `JUHE_AI_PERF_WARMUP_SECONDS` | `1` | 每个档位预热秒数，不计入结果 |
| `JUHE_AI_PERF_REQUEST_TIMEOUT_MS` | `5000` | 客户端请求超时 |
| `JUHE_AI_PERF_UPSTREAM_LATENCY_MS` | `20` | 模拟上游首响应延迟 |
| `JUHE_AI_PERF_UPSTREAM_BODY_BYTES` | `512` | 模拟上游响应正文规模 |
| `JUHE_AI_PERF_UPSTREAM_ERROR_RATE` | `0` | 模拟上游错误率，范围 `0` 到 `1` |
| `JUHE_AI_PERF_ACCOUNT_COUNT` | `8` | 临时上游账户数量 |
| `JUHE_AI_PERF_ACCOUNT_CONCURRENCY` | `10000` | 单个临时账户并发上限 |
| `JUHE_AI_PERF_P95_TARGET_MS` | `1000` | 汇总里判断稳定容量的 p95 延迟阈值 |
| `JUHE_AI_PERF_REPORT_PATH` | 空 | 写入 JSON 报告的路径 |

结果边界：

- 这是本机模拟测试，用于评估网关代码路径、SQLite 写入、使用记录队列和审计采样的容量参考。
- 脚本不启动生产形态的独立 DB service / background worker 子进程；正式部署容量仍需在目标机器和目标网络下补测。
- 真实 OpenAI 上游会引入网络延迟、供应商限流、代理质量和真实模型耗时，不能直接用本机模拟 QPS 作为线上承诺。

## 8. 自动烟测

项目启动后执行：

```powershell
pnpm test:smoke
```

默认会走真实管理流程：先在脚本进程校验烟测管理员凭据并写入同库短时会话，再选择一个启用且可用的 GPT 账户，为该账户所在分组创建临时路由策略和临时本地 API Key，然后用这个 Key 请求 `/v1/models` 和 `/v1/responses`，最后删除临时 Key 和临时路由策略。脚本不会解析登录验证码图片，也不会为真实账号临时改绑分组，避免破坏账号已有分组绑定；HTTP 登录、验证码图片和初始改密边界由 `pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary` 覆盖，验证码生成限频由 `pnpm --filter juhe-ai-backend test:captcha-rate-limit` 覆盖。只有本地无可用真实账户且未指定 `JUHE_AI_SMOKE_ACCOUNT_NAME` 时，才会自动创建临时 mock 账号、临时分组、临时路由策略和临时 API Key，并在结束后清理。OAuth 账户会使用 Codex 兼容流式 `/v1/responses` 请求体，校验代理链路、流式完成事件和使用记录 token/cost 入库；OAuth smoke 不覆盖 `/v1/chat/completions`。

Codex 或其他 OpenAI 兼容客户端接入本地网关时，Base URL 填 `http://localhost:3000`、`http://127.0.0.1:3000`、`http://localhost:3000/v1` 或 `http://127.0.0.1:3000/v1` 都可以；API Key 只填 API Key 页面展示的 `sk-...` 明文，不要带 `Bearer ` 前缀，也不要填写上游 OpenAI / 中转供应商的账号密钥。客户端会自动发送 `Authorization: Bearer <API Key>`，如果填错 Key 会返回 `Invalid API key`，如果没有 Bearer 头会返回 `Missing bearer token`。

网关会缓存已通过校验的本地 API Key，重复请求会刷新短 TTL，减少高频调用对 SQLite 的鉴权查询压力；禁用、删除或修改 API Key 会立即清理对应缓存，缓存最长 5 分钟必须重新查库。

需要固定测试账号或模型时编辑 `backend/.env`：

```dotenv
# 只影响 pnpm test:smoke，不影响正常启动和网关转发。
# 当前 smoke 写入脚本进程连接的业务库，JUHE_AI_BACKEND_URL 必须指向共享同一 JUHE_AI_DATABASE_PATH 和 JUHE_AI_SECRET 的本机后端。
JUHE_AI_BACKEND_URL=http://127.0.0.1:3000
JUHE_AI_SMOKE_ADMIN_USERNAME=admin
JUHE_AI_SMOKE_ADMIN_PASSWORD=admin
JUHE_AI_SMOKE_ACCOUNT_NAME=
JUHE_AI_SMOKE_MODEL=gpt-5.4-mini
JUHE_AI_SMOKE_PROMPT=只输出 OK
```

`JUHE_AI_SMOKE_ACCOUNT_NAME` 留空时自动选第一个启用并且可用的 GPT 账号；填写后必须和账户页里的账号名称完全一致。

## 9. 发布前固定验证清单

准备发布前，在项目根目录按顺序执行：

```powershell
pnpm typecheck
pnpm build
pnpm test:scope
pnpm --filter juhe-ai-backend test:auth-memory-maintenance-boundary
pnpm --filter juhe-ai-backend test:deduplication-maintenance-boundary
pnpm test:proxy-negative
pnpm --filter juhe-ai-backend test:db-service-system-api-boundary
pnpm --filter juhe-ai-backend test:db-service-system-api-http
pnpm --filter juhe-ai-backend test:db-service-http-proxy-boundary
pnpm --filter juhe-ai-backend test:db-service-role-boundary
pnpm --filter juhe-ai-backend test:runtime-config-env-override
pnpm --filter juhe-ai-backend test:account-list-visible
pnpm --filter juhe-ai-backend test:account-options-lightweight
pnpm --filter juhe-ai-backend test:group-options-lightweight
pnpm --filter juhe-ai-backend test:default-group-current-contract
pnpm --filter juhe-ai-backend test:system-account-options-lightweight
pnpm --filter juhe-ai-backend test:authorization-options-query-guard
pnpm --filter juhe-ai-backend test:team-proxy-list-pagination
pnpm --filter juhe-ai-backend test:system-team-fanout-boundary
pnpm --filter juhe-ai-backend test:api-key-list-query-guard
pnpm --filter juhe-ai-backend test:usage-record-list-query-guard
pnpm --filter juhe-ai-backend test:usage-record-list-catalog-window
pnpm --filter juhe-ai-backend test:log-list-query-guard
pnpm --filter juhe-ai-backend test:log-list-page-window
pnpm --filter juhe-ai-backend test:management-list-page-window
pnpm --filter juhe-ai-backend test:base-list-page-window
pnpm --filter juhe-ai-backend test:runtime-log-search-guard
pnpm --filter juhe-ai-backend test:query-values
pnpm --filter juhe-ai-backend test:record-maintenance-index-guard
pnpm --filter juhe-ai-backend test:runtime-snapshot-unavailable-contract
pnpm --filter juhe-ai-backend test:system-metrics-process-latest
pnpm --filter juhe-ai-backend test:account-single-read
pnpm --filter juhe-ai-backend test:system-account-single-read
pnpm --filter juhe-ai-backend test:api-key-single-read
pnpm --filter juhe-ai-backend test:group-single-read
pnpm --filter juhe-ai-backend test:system-team-single-read
pnpm --filter juhe-ai-backend test:resource-authorization-single-read
pnpm --filter juhe-ai-backend test:proxy-single-read
pnpm --filter juhe-ai-backend test:announcement-single-read
pnpm --filter juhe-ai-backend test:account-test-response-limit
pnpm --filter juhe-ai-backend test:diagnostic-task-boundary
pnpm --filter juhe-ai-backend test:account-test-limited-diagnostics
pnpm --filter juhe-ai-backend test:account-diagnostic-mock-ai
pnpm --filter juhe-ai-backend test:model-catalog
pnpm --filter juhe-ai-backend test:model-check-trusted-comparison
pnpm --filter juhe-ai-backend test:model-check-trusted-comparison-success
pnpm --filter juhe-ai-backend test:model-check-distribution-similarity
pnpm --filter juhe-ai-backend test:model-check-full-profile
pnpm --filter juhe-ai-backend test:model-check-unavailable-boundary
pnpm --filter juhe-ai-backend test:model-check-storage-sanitizer
pnpm --filter juhe-ai-backend test:model-check-strict-model-match
pnpm --filter juhe-ai-backend test:model-check-user-authorized-resource
pnpm --filter juhe-ai-backend test:model-checks-postgres-smoke
pnpm --filter juhe-ai-backend test:authorized-account-dispatch-query
pnpm --filter juhe-ai-backend test:authorized-account-admin-dispatch
pnpm test:group-scheduling-policy
pnpm test:high-concurrency-queue
pnpm --filter juhe-ai-backend test:account-concurrency-lane-index
pnpm test:client-ip-concurrency
pnpm --filter juhe-ai-backend test:client-ip-error-circuit
pnpm --filter juhe-ai-backend test:client-ip-policy-hit-buffer
pnpm --filter juhe-ai-backend test:gateway-client-ip-account-avoidance
pnpm --filter juhe-ai-backend test:client-ip-stats
pnpm --filter juhe-ai-backend test:authorization-quota-batch
pnpm --filter juhe-ai-backend test:authorization-usage-pagination
pnpm --filter juhe-ai-backend test:resource-authorization-expiry-batch-boundary
pnpm --filter juhe-ai-backend test:account-usage-query-guard
pnpm --filter juhe-ai-backend test:account-quality-refresh
pnpm --filter juhe-ai-backend test:account-quality-gateway-status
pnpm --filter juhe-ai-backend test:cooldown-retest-recovery
pnpm --filter juhe-ai-backend test:group-account-stats-dirty-cache
pnpm --filter juhe-ai-backend test:deleted-api-key-aggregation-cleanup
pnpm --filter juhe-ai-backend test:openai-oauth-credentials-patch
pnpm --filter juhe-ai-backend test:openai-oauth-token-response-boundary
pnpm --filter juhe-ai-backend test:openai-oauth-refresh-activation
pnpm --filter juhe-ai-backend test:openai-oauth-access-token-refresh
pnpm --filter juhe-ai-backend test:table-monitor-default-scan
pnpm --filter juhe-ai-backend test:runtime-log-grep-path
pnpm --filter juhe-ai-backend test:audit-log-settings-contract
pnpm --filter juhe-ai-backend test:audit-log-retention
pnpm --filter juhe-ai-backend test:queue-failure-requeue-boundary
pnpm --filter juhe-ai-backend test:audit-payload-large-window
pnpm test:large-response
pnpm test:stream-first-output-timeout
pnpm --filter juhe-ai-backend test:response-inspection-policy
pnpm test:codex-client-strategy
pnpm --filter juhe-ai-backend test:openai-oauth-codex-adapter
pnpm --filter juhe-ai-backend test:usage-stats-batch-statement
pnpm --filter juhe-ai-backend test:usage-quota-hourly-window-config
pnpm --filter juhe-ai-backend test:gateway-runtime-cache
pnpm --filter juhe-ai-backend test:gateway-quota-passive-snapshot
pnpm --filter juhe-ai-backend test:gateway-dispatch-candidate-window
pnpm --filter juhe-ai-backend test:hybrid-gateway-mock-ai
pnpm --filter juhe-ai-backend test:hybrid-real-gateway-e2e
pnpm --filter juhe-ai-backend test:hybrid-real-model-probe
pnpm --filter juhe-ai-backend test:hybrid-quality-comparison
pnpm --filter juhe-ai-backend test:hybrid-project-workflow
pnpm --filter juhe-ai-backend test:external-source-auth
pnpm --filter juhe-ai-backend test:usage-stats-shard-fanout-boundary
pnpm --filter juhe-ai-backend test:client-ip-stats-shard-fanout-boundary
pnpm --filter juhe-ai-backend test:data-retention-cleanup-catalog-window
pnpm --filter juhe-ai-backend test:data-retention-sql-guards
pnpm test:smoke:mock
pnpm test:session-affinity
pnpm test:perf
pnpm test:smoke
```

检查边界：

- `pnpm test:scope` 只验证普通用户、管理员用户侧和管理员管理侧接口边界；它同时固定普通用户页面会用到的轻量辅助接口可访问，以及 `my-*` 命名空间下保留管理员能力的显式例外会返回 403。该脚本使用临时库和脚本种子数据，不等同于真实 OpenAI 网关 smoke。
- `pnpm --filter juhe-ai-backend test:auth-memory-maintenance-boundary` 验证验证码挑战、验证码生成限频和登录失败限频的进程内状态维护只做固定小批量清理，容量淘汰不展开全部 Map 排序，单个限频 key 的时间戳数组也不会超过阈值所需窗口。
- `pnpm --filter juhe-ai-backend test:captcha-rate-limit` 验证公开验证码接口按客户端 IP 限制生成频率，超限请求在生成 PNG 前返回 429，且不淘汰同一 IP 已发出的未提交验证码。
- `pnpm --filter juhe-ai-backend test:auth-captcha-environment-guard` 验证开发、测试和生产环境可通过 `JUHE_AI_AUTH_CAPTCHA_DISABLED=true` 临时关闭验证码并继续使用账号密码创建正式会话，错误密码仍被拒绝；`pnpm --filter juhe-ai-frontend test:login-captcha-mode` 验证登录页按后端返回的验证码能力隐藏表单且只在需要时提交验证码字段。开关默认关闭，开启时服务启动日志必须输出 `auth_captcha_disabled` warn，排障后恢复 `false`。
- `pnpm --filter juhe-ai-backend test:system-api-rate-limit` 验证 Node `/__aisys__/api` 后台接口限流位于 JSON body parser 前，阈值配置可从系统设置读取，IP 级和登录用户级超限会返回 `429` 与 `Retry-After`，健康检查不受影响，两层 limiter 都检查 client IP allowlist，整体开关字段不能通过系统设置写入。该脚本是 Go 对齐时必须保留的 Node 对照证据，不代表 Go 已覆盖 allowlist bypass 或 authenticated 404。
- `pnpm --filter juhe-ai-backend test:system-api-temporary-parity` 验证临时发布与正式环境保持相同 API 语义：遗留 `JUHE_AI_SYSTEM_API_READ_ONLY` 配置不能拦截管理、页面变更确认或 Public API 的自身认证链。
- `pnpm --filter juhe-ai-backend test:deduplication-maintenance-boundary` 验证管理端写接口防重复提交缓存满载时只做固定批量过期清理，容量淘汰不展开全部 Map 排序，且不同 key 不会被误拦截。
- `pnpm test:proxy-negative` 验证停用代理、网关 503、失败使用记录和使用中代理不可删除等负向场景；它使用临时库和假上游，不替代真实 OpenAI + 代理链路。
- `pnpm --filter juhe-ai-backend test:db-service-system-api-boundary`、`test:db-service-system-api-http` 和 `test:db-service-http-proxy-boundary` 验证系统管理 / 公开系统 API 由 DB service 承载，主进程不直接挂载管理路由、不解析管理 API body，DB service 内部 system API 可读取公开设置，系统 API JSON 请求体保持 `256KB` 上限，主进程到 DB service 的 HTTP 代理有并发上限和内部超时，并固定 DB service 父进程 IPC 通过优先级队列处理，维护写低优先级且不能连续占住事件循环。
- `pnpm --filter juhe-ai-backend test:db-service-role-boundary` 验证只有 db-service 角色可本地执行内部 DB 操作，worker 和没有 DB service 子进程的 server 都必须拒绝本地同步执行。
- `pnpm --filter juhe-ai-backend test:sqlite-writer-boundary` 验证业务库、数据集目录库、统计结果库和 usage shard 的单写者 owner、运行时默认 strict、非 owner `query_only` 写入拒绝、模型检测 dataset writer 源码边界，以及 server -> ingest-worker dataset writer 动态转发。
- `pnpm --filter juhe-ai-backend test:background-ipc-protected-queue` 验证 server / DB service 侧记录类写入只进入对应 worker IPC 队列，不在 IPC 不可用或队列满时回退到本地 SQLite 写队列；同时覆盖 ingest 内 `recordMaintenance` 低优先级容量组与运行日志等高优先级 regular 组隔离。
- `pnpm --filter juhe-ai-backend test:runtime-config-env-override` 验证后端同名进程环境变量优先于 `backend/.env`，便于临时端口、隔离库和部署平台覆盖。
- `pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-runtime-state` 验证 performance 模式下网关运行态、授权额度和 API Key 额度缓存失效会写入 Redis runtime state 版本，server 网关 / 配额读取入口按 1 秒节流同步版本并触发本地 handler，且 API Key 额度失效保留定点 `apiKeyId`。
- `pnpm --filter juhe-ai-backend test:redis-stream-queue` 验证高性能模式 `JUHE_AI_QUEUE_DRIVER=redis_stream` 默认启用、Redis Stream `XREADGROUP` / `XAUTOCLAIM` 解析，使用记录、审计日志、操作日志、公开接口日志和数据维护队列写入 Stream 后由 consumer ack，落库失败保持 pending，审计 payload Buffer 通过 base64 标记安全恢复，PG 审计 Stream 单批限制为 25，以及 server / DB service / 非 ingest 角色才走 Redis Streams 入队；普通运行日志不属于 Redis Stream 队列，由 `runtime-log-file-import` 从角色 JSONL 文件消费。该脚本不连接真实 Redis。
- `runtime-log-hot-path-boundary` 固定业务入口不包含逐行运行日志解析、正则、脱敏、SHA-256、索引 DTO、runtime-log Redis producer 或 `RuntimeLogIndexStream`；`runtime-log-file-consumer*` 固定 cursor 提交顺序、超长行、轮转保护、截断代次和目录扫描公平性；`runtime-log-redis-removal` 固定 runtime-log 专属 Redis producer/consumer、IPC line message 和有损本地队列均已删除。
- `runtime-log-file-consumer-pressure` 在消费暂停后制造多角色文件 backlog，要求完整追平、稳定 ID 无重复且 cursor 到达文件尾；`runtime-log-postgres-consumer-smoke` 用唯一 marker 写入真实 PostgreSQL，验证行和 cursor 后清理测试数据，并断言隔离 namespace 下旧 runtime-log Stream 长度不变。
- `pnpm --filter juhe-ai-backend test:performance-boundary` 聚合高性能模式隔离、no-SQLite fallback、Redis-only 静态门禁、repository Redis shared cache 和 storage adapter 边界。
- `pnpm --filter juhe-ai-backend test:performance-redis-boundary` 验证高性能模式缓存 / 运行态 / 队列全局静态门禁：performance driver 必须是 `redis` / `redis` / `redis_stream`，`SharedJsonCache`、`RuntimeStateStore`、账号并发槽、高并发分组队列、Client-IP 并发、OAuth session / refresh lock / backoff、公开模型限流、系统 API 限流和记录类队列必须在 Redis driver 下走 Redis / Redis Streams；新增 `createAppCache` 调用必须被归类为 Redis shared cache 的本地 L1 或明确的进程内易失优化，不能把 memory 当跨进程事实源。该脚本只读源码，不连接真实 Redis。
- `pnpm --filter juhe-ai-backend test:performance-no-sqlite-fallback` 验证 PostgreSQL / performance 模式下 SQLite business / dataset / stats / usage shard / Responses bridge state 入口全部 fail-fast，且 PG 使用记录写入只记录逻辑 shard location，不访问本地 SQLite shard 路径。
- `pnpm --filter juhe-ai-backend test:performance-mode-isolation` 验证高性能模式隔离静态门禁：API Key 删除不能因 PostgreSQL 模式跳过记录维护投递，record-maintenance / data-retention usage 清理必须调用 PG-aware 异步入口，已删除 API Key / AI 账户关联记录清理、审计 payload blob 清理和审计保留清理必须提供 PG 分支，SQLite 同步入口在 PostgreSQL 模式 fail-fast。
- `pnpm --filter juhe-ai-backend test:storage-adapter-boundary` 验证存储适配接口收敛清单：DB / Redis / 队列直接调用点必须全部归类，运行态源码不能值导入 `node:sqlite`，不能绕过 `shared/redis-client.ts` / `storage/postgres-client.ts` 直接加载 `redis` / `pg` 驱动，并持续固定网关、DB service、HTTP routes 和 SQLite-only helper 的真实存储边界。已清理的无消费者 `backend/src/storage/runtime/*` descriptor 骨架不属于运行时契约；未来必须在真实消费者与业务 Port 契约确定后再新增 adapter。需要行级清单时设置 `JUHE_STORAGE_ADAPTER_BOUNDARY_PRINT_HITS=1` 后再运行。
- `pnpm --filter juhe-ai-backend test:account-list-visible` 验证临时种子账户在管理员全量、管理员按系统账户筛选和用户侧账户列表中都可见，并固定页码越界时 `total > 0` 的前端回退契约。
- `pnpm --filter juhe-ai-backend test:account-options-lightweight`、`test:group-options-lightweight`、`test:default-group-current-contract`、`test:system-account-options-lightweight`、`test:authorization-options-query-guard`、`test:team-proxy-list-pagination` 和 `test:system-team-fanout-boundary` 验证远程下拉 / 团队列表 / 代理列表只返回轻量或分页结果，支持精确 / 前缀搜索、通配符转义、默认上限和权限作用域，并固定默认分组只认 `is_default`，不按固定名称或最新分组兜底，授权目标分组选项读取和团队账号授权展开都不会自动补建缺失默认分组；其中 `test:system-account-options-lightweight` 固定系统账户 options 只返回 `id/username/displayName/status`，`ids` 用于回填，`keyword` 只查用户名 / 显示名精确或前缀，普通用户 403，不读取密码、角色、改密、最后登录或创建 / 更新时间；`test:authorization-options-query-guard` 固定 Node authorization grantee accounts / teams / groups 的轻量字段、`ids` 回填、前缀搜索、`%` 字面量和目标分组不自动补建默认分组；`test:group-options-lightweight` 覆盖 Node `groups/account-options` / `my-groups/account-options` 只返回轻量分组摘要加 `accountIds`，授权分组不泄漏成员账号 ID；系统团队成员数、单次加成员数和团队有效授权展开数都有固定上限，避免管理写请求在 DB service 里同步线性展开。Go W2 对应 smoke 包括 `go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementSystemAccountOptionsPostgresSmoke -count=1`、`go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthorizationGranteeAccountsPostgresSmoke -count=1`、`go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthorizationGranteeTeamsPostgresSmoke -count=1`、`go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthorizationGranteeGroupsPostgresSmoke -count=1` 和 `go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementGroupAccountOptionsPostgresSmoke -count=1`；其中 Go authorization smoke 当前已覆盖 grantee accounts / grantee teams / grantee groups，授权来源 / grant / 授权写接口仍未迁移。
- Go W5 代理管理写接口真实 PostgreSQL smoke：`go test -v -tags=integration ./internal/testkit/integration -run TestW5ManagementProxyCRUDPostgresSmoke -count=1`。该用例执行 migration 后通过管理端 session 请求创建、更新和删除代理，验证 Node 兼容 AES-256-GCM 密文可解、连接参数变化清空检测缓存、绑定账户时删除返回 `409`、解绑后删除成功；不执行真实外网代理检测。
- Go W5 代理手动检测目标测试：`go test -race ./internal/modules/managementproxies ./internal/httpapi ./internal/store/postgres -count=1`。该测试使用 fake prober 和本地 httptest HTTP proxy，覆盖 `POST /__aisys__/api/proxies/{id}/test` 的 `ProxyTestReport`、状态写回、出口信息保留语义、诊断闸门、`proxies.test` 操作日志、HTTP proxy 路径和 512KB 响应体上限；常规测试不依赖公网代理或真实外网出口服务。
- Go W5 管理端全局品牌设置读写目标测试：`go test ./internal/modules/managementsettings ./internal/modules/gatewaycache ./internal/httpapi ./internal/app ./internal/store/postgres -count=1`。已有记录覆盖 `GET/PATCH /__aisys__/api/settings/global` 默认关闭 404、admin / super_admin、普通 user 403、GET read auth 不 touch、PATCH write auth touch、read / write 两层限流、`256 KiB` / `413`、strict partial update、稳定 PostgreSQL 事务、`settings:global` shared cache version、`settings.update_global` operation log、`no-store` 和精确 DTO。`TestW5ManagementGlobalSettingsPatchPostgresRedisSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实 PostgreSQL / Redis / HTTP 通过；前端真实 Go 后端 smoke、生产切流和 Node 删除仍未完成。
- Go W5 管理端系统运行设置目标测试：`go test ./internal/systemsettings ./internal/modules/managementsettings ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`。现有非容器测试固定 55 key catalog，覆盖 `gptPriorityPriceMultiplier=2`、`gptFlexPriceMultiplier=0.5`、`0.01..100` decimal JSON number 校验与普通数字规范化、前端 `usageHotWindowRefreshIntervalSeconds` 对齐、PATCH `256 KiB` / `413`、parser 位于 IP limiter 后且 auth / touch / authenticated user limiter 前、GET read auth 不 touch、PATCH touch、PostgreSQL 固定有界读取、`ORDER BY key ASC FOR UPDATE`、稳定 key 更新、完整 snapshot、migration `000024` 与增量 seed migration `000034`、PostgreSQL 在线禁改 `usageStatsTimezone`、`settings:system` / `settings_updated` 双失效和 `settings.update` / `action=update_settings` operation log decimal 数值输出。真实依赖 smoke 已在隔离环境有 `PASS` 记录；loopback listener 写 smoke 另见 6.2，二者都不代表生产接管。
- Go W5 管理端 API Key 创建、更新、完整密钥查看与刷新目标测试：在 `backend-go` 下运行：

```powershell
go test ./internal/modules/managementapikeys ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test -race ./internal/modules/managementapikeys ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
go test -v -tags=integration ./internal/testkit/integration -run '^TestW5ManagementAPIKeyUpdatePostgresRedisSmoke$' -count=1
```

前两条固定管理 / 个人 create/update/secret/refresh 的 owner scope、strict create / partial update JSON、AES-GCM JSON 密文、密文缺失 / 错误通用 500、update / refresh 行锁事务、hash/prefix/suffix/ciphertext 同步、admin owner 字段显隐、self owner 隐藏、`256 KiB` JSON transport、middleware 顺序、GET read auth 不 touch、写操作 auth touch、`Cache-Control: no-store` / `Pragma: no-cache`、validation cache 必需失效、gateway runtime / quota best-effort，以及不泄露密钥的 operation log。第三条只验证 integration 测试包可编译；第四条才使用 Testcontainers 启动 PostgreSQL 与 Redis，并运行真实 Asynq ingest worker，额外验证 update sparse / route / schedule / quota / duplicate / committed-error、validation/runtime/quota Redis 状态、operation log、失败 trace 零日志和完整 key 不进入持久化日志或测试失败诊断。本机 Docker 不可用时输出 `SKIP` 只能记为未执行，不能写成真实 PostgreSQL / Redis / Asynq 通过。

refresh 成功响应包含一次性完整 key，测试失败信息、shell 输出、前端 smoke 摘要和文档不得打印响应 body。validation cache 失效在数据库提交后使用脱离客户端取消的 5 秒有界 context；该失效失败会返回 500，但不能据此假设数据库回滚或旧 key 仍有效。生产 smoke 必须继续验证刷新后旧 key 被拒绝、新 key 可调用，并准备缓存失效失败的告警和人工恢复步骤。

该切片不包含 API Key delete、策略路由编辑或生产接管。完整范围见 [W5 管理端 API Key 密钥生命周期迁移记录](../migration/W5-管理端APIKey密钥生命周期迁移记录.md)。
- Go W5 管理端策略路由创建目标测试：在 `backend-go` 下运行：

```powershell
go test ./internal/modules/managementroutestrategies ./internal/modules/publicroutestrategies ./internal/store/postgres ./internal/httpapi ./internal/app -count=1
go test -race ./internal/modules/managementroutestrategies ./internal/modules/publicroutestrategies ./internal/httpapi -count=1
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
go test -v -tags=integration ./internal/testkit/integration -run '^TestW5ManagementRouteStrategyCreatePostgresSmoke$' -count=1
```

前两条固定管理 / 个人 owner 作用域、strict JSON 与字段 presence、五种模式当前 Node 创建规则、自有 / 有效授权分组、稳定事务锁、同 owner 名称唯一、管理 owner 字段显隐、提交后 `route_strategy_created` 失效和 `route_strategies.create` operation log。第三条只验证 integration 包可编译；第四条才启动真实 PostgreSQL，覆盖停用目标账户、五模式、授权跨 owner 分组、重复名称、持久化和失效。本机 Docker 不可用时输出 `SKIP` 不计真实依赖通过。前端请求契约另运行 `pnpm test:route-strategy-create-api` 和 `pnpm --filter juhe-ai-frontend typecheck`。完整边界见 [W5 管理端策略路由创建迁移记录](../migration/W5-管理端策略路由创建迁移记录.md)。
- Go W5 管理端策略路由更新目标测试：在 `backend-go` 下运行：

```powershell
go test ./internal/modules/managementroutestrategies ./internal/modules/publicroutestrategies ./internal/store/postgres ./internal/httpapi ./internal/app -count=1
go test -race ./internal/modules/managementroutestrategies ./internal/modules/publicroutestrategies ./internal/httpapi -count=1
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
go test -v -tags=integration ./internal/testkit/integration -run '^TestW5ManagementRouteStrategyUpdatePostgresSmoke$' -count=1
```

前两条固定管理全局 / owner narrowing、个人 actor 作用域、strict partial JSON、完整独立 normal/hybrid 配置校验、五模式与授权分组、事务锁定 current snapshot、绑定整体替换、重复名称、错误优先级、提交失败零响应、`route_strategy_updated` 失效和 `route_strategies.update` operation log。第三条只验证 integration 包可编译；第四条才启动真实 PostgreSQL，覆盖管理端混合字段 PATCH、DB 行与 binding、API Key 绑定保持、重复名称 409、binding 插入中途失败全量回滚、跨 owner self 404、self 成功、session touch、失效和原始 operation-log payload。本机 Docker 不可用时输出 `SKIP` 不计真实依赖通过。前端创建与更新请求契约统一运行 `pnpm test:route-strategy-create-api` 和 `pnpm --filter juhe-ai-frontend typecheck`；跨进程 Node speed-first 运行态失效还需运行 Node runtime-state 专项回归和真实 Redis 互操作。完整边界见 [W5 管理端策略路由更新迁移记录](../migration/W5-管理端策略路由更新迁移记录.md)。
- Go W5 管理端策略路由删除目标测试：在 `backend-go` 下运行：

```powershell
go test ./... -count=1
go test -race ./internal/modules/managementroutestrategies ./internal/httpapi ./internal/app ./internal/store/postgres -count=1
go vet ./...
go mod tidy -diff
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
go test -v -tags=integration ./internal/testkit/integration -run '^TestW5ManagementRouteStrategyDeletePostgresSmoke$' -count=1
```

前五条固定管理全局 / owner narrowing、个人 actor 作用域、无 body parser / mutation guard、写限流与 auth/touch 顺序、默认策略和 API Key 引用返回 400、事务锁读 SQL `FOR UPDATE` 门禁、binding 级联回滚、204 空响应、`route_strategy_deleted` 失效和 `route_strategies.delete` marker-only operation log。最后一条才启动真实 PostgreSQL，覆盖 admin/self 作用域、默认/API Key 保护、触发器失败回滚、运行态失效和原始日志 payload。本机 Windows Docker provider 不可用时输出 `SKIP`，不计真实 PostgreSQL 通过。前端创建、更新和删除请求契约统一运行 `pnpm test:route-strategy-create-api` 与 `pnpm --filter juhe-ai-frontend typecheck`。Node speed-first runtime/index CAS 非真实 Redis 回归已通过，真实 Redis URL 未配置，不能记录为真实 Redis 互操作通过。完整边界见 [W5 管理端策略路由删除迁移记录](../migration/W5-管理端策略路由删除迁移记录.md)。
- Go W6 统计窗口目标测试：`go test ./internal/modules/managementstats ./internal/httpapi ./internal/app -count=1`。该测试覆盖 `GET /__aisys__/api/stats/usage-window` 和 `GET /__aisys__/api/my-stats/usage-window` 的管理员 / self 权限、只读鉴权不 touch、跨 UTC 日期边界、DST 自然日、大小写不敏感 IANA 名称、Node 历史别名、Go-only `Local` / `Factory` 拒绝、60 秒配置缓存、缓存刷新 singleflight、固定 31 天窗口、`Cache-Control: no-store`、设置缺失 / 空白 / 非法时区和通用 500 脱敏；该路径只能读取系统设置，禁止扫描 `usage_records` 或实时聚合统计明细。已注册业务路由的 authenticated user read / write limiter 已在后续 W6 横向切片补齐，限流专项范围与剩余差异见 `../migration/W6-System-API限流对齐记录.md`。
- Go W6 公开接口日志管理读目标测试：在 `backend-go` 运行：

```powershell
go test ./internal/modules/publicapilog ./internal/modules/managementpublicapilogs ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test ./... -count=1
go test -race ./internal/modules/publicapilog ./internal/modules/managementpublicapilogs ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1

$env:JUHE_AI_REQUIRE_INTEGRATION = '1'
$integrationExitCode = 0
try {
  go test -v -tags=integration ./internal/testkit/integration -run '^TestW6ManagementPublicAPILogsGoWriterReaderPostgresSmoke$' -count=1
  $integrationExitCode = $LASTEXITCODE
} finally {
  Remove-Item Env:\JUHE_AI_REQUIRE_INTEGRATION -ErrorAction SilentlyContinue
}
if ($integrationExitCode -ne 0) {
  throw "公开接口日志 integration 失败，退出码：$integrationExitCode"
}
```

该矩阵固定 W1b query/body/response 捕获范围内原值、`Authorization` / Cookie / 来源 token 不进入 headers 快照、管理端列表不读取 payload、详情单行读取、admin-only、分页 / 筛选 / 404 / `no-store` / session 不 touch，以及 fresh Goose `boolean/bigint/timestamptz` 类型。integration 不新增索引，只对既有 created/source 常用索引执行 `EXPLAIN`；trace/clientIp 等筛选没有本批 `EXPLAIN` 证据。2026-07-14 Node `pnpm test:public-api-logs`、Go targeted/full/targeted race/integration compile 已通过；强制真实用例因 Windows Docker provider 不可用退出 `1`，只证明 strict gate 有效，不记录真实 PostgreSQL、类型断言或 EXPLAIN 通过。生产切流前还必须离线同步 Node PostgreSQL integer/text 旧结构并确定单写 owner，runtime 不做兼容。
- Go W6 system API 限流目标测试：在 `backend-go` 先执行非 Docker 门禁 `go test ./internal/store/postgres -run TestW1PublicSettingsMigrationSeedsAllSystemAPIRateLimits -count=1` 和 `go test ./internal/httpapi -run 'Test(RouterSystemAPIRateLimit|SystemAPIRateLimit|RedisSystemAPIRateLimit)' -count=1`，分别固定 fresh DB migration 六项默认值 `600/120/180/40/300/120`，以及同进程 PATCH 清缓存、Redis `settings:system` version 跨 runtime 立即刷新、同一请求四类 bucket 共用快照、并发 clear/version 竞态和 60 秒 TTL 兜底；再执行 `go test ./internal/httpapi ./internal/store/postgres ./internal/app -count=1` 和 `go test -race ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`。具备 Docker / testcontainers 时补充执行 `go test -v -tags=integration ./internal/testkit/integration -run TestW0PostgresMigrationSmoke -count=1`，用于验证 migration 真实 apply，不是默认值的唯一验证入口。该矩阵同时固定 IP read / write 的 60 秒 minute + 10 秒 burst、用户 read / write 的 60 秒 fixed-window、health 唯一跳过、captcha 纳入、`/auth/*` 与 `settings/public` 跳过用户层、Redis / 内存实现、生产两层 Redis 注入、`429 Retry-After` 和中文文案。该验证只覆盖已迁移、已注册管理业务路由：Node 全局 `requireAuth` 后的 authenticated 404 会进入用户 limiter，而 Go 未注册路由不会进入该层；客户端 IP 列表和四条 policy 写路径已经进入 Go opt-in，但真实 Redis/Node version 互操作、IP 详情、统计生产 writer / worker、生产切流和 Node 删除仍需独立证据，因此不得写成 100% Node 等价。
- Go W6 管理端客户端 IP 策略写接口目标测试：在 `backend-go` 先运行 `go test ./... -count=1`、`go test -race ./internal/httpapi ./internal/app ./internal/modules/managementclientippolicies ./internal/modules/gatewaycache -count=1`、`go vet ./...`、`go mod tidy -diff` 和 `go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`；具备 Docker / testcontainers 时必须设置 `JUHE_AI_REQUIRE_INTEGRATION=1`，再运行 `go test -v -tags=integration ./internal/testkit/integration -run '^TestW6ManagementClientIPPolicyPostgresRedisAsynqSmoke$' -count=1`，避免 provider 不健康时 `SKIP` 仍以零退出码被自动门禁误判。非 Docker 门禁固定 allowlist / unallowlist / blacklist / unblock 四条 POST 的 admin-only、strict JSON、ECMAScript trim、500 UTF-16 单元上限、blacklist duration / Node 数值语义、写鉴权 / session touch / 两层限流 / mutation guard 顺序、PostgreSQL 行锁事务、单 IP 唯一 active policy、两类 `disabledCount=0`、提交后 `gateway:client-ip-policy-by-ip` 共享缓存版本失效、operation log 和 `no-store`。integration production-component smoke 使用真实 PostgreSQL store、Redis version reader / 两层 limiter 和 Asynq ingest worker，覆盖策略替换、共享版本、registry 行锁等待、session touch 和系统级审计归属，但不启动完整 `app.RunServer` listener。本机 Windows Docker provider 不可用时普通模式输出 `SKIP`，强制模式已确认返回非零；两者都不能记录为真实 PostgreSQL / Redis / Asynq 通过。真实 listener、blacklist / unblock 安全真实前端 smoke、生产切流或 Node 写路径删除仍未完成。完整边界见 [W6 管理端客户端 IP 统计与策略迁移记录](../migration/W6-管理端客户端IP策略迁移记录.md)。
- Go W6 管理端客户端 IP 列表目标测试：在 `backend-go` 运行：

```powershell
go test ./... -count=1
go test -race ./internal/modules/managementclientipstats ./internal/httpapi ./internal/store/postgres ./internal/app -count=1
go vet ./...
go mod tidy -diff
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
go vet -tags=integration ./internal/testkit/integration
go test -race -tags=integration ./internal/testkit/integration -run '^$' -count=1
```

上述非 Docker 矩阵已通过，固定 `GET /__aisys__/api/ip-stats` 的 admin-only、只读 session、不 touch、query/date/status/sort/page、1000 行窗口、`pageSize+1`、`pageUpperBound/rangeReady`、预聚合只读和 migration `000040` reader-only 边界。默认和显式 `requestCount desc` 必须走 `ORDER BY request_count DESC, ip_hash ASC`，其他排序继续走静态 `CASE`。前端另运行 `pnpm --filter juhe-ai-frontend test:ip-stats-policy-api`、`pnpm --filter juhe-ai-frontend test:plan0081-real-go-management-smoke` 和 `pnpm --filter juhe-ai-frontend typecheck`，当前均已通过。

真实依赖目标命令为：

```powershell
$env:JUHE_AI_REQUIRE_INTEGRATION = '1'
try {
  go test -v -tags=integration ./internal/testkit/integration -run '^(TestW6ManagementClientIPStatsListPostgresRedisSmoke|TestW6ManagementClientIPStatsNodeWriterGoReaderSmoke)$' -count=1
} finally {
  Remove-Item Env:\JUHE_AI_REQUIRE_INTEGRATION -ErrorAction SilentlyContinue
}
```

`TestW6ManagementClientIPStatsListPostgresRedisSmoke` 已加入 Goose migrations、真实 PostgreSQL + Redis 组件、管理鉴权 / 限流、ready / dirty / state、筛选 / 排序和 custom / generic plan `EXPLAIN` 断言。`TestW6ManagementClientIPStatsNodeWriterGoReaderSmoke` 与 `backend/src/scripts/regression/client-ip-stats-node-writer-go-reader-fixture.ts` 已加入跨运行时入口；follow-up 必须固定“真实 Node schema / writer / refresh -> Goose migrations -> Go reader”顺序、非 UTC 午夜边界、非对称计数和 Node 子进程环境白名单，审查完成和真实执行前不记录为通过。本机 Docker provider 不可用，普通模式明确 `SKIP`，`JUHE_AI_REQUIRE_INTEGRATION=1` 明确失败，因此真实 PostgreSQL / Redis、EXPLAIN 和 Node writer -> Go reader 均尚未跑通。真实 `pnpm smoke:plan0081-real-go-management` 还要求显式 Go Base URL / 管理 Cookie，本机未执行真实 listener。Go 列表仍只读 Node production writer / worker 生成的预聚合结果，详情、daily / account / range writer 和生产切流不在本切片内。
- `pnpm --filter juhe-ai-backend test:auth-must-change-password-boundary` 和 `test:system-account-session-driver` 验证初始密码、个人资料更新、普通改密旧密码校验、改密后撤销旧会话和系统账户 session 双 driver 语义；认证路由必须使用 async 系统账户 / 会话 repository 和 async 操作日志入口。
- `pnpm --filter juhe-ai-backend test:api-key-list-query-guard`、`test:account-list-query-guard`、`test:usage-record-list-query-guard`、`test:usage-record-list-catalog-window`、`test:log-list-query-guard`、`test:operation-log-search-maintenance`、`test:log-list-page-window`、`test:management-list-page-window`、`test:base-list-page-window`、`test:runtime-log-search-guard`、`test:query-values` 和 `test:record-maintenance-index-guard` 验证 API Key、AI 账户、使用记录、日志、统计和基础管理列表不会退回多字段无边界包含扫描、全量统计或深 `OFFSET`；AI 账户列表只在账户名称字段支持包含匹配，且包含匹配必须先命中 `account_name_search_terms` 词项索引候选 ID，不能裸扫 `accounts` 主表，列表 / options / 详情不在读取请求里按被授权人全量回扫授权关系补建实例，也不会无界 `UPDATE accounts` 清理全部过期账号，账号到期清理必须命中到期时间部分索引并按固定批量 ID 更新，使用记录列表必须先读 1001 行以内 catalog 窗口再按当前页回 shard 补详情；管理员全用户使用记录只允许部署时区当天和 createdAt 降序，业务筛选、手工日期与其他排序必须先选择系统账户；日志 / 管理列表实际 SQL offset 必须限制在 1000 行内，操作日志 `summaryKeyword` 必须通过 `operation_log_summary_search_terms` 摘要倒排词项索引定位且支持规范化中英文数字单字符、不搜索资源名或操作人，离线重建按 `(created_at,id)` 游标分批执行，运行日志 keyword 只模糊匹配 `message` 且不检索 `raw_json`，空 query 不会误解析为 0，数据集 / 统计结果维护和日志详情查询具备目标索引。

- `pnpm --filter juhe-ai-frontend test:usage-record-admin-list` 验证管理端全用户默认当天列表不再前端短路，auto/manual 日期模式不会互相覆盖，全用户状态会清空并禁用业务筛选，切回全部用户恢复 auto today，零点、页面可见和窗口聚焦检查可在 auto 模式跨日重置分页并刷新。
- `pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-index` 验证网关 API Key 校验缓存和 API Key 额度缓存按反向索引做定点失效，不允许在 API Key 更新 / 删除这类写请求里遍历全部缓存条目，并固定网关设置、分组访问、模型目录、模型路由索引、响应检查策略、客户端 IP 策略单 IP 条目和混合路由评分结果 Redis shared cache 路径。
- `pnpm --filter juhe-ai-backend test:authorization-list-team-usage-batch` 验证授权列表团队用量按批量窗口读取，并固定授权读路径资源 lookup 复用统一 `repository-lookups` Redis shared cache helper，不在授权读仓储里保留重复私有 async lookup。
- `pnpm --filter juhe-ai-backend test:gateway-api-key-route-query-guard` 验证网关 API Key 只读取绑定路由策略，并由策略分组绑定读取固定 20 条窗口，查询计划命中路由索引，不扫描绑定表或创建临时排序；`test:settings-query-boundary` 验证系统设置读取在 SQL 层按固定 key 白名单收敛，不随脏数据行数线性增长。
- `pnpm --filter juhe-ai-backend test:authorization-usage-pagination`、`test:resource-authorization-single-read`、`test:resource-authorization-expiry-batch-boundary`、`test:account-usage-query-guard`、`test:account-test-limited-diagnostics`、`test:model-check-trusted-comparison`、`test:model-check-trusted-comparison-success`、`test:model-check-distribution-similarity`、`test:model-check-full-profile`、`test:model-check-unavailable-boundary`、`test:model-check-storage-sanitizer`、`test:model-check-strict-model-match`、`test:model-check-user-authorized-resource`、`test:deleted-api-key-aggregation-cleanup` 和 `test:openai-oauth-credentials-patch` 验证授权用量 / 账户用量使用 `pageSize + 1` 分页契约，不在请求路径做精确总数统计，授权单读路径不会因资源 lookup / 授权统计 / 授权来源缓存迁移破坏可见性和返回结构，授权过期扫描按固定批量处理且查询计划命中到期时间部分索引、不全表扫描或临时排序，授权账户测试 limited 诊断不会泄露内部异常文本，模型检测可信对比默认关闭，显式开启但未选择可信对比账户时会失败并不写报告，可信对比成功路径会执行同探针集和分布相似度对照，分布相似度明显偏离时会扣分降级，内部完整 profile 覆盖 AI 账户目标、长上下文和辅助模型对照，非账户目标、未绑定可用分组账户、停用代理和持续上游 503 不会被误判为假模型，存储摘要不会泄露 API Key / token / 代理密码 / 原始体，严格模型匹配不会把 `gpt-5.4-mini-*` 误判为 `gpt-5.4`，普通用户检测被授权账户时命中所有者账户但使用记录和可见详情按当前用户归属，删除 API Key 后相关使用记录、统计缓存、额度窗口、审计日志和审计错误组会进入后台分批清理，OAuth 创建时客户端 `credentialsPatch` 不能覆盖服务端 token 凭据。
- Go W4 授权 team/user usage overview 的 Go schema / query guard 额外固定：管理侧和个人侧 overview 只读授权用量 range window 预聚合表，使用 `pageSize + 1` 分页，不读取 `usage_records`，不在请求路径实时汇总；usage range window 刷新 worker 只允许读取授权日汇总表写入 range window，不得读取 `juhe_usage.usage_records`；gateway quota snapshot 构建 worker 只允许读取 `usage_stats_*` 预聚合表和 `usage_quota_hourly_windows`，不得读取 `juhe_usage.usage_records`。
- `pnpm --filter juhe-ai-backend test:runtime-snapshot-unavailable-contract` 和 `test:system-metrics-process-latest` 验证运行态不可观测时返回显式可用性字段和进程采样状态，不把未知压成 0、空数组或默认时间。
- `pnpm --filter juhe-ai-backend test:account-single-read`、`test:system-account-single-read`、`test:api-key-single-read`、`test:group-single-read`、`test:system-team-single-read`、`test:resource-authorization-single-read`、`test:proxy-single-read`、`test:announcement-single-read` 验证账户、系统账号、API Key、分组、系统团队、资源授权、代理和公告的单条写路径不依赖前 200 条列表或全量摘要装配；`account-single-read` 同时固定账户单独绑定分组路由使用 async repository。
- `pnpm --filter juhe-ai-backend test:account-test-response-limit` 和 `test:openai-oauth-token-response-boundary` 验证账户测试 / 模型检测响应体只保留 `256KB` 有界预览，截断后仍能识别流式首 token；代理检测和 OpenAI OAuth token endpoint 响应也使用有界收集器，防止外部探测目标或 OAuth 服务异常返回大响应时无界占用内存；OAuth 管理端创建、刷新和重新授权不能重新导入同步 repository / 操作日志入口。
- `pnpm --filter juhe-ai-backend test:openai-oauth-refresh-hot-path-cache` 验证请求链路 OAuth 懒刷新：同一账号并发临期刷新只读写一次 DB service，强制刷新不复用最近缓存，距离过期 5 到 60 秒的 token 只触发后台预热不阻塞当前请求，已过期 token 仍必须在当前请求内刷新后再转发。
- `pnpm --filter juhe-ai-backend test:diagnostic-task-boundary` 验证模型检测和代理检测共享固定 in-flight 上限，超过上限时快速拒绝并返回 `Retry-After`；账户测试不接入该共享闸门，由后台 worker 独立异步消费。
- `pnpm --filter juhe-ai-backend test:model-catalog` 验证全局 / 个人自定义模型进入本地模型目录，草稿、停用、未定价和 `mapping_target_only` 不进入 `/v1/models`，普通 OpenAI-compatible `/v1/models` 模型项只暴露 `created/id/object/owned_by`，Codex `client_version` 模型刷新使用 `{ models: [...] }` 和 Codex `ModelInfo` 字段，并由模型精确能力数组驱动服务等级与思考级别展示；同时覆盖后端历史 `pricingModel` 计价、管理接口创建 / 修改 / 删除权限、个人模型隔离、用户维度默认健康检查模型偏好和普通用户维护视图边界。当前手动维护页面的价格模板只回填同用途直接价格，保存时清空 `pricingModel` 并把非当前用途价格提交为 `null`；自定义模型必须可保存与回显服务等级、思考级别精确能力字段。
- `pnpm --filter juhe-ai-backend test:account-model-filter` 验证网关调度模型过滤按下游模型精确匹配账号支持模型；请求缺少 `model` 时不保留候选账号；启用的账号模型映射可用 `sourceModel` 精确命中下游模型并要求 `upstreamModel` 命中账号支持模型后保留候选账号；未配置支持模型、禁用映射或上游模型不在支持列表时仍会跳过。
- `pnpm --filter juhe-ai-backend test:account-model-mapping` 验证 AI 账户模型映射保存 / 更新、运行时账号快照、选中账号后的 JSON 顶层 `model` 改写、`requestModel` 下游语义、非法 JSON 本地 400、使用记录 `model/upstreamModel/pricingModel/modelMappingApplied/modelMappingSource` 字段、右侧 Responses 只允许 `responses -> responses` 原生直连，运行时忽略历史残留的账号级跨协议映射；跨协议 `responses -> chat_completions` 这类能力归混合供应商账户，不再由 API Key 字段注入。授权调用按资源账号所有者目录解析个人映射目标价格。
- `pnpm --filter juhe-ai-backend test:account-diagnostic-mock-ai` 使用临时库、真实账号配置、真实网关测试链路和本机 mock OpenAI 上游验证账号诊断等待策略：手动测试按 `10s + 20s + 30s` 三档重试、持续失败不按状态码提前分类停止、Codex 切号探针明确失败立即淘汰候选、本地超时才在同候选账号递进三档；纯图像 `gpt-image-2` 必须只命中一次上游 `GET /v1/models`，目录精确包含目标模型后成功，不能调用文本 Responses 或触发生图。
- 旧 `test:account-batch-test-mock-ai` 不再作为当前验证入口；产品和接口均不提供多账户批量测试，相关前端入口、批量提交协议与回归脚本应删除。
- `pnpm --filter juhe-ai-backend test:account-test-mock-ai-edge` 使用本机 mock OpenAI 上游验证账号测试慢响应重试、`10s + 20s + 30s` 总运行超时、queued 等待记录与队列等待上限、当前 `testSessionId` 取消 running / queued 任务、A/B 两个会话互不阻塞或覆盖，以及无前端 heartbeat 时后台任务仍继续执行到终态。
- `pnpm --filter juhe-ai-backend test:account-test-responses-contract` 验证 API Key 账户 Responses 测试不发送 `max_output_tokens`，并验证 GPT API Key 账户默认具备 `codex_responses` 能力、GPT OAuth 账户兼容能力固定为 `codex_responses`；`pnpm --filter juhe-ai-backend test:openai-api-key-passthrough` 验证 OpenAI 标准透传、Codex Responses 请求体 / 请求头补齐、工具别名归一、无效 Codex 字段剔除和非 Responses 路径不改写。
- 账户人工测试回归必须同时固定：创建 / 编辑表单测试只使用表单 `healthCheckModel`；列表测试按需读取 `test-options`，候选来自账户供应商、协议档案和所有者可见启用文本目录模型且不受 `supportedModels` 限制；成功或失败均不修改账户、账户内 API Key、健康信号、额度快照、冷却、最近错误和调度状态。
- 账户激活回归必须固定：新建 / 导入请求 `active` 时先写 `pending_test`，仅后台激活检查成功后写 `active`；关键连接字段编辑保存后由后台复检处理，人工测试不能替代激活或复检。
- 账户批量编辑回归必须固定独立事务接口、显式字段覆盖白名单、版本号乐观锁和全成全败；名称、分组、供应商、协议档案、账户类型、凭据、Base URL、状态与运行态字段必须拒绝，授权实例必须拒绝，模型区字段只允许同 `provider + profile + type` 账户一起编辑。
- GPT 请求覆盖回归必须验证账户 `credentials.service_tier_override` / `credentials.reasoning_effort_override` 的保存与清空、模型和自定义模型精确能力数组校验、已选模型能力合集、未知模型不清空已知能力、GPT API Key / OAuth 对 Flex 使用同一规则、无能力时控件保留并禁用、网关在模型映射与协议适配后的最终模型精确支持时逐字段覆盖、最终模型不支持或能力未知时逐字段保留客户端原值且不降级不报错、大 JSON 请求交给 worker 修改，以及 Codex `/models` 只按能力暴露可选项；Ultra 级别与 Multi-agent Beta 不得被本期能力数组或请求覆盖误开放。
- `pnpm --filter juhe-ai-backend test:service-tier-billing` 验证 GPT-5.6 Sol / Terra / Luna standard、priority、flex 三档价格、稳定别名、272K 长上下文倍率、网关模型目录计价路径、可配置通用倍率与精确价格优先级，以及 OpenAI reasoning 不重复计费和 Gemini thoughts 不漏计。
- `pnpm --filter juhe-ai-backend test:gateway-dispatch-candidate-window` 验证网关分组候选按有 / 无时间计划两类 `512` 扫描窗口读取，最终候选仍保持 `256` 上限，冷却账号前置过滤，计划账号不会被无计划窗口误漏，前 256 个候选水合失败时可以继续补齐后续可用账号，并固定审计诊断字段里的扫描、水合和最终候选计数。
- `pnpm --filter juhe-ai-backend test:hybrid-gateway-mock-ai` 使用本机 mock OpenAI 上游验证 API Key 级混合智能路由：评分模型严格 JSON、`1-10` 等级到目标模型、路由策略分组池筛选、顶层 `model` 改写、评分失败后低档兜底、低档无可用池错误收口、质量评分模型不可用默认放行原始 `200`、质量失败同档带反馈修复、修复仍失败后升档、精确评分缓存、模型 / 缓存亲和、连续低分降级和 `hybrid_scoring` 使用记录入账；评分缓存命中只省评分调用，目标请求仍必须执行，且不同 `x-client-request-id` 不会打散同一请求的评分缓存。`pnpm --filter juhe-ai-backend test:hybrid-scoring-shared-cache-redis` 使用测试 Redis 验证清空本地评分 LRU 后，第二次相同请求仍能通过 Redis shared cache 避免重复入口评分模型调用。`pnpm --filter juhe-ai-backend test:hybrid-real-gateway-e2e` 使用真实 OpenAI-compatible 聚合平台验证真实账号稳定性；运行前必须设置 `JUHE_REAL_HYBRID_API_KEY`，可用 `JUHE_REAL_HYBRID_BASE_URL` 和 `JUHE_REAL_HYBRID_MODEL_1_3` / `MODEL_4_6` / `MODEL_7_8` / `MODEL_9_10` 覆盖平台实际小写模型 ID。`pnpm --filter juhe-ai-backend test:hybrid-real-model-probe` 用同一真实 key 串行探测候选模型可用性，适合在创建混合配置前确认平台实际模型 ID。`pnpm --filter juhe-ai-backend test:hybrid-quality-comparison` 使用 `16` 个综合样本题对比 `hybrid`、固定 `gpt-5.5` 和固定 Opus 基线，默认 Opus 基线为 `claude-opus-4-7`，可用 `JUHE_REAL_HYBRID_QUALITY_OPUS_MODEL` 改成平台实际可用的小写模型 ID；每份答案单独匿名交给评审模型按验收点打分，`totalScore >= 80` 且无致命问题才算正确；真实评测默认串行执行，只有显式设置 `JUHE_REAL_HYBRID_QUALITY_PARALLEL_REQUESTS=1` 或 `JUHE_REAL_HYBRID_QUALITY_PARALLEL_JUDGES=1` 才会并发。`pnpm --filter juhe-ai-backend test:hybrid-project-workflow` 会克隆万行级 React / Vite 后台项目并串行跑规划、执行、修复和复核工作流，对比 `hybrid_main_agent`、固定 `glm-5.2`、固定 `gpt-5.5` 和固定 Opus；默认只做源码静态验收，避免把外部依赖构建波动误判为模型能力问题；可用 `JUHE_REAL_HYBRID_PROJECT_STRATEGIES=fixed_glm52` 单独跑 GLM 基线，可用 `JUHE_REAL_HYBRID_PROJECT_MODEL_1_2`、`MODEL_1_3`、`MODEL_5_6`、`MODEL_7_8`、`MODEL_9` 和 `JUHE_REAL_HYBRID_PROJECT_GPT_MODEL` 覆盖非 Claude 模型池；可用 `JUHE_REAL_HYBRID_PROJECT_UPSTREAM_RETRIES`、`JUHE_REAL_HYBRID_PROJECT_UPSTREAM_RETRY_DELAY_MS` 和 `JUHE_REAL_HYBRID_PROJECT_CLONE_RETRIES` 排除瞬时上游错误和 GitHub clone reset 对结果的影响；长请求工作流会按请求超时和重试次数自动放大评分缓存 TTL。
- `pnpm --filter juhe-ai-backend test:api-key-availability-schedule`、`test:account-availability-schedule`、`test:account-import-availability-schedule` 和 `pnpm test:time-schedule` 验证 API Key / AI 账户时间计划的前后端契约、API Key 单一运行状态、AI 账户派生可用状态、网关校验和候选过滤，覆盖跨天窗口、日期范围、允许 / 拒绝例外、重叠窗口、人工启用 / 停用、导入继承计划、前端表单 payload 保真、下一次边界检查点推进和中文状态摘要；高数据量守卫同时固定后台计划同步只能读取 `next_check_at <= now` 的 500 行窗口，不能每 10 秒全量加载所有计划对象，也不能用滚动 `updated_at` 游标延迟边界切换。
- `pnpm --filter juhe-ai-backend test:authorized-account-dispatch-query` 和 `test:authorized-account-admin-dispatch` 验证网关候选账号调度会批量读取授权账号关系，读取路径不按被授权人回扫授权并补建实例；管理员代用户测试授权账户时按被代用户绑定固定命中当前账号。
- `pnpm test:group-scheduling-policy`、`test:high-concurrency-queue`、`pnpm --filter juhe-ai-backend test:account-concurrency-lane-index`、`test:client-ip-concurrency`、`test:client-ip-error-circuit`、`test:client-ip-policy-hit-buffer`、`test:client-ip-policy-shared-cache-redis` 和 `test:gateway-client-ip-account-avoidance` 验证高并发分组策略字段、分组短队列、账号释放按账号+通道反向索引唤醒等待项、账号通道并发读取走 O(1) 计数索引、每 API Key 队列上限、单 IP 并发闸口、单 IP 等待队列上限、认证前 / 认证后 IP 错误熔断固定样本窗口、本地 `429` 失败口径、Redis cache driver 下 IP 策略按单 IP 条目读取 Redis shared cache 或按 `ip_hash` 索引回源、命中缓冲有固定上限，以及 IP 级账号回避的待确认失败按账号去重、固定 256 条上限和 fallback 切组有界转移。
- `pnpm --filter juhe-ai-backend test:client-ip-stats` 验证 IP 统计按 usage shard 游标增量聚合、列表只读预聚合窗口、封禁筛选命中索引；同时固定网关运行态不随 API Key runtime 携带全量 active IP 策略，standalone 本地路径不请求 DB service。
- `pnpm --filter juhe-ai-backend test:authorization-quota-batch` 验证 DB service 内授权额度批量检查会按授权和用量窗口去重读取，且仍能正确拒绝超限授权账号。
- `pnpm --filter juhe-ai-backend test:account-quality-refresh` 验证账号质量刷新只按近窗口样本账号补业务元数据和旧质量行，stale 推进与失效清理按固定批次滚动完成，不一次性加载全部账号或全部质量缓存；同时验证频繁失败候选生成、候选查询索引、后台确认使用调用方上下文和 `cooldown_retest` 流量来源。
- `pnpm --filter juhe-ai-backend test:account-quality-gateway-status` 使用真实本机 mock OpenAI 上游和真实网关请求验证账号质量状态流转：真实失败先形成质量反馈，后台确认失败升级为 `temporary_unavailable`，mock 上游恢复后冷却复测恢复 `active`。
- `pnpm --filter juhe-ai-backend test:cooldown-retest-recovery` 验证 `temporary_unavailable` / `rate_limited` 的快速恢复、慢速退避、最长观察窗口转异常、授权实例本地状态隔离和 mock 上游探针恢复。
- `pnpm --filter juhe-ai-backend test:group-account-stats-dirty-cache` 验证分组账户统计写请求只打业务库脏标记，`__all__` 全量影响由 worker 按游标固定批次刷新，不在写请求或单轮 worker 中展开全部分组。
- `pnpm --filter juhe-ai-backend test:table-monitor-default-scan` 验证表监控默认采样使用 cursor 游标，滚动写入可推导行数，不会因为忘记传参数而扫描所有表或执行精确 `COUNT(*)`。
- `pnpm --filter juhe-ai-backend test:runtime-log-grep-path` 验证 `/__aisys__/api/runtime-logs/grep` 不会把日志搜索请求本身混入搜索结果。
- `pnpm --filter juhe-ai-frontend test:log-progressive-loading` 验证审计与运行日志列表不捆绑重量运行态、运行日志 facets / grep 文件范围按交互拆分、审计 payload 首窗口和下一段按需读取，以及 grep 响应不重复返回原文。
- `pnpm --filter juhe-ai-frontend test:audit-payload-window-merge` 验证任意字节边界的审计 payload 窗口按解码后字节无损合并，避免直接拼接带 padding 的 base64。
- `pnpm --filter juhe-ai-backend test:audit-log-settings-contract` 验证原始审计日志 standalone / 本地队列固定为 `50000` 条和 `256MB` 硬上限，功能文档不能再把队列描述为无限制或 1GB 级内存缓冲；高性能 `redis_stream` 下审计队列持久化边界由 `test:redis-stream-queue` 覆盖。
- `pnpm --filter juhe-ai-backend test:audit-log-retention` 验证审计成功采样、压缩、去重、错误聚合、payload 窗口读取和保留期清理；同时固定数据保留 worker 使用异步审计清理入口，无引用 payload blob 文件按固定并发窗口异步删除。
- `pnpm --filter juhe-ai-backend test:public-api-logs` 验证公开接口日志只保存有界请求 / 响应快照，query/body/response 在捕获范围内保留原值且不写 `[redacted]`，`Authorization` / Cookie / 来源 token 不进入 headers 快照；同时固定 API Key / account 业务响应白名单、管理员读取和 30 天保留清理。高数据量守卫会额外检查快照大小估算不能对原始大对象直接 `JSON.stringify`。
- `pnpm --filter juhe-ai-backend test:record-maintenance-queue` 验证数据维护队列只在 worker 执行，并固定已删除 API Key / AI 账户关联清理和后台 pending 重试都走异步入口，避免无引用审计 payload blob 清理回退到同步文件删除路径。`pnpm --filter juhe-ai-backend test:record-cleanup-postgres-smoke` 在真实 PostgreSQL URL 下写入临时 usage / audit / stats / model check 测试行，并验证已删除 API Key / AI 账户关联记录清理直接走 `juhe_usage` / `juhe_dataset` / `juhe_stats`，完成后清理目标表。`pnpm --filter juhe-ai-backend test:record-maintenance-redis-stream-smoke` 在真实 Redis queue URL 下验证高性能模式数据维护 Stream 入队、consumer `XACK` 后 `XDEL`、pending / lag 归零和测试消息清理。运行日志真实依赖验证改由 file consumer PostgreSQL smoke 执行，且必须确认 runtime-log Stream 没有新增条目。
- `pnpm --filter juhe-ai-backend test:deleted-api-key-aggregation-cleanup` 验证删除 API Key 后历史使用记录、统计缓存、额度窗口、审计日志和错误组按后台小批次清理；同时固定删除清理通过去重 shard 目录定位相关 usage shard，不枚举全部 shard，也不从目录明细临时 `GROUP BY shard_key`。
- `pnpm --filter juhe-ai-backend test:queue-failure-requeue-boundary` 验证使用记录、审计、操作日志、公开接口日志和数据维护本地队列 flush 失败时保持原队列等待重试，不能复制全部积压记录或重新 reduce 全队列字节数；运行日志改由文件 consumer 的 cursor 提交回归覆盖。
- `pnpm --filter juhe-ai-backend test:audit-payload-large-window` 验证超过单次读取窗口的审计 payload 会保持 plain blob，后半段读取可以按 offset 返回有限窗口，不退回 gzip 从头解压。
- `pnpm test:large-response` 验证非流式大响应会完整转发给客户端，同时网关只做有限响应捕获，避免回退到全量读入内存。
- `pnpm --filter juhe-ai-backend test:gateway-raw-body-parser-error` 验证客户端中断上传映射为可重试 HTTP 408、完整非法 JSON 保持本地 400、请求体过大保持 413；同时固定 `express.raw` 使用局部错误包装器，`preResolveGatewayRuntime` 等前置 middleware 的内部异常必须进入全局错误边界，不能误报为“网关请求体无效”。
- `pnpm test:stream-first-output-timeout` 验证流式上游首段内容等待超时、首段后无新数据超时、持续 raw chunk 但暂未形成完整 SSE 事件时不误熔断，以及心跳等任意上游 chunk 会刷新输出停顿计时，网关会按协议补发 `response.failed` 或正常完成，避免客户端长时间停在“正在思考”。
- `pnpm --filter juhe-ai-backend test:upstream-request-failure` 保留 transport / timeout、首字节超时、正文已输出后中断和客户端重试边界回归；通用客户端的完整非 `2xx` 响应不得再按旧断言切号，透明语义以 `test:gateway-generic-upstream-opaque` 为准。
- `pnpm --filter juhe-ai-backend test:normal-route-speed-first-mock-ai` 使用本机 Mock OpenAI 上游验证普通路由速度优先：非流式首字超时隐藏切号、连续慢样本触发降级后置、成本优先隔离、后台恢复探针链路、全部降级旁路、流式首字超时隐藏切号，以及切号后首字耗时按当前账号 attempt 独立计时。
- 后台探针与普通路由快速模式高并发保护定向验证使用以下命令；其中 HTTP admission 回归会实际发送两个 4MB 请求，断言容量为 1 时第二个请求在首个响应完成前不会通过 `express.raw` 进入业务处理：

```powershell
pnpm --filter juhe-ai-backend test:cooldown-retest-recovery
pnpm --filter juhe-ai-backend test:speed-first-body-admission
pnpm --filter juhe-ai-backend test:speed-first-body-admission-http
pnpm --filter juhe-ai-backend test:speed-first-cutover-reservation
pnpm --filter juhe-ai-backend test:normal-route-speed-first-runtime
pnpm --filter juhe-ai-backend test:normal-route-speed-first-mock-ai
pnpm --filter juhe-ai-backend test:api-key-inflight-quota
pnpm --filter juhe-ai-backend test:gateway-quota-fast-path
pnpm --filter juhe-ai-backend test:gateway-quota-passive-snapshot
pnpm --filter juhe-ai-backend test:service-tier-billing
pnpm --filter juhe-ai-backend typecheck
git diff --check
```

  冷却复测只以完整探测成功为成功，任何其他结果都必须累计失败并推进下一次时间；候选扫描按复合游标有界前进并在末尾回绕。正文 admission 只作用于 `normal + speed_first + high_concurrency`，等待项不能持有完整 raw body。确认慢切换必须先取得具体目标账户并发槽，同一慢账户同时最多迁移 2 个请求；API Key 在途额度必须把 worker 成本快照和当前 reservation 合并判断，无定价模型不伪造成本。
- `pnpm --filter juhe-ai-backend test:response-inspection-policy` 验证网关运行态响应检查策略只读取供应商内固定窗口，管理端策略创建有总量上限，SSE 事件和非流式 JSON 都按已合并策略匹配，不退回请求内无界策略表读取。
- `pnpm test:codex-client-strategy` 验证 Codex profile 精确依赖 `x-codex-turn-metadata.turn_id`，不使用 `session_id` / `x-client-request-id` fallback，并覆盖 body hash 隔离、第 4 次 turn 级账号避让、状态丢失和非 Responses 隔离。
- `pnpm --filter juhe-ai-backend test:openai-oauth-codex-adapter`、`test:usage-stats-batch-statement`、`test:stats-aggregation-batch-yield`、`test:usage-rank-snapshot-skip`、`test:usage-quota-hourly-window-config`、`test:gateway-runtime-cache`、`test:gateway-quota-passive-snapshot`、`test:usage-stats-shard-fanout-boundary`、`test:client-ip-stats-shard-fanout-boundary`、`test:data-retention-cleanup-catalog-window`、`test:data-retention-sql-guards`、`test:sqlite-high-volume-guards` 和 `pnpm test:session-affinity` 共同验证 OAuth / API Key 账号类型只影响能力和转发，不作为本地统计、会话亲和、Codex prompt cache 或网关运行配置缓存的默认分片；额度小时窗口刷新只读固定上限配置表，不扫描全部额度 JSON，且用量聚合空轮不重建额度窗口；排行 / 范围窗口快照水位不对 `usage_stats_daily` 做全表 `COUNT(*)`；额度快照由 worker 分页构建完整窗口，server 请求链路只读内存快照且快照缺失不主动查 DB service；usage / client IP 聚合的 shard fan-out 有界且批间让出，不会误报全局 lag，使用记录保留清理同时等待 usage 聚合和 client IP 聚合游标，固定窗口清理列具备索引。
- `pnpm test:smoke:mock` 使用临时库、假账号和 mock 上游跑完整网关 e2e，验证第 4 次 Codex turn 切号、非 Codex 不切号、Codex 建流前 HTTP 非 `2xx` 账号耗尽时收口为可重试 SSE，以及 Codex profile 下 `context_length_exceeded`、`cyber_policy` 等当前策略边界；用于没有真实账户时补足调度链路验证。
- `pnpm test:session-affinity` 验证会话亲和只作为同层级软排序，不能跨过超级优先、账号优先级或更优质量候选，也不能挡住不可用绑定账号后的正常回退；非绑定账号失败不应误删当前会话亲和，手动迁移亲和绑定也不能扫描全部亲和缓存。
- `pnpm test:perf` 使用临时库和模拟上游压测本机网关，输出 QPS、延迟分位、错误率、事件循环延迟、CPU、内存和队列指标；它不消耗真实 OpenAI 额度，也不替代真实部署实例的容量压测。
- `pnpm --filter juhe-ai-backend test:db-service-runtime-metrics` 验证 DB service queue wait、exec duration、slow operation 和等待 dataset writer 的 pending 指标会进入 runtime snapshot 与父进程状态缓存。
- `pnpm --filter juhe-ai-backend test:db-service-queue-expiry` 验证 DB service 子进程会清理已过期排队请求、递增 `queueExpiredCount`，且后续正常请求不受过期项影响。
- `pnpm --filter juhe-ai-backend test:background-queue-health` 验证后台队列健康视图包含 worker 本地队列、server IPC 队列、oldest queued、flush duration、pending write 和 writer pool 指标，并能识别积压、丢弃、拒绝、慢 flush 与 writer pool 异常。
- `pnpm --filter juhe-ai-backend test:usage-record-writer-pool` 验证可选 usage writer pool 只写 usage shard 行，dataset shard catalog、使用记录索引和账号最近使用时间仍由 ingest-worker 主写入链路正确提交；该测试不代表 writer pool 适合默认开启。
- `pnpm --filter juhe-ai-backend test:usage-hot-data-pressure` 在真实 PostgreSQL / Redis performance 环境下压测 `usage_records` 日分区写入、用户维度查询、`usage_scope_range_windows` 查询、统计安全游标后的 PG 整日分区删除和表监控采样；默认会创建并清理合成高增长数据。可用 `JUHE_USAGE_PRESSURE_ROWS`、`JUHE_USAGE_PRESSURE_CLEANUP_ROWS`、`JUHE_USAGE_PRESSURE_RANGE_WINDOW_ROWS`、`JUHE_USAGE_PRESSURE_QUERY_ITERATIONS`、`JUHE_USAGE_PRESSURE_QUERY_CONCURRENCY` 和 `JUHE_USAGE_PRESSURE_REPORT_PATH` 调整规模和输出路径；该脚本不进入普通回归聚合，只在容量验证或上线前压力演练时运行。
- `pnpm --filter juhe-ai-backend test:usage-real-entry-mixed-load` 在真实 PostgreSQL / Redis performance 环境下启动临时后端进程和本机 mock OpenAI 上游，通过真实 `/v1/chat/completions` 写入 usage，再并发读取后台使用记录、统计窗口和表监控接口，用于观察在线读是否被持续写入影响。必须显式设置 `JUHE_AI_MIXED_LOAD_ALLOW_SETTINGS_WRITE=1` 和 `JUHE_AI_MIXED_LOAD_ALLOW_PRIVATE_UPSTREAM=1`；可用 `JUHE_AI_MIXED_LOAD_SEED_WRITES`、`JUHE_AI_MIXED_LOAD_BASELINE_READ_SECONDS`、`JUHE_AI_MIXED_LOAD_SECONDS`、`JUHE_AI_MIXED_LOAD_WRITE_CONCURRENCY`、`JUHE_AI_MIXED_LOAD_READ_CONCURRENCY`、`JUHE_AI_MIXED_LOAD_ACCOUNT_COUNT`、`JUHE_AI_MIXED_LOAD_MAX_READ_P95_RATIO` 和 `JUHE_AI_MIXED_LOAD_REPORT_PATH` 调整规模和阈值。该脚本会临时放宽系统 API 限流并在结束后恢复设置、清理 fixture 和 trace 数据，不进入普通回归聚合。
- `pnpm --filter juhe-ai-backend test:codex-context-state-writer-pool` 验证 Responses bridge state writer pool 的并发 shard 写入、read-after-write、compact 边界、cleanup 全局屏障和 shared storageKey 保护。
- `pnpm --filter juhe-ai-backend test:keyed-child-process-pool-watchdog` 验证 keyed child process pool 的 active job watchdog 会释放超时槽位、重启卡死 worker，并继续处理同 slot 后续任务。
- `pnpm --filter juhe-ai-backend test:codex-context-state-pressure` 使用临时 Responses bridge state shard 库压测 Chat-only bridge 的状态索引写入、`previous_response_id` 链恢复和 TTL 清理，默认对比 `1` 与 `16` 个 shard、直写与 writer pool 两种模式，并输出 ops/s、p50/p95/p99、shard 分布、writer pool runtime 和 SQLite 文件大小；可用 `JUHE_CODEX_CONTEXT_PRESSURE_SHARDS`、`JUHE_CODEX_CONTEXT_PRESSURE_WRITER_POOL`、`JUHE_CODEX_CONTEXT_PRESSURE_SESSIONS`、`JUHE_CODEX_CONTEXT_PRESSURE_RESPONSES_PER_SESSION`、`JUHE_CODEX_CONTEXT_PRESSURE_SAME_SESSION_RESPONSES`、`JUHE_CODEX_CONTEXT_PRESSURE_COMPACTS`、`JUHE_CODEX_CONTEXT_PRESSURE_EXPIRED_SESSIONS` 和 `JUHE_CODEX_CONTEXT_PRESSURE_EXPIRED_RESPONSES_PER_SESSION` 调整压力规模。
- `pnpm test:data-retention-high-volume-settings` 验证数据保留清理吞吐为后台内部常量：固定 10 分钟一轮、1000 行小批、每类每轮 2 万行处理能力、连续批次之间保留 25ms SQLite 写入间隙，并确认这些吞吐参数不暴露为系统设置。
- `pnpm test:smoke` 必须走同库产品流程：校验烟测管理员凭据并创建短时会话 -> 管理 API -> 目标账号当前分组或临时 mock 分组 -> 临时本地 API Key -> `/v1` 网关请求 -> 使用记录 / 审计运行态 -> 清理。该脚本不验证远端后端的 HTTP 登录验证码链路；`JUHE_AI_BACKEND_URL` 只能指向共享同一业务库和密钥的本机后端。
- 发布前如本次版本触及团队、授权、分组、API Key 或网关调度，需额外抽查一次多用户团队授权闭环：管理员建临时团队并添加成员 -> 资源所有者把临时账户或分组授权给团队 -> 团队成员把授权账户实例绑定到自己的分组 -> 本地 API Key 绑定调用方路由策略且该策略绑定自有分组后请求 `/v1` -> 在使用记录 / 授权用量 / 审计日志确认调用方、资源所有者和授权 ID -> 收回授权或移除成员后再次请求应被拒绝 -> 清理临时团队、授权、分组、路由策略和 API Key。授权方分组不能直接作为被授权方 API Key 的调度池。
- 发布前需人工抽查一次页面状态缓存和模式切换体验：管理员在用户模式 / 管理模式之间切换时，左侧菜单和内容页保持同一模式；在账户、分组、API Key、授权、使用记录、审计日志、运行日志、用量统计或统计概览中设置筛选 / 分页 / 排序后，切到其他页面再返回应恢复当前会话现场；刷新页面应恢复非敏感查询状态；点击重置筛选应清理页面缓存。
- 发布前仍需人工抽查一次管理面使用记录详情和审计日志检索，确认 smoke 产生的 trace 可用于排障；不要把真实 token、API Key 或上游错误敏感内容写入文档或截图。
- 服务档位、上游桶和 Codex attestation 变更至少执行 `test:service-tier-billing`、`test:usage-pricing`、`test:gateway-proxy-health`、`test:openai-oauth-codex-adapter`、`test:openai-api-key-passthrough`、`test:usage-record-writer-pool` 和 `test:postgres-schema-sql`。使用记录详情需确认 requested / effective / reported / billed 四个档位可区分；OAuth 合法 attestation 只到达上游，API Key、审计和 usage 捕获均不可见正文。

## 10. 设置与流熔断验证

系统设置接口只应返回当前系统运行设置白名单字段；字段集合必须与后端 `systemSettingKeys` 保持一致，并覆盖后台接口限流、临时不可调用、流熔断、后台任务、操作日志和数据保留配置。数据保留清理间隔、单批删除行数和单轮最大批数属于后台内部常量，不应出现在系统设置响应中：

```powershell
$settings = Invoke-RestMethod http://127.0.0.1:3000/__aisys__/api/settings
$expectedSettingKeys = @(
  'accountHealthCheckBatchSize',
  'accountHealthCheckFailureThreshold',
  'accountHealthCheckIntervalHours',
  'accountHealthCheckJitterMinutes',
  'accountQualityRefreshIntervalSeconds',
  'accountQualityWindowMinutes',
  'accountTestTaskConcurrency',
  'cooldownAccountRetestBatchSize',
  'cooldownAccountRetestIntervalSeconds',
  'cooldownAccountRetestMaxBackoffHours',
  'defaultTemporaryUnschedulableMinutes',
  'gatewayTextRawBodyLimitMegabytes',
  'groupAccountStatsRefreshIntervalSeconds',
  'modelCheckRetentionDays',
  'oauthAccessTokenRefreshBatchSize',
  'oauthAccessTokenRefreshIntervalSeconds',
  'oauthAccessTokenRefreshLeadSeconds',
  'oauthAccessTokenRefreshRetryBackoffSeconds',
  'operationLogMaxChangesPerRecord',
  'operationLogRetentionDays',
  'publicApiLogRetentionDays',
  'runtimeLogIndexRetentionDays',
  'statsAggregationBatchSize',
  'statsAggregationIntervalSeconds',
  'statsAggregationMaxBatchesPerRun',
  'streamFailureThresholdCount',
  'streamFailureThresholdWindowMinutes',
  'textFirstResponseTimeoutSeconds',
  'textStreamIdleTimeoutSeconds',
  'textUncommittedAttemptMaxLifetimeSeconds',
  'imageFirstResponseTimeoutSeconds',
  'imageStreamIdleTimeoutSeconds',
  'imageUncommittedAttemptMaxLifetimeSeconds',
  'noAvailableAccountWaitTimeoutSeconds',
  'systemApiRateLimitIpReadBurstPer10Seconds',
  'systemApiRateLimitIpReadPerMinute',
  'systemApiRateLimitIpWriteBurstPer10Seconds',
  'systemApiRateLimitIpWritePerMinute',
  'systemApiRateLimitUserReadPerMinute',
  'systemApiRateLimitUserWritePerMinute',
  'systemMetricsHourlyRetentionDays',
  'systemMetricsRetentionDays',
  'systemMetricsSampleIntervalSeconds',
  'tableMonitorMaxTablesPerRun',
  'temporaryUnschedulableRetryAttempts',
  'temporaryUnschedulableRetryIntervalSeconds',
  'usageHotWindowRefreshIntervalSeconds',
  'usageRankSnapshotRetentionDays',
  'usageRecordRetentionDays',
  'usageStatsDailyRetentionDays',
  'usageStatsHourlyRetentionDays',
  'usageStatsMinuteRetentionHours',
  'usageStatsMonthlyRetentionMonths',
  'usageStatsTimezone',
  'usageStatsWeeklyRetentionWeeks'
)
$actualSettingKeys = @($settings.data.PSObject.Properties.Name | Sort-Object)
Compare-Object $expectedSettingKeys $actualSettingKeys
if ($actualSettingKeys.Count -ne 55) { throw "系统设置字段数量应为 55，实际为 $($actualSettingKeys.Count)" }
$settings.data.defaultTemporaryUnschedulableMinutes
$settings.data.temporaryUnschedulableRetryIntervalSeconds
$settings.data.temporaryUnschedulableRetryAttempts
$settings.data.textFirstResponseTimeoutSeconds
$settings.data.textStreamIdleTimeoutSeconds
$settings.data.textUncommittedAttemptMaxLifetimeSeconds
$settings.data.imageFirstResponseTimeoutSeconds
$settings.data.imageStreamIdleTimeoutSeconds
$settings.data.imageUncommittedAttemptMaxLifetimeSeconds
$settings.data.noAvailableAccountWaitTimeoutSeconds
$settings.data.systemApiRateLimitIpReadPerMinute
$settings.data.systemApiRateLimitUserReadPerMinute
$settings.data.statsAggregationIntervalSeconds
$settings.data.systemMetricsSampleIntervalSeconds
@(
  'systemApiRateLimitEnabled',
  'streamCircuitBreakerEnabled',
  'operationLogEnabled',
  'dataRetentionCleanupIntervalMinutes',
  'dataRetentionCleanupBatchSize',
  'dataRetentionCleanupMaxBatchesPerRun'
) | ForEach-Object {
  if ($settings.data.PSObject.Properties.Name -contains $_) { throw "系统设置不应暴露内部常量或固定启用项：$_" }
}
```

W5 Go opt-in 下，`GET /settings` 使用 read auth 且不 touch；`PATCH /settings` 使用 write auth touch。PATCH 的 system API IP limiter 先执行，随后才是 `256 KiB` JSON parser；解析成功后才进入 auth / touch 和 authenticated user write limiter。malformed JSON / 超限 body 因此会在 auth 前返回 `400` / `413`，但仍消耗 IP limiter 窗口。

验证 fresh PostgreSQL migration 时，应显式设置部署 IANA 时区：

```powershell
Set-Location backend-go
$env:JUHE_AI_USAGE_STATS_TIMEZONE = 'Asia/Shanghai'
goose -dir db/migrations postgres $env:JUHE_AI_POSTGRES_URL up
```

`Asia/Shanghai` 只是示例，必须替换为实际部署 IANA 时区。migration `000024` 优先使用 `JUHE_AI_USAGE_STATS_TIMEZONE`，未设置时回退 PostgreSQL `TimeZone`，再回退 `UTC`；Node / Go 共存期必须与 Node `Intl` 实际部署时区一致。PostgreSQL 在线 PATCH 禁止修改 `usageStatsTimezone`。当前真实依赖 smoke 仍受本机无 Docker 阻塞，不能把 integration `SKIP` 写成通过。

system API 六项限流设置的当前默认值依次为：IP read 每分钟 `600`、IP read 每 10 秒 burst `120`、IP write 每分钟 `180`、IP write 每 10 秒 burst `40`、用户 read 每分钟 `300`、用户 write 每分钟 `120`。Go 读取这些设置后缓存 60 秒；验证配置热更新时应等待缓存刷新或构造可控时钟，不能把 60 秒内继续使用旧值误判为设置未生效。

当前系统不提供“特征规则”菜单、只读接口或运行时规则表。通用客户端的完整上游响应必须保持不透明：HTTP 非 2xx、`200 + error`、未知错误对象和 SSE 失败事件均原样转发，不得触发账号重试、切号、Key 轮换或账号副作用。响应检查与账户错误策略只在精确识别的 Codex、Claude Code、Gemini CLI 客户端画像下执行；本地 transport / timeout / capacity 失败仍可在语义提交前由服务端救回。

PLAN-0134 聚焦验证使用以下 Windows / PowerShell 入口：

```powershell
pnpm.cmd --dir backend run test:gateway-timeout-profile
pnpm.cmd --dir backend run test:server-retry-budget
pnpm.cmd --dir backend run test:gateway-upstream-semantic-policy
pnpm.cmd --dir backend run test:gateway-downstream-commit
pnpm.cmd --dir backend run test:gateway-generic-upstream-opaque
pnpm.cmd --dir backend run test:image-provider-timeout-signal
pnpm.cmd --dir backend run test:gateway-concurrency-preparation
pnpm.cmd --dir backend run test:normal-route-speed-first-mock-ai
pnpm.cmd --dir backend run test:protocol-boundary-openai-anthropic
pnpm.cmd --dir backend run typecheck
```

必须覆盖：文本 / 图像 `120/30/1800` 与 `600/120/3600` 档位；270 秒只累计零可派发账号时间；SSE 等待心跳只设置传输提交；心跳后非流式响应断开或写协议重试事件而不拼接 JSON；通用上游状态 / JSON / SSE 透明转发；精确客户端仍保留专属语义；图像 provider 600 / 900 秒边界和 AbortSignal 传播；图像 lane 不进入 `speed_first` 慢样本。

检查点：系统不存在特征规则管理接口；前端菜单不出现“特征规则”入口。只有精确识别的 Codex、Claude Code、Gemini CLI 客户端画像允许把确认的 `invalid_request_error` 类协议失败纳入切号，并由专属回归覆盖“切号成功”“第三账号救回”“全部账号失败统一兜底”和“本地屏蔽耗尽快速失败”路径；通用客户端仍原样转发完整响应。

检查点：系统设置页能保存并回显；流式异常达到阈值后账号列表会显示临时不可调用、限流中、长期不可用或异常摘要；临时不可调用与限流状态可以通过账户列表“更多”菜单的专用恢复动作清理冷却与最近错误并恢复调度，不能通过人工测试恢复。冷却到期后的后台复测会自动处理 `temporary_unavailable` 和 `rate_limited`；复测连续失败会按指数退避，单次暂停不超过最大暂停时间，超过 `cooldownAccountRetestMaxBackoffHours` 表达的快速恢复窗口后进入长期不可用，并固定每 1 小时复测；从观察起点满 7 天仍失败时转为 `error`，写入 `cooldown_retest_observation_timeout`。长期复测间隔是状态机常量，不提供系统设置。“启用账户”只用于真正停用的账户，不用于清理临时状态。账户测试只提供人工诊断，停用、异常、待检查、临时不可调用和限流账户的测试成功或失败都不得改写运行态、健康信号、账户内 API Key 或调度状态。后台激活检查、周期健康检查、冷却复测、OAuth 刷新和网关异步错误处理必须遵守各自状态边界，不能绕过停用硬边界或普通显式异常硬状态。

账户健康检查优先队列回归执行 `pnpm --filter juhe-ai-backend test:retry-queue-priority` 和 `pnpm --filter juhe-ai-backend test:account-health-check-dispatch`。必须验证首次激活优先于普通周期复检、普通任务不能占满全部并发槽、等待中的重复任务升级为最新内容、运行中的重复任务完成后只追加一次，以及普通新增、导入、OAuth 和外部推送入口都携带首次激活原因即时投递。

AI 账户上游余额查询定向验证：

```powershell
pnpm --filter juhe-ai-backend test:account-balance-config
pnpm --filter juhe-ai-backend test:account-balance-http-contract
pnpm --filter juhe-ai-backend test:account-balance-adapters
pnpm --filter juhe-ai-backend test:account-balance-draft-guard
pnpm --filter juhe-ai-backend test:account-balance-refresh
pnpm --filter juhe-ai-backend test:worker-scheduler-partial-outcome
pnpm --filter juhe-ai-backend test:account-balance-auto-detect
pnpm --filter juhe-ai-backend test:postgres-schema-sql
pnpm --filter juhe-ai-backend test:background-job-registry
pnpm --filter juhe-ai-frontend test:account-balance-query
pnpm typecheck
pnpm --filter juhe-ai-frontend build
git diff --check
```

浏览器验收必须使用已同步当前 schema 的数据库。旧 SQLite 缺少 `balance_query_enabled`、`balance_query_config_json`、`balance_query_next_refresh_at` 时，按项目原则离线同步结构，不在运行时代码中自动补列。登录后验证物理单 API Key 显示配置区，多 Key、OAuth 和授权实例不显示；列表成功显示金额，失败只显示“查询失败”且悬浮可见原因，刷新图标按账户独立旋转，桌面与移动端均不重叠。

## 11. AI 问答定向验证

AI 问答修改必须运行全部聊天定向回归，不能只抽查页面：

```powershell
$backendChatTests = (Get-Content backend/package.json -Raw | ConvertFrom-Json).scripts.PSObject.Properties.Name |
  Where-Object { $_ -like 'test:chat-*' -and $_ -ne 'test:chat-postgres-redis-smoke' }
$frontendChatTests = (Get-Content frontend/package.json -Raw | ConvertFrom-Json).scripts.PSObject.Properties.Name |
  Where-Object { $_ -like 'test:chat-*' }

$backendChatTests | ForEach-Object { pnpm.cmd --dir backend run $_; if ($LASTEXITCODE) { throw "失败：backend $_" } }
$frontendChatTests | ForEach-Object { pnpm.cmd --dir frontend run $_; if ($LASTEXITCODE) { throw "失败：frontend $_" } }
pnpm.cmd run test:development-auto-login
pnpm.cmd run typecheck
pnpm.cmd run build
git diff --check
```

PLAN-0150-20260722T022751000Z 新增交互与恢复行为可先用以下窄回归定位，最终仍必须执行上面的全部聊天集合：

`test:chat-model-lazy-loading` 必须覆盖：会话首屏不请求模型列表、新会话响应直接携带首个可用模型、下拉首次展开才读取 `id/name`、单模型能力按 ID 读取、同请求去重、成功结果缓存、错误不缓存，以及切换模型、切换会话和卸载时取消旧能力请求。`test:chat-gateway-mock-ai` 同时固定轻量字段集合、供应商维度默认模型、独立能力详情和含斜杠模型 ID 的编码边界。

```powershell
pnpm.cmd --dir backend run test:chat-routes-contract
pnpm.cmd --dir backend run test:chat-conversation-clear
pnpm.cmd --dir backend run test:chat-context-compaction
pnpm.cmd --dir backend run test:chat-generation-status
pnpm.cmd --dir backend run test:chat-stream-reattach
pnpm.cmd --dir backend run test:chat-content-blocks
pnpm.cmd --dir backend run test:chat-assistant-timeline
pnpm.cmd --dir backend run test:chat-transport
pnpm.cmd --dir backend run test:chat-image-generation
pnpm.cmd --dir backend run test:chat-image-lineage
pnpm.cmd --dir backend run test:gateway-multipart-image-model
pnpm.cmd --dir backend run test:chat-internal-tool-registry
pnpm.cmd --dir backend run test:chat-internal-tool-orchestrator
pnpm.cmd --dir backend run test:chat-internal-tool-sse
pnpm.cmd --dir backend run test:chat-generate-image-tool
pnpm.cmd --dir backend run test:chat-generated-image-artifact
pnpm.cmd --dir backend run test:chat-image-policy
pnpm.cmd --dir backend run test:chat-image-preview
pnpm.cmd --dir backend run test:chat-repository
pnpm.cmd --dir frontend run test:chat-mobile-toolbox
pnpm.cmd --dir frontend run test:chat-image-model-command
pnpm.cmd --dir frontend run test:chat-manual-context-actions
pnpm.cmd --dir frontend run test:chat-stream-liveness
pnpm.cmd --dir frontend run test:chat-turn-editing
pnpm.cmd --dir frontend run test:chat-conversation-optimistic
pnpm.cmd --dir frontend run test:chat-cache-sync
pnpm.cmd --dir frontend run test:chat-composer-document
pnpm.cmd --dir frontend run test:chat-message-layout
pnpm.cmd --dir frontend run test:chat-stop-generation
pnpm.cmd --dir frontend run test:chat-stream-http
pnpm.cmd --dir frontend run test:chat-conversation-load
pnpm.cmd --dir frontend run test:chat-local-cache
pnpm.cmd --dir frontend run test:chat-local-cache-indexeddb-browser
```

`test:chat-local-cache-indexeddb-browser` 会启动独立无头 Chromium 和一次性 profile，验证真实浏览器 IndexedDB 的写入、分页读取、会话隔离和清理。Windows runner 必须优先通过浏览器级 CDP `Browser.close` 正常退出，启动时禁用 Crashpad，并等待 `ChildProcess.close` 后再删除 profile；只有 CDP 关闭失败时才允许按本 run 浏览器 PID 回退终止。临时目录删除使用有界重试，完成后不得遗留本次运行的新 profile，也不得扫描或终止用户正在使用的浏览器。

这些回归必须固定：移动端页面级“对话记录”与输入级 `+ -> 添加图片` 分层，应用壳 Teleport 目标在沉浸页生命周期内稳定存在，不能因桌面/手机同时切换条件导致按钮挂载目标丢失；图片先显示本地预览、压缩最多 2 路并发，排队取消不保留原图强引用；`/compact`、`/clear` 等待服务端结果，清空与重命名/置顶使用同一会话 mutation 队列，旧同步、直接消息刷新和旧发送失败协程均不能复活消息、草稿或覆盖新请求 lifecycle；大于 100 条的同步差异必须回退到最新窗口再推进 revision；失效 pending 必须回退首个可用会话；5 秒 heartbeat 不进入消息事件；10 秒静默只查询 submission status 并重附着同一 runner，前台 watchdog、页面待确认和最终权威同步都有上限，同版本快照不能重启已经耗尽的探活周期；SSE、Images 和上游模型 reader 提前失败时必须真实 cancel；停止 HTTP 失败必须等待旧发送收口后抛给页面显示；Responses 使用 reasoning effort 时必须同时请求 `reasoning.summary=auto`；内部工具只按模型 function-calling、用户权限、provider 和路由账户能力注入，Chat/Responses 均串行执行，超时 signal 必须传给执行器，`output_item.done` 可在 `response.completed.output` 缺失时恢复工具调用；`generate_image` 经现有网关调用 `gpt-image-2`，默认 WebP，原图和最长边 640 的预览使用同一 assetId、不同对象/ETag，条件请求返回 304；消息完成、失败或取消写库前，活动 reasoning/tool/image 块必须收敛到对应终态，取消的工具组不得显示为已完成，资产提交后不能被补偿误删对象；相邻工具段在实际助手组件中聚合且不得展示原始 JSON；替换含助手生成图片的轮次必须解除旧来源，随新用户消息明确复用的资产重新绑定并延长保留期，其他资产才立即过期；复用生成图必须通过新 `user_input` 引用认领并写回图片说明，不占新上传草稿槽位、不能被未提交删除接口认领；删除后续用户引用时，原 `assistant_output` 或其他用户引用仍有效则保持资产，只有无来源且最后引用删除后才立即过期，并且旧引用记录必须同步删除；恢复被编辑前草稿时必须移除已随旧回答失效且未被新消息保留的生成图并提示用户；未接受的乐观失败轮次按新提交重试，已接受的失败/取消尾轮才携带 `replaceTurnId` 原位替换；重命名和置顶可回滚乐观更新，删除、压缩与清空不做乐观完成。

图像编辑专项还必须验证：`/image-model` 只设置当前会话注册表内的默认图像模型并可回滚乐观更新；主模型只接收最近 12 条图像谱系元数据，不能接收历史图片 Base64；`generate_image(action=edit)` 必须使用同会话明确 `assetId`，最多 5 张、合计 48 MiB；multipart `/v1/images/edits` 必须保留模型感知路由；新输出保存独立 `assetId` 并继承根图，清空会话时同步删除谱系。

真实 PostgreSQL / Redis smoke 只从 Git 忽略的私有资源或临时环境变量加载连接串，然后单独执行 `pnpm.cmd --dir backend run test:chat-postgres-redis-smoke`；文档、日志和提交中不得出现连接串。真实模型至少覆盖：默认 Markdown、代码高亮、LaTeX、Mermaid、fenced SVG、Responses Hosted Web Search、有序且终态折叠的 reasoning/tool 块、手机原生图片选择、停止后原位重新发送、大会话切换，以及文本模型自动调用本站 `generate_image` 生成和基于既有 `assetId` 二次编辑。生图验收必须检查消息首屏只请求 preview、点击与下载使用 original、原图第二次打开命中浏览器缓存或 304；真实生图如果被上游 HTTP 错误或工具能力拒绝阻断，必须同时保留 Mock 全链路成功证据和脱敏的真实失败证据，不能把外部失败写成应用成功。真实模型密钥只从私有文件或进程环境注入运行时，不写入仓库。

50 轮长会话 fixture 与 fixture preview 使用：

```powershell
pnpm.cmd --dir backend run test:chat-long-session-fixture
pnpm.cmd --dir backend run test:chat-long-session-process-tree
pnpm.cmd --dir backend run test:chat-long-session-entry-recovery
pnpm.cmd --dir backend run test:chat-long-session-module-init
pnpm.cmd --dir backend exec tsx src/scripts/regression/chat-long-session-real-e2e.ts --dry-run
```

Windows 进程树回归会启动一个短命根进程和本地 TCP 子进程，并验证持续快照只能沿本 run 已记录祖先吸收晚生或重父化后代；即使另一个开发服务在同一工作树、同一时刻启动相同 worker 命令，也不能仅凭命令或创建时间误杀。回归还会真实懒启动 runner-local SQLite read worker，确认 pool close 后该 PID 身份归零、临时 SQLite 可立即删除，并确认临时 backend 的 Node `ChildProcess` 在 OS 进程消失后仍等待 `close` 事件释放 stdio 句柄。`test:chat-long-session-entry-recovery` 直接执行真实 runner 模块，用本机 HTTP/SSE fixture 分别触发流消费异常和 hard deadline，断言 submission 查询、stop、canceled 恢复及安全 failure code；`test:chat-long-session-module-init` 直接完成整个模块求值后走 no-op main，任何变量 initializer、解构默认值、计算属性或 class static block 的真实 TDZ / throw 都会令命令失败。结构门禁只固定 `await main()` 唯一且必须是 Program 最后 statement，禁止在后置 error class、const 或 helper 初始化前启动真实流程。上述回归同时确认服务端口关闭且不访问外部网络。非 Windows 平台会明确跳过。`--dry-run` 只预览 50 轮 fixture 的阶段、探针、controls 和 artifact checkpoint 计划，不启动后端，也不验证数据库或 SSE。无需真实凭据的本地预检使用：

```powershell
pnpm.cmd --dir backend run test:local-chat-long-session-preflight
```

本地预检复用 Mock AI 全链路，在临时 SQLite / 资产目录中验证 repository seed、后端启动、SSE 和清理，不访问真实上游且不写正式验收报告。

定位真实上游第一轮失败时，先执行单轮 probe，禁止直接重跑 50 轮。probe 复用真实后端、路由和 SSE 链路，但只发送 fixture 第 1 轮，不执行评分、压缩或第 51 轮检查，也不写正式报告；成功输出不含 prompt、assistant output 或凭据，失败只输出经凭据、本轮 prompt 和失败前 output 脱敏后的有界 `type/code/message`：

```powershell
$env:JUHE_AI_CHAT_REAL_CREDENTIAL_FILE = 'D:\private\chat-real-credential.json'
pnpm.cmd --dir backend run test:real-chat-long-session-probe
Remove-Item Env:\JUHE_AI_CHAT_REAL_CREDENTIAL_FILE -ErrorAction SilentlyContinue
```

真实 50 轮验收会产生真实模型费用，必须单独执行，不能并入上面的 `test:chat-*` 自动集合。推荐从现有私有凭据文件读取 `baseURL`、API Key 和支持模型；也可以显式提供三个环境变量。脚本只在真实模式写报告，所有数据库、资产和日志都位于临时目录，默认报告从仓库根目录写入带 UTC 时间和 run hash 的独占文件 `reports/chat-long-session-real-YYYY-MM-DD_HH-mm-ss-sss-<runHash>.json`：

```powershell
$env:JUHE_AI_CHAT_REAL_CREDENTIAL_FILE = 'D:\private\chat-real-credential.json'
pnpm.cmd --dir backend run test:real-chat-long-session
Remove-Item Env:\JUHE_AI_CHAT_REAL_CREDENTIAL_FILE -ErrorAction SilentlyContinue
```

凭据文件支持 JSON 的 `baseURL` / `baseUrl`、`apiKey` / `api_key`、`models` 字段，也兼容现有逐行 Base URL、Key、模型格式。需要自定义报告位置时设置 `JUHE_AI_CHAT_REAL_OUTPUT`；仅排障时可设置 `JUHE_AI_CHAT_REAL_KEEP_TEMP=1` 保留临时目录。报告只允许哈希 ID、SSE 时延/事件数/终态、usage 白名单、context/checkpoint/asset/消息账本和文件大小环境观察，不得加入完整 prompt、response、artifact、cookie、Authorization、API Key、图片说明或请求响应快照。

真实长会话脚本通过内部 `JUHE_AI_DISABLE_BASE_ENV=true` 隔离 `backend/.env`，并强制使用临时 SQLite 与进程内 memory cache/state/queue，同时关闭 usage writer 与 Codex context state writer 子进程池；开发机或服务器上的 PostgreSQL、Redis、外部工具端点、真实文件路径和 performance 配置不会进入当前验收进程或 child。runner 自身以 `db-service` 角色查询持久化 usage，会懒启动一个 runner-local SQLite read worker；cleanup 必须按“停止临时 backend 全树并等待 `ChildProcess.close` -> 关闭 runner-local read worker pool 并验证其 PID 身份归零 -> 关闭 runner SQLite -> 删除临时根目录”的顺序执行。每次删除遇到 `EBUSY` 都通过 Windows Restart Manager 采集持锁进程；单次诊断必须在 2 秒后终止其完整子进程树并降级为 unavailable，不能阻塞后续删除重试。错误只允许记录目标文件 basename / 路径 hash、PID、命令类别和是否属于本 run 身份，禁止输出完整路径、命令行或文件内容。该开关在普通生产启动中默认关闭，不改变既有 `.env` 加载行为。会话策略固定为保留 3 天、最多 50 个会话、每会话 50 个用户轮次。runner 在 Windows 上每 250ms 沿本 run root 的已记录祖先链持续扩展进程身份，cleanup 前最后采样，再按 PID、创建时间和完整命令行精确停止；不得扫描并终止同工作树的开发服务 worker。

50 轮内容不再要求每轮重复完整大 HTML：第 `1、5、10、16、17、20、25、30、31、38、40、41、43、47、50` 轮是 artifact checkpoint，返回累计完整 HTML；其余轮只返回严格有界 JSON manifest，包含当前 `introducedFeatureId`、不超过 200 字的决策摘要、累计锚点和已确认禁止项，禁止夹带 HTML 或 Markdown 代码块。checkpoint 只能返回一个 `html` code fence，禁止解释、HTML/CSS 注释、重复示例、占位与冗余内容；完整 artifact 必须保持可读但高度紧凑，整个 assistant output 的 UTF-8 字节数不得超过 `32768`。runner 在 completed 边界检查实际字节数，超限记为 `chat_long_session_artifact_too_large` deterministic quality failure，不把它伪装成连接异常继续重试。评分只在 checkpoint 检查累计 requirements、anchors、forbidden 与相邻 checkpoint continuity，中间轮独立计算 `manifestAccuracy`，最终轮仍必须覆盖至少 90% 的 50 项真实 HTML/CSS evidence；32 KiB 上限不放宽评分要求。

单轮 hard deadline 为 15 分钟；heartbeat 不算上游事件或内容进度，连续 180 秒没有非 heartbeat 事件，或连续 300 秒没有文本、reasoning、tool 生命周期进度时才显式 stop。报告只记录 `lastEventAt`、`lastDeltaAt`、`eventCount`、`partialBytes` 和 idle 时长，不记录部分正文。deadline、畸形 SSE、累计响应/内存上限等任何 reader 消费异常都必须先在 2 秒内有界 cancel，再立即进入最长 30 秒的 submission 确权和 stop，禁止继续等待单轮 15 分钟上限。上游 `terminated`、`stream terminated`、`socket hang up`、`premature close` 和 `ECONNRESET` 等精确连接终止语义按 transient 处理并原位替换；同一 code/message 一旦包含 invalid request、policy violation、unsupported parameter 等确定性业务语义，必须由确定性分类优先，不能因同时出现 terminated 而重试。transient 总尝试数固定为 3，第 2、3 次 submit 前分别通过全局 run budget 等待 `2000ms`、`5000ms`，禁止连续贴着重打；attempt metric 只新增安全的 `delayMs`，首轮为 0。stop 权威收口为 canceled 后，runner 使用新 `clientMessageId + replaceTurnId` 原位恢复；普通产品交互也允许用户显式编辑最近已停止轮次，仍禁止替换旧轮次或 streaming 轮次。runner 与生产 Chat 上游统一读取 `JUHE_AI_CHAT_UPSTREAM_SSE_MAX_EVENTS`，默认 `65536`，只允许 `2048..262144` 的整数；hermetic 验收环境固定为 `65536`。未确认现有 Chat 请求合同支持前，验收脚本不得自行注入 `max_output_tokens`。

定向回归还必须覆盖两类有界读取：Chat Completions 与 Responses SSE 均需验证超过 2048 个合法小 delta 在默认 `65536` 预算下完成、显式 `2048` 预算拒绝第 2049 个事件、单事件与 pending buffer 保持 `64 KiB`、Responses content/aux 保持 `192 KiB`；模型列表 / 上游错误普通 JSON 响应需在读取流时立即执行总字节上限。运行配置回归必须覆盖 `JUHE_AI_CHAT_UPSTREAM_SSE_MAX_EVENTS` 的默认值、`2048..262144` 边界和非法整数 fail-fast。事件预算提高是合法长输出与 CPU DoS 防护之间的权衡，禁止移除事件计数或改为保存事件数组，也禁止先 `response.text()` 完整读入内存后再截断。

Browser 验收使用 `/__aisys__/my-chat`，至少检查桌面默认视口、`390x844`、`360x800` 和高度压缩到 `420px` 的软键盘代理视口：全局 Header 隐藏、内容区零外边距、聊天工作区跟随 `visualViewport` 且 body 不外滚，消息区仍有正高度；用户右侧、助手左侧、无头像和角色名；工具重复调用聚合且不显示原始事件 ID/JSON；输入框发送后立即出现用户气泡和动态助手占位；空白生成显示计时思考，过程出现后让位并按完成态折叠；移动端左上“对话记录”和输入框左下 `+ -> 添加图片` 无重叠，原生文件选择、预览、发送可用；流式输出期间用户上滚会中断自动跟随，输入框或键盘变高不会把已离底用户拉回，“回到底部”恢复跟随；会话抽屉的新建、选择和可见更多菜单可用；失败/停止尾轮可以从原用户消息重新发送；粗指针关键操作命中区至少 `44x44px`。复制功能需要同时覆盖 Clipboard API 和选区回退；受控浏览器若同时禁止两种能力，应显示失败提示，不能把权限限制误判为消息渲染失败。

编辑器与手机端定向回归执行：

```powershell
pnpm.cmd --dir frontend run test:chat-composer-editor-keydown
pnpm.cmd --dir frontend run test:chat-mobile-interaction
```

第一条使用测试 DOM 挂载真实 Tiptap `EditorView`，通过 StarterKit 的 `handleTextInput` 链逐字符输入列表，并用真实 `EditorState` 和结构块命令验证列表续项、双 Enter 退出、标题退出后普通 Enter 发送、引用/代码块、Shift、Ctrl/Cmd 和 IME；第二条固定动态视口、抽屉菜单语义与回焦、抽屉关闭后再打开创建 Modal 的状态顺序、ResizeObserver 程序滚动窗口、虚拟列表高度口径、安全区和具体粗指针命中规则。Browser 自动化验证 Markdown input rule 时必须逐键输入，直接 fill 整个 contenteditable 文本不会触发 StarterKit 输入规则，不能据此判断真实键盘路径。

发送恢复验收还必须覆盖专用提交状态：连续 `not_found` 才恢复草稿，`preparing` 保持等待，`accepted + streaming` 经 stop 收口到终态，状态接口持续网络失败时保持防重复门禁；停止按钮在服务端尚未返回 `turnId` 的 preparing 窗口也必须先 abort 本地提交，并以 `clientMessageId` 收口，不能因一次 `chat_generation_not_found` 让原请求继续生成；刷新页面后从 `sessionStorage` 恢复相同 `clientMessageId` 继续确权。服务重启后数据库已有 streaming、但进程内流句柄丢失的轮次必须能由 stop 收口，不能依赖 20 分钟后的维护任务。

## 12. PLAN-0143 供应商认证与协议验收

本次供应商扩展在独立 `codex/oauth-provider-expansion-master-final` worktree 验收，不能在迁移分支执行写入或提交。专项回归至少覆盖：

```powershell
pnpm.cmd test:gemini-google-oauth-token
pnpm.cmd test:gemini-interactions
pnpm.cmd test:xai-provider
pnpm.cmd test:provider-auth-account-credential
pnpm.cmd test:provider-auth-account-capability
pnpm.cmd test:xai-account-capability
pnpm.cmd --filter juhe-ai-backend test:account-test-request
pnpm.cmd --filter juhe-ai-backend test:account-health-check-endpoint-mode
pnpm.cmd test:model-catalog
pnpm.cmd --filter juhe-ai-backend typecheck
pnpm.cmd --filter juhe-ai-frontend typecheck
pnpm.cmd --filter juhe-ai-frontend test:account-import-protocol
pnpm.cmd --filter juhe-ai-frontend test:account-test-run-session
go test ./...
go vet ./...
git diff --check
```

验收重点：OpenAI OAuth 刷新与授权会话不能因下游取消而留下半状态；`codex-auto-review` 保持无独立价格和 Responses-only，新建 GPT API Key / OAuth 账户默认写入支持模型，但不进入通用 OpenAI-compatible 默认模型或默认健康检查；xAI 只提供官方 API Key 与 OpenAI Chat / Responses；Anthropic 当前开放 API Key 与 Bearer Token 型 OAuth Access Token，`workload_identity` 不得出现在账户类型、导入协议、前端表单或运行时 token exchange 中。官方 WIF 仅保留能力调研，后续只有具备受控的服务端 assertion provider、能够自动获取和轮换可信 JWT 并校验 `exp` 时才可重新评估，Claude 订阅网页登录 / Setup Token / Cookie / sessionKey 私有代理链路仍明确不支持。Gemini Google OAuth 使用官方 token endpoint，access-only 凭据不自动刷新。Interactions JSON / SSE 使用官方 `/v1beta/interactions` 资源族、JSON `event_type` 和 `metadata.total_usage`，POST 创建流以 body `stream: true` + Accept 协商，GET 资源流以 `?stream=true` 协商，禁止追加 `alt=sse`；DELETE 2xx 空 body 与 cancel 路径必须有定向回归。xAI 工具调用费用和 Gemini 长上下文分档在当前价格 schema 无法精确表达时，必须保留限制说明，不得伪造成本。
