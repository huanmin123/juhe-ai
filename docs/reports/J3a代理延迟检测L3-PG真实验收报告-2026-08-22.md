# J3a 代理延迟检测 L3 PostgreSQL/PgBouncer 真实验收报告

## 结论

在 dev 环境完成受控 PostgreSQL smoke：一次性 scratch 数据库通过 PgBouncer `6432` 连接，Go `ResultProjector` 真实写入 `juhe_business.proxy_profiles`、receipt 与 cursor；现役 Node 代理管理 PG CRUD/CAS smoke 也已通过。当前代码已完成一次性 Go owner 接线，Node J3a scheduler、旧 executor、outcome reader/projector 和 business writer 已删除。

本报告不代表生产已发布。真实 PostgreSQL/PgBouncer 结果仅覆盖本次临时 scratch 数据库、临时角色和本次测试窗口；真实 Go 手动进程联调、active-path-zero、发布重启与生产/L4 仍未执行。

## 2026-08-23 受控复验

本轮创建一次性 `juhe_ai_sub2api_dev_j3a_projector` scratch，初始化当前业务 schema 与 `juhe_jobs` schema 后，经 PgBouncer `6432` 运行 `TestPostgresResultProjectorSmoke`，退出码为 `0`；同时以受控 dev 环境运行 `JUHE_AI_ENV_FILE=.local/project-resources/dev/env/shared.env pnpm.cmd run test:proxy-postgres-smoke`，退出码为 `0`，验证现役 Node 代理管理 CRUD、CAS、微秒版本保留、并发合并和索引计划。Go business-result projector 在同一 scratch 的 committed outcome fixture 覆盖：

- Go reader 按 `(stored_at, outcome_id)` 读取并保留微秒级 `observed_at`；
- Go projector applied 路径更新 `test_status/latency_ms/last_test_message/last_tested_at`，不改 `updated_at`；
- config revision 不匹配返回 `stale` 且不写业务状态；
- `applied` receipt、cursor 单调推进和重复 outcome replay 均通过；
- config revision stale、删除对象 ignored、无 provider unknown 的 CAS 语义均通过；
- applied 后的 `ProjectManualOutbound` CAS 写入 `outbound_ip/outbound_region` 均通过。

本轮同时修复了两项真实环境暴露的问题：PostgreSQL lease takeover 改用数据库 `statement_timestamp()` 判断过期，避免 PgBouncer 客户端时钟微偏导致 successor 误判 busy；Go reader 不再依赖 Go outcome 表中不存在的 `overall_status` 行列，状态继续由严格校验的 JSONB payload 提供。Node J3a outcome reader/projector 与其 fixture 已删除。

Go PG projector 复验入口为 `backend-go/projects/jobs` 下的 `TestPostgresResultProjectorSmoke`（仅通过 `J3A_PG_SMOKE_JOBS_URL` 与 `J3A_PG_PROJECTOR_URL` 接收受控 DSN，退出码 `0`）；该命令插入并清理 committed outcome 与 proxy fixture，断言 applied/stale/deleted/no-target、receipt、cursor、CAS、replay 及 outbound diagnostics。Node 侧只保留 `pnpm.cmd --dir backend typecheck` 与 Go-only 路径删除回归，不再有 Node outcome reader/projector 命令。

上述 Go 验证使用受控 committed outcome fixture，证明 Go reader/projector 事务路径，不等同真实生产服务已发布；Node→Go 手动进程联调、active-path-zero、owner handoff、重启和生产/L4 仍未完成。

## 环境与边界

- 环境：dev PostgreSQL 18.4，服务端 `192.168.1.203`，应用连接经 PgBouncer `6432`；PgBouncer pool mode 为 transaction。
- 数据库：固定 `juhe_ai_sub2api_dev_j3a_` 前缀下的一次性 scratch 数据库；未使用 `juhe_ai_sub2api_dev` 主开发库承载 Go projector fixture。
- 角色：测试使用已受控的 dev app role；密码、DSN、SCRAM verifier、连接串均未写入报告或日志。该 smoke 验证 Go projector 的业务写回，不替代部署期的最小权限 role manifest。
- 未访问：生产数据库、Redis、Docker 业务服务、生产发布链路；本轮只在临时 scratch 验证 Go PostgreSQL result projector，未修改 `shared.env`。

## 真实 smoke 覆盖

`TestPostgresResultProjectorSmoke` 通过以下路径：

- jobs Store `Ping`、jobs schema owner 检查和 `EnsureSchema`；
- `IssueInput`、owner/proxy lease、`AdmitExecution`、execution claim；
- `AppendOutcome` 首次提交、`ProjectOutcome` applied、receipt、cursor 和相同 outcome replay；
- `proxy_profiles` 的 test status、latency、last tested、outbound diagnostics CAS；
- config revision stale、删除对象 ignored、无 target unknown 的 fail-closed 分支；
- proxy/owner lease 与临时 fixture 清理。

本轮 smoke 原始结果为 `TestPostgresResultProjectorSmoke PASS`，测试进程退出码 `0`。

## 清理与残留核验

- scratch database：已删除；
- 临时 fixture：测试自身清理；scratch database 在复验后删除；
- Redis、Node business SQLite：未创建或修改；
- Go J3a owner：代码路径已切为 Go-only；生产发布开关未在本报告中执行。

## 本地验证

- `go test -count=1 ./...`：通过（退出码 `0`）；
- 五个 `go.work` module 的 `go test -race -count=1 ./...`：当前复跑通过；J3a business-result projector PostgreSQL applied/stale/receipt/cursor/CAS 集成由显式 `J3A_PG_PROJECTOR_URL` 门禁复跑通过；
- `go vet ./...`：通过（退出码 `0`）；
- `go test -count=1 ./internal/proxylatency`：通过；
- `go test -race -count=1 ./internal/proxylatency`：通过；
- `go vet ./internal/proxylatency`：通过；
- `JUHE_AI_ENV_FILE=.local/project-resources/dev/env/shared.env pnpm.cmd run test:proxy-postgres-smoke`：通过（退出码 `0`；临时代理行已清理）；
- `pnpm.cmd --dir backend run test:proxy-latency-go-only`：通过（退出码 `0`）；
- `pnpm.cmd --filter juhe-ai-backend typecheck`：通过（退出码 `0`）；
- `git diff --check`：通过。

早期历史快照曾记录范围外编译或外网错误；该问题已不再代表当前工作树。当前五个 `go.work` module 的普通 test、race 与 vet 通过；真实 Node→Go 进程 handoff、active-path-zero、重启恢复和生产/L4 仍按独立门禁记录，不能用旧报告替代。

Go result projector 的测试直接覆盖 outcome 表及业务事务；Node J3a reader/projector fixture 已删除。Go PostgreSQL smoke 覆盖 `NOT NULL`、`UNIQUE`、`DEFAULT` 和索引约束，不在 Node SQLite fixture 中重复声明。

## 后续门禁

本报告不宣称生产部署或 L4 发布已完成。J3a 运行时已无 Node fallback/双写路径；仍需按 L3/L4 计划验证 Go 服务启动、真实 Node→Go 手动联调、active-path-zero、Redis/Docker、部署与重启证据。
