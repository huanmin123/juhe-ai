# 高性能模式压测报告（2026-06-26）

> 历史容量参考。记录 `PostgreSQL + Redis` 高性能模式在 `<测试主机IP>` 测试栈上的 System API、真实网关链路和可靠性演练结果；不作为当前部署操作手册。本文不记录任何真实连接串或密码。

## 测试范围

- 被测链路：管理端 System API 的登录后读写链路，覆盖设置公开读取、当前用户、供应商、分组、AI 账户、API Key secret 和账号 PATCH。
- 存储模式：`JUHE_AI_RUNTIME_MODE=performance`、`JUHE_AI_DATABASE_DRIVER=postgres`、`JUHE_AI_CACHE_DRIVER=redis`、`JUHE_AI_RUNTIME_STATE_DRIVER=redis`、`JUHE_AI_QUEUE_DRIVER=redis_stream`。
- 中间件：远端 Docker PostgreSQL / PgBouncer / Redis cache / Redis state。
- 压测脚本：`pnpm --filter juhe-ai-backend test:performance-system-api-load`。
- 网关压测脚本：`pnpm --filter juhe-ai-backend test:performance-gateway-load`，启动真实后端、DB service、worker、Redis Streams 队列和本地 mock OpenAI 上游。
- 可靠性演练脚本：`pnpm --filter juhe-ai-backend test:performance-reliability-drill`。

补充说明：2026-06-26 已新增 `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`，用于验证网关事实表写入和关键读路径在 PostgreSQL 模式下的 adapter readiness。该命令不是吞吐压测，只用于判断完整 `/v1/*` 网关压测是否具备前置条件。

补充说明：2026-06-27 已新增 `pnpm --filter juhe-ai-backend test:performance-reliability-drill`，用于验证 Redis Streams pending 接管、Redis state 重启后积压恢复、PostgreSQL 逻辑备份恢复到临时库、PostgreSQL / PgBouncer 重启后连接恢复和 PG 阻塞快照。

## 正式结果（相同模型配置校验短路后）

执行时间：2026-06-26 21:31:54 至 21:32:38（Asia/Shanghai）。

本轮优化：

- 账号 PATCH 请求里 `groupId` 与当前绑定分组一致时，不再重复执行分组校验和分组重绑。
- 网关运行时缓存失效携带 `reason`；`account_updated` 只清网关运行时缓存，不再误清 `system_settings` 仓储缓存。
- `settings_updated` 和无 `reason` 的兼容清理仍会清 `system_settings` 仓储缓存，避免设置更新后读到旧值。
- 账号 PATCH 提交的 `supportedModels` / `modelMappings` 归一化后与当前值完全一致时，直接复用当前配置，不再重复查询 provider catalog / 跨协议模型池。

关键参数：

```powershell
$env:JUHE_AI_DB_POOL_MAX = "150"
$env:JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT = "256"
$env:JUHE_PERFORMANCE_LOAD_DURATION_MS = "30000"
$env:JUHE_PERFORMANCE_LOAD_CONCURRENCY = "100"
$env:JUHE_PERFORMANCE_LOAD_ACCOUNT_COUNT = "25"
$env:JUHE_PERFORMANCE_LOAD_WRITE_RATIO = "0.15"
$env:JUHE_PERFORMANCE_LOAD_RESET_PG_STAT_STATEMENTS = "true"
pnpm --filter juhe-ai-backend test:performance-system-api-load
```

结果摘要：

| 指标 | 结果 |
| --- | --- |
| 总请求 | 20373 |
| HTTP 成功 / 失败 | 20373 / 0 |
| 端到端脚本 RPS | 455.09 |
| 30 秒负载窗口估算 RPS | 679.1 |
| 总体延迟 | p50 129.10ms，p95 335.60ms，p99 429.41ms，max 718.19ms |
| PATCH `/accounts/:id` | p50 309.66ms，p95 451.17ms，p99 549.14ms，max 718.19ms |

PostgreSQL 观察结果：

| 指标 | 结果 |
| --- | --- |
| deadlocks delta | 0 |
| 最大 active 连接 | 17 |
| 最大 idle in transaction | 14 |
| 最大 lock waiters | 6 |
| 最大 not granted locks | 0 |
| 最大事务时长 | 0.38s |
| 最大 active query 时长 | 0.35s |

慢 SQL 观察：

- `UPDATE juhe_business.accounts ...` 调用 3026 次，mean 12.202ms，max 406.065ms，仍是最主要写入热点。
- `UPDATE juhe_business.group_accounts ...` 调用 3026 次，mean 0.156ms，max 1.186ms。
- 本轮慢 SQL 列表中不再出现 `system_settings` 白名单查询；对比本轮优化前，该查询曾调用 11813 次、返回 649715 行。
- 本轮慢 SQL 列表中不再出现每次 PATCH 都触发的分组详情校验查询，也不再出现每次 PATCH 都触发的 provider catalog / 跨协议模型池校验查询；相同分组提交只保留必要的绑定优先级轻量更新。

结论：在 `100` 并发、`25` 个账号分摊写入、15% 写请求下，本轮 System API 高性能模式压测通过。未观察到死锁、大事务、长期阻塞或长时间 active query。对比同参数本轮基线，总体 p95 从 648.22ms 降到 335.60ms，PATCH p95 从 777.49ms 降到 451.17ms，端到端脚本 RPS 从 341.17 提升到 455.09。剩余主要热点已经集中到账号主表 `UPDATE accounts`；继续优化会触及主表写入形态、行锁和语义校验，负优化风险明显升高，建议本轮到此停止。

## 本轮对比

同样 `100` 并发、`25` 个账号、15% 写请求：

| 指标 | 本轮基线 | 相同分组校验跳过 | settings cache 修正 | 相同模型配置短路 |
| --- | --- | --- | --- | --- |
| 报告文件 | `performance-system-api-baseline-20260626-191417.json` | `performance-system-api-group-skip-20260626-192855.json` | `performance-system-api-settings-cache-20260626-193612.json` | `performance-system-api-model-config-skip-20260626-213152.json` |
| 总请求 | 15020 | 13843 | 17967 | 20373 |
| HTTP 失败 | 0 | 0 | 0 | 0 |
| 端到端脚本 RPS | 341.17 | 306.79 | 394.66 | 455.09 |
| 总体 p95 | 648.22ms | 660.83ms | 527.59ms | 335.60ms |
| PATCH p95 | 777.49ms | 783.83ms | 660.05ms | 451.17ms |
| deadlocks delta | 0 | 0 | 0 | 0 |
| 最大事务时长 | 0.14s | 0.32s | 0.33s | 0.38s |
| 最大 active query 时长 | 0.12s | 0.16s | 0.08s | 0.35s |
| 最大 lock waiters | 8 | 10 | 13 | 6 |

相同分组校验跳过能删除一条冗余查询，但单独压测没有稳定改善 p95；它作为低风险查询收敛保留，不作为主要性能提升来源。主要收益来自缓存失效粒度修正和相同模型配置短路：前者避免账号更新反复冲掉系统设置缓存，后者避免重复查 provider catalog / 跨协议模型池。

## 被压测否决的优化

尝试过“账号 UPDATE 按 payload 动态省略凭据列”。该方案在类型检查和功能回归上可通过，但压测变差，已回滚：

| 指标 | 本轮基线 | 动态省略凭据列 |
| --- | --- | --- |
| 报告文件 | `performance-system-api-baseline-20260626-191417.json` | `performance-system-api-optimized-20260626-192351.json` |
| 总请求 | 15020 | 13345 |
| 端到端脚本 RPS | 341.17 | 284.11 |
| 总体 p95 | 648.22ms | 743.47ms |
| PATCH p95 | 777.49ms | 883.68ms |
| deadlocks delta | 0 | 0 |

结论：这类动态 SQL 形态可能破坏 PostgreSQL statement 复用和执行稳定性，实际收益不成立。后续账号主表写入优化必须继续以压测结果为准，不能只按“少写字段”直觉推进。

## 历史对比

账号更新写路径优化前，同样 `100` 并发、`25` 个账号、15% 写请求的历史结果：

| 指标 | 账号写路径优化前 | 账号写路径优化后 | 本轮最终 |
| --- | --- | --- | --- |
| 总请求 | 10875 | 15216 | 20373 |
| HTTP 失败 | 0 | 0 | 0 |
| 总体 p95 | 1138.22ms | 645.22ms | 335.60ms |
| 总体 p99 | 1349.01ms | 753.57ms | 429.41ms |
| PATCH p95 | 1385.42ms | 765.13ms | 451.17ms |
| PATCH p99 | 1538.8ms | 844.65ms | 549.14ms |
| 最大事务时长 | 0.89s | 0.16s | 0.38s |
| 最大 active query 时长 | 0.57s | 0.10s | 0.35s |

## 探索性风险

### 查询 / 搜索 / 筛选索引边界

2026-06-26 已补做一轮列表、搜索、筛选、分页窗口和统计 fan-out 守护检查。通过项：

- API Key 列表：搜索只按名称精确 / 前缀匹配，列表不返回完整密钥。
- AI 账户列表：搜索只按账户名称精确 / 前缀 / 词项索引候选包含匹配，不裸扫 `accounts` 主表；分组使用独立筛选。
- 使用记录列表：`model` 精确匹配，`clientIp` 前缀范围匹配；候选窗口先读 usage catalog 固定窗口，再按当前页 ID 回 usage shard 补详情。
- 审计 / 操作 / 运行日志：结构化过滤避免前导通配符，操作日志摘要搜索走倒排词项索引；深翻页 offset / 合并窗口限制在 1000 行级别。
- 基础管理列表：API Key、账户、分组、用户、代理、公告、外部来源、团队、统一授权和授权用量详情 offset 均限制在 1000 行内。
- 授权候选项、账号用量、授权用量：候选项支持精确 / 前缀搜索和 limit；账号用量先解析账号 ID，窗口查询不接收前导通配符；授权用量按窗口分页。
- 统计聚合：基础统计 upsert statement 在 batch 内复用；usage stats / client IP stats 单次聚合只访问有界 shard 窗口并轮转推进。
- 数据维护：删除清理、详情读取和过期清理查询使用目标索引并避免临时排序；网关 API Key 路由绑定读取固定 20 条窗口并命中路由索引。

已运行命令：

```powershell
pnpm --filter juhe-ai-backend test:api-key-list-query-guard
pnpm --filter juhe-ai-backend test:account-list-query-guard
pnpm --filter juhe-ai-backend test:usage-record-list-query-guard
pnpm --filter juhe-ai-backend test:usage-record-list-catalog-window
pnpm --filter juhe-ai-backend test:log-list-query-guard
pnpm --filter juhe-ai-backend test:log-list-page-window
pnpm --filter juhe-ai-backend test:management-list-page-window
pnpm --filter juhe-ai-backend test:base-list-page-window
pnpm --filter juhe-ai-backend test:authorization-options-query-guard
pnpm --filter juhe-ai-backend test:account-usage-query-guard
pnpm --filter juhe-ai-backend test:authorization-usage-pagination
pnpm --filter juhe-ai-backend test:usage-stats-batch-statement
pnpm --filter juhe-ai-backend test:record-maintenance-index-guard
pnpm --filter juhe-ai-backend test:resource-authorization-expiry-batch-boundary
pnpm --filter juhe-ai-backend test:gateway-api-key-route-query-guard
pnpm --filter juhe-ai-backend test:settings-query-boundary
pnpm --filter juhe-ai-backend test:external-public-account-usage-query-guard
pnpm --filter juhe-ai-backend test:usage-stats-shard-fanout-boundary
pnpm --filter juhe-ai-backend test:client-ip-stats-shard-fanout-boundary
pnpm --filter juhe-ai-backend test:group-account-stats-dirty-cache
```

边界说明：以上守护主要覆盖 SQLite / 源码 / 查询形态约束，用于防止退回无界扫描、深 offset、前导通配符和请求链路实时聚合；它不能替代 PostgreSQL 大表下的真实 `EXPLAIN (ANALYZE, BUFFERS)`。当前远端 PG 经过 System API 压测后，`pg_stat_user_tables` 热点仍集中在 `accounts`、`group_accounts`、`account_supported_models`、`account_model_mappings` 和账号名称搜索词项表，usage / audit / stats 没有真实网关流量样本。后续必须用 `/v1/*` 网关真实压测和 worker 消化压测单独验证 usage、audit、operation log、public API log 和 stats 窗口表。

### 网关事实表 PG readiness

2026-06-26 针对真实远端 PostgreSQL / Redis 栈补了网关事实表 readiness 门禁，命令：

```powershell
pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness
```

该命令会在 `JUHE_AI_RUNTIME_MODE=performance` 下直接调用 usage、audit、operation log、public API log 和 usage stats 的关键写入 / 聚合入口，并输出 JSON 报告。它会创建带唯一 trace 前缀的临时 usage / audit / operation / public API 样本，结束时清理 PG 测试行和审计 payload 测试文件。

初始结果：`usage_records_write`、`audit_logs_write`、`operation_logs_write`、`public_api_logs_write`、`usage_stats_aggregate` 全部被 SQLite fail-fast 拦截，说明此前 System API 压测不能代表网关事实表已经 PG-ready。

本轮已完成：

- `createUsageRecordsBatchAsync` 在 PostgreSQL 模式下支持批量写入 `juhe_usage.usage_records`、`usage_record_shard_entries`、usage shard catalog，并更新账号 `last_used_at` / 健康成功信号。
- `createAuditLogsBatchAsync` 在 PostgreSQL 模式下支持审计 metadata、attempt、payload ref、payload blob 表、payload blob 文件写入和审计错误分组 upsert。
- `createOperationLogsBatchAsync` 和 `createPublicApiLogsBatchAsync` 已支持 PG 写入；操作日志摘要搜索词在 PG 下改为批量 INSERT，单条 readiness 延迟从 1243.47ms 降到约 61ms。
- `aggregateUsageStatsBatchAsync` 已支持 PG 核心统计聚合：按 `juhe_usage.usage_records(created_at, id)` 全局游标读取，批量写入 `juhe_stats.usage_stats_totals/minute/hourly/daily/weekly/monthly`，并更新 `stats_job_state`。
- readiness 最终复测全绿：`usage_records_write` 33.30ms、`audit_logs_write` 62.22ms、`operation_logs_write` 60.06ms、`public_api_logs_write` 17.70ms、`usage_stats_aggregate` 79.98ms。
- 本轮复测报告：`reports/performance-gateway-persistence-readiness-20260626-final.json`。
- 2026-06-27 远端 schema-only 同步补齐 `usage_records` 增量列后再次复测全绿：`usage_records_write` 31.00ms、`audit_logs_write` 64.38ms、`operation_logs_write` 58.29ms、`public_api_logs_write` 13.04ms、`usage_stats_aggregate` 42.47ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T204430Z.json`。
- 2026-06-27 补齐 usage model / error / latency / account quality PG 派生聚合后再次复测全绿：`usage_records_write` 36.84ms、`audit_logs_write` 40.40ms、`operation_logs_write` 61.41ms、`public_api_logs_write` 11.67ms、`usage_stats_aggregate` 173.05ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T211555Z.json`。
- 2026-06-27 补齐 PG quota snapshot 后再次复测全绿：`usage_records_write` 43.37ms、`audit_logs_write` 52.88ms、`operation_logs_write` 62.43ms、`public_api_logs_write` 12.81ms、`usage_stats_aggregate` 170.89ms、`gateway_quota_snapshot_pg` 143.82ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T213627Z.json`。
- 2026-06-27 扩展直接账户授权和直接分组授权 quota snapshot 后再次复测全绿：`usage_records_write` 51.51ms、`audit_logs_write` 56.21ms、`operation_logs_write` 56.26ms、`public_api_logs_write` 13.30ms、`usage_stats_aggregate` 133.42ms、`gateway_quota_snapshot_pg` 96.24ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T214514Z.json`。
- 2026-06-27 扩展团队账户授权和团队分组授权 quota snapshot 后再次复测全绿：`usage_records_write` 51.10ms、`audit_logs_write` 50.15ms、`operation_logs_write` 61.07ms、`public_api_logs_write` 9.70ms、`usage_stats_aggregate` 160.78ms、`gateway_quota_snapshot_pg` 68.35ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T215731Z.json`。
- 2026-06-27 扩展 PG 审计错误分组后再次复测全绿：`usage_records_write` 61.39ms、`audit_logs_write` 81.52ms、`operation_logs_write` 38.24ms、`public_api_logs_write` 9.87ms、`usage_stats_aggregate` 150.37ms、`gateway_quota_snapshot_pg` 94.36ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T220339Z.json`。
- 2026-06-27 扩展 PG 审计读路径后再次复测全绿：`usage_records_write` 55.20ms、`audit_logs_write` 150.46ms、`operation_logs_write` 61.09ms、`public_api_logs_write` 11.57ms、`usage_stats_aggregate` 166.59ms、`gateway_quota_snapshot_pg` 96.97ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T221516Z.json`。
- 2026-06-27 扩展 PG 操作日志读路径后再次复测全绿：`usage_records_write` 46.71ms、`audit_logs_write` 138.89ms、`operation_logs_write` 199.23ms、`public_api_logs_write` 12.52ms、`usage_stats_aggregate` 166.50ms、`gateway_quota_snapshot_pg` 106.93ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T222632Z.json`。
- 2026-06-27 扩展 PG 运行日志读路径后再次复测全绿：`usage_records_write` 57.31ms、`audit_logs_write` 162.36ms、`operation_logs_write` 182.93ms、`public_api_logs_write` 10.64ms、`runtime_logs_read` 17.27ms、`usage_stats_aggregate` 147.80ms、`gateway_quota_snapshot_pg` 92.67ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T223547Z.json`。
- 2026-06-27 扩展 PG 公开接口日志读路径后再次复测全绿：`usage_records_write` 65.18ms、`audit_logs_write` 149.05ms、`operation_logs_write` 187.80ms、`public_api_logs_write` 17.95ms、`runtime_logs_read` 13.15ms、`usage_stats_aggregate` 119.90ms、`gateway_quota_snapshot_pg` 77.73ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T224149Z.json`。
- 2026-06-27 补充 API Key 校验 / 系统设置 Redis shared cache 后再次复测全绿：`usage_records_write` 58.41ms、`audit_logs_write` 140.46ms、`operation_logs_write` 191.00ms、`public_api_logs_write` 26.61ms、`runtime_logs_read` 14.87ms、`usage_stats_aggregate` 142.41ms、`gateway_quota_snapshot_pg` 97.05ms。报告文件：`reports/performance-gateway-persistence-readiness-20260626T225814Z.json`。

当前结论：网关事实表 readiness 已具备完整 `/v1/*` 压测前置条件，但它仍不是容量结论。真实网关吞吐结论见下一节。当前 PG 统计聚合已覆盖核心用量桶、usage model / error / latency、账号质量、API Key quota 成本快照、直接账户 / 分组授权和团队账户 / 分组授权 quota snapshot；PG 审计写入已覆盖错误分组，审计日志列表、按 ID、错误组、错误组事件、详情和 payload 窗口读取已通过真实 PG readiness；PG 操作日志列表、summary 搜索、viewer 列表、管理员详情和 viewer 详情也已通过真实 PG readiness；PG 公开接口日志列表和详情读取已通过真实 PG readiness；PG 运行日志 trace 列表、keyword 搜索和详情读取已通过真实 PG readiness；API Key 校验和系统设置 Redis shared cache 已通过本地与远端 PG/Redis 回归。运行日志写入索引队列、cursor / facet 维护和尾部压力仍是后续项。

### 真实网关 `/v1/*` 压测

2026-06-26 在 `<测试主机IP>` 远端 PostgreSQL / Redis 栈上补跑真实网关链路压测。脚本启动真实 server、DB service、`ingest-worker`、`stats-worker` / `ops-worker` 的 PG-ready 子集、Redis Streams 使用记录队列和本地 mock OpenAI 上游；请求覆盖 `/v1/responses` 与 `/v1/chat/completions`，并在结束后等待 worker 消化队列。

关键参数：

```powershell
$env:JUHE_AI_RUNTIME_MODE = "performance"
$env:JUHE_AI_DATABASE_DRIVER = "postgres"
$env:JUHE_AI_CACHE_DRIVER = "redis"
$env:JUHE_AI_RUNTIME_STATE_DRIVER = "redis"
$env:JUHE_AI_QUEUE_DRIVER = "redis_stream"
$env:JUHE_AI_GATEWAY_LOAD_CONCURRENCY = "100"
$env:JUHE_AI_GATEWAY_LOAD_DURATION_SECONDS = "20"
$env:JUHE_AI_GATEWAY_LOAD_WARMUP_SECONDS = "3"
$env:JUHE_AI_GATEWAY_LOAD_SETTLE_SECONDS = "35"
$env:JUHE_AI_GATEWAY_LOAD_ACCOUNT_COUNT = "24"
$env:JUHE_AI_GATEWAY_LOAD_ACCOUNT_CONCURRENCY = "128"
pnpm --filter juhe-ai-backend test:performance-gateway-load
```

最终报告：`reports/performance-gateway-load-final-100c-20s.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 总请求 / 成功 / 失败 | 11192 / 11192 / 0 |
| 端到端脚本 RPS | 200.34 |
| 延迟 | p50 187.95ms，p95 232.02ms，p99 275.32ms，max 310.63ms |
| 上游请求数 | 12493，含 3 秒 warmup |
| usage records / catalog entries | 12493 / 12493 |
| audit logs | 615，抽样预期下限 391 |
| Redis Streams pending | 0 |
| deadlocks delta | 0 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 1.85s |
| 最大 active query | 0.03s |

本轮真实瓶颈与处理：

- 初始完整成功审计会让 PG 审计写入尾部积压，且出现较长 `idle in transaction`。已把 PG 审计 flush 拆为 25 条小批次，并让触发阈值同步使用有效批次大小。
- 100 并发下 10% 成功审计抽样约产生 21 条/秒，超过当前审计 payload / blob / hot-search 写入能力；尝试把单批调到 50 没提升吞吐，反而把最大事务拉到 3.67s，因此回退为 25。
- 高性能 / PostgreSQL 模式下成功审计默认抽样改为 5%，失败审计仍全量。最终 100 并发下审计追上预期，最大事务回到 1.85s。
- PostgreSQL 模式下仅启用 PG-ready 后台 job 子集；SQLite 文件日志索引、账号探测等未迁移 job 在 performance worker 中跳过，避免后台任务误触 SQLite。

慢 SQL 与索引核查：

- `pg_stat_statements` 慢项集中在 `juhe_usage.usage_record_shard_entries` 批量 INSERT，单次 max 约 76.19ms；未观察到慢 SELECT、锁等待或长 active query。
- `usage_record_shard_entries` 列表、API Key 筛选、耗时排序均走索引且无 Sort，典型执行约 0.50-0.58ms。
- `audit_logs` owner 列表、path 筛选、API Key 筛选均走 index-only scan 且无 Sort，典型执行约 0.54-0.57ms。
- `usage_stats_totals` 当前只有 68 行，PG 选择 Seq Scan，表上已有主键 `(system_account_id, scope_type, scope_id)`；这不是当前短板。
- 网关 API Key 分组绑定索引 `idx_api_key_group_bindings_gateway_route` 可覆盖排序；分组候选窗口发现旧索引 `idx_group_accounts_dispatch_priority` 的 `local_super_priority_enabled` 方向与查询 `DESC` 不一致，已修正 schema 并在测试库重建索引。复查后分组候选查询为 Index Scan，无 Sort。

清理：

- 最终压测报告生成后，脚本 cleanup 暴露 `audit_payload_refs.blob_id` 字段名错误；已修正为 `headers_blob_id / body_blob_id`。
- 已按最终报告 trace 前缀补清理本轮压测数据，复查剩余 `usage=0`、`audit=0`、`apiKey=0`、`accounts=0`。

### 2026-06-27 真实网关复测

本轮复测先发现一个压测 fixture 问题：父进程创建 mock 上游账号时使用 `runtimeConfig.secret` 加密凭据，子进程启动真实 server 时如果未显式设置 `JUHE_AI_SECRET`，会使用另一个硬编码 fallback，导致子进程无法解密账号凭据。表现为所有请求在调度前返回 503 “没有可用的上游账户”，mock upstream 请求数为 0，但 PostgreSQL deadlocks、lock waiters、Redis pending 均为 0。已修正为子进程继承 `runtimeConfig.secret`。

修复后先跑 mixed smoke：`10` 并发、`5s`、`8` 个账号、Chat / Responses 混合，请求 620 / 620 / 0，p95 96.60ms，Redis pending 0，deadlocks delta 0。

正式复测参数：

```powershell
$env:JUHE_AI_GATEWAY_LOAD_CONCURRENCY = "100"
$env:JUHE_AI_GATEWAY_LOAD_DURATION_SECONDS = "20"
$env:JUHE_AI_GATEWAY_LOAD_WARMUP_SECONDS = "3"
$env:JUHE_AI_GATEWAY_LOAD_SETTLE_SECONDS = "20"
$env:JUHE_AI_GATEWAY_LOAD_ACCOUNT_COUNT = "64"
$env:JUHE_AI_GATEWAY_LOAD_ACCOUNT_CONCURRENCY = "10000"
$env:JUHE_AI_GATEWAY_LOAD_REPORT_PATH = "reports/performance-gateway-load-100c-settle20-latest.json"
pnpm --filter juhe-ai-backend test:performance-gateway-load
```

最终报告：`reports/performance-gateway-load-100c-settle20-latest.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 总请求 / 成功 / 失败 | 10561 / 10561 / 0 |
| 成功 QPS | 257.18 |
| 延迟 | p50 186.41ms，p95 227.75ms，p99 252.20ms，max 309.20ms |
| 上游请求数 | 11682，Chat 5826 / Responses 5856，含 warmup |
| usage records / catalog entries | 11682 / 11682 |
| audit logs | 450，抽样预期下限 369 |
| Redis Streams pending | 0 |
| deadlocks delta | 0 |
| 最大 active / idle in transaction | 6 / 2 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 2.14s |
| 最大 active query | 0.02s |

慢 SQL 仍集中在 `juhe_usage.usage_record_shard_entries` 批量 INSERT，单次 max 约 62.95ms；未观察到慢 SELECT、死锁、长期锁等待或长 active query。

补充判断：10 秒 settle 复测中业务请求也已经 9916 / 9916 / 0，但审计抽样落盘 325 条，低于脚本预期下限 346；20 秒 settle 后审计落盘达到预期。高性能模式成功审计是 5% 抽样且异步批量 flush，压测判断必须同时看业务成功率、usage queue pending、PG 锁等待和审计最终消化，不能只用短 settle 下的审计采样数误判主链路失败。

### 2026-06-27 PG 派生统计补齐后复测

补齐 `aggregateUsageStatsBatchAsync` 的 PG usage model / error / latency / account quality 批量写入后，使用同样的 100 并发、20 秒、64 账号、20 秒 settle 参数再次复测。

最终报告：`reports/performance-gateway-load-1782509053407-1f72c9.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 总请求 / 成功 / 失败 | 10421 / 10421 / 0 |
| 成功 QPS | 254.04 |
| 延迟 | p50 189.87ms，p95 239.97ms，p99 276.24ms，max 322.08ms |
| usage records / catalog entries | 11351 / 11351 |
| audit logs | 475，抽样预期下限 364 |
| Redis Streams pending | 0 |
| deadlocks delta | 0 |
| 最大 active / idle in transaction | 6 / 2 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 2.03s |
| 最大 active query | 0.02s |

结论：派生统计补写没有引入死锁、长期锁等待或 Redis Stream 积压；主链路 p95 / p99 与上一轮 10561 请求复测处于同一量级。

### 2026-06-27 PG quota snapshot 补齐后复测

补齐 `refreshUsageQuotaHourlyWindowsCacheAsync`、`buildGatewayQuotaSnapshotAsync` 和 PG 成本批量读取后，再次使用同样的 100 并发、20 秒、64 账号、20 秒 settle 参数复测。

最终报告：`reports/performance-gateway-load-1782509811803-04b050.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 总请求 / 成功 / 失败 | 10479 / 10479 / 0 |
| 成功 QPS | 256.09 |
| 延迟 | p50 188.94ms，p95 219.37ms，p99 237.76ms，max 248.77ms |
| usage records / catalog entries | 11490 / 11490 |
| audit logs | 525，抽样预期下限 366 |
| Redis Streams pending | 0 |
| deadlocks delta | 0 |
| 最大 active / idle in transaction | 6 / 2 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 1.71s |
| 最大 active query | 0.02s |

结论：PG quota snapshot 补齐后没有引入死锁、长期锁等待或 Redis Stream 积压；主链路延迟较上一轮没有恶化。

补充 readiness：继续扩展团队账户授权和团队分组授权 quota snapshot 后，`test:performance-gateway-persistence-readiness` 再次通过，`gateway_quota_snapshot_pg` 68.35ms，报告 `reports/performance-gateway-persistence-readiness-20260626T215731Z.json`。该 readiness 已覆盖 API Key、直接账户授权、直接分组授权、团队账户授权和团队分组授权额度快照。

补充 readiness：继续扩展 PG 审计错误分组后，`test:performance-gateway-persistence-readiness` 再次通过，`audit_logs_write` 81.52ms，报告 `reports/performance-gateway-persistence-readiness-20260626T220339Z.json`。该 readiness 已覆盖失败审计写入 `audit_error_groups` 并回填 `audit_logs.error_group_id`。

补充 readiness：继续扩展 PG 审计读路径后，`test:performance-gateway-persistence-readiness` 再次通过，`audit_logs_write` 150.46ms，报告 `reports/performance-gateway-persistence-readiness-20260626T221516Z.json`。该 readiness 已覆盖审计列表、按 ID 列表、错误组列表、错误组事件、详情和 payload 窗口读取。

补充 readiness：继续扩展 PG 操作日志读路径后，`test:performance-gateway-persistence-readiness` 再次通过，`operation_logs_write` 199.23ms，报告 `reports/performance-gateway-persistence-readiness-20260626T222632Z.json`。该 readiness 已覆盖操作日志写入后的 trace 列表、summary 搜索、viewer 搜索列表、管理员详情和 viewer full 详情读取。

补充 readiness：继续扩展 PG 运行日志读路径后，`test:performance-gateway-persistence-readiness` 再次通过，`runtime_logs_read` 17.27ms，报告 `reports/performance-gateway-persistence-readiness-20260626T223547Z.json`。该 readiness 已覆盖运行日志 trace 列表、keyword 搜索和详情 raw_json 读取。

补充 readiness：继续扩展 PG 公开接口日志读路径后，`test:performance-gateway-persistence-readiness` 再次通过，`public_api_logs_write` 17.95ms，报告 `reports/performance-gateway-persistence-readiness-20260626T224149Z.json`。该 readiness 已覆盖公开接口日志写入后的 trace/path/result 列表和详情 request/response 读取。

### 2026-06-27 共享缓存补充后复测

本轮新增 API Key 校验 Redis shared cache、系统设置 / 全局设置 Redis shared cache，并补充源码门禁与本地 / 远端 PG/Redis 回归。

验证命令：

- `pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-index`
- `pnpm --filter juhe-ai-backend test:settings-management-driver`
- `pnpm --filter juhe-ai-backend test:settings-public-driver`
- `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`
- `pnpm --filter juhe-ai-backend test:performance-gateway-load`

readiness 报告：`reports/performance-gateway-persistence-readiness-20260626T225814Z.json`。

| 检查项 | 结果 |
| --- | --- |
| pass | true |
| usage_records_write | 58.41ms |
| audit_logs_write | 140.46ms |
| operation_logs_write | 191.00ms |
| public_api_logs_write | 26.61ms |
| runtime_logs_read | 14.87ms |
| usage_stats_aggregate | 142.41ms |
| gateway_quota_snapshot_pg | 97.05ms |

真实网关报告：`reports/performance-gateway-load-1782514762694-c6ff91.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 参数 | 100 并发、20 秒、0 秒 warmup、20 秒 settle、64 账号 |
| 总请求 / 成功 / 失败 | 9555 / 9555 / 0 |
| 成功 QPS | 233.56 |
| 延迟 | p50 196.91ms，p95 237.66ms，p99 1293.70ms，max 1352.28ms |
| usage records / catalog entries | 9555 / 9555 |
| audit logs | 450，抽样预期下限 333 |
| Redis Streams pending | 0 |
| deadlocks delta | 0 |
| 最大 active / idle in transaction | 6 / 2 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 1.83s |
| 最大 active query | 0.01s |

结论：新增共享缓存后没有引入死锁、长期锁等待或 Redis Stream 积压。该轮 `warmup=0`，p99 被启动初期请求抖动拉高；p95 仍与上一轮派生统计 / quota snapshot 后复测处于同一量级。慢 SQL 仍集中在 `juhe_usage.usage_record_shard_entries` 批量 INSERT，报告采样中出现的 `trace_id LIKE` 计数来自压测脚本自身统计查询，不是网关请求链路慢 SELECT。

### 2026-06-27 网关 / 管理端模型与策略共享缓存补充后复测

本轮新增网关模型目录 Redis shared cache、管理端模型目录 Redis shared cache、响应检查策略 Redis shared cache，并补充源码门禁、模型目录回归、网关运行态缓存回归、动态分组模型路由缓存回归、System API PG/Redis smoke 和真实 PG/Redis 压测。

验证命令：

- `pnpm --filter juhe-ai-backend typecheck`
- `pnpm --filter juhe-ai-backend test:gateway-cache-invalidation-index`
- `pnpm --filter juhe-ai-backend test:model-catalog`
- `pnpm --filter juhe-ai-backend test:gateway-runtime-cache`
- `pnpm --filter juhe-ai-backend test:api-key-group-route-cache`
- `pnpm --filter juhe-ai-backend test:performance-system-api-smoke`
- `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`
- `pnpm --filter juhe-ai-backend test:performance-gateway-load`

readiness 报告：`reports/performance-gateway-persistence-readiness-20260626T231415Z.json`。

| 检查项 | 结果 |
| --- | --- |
| pass | true |
| usage_records_write | 53.71ms |
| audit_logs_write | 152.89ms |
| operation_logs_write | 185.36ms |
| public_api_logs_write | 25.43ms |
| runtime_logs_read | 20.80ms |
| usage_stats_aggregate | 176.00ms |
| gateway_quota_snapshot_pg | 100.21ms |

真实网关报告：`reports/performance-gateway-load-1782516429515-85066d.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 参数 | 100 并发、20 秒、3 秒 warmup、20 秒 settle、64 账号 |
| 总请求 / 成功 / 失败 | 10262 / 10262 / 0 |
| 成功 QPS | 249.80 |
| 延迟 | p50 195.77ms，p95 234.95ms，p99 304.58ms，max 312.53ms |
| usage records / catalog entries | 11162 / 11162 |
| audit logs | 475，抽样预期下限 359 |
| Redis Streams pending | 0 |
| deadlocks delta | 0 |
| 最大 active / idle in transaction | 6 / 2 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 1.86s |
| 最大 active query | 0.02s |

结论：新增网关模型目录、管理端模型目录和响应检查策略共享缓存后没有引入死锁、长期锁等待或 Redis Stream 积压；`warmup=3s` 后 p99 稳定在 304.58ms，上一轮 `warmup=0` 的高 p99 可以判断为启动期抖动。慢 SQL 仍集中在 `juhe_usage.usage_record_shard_entries` 批量 INSERT，单次 max 约 66.95ms，未观察到网关请求读路径慢 SELECT。

### 2026-06-27 审计 Redis Streams 与 DB service PG 并发读补充后复测

本轮继续补齐审计日志持久队列和 PG 模式 DB service 读路径调度。审计日志在 `JUHE_AI_QUEUE_DRIVER=redis_stream` 且非 ingest-worker 进程下改为写入 Redis Stream，由 ingest-worker 消费并落 PostgreSQL；队列 payload 增加 Buffer 安全编码，避免请求 / 响应 body 在 JSON 序列化后退化为普通对象。PG 模式下审计 Redis Stream consumer 单次 `readCount` 限制为 25，和 PG 审计 flush 批次一致，避免一次拉取过多审计消息形成长事务或大 pending 批。

同时，DB service 在 PostgreSQL 模式下对网关热读和只读管理入口增加并发 dispatch 白名单，覆盖 `read_gateway_runtime`、API Key 校验、网关设置、分组用量访问、候选账号、quota 检查、模型目录和策略读取等操作；SQLite 模式仍保持原先串行保护。压测脚本同步扩展 Redis Streams 采样，现在分别采集 usage stream 与 audit stream，并以二者 pending 合计作为阈值判断。

验证命令：

- `pnpm --filter juhe-ai-backend typecheck`
- `pnpm --filter juhe-ai-backend test:redis-stream-queue`
- `pnpm --filter juhe-ai-backend test:audit-log-async-flush`
- `pnpm --filter juhe-ai-backend test:gateway-audit-payload-storage`
- `pnpm --filter juhe-ai-backend test:db-service-runtime-metrics`
- `pnpm --filter juhe-ai-backend test:performance-system-api-smoke`
- `pnpm --filter juhe-ai-backend test:performance-gateway-persistence-readiness`
- `pnpm --filter juhe-ai-backend test:performance-gateway-load`

readiness 报告：`reports/performance-gateway-persistence-readiness-20260627T000410Z.json`。

| 检查项 | 结果 |
| --- | --- |
| pass | true |
| usage_records_write | 62.81ms |
| audit_logs_write | 251.93ms |
| operation_logs_write | 295.97ms |
| public_api_logs_write | 29.28ms |
| runtime_logs_read | 19.50ms |
| usage_stats_aggregate | 247.80ms |
| gateway_quota_snapshot_pg | 97.30ms |

真实网关报告：`reports/performance-gateway-load-1782519761985-a19d6b.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 参数 | 100 并发、20 秒、3 秒 warmup、20 秒 settle、64 账号 |
| 总请求 / 成功 / 失败 | 10703 / 10703 / 0 |
| 成功 QPS | 260.94 |
| 延迟 | p50 186.53ms，p95 211.37ms，p99 320.89ms，max 326.01ms |
| usage records / catalog entries | 11703 / 11703 |
| audit logs | 454，抽样预期下限 374 |
| Redis usage stream pending | 0 |
| Redis audit stream pending | 25 |
| Redis Streams total pending | 25 |
| deadlocks delta | 0 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 1.73s |
| 最大 active query | 0.03s |

定位过程：

- 补 DB service 并发白名单前，100 并发冷启动网关链路会出现 503，响应为“本地数据库服务请求超时，请稍后重试”。根因不是 PG 死锁，而是 PG 模式下 `read_gateway_runtime` 等热读仍被串行队列压住。
- 审计 Redis Stream 初版未限制 `readCount` 时，业务请求 9985 / 9985 / 0，p95 240.39ms，但 audit stream pending 136，超过脚本阈值 100。原因是 audit consumer 默认一次可拉较大批次，强制停测时容易留下大 pending 批。
- 限制审计 PG Stream 单批为 25 后，旧 pending 被逐步接管并 ACK；最终 100 并发复测业务全成功，total pending 25，等于一个审计消费批次上限，低于阈值且没有继续增长。压测采样显示 audit logs 以 25 条批次持续追上，未观察到死锁、长期锁等待或长事务扩大。

结论：本轮最有价值的问题不在 PostgreSQL 本身，而在应用侧 DB service 调度和审计队列消费批次。补齐后，网关 100 并发主链路较上一轮 p95 从 234.95ms 降到 211.37ms，成功 QPS 从 249.80 提到 260.94；Redis 队列从只覆盖 usage 扩展到 usage + audit，重启 / 强停后的审计消息安全性明显提升。operation 当时尚未迁移，但已在下一节补齐；public API / runtime log 和记录维护队列仍只在出现明确积压、重启丢失风险或内存压力时继续迁移。

### 2026-06-27 operation Redis Streams 与队列 lag 门禁补充后复测

本轮继续迁移 operation log 队列。`JUHE_AI_QUEUE_DRIVER=redis_stream` 下，server / DB service / 非 ingest-worker 写入操作日志时不再只走本地 IPC，而是写入 `juhe-ai:queue:operation-logs`，由 ingest-worker consumer group `juhe-ai:operation-log-writers` 消费，落库成功后 ACK，失败保留 pending 等待重投。standalone / memory 队列仍保持原本本地队列和 IPC 行为。

同时修正压测观测口径：Redis Streams 不只看 pending，还通过 `XINFO GROUPS` 采样 consumer group `lag`，最终门禁使用 `pending + lag` 的 backlog。此前一次复测中 audit pending 只有 25，但 audit lag 仍有 280，说明只看 pending 会漏掉未被 consumer 读到的尾部积压。

本轮探索结果：

- 2 路 audit Redis consumer 被压测否决：审计落盘数追上，但出现 `maxLockWaiters=1`、最大事务 4.48s、audit backlog 210。结论是审计写入不适合靠并发事务硬顶。
- 最终方案保留 audit 单路 consumer 和 25 条小批次，把 performance / PostgreSQL 成功审计抽样从 5% 降到 3%，失败审计仍全量。这样减少审计尾部写入压力，不把排障采样放到网关吞吐前面。
- 前两轮失败报告遗留的 audit lag 已被后续 run drain，并按 trace 前缀补清理 PG 测试明细。

验证命令：

- `pnpm --filter juhe-ai-backend typecheck`
- `pnpm --filter juhe-ai-backend test:redis-stream-queue`
- `pnpm --filter juhe-ai-backend test:operation-log-queue`
- `pnpm --filter juhe-ai-backend test:audit-log-settings-contract`
- `pnpm --filter juhe-ai-backend test:audit-log-async-flush`
- `pnpm --filter juhe-ai-backend test:performance-gateway-load`

最终报告：`reports/performance-gateway-load-operation-stream-rerun-20260627T091423.json`。

| 指标 | 结果 |
| --- | --- |
| pass | true |
| 参数 | 100 并发、20 秒、3 秒 warmup、60 秒 settle、64 账号 |
| 总请求 / 成功 / 失败 | 10058 / 10058 / 0 |
| 延迟 | p50 200.02ms，p95 242.14ms，p99 277.25ms，错误率 0 |
| usage records / catalog entries | 11080 / 11080 |
| audit logs | 345，抽样预期下限 210 |
| operation logs | 0，本轮网关压测主链路未产生 operation log |
| Redis usage stream pending / lag | 0 / 0 |
| Redis audit stream pending / lag | 0 / 0 |
| Redis operation stream pending / lag | 0 / 0 |
| Redis total pending / backlog | 0 / 0 |
| deadlocks delta | 0 |
| 最大 lock waiters / not granted locks | 0 / 0 |
| 最大事务时长 | 2.37s |
| 最大 active query | 0.03s |

结论：operation log 已成为第 3 条生产 Redis Stream；压测脚本可以同时观测 usage / audit / operation 三条 Stream 的 pending 和 lag。最终采用“审计单路小批次 + 3% 成功抽样 + 失败全量”的策略后，100 并发真实网关链路无错误、无死锁、无锁等待，三条 Stream backlog 全部归零。剩余高风险本地队列仍是 public API log、runtime log、record maintenance 和账号测试 / 探测队列。

### 管理端在途请求阈值

探索压测中 `systemApiDbServiceMaxInFlight=64` 时，`100` 并发多账号压测出现 `system_api_busy` 503，错误率约 39.62%。这不是 PostgreSQL 死锁，而是应用层管理端入口保护提前拒绝。

已改为环境变量 `JUHE_AI_SYSTEM_API_DB_SERVICE_MAX_IN_FLIGHT`，standalone 默认 `64`，performance 默认 `256`。生产高性能模式仍需配合 `JUHE_AI_DB_POOL_MAX`、PgBouncer pool 和机器资源压测后再调整。

### 单热点账号写入

探索压测中把 15% 写请求全部打到同一个账号时，出现明显行锁排队：HTTP 503 错误率约 69.29%，最大事务时长约 21.52s，最大 active query 时长约 21.51s，最大 lock waiters 48。

这说明 PostgreSQL 消除了 SQLite 文件级写锁，但不能消除热点行竞争。生产队列和管理操作应避免大量并发写同一账号；如果后续确实存在同一资源高频状态写入，需要按资源维度串行、合并写入或拆分轻量运行态字段。

### 可靠性与灾备演练

2026-06-27 在 `<测试主机IP>` 远端 Docker 栈执行完整可靠性演练，命令：

```powershell
pnpm --filter juhe-ai-backend test:performance-reliability-drill
```

本轮启用 `remote_docker` 备份恢复模式，并配置 Redis state / PostgreSQL / PgBouncer 重启命令。脚本使用临时 Redis Stream key 和临时 PostgreSQL restore 数据库，结束后清理临时库、临时 dump 和临时 stream。

最终报告：`reports/performance-reliability-drill-20260627-022408.json`。

| 检查项 | 结果 |
| --- | --- |
| pass | true |
| PostgreSQL health | 57.85ms，数据库 `juhe_ai` 可连接 |
| PostgreSQL 阻塞快照 | deadlocks 0，lock waiters 0，idle in transaction 0，最大事务 0s，最大 active query 0s |
| Redis queue health | PONG，353.52ms |
| 生产 usage stream 快照 | length 75580，pending 0 |
| Redis Stream pending 接管 | 3 条 pending 从 dead consumer 转到 live consumer，ACK 后 pending 0 |
| PostgreSQL 备份恢复 | 14.03s，远端容器内 `pg_dump` 后恢复到临时库 |
| 备份大小 | 7,591,743 bytes |
| schema 表数量校验 | `juhe_business=47`、`juhe_dataset=19`、`juhe_usage=5`、`juhe_stats=59`、`juhe_codex_context=3`，源库和恢复库一致 |
| Redis state 重启恢复 | 重启前 3 条 pending；Redis PING 约 1.03s 恢复；重启后 3 条 pending 全部接管并 ACK，pending 0 |
| PostgreSQL / PgBouncer 重启恢复 | 健康检查恢复约 41ms |
| 演练后容器状态 | `juhe-ai-pgbouncer`、`juhe-ai-postgres`、`juhe-ai-redis-state`、`juhe-ai-redis-cache` 均 healthy |

结论：当前测试栈已经验证 Redis Streams at-least-once 队列在 Redis state 重启后不会丢失 pending 演练消息，PostgreSQL 逻辑备份可以恢复到新库并保持核心 schema 表结构一致，PG / PgBouncer 重启后应用侧新连接可以恢复。生产切换前仍需要在最终生产规格机器上重复同一演练，并把重启命令放进维护窗口执行。

## 清理验证

压测脚本会创建 `压测分组*`、`压测AI账户*` 和 `压测APIKey*` 临时夹具。2026-06-26 修复多账号清理后复查：

```json
{"groups":"0","accounts":"0","api_keys":"0"}
```

## 边界

- 本报告覆盖 System API 管理链路和真实网关 `/v1/*` mock 上游转发压测；不代表真实外部上游网络质量。
- 本报告已覆盖测试栈 PostgreSQL 备份恢复、Redis state 重启恢复、Redis Streams 重启后 pending 积压恢复和 PostgreSQL / PgBouncer 重启恢复；不代表生产最终机器规格和维护窗口表现。
- 完整管理端授权实例列表视图、完整派生统计刷新和应用容器滚动升级仍需后续验证。
