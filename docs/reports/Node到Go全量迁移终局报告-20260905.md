# Node 到 Go 全量迁移终局报告（2026-09-05）

> 本文是 Node→Go 全量清零迁移（`docs/migration/final-migration/PLAN.md`，W1-W10）的终局报告。进度事实源是 PLAN.md 执行日志；本文汇总终局数字、里程碑、质量门、架构终态、已知遗留与回滚指引。审计记录以 `docs/bug/问题-01NN-*.md` 与 `docs/plans/` 对应计划文档为准，本文不复制其正文。

## 1. 结论（执行摘要）

- **迁移已完成**：Node 后端全量归档，Go 三二进制（`juhe-ai-gateway`、`juhe-ai-jobs`、`juhe-ai-maintenance`）全量接管管理面、公开面、`/v1` 网关链、chat、aipublic 与后台任务域；`backend/` 已从工作区删除，git 历史完整。终局提交 `3f51156e0`（X02+X01+X03+五项清障），文档收场（X06）同日完成。
- **归档数字**：`backend/` 1700 文件（137,807 行 / 5.65MB）原样归档至 `migration-backup/node/final-archive/`，`SHA256SUMS` 1700 条全量校验通过。
- **Go 侧规模**（2026-09-05 master 实测）：`backend-go/` 共 1,404 个 `.go` 文件，非测试代码 374,584 行、测试代码 142,770 行；475 个 `_test.go` 文件、3,030 个 `func Test`。包分布：`gateway/internal` 61 包、`jobs/internal` 39 包、`maintenance/internal` 6 包、`shared/platform` 9 个无业务基础设施包、`shared/contracts` 1 包。`go.work` 固定五个模块：`shared/contracts`、`shared/platform`、`projects/gateway`、`projects/jobs`、`projects/maintenance`。
- **验收结果**：三模块 build+test 零失败（含 `cmd/juhe-ai-gateway/acceptance` 端到端套件：黑盒 12 文件、7 场景，全套 66s 全绿、0 skip 除 PG 门控）；12 项 node 回归测试全绿；frontend build 通过。历史 KNOWN-BASELINE-FAIL（ownermanifest probe_cursor 断言）已按 91/91 type_line +36 漂移数据驱动修正正式解除，未删断言。
- **go-only 终态**：`internal/legacybridge` 包、kernel `RegisterFallback` 扩展点与 `JUHE_AI_LEGACY_BRIDGE_TARGET` 已删除，无任何 502 代理回退路径；`deploy/start.sh|start.ps1`、`docker/compose.yml`、`Jenkinsfile`、`scripts/validate-release-package.mjs` 全部 go-only；前端 `VITE_JUHE_AI_DEPLOY_MODE=go` 模式已接线，X04 降级面清单所列前缀已随读面收口（`d8bfbc2a7`）在 Go 侧全量挂载。

## 2. 里程碑时间线（取自 PLAN.md 执行日志）

| 日期 | 波次 | 内容 |
| --- | --- | --- |
| 2026-09-04 | M0/B0 | 基线 `f9c1fbeac`；worktree 创建后按用户决策合并回 master（merge `29b4b99f7`），改为 master 直推模式 |
| 2026-09-04 | W1 | K1 kernel、K2 authsys（首次翻转归档）、K3 限流、K4 oplog producer、K5 inval 总线、K6 legacybridge、K7 mockupstream+golden、S-PG/S-SQ 双模式 schema（614 语句逐字节等价、六库 DDL 等价幂等） |
| 2026-09-04 | W2+W3 | M01-M17 管理域（公告/团队/授权/分组/路由策略/API Key/账户 CRUD/批量导入导出/授权实例视角/providers/settings/table-monitor/日志读面/OIDC/四供应商 OAuth）；C03 pricing；G01-G04 网关协议驱动；P03/P04 本体 |
| 2026-09-04 | W4 | M15-M17、P01-P03、G01-G03 收尾（详情见 PLAN.md 工作包表） |
| 2026-09-04 | W5 | G05 preauth、G06 body 管线、G07 quota、G08 routing、G09 hybrid、G10 runtime-cache、P03/P04 测试补齐+契约修复（11+7 项偏差）、P05 publicapilogs（队列消灭架构）；gateway 51 套件绿 |
| 2026-09-05 | W6 | G11 circuit、G12 accounteffects、G13a/b/c client-ip/hotquality/proxyhealth、G14 session、C02 openaicompat，全部 -race 绿 |
| 2026-09-05 | W7 | C01 chat 补完（8 处编译错误+8 个真实契约 bug）、G15 dispatch、G16 response、G17 usage、G18 codex、G19 obs（与 Node 实跑逐例比对）；G16 发现并修复 G02 巡检策略整体失效缺陷；gateway 全模块零失败 |
| 2026-09-05 | W8 | C01 生成波、J-A（taskruns+账户质量）、J-B（OAuth 刷新/保活）、J-Ca/b/c（统计 12 job）、J-D（retention）、J-E（运行维稳+internal-api HMAC）、J-F（usage writer，与 G17 契约 62 字段程序化比对一致）；补挂清收 4 项加法式 |
| 2026-09-05 | W9 | G20 装配三阶段（account-selector/chat 家族/hybrid Redis 协作者/pricing CostEstimator/openaicompat 入链）+ jobs 组合根（31 job 注册表：17 go-wired/3 go-equivalent/11 go-partial 显式登记）+ X03 前置部署三模式资产（`5ae58a13e`）；读面收口（`d8bfbc2a7`）后 Node 管理面 Go 侧全路由覆盖 |
| 2026-09-05 | W10 | X05 acceptance 基建+缺陷修复（`e1ec965a4`、`20e033ec4`）+ X04 前端 go 模式接线；X02+X01+X03 终局提交（`3f51156e0`）；X06 文档收场（本报告） |

过程安全事件（均已处置并记录于 PLAN.md 执行日志）：W5 子代理向 5 个 Node 参考文件写入 173 行 provider-anchor 实验代码（Go 产物零污染，`git checkout --` 回滚，此后波次强调 Node 只读红线）；W7 后残留代理 19 个流氓提交（authz 目录整体回退至 `7336e220f` 已验证状态，pre-commit 追加提交令牌门）；残留进程修改已归档 Go 包 +281 行（取证存 `.local/forensics/` 后回滚）。

## 3. 质量门

### 3.1 每工作包门禁

- 每个 WP 交付固定节奏：实现 → `go build`/`go vet` → 包级 `go test`，关键包加 `go test -race` → 主 Agent 统一复验后逐包提交登记。 golden 先录制后实现（G0），测试/文档/复查与实现四线并行。
- 移植即对照：每包以 Node 源码为行为参照逐项对照路由、鉴权、DTO、副作用与失败路径；中文编码、时钟冻结、revision 递增、单位混淆等移植缺陷在包内即发现即修（例：M04 中文双重编码、P04 ms/ns 混淆致授权码 born-expired、G02 tier 正则）。

### 3.2 审计修复循环（双重确证协作模式）

迁移代码在实现波之外由独立审计线复核，采用"取证代理给 Node/Go 双侧 file:line 证据 → 不同档位独立复核代理只拿候选项重新推导 → 两侧结论一致才定案（CONFIRMED/PARTIAL/REFUTED）→ 修复 → 未参与写入的终审代理独立复审"的循环：

- **审计线一**（`docs/plans/计划-20260904T071852927Z-已提交Go迁移功能审计与缺陷修复.md`）：范围 af841ce7..M08（K1-K7、S-SQ、S-PG、M01、M03-M08），产出 `docs/bug/问题-0152` ~ `问题-0171` 共 20 份逐项缺陷登记，修复随切片提交。
- **审计线二**（`docs/reports/Node-Go迁移独立审计报告-20260905.md`）：覆盖 M09 后全部波次与宏观完整性矩阵；15 个子代理（8 取证 / 6 独立复核 / 4 写入 / 1 终审），双重确证并修复 9 类缺陷（授权配额 fail-open、调度默认值偏离、team 授权实例跨成员可见、accounts 列表授权过滤、ipstats SQLite 数据源错位、ipstats detail 水合、操作日志 VisibilityScope 回填、pricing 文档失真、误导性文案），独立终审 7/7 PASS；确证未修复项登记 `docs/bug/问题-0172`（T1-T9，其中 T1/T4/T5 已随后续切片修复）。
- **逐包审计记录**：`docs(K1)/docs(K4)/docs(K6)/docs(K7)/docs(M03)/docs(M08)` 等大量独立提交逐包记录候选偏差与复核结论，可按提交历史追溯。
- **X05 端到端验收**：acceptance 黑盒套件（fresh SQLite 启动/认证流/管理面 CRUD 八域主干/公开面 OIDC+PKCE/网关链/chat/jobs 冒烟+PG 门控变体）发现 4 个真实产品缺陷（F4 操作日志租约自毁、seed 管理员密码哈希不兼容、`account_model_mappings` 复合主键漂移致 `/v1` 全量 500、jobs `/health` 字段错位），全部修复后全套 66s 全绿。

### 3.3 过程纪律

- pre-commit 钩子强制「backend/ 只删不改」归档不变量，并追加提交令牌门冻结并行代理提交能力；X06 收场时按钩子注释的既定动作删除（backend/ 已不存在，守卫完成使命）。
- 已归档包任何未审查改动一律回滚（两次实际处置，见 §2）。
- 全模块回归：W5 后每波波末主 Agent 统一跑 gateway 全模块 build/vet/test；终局执行三模块 build+test 零失败。

## 4. 架构终态（文字图）

```text
客户端 / 管理浏览器
  │
  ▼
juhe-ai-gateway（唯一常驻主入口，JUHE_AI_HOST:JUHE_AI_PORT，缺省 127.0.0.1:3000）
  ├─ /__aisys__/*          Web 静态资源 + 管理 API（kernel 中间件链：请求上下文/安全头/压缩/
  │                         no-store/body 限额/IP+用户两级限流/404 405 JSON）
  │                         ├─ authsys（auth/system-accounts/会话/验证码/登录保护）
  │                         ├─ internal/ 共 61 包——管理域（announcements/authz/systemteams/groups/routestrategies/
  │                         │  apikeys/accounts/providers/oauthmgmt/settings/pricing/policyreads/
  │                         │  statreads/logreads/tablemonitor/ipstats/uibootstrap/helpweb 等）、
  │                         │  网关链 G 系列、chat、模型检测、F3/F4 owner（auditlog/operationlog）
  │                         └─ /__aisys__/help（前端 dist 存在时挂载）
  ├─ /v1 + 协议路径         网关链（JUHE_AI_GATEWAY_CHAIN_ENABLED）：G05 preauth → G06 body →
  │                         G07 quota → G08 routing(+G09 hybrid) → G15 dispatch → 协议驱动
  │                         (openai/anthropic/gemini/codex) → G16 response → G17 usage → G19 obs；
  │                         openaicompat files/vector-stores 挂链内非协议路径
  ├─ /__aipublic__/*        外部维护 + external-integrations 公开读（aipublic 包）
  ├─ /.well-known + /oauth  OIDC 公开协议面（oidc 包）
  └─ /__aidelegated__/v1    委托授权公开 API（delegated 包）

juhe-ai-jobs（后台 owner 进程；F1 运行日志、F2 表监控常驻；J1 默认关闭）
  ├─ jobregistry：31 job 注册表对齐 Node entries（17 go-wired / 3 go-equivalent /
  │                11 go-partial 显式登记；队列 IPC 按计划消灭登记）
  ├─ jobsched 调度器（fixedRate/fixedDelay/jitter/phase/overlap/resource lane/退避/排空）
  ├─ accountprobe/proberepo（全协议诊断探针栈）、cleanuprepo（retention/cleanup 仓储）、
  │  oauthrefresh/accountquality/statsagg/statsverify/opsjobs/usagewriter/taskruns/
  │  internalapi（HMAC 派发）等 39 包
  └─ JUHE_AI_JOBS_WORKER_ENABLED 默认关（装配后零行为变化，启用属部署决策）

juhe-ai-maintenance（一次性 CLI）
  └─ --ensure-schema/--seed --driver sqlite|--postgres（六库 DDL 幂等 + 164+13 种子幂等）、
     ownermanifest 校验、J3a/J3b bootstrap、businesshandoff、goruntimemetrics

存储（双模式）
  ├─ standalone：SQLite 六库（业务库 / 数据集目录库 / 运行日志索引库 / 使用记录目录库 /
  │              统计结果库 / chat 库）；gateway 启动 preflight 六库 ensure+seed
  └─ performance：PostgreSQL（含 PgBouncer）+ Redis cache/state/queue 三实例分库

共享（go.work 五模块）
  └─ shared/contracts（跨项目 schema 契约）+ shared/platform（sqlpool/upstreamhttp/
     mockupstream/supervisor/schedulejitter/sqlitepath/ownermode/gometrics/
     operationlogappend；无业务编排，三项目互相禁止 import）

已不存在：Node backend/（已归档 migration-backup/node/final-archive/）、
internal/legacybridge 代理路径、Node worker/DB service/IPC、JUHE_AI_DEPLOY_MODE 分支。
```

## 5. 已知遗留与平台侧待办

以下各项均有代码或文档证据，按影响排序；它们不阻塞 go-only 运行，但属于如实登记的边界：

1. **hybrid 混合智能路由协作者未全装配**：`gatewayhybrid.AuxiliaryDispatcher`（辅助派发端口）在组装根无赋值；Redis 模式下仅装配 `SharedJSONCache`（`gateway:hybrid-scoring-result`）与 `RuntimeStateStore`（`gateway-hybrid-route-affinity`）两个协作者（`cmd/juhe-ai-gateway/chain_runtime.go`、`chain_compose.go`）。纯路由评分/亲和主路径可用，辅助派发语义未激活。
2. **PG 统计扣减链显式报错**：`jobs/internal/cleanuprepo` 的 api-key/account retry 扣减链仅 SQLite 生效，PG 模式显式报错登记（提交 `20e033ec4`），不静默跳过。
3. **限流 Redis 拓扑等价继承**：Go 两级限流沿用与 Node 相同的共享单实例 Redis Lua 多桶键空间（PLAN.md K3/G07 行；迁移期与 Node 互通验证），未引入集群化扩展。
4. **WebP 有损编码待决策**：chat 图片管线预览编码为自写 VP8L 无损 WebP 编码器（`cmd/juhe-ai-gateway/chain_chat_images.go`，`x/image/webp` 仅解码器故自研；round-trip 验证过）；有损编码（体积更小）未实现，是否引入待决策。
5. **table-monitor 非业务数据 cleanup POST 未移植**：W6 记录约定该写操作 Node-owned（gateway 无 record-maintenance worker channel，见 `internal/tablemonitor/store.go` 头注与 `compose.go` 挂载矩阵注释）；Node 归档后该 POST 返回 404，属显式接受的降级。
6. **jobs 11 项 go-partial 显式登记**：3 个 scheduled job（account-circuit-control-plane-maintenance、account-list-availability-projection-maintenance、account-circuit-recovery）缺 Redis CircuitStore 等跨模块依赖未接线（`cmd/juhe-ai-jobs/worker_partial_jobs.go` 缺口清单，启动日志与 `/health` 可观测，硬门禁保证不进调度循环）；其余为队列 IPC 按计划消灭与网关域单实现依赖登记。
7. **问题-0172 未闭项**：T2（OAuth 轮换/账户写路径 inval 未注入、跨进程失效协议不兼容——go-only 下影响降级为 Go 自身缓存失效缺口）等按 `docs/bug/问题-0172-第三方独立审计M09后迁移缺陷清单.md` 跟踪。
8. **平台侧待办**（不在本仓库内，见 `docs/migration/部署go-only双轨开关.md` §6）：平台仓库（k8s-juhe）overlay 仍声明 `juhe-ai` Node 容器，需切 go-only 双容器拓扑后 Jenkins `'-'` 哨兵才与运行态一致；owner lock（`run-with-owner-lock.mjs`）无 Go 等价包装，go-only 下该保护被启动脚本显式拒绝；`docker/compose.performance.yml` 的 go-only 高性能变体未落地；主机侧如有 PM2/systemd 生态文件需改为直接拉起两个 Go 二进制；Mac LaunchDaemon / 八台 Edge 常驻定义在 `.local/project-resources/prod/` 私有资料中。

## 6. 回滚指引

- **代码回滚**：全部迁移以可审计提交落在 master；`backend/` 删除由终局提交 `3f51156e0` 完成，git 历史完整，可按提交区间 revert 或检阅任一历史版本。禁止 reset / force-push 共享主线。
- **归档复原**：`migration-backup/node/final-archive/` 按 `SHA256SUMS`（1700 条）校验后可整树还原 `backend/` 原状（1700 文件 / 137,807 行）；`migration-backup/` 不参与构建、测试、部署与运行时 import，禁止修改。
- **运行时回滚**：go-only 是唯一受支持拓扑，启动脚本不提供 node 模式止血入口；历史双镜像拓扑回滚按 `docs/deploy/部署指南.md` 与 `docs/migration/部署go-only双轨开关.md` §5 执行——选择 Node 时代 release state 时由 Jenkins 按历史 `nodeImageDigest` 复原平台 kustomization，属恢复旧拓扑而非受支持的长期运行形态。schema 回退按部署指南备份恢复，`--ensure-schema` 幂等、无自动降级。

## 7. 关联文档

- [Node 全量清零迁移执行看板（PLAN）](../migration/final-migration/PLAN.md)：唯一进度事实源，含全部 WP 状态、提交 SHA 与执行日志。
- [Node全量清零迁移总计划-20260904](../migration/Node全量清零迁移总计划-20260904.md)：生命周期、mock 门禁、git 纪律与归档规则。
- [部署 go-only 终态](../migration/部署go-only双轨开关.md)：唯一拓扑、启动脚本、docker、CI 与平台侧待办。
- [X04 前端 Go 模式收口与契约差异清单](../migration/X04-前端Go模式收口与契约差异清单.md)：前端连接面与降级面收口记录。
- [Node→Go 迁移独立审计报告（20260905）](Node-Go迁移独立审计报告-20260905.md)：审计线二全记录。
- `docs/bug/问题-0152` ~ `问题-0172`：两条审计线的逐项缺陷登记。
