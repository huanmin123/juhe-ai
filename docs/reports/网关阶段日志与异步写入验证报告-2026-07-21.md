# 网关阶段日志与异步写入验证报告（2026-07-21）

## 当前结论

- 网关请求已能按同一 `traceId` 重建“body 接收/捕获 -> handler -> 分组权限 -> 客户端画像/协议 -> 候选账户 -> 模型能力 -> 账号避让 -> 配额/容量 -> IP/账号并发 -> 上游准备/fetch/首输出/body -> 下游 finish -> audit finalize”的阶段时间线。
- Node Pino 在主线程完成字段组装和 JSON 序列化，再通过异步 `fs` stream 将 JSONL 顺序追加到角色日志文件。角色文件是 runtime-log 的唯一 durable spool；常态不采样、不因索引队列丢行，永久慢盘下按 normal 64 MiB / failure 8 MiB 预算显式 drop 并保留有界现场。
- `ingest-worker` 是唯一 runtime-log consumer：按 file identity + truncation generation + byte cursor 有界读取当前及轮转文件，批次索引成功后才推进 cursor。业务进程不做 runtime-log `JSON.parse`、hash、Redis XADD 或 IPC line 回传。
- Go 日志使用 normal/failure/emergency 三类有界异步 `slog.Handler` 通道。failure 与 emergency 同时饱和时不阻塞业务，累计 `failureDropped`，并在 drop summary 中保留最后一条有界现场；这不是“每条失败永久无损”的承诺。
- 合并 `origin/master=303a95e43` 后的最终 r16：5 秒压测 + 1 秒预热、并发 10、mock 上游 5ms，共 1,394/1,394 成功，QPS 277.34、p50 31.150ms、p95 93.016ms、p99 108.447ms、CPU/request 0.9978ms、event-loop p99 80.347ms；writer `error/drop/failureDrop=0`，采样时 `pendingBytes=0`、`needDrain=false`、`degraded=false`。

## 环境与范围

- 本机：Windows / Node.js 22 / pnpm，SQLite + memory 单机 mock 网关。
- 请求：`POST /v1/chat/completions`，8 个 mock 账户，并发 10，上游固定延迟 5ms，metadata-only 审计。
- 真实依赖：`192.168.1.203` 测试 PostgreSQL 与 Redis cache/state/queue；连接信息仅从本机密码文件临时注入，未写入代码、文档或 Git。
- 未调用真实模型，不产生模型额度费用；未执行生产部署、重启或切流。

## 阶段证据

最终 r16 原始 JSON/JSONL 经逐行解析检查：43,524 行全部是合法 JSON，顶层重复 key 为 0；共 1,404 个 summary/trace，每个 trace 稳定出现 28 类阶段和 1 个汇总，缺失、孤立、错误 stageCount 和 summary drop 均为 0。下面的阶段分位值来自 r12 历史测量，r16 用于确认最新 master 上的 writer 批量落盘和最终 drain，不把不同轮次的单次 QPS 波动解释为业务回归。

| 阶段 | r12 p50 | r12 p95 | r12 最大值 | 结论 |
| --- | ---: | ---: | ---: | --- |
| `preflight.completed` | 0.461ms | 0.735ms | 861.801ms | 热路径快，冷启动仍需单独观察 |
| `upstream.fetch_headers` | 16.829ms | 25.443ms | 233.371ms | 主要稳定耗时仍在上游连接/首包 |
| `audit.finalize` | 0.023ms | 0.036ms | 0.445ms | 审计收口本身不是瓶颈 |

合并前历史对照：r10 每个 trace 有 28 类阶段，QPS 309.04、p95 87.921ms；r11 为 1,576/1,576 成功，QPS 314.55、p50 26.688ms、p95 83.905ms、p99 102.332ms、CPU/request 0.8528ms。r13 在逐行 writer + 8/1 MiB 预算下发现 42,033 条显式 drop；r14 提升到 64/8 MiB 后 drop 为 0，但压测采样时仍有 33,677,053 bytes 待排空，进程总耗时约 26 秒；r15 增加 `_writev` 批量追加后待写字节在压测结束前收敛为 0，进程总耗时约 11 秒。这个序列证明本次瓶颈在逐行文件写入吞吐，而不在路由、分组或账户选择阶段。

## 失败现场

- mock 上游 100% 错误的 failure r2 返回 HTTP 503。日志保留 42 个已执行阶段和 4 次上游尝试；`upstream.dispatch.failed` 为 `expected_failure`、`reasonCode=upstream_accounts_exhausted`，`audit.finalize` 保留 `statusCode=503`、`errorPhase=dispatch`、`errorCode=upstream_retryable_error`、`attemptCount=4`。
- 不可预知 Node 异常同时写 `gateway.request.stage` info 和 `gateway.request.failure` error，单条 failure 直接携带当前阶段、已完成阶段摘要、Error/cause/stack 和队列/重试快照，不依赖最终 timing summary 才能还原现场。
- Go normal/failure 在入队前统一复制和裁剪；大字符串保留原始字节数、SHA-256 和 truncation reason，任意 `slog.Any`/`LogValuer` 不会把无界对象图留在队列。failure 先进入 failure lane，再进入 emergency lane；两者同时满时只保证 `failureDropped` 累计值和 `lastFailure` 有界快照最终出现在 `system_log_drop`。
- Node 文件目的地首次 open/write/rotation 故障和 failure reserve 耗尽使用单 in-flight + 单 pending 的独立异步 stderr 诊断；持续故障只替换最后现场并累计 drop，不回写被消费 spool，也不在请求线程调用同步 `writeSync`。

## Runtime-log 架构与门禁

当前唯一架构：

```text
Node Pino role logger -> role JSONL durable spool -> ingest-worker file cursor consumer -> SQLite/PostgreSQL runtime_logs
Go slog -> bounded normal/failure/emergency lanes -> configured Go log destination
```

明确不存在：runtime-log Redis Stream producer/consumer、主进程逐行 parse/hash、IPC line 回传、`AsyncLogPublisher` 或独立 writer worker 可丢主日志的旧路径。Redis 仍服务使用记录、审计、操作日志和公开接口等其他事实队列。

候选分支合并 `origin/master=303a95e43` 后的验证证据：

- Node `pnpm typecheck`：通过，exit 0。
- Node `pnpm build`：通过，exit 0；仅有既有 Vite 大 chunk 告警。
- runtime-log file consumer 7 项门禁通过：hot-path boundary、consumer contract、file consumer、rotation、pressure、Redis removal、PostgreSQL consumer smoke。
- Go `go test ./... -count=1`：首次因 migration schema 名硬编码失败；修复根因后通过，exit 0。
- Go `go vet ./...`：通过，exit 0。
- 网关 timing、关联失败现场和 Go failure/emergency 专项回归通过。
- r16 性能与结构检查通过：解析 `reports/logging-gateway-info-r16-20260721.json` 和角色 JSONL，确认 1,394/1,394 HTTP 成功、1,404 个 summary/trace、每 trace 28 类阶段、非法 JSON/重复 key/error/drop/pending 均为 0。

## 复核与修复

- 首轮两名只读 Agent 独立复核指出：阶段覆盖不完整、wall/monotonic 时钟混用、failure 预留容量与 shutdown 证据不足、Go bytes/drop/context 不完整、DB/JSON worker 关联缺失、runtime cursor 轮转去重和主线程索引解析等问题。
- 已补齐固定阶段枚举、单调计时、summary/failure 现场、Go failure/emergency 与 `lastFailure`、HTTP/worker/DB service 关联、`audit.finalize` 真实工作窗口，并采用最新 master 的 file identity + truncation generation + cursor 语义。
- master 合并进一步删除了旧 runtime-log Redis/IPC/主进程解析链路；7 项 file-consumer 门禁、Node/Go 全量门禁在当前候选树通过，r15 验证最终 writer/drain。最终 master 同步、两名只读复核与交叉质疑的关闭状态仍以 PLAN-0146 清单为准，不以本报告替代审批。
- 最终独立复核进一步发现并修复：Node 慢盘无界缓冲与逐行写入、writer 故障静默、阶段业务字段重复 JSON key、DB IPC 根 requestId 丢失、Node/Go failure 枚举和字符串化无界、Go normal 队列真实内存预算失真、Go HTTP 完成结果缺字段、JSON worker 快速完成/取消缺证据、runtime importer stderr 背压无界。新增慢/失败 writer、批量写与最终 drain、raw JSON 键次数、DB request/job/parent、normal/failure budget、Go HTTP result 和 importer 独立异步 fallback 回归；最新 master 合并、修复后双复核和交叉质疑仍待完成。

真实 PostgreSQL/Redis smoke 使用隔离测试资源：角色文件经 ingest-worker 批量写入 PostgreSQL、cursor 提交且无重复；runtime-log Redis Stream 增量为 0。测试库历史 Goose 链的完整升级没有被本报告宣称通过，细节以 `docs/plans/2026-07-20-运行日志热路径无损解耦任务清单.md` 为准。

## 合并前原始对照

- `reports/logging-gateway-silent-r3-20260721.json`
- `reports/logging-gateway-info-r10-20260721.json`
- `reports/logging-gateway-info-r11-20260721.json`
- `reports/logging-gateway-failure-r2-20260721.json`
- `reports/logging-gateway-info-r12-20260721.json`
- `reports/logging-gateway-info-r12-20260721/juhe-ai.log`
- `reports/logging-gateway-info-r13-20260721.json`（容量失败实验，显式 drop 42,033）
- `reports/logging-gateway-info-r14-20260721.json`（64/8 MiB 零 drop，但存在 33.7 MB 退出 backlog）
- `reports/logging-gateway-info-r15-20260721.json`（批量写后零 drop、零 pending）
- `backend/reports/logging-gateway-info-r15-20260721/juhe-ai.log`
- `reports/logging-gateway-info-r16-20260721.json`（最新 master 合并后最终门禁）
- `backend/reports/logging-gateway-info-r16-20260721/juhe-ai.log`

这些产物受 `.gitignore` 管理，不进入发布包。r16 是合并最新 master 后的最终 writer/drain 证据；r10-r15 只用于历史对照和根因定位。

## 上线边界

- 本报告不授权生产上线；本任务未执行发布、部署、生产重启或切流。
- 上线前应在目标服务器以真实日志目录、目标磁盘和预期峰值并发复跑，确认 Node file backlog 能收敛、cursor 正常推进、Go normal/failure drop 符合容量目标。
- 出现 10 秒级慢请求时，优先按同一 `traceId` 比较 `preflight.completed`、`upstream.dispatch.completed` 与 `downstream.response.completed`；若本地阶段仍为毫秒级，继续检查上游连接、首字节、重试/切换与网络代理。
