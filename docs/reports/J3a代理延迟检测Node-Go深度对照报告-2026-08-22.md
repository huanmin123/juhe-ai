# J3a 代理延迟检测 Node↔Go 深度对照报告

## 结论

本次对照确认：Go `proxylatency` 的基础探测域已经覆盖 Node 的代理 URL、凭据、SOCKS 远端 DNS、按传输模式拆分的请求头、禁止重定向、完整响应 drain、512 KiB body retention、64 KiB CONNECT response header、结果分类、lease/fence、幂等 replay 和健康状态；此前 dev 隔离 PostgreSQL/PgBouncer `6432` required smoke 也已通过并完成清理。

这不等于 J3a 已完成完整迁移。当前仍有三项明确阻断：

1. Node committed-outcome projector/readback 尚未接入，Go outcome 不能回写 Node `proxy_profiles` 读模型。
2. Node scheduler/business writer 尚未 drain，也没有 owner handoff、active-path-zero 和回滚证据；Go owner gate 必须保持关闭。
3. Node `POST /proxies/:id/test` 的 `404/503 + Retry-After/200/502`、25 秒同步等待和手动 outbound 字段尚无 Go bridge/readback 闭环。

数值默认值、目标排序和线级 wire golden fixture 仍需在同一 fixture 上做兼容性决策和验证；不能把当前本地 Go 通过当作跨运行时通过。

## 证据边界

| 类别 | 证据 | 结论 |
| --- | --- | --- |
| Node 事实源 | `backend/src/modules/proxies/proxy-test.service.ts`、`backend/src/storage/proxy.repository.ts`、`backend/src/modules/proxies/proxies.routes.ts`、`backend/src/modules/background/background-jobs.ts` | 作为行为 oracle，不修改 Node |
| Go 映射 | `backend-go/projects/jobs/internal/proxylatency/{direct_input_reader.go,store.go,transport.go,executor.go,runner.go,config.go}`、`cmd/juhe-ai-jobs/main.go` | 基础探测与 jobs facts 已实现 |
| 本地门禁 | Go unit/race/vet、Node typecheck、proxy regression、`git diff --check` | 当前工作区可复核；全量 jobs 仍受范围外脏改动影响 |
| 真实 dev | `docs/reports/J3a代理延迟检测L3-PG真实验收报告-2026-08-22.md` | 一次隔离 scratch/PgBouncer smoke，通过后已清理；不代表跨运行时闭环或生产 |
| 未验证 | Node→Go→Node、scheduler drain、owner handoff、manual bridge、生产/L4 | 明确保留为后续门禁 |

## Node 事实与 Go 映射矩阵

| Node 机制 | Node 可观察事实 | Go 当前映射 | 判定 |
| --- | --- | --- | --- |
| 候选选择 | enabled proxy；未检测优先，`last_tested_at ASC`、`updated_at DESC`、`id ASC`；provider/profile 有固定优先级 | `direct_input_reader.go` 只读事务、候选排序、启用 profile 优先和 fallback | 基础通过；provider 名称排序尚缺同 fixture golden |
| URL/凭据 | `socks5` 与 `socks5h` 均规范为 `socks5h`；username 为空时丢弃 password-only；username/password URL 编码；无效代理 fail-closed | `proxyURLForIssuedInput`、AES-GCM v1 envelope、password-only fail-closed、SOCKS remote DNS | 通过 |
| HTTP forward wire | absolute-form；`accept`、`user-agent`、`host`、`connection: close`、`proxy-connection: close`、认证 | `applyNodeProbeHeaders` 仅 forward HTTP target 注入对应头 | 通过 |
| HTTPS CONNECT/SOCKS wire | CONNECT 握手带 Host/Proxy-Connection/auth；隧道目标只带 accept/user-agent；SOCKS 不泄漏 forward 头 | `ProxyConnectHeader` 与 mode-specific header helper；capture tests | 通过 |
| 请求限制 | 15 秒 probe、25 秒 candidate、45 秒 refresh budget、60 秒 scheduler task；父 abort 销毁请求/agent | Go 以 issued expiry、persisted proxy lease 和 runner context 约束；默认值不同 | 需决策并补同 fixture 测试 |
| 响应处理 | CONNECT header 64 KiB；body retention 512 KiB 但持续读到完整 framing；截断/提前 close/不完整 response 失败 | `MaxResponseHeaderBytes`、bounded collector、full drain、transport failure 分类 | 通过 |
| 结果分类 | 完整 framing 的任意 HTTP 状态为 passed item；transport/incomplete 为 failed；配置/取消/lease 为 task failure/unknown；周期聚合有 failed/warning/unknown/passed 规则 | Go item/outcome 分类与 health readiness 规则已覆盖 | 基础通过；跨 runtime report/projector 未闭环 |
| 持久化 CAS | `id + config revision/updated_at + last_tested_at <= testedAt`；stale 是 no-op，不 bump `updated_at`/缓存 | Go outcome 带 observed/config revision/fence；Node projector 尚未实现 | **阻断：缺 projector** |
| 周期调度 | 60 秒间隔、4 分钟初始延迟、coalesce-one、30 秒–10 分钟 backoff、候选池/批次 deadline、lease skip/deferred/partial | Go runner 是 opt-in、serial draft cycle、owner/proxy lease；Node writer 未 drain | **阻断：缺 owner handoff** |
| 手动入口 | `404`、槽满 `503 + Retry-After`、成功 `200`、执行异常 `502`；同步等待最多 25 秒；manual report 可含 outbound | 契约已冻结，但 Go bridge/readback 未实现 | **阻断：缺 manual bridge** |
| 健康 | proxy test 状态与 gateway request-time health 分离；调度失败/持久化失败/release failure 可观测 | Go `/health` 暴露 J3a 字段且 snapshot 原子；Node/Go aggregate 尚未闭环 | 部分通过，后续闭环门禁 |

## 必须保留的迁移边界

- 不修改 Node 源码来“配合”当前 Go；Node 仍是事实源，迁移方在接线阶段按本报告同步。
- 不启用 Go owner、不停止 Node scheduler、不做双写、不增加 Node RPC/Redis fallback。
- Go 不写 Node business SQLite 或 `juhe_business.proxy_profiles`；projector 必须通过冻结的 committed outcome/cursor/receipt 协议完成。
- dev smoke 只证明 jobs schema、reader、lease、input、claim、outcome 和权限隔离；不证明 Node projector、owner handoff、manual route 或生产/L4。

## 后续阻断门禁

### P1：Node→Go→Node outcome 闭环

固定同一 proxy fixture，Node 发布 input，Go jobs 执行并提交 committed outcome，Node projector 按 `(stored_at, outcome_id)` 游标消费；覆盖 applied/stale/ignored/rejected receipt、旧 revision、重复 outcome、poison payload、重启恢复和 cursor 不丢失。未完成前保持 owner gate off。

### P2：scheduler drain 与 owner handoff

为 Node scheduler/business writer 增加停止/排空证据；确认没有活跃 Node candidate scan、DB-service 写入、queue/IPC/启动器路径后，才允许受控开启 Go owner。必须保存旧 owner manifest、回滚命令和 active-path-zero 检查，禁止先双跑。

### P3：manual bridge

实现并测试 loopback v1 encrypted input 的 404、503/Retry-After、200、502、25 秒 deadline、取消、重复 request 和 outbound 字段 readback；手动路径与周期 scheduler 分离，不引入持久队列。

### P4：兼容性证据

在同一 Node/Go fixture 上冻结 probe/candidate/run/scheduler 数值、provider target 排序、IPv6 CONNECT authority、SOCKS wire、407/502/TLS/truncated/slow-drip 线级行为；每项要有 Node 与 Go golden 输出，不能只靠源码相似。

## 当前验证与剩余风险

- 已验证：Go proxylatency package unit/race/vet、Node typecheck/proxy regressions、dev PgBouncer `6432` required smoke、JSONB canonical digest、cleanup 和 owner gate off。
- 未验证：跨运行时 projector/readback、Node scheduler drain、owner handoff、manual bridge、真实服务重启/回滚、生产/L4。
- 当前工作树还有其他 J1/J2/accounthealth 等用户改动；本报告不将其归因于 J3a，也不覆盖或回滚它们。

本报告只关闭“Node 机制对照取证”这一文档单元，不关闭 J3a 完整迁移或生产切换。
