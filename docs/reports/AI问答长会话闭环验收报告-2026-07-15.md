# AI 问答 20 轮历史基线报告（2026-07-15）

> 本报告是 20 轮历史基线，只记录当时的上下文、图片、压缩和缓存验证，不构成后续 50 轮深度验收证据。

## 环境与边界

- 分支：`codex/ai-chat-mvp` 隔离工作树。
- 入口：开发环境 `/__aisys__/my-chat`，使用真实 GPT API Key 与 `gpt-5.6-sol`。
- 会话：专用验收会话 SHA256 `7e68e70697bbebe272e7f013e3d727c444e22266f5c99433282d486dbe24343c`。
- 真实凭据只从本机授权文件读取，未写入仓库、日志或本报告。
- 922K 模型窗口下，20 轮自然对话只使用 8.5K token；不为触发 70% 阈值灌入数十万无意义 token。压缩通过独立临时数据库、真实模型和正常 HTTP 追问完成专项验收。

## 真实用户对话

- 围绕 `ORBIT-482` 技术项目连续完成 20 轮，不是独立问答集合。
- 长期记忆锚点：预算 120 万元、RPO 5 分钟、技术栈 TypeScript + PostgreSQL + Redis。
- 覆盖 Markdown 段落、列表、表格、代码片段、链接、联网搜索工具折叠和连续流式输出。
- 第 6 轮使用输入文档按“文字 + 图片 + 文字”顺序粘贴图片；模型准确识别 `ORBIT-IMAGE-735`，并结合前文指出缺少 Outbox Relay/Worker。
- 第 10、20 轮均能复述长期约束、图片标签和 Outbox + Relay/Worker 决策。

## 图片与存储

- 图片处理状态与隐藏说明状态均为 `ready`。
- 原图 9622 bytes，派生为 960x540 WebP；隐藏说明 682 字符。
- 40 条消息中没有 `data:image` / Base64 派生存储，历史模型上下文使用图片说明。
- SQLite `PRAGMA quick_check=ok`。
- 20 个用户消息、20 个助手消息全部 `completed`；40 个序号唯一连续，20 个 turn 均严格一问一答。
- 20 条幂等映射全部能关联对应用户与助手消息；`content_bytes` 账本无偏差。

## 上下文与压缩

- 真实 UI 会话最终 `context_revision=20`、`active_context_tokens=8511`、`effective_context_limit_tokens=922000`，用量来自上游而非本地估算。
- 真实压缩专项使用独立临时库写入长早期历史和最近完整轮次，调用真实压缩模型生成 checkpoint。
- 断言 checkpoint 保留早期随机记忆，压缩后只保留最近完整轮次原文；随后通过正常 HTTP 对话成功召回早期记忆。
- 同一专项同时验证历史图片改用隐藏说明，不重复发送图片 Base64。

## Prompt Cache

- 20/20 轮使用记录成功，始终命中同一合法上游账户。
- 缓存命中率最小 50.7%、平均 79.2%、最大 94.0%，最后一轮 92.8%。
- 累计输入 134818 tokens，其中 107008 tokens 为上游明确返回的 cache read tokens。
- 结论只基于真实 usage 字段，不把客户端估算当作缓存命中。

## Worker、队列与日志

- server、ingest-worker、stats-worker、ops-worker、db-service 均有运行态采样，RSS 稳定在约 36–120 MiB。
- 对话完成后的新采样：server 4ms、ingest-worker 131ms、stats-worker 13ms、ops-worker 4ms、db-service 4ms；无持续卡死。
- 本地队列和重试队列最终归零，无 dropped、rejected 或 flush failure 积压。
- 发现 `account-quality-refresh` 两次因 1000ms 快照等待低于 ingest 短时 1.1–1.3s 延迟而失败；安全门没有推进游标，后续自动恢复。已按 [BUG-0112](../bug/问题-0112-统计安全快照超时误报失败.md) 调整为 3000ms，超过窗口仍保留 error。
- 隔离开发服务完整重启后，新 worker 的首次 `account-quality-refresh` 在 3ms 内成功，`failureCount=0`；旧 PID 全部退出，无孤儿进程，重启后无新增 warn/error/fatal。
- stderr 在本轮没有新增内容；现存内容仅为 Node SQLite experimental warning。

## 自动化验证

- 后端 19 项本地 AI 问答回归全部通过；真实 PostgreSQL/Redis smoke 已在本功能分支先前验证通过，本次后台超时修改不涉及聊天存储实现。
- 前端 19 项 AI 问答回归全部通过，覆盖流式状态、最近轮次编辑、断流确权、编辑器历史、图片文档、滚动、消息布局、工具过程、Markdown、代码和 Mermaid。
- `pnpm typecheck` 与 `pnpm build` 全部通过；构建只有既有 Vite chunk size warning。
- 后台定向回归 `test:usage-pricing`、`test:background-queue-health`、`test:background-metrics-worker-role`、`test:background-ipc-snapshot-current-only` 全部通过。

## 验收结论

- 真实连续对话、图片输入、Markdown/工具渲染、上下文记忆、图片降负、真实压缩、幂等存储、Prompt Cache、worker 与队列闭环均通过。
- 未通过无意义 token 灌注在同一个 922K UI 会话自然触发 70% 压缩；该阈值分支由真实模型压缩专项和既有路由/仓储回归共同覆盖。
- 本轮发现的后台快照误报已修复并记录；类型检查、生产构建、聊天专项与后台相关回归均已通过。
