# 厂商模型目录更新与清洗指南

> 面向 AI 和维护者。
> 本文记录各厂商模型目录的更新、核对、清洗和验证方法。不同厂商的官方来源、废弃规则和模型命名方式可能不同，后续新增厂商时必须单独补充厂商章节，不要把 OpenAI 的做法直接套用到其他厂商。

## 1. 目标

- 保证供应商页和 `/v1/models` 只展示当前真实存在、可用于对应厂商 API 的模型。
- 清理官方已废弃、已关闭、仅历史兼容、仅旧快照、第三方快照误收录或官方不存在的模型。
- 保留成本估算需要的当前稳定别名和官方快照，避免旧模型被误估价。
- 模型价格和 usage 统计口径的详细核对方法见 [模型价格与用量统计口径](模型价格与用量统计口径.md)。
- 记录每次清洗的来源、工具、口径和验证方式，让下一次更新可以快速复用。

## 2. 通用流程

1. 先定位当前内置模型来源。
   - 当前 OpenAI 目录在 `backend/src/modules/model-pricing/`。
   - 供应商模型列表由 `listProviderModelPricing()` 暴露，网关 `/v1/models` 也复用同一目录。
   - 前端账号测试候选在 `frontend/src/views/accounts/accountOptions.ts`，测试弹窗优先从后端模型接口加载。

2. 收集厂商官方来源。
   - 优先使用厂商官方文档、官方模型列表、官方价格页和官方废弃说明。
   - 不把第三方价格仓库、博客、论坛或社区清单作为最终事实来源。
   - 如果厂商提供真实 API 的模型列表接口，可以在安全环境下作为二次核验，但不要在文档或日志中暴露真实 API Key。

3. 抽取本地模型 ID。
   - 使用 `rg` 查找模型定义，不手动凭印象整理。
   - 对模型 ID 去重、分组，并标出是否是稳定别名、日期快照、预览模型、专用能力模型或废弃模型。

4. 建立清理口径。
   - 官方模型页不存在：从展示目录移除。
   - 官方废弃页列为已关闭或有明确关闭日期：从展示目录移除；必要时在成本估算中显式屏蔽。
   - 仅历史快照、旧 preview、旧 realtime/audio/search 预览：从展示目录移除。
   - 当前稳定别名和仍推荐使用的官方快照：可保留。
   - 模糊匹配不能把不存在的模型映射到相近模型，例如不能把 `xxx-spark` 误映射到 `xxx`。

5. 修改代码后做验证。
   - 验证模型列表数量和具体 ID。
   - 验证已废弃或不存在 ID 不再命中。
   - 跑相关 typecheck。
   - 如果后端类型检查失败，要区分是否为本次改动造成，并记录失败位置。

## 3. OpenAI 厂商

### 3.1 官方来源

OpenAI 模型目录清洗必须优先使用官方页面：

- 模型总览：[OpenAI Models](https://developers.openai.com/api/docs/models)
- 价格目录：[OpenAI Pricing](https://developers.openai.com/api/docs/pricing)
- 废弃说明：[OpenAI Deprecations](https://developers.openai.com/api/docs/deprecations)
- 单模型页面：`https://developers.openai.com/api/docs/models/<model-id-or-family>`

如果 `openai-docs` 技能可用，先用 OpenAI developer docs MCP 检索和读取官方文档；如果当前会话没有 MCP 工具或无结果，再回退到官方域名网页检索。回退检索只使用 `developers.openai.com` 或 `platform.openai.com`。

### 3.2 本次清洗方法记录

本次清洗日期：`2026-05-08`。

本次采用的方法：

- 先读取 `openai-docs` 技能说明，确认 OpenAI 资料必须以官方文档为准。
- 尝试配置 OpenAI docs MCP：`codex mcp add openaiDeveloperDocs --url https://developers.openai.com/mcp`。
- 当前会话 MCP 工具未热刷新，因此回退到官方网页。
- 用 `web` 和 PowerShell `Invoke-WebRequest` 访问官方模型页、价格页和废弃页。
- 用正则从官方页面提取模型 ID、单模型链接和废弃页中的关闭表。
- 用本地脚本逐个检查内置模型对应的官方单模型页面是否存在。
- 用本地脚本验证已知废弃或不存在 ID 不再出现在内置展示清单。

关键判断：

- `Models` 页面用于确认当前推荐和可见模型族。
- `Pricing` 页面用于确认仍有官方计价的模型和价格字段。
- `Deprecations` 页面用于清理已关闭、即将关闭或官方建议替换的模型。
- 单模型页标题为 `Page not found | OpenAI API` 时，视为官方模型页不存在。
- 有些旧模型仍可能出现在价格页或废弃页历史内容中，但只要废弃页给出关闭计划，就不再作为内置展示模型。

### 3.3 常用命令

抽取本地内置模型：

```powershell
rg 'model: "' backend/src/modules/model-pricing
```

从 OpenAI 官方模型首页提取单模型页面链接：

```powershell
$content = (Invoke-WebRequest -Uri 'https://developers.openai.com/api/docs/models' -UseBasicParsing).Content
[regex]::Matches($content, '/api/docs/models/[^"''?#< ]+') |
  ForEach-Object { $_.Value } |
  Sort-Object -Unique
```

从 OpenAI 官方价格页提取可能出现的模型 ID：

```powershell
$content = (Invoke-WebRequest -Uri 'https://developers.openai.com/api/docs/pricing' -UseBasicParsing).Content
[regex]::Matches($content, 'gpt-[a-zA-Z0-9.\-]+|o[0-9][a-zA-Z0-9.\-]*') |
  ForEach-Object { $_.Value } |
  Sort-Object -Unique
```

从 OpenAI 官方废弃页提取废弃相关模型 ID：

```powershell
$content = (Invoke-WebRequest -Uri 'https://developers.openai.com/api/docs/deprecations' -UseBasicParsing).Content
[regex]::Matches($content, 'gpt-[a-zA-Z0-9.\-]+|o[0-9][a-zA-Z0-9.\-]*') |
  ForEach-Object { $_.Value } |
  Sort-Object -Unique
```

验证内置模型是否都有官方模型页：

```powershell
$ids = rg 'model: "' backend/src/modules/model-pricing |
  ForEach-Object {
    if ($_ -match 'model:\s*"([^"]+)"') { $Matches[1] }
  } |
  Sort-Object -Unique

$bad = @()
foreach ($id in $ids) {
  $slug = ($id -replace '-\d{4}-\d{2}-\d{2}$','')
  $content = (Invoke-WebRequest -Uri "https://developers.openai.com/api/docs/models/$slug" -UseBasicParsing).Content
  $title = [regex]::Match($content, '<title>(.*?)</title>').Groups[1].Value
  if ($title -match '^Page not found') {
    $bad += "$id -> $slug"
  }
}

if ($bad.Count) { $bad } else { 'All curated model docs pages resolved.' }
```

验证已知废弃或不存在模型不在展示清单中：

```powershell
$ids = rg 'model: "' backend/src/modules/model-pricing |
  ForEach-Object {
    if ($_ -match 'model:\s*"([^"]+)"') { $Matches[1] }
  } |
  Sort-Object -Unique

$deprecated = @(
  'gpt-3.5-turbo',
  'gpt-4',
  'gpt-4-turbo',
  'gpt-4o-realtime-preview',
  'gpt-4o-mini-realtime-preview',
  'gpt-4o-audio-preview',
  'gpt-4o-mini-audio-preview',
  'gpt-5-chat-latest',
  'gpt-5-codex',
  'gpt-5.1-chat-latest',
  'gpt-5.1-codex',
  'gpt-5.1-codex-max',
  'gpt-5.1-codex-mini',
  'gpt-5.2-codex',
  'gpt-5.3-codex-spark',
  'gpt-image-1',
  'o1-2024-12-17',
  'o1-pro-2025-03-19',
  'o3-mini-2025-01-31',
  'o4-mini-2025-04-16'
)

$found = $ids | Where-Object { $deprecated -contains $_ }
if ($found) { $found } else { 'No known deprecated/nonexistent model IDs in curated list.' }
```

验证后端暴露的模型列表和成本匹配：

```powershell
@'
import { listProviderModelPricing, getProviderModelPricing } from './src/modules/model-pricing/model-pricing.service.ts'

const models = listProviderModelPricing('openai').map((item) => item.model)
console.log(models.length)
console.log(models.join('\n'))

for (const id of [
  'gpt-3.5-turbo',
  'gpt-4',
  'gpt-5.2-codex',
  'gpt-5.3-codex-spark',
  'gpt-4o-realtime-preview',
  'gpt-5.5',
  'gpt-5.4-mini-2026-03-17'
]) {
  console.log(`${id} => ${getProviderModelPricing('openai', id)?.model ?? 'none'}`)
}
'@ | pnpm --filter juhe-ai-backend exec tsx -
```

### 3.4 代码落点

- 聚合入口：`backend/src/modules/model-pricing/openai-model-pricing.data.ts`
- GPT-5 族：`backend/src/modules/model-pricing/openai-model-pricing.gpt5.data.ts`
- GPT-4.1 / GPT-4o 族：`backend/src/modules/model-pricing/openai-model-pricing.gpt4.data.ts`
- 音频 / 图片 / Realtime：`backend/src/modules/model-pricing/openai-model-pricing.media-realtime.data.ts`
- o-series 推理模型：`backend/src/modules/model-pricing/openai-model-pricing.reasoning.data.ts`
- 计价查找、日期快照兜底和废弃模型屏蔽：`backend/src/modules/model-pricing/model-pricing.service.ts`
- 账号测试默认候选：`frontend/src/views/accounts/accountOptions.ts`
- 账号测试弹窗模型加载：`frontend/src/views/accounts/AccountsView.vue`
- `/v1/models` 输出：`backend/src/modules/gateway/openai-gateway-route-helpers.ts`

### 3.5 OpenAI 清理规则

- 保留官方当前模型页存在、且未被废弃页列为关闭对象的稳定模型。
- 可以保留官方当前日期快照，例如 `gpt-5.5-2026-04-23`，但不要保留旧快照或废弃页明确给出替代模型的快照。
- 不保留 `gpt-3.5`、旧 `gpt-4`、旧 `gpt-4-turbo`、旧 preview、旧 realtime preview、旧 audio preview、旧 search preview 等展示项。
- 不保留官方模型页不存在的 ID，例如 `gpt-5.3-codex-spark`。
- 不让废弃模型进入 `findOpenAIModelPricing()` 的模糊匹配结果，避免旧 ID 被误计价。
- 更新账号测试默认模型时，优先选择当前稳定、低风险的官方模型；前端默认候选必须来自后端模型接口或与后端目录一致。

### 3.6 验证记录模板

每次更新 OpenAI 模型目录后，在提交说明或对应计划中记录：

```text
模型来源：
- Models: <访问日期 + URL>
- Pricing: <访问日期 + URL>
- Deprecations: <访问日期 + URL>

清理范围：
- 新增：
- 保留：
- 删除：
- 显式屏蔽：

验证：
- 官方模型页解析：通过 / 未通过
- 废弃模型不在清单：通过 / 未通过
- 后端模型列表脚本：通过 / 未通过
- pnpm --filter juhe-ai-backend typecheck：通过 / 未通过，失败原因
- pnpm --filter juhe-ai-frontend typecheck：通过 / 未通过，失败原因
```

## 4. 其他厂商扩展模板

新增其他厂商时，在本节追加独立小节，至少写清楚：

- 厂商名称和供应商 code。
- 官方模型列表来源。
- 官方价格来源。
- 官方废弃或生命周期说明来源。
- 是否有真实 API 模型列表接口。
- 模型 ID 是否有日期快照、别名、区域差异、账号等级差异或灰度差异。
- 哪些模型只用于特定端点，不应该暴露到通用 `/v1/models`。
- 本地代码落点。
- 清理规则和验证命令。

模板：

```text
### <厂商名> 厂商

官方来源：
- 模型列表：
- 价格：
- 废弃说明：
- API 列表接口：

清理规则：
- 保留：
- 删除：
- 显式屏蔽：
- 模糊匹配：

验证命令：
- <命令>

注意事项：
- <账号权限、区域、灰度、端点差异等>
```

## 5. 注意事项

- 不要把某个厂商的废弃规则推导到另一个厂商。
- 不要只因为第三方价格仓库还存在某个模型，就把它放回内置目录。
- 不要在代码注释里写过长的调研过程；调研过程写在本文，代码里只保留必要来源日期。
- 不要把真实上游 API Key、OAuth Token、账户 ID 或完整敏感响应写入文档。
- 如果本地已有用户改动，清理模型时只改模型目录和直接相关默认候选，不顺手重构无关模块。
