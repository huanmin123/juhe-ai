# 厂商模型目录更新与清洗指南

> 面向 AI 和维护者。
> 本文记录各厂商模型目录的更新、核对、清洗和验证方法。不同厂商的官方来源、废弃规则和模型命名方式可能不同，后续新增厂商时必须单独补充厂商章节，不要把 OpenAI 的做法直接套用到其他厂商。
> 本文只管理代码内置的官方厂商模型目录。管理员全局模型、用户个人模型、自定义价格和账号模型映射见 [自定义模型与模型映射设计](自定义模型与模型映射设计.md)。

## 1. 目标

- 保证代码内置官方模型目录只展示真实存在、可用于对应厂商 API 的模型。公开客户端 `/models`、`/v1/models` 动态聚合全部启用真实供应商目录；有效 API Key 按其路由策略全部 active 分组涉及的供应商聚合，并叠加调用方可见的全局 / 个人自定义模型。各具体供应商仍只维护自身模型事实，客户端聚合不改变供应商归属。
- 清理官方已废弃、已关闭、仅历史兼容、仅旧快照、第三方快照误收录或官方不存在的模型。
- 保留成本估算需要的当前稳定别名和官方快照，避免旧模型被误估价。
- 新增智谱 GLM 等非 OpenAI 厂商时，必须单独记录官方模型页、价格页、接入端点、废弃规则和模型别名，不要复用 OpenAI 的清洗结论。
- 模型价格和 usage 统计口径的详细核对方法见 [模型价格与用量统计口径](模型价格与用量统计口径.md)。
- 记录每次清洗的来源、工具、口径和验证方式，让下一次更新可以快速复用。

## 2. 通用流程

1. 先定位当前内置模型来源。
   - 当前 OpenAI 目录在 `backend/src/modules/model-pricing/`。
   - 内置供应商模型列表由 `listProviderModelPricing()` 暴露；网关 `/v1/models` 的最终目录在内置模型基础上叠加全局 / 个人自定义模型。
   - 客户端模型列表和 AI 问答直接复用供应商级目录缓存，不读取 `default/codex`、`chat_list:*` 或 `chat_model:*` 发布响应快照；最终发布快照基础设施已完整退场。
   - 管理后台通用模型筛选由 `/providers/models/options` 暴露启用供应商的模型名称选项，前端只在该选项目录内做输入匹配，列表查询仍提交完整模型名。
   - 新账户检查模型默认来自供应商模型目录的有效默认链；列表人工测试候选由账户 `test-options` 接口按所有者作用域和协议档案按需返回，不维护前端静态候选。

2. 收集厂商官方来源。
   - 优先使用厂商官方文档、官方模型列表、官方价格页和官方废弃说明。
   - 官方资料缺少发布日期时，可以使用官方仓库、官方模型托管账号、可信新闻、论坛或社区首发记录交叉核验；单一无来源转载不能作为最终事实。
   - 如果厂商提供真实 API 的模型列表接口，可以在安全环境下作为二次核验，但不要在文档或日志中暴露真实 API Key。

3. 抽取本地模型 ID。
   - 使用 `rg` 查找模型定义，不手动凭印象整理。
   - 对模型 ID 去重、分组，并标出是否是稳定别名、日期快照、预览模型、专用能力模型或废弃模型。

4. 建立清理口径。
   - 官方模型页不存在：从展示目录移除。
   - 官方价格页没有可用单价：不要猜价；除非单模型页明确给出同口径价格，否则不进入内置可见目录。
   - 官方废弃页列出 shutdown date：只有 shutdown date 已经到达或早于当前 UTC 日期时，才从展示目录移除并在成本估算中屏蔽；shutdown date 仍在未来的模型继续保留计价，并标记关闭日期。
- 仅历史快照、旧 preview、旧 realtime/audio/search 预览：从展示目录移除。
- 当前稳定别名和仍推荐使用的官方快照：可保留。
- 模糊匹配不能把不存在的模型映射到相近模型，例如不能把 `xxx-spark` 误映射到 `xxx`。
- 模型用途和接口协议必须分开记录：用途用于前端分类，例如对话 / 编码、图像、音频；接口协议用于展示模型支持的 API 形态，例如 `Chat Completions`、`Responses`、`Images API`。
- 本地价格数据里的 `mode` 只能作为计价或默认口径参考，不能当成模型唯一支持的接口协议；如果官方确认同一模型同时支持多个协议，应在接口返回里同时展示。
- 供应商模型接口里的 `releaseDate` 在页面展示为“发布时间”，表示模型大致对外可用或官方快照对应的发布时间，用于模型列表倒序排序；不要把它理解成价格数据更新日期。
- 客户端模型聚合不按 `releaseDate` 过滤，但内置事实源在聚合前执行完整性门禁；缺少发布日期、协议、输入输出模态或计价事实的内置模型必须从内置源删除。管理员和用户自定义模型仍可按自身资料完整度保留空字段。
- GPT 模型必须同时核对精确 API 请求能力：`supportedServiceTiers`、`supportedReasoningEfforts` 和可选 `defaultReasoningEffort`。稳定别名、日期快照和 `pricingModel` 别名继承已确认规范模型能力，不能按模型名前缀猜测。
- OpenAI API wire reasoning 与 Codex picker 能力分开记录。Codex `/models` 使用独立默认 reasoning、支持级别、服务等级和多代理版本；`ultra` 不进入账户 reasoning 覆盖数组，自定义模型第一版也不能仅凭字符串声明 Ultra。Responses Multi-agent Beta 不纳入本期账户覆盖字段。
- 容量字段必须拆开：`context_window_tokens` 表示总上下文窗口，`max_input_tokens` 表示官方明确公布的最大输入，`max_output_tokens` 表示最大输出。厂商只公布总窗口和最大输出时，不得把总窗口复制到最大输入；请求预算可以用 `context - max output` 派生保守输入上限。
- `input_modalities`、`output_modalities` 和 `supported_tools` 必须来自模型官方页。协议支持不能替代模型能力，例如 Responses 可用不等于模型支持图片，也不等于模型支持 `web_search`。
- `supported_service_tiers` 只记录供应商真实请求参数支持的 `priority` / `flex`；购买套餐、预置吞吐、批处理或同名但取值不同的厂商能力不得强行映射。
- 上游标准模型列表接口通常只负责返回可用模型 ID；AI 问答使用“上游 ID 列表与本地官方能力快照取交集”，不能把列表接口缺少能力字段解释为模型不支持。

5. 修改代码后做验证。
   - 验证模型列表数量和具体 ID。
   - 验证已废弃或不存在 ID 不再命中。
   - 跑相关 typecheck。
   - 如果后端类型检查失败，要区分是否为本次改动造成，并记录失败位置。

### 2.1 当前逐供应商能力核对矩阵

以下结论只描述当前代码能安全透传和计价的能力；“默认”表示目录是否替上游或客户端做选择，不表示模型一定支持该档位。

| 供应商 | 服务等级 | 思考级别 | 默认思考级别 | 当前处理结论 |
| --- | --- | --- | --- | --- |
| GPT / OpenAI | `priority`、`flex` 按模型精确声明；未声明时省略 | `none`、`minimal`、`low`、`medium`、`high`、`xhigh`、`max` 按模型精确声明 | 不写入 GPT API 模型目录，由上游决定；Codex `/models` 的独立 picker 元数据另行维护 | 中转不替 OpenAI API 请求选择默认 effort |
| Anthropic | 官方原生 `service_tier` 是 `auto` / `standard_only`，不等价于本站 `priority` / `flex`，因此目录不展示本站服务等级 | 仅官方模型公开的 `low`、`medium`、`high`、`xhigh`、`max` | 当前官方 effort 文档明确为 `high` 的模型记录 `high` | 原生 Messages 透传自己的 service tier 语义，禁止错误映射成 OpenAI 档位 |
| Gemini | `service_tier=priority|flex`；Standard 通过省略字段或 `standard` 表达 | 按模型记录 `minimal` / `low` / `medium` / `high` | 3.5 Flash=`medium`；3.1 Pro / 3 Flash=`high`；3.1 Flash-Lite 与 2.5 系列不替上游填写 | Priority/Flex 专用价格必须进入原始计价、目录计价和账单明细 |
| xAI / Grok | 官方请求支持 `default` / `priority`；本目录只列非默认的 `priority`，标准由省略字段表示 | 仅对官方发布资料明确列出的模型记录 `low` / `medium` / `high` / `xhigh` 等精确档位 | 有官方默认值时记录，否则不替上游设置 | 复用 OpenAI Chat / Responses 协议，模型间能力不按名称推断 |
| DeepSeek | 不支持本站服务等级 | V4=`high` / `max`；旧兼容模型不声明 | V4 官方默认 `high`；网关不替旧模型添加 effort | `reasoning_effort` 只在 DeepSeek 自身 wire 能力成立时透传 |
| GLM | 不支持本站服务等级 | GLM-5.2=`high` / `max`；其他模型不声明离散 effort | GLM-5.2 官方默认 `max` | 不把 thinking 开关扩写成 OpenAI 服务等级 |
| 通用 OpenAI-compatible | 只能使用用户自定义且有真实上游依据的 `priority` / `flex` | 只能使用用户自定义且有真实上游依据的 effort | 新增自定义模型默认留空 | 不从供应商名称、模型前缀或客户端字段猜测能力 |

GPT 和自定义模型的默认 effort 留空是有意设计：本项目是中转，不是客户端；转发路径应保留客户端显式参数，未提供时让真实上游自行决策。

## 3. OpenAI 厂商

### 3.1 官方来源

OpenAI 模型目录清洗必须优先使用官方页面：

- 模型总览：[OpenAI Models](https://developers.openai.com/api/docs/models)
- 最新模型指南：[Using the latest model](https://developers.openai.com/api/docs/guides/latest-model.md)
- 价格目录：[OpenAI Pricing](https://developers.openai.com/api/docs/pricing)
- 废弃说明：[OpenAI Deprecations](https://developers.openai.com/api/docs/deprecations)
- 单模型页面：`https://developers.openai.com/api/docs/models/<model-id-or-family>`

如果 `openai-docs` 技能可用，先用 OpenAI developer docs MCP 检索和读取官方文档；如果当前会话没有 MCP 工具或无结果，再回退到官方域名网页检索。回退检索只使用 `developers.openai.com` 或 `platform.openai.com`。

### 3.2 本次清洗方法记录

本次清洗日期：`2026-05-08`。

本次采用的方法：

- 先读取 `openai-docs` 技能说明，确认 OpenAI 资料必须以官方文档为准。
- 尝试配置 OpenAI docs MCP：`codex mcp add openaiDeveloperDocs --url https://developers.openai.com/mcp`。
- 当前会话 MCP 工具未热刷新，因此回退到官方网页。
- 用 OpenAI developer docs MCP、`web` 和 PowerShell `Invoke-WebRequest` 访问官方模型页、价格页和废弃页。
- 用正则从官方页面提取模型 ID、单模型链接和废弃页中的关闭表。
- 用本地脚本逐个检查内置模型对应的官方单模型页面是否存在。
- 用本地脚本验证已关闭或不存在 ID 不再出现在内置展示清单，未来关闭但仍可用的 ID 仍能计价。

关键判断：

- `Models` 页面用于确认当前推荐和可见模型族。
- `Pricing` 页面用于确认仍有官方计价的模型和价格字段。
- `Deprecations` 页面用于确认 shutdown date 和替代模型；“已宣布废弃”不等于“现在不能用”，只有 shutdown date 到达后才视为不可访问。
- 单模型页标题为 `Page not found | OpenAI API` 时，视为官方模型页不存在。
- 有些旧模型不再推荐但仍有官方模型页、价格页和未来 shutdown date，例如 `gpt-5.2-codex`、`gpt-5-codex`、`gpt-4`、`gpt-3.5-turbo`；这些模型仍应进入计价目录，避免用户真实调用后无法记账。
- OpenAI 的 `Chat Completions` 和 `Responses` 是接口协议，不是模型用途分类；通用文本模型如果官方端点表同时支持 `/v1/chat/completions` 和 `/v1/responses`，供应商页应同时显示两个协议标签。Codex / Pro 等仅 Responses 口径的模型只显示 `Responses`。

2026-05-08 复查修正：

- 之前把废弃页中的未来 shutdown date 误当成“立即不可用”，导致 `gpt-5.2`、`gpt-5.2-codex`、`gpt-5.1`、`gpt-5`、旧 Codex、`gpt-4`、`gpt-3.5-turbo`、`gpt-image-1`、`o1`、`o3-mini` 等仍可用且有官方价格的模型缺失。
- 本次改为按当前日期过滤 shutdown：未来关闭的模型继续保留计价，供应商页显示 `将停用 <date>`；到期后 `listProviderModelPricing()` 自动不再展示，`getProviderModelPricing()` 不再命中。
- 模糊匹配只允许已知日期快照或明确家族兜底，不把 `gpt-5.3-codex-spark` 这类官方不存在的 ID 映射到相近模型。
- 供应商模型接口新增 `supportedApiProtocols` 字段，用数组表达模型支持的接口协议，避免把单个 `mode` 误当成唯一协议。
- 供应商模型列表把 `releaseDate` 统一改为“发布时间”口径：优先使用官方模型快照日期；稳定别名没有快照日期时，使用官方 endpoint support 表、changelog 或 deprecations 中可确认的模型族发布时间；官方只给到月份的新增项用当月 `01` 作为近似排序锚点。
- `listProviderModelPricing()` 和最终 `listProviderModelCatalog()` 都要求可展示内置模型有 `releaseDate`，并按发布时间倒序返回，避免前端出现空日期或旧模型排在新模型前面；自定义模型未填写发布时间时排在已填写发布时间的模型之后。
- 验证命令：`pnpm --filter juhe-ai-backend test:usage-pricing`、`pnpm --filter juhe-ai-backend typecheck`、`pnpm --filter juhe-ai-frontend typecheck`。

2026-06-22 复查修正：

- OpenAI developer docs `latest-model.md` 明确当前最新模型为 `gpt-5.5`，本地 `gpt` 和通用 `openai` 目录继续以 `gpt-5.5` 系列作为首屏默认顺序。
- OpenAI-compatible 目录首屏顺序由 `releaseDate` 驱动并用回归测试固定：`gpt-5.5`、`gpt-5.5-2026-04-23`、`gpt-5.5-pro`、`gpt-5.5-pro-2026-04-23`、`gpt-image-2`、`gpt-image-2-2026-04-21`、`gpt-5.4-mini`、`gpt-5.4-mini-2026-03-17`。
- `catalog_order` 仅用于 GLM、DeepSeek、Anthropic 这类官方目录顺序比日期更可靠的供应商；OpenAI-compatible 继续优先使用官方可确认的发布日期倒序。

2026-07-10 能力口径补充：

- GPT 模型目录以精确数组表达服务等级与 OpenAI API reasoning effort；`supportsServiceTier` 如保留，只能由 `supportedServiceTiers.length > 0` 派生。
- GPT 档位价格分别维护 standard / priority / flex；模型声明支持档位但缺少专用价时保持未定价，禁止使用系统倍率或默认倍率猜测。长上下文阈值与输入、输出倍率属于同一价格快照，计价时在选定档位后应用。
- Priority 与 Flex 平级，模型能力数组必须逐模型精确声明，不允许从 `supportsServiceTier`、Priority 能力或通用倍率推导 Flex。
- 账户服务等级 / 思考级别覆盖的配置候选取支持模型能力并集；运行时使用映射后的最终上游模型能力逐字段判断，最终模型不支持或能力未知时保留客户端值，不做向下降级。
- Codex `/models` 不再为所有模型统一返回 `minimal|low|medium|high` 和空服务等级，必须消费 Codex 专用能力字段。
- 模型目录默认检查模型只初始化后续新账户；已有账户的 `healthCheckModel` 不随目录默认修改。

2026-07-13 跨厂商能力复查：

- OpenAI 模型把总上下文、最大输入、最大输出、输入/输出模态、工具、reasoning effort 和 service tier 分开记录；AI 问答不再接受客户端自报上下文大小。
- 同一模型 ID 可由多个可达供应商账户承接时，AI 问答只暴露所有候选目录共同支持的思考档位、服务等级、模态和工具，并取最小容量，避免切换账户后把不支持的参数发给上游。
- OpenAI 标准 `GET /v1/models` 仅用于确认可用 ID；精确能力来自本站按官方文档维护的快照。

### 3.3 常用命令

抽取本地内置模型：

```powershell
rg 'model: "' backend/src/modules/model-pricing
```

从 OpenAI 官方模型首页提取单模型页面链接：

```powershell
$content = (Invoke-WebRequest -Uri 'https://developers.openai.com/api/docs/models' -UseBasicParsing).Content
[regex]::Matches($content, '/api/docs/models/[^"''?#< ]+') |
  ForEach-Object { $_.Value } |
  Sort-Object -Unique
```

从 OpenAI 官方价格页提取可能出现的模型 ID：

```powershell
$content = (Invoke-WebRequest -Uri 'https://developers.openai.com/api/docs/pricing' -UseBasicParsing).Content
[regex]::Matches($content, 'gpt-[a-zA-Z0-9.\-]+|o[0-9][a-zA-Z0-9.\-]*') |
  ForEach-Object { $_.Value } |
  Sort-Object -Unique
```

从 OpenAI 官方废弃页提取废弃相关模型 ID：

```powershell
$content = (Invoke-WebRequest -Uri 'https://developers.openai.com/api/docs/deprecations' -UseBasicParsing).Content
[regex]::Matches($content, 'gpt-[a-zA-Z0-9.\-]+|o[0-9][a-zA-Z0-9.\-]*') |
  ForEach-Object { $_.Value } |
  Sort-Object -Unique
```

验证内置模型是否都有官方模型页：

```powershell
$ids = rg 'model: "' backend/src/modules/model-pricing |
  ForEach-Object {
    if ($_ -match 'model:\s*"([^"]+)"') { $Matches[1] }
  } |
  Sort-Object -Unique

$bad = @()
foreach ($id in $ids) {
  $slug = ($id -replace '-\d{4}-\d{2}-\d{2}$','')
  $content = (Invoke-WebRequest -Uri "https://developers.openai.com/api/docs/models/$slug" -UseBasicParsing).Content
  $title = [regex]::Match($content, '<title>(.*?)</title>').Groups[1].Value
  if ($title -match '^Page not found') {
    $bad += "$id -> $slug"
  }
}

if ($bad.Count) { $bad } else { 'All curated model docs pages resolved.' }
```

验证已关闭或不存在模型不在展示清单中，同时确认未来关闭模型仍可计价：

```powershell
$ids = rg 'model: "' backend/src/modules/model-pricing |
  ForEach-Object {
    if ($_ -match 'model:\s*"([^"]+)"') { $Matches[1] }
  } |
  Sort-Object -Unique

$unavailable = @(
  'gpt-4o-realtime-preview',
  'gpt-4o-mini-realtime-preview',
  'gpt-4o-audio-preview',
  'gpt-4o-mini-audio-preview',
  'gpt-5.3-codex-spark',
  'o1-2024-12-17',
  'o1-pro-2025-03-19',
  'o3-mini-2025-01-31',
  'o4-mini-2025-04-16'
)

$mustPrice = @(
  'gpt-5.3-codex',
  'gpt-5.2',
  'gpt-5.2-codex',
  'gpt-5.1',
  'gpt-5-codex',
  'gpt-4',
  'gpt-4-turbo',
  'gpt-3.5-turbo',
  'gpt-image-1',
  'o1',
  'o3-mini'
)

$foundUnavailable = $ids | Where-Object { $unavailable -contains $_ }
$missingPriced = $mustPrice | Where-Object { $ids -notcontains $_ }
if ($foundUnavailable) { "Unexpected unavailable: $($foundUnavailable -join ', ')" }
if ($missingPriced) { "Missing still-usable priced models: $($missingPriced -join ', ')" }
if (!$foundUnavailable -and !$missingPriced) { 'Unavailable models excluded and still-usable priced models included.' }
```

验证后端暴露的模型列表和成本匹配：

```powershell
@'
import { listProviderModelPricing, getProviderModelPricing } from './src/modules/model-pricing/model-pricing.service.ts'

const models = listProviderModelPricing('openai').map((item) => item.model)
console.log(models.length)
console.log(models.join('\n'))

for (const id of [
  'gpt-5.3-codex',
  'gpt-5.2',
  'gpt-5.2-codex',
  'gpt-5-codex',
  'gpt-4',
  'gpt-3.5-turbo',
  'gpt-5.3-codex-spark',
  'gpt-4o-realtime-preview',
  'gpt-5.5',
  'gpt-5.4-mini-2026-03-17'
]) {
  console.log(`${id} => ${getProviderModelPricing('openai', id)?.model ?? 'none'}`)
}
'@ | pnpm --filter juhe-ai-backend exec tsx -
```

### 3.4 代码落点

- 聚合入口：`backend/src/modules/model-pricing/openai-model-pricing.data.ts`
- GPT-5 族：`backend/src/modules/model-pricing/openai-model-pricing.gpt5.data.ts`
- GPT-4.1 / GPT-4o 族：`backend/src/modules/model-pricing/openai-model-pricing.gpt4.data.ts`
- 图片模型：`backend/src/modules/model-pricing/openai-model-pricing.image.data.ts`
- 当前产品未实现专用音频模型调用链，因此内置目录不收录 Audio、Realtime、转写或语音合成模型；新增自定义模型也只开放文本和图像用途。
- o-series 推理模型：`backend/src/modules/model-pricing/openai-model-pricing.reasoning.data.ts`
- 计价查找、日期快照兜底、shutdown date 过滤和不可用模型屏蔽：`backend/src/modules/model-pricing/model-pricing.service.ts`
- 管理后台模型筛选选项接口：`backend/src/modules/providers/providers.routes.ts`
- 新账户默认检查模型：供应商选项和 `provider_default_health_check_models`
- 列表人工测试模型加载：账户 `test-options` 接口
- `/v1/models` 输出：`backend/src/modules/gateway/protocols/openai-v1/route-helpers.ts`

### 3.5 OpenAI 清理规则

- 保留官方当前模型页存在、价格页有单价、且 shutdown date 尚未到达的模型，包括不再推荐但仍可访问的旧模型。
- 可以保留官方当前日期快照，例如 `gpt-5.5-2026-04-23`；旧快照如果官方价格页仍有单价且 shutdown date 未到，也可保留用于计费。
- 不保留已经 shutdown 的模型、官方模型页不存在的模型、官方价格页没有单价且无法从单模型页确认同口径价格的模型。
- 不保留旧 realtime preview、旧 audio preview、旧 search preview 等已经 shutdown 或由 GA 模型替代的 preview 展示项。
- 不保留官方模型页不存在的 ID，例如 `gpt-5.3-codex-spark`。
- 不让不存在或已 shutdown 的模型进入 `findOpenAIModelPricing()` 的模糊匹配结果，避免旧 ID 被误计价。
- 更新协议档案或供应商默认检查模型时，优先选择当前稳定、低风险且支持最小检查请求的官方文本模型；默认变化只影响后续新账户，不批量改写已有账户。

### 3.6 验证记录模板

每次更新 OpenAI 模型目录后，在提交说明或对应计划中记录：

```text
模型来源：
- Models: <访问日期 + URL>
- Pricing: <访问日期 + URL>
- Deprecations: <访问日期 + URL>

清理范围：
- 新增：
- 保留：
- 删除：
- 显式屏蔽：

验证：
- 官方模型页解析：通过 / 未通过
- 废弃模型不在清单：通过 / 未通过
- 后端模型列表脚本：通过 / 未通过
- GPT 服务等级 / wire reasoning 精确数组：通过 / 未通过 / 不适用
- Codex 默认 reasoning / 支持级别 / 服务等级能力：通过 / 未通过 / 不适用
- 默认检查模型仍为启用文本模型且只影响新账户：通过 / 未通过
- pnpm --filter juhe-ai-backend typecheck：通过 / 未通过，失败原因
- pnpm --filter juhe-ai-frontend typecheck：通过 / 未通过，失败原因
```

## 4. 智谱 GLM 厂商

### 4.1 官方来源

智谱 GLM 模型目录清洗必须优先使用官方来源：

- 通用 OpenAI 兼容入口：[OpenAI API 兼容](https://docs.bigmodel.cn/cn/guide/develop/openai/introduction)
- 通用 HTTP API：[HTTP API](https://docs.bigmodel.cn/cn/guide/develop/http/introduction)
- 模型总览：[模型总览](https://docs.bigmodel.cn/cn/guide/start/model-overview)
- 模型参数：[参数说明](https://docs.bigmodel.cn/cn/guide/start/concept-param)
- 价格：[Z.ai Pricing](https://docs.z.ai/guides/overview/pricing)
- 思考模式：[Thinking Mode](https://docs.z.ai/guides/capabilities/thinking-mode)
- GLM Coding Plan 接入：[接入工具](https://docs.bigmodel.cn/cn/coding-plan/tool/others)
- GLM Coding Plan 套餐概览：[套餐概览](https://docs.bigmodel.cn/cn/coding-plan/overview)

如果需要核对开源模型，可参考官方仓库：

- [GLM-4.5](https://github.com/zai-org/GLM-4.5)
- [GLM-5](https://github.com/zai-org/GLM-5)

### 4.2 本地目录规则

- 通用 GLM API、GLM Coding Plan 和 GLM Coding Anthropic 都归入 `providerCode = glm`，但必须分成独立账户协议档案；分组只按 GLM 供应商归属。当前模型目录清洗优先维护两个 OpenAI v1 Chat 档案，Anthropic Messages 档案按自身协议能力独立验收。
- 当前对话模型只收录 `chat_completions` 能力，不把 `/responses` 当成 GLM 默认能力；视觉模型用模态字段区分，不再伪装成纯文本模型。
- Coding Plan 模型只在 Coding Plan 账户档案和检查中使用，不要默认和通用 GLM API 混用同一套检查模型。
- `glm-5.1` 和 `glm-5` 这类历史模型如果官方说明会自动切换到 `glm-5.2`，目录中应优先保留官方当前可见模型，旧别名只在明确确认可计价且有稳定兼容价值时保留。
- 价格必须来自官方价格页或官方模型页；如果短期内无法确认单价，先不进入计价目录。
- GLM 不支持 OpenAI `service_tier=priority|flex`，模型目录不声明服务等级。
- 只有 `glm-5.2` 官方公开 `reasoning_effort` 强度能力，实际可选档位记录为 `high|max`，默认 `max`；其他只支持 thinking 开关的模型不显示思考级别选择。
- 官网给出“总上下文”而非独立最大输入时，只记录 `context_window_tokens` 与 `max_output_tokens`，不复制成 `max_input_tokens`。

### 4.3 验证记录模板

每次更新智谱 GLM 目录后，在提交说明或对应计划中记录：

```text
模型来源：
- OpenAI 兼容：<访问日期 + URL>
- HTTP API：<访问日期 + URL>
- Coding Plan：<访问日期 + URL>

清理范围：
- 新增：
- 保留：
- 删除：
- 显式屏蔽：

验证：
- 通用 GLM API 能否通过 OpenAI SDK 访问
- Coding Plan Key 能否通过 OpenAI Chat Completions 访问
- 通用 API Key 不能进入 Coding Plan 分组
- Coding Plan Key 不能进入通用分组
```

## 5. Anthropic 厂商

### 5.1 官方来源

Anthropic 模型目录清洗必须优先使用官方来源：

- 模型概览：[Anthropic Models](https://docs.anthropic.com/en/docs/about-claude/models/overview)
- 模型列表 API：[List Models](https://platform.claude.com/docs/en/api/models/list)
- 模型废弃说明：[Model deprecations](https://docs.anthropic.com/en/docs/about-claude/model-deprecations)
- Messages API：[Messages](https://docs.anthropic.com/en/api/messages)
- 价格：[Pricing](https://www.anthropic.com/pricing)
- Message token counting：[Count Message tokens](https://docs.anthropic.com/en/api/messages-count-tokens)
- Claude Code 模型配置：[Claude Code model configuration](https://docs.anthropic.com/en/docs/claude-code/model-config)
- Workload Identity Federation：[Workload identity federation](https://platform.claude.com/docs/en/manage-claude/workload-identity-federation)

Anthropic 没有放入 `openai` 聚合目录；官方 Claude 模型只归入 `providerCode = anthropic` 和 `profile_anthropic_anthropic_v1`。如果未来重新评估第三方 Anthropic-compatible、Vertex Anthropic 或 Bedrock Claude，必须建立各自供应商 / 协议档案，不复用官方 Anthropic 目录结论；当前 DeepSeek 不接入 Anthropic-compatible surface。

### 5.2 本地目录规则

- 当前只接入官方 Anthropic API Key 直连，账户类型只允许 `api_key`；Claude Code 是下游客户端画像，其模型配置别名只做客户端解析 / 隐藏计价，不进入 Anthropic 官方模型发现目录，也不新增 Claude.ai / Claude Code 订阅 OAuth 账号目录。官方 Workload Identity Federation 链接只作为能力调研来源：仅保存静态 IdP JWT 无法形成可信自动轮换 source，本次不开放 `workload_identity`；后续必须先接入受控的服务端 assertion provider，能够自动获取、轮换并校验 JWT `exp`，再重新评估认证和目录边界。
- 模型能力以 Anthropic native `messages` 和 `message_token_counting` 标记，不把 `/v1/chat/completions` 或 `/v1/responses` 当成默认能力。
- `GET /v1/models` 在 Anthropic 档案下返回 Anthropic native 模型列表形态；OpenAI v1 分组不会因为同名模型接收 `/v1/messages` 请求。
- 官方 dated ID 可以直接进入 Anthropic 目录并沿用同族稳定别名价格，例如 `claude-haiku-4-5-20251001` 沿用 `claude-haiku-4-5`，`claude-sonnet-4-5-20250929` 沿用 `claude-sonnet-4-5`，`claude-opus-4-5-20251101` 沿用 `claude-opus-4-5`。
- 可见目录按 Anthropic Models API 的“新模型在前”口径维护；当稳定别名没有官方精确发布日期时，不编造 `releaseDate`，用 `catalog_order` 固定下拉框从最新到旧。
- 官方 deprecations 已给 shutdown date 的模型只在 shutdown date 晚于当前 UTC 日期时保留，例如 `claude-opus-4-1`、`claude-opus-4-1-20250805` 在 `2026-08-05` 前可保留并标记关闭日期；`claude-opus-4-20250514`、`claude-sonnet-4-20250514`、Claude 3.x 等已经退休的模型不进入目录。
- Claude Code 模型配置别名按目标族做隐藏计价，但不进入目录：`best`、`fable`、`opus`、`opus[1m]`、`opusplan`、`sonnet`、`sonnet[1m]`、`haiku`。`default` 是清除覆盖并回到推荐模型的控制值，同样不进入目录。
- Antigravity / 兼容网关别名可以作为隐藏计价别名收录并按对应 Claude 族计价，但不能进入官方 Anthropic 模型发现目录；必须明确它们不是 Anthropic 官方直连模型 ID。当前隐藏计价覆盖 Opus 4.6 thinking 的 `antigravity/`、`google/antigravity-`、`google-antigravity/`、`google-antigravity:` 写法，以及 Sonnet 4.6 / Sonnet 4.6 thinking 的同类写法。
- Antigravity 的 `-low`、`-medium`、`-high`、`-max` 等 effort 后缀只做计价回落，不单独列入模型发现目录；旧的 `claude-opus-4-5-thinking` / `google/antigravity-claude-opus-4-5-thinking` 不收录。
- 兼容代理真实联调模型如 `claude-fake-5` 可以作为临时隐藏计价别名收录，但不能进入官方 Anthropic 模型发现目录；后续如果该代理拆为独立供应商，应迁移到对应供应商目录。
- 除已记录的兼容代理验证别名外，价格必须来自 Anthropic 官方价格页或官方模型页；如果短期内无法确认输入、输出、缓存读写等单价，先不进入可计价目录。
- Prompt cache、thinking、tool use、vision 等能力只作为模型能力或 usage 口径记录，不用 OpenAI `chat_completions` 标签代替。
- Anthropic 的 `service_tier` 取值为 `auto|standard_only`，与本站 OpenAI wire 的 `priority|flex` 不同，因此 Claude 模型不声明本站服务等级选项。
- Claude Code、Antigravity、thinking 后缀和测试代理名不得留在内置事实源；第三方兼容需求只能通过管理员或用户自定义模型承接。

### 5.3 验证记录模板

每次更新 Anthropic 目录后，在提交说明或对应计划中记录：

```text
模型来源：
- Models: <访问日期 + URL>
- Pricing: <访问日期 + URL>
- Messages API: <访问日期 + URL>

清理范围：
- 新增：
- 保留：
- 删除：
- 显式屏蔽：

验证：
- Anthropic API Key 能否通过 Messages JSON 访问
- Anthropic API Key 能否通过 Messages SSE 访问
- Count Tokens 是否可用
- OpenAI v1 分组不能接收 /v1/messages
- pnpm --filter juhe-ai-backend test:anthropic-gateway-mock-ai：通过 / 未通过，失败原因
- pnpm --filter juhe-ai-backend typecheck：通过 / 未通过，失败原因
- pnpm --filter juhe-ai-frontend typecheck：通过 / 未通过，失败原因
```

## 6. 其他厂商扩展模板

新增其他厂商时，在本节追加独立小节，至少写清楚：

- 厂商名称和供应商 code。
- 官方模型列表来源。
- 官方价格来源。
- 官方废弃或生命周期说明来源。
- 是否有真实 API 模型列表接口。
- 模型 ID 是否有日期快照、别名、区域差异、账号等级差异或灰度差异。
- 哪些模型只用于特定端点，不应该暴露到通用 `/v1/models`。
- 本地代码落点。
- 清理规则和验证命令。

模板：

```text
### <厂商名> 厂商

官方来源：
- 模型列表：
- 价格：
- 废弃说明：
- API 列表接口：

清理规则：
- 保留：
- 删除：
- 显式屏蔽：
- 模糊匹配：

验证命令：
- <命令>

注意事项：
- <账号权限、区域、灰度、端点差异等>
```

## 7. DeepSeek 厂商

### 7.1 官方来源

DeepSeek 模型目录清洗必须优先使用官方来源：

- API 文档：[DeepSeek API Docs](https://api-docs.deepseek.com/)
- Chat Completions：[Create Chat Completion](https://api-docs.deepseek.com/api/create-chat-completion)
- Thinking Mode：[Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode)
- Context Caching：[Context Caching](https://api-docs.deepseek.com/guides/kv_cache)
- Tool Calls：[Tool Calls](https://api-docs.deepseek.com/guides/tool_calls)
- List Models：[List Models](https://api-docs.deepseek.com/api/list-models)
- 价格：[Pricing](https://api-docs.deepseek.com/quick_start/pricing)
- Anthropic 兼容：[Anthropic API](https://api-docs.deepseek.com/guides/anthropic_api)
- 余额：[Get User Balance](https://api-docs.deepseek.com/api/get-user-balance)
- 官方仓库：[DeepSeek-R1](https://github.com/deepseek-ai/DeepSeek-R1)
- 官方仓库：[DeepSeek-V3](https://github.com/deepseek-ai/DeepSeek-V3)

### 7.2 本地目录规则

- DeepSeek 是独立供应商，不能复用 GPT 的模型目录、价格目录或废弃口径。
- 当前接入 DeepSeek OpenAI-compatible Chat surface 和 DeepSeek Anthropic-compatible Messages surface，两者共用 DeepSeek 供应商模型目录和默认分组；账户协议档案、endpoint mode、支持模型和模型映射负责区分真实上游能力。
- 当前公开模型目录只保留 `deepseek-v4-flash` 和 `deepseek-v4-pro`；`deepseek-ai-v4-*` 不是官方公开 ID，`deepseek-chat` / `deepseek-reasoner` 是即将退役的兼容别名，不继续进入当前内置目录。
- DeepSeek 官方 List Models 接口可作为人工核对或后续后台刷新来源；网关 `/v1/models` 仍使用本地目录，不在请求链路实时请求上游。
- `user_id`、`thinking`、`reasoning_effort`、`reasoning_content`、`prompt_cache_hit_tokens` 和 `prompt_cache_miss_tokens` 只作为 DeepSeek 供应商扩展能力或 usage 字段记录，不写成全局 OpenAI-compatible 必备字段。
- Chat Prefix Completion、FIM Completion 和 Strict Tool Calls 属于 beta 能力，不能进入默认 Chat Completions 模型目录能力。
- DeepSeek Balance 接口不作为模型可用性或价格来源；只可作为后续账户页诊断候选。
- 价格必须来自 DeepSeek 官方文档或官方 API 文档；第三方价格仓库、社区表格和开源权重仓库不作为云 API 定价依据。
- DeepSeek V4 只记录真实有效的 `high|max` 思考档位，默认 `high`；不支持 service tier，也不支持图片输入。
- DeepSeek 官网公布 1M 总上下文与 384K 最大输出，但没有独立最大输入字段；本地不把 1M 重复写成最大输入。

### 7.3 验证记录模板

每次更新 DeepSeek 目录后，在提交说明或对应计划中记录：

```text
模型来源：
- API Docs: <访问日期 + URL>
- Chat Completions: <访问日期 + URL>
- Thinking Mode: <访问日期 + URL>
- Context Caching: <访问日期 + URL>
- List Models: <访问日期 + URL>
- Pricing: <访问日期 + URL>
- GitHub: <访问日期 + URL>

清理范围：
- 新增：
- 保留：
- 删除：
- 显式屏蔽：

验证：
- DeepSeek OpenAI-compatible surface 是否可通过 OpenAI SDK 访问
- `thinking` / `reasoning_effort` 是否可透传
- `reasoning_content` 和 cache usage 是否能按 DeepSeek 扩展字段保留
- 旧别名是否仍需保留
- pnpm --filter juhe-ai-backend typecheck：通过 / 未通过，失败原因
- pnpm --filter juhe-ai-frontend typecheck：通过 / 未通过，失败原因
```

## 8. Gemini 厂商

### 8.1 官方来源

- 模型总览：[Gemini models](https://ai.google.dev/gemini-api/docs/models)
- 价格：[Gemini API pricing](https://ai.google.dev/gemini-api/docs/pricing)
- 思考：[Thinking](https://ai.google.dev/gemini-api/docs/thinking)
- Priority：[Priority mode](https://ai.google.dev/gemini-api/docs/generate-content/priority-inference)
- Flex：[Flex mode](https://ai.google.dev/gemini-api/docs/generate-content/flex-inference)

### 8.2 本地目录规则

- Gemini native 与官方 OpenAI compatibility 共享 Gemini 供应商目录，但 `supportedApiProtocols` 必须按模型官方端点记录。
- `inputTokenLimit`、`outputTokenLimit`、输入/输出模态和 thinking level 逐模型记录；不从模型名前缀猜测。
- Gemini 官方 `service_tier=priority|flex` 可以进入精确服务等级数组；Standard 通过省略字段或官方允许的 `standard` 表达；未公开这两个请求值的模型不得显示服务等级。目录中的 Priority/Flex 价格必须同时写入 `serviceTierPrices`，不能只写能力标签。
- 图片生成、Live、TTS、视频和 Deep Research 专用模型不能混入普通聊天模型；应在对应协议和输出模态接入完成后进入各自分类。
- 标准模型列表接口只用于确认当前账户可见 ID，本地官方快照仍负责价格和精确能力。

## 9. 注意事项

- 不要把某个厂商的废弃规则推导到另一个厂商。
- 不要只因为第三方价格仓库还存在某个模型，就把它放回内置目录。
- 不要在代码注释里写过长的调研过程；调研过程写在本文，代码里只保留必要来源日期。
- 不要把真实上游 API Key、OAuth Token、账户 ID 或完整敏感响应写入文档。
- 如果本地已有用户改动，清理模型时只改模型目录和直接相关默认候选，不顺手重构无关模块。
