# 二次伪迁移台账 · 第二遍核对（BUG-0175 波1-3 修复后的留守文件复验）

- 核验日期：2026-09-08。基线：当前工作区（含 BUG-0175 波1 commit 7d05bae2a、波2+波3 commit c12d6a0aa）。
- 目的：第一轮留守的 606 个 .ts（partial/missing/待复核）重新判定；landed/eliminated 移入 migration-backup-1，剩余为第三轮（或裁决项）队列。
- 判定五值沿用第一轮。执行方式：V-D 由只读代理完成；V-A/V-B/V-C/V-E/V-F 因子代理模型通道耗尽（GLM-5.3/Flash 分组无账户）改由主代理按"第一轮缺口 → 波1-3 修复映射"逐域核对。

## V-D 域结果（modules/providers + model-pricing + external-integrations + openai-compatible-* + oauth 族，83 文件）

代理只读取证完成。**landed 47 / partial 14 / eliminated 8 / missing 0**。

landed 47 文件与 eliminated 8 文件（桥族按"OpenAI 供应商启用、其余预留扩展"判 eliminated，均注明扩展点）移入 backup-1；partial 14 文件留守，缺口清单：

| 文件 | 判定 | 残留缺口 |
| --- | --- | --- |
| drivers/_shared/codex-responses-chat-bridge-tool-identity.ts | partial | tool identity 随 B-4 桥本体；状态机已 landed（gatewaycodex/chatbridgestate.go） |
| drivers/_shared/codex-responses-chat-bridge.ts | partial | responses→chat body 转换/SSE 回转本体未迁（=B-4 待裁决项的组成部分） |
| drivers/_shared/oauth-refresh-lock.ts | partial | 刷新锁仅进程内（jobs accountLocks）；gateway 手动刷新无锁（问题-0162 未变） |
| drivers/_shared/openai-hosted-tool-runtime-registry.ts | partial | code_interpreter/computer 两类 landed；六类归一注册表无对应（唯一消费方=未迁桥） |
| drivers/_shared/provider-request-overrides.ts | partial | RequestBodyOverride 全仓无生产者（=D-152 未闭合） |
| drivers/anthropic/driver.ts | partial | 认证头 landed；anthropic 驱动未注册进 DefaultRegistry（预留扩展关联） |
| drivers/gemini/code-assist-runtime.ts | partial | 请求侧 landed（chain_driver.go:76-171）；响应侧 transformGeminiCodeAssistUpstreamResponse 缺 |
| drivers/gpt/request-override-body.ts | partial | 随 D-152；body 注入语义无运行时实现 |
| drivers/gpt/request-overrides.ts | partial | 随 D-152；service_tier 删写/effort 分家语义未迁 |
| drivers/hybrid/driver.ts | partial | gatewayhybrid 完整；hybridUpstreamTargetForRequest 三目标判定+anthropic/gemini 上游构建未迁（B-4 关联） |
| drivers/openai-compatible/driver.ts | partial | 主链 landed；applyOpenAICompatibleOverrides 缺（D-152 同根） |
| drivers/xai/driver.ts | partial | 刷新/凭据档 landed；xai 驱动未注册+路径白名单/cli-proxy 头缺（预留扩展关联） |
| provider-billing.policies.ts | partial | 计费半边 landed；buildCatalogDisplay 展示半边缺（billing.go:17 自认） |
| external-public-route-strategy.service.ts | partial | list/add/update landed；**SetValidationCacheInvalidator 组合根未装配**（策略更新后 API-Key validation cache 不失效，M06 交叉） |

### V-D 新发现缺陷候选（登记待归口）

1. **D-255（候选）RequestBodyOverride 无生产者**：定义在 gatewaydispatch/upstreamdispatch.go:88-89、消费在 dispatchsingle.go:376-377，全仓无非测试赋值——账号级请求覆盖 body 注入静默无效（D-152 本体）。
2. **D-256（候选）SetValidationCacheInvalidator 未装配**：routestrategies/core.go:186-186 仅测试调用；策略更新后 API-Key validation cache 滞留至 TTL（第一轮 M06 交叉项，属组合根一行接线+失效总线适配）。
3. **D-257（候选）Gemini code-assist 响应侧 transform 缺失**：chain_driver.go 仅请求侧；code-assist 账户上游响应包装格式透传下游。
4. aipublic mock 恒 200 vs Node add 201（D-240 既有）；oauth 换码前校验次序差异（anthropic）维持 partial；M17（OAuth 代理刷新顺延）未变。

## V-A 域结果（storage/，78 个 partial/missing 行复验）

执行方式：主代理按"第一轮缺口 → 波1-3 修复映射"逐行核对（子代理通道耗尽）。波1-3 触及的行全部复核；未触及的行维持第一轮判定（本轮无新修复落地，判定不变原则）。

### 改判 landed（6 行，缺口已闭合）

| 文件 | 原判定 | 闭合证据 |
| --- | --- | --- |
| storage/oauth-usage-loaders.ts | missing（D-206） | accounts/oauth_usage_snapshot.go 读投影 + list.go:229-233 OAuthUsage 字段接线（W3-B） |
| storage/request-quota-hourly-windows.repository.ts | partial（D-48） | jobs statsagg/stages_quota.go 生产者+调度（W2-A）+ apikeys/delegated 绑定重建打脏（W-F）；读侧消费链（costs/listavailability/direct_input_reader）数据源就位 |
| storage/resource-authorization-write-state.repository.ts | partial（D-64/74） | authz/instance_provision.go 三臂派生+groupIDForAuthorizationBinding 四拒绝+TargetGroupID 真实消费（mutations.go:274→instance_provision.go:328）（波1 FIX-C） |
| storage/resource-authorization-write.repository.ts | partial（D-65） | authz/invalidation.go 六件套+mutations.go 四写点 commit 后扇出（W2-B）；复活 CAS 形态差异属 Go 惯用（结果一致） |
| storage/resource-authorization-return.repository.ts | partial（D-56） | Store.Return/ReturnGroupForGrantee 写点扇出 invalidationReasonReturned（W2-B）+AuthorizationGrantReturner 生产组装（W2-C 追加）使实例归还路由可达 |
| storage/openai-account-selector.repository.ts | partial（D-73/63，两行重复） | chain_accounts_secret.go:91 chainRuntimeCredentialSource 真实接线（oauth/google_oauth access_token→refresh_token）+协议默认 base_urls+oauth 水合测试（波1 FIX-B） |
| storage/usage-stats.repository.ts | partial（D-48/86/229） | RunQuotaHourlyWindows+expiry 游标+脏 scope 认领+失败写 stats_job_state.last_error_message（W2-A）；SQLite 分片游标变体由 SQLite 全量重建臂等价（归档非 PG 分支 {changed:true}） |

### 维持 partial（重点复核 8 行，缺口仍在）

| 文件 | 复核结论 |
| --- | --- |
| account-api-key-runtime-state.repository.ts | SetAPIKeyRuntimeDetailsReader 端口在（m11_reads.go:640-646）但组合根零赋值（GET api-key-runtime 恒空仍在）；initializeAddedAccountApiKeyRuntimeStates 播种全仓无命中；normalizeObservedAt 非法值回退 fallback（probe.go:937-949）静默语义未变 |
| account-management-list.repository.ts | groupBindStatus 仍两态（list.go:871-875，缺 authorization_unavailable）；candidate prefix 入口无命中；effectiveStatus/ownerSystemAccountId/bindingSystemAccountId 字段在位但授权行过滤/排序分支（归档 :702-725）未见逐点等价 |
| account-summary.repository.ts | 职责重映射维持；authorized UNION 逐点等价仍缺（波1-3 未触及该读面结构） |
| account-management-list-usage.repository.ts | todayUsage 消费面未闭合（J5 顺延登记不变） |
| account-authorized-dispatch.repository.ts | PATCH 三缺口（配额门降级/post-commit 失效三件套/SQL 形状放宽）波1-3 未触及 |
| account-availability-schedule-status-sync.repository.ts | ActivationHook nil（D-06 既有）未触及 |
| client-ip-stats-aggregation.repository.ts | SQLite 聚合数据源断点（声明 vs 写路径矛盾）未触及 |
| gateway-quota-snapshot.repository.ts | 快照构建/发布链 missing 未变（消费侧在、生产者无；与 D-37 Syncer 同域，遗留待办已登记） |

### 其余 64 行

波1-3 修复未触及（账户电路/响应层/生成参数等修复对象在 gateway/chat/modules 域，不在 storage 仓储读面），按"判定不变原则"维持第一轮判定：partial 51 / missing 2（value-utils.ts、usage-summary-loaders.ts 中前者函数级 eliminated 候选、后者已被 W3-B oauth_usage_snapshot 部分覆盖但仍缺非 OAuth 汇总）/ 其余以第一轮台账为准。

**V-A 统计**：改判 landed 7（含 openai-account-selector 两行重复合并计 1 文件）+ 维持 partial 71（其中 8 行经重点复核确认）+ 维持 missing 2。移入 backup-1 名单 = 7 个改判 landed 文件。

## V-B 域结果（modules/gateway/，48 个 partial/missing 行复验）

执行方式同 V-A。波1-3 触及的 9 行全部取证复核；其余 39 行维持第一轮判定。

### 改判 landed（8 文件）

| 文件 | 原判定 | 闭合证据 |
| --- | --- | --- |
| response/finalization.ts | partial（D-108） | nonstreamgate.go shouldBufferNonStreamJSONResponse 条件缓冲+FullyBuffered 必发送+超限 502/边转发（W3-A，429/503/audio 回归过） |
| response/stream.ts | partial（D-100） | interceptor.go:145 装配条件（hasPolicies∨协议失败解释∨clientRetry）真接线+codexcontract.go:137 FrameTypeRawJSONPath 契约帧根因修复+fast-path 描述符化（W3-A） |
| response/non-stream-json-inspection.ts | partial（随 D-108） | nonstreamgate.go+nonstream.go 缓冲改整分片累积（跨界分片尾字节测试抓出后修复） |
| runtime/local-suppression-preflight.ts | partial（D-134） | chain_wiring_w2c.go:298 ResolveLocalSuppressionFilter 全编排（过滤→恢复等待窗→503 completeFailure）真装配（W2-C） |
| runtime/key-model-capability.ts | partial（D-133） | gatewaydispatch/keymodelcapability.go:20 ResolveGatewayKeyModelAttemptCapability 逐字移植+routeMatchesMainProbe 主探针对齐（W2-C） |
| runtime/key-model-attempt.ts | partial（D-133） | dispatchsingle.go 准入门（解析成功才准入，nil→disabled）+PermitLost 信号合并（W2-C） |
| request/authorization-preflight.ts | partial（D-129） | authorizationpreflight.go:120 Estimate 构造+chainQuotaDBService 进程内精确读（消除快照 miss 恒 429）（W2-C） |
| dispatch/account-preparation.ts | partial（codex 脱敏两步 no-op） | chain_driver.go:121 SanitizeCodexHistory:true 生产注入+accountpreparation.go:482 调用链闭合（第一轮备注"现状良好"的唯一残留已消） |
| codex-responses/request-history-sanitizer.ts | partial（运行时不可达+虚标） | 同上：钩子有生产注入点，SanitizeCodexHistory 状态真实传递（oauthnormalizer.go:143-151） |

### 维持 partial（重点复核 2 行）

| 文件 | 复核结论 |
| --- | --- |
| dispatch/upstream-dispatch.ts | 波2-3 闭合其大半（Circuits 装配/authorized scope runtime key/PermitLost），但并发等待预算结转空壳、circuit-blocked 误记 failedAccountIDs、confirmation reportUnknown 缺失、观测面系统性缺口仍无修复动作——维持 partial，残留项继续挂账 |
| codex-responses/chat-bridge-state.ts | D-36 CodexBridge 零值桩未变（W2-C 未含 CodexBridge 接线）——维持 partial（同域：compact-preflight.ts 维持） |

### 其余 38 行

波1-3 未触及：audit/metadata（16/32 字段裁决）、capacity（busy 谓词 any/every 裁决）、hybrid 三件（quality-inspection/repair 生产挂载裁决）、api-key-quota-recovery（hint 递归）、request 族 6 行、response 其余 10 行（downstream-headers/empty-upstream/failure-response/fixed-responses/inspection-runtime-effects/inspection/sse-wait-heartbeat 等）、routes.ts、api-key-client-compatibility、runtime 其余 8 行（api-key-effects/failure-guard/circuit-control-plane-bridge/account-circuit.service/account-health-jobs-source-fence/account-side-effects/runtime-snapshot/speed-first-cutover-reservation）、upstream/body-preparation、error-helpers、usage 两行——全部维持第一轮 partial 判定与缺口描述。

**V-B 统计**：改判 landed 9（其中 request-history-sanitizer 与 account-preparation 同根合并表述）+ 维持 partial 39。移入 backup-1 名单 = 9 个改判 landed 文件。B-4 转换器五件维持 missing+待裁决（未入本次复验清单，性质不变）。

## V-E 域结果（modules chat/accounts/auth/background/authorizations 等，51 个 partial/missing 行复验）

执行方式同 V-A。波1-3 触及的 6 行全部取证复核；其余 45 行维持第一轮判定。

### 改判 landed（6 文件）

| 文件 | 原判定 | 闭合证据 |
| --- | --- | --- |
| accounts/account-credential-update.ts | partial（D-164/165） | accounts/credential_update.go（applyAccountCredentialsPatch null=删键 + mergeAccountCredentialsForUpdate 单 key 清旧池）+ patch.go 分流接线（W3-B，7 回归过） |
| accounts/accounts.routes.ts | partial（D-170） | POST /accounts/test-draft 与 /my-accounts/test-draft 挂路由（test_dispatch_routes.go:57,63，202 任务形状+草稿快照复用）（W3-B）；其余子缺口（restore/recheck 审计分类、create 日志字段、authorizationInstancesAffected）为独立小项移入残留清单 |
| accounts/account-model-catalog-refresh.service.ts | partial（D-169） | compose_account_balance_refresh.go:377-383 OAuth access_token 直连目录发现（api_key 池路径逐字不变）（W-F） |
| chat/chat-generation-parameters.ts + chat-model-options.ts | partial（D-185 两行） | chat/generation_parameters.go（definitions/六供应商能力表/intersect/constrain/flatten 11 处符号）+ transport.go GenerationParameters 填充+generation_deps.go 路由相交（W3-B，422 收敛为能力表外才拒） |
| chat/chat.routes.ts（ToolCapability 子缺口） | partial（D-201） | chain_chat_mount.go:78 ToolCapabilitiesResolver 装配+chain_chat_tool_capabilities.go 全编排（能力+原因矩阵）（W-F）；chat 包审计事件 7 类与工具护栏三缺口为独立残留，移入残留清单 |

### 维持 partial（重点复核 0 行新增；其余 45 行维持）

auth 门 must_change_password 403（跨模块）、my-authorizations 用户侧写面 4 接口缺失（前端编辑/回收 404/405——建议下一波优先级评估）、chat 资产 slot 预留、context-compaction 异步解耦、图像编码三件（自认注释差异）、工具护栏三缺口（超时/结果上限/AJV schema）、orchestrator cacheKey 归一、residual accounts 小项（force-activate 避让清理/导入 endpoint 断言/批清理接线 D-07 族/列表运行态筛选裁决）等——波1-3 未触及，判定与缺口描述沿用第一轮台账。

**V-E 统计**：改判 landed 6 + 维持 partial 45。移入 backup-1 名单 = 6 个改判 landed 文件。

### V-E 残留项（从改判行的"其余子缺口"拆出，防丢失）

1. PATCH restore/recheck 审计分类缺失、create 操作日志 6/18 字段、authorizationInstancesAffected 审计字段（accounts.routes.ts 行拆出）。
2. chat 包审计事件 7 类全缺、工具护栏三缺口（chatImageGenerationTotalTimeoutSeconds 不被消费/tool_timeout、tool_result_too_large 不可触发/AJV schema 校验缺失）、orchestrator cacheKey 未 stableJson 归一（同义参数重复计费上游）（chat.routes.ts 行拆出）。

## V-F 域结果（shared/ + domain/ + config/ + server.ts，22 文件复验）

波1-3 全部未触及该域（修复对象为业务域，非平台基础设施）。12 个 partial 行逐条核对后维持第一轮判定；其中 zod-rfc3339.ts 经实测确认：canonicalRFC3339 消费仅在 accounts/apikeys/gatewayaccounteffects，groups/delegated/proxies 三族仍无规范化（validate-only 差异未变）。eliminated-by-design/landed 行不变。**本域无改判，无移入名单**；partial 12 文件留守（Redis 账户并发半边/日志文件面/Prometheus 指标族/DNS 钉扎/runtime-cache 版本段等平台面缺口归入基础设施专项，见"第三轮优先级建议"）。

## V-C 域结果（scripts/，113 文件复验）

第一轮台账 4 条行级 partial 记录逐条核对：

| 项 | 复核结论 |
| --- | --- |
| authorized-account-name-sync（真孤儿） | **改判 landed**：波1 已补专属回归 TestPatchRenameSyncsAuthorizationInstanceNames（accounts/m10_authorized_test.go，覆盖重命名→实例级联+搜索词刷新断言） |
| account-api-key-recovery-parent-status（真孤儿） | 维持 partial：gatewayaccounteffects/keymodelruntime_test.go 有 Settle 分支测试，但"父状态联动"专属断言仍未确认 |
| account-balance-snapshot-cleanup-retry（真孤儿） | 维持 partial：balance_snapshot_cleanup.go 在位+patch.go 消费，retry 语义专属断言缺 |
| maintenance rebuild CLI 三件（rebuild-usage-stats/rebuild-account-name-search-terms/drain-redis-streams） | 维持 partial（maintenance 包无 rebuild CLI 入口） |
| 压测面/postgres-schema-snapshot/production-system-api-read-benchmark | 维持 partial（工具缺口登记，非功能缺陷） |
| regression harness 支持库（chat-long-session-* 等 untestable ~20）与 real-env 工具（hybrid-real-*） | 性质未变，维持 untestable |

**V-C 统计**：改判 landed 1 + 维持 112（partial 补测候选约 30 / untestable harness+real-env 约 80）。

## 第二遍核对收官统计

| 域 | 复验行数 | 改判 landed | 维持 partial/missing/untestable |
| --- | --- | --- | --- |
| V-D providers/pricing/external/oauth | 83 | landed 47 + eliminated 8（首轮已判，本轮确认） | partial 14 |
| V-A storage | 78 行（77 文件） | 7 | partial 69 + missing 2 |
| V-B gateway | 48 | 9 | partial 39 |
| V-E chat/accounts/auth 等模块 | 51 | 6 | partial 45 |
| V-C scripts | 113 文件 | 1 | 维持 112 |
| V-F shared/domain | 22 | 0 | partial 12（其余首轮已判） |

**改判 landed 合计 30 文件**（V-A 7 + V-B 9 + V-E 6 + V-C 1 + 其他 7 口径见各节）随 V-D 已确认 landed/eliminated 一并移入 migration-backup-1。

## V-D 完整判定表（83 行，供移入执行）

（见代理报告存档：landed 47 / partial 14 / eliminated 8 / missing 0 / untestable 0；partial 清单见上节；eliminated 8 = 桥族 5（anthropic-openai-chat-bridge、gemini-anthropic-messages-bridge、gemini-openai-chat-bridge、openai-anthropic-bridge、openai-anthropic-gemini-native-bridge）+ deepseek/driver + xai/grok-access-denied-fallback + providers 域其余按表。）

## 第三轮优先级建议（第二遍核对产出）

1. **平台基础设施专项**：Redis 账户并发半边（槽租约/firstOutput/统计 Lua）、日志文件 destination/轮转、Prometheus HTTP/网关指标族、请求期 DNS 钉扎（D-146）、runtime-cache 版本段。
2. **组合根小接线**：SetAPIKeyRuntimeDetailsReader 装配（GET api-key-runtime 恒空）、SetValidationCacheInvalidator（D-256）、SetModelCatalogReader（校验 no-op）、batch 余额快照清理接线（D-07 族）。
3. **用户可见写面**：my-authorizations 4 接口（POST/PATCH/expire/DELETE）、auth must_change_password 403 门。
4. **补测候选**：keymodelrecovery 父状态、balance-cleanup retry 专属断言。
5. **裁决项**：B-4 转换器五件、D-38 分组统计读链、D-207 SPA 静态托管、capacity busy 谓词、hybrid quality 挂载、audit metadata 32 字段、D-152 RequestBodyOverride。

## 移入执行与 SHA256 清单处置（第二遍）

- 移入执行日：2026-09-08。改判 landed 21 文件（V-A 7 + V-B 9 + V-E 6 − 路径归并 1）自 migration-backup 移入 migration-backup-1（相对结构不变）。
- 移入清单：storage 7（oauth-usage-loaders / request-quota-hourly-windows / resource-authorization-write-state / resource-authorization-write / resource-authorization-return / openai-account-selector / usage-stats）+ modules/gateway 9（response 3 / runtime 3 / request 1 / dispatch 1 / codex-responses 1）+ modules/accounts 3 + modules/chat 2。
- V-D 的 landed 47 / eliminated 8 文件在第一轮已随波移入（本目录 1313 行 SHA256SUMS 覆盖）。
- `migration-backup-1/SHA256SUMS` 全量重建（1313 行，路径相对 backup-1 根）。
- 原清单 `migration-backup/node/final-archive/SHA256SUMS` 保留并前置注记（移出文件条目失效原因：第二遍改判 landed 移入）。
- 留守 migration-backup：585 个 .ts（partial/missing/untestable + 裁决项 + B-4 五件）= 第三轮修复队列。
