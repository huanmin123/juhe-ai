# W4 团队与统一授权迁移记录

> 本文记录 W4 “团队与统一授权”迁移的当前切片。W4 涉及系统团队、团队成员、授权 grant、授权来源展开、归还、到期和统计联动；必须按小切片推进，不能因为团队读 / 创建 / 成员维护进入 Go opt-in 就声明统一授权接管。

## 基本信息

- 模块：团队与统一授权
- 状态：Go 实现中
- 迁移波次：W4 团队与统一授权
- 当前 Node owner：`backend/src/modules/system-teams/`、`backend/src/modules/authorizations/`、`backend/src/storage/system-team.repository.ts`、`backend/src/storage/resource-authorization-*.ts`
- 当前 Go owner：`backend-go/internal/modules/managementsystemteams/`、`backend-go/internal/modules/managementauthorizations/`、`backend-go/internal/modules/gatewayquotasnapshot/`、`backend-go/internal/httpapi/management_system_teams.go`、`backend-go/internal/httpapi/management_authorizations.go`、`backend-go/internal/store/postgres/managementsystemteams.go`、`backend-go/internal/store/postgres/managementauthorizations.go`、`backend-go/internal/store/postgres/gatewayquota.go`、`backend-go/internal/store/postgres/queries/w4_management_system_teams.sql`、`backend-go/db/migrations/000018_w4_resource_authorization_sources_and_grants.sql`、`backend-go/db/migrations/000019_w4_authorization_quota_and_stats_state.sql`、`backend-go/db/migrations/000020_w4_authorization_usage_windows.sql`、`backend-go/internal/modules/gatewaycache/system_account_invalidator.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/app/server.go`、`backend-go/internal/app/authorization_expiry_sweep_worker.go`、`backend-go/internal/app/authorization_usage_range_window_refresh_worker.go`、`backend-go/internal/app/gateway_quota_snapshot_worker.go`、`backend-go/cmd/juhe-ai-worker/main.go`
- 关联功能文档：`../functions/系统团队与统一授权设计.md`、`../functions/接口契约与权限矩阵.md`
- 关联计划：`../plans/计划-0081-Node转Go渐进减法迁移.md`

## 当前已迁移切片

| 切片 | 路径 | 状态 | 边界 |
| --- | --- | --- | --- |
| 系统团队列表 / 详情 | `GET /__aisys__/api/system-teams`、`GET /__aisys__/api/system-teams/{id}`、`GET /__aisys__/api/my-teams`、`GET /__aisys__/api/my-teams/{id}` | Go opt-in 已实现，未生产接管 | 仅迁团队摘要读和当前 active 成员详情；管理侧 admin-only，个人侧 self-only；支持团队名前缀搜索、分页、`systemAccountId` 作用域；不展开授权来源 / grant，不返回密码 / token，不 touch session，不删除 Node |
| 系统团队创建 | `POST /__aisys__/api/system-teams` | Go opt-in 已实现，未生产接管 | 仅迁团队创建写接口；要求管理员角色，严格 JSON 字段集，写 `system_teams` 摘要字段并 best-effort 入队 operation log；不展开授权来源，不重算最终用户授权，不删除 Node |
| 系统团队更新 | `PATCH /__aisys__/api/system-teams/{id}` | Go opt-in 已实现，未生产接管 | 迁移团队名称 / 说明 / 状态 partial update；状态从非 disabled 到 disabled 时 revoke 当前团队 active 授权来源，状态从 disabled 到 active 时按当前 active 团队 grant 重新展开到 active 成员；发布 `team_authorization_changed` 授权缓存失效并在统计脏表存在时写 `__all__` 脏标记；不执行批量到期扫描生产调度，不删除 Node |
| 系统团队成员维护 | `POST /__aisys__/api/system-teams/{id}/members`、`DELETE /__aisys__/api/system-teams/{id}/members/{memberId}` | Go opt-in 已实现，未生产接管 | 新增成员要求 active 团队和 active 系统账户，单次最多 20 个、团队 active 成员最多 20 个；新增 / 重新加入成员时按当前 active 团队 grant 展开最终用户授权；移除成员时 revoke 该成员当前 active team source 并刷新 effective source；写 `team_members_changed` stats dirty 和授权缓存失效；不执行批量到期扫描生产调度，不删除 Node |
| 授权来源 / grant schema | `resource_authorization_sources`、`resource_authorization_grants` | Go migration 已补，团队状态、成员维护、授权创建、授权更新、授权归还、授权回收和批量到期扫描副作用已写入来源 | 补 PostgreSQL 当前 schema 基线、CHECK / FK / 索引和 sqlc models；当前由团队状态、成员维护、授权创建、普通授权更新、授权归还、授权回收和批量到期扫描写授权来源 / grant；生产周期 runner / 部署调度仍未完成 |
| 授权额度 / 统计状态 schema | `request_quota_hourly_window_configs`、`group_account_stats_dirty` | Go migration 已补，已被团队 PATCH 状态副作用按表存在写入 | 补 Node 当前默认额度小时窗口和统计脏标记表；团队状态影响授权时按全量脏标记 `__all__` 写入；不代表 usage stats worker 或生产 gateway quota snapshot 消费已迁移 |
| 授权缓存失效前置 | `gateway_runtime_cache`、`authorization_quota_cache`、`api_key_quota_cache` runtime state topic | Go gatewaycache 已补，团队 PATCH、成员维护、授权创建、授权更新、授权有效期更新、授权归还、授权回收和批量到期扫描已接入 | 补 Node 兼容 Redis runtime state 发布能力：授权业务变化可同时发布 gateway runtime 与 authorization quota topic，API Key 额度可携带 `apiKeyId`；团队 PATCH、成员维护、授权创建、授权更新、授权有效期更新、授权归还、授权回收和批量到期扫描副作用已调用 |
| 授权批量到期扫描 | `juhe-ai-worker authorization-expiry-sweep`，无 HTTP 路径 | Go worker 入口已实现，未生产接管 | 新增 `managementauthorizations.Service.ExpireDue`、`postgres.Store.ExpireDueManagementResourceAuthorizations` 和 worker 子命令；默认批量 20，worker 默认 1 分钟间隔、54 秒初始延迟；按 `resource_authorization_grants.expires_at ASC, updated_at ASC, id ASC` 固定窗口选择 active / paused 到期 grant，使用 `FOR UPDATE SKIP LOCKED`，标记 grant `expired`，复用 runtime 同步 helper 刷新个人 / 团队授权，写 `authorization_expired` stats dirty 并发布授权缓存失效；不读取 `usage_records`，不实时聚合；真实 PG/Redis smoke、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费和 Node 删除仍未完成 |
| 授权用量 range window 刷新 | `juhe-ai-worker authorization-usage-range-windows-refresh`，无 HTTP 路径 | Go worker 入口已实现，未生产接管 | 新增 `ManagementUsageStatsTimezoneReader`、`ManagementAuthorizationUsageRangeWindowRefresher`、`managementauthorizations.Service.RefreshUsageRangeWindows`、`postgres.Store.RefreshManagementAuthorizationUsageRangeWindows` 和 worker 子命令；默认每 6 小时刷新，43 分钟初始延迟，支持 `--run-once` 与 `--timezone` 本地 smoke；按 `usageStatsTimezone` 计算当天、昨日、近 7 天、近 31 天和当月 hot ranges，每个 range 单独事务，先删当前 range 再从 `authorization_team_usage_summary_daily` / `authorization_user_usage_summary_daily` 汇总写入 range window；不读取 `usage_records`，不在请求路径聚合；真实 PG smoke、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费和 Node 删除仍未完成 |
| 网关配额快照构建 / 发布 | `juhe-ai-worker gateway-quota-snapshot-build`，无 HTTP 路径 | Go worker 入口已实现，未生产接管 | 新增 `gatewayquotasnapshot.Service`、`GatewayQuotaSnapshotReader` port、`RuntimeStatePublisher`、`postgres.Store` 聚合表读取和 worker 子命令；默认每 1 分钟构建一次，37 秒初始延迟，支持 `--run-once`、`--timezone`、`--publish-runtime-state` 与 `--snapshot-ttl` 本地 smoke；按 Node 当前 API Key / direct authorization / team authorization scope 构建成本快照和决策项，只读取 `api_keys`、`resource_authorizations`、`resource_authorization_grants`、`accounts`、系统设置、`usage_stats_*` 预聚合表与 `usage_quota_hourly_windows`，不读取 `usage_records`，不实时 SUM/GROUP BY；开启发布时写入 Node 兼容 `gateway_quota_snapshot/current` Redis runtime state，Node gateway Redis 模式优先读取共享快照，缺失 / 不完整时仍回 DB service 精确补判；真实 PG/Redis smoke、生产部署 / supervisor 接管和 Node 删除仍未完成 |
| 授权列表 | `GET /__aisys__/api/authorizations`、`GET /__aisys__/api/my-authorizations` | Go opt-in 已实现，未生产接管 | 以 `resource_authorization_grants` 为分页主行；管理侧 admin-only，个人侧 self-only；支持 Node 当前查询参数、作用域过滤、`sourceSummary` 轻量响应和 grant ID 列表契约；列表只读当前 grant 状态，不触发到期扫描，不返回 usage / limits / authorizationSources 详情，不删除 Node |
| 授权详情 | `GET /__aisys__/api/authorizations/{id}`、`GET /__aisys__/api/my-authorizations/{id}` | Go opt-in 已实现，未生产接管 | 路径 `{id}` 按 `resource_authorization_grants.id` grant ID 解释；管理侧 admin-only，个人侧 self-only；读取当前 grant 摘要、limits、source 明细和基础 usage 空对象；非资源归属人脱敏 source team ID、createdBy / revokedBy 等内部字段；不触发到期扫描，详情本身不做 usage 聚合 |
| 授权用量 overview | `GET /__aisys__/api/authorizations/usage/team-details`、`GET /__aisys__/api/my-authorizations/usage/team-details`、`GET /__aisys__/api/authorizations/usage/user-details` 和 `GET /__aisys__/api/my-authorizations/usage/user-details` | Go opt-in 已实现，未生产接管 | 只读 `juhe_stats.authorization_team_usage_range_windows` / `authorization_user_usage_range_windows` 预聚合窗口；管理侧 admin-only，个人侧 self-only；支持资源、团队、用户、日期范围和分页过滤；不扫描 `usage_records`，不实时 SUM/GROUP BY；range window 刷新已有 Go worker 入口但未做真实 PG smoke / 生产部署接管；不接管生产 gateway quota snapshot 消费或 Node 删除 |
| 授权用量明细 | `GET /__aisys__/api/authorizations/{id}/usage`、`GET /__aisys__/api/my-authorizations/{id}/usage` | Go opt-in 已实现，未生产接管 | 路径 `{id}` 按 grant ID 解释；管理侧 admin-only，个人侧 self-only；复用授权详情可见性与脱敏规则；返回授权总 usage、`usageBySystemAccount*` 分页和 `usageRange`；只读 `authorization_user_usage_range_windows` / `authorization_team_usage_range_windows`，不扫描 `usage_records`、不实时 SUM/GROUP BY、不触发到期扫描 |
| 授权创建 | `POST /__aisys__/api/authorizations`、`POST /__aisys__/api/my-authorizations` | Go opt-in 已实现，未生产接管 | 管理侧 admin-only 且必须指定授权人，个人侧 self-only；支持账户 / 分组资源授权给系统账户或团队；个人账号授权必须指定目标分组；写 resource authorization grant、manual / team source、账户授权实例和分组绑定；写 `resource_authorization_created` stats dirty 与授权缓存失效；不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，不删除 Node |
| 授权更新 | `PATCH /__aisys__/api/authorizations/{id}`、`PATCH /__aisys__/api/my-authorizations/{id}` | Go opt-in 已实现，未生产接管 | 路径 `{id}` 按授权业务 grant ID 解释；管理侧 admin-only，可用 `systemAccountId` 收窄资源归属作用域；个人侧 self-only 且忽略 query `systemAccountId`；支持普通 PATCH 的 `status=active/paused`、`expiresAt` 和 `limits` 更新，刷新 manual / team runtime source，写 `resource_authorization_updated` stats dirty 与授权缓存失效；不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，不删除 Node |
| 授权有效期更新 | `PATCH /__aisys__/api/authorizations/{id}/expire`、`PATCH /__aisys__/api/my-authorizations/{id}/expire` | Go opt-in 已实现，未生产接管 | 路径 `{id}` 按授权业务 grant ID 解释；管理侧 admin-only，可用 `systemAccountId` 收窄资源归属作用域；个人侧 self-only 且忽略 query `systemAccountId`；`expiresAt` 必填且可为 `null`，`limits` 可选且可为 `null`，不接受 `status`，复用授权更新链路刷新 runtime、stats dirty 与授权缓存失效；单条有效期更新不执行后台到期扫描，不接管生产 gateway quota snapshot 消费，不删除 Node |
| 授权归还 | `DELETE /__aisys__/api/authorizations/{id}/return`、`DELETE /__aisys__/api/my-authorizations/{id}/return` | Go opt-in 已实现，未生产接管；Node 生产问题已同步修正为 grant ID 契约 | 路径 `{id}` 按授权业务 grant ID 解释，不按 runtime authorization ID；管理侧 admin-only，个人侧 self-only；只允许被授权系统账户归还 direct manual grant；撤销 manual source，刷新 runtime authorization 为 `returned`，写 `resource_authorization_returned` stats dirty 与授权缓存失效；不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，不删除 Node |
| 资源页授权归还 | `POST /__aisys__/api/accounts/{id}/return-authorization`、`POST /__aisys__/api/my-accounts/{id}/return-authorization`、`POST /__aisys__/api/groups/{id}/return-authorization`、`POST /__aisys__/api/my-groups/{id}/return-authorization` | Go opt-in 已实现，未生产接管 | 路径 `{id}` 按授权账户实例 ID 或授权分组资源 ID 解释，用于对齐 Node 账号页 / 分组页归还入口；内部解析 runtime authorization 后只归还 direct manual grant，复用授权归还的 manual source 撤销、runtime refresh、stats dirty 与授权缓存失效；404 文案按账户 / 分组区分；不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，不删除 Node |
| 资源页归还按钮权限 | `accounts/options` / `my-accounts/options`、`groups/options` / `my-groups/options`、`groups/account-options` / `my-groups/account-options` 的 `permissions.canReturnAuthorization` | Go opt-in 已补，未生产接管 | 只在授权 runtime 存在 active manual source 时标记为 `true`；owner 资源、纯团队来源授权、已归还 / 已回收 / 已过期 source 均为 `false`；用于让已迁资源页归还入口在 Go opt-in 下可见，不代表账户 / 分组完整列表、详情或本地调度写接口已迁移 |
| 授权回收 | `DELETE /__aisys__/api/authorizations/{id}`、`DELETE /__aisys__/api/my-authorizations/{id}` | Go opt-in 已实现，未生产接管 | 路径 `{id}` 按授权业务 grant ID 解释；管理侧 admin-only，可用 `systemAccountId` 收窄资源归属作用域；个人侧 self-only 且忽略 query `systemAccountId`；标记 grant `revoked`，撤销 manual source 或当前资源 team source，刷新 runtime authorization，写 `resource_authorization_revoked` stats dirty 与授权缓存失效；不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，不删除 Node |
| 前端本地调用 / UI 边界 smoke | `pnpm --filter juhe-ai-frontend test:w4-team-authorization-smoke` | 已补本地 mock HTTP smoke，未连接真实 Go 后端 | 固定前端 `systemTeams` / `myTeams` / `authorizations` / `myAuthorizations` API 路径、团队创建 / 更新 / 成员维护入口、授权列表 / 详情 / 创建 / 状态更新 / 有效期更新 / 归还 / 回收调用、管理侧 `systemAccountId` 作用域和个人侧 `my-*` 去除管理作用域边界；不代表浏览器连接真实 Go 后端、Cookie / HTTPS 或生产切流已完成 |

## 当前契约

### `GET /__aisys__/api/system-teams` / `GET /__aisys__/api/my-teams`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `GET /__aisys__/api/system-teams`、`GET /__aisys__/api/system-teams/{id}`；个人侧 `GET /__aisys__/api/my-teams`、`GET /__aisys__/api/my-teams/{id}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端读鉴权 `ManagementAPIAuthMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active；读接口不 touch session |
| 权限 | `system-teams` 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；`my-teams` 强制当前登录系统账户作用域，普通用户可读 |
| 查询参数 | `keyword` 只按团队名称精确 / 前缀过滤，不扫描说明；`page/pageSize` 归一化并返回窗口上界 `total`；管理侧 `systemAccountId` 非空时按该用户 active 成员关系过滤，`all` / 缺省表示全局团队；个人侧忽略 query `systemAccountId` |
| 列表响应 | `200 { data: { items, total, hasMore, page, pageSize } }`；列表项包含 `id/name/description/status/memberCount/activeMemberCount/createdBy/createdAt/updatedAt`，成员数按当前 active 成员计数 |
| 详情响应 | `200 { data: { id, name, description, status, memberCount, activeMemberCount, members, createdBy, createdAt, updatedAt } }`；`members` 只包含当前 active 成员的 `id/teamId/systemAccountId/systemAccountName/username/memberRole/status/joinedAt/createdAt/updatedAt` |
| PG 读取 | 只读取 `system_teams`、`system_team_members`、`system_accounts` 轻量字段；列表先读分页团队，再按当前页 team IDs 批量统计 active 成员数，不展开成员；详情只拉目标团队最多 500 个 active 成员 |
| 切片边界 | 这是 W4 团队读接口的 Go opt-in 灰度能力；不迁成员增删、授权来源展开 / grant、授权用量明细、浏览器连接真实 Go 后端的前端团队页 smoke、生产切流或 Node 删除 |

### `POST /__aisys__/api/system-teams`

| 项 | 内容 |
| --- | --- |
| 路径 | `POST /__aisys__/api/system-teams` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 允许 `super_admin` / `admin`；普通 `user` 返回 `403 { message: "需要管理员权限" }` |
| 请求体 | 严格 JSON object，只允许 `name`、`description`、`status`；未知字段、缺少团队名称、非字符串说明、非法状态和尾随 JSON 返回中文 400 |
| 字段规则 | `name` trim 后必填且最长 100 字符，全局唯一；`description` trim 后最长 200 字符，空白 / `null` 表示无说明；`status` 只允许 `active` / `disabled`，默认 `active` |
| PG 写入 | 只写 `juhe_business.system_teams` 的 `id/name/description/status/created_by/created_at/updated_at`；不读取 / 聚合 `system_team_members`、`resource_authorizations` 或 `authorization_sources` |
| 成功响应 | `201 { data: { id, name, description, status, memberCount, activeMemberCount, createdBy, createdAt, updatedAt } }`；新建团队的 `memberCount` / `activeMemberCount` 固定为 0 |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=system_teams.create`、`module=system_teams`、`action=create`、`mode=admin`、`resourceType=system_team`、`viewer=team_creator`；记录 `name/description/status`，不记录成员或授权来源 |
| 切片边界 | 这是 W4 团队创建的 Go opt-in 灰度能力；真实 PostgreSQL + Redis queue / operation-log worker integration、浏览器连接真实 Go 后端的前端团队页 smoke、生产单 owner 切流和 Node `/system-teams` 删除仍未完成 |

### `PATCH /__aisys__/api/system-teams/{id}`

| 项 | 内容 |
| --- | --- |
| 路径 | `PATCH /__aisys__/api/system-teams/{id}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 允许 `super_admin` / `admin`；普通 `user` 返回 `403 { message: "需要管理员权限" }` |
| 查询参数 | 管理侧 `systemAccountId` 非空时按该用户 active 成员关系限制可更新团队，`all` / 缺省表示全局团队；空字符串返回中文 400 |
| 请求体 | 严格 JSON object，只允许 `name`、`description`、`status`；允许空 object 仅刷新 `updated_at`；未知字段、空团队名称、非字符串说明、非法状态和尾随 JSON 返回中文 400 |
| 字段规则 | `name` trim 后非空且最长 100 字符，全局唯一；`description` trim 后最长 200 字符，空白 / `null` 表示清空；`status` 只允许 `active` / `disabled` |
| PG 写入 | 在事务中 `FOR UPDATE` 锁定 `system_teams` 目标行，更新团队字段；仅状态发生 `非 disabled -> disabled` 或 `disabled -> active` 时处理授权来源副作用 |
| 禁用副作用 | 禁用团队时查询当前团队 active team source，最多展开 `20 * 20` 条；逐条标记 source `revoked`、`ended_reason=team_disabled`，并按 Node 规则刷新 `resource_authorizations` 的 effective source / status |
| 启用副作用 | 启用团队时查询最多 20 个 active 成员和最多 20 条 active team grant；跳过资源 owner 自己，按团队 source upsert 最终用户 runtime authorization，覆盖 manual source 为 `superseded`，并刷新 effective source / status |
| 统计 / 缓存 | 授权变化时，如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='team_authorization_changed'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `200 { data: { id, name, description, status, memberCount, activeMemberCount, members, createdBy, createdAt, updatedAt } }`；响应包含更新后的 active 成员详情 |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=system_teams.update`、`module=system_teams`、`action=update`、`mode=admin`、`resourceType=system_team`；只记录实际变化的 `name/description/status`，metadata 写 `authorizationChanged` |
| 切片边界 | 这是 W4 团队更新的 Go opt-in 灰度能力；批量到期扫描生产周期 runner / 部署调度、生产 gateway quota snapshot 消费、真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端团队页 smoke、生产切流和 Node 删除仍未完成 |

### `POST /__aisys__/api/system-teams/{id}/members` / `DELETE /__aisys__/api/system-teams/{id}/members/{memberId}`

| 项 | 内容 |
| --- | --- |
| 路径 | 新增成员：`POST /__aisys__/api/system-teams/{id}/members`；移除成员：`DELETE /__aisys__/api/system-teams/{id}/members/{memberId}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 允许 `super_admin` / `admin`；普通 `user` 返回 `403 { message: "需要管理员权限" }` |
| 查询参数 | 管理侧 `systemAccountId` 非空时按该用户 active 成员关系限制可维护团队，`all` / 缺省表示全局团队；空字符串返回中文 400 |
| 新增请求体 | 严格 JSON object，只允许 `systemAccountIds`；必须为 1 到 20 个非空字符串，trim 后不能重复；未知字段、尾随 JSON、空数组、非字符串或空白成员 ID 返回中文 400 |
| 新增 PG 写入 | 在事务中锁定 active `system_teams` 目标行；目标系统账户必须存在且 active；已有 removed 成员重新置为 active 并刷新 `joined_at`，不存在则插入 `system_team_members`；已有 active 成员保持幂等；团队 active 成员总数最多 20 个 |
| 新增授权副作用 | 新增或重新加入成员时读取最多 20 条 active team grant，跳过资源 owner 自己，按团队来源 upsert 最终用户 runtime authorization，并刷新 effective source / status |
| 移除 PG 写入 | 按 team ID、member ID 和可选 `systemAccountId` 作用域锁定 active member；更新 member `status=removed`、`removed_at` 和 `updated_at` |
| 移除授权副作用 | 查询被移除成员当前 active team source，最多 20 条；逐条标记 source `revoked`、`ended_reason=member_removed`，并刷新对应 `resource_authorizations` effective source / status |
| 统计 / 缓存 | 成员新增 / 移除后，如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='team_members_changed'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `200 { data: { id, name, description, status, memberCount, activeMemberCount, members, createdBy, createdAt, updatedAt } }`；响应包含更新后的 active 成员详情 |
| 操作日志 | 新增成员 best-effort 入队 `system_teams.add_members`，移除成员 best-effort 入队 `system_teams.remove_member`；记录 affected 成员 target / viewer，成员名称优先用 display name，其次 username / system account ID |
| 切片边界 | 这是 W4 团队成员维护的 Go opt-in 灰度能力；批量到期扫描生产周期 runner / 部署调度、生产 gateway quota snapshot 消费、真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端团队页 smoke、生产切流和 Node 删除仍未完成 |

### `POST /__aisys__/api/authorizations` / `POST /__aisys__/api/my-authorizations`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `POST /__aisys__/api/authorizations`；个人侧 `POST /__aisys__/api/my-authorizations` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；管理侧必须通过 `systemAccountId` 指定资源归属人，缺省返回 `管理员新增授权时必须指定授权人`；个人侧强制当前登录系统账户作用域 |
| 请求体 | 严格 JSON object，只允许 `resourceType`、`resourceId`、`granteeType`、`granteeId`、`targetGroupId`、`remark`、`expiresAt`、`limits`；未知字段、尾随 JSON、空白必填字段、`limits:null` 和非法类型返回中文 400 |
| 字段规则 | `resourceType` 只允许 `account` / `group`；`granteeType` 只允许 `system_account` / `team`；`remark` trim 后最长 200 字符；`expiresAt` 必须是未来 UTC server datetime，账户资源授权不能晚于源账户到期时间 |
| 目标分组 | 只有 `resourceType=account` 且 `granteeType=system_account` 时可以指定 `targetGroupId`，且必须指定；目标分组必须属于被授权用户、启用且 provider 与授权账户一致；未指定时不自动创建默认分组 |
| 额度限制 | `limits` 支持 `hourly/daily/weekly/monthly/total`；每项 `enabled` 必须为 `true`，金额大于 0、最多 6 位小数且不超过安全整数；`hourly.hours` 必须为 1 到 720；空 limits 不写 quota JSON |
| grant 写入 | 在事务中校验资源归属和被授权目标 active 状态；相同资源 / grantee 已有 active grant 时拒绝重复授权；可复用 paused / expired / revoked / returned grant 并重新置 active；写 `resource_authorization_grants` |
| 个人授权副作用 | 写入 / 恢复 `resource_authorizations` manual source；若已有 active team source，manual source 标记为 `superseded`，effective source 仍由 team 决定；账户资源授权会创建或恢复被授权用户本地授权账户实例，并绑定到目标分组 |
| 团队授权副作用 | 团队必须 active，active 成员去除资源 owner 后不能为空；最多展开 20 个 active 成员和 20 条 active team grant；按 team source 写入最终用户授权并刷新 effective source |
| 统计 / 缓存 | 授权创建后如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='resource_authorization_created'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `201 { data: authorization }`；响应为 grant 摘要，包含资源、grantee、状态、limits、来源摘要、创建 / 更新时间和基础 usage 空对象 |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=authorizations.create`、`module=authorizations`、`action=create`、`resourceType=authorization`；记录资源、grantee、目标分组、状态、到期时间和 limits |
| 切片边界 | 这是 W4 授权创建的 Go opt-in 灰度能力；批量到期扫描生产周期 runner / 部署调度、生产 gateway quota snapshot 消费、真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### `PATCH /__aisys__/api/authorizations/{id}` / `PATCH /__aisys__/api/my-authorizations/{id}`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `PATCH /__aisys__/api/authorizations/{id}`；个人侧 `PATCH /__aisys__/api/my-authorizations/{id}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作为资源归属作用域 |
| ID 契约 | 路径 `{id}` 固定为 `resource_authorization_grants.id` 授权业务记录 ID，和列表、详情、归还、回收保持同一 ID 语义 |
| 查询参数 | 管理侧 `systemAccountId=all` / 缺省表示全局可管理范围，具体系统账户 ID 表示只允许修改该资源归属人的授权；个人侧忽略 query `systemAccountId` |
| 请求体 | 严格 JSON object，只允许 `status`、`expiresAt`、`limits`，至少提交一个字段；未知字段、尾随 JSON、空 object、非法类型和非法状态返回中文 400 |
| 字段规则 | `status` 只允许 `active` / `paused`；`expiresAt` 必须是未来 server datetime，`null` 表示清空到期时间；`limits:null` 表示清空额度限制，object 支持 `hourly/daily/weekly/monthly/total`，`hourly.hours` 必须为 1 到 720 |
| grant 写入 | 在事务中按资源归属作用域锁定目标 grant；更新 `status/expires_at/limits_json/revoked_by/revoked_at/updated_at`；已按时间过期的 grant 会写回 `expired`，`expired -> active` 必须同时调整未来 `expiresAt` 或清空到期时间 |
| Runtime 状态 | direct 授权按 manual source 刷新最终用户 runtime；team 授权 active 时按当前 active 团队成员重新同步该 grant 的 team source，paused / expired 时标记当前资源 team source 失效并刷新 runtime |
| 统计 / 缓存 | 授权更新后如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='resource_authorization_updated'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `200 { data: authorization }`；目标不存在、越权或不在作用域内返回 `404 { message: "授权记录不存在" }` |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=authorizations.update`、`module=authorizations`、`action=update`、`resourceType=authorization`；只记录提交后的 `status/expiresAt/limits` |
| 切片边界 | 这是 W4 普通授权 PATCH 的 Go opt-in 灰度能力；不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### `PATCH /__aisys__/api/authorizations/{id}/expire` / `PATCH /__aisys__/api/my-authorizations/{id}/expire`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `PATCH /__aisys__/api/authorizations/{id}/expire`；个人侧 `PATCH /__aisys__/api/my-authorizations/{id}/expire` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作为资源归属作用域 |
| ID 契约 | 路径 `{id}` 固定为 `resource_authorization_grants.id` 授权业务记录 ID，和列表、详情、普通更新、归还、回收保持同一 ID 语义 |
| 查询参数 | 管理侧 `systemAccountId=all` / 缺省表示全局可管理范围，具体系统账户 ID 表示只允许修改该资源归属人的授权；个人侧忽略 query `systemAccountId` |
| 请求体 | 严格 JSON object，只允许 `expiresAt`、`limits`；`expiresAt` 必填且可为 `null`；`limits` 可选且可为 `null`；未知字段、尾随 JSON、缺少 `expiresAt`、非法类型返回中文 400 |
| 字段规则 | `expiresAt` 必须是未来 server datetime，`null` 表示清空到期时间；`limits:null` 表示清空额度限制，object 支持 `hourly/daily/weekly/monthly/total`，`hourly.hours` 必须为 1 到 720；该接口不接受 `status`，因此不会把暂停 / 恢复动作混入有效期配置修改 |
| grant 写入 | 复用授权更新写链路，在事务中按资源归属作用域锁定目标 grant；更新 `expires_at/limits_json/updated_at`，并沿用已按时间过期 grant 的状态刷新规则 |
| Runtime 状态 | direct 授权按 manual source 刷新最终用户 runtime；team 授权按当前 grant 状态刷新团队来源；该接口不会主动设置 `paused` 或 `active`，但清空 / 调整未来到期时间时可按普通更新链路恢复已过期记录 |
| 统计 / 缓存 | 授权有效期更新后如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='resource_authorization_updated'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `200 { data: authorization }`；目标不存在、越权或不在作用域内返回 `404 { message: "授权记录不存在" }` |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=authorizations.update_expire`、`module=authorizations`、`action=update_expire`、`resourceType=authorization`；记录提交后的 `status/expiresAt/limits` |
| 切片边界 | 这是 W4 授权有效期 / 额度配置更新的 Go opt-in 灰度能力；它不是批量到期扫描任务，不执行批量到期扫描生产调度，不接管生产 gateway quota snapshot 消费，真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### `DELETE /__aisys__/api/authorizations/{id}/return` / `DELETE /__aisys__/api/my-authorizations/{id}/return`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `DELETE /__aisys__/api/authorizations/{id}/return`；个人侧 `DELETE /__aisys__/api/my-authorizations/{id}/return` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作用域 |
| ID 契约 | 路径 `{id}` 固定为 `resource_authorization_grants.id` 授权业务记录 ID；这是授权列表操作按钮和前端路由当前使用的 ID。Node 生产修复已把 `returnResourceAuthorizationForGrantee` 从按 `runtime_authorization.id` 查找改为按 `grant_row.id` 查找 |
| 查询参数 | 管理侧 `systemAccountId` 非空时作为被授权系统账户作用域，`all` / 缺省回落到当前登录管理员系统账户；个人侧忽略 query `systemAccountId` |
| 归还范围 | 只允许 `grantee_type=system_account` 的 direct grant 由被授权系统账户归还；资源 owner 自己、团队授权、已回收 / 已归还 grant、没有 active manual source 的 runtime authorization 均返回 404 |
| PG 写入 | 在事务中锁定目标 grant，标记 grant `status=returned`、写 `revoked_by/revoked_at`；撤销该 runtime authorization 的 manual source，`ended_reason=grantee_returned`，再刷新 effective source |
| Runtime 状态 | 若归还后没有 active source，`resource_authorizations.status` 写为 `returned`，`revoked_reason=grantee_returned`；归还路径不保留已过期状态覆盖 |
| 统计 / 缓存 | 授权归还后如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='resource_authorization_returned'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `204` 无响应体；重复归还或不可归还目标返回 `404 { message: "授权记录不存在" }` |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=authorizations.return`、`module=authorizations`、`action=return`、`resourceType=authorization`；记录被归还 grant、资源、grantee 和 targeted viewer |
| 切片边界 | 这是 W4 授权归还的 Go opt-in 灰度能力和 Node 生产 ID 契约修复；批量到期扫描生产周期 runner / 部署调度、生产 gateway quota snapshot 消费、真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### `POST /__aisys__/api/accounts/{id}/return-authorization` / `POST /__aisys__/api/groups/{id}/return-authorization`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `POST /__aisys__/api/accounts/{id}/return-authorization`、`POST /__aisys__/api/groups/{id}/return-authorization`；个人侧 `POST /__aisys__/api/my-accounts/{id}/return-authorization`、`POST /__aisys__/api/my-groups/{id}/return-authorization` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作为被授权账户作用域 |
| ID 契约 | 账户路径 `{id}` 是被授权用户本地授权账户实例 ID，通过 `accounts.authorization_instance_authorization_id` 找 runtime authorization；分组路径 `{id}` 是授权分组资源 ID，通过 `resource_authorizations.resource_type='group'` 和 `grantee_system_account_id` 找 runtime authorization；二者都不是 grant ID |
| 查询参数 | 管理侧 `systemAccountId` 非空时作为被授权系统账户作用域，`all` / 缺省回落到当前登录管理员系统账户；个人侧忽略 query `systemAccountId` |
| 归还范围 | 只允许被授权系统账户归还 direct manual grant；资源 owner 自己、团队授权、已回收 / 已归还 grant、没有 active manual source 的 runtime authorization 均返回 404 |
| 入口权限标记 | Go `accounts/options`、`groups/options` 和 `groups/account-options` 会按 runtime authorization 的 active manual source 设置 `permissions.canReturnAuthorization`；纯团队来源授权不显示资源页归还入口 |
| PG 写入 | 解析 runtime authorization 后查找对应 direct grant，复用授权归还事务尾部：标记 grant `returned`，撤销 manual source，`ended_reason=grantee_returned`，刷新 effective source |
| 统计 / 缓存 | 授权资源页归还后如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='resource_authorization_returned'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `204` 无响应体；账户不可归还返回 `404 { message: "授权账户不存在或不可归还" }`，分组不可归还返回 `404 { message: "授权分组不存在或不可归还" }` |
| 操作日志 | best-effort 入队 `operation-log:write`，账户路径 `operationKey=accounts.return_authorization`，分组路径 `operationKey=groups.return_authorization`；`operationScopeSystemAccountId` 为 grantee，记录 targeted viewer |
| 切片边界 | 这是 W4 账号页 / 分组页授权归还兼容入口；底层仍按 direct grant 归还，不代表账号 / 分组完整写接口、生产切流或 Node 删除已完成 |

### `DELETE /__aisys__/api/authorizations/{id}` / `DELETE /__aisys__/api/my-authorizations/{id}`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `DELETE /__aisys__/api/authorizations/{id}`；个人侧 `DELETE /__aisys__/api/my-authorizations/{id}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作为资源归属作用域 |
| ID 契约 | 路径 `{id}` 固定为 `resource_authorization_grants.id` 授权业务记录 ID，和列表、详情、归还保持同一 ID 语义 |
| 查询参数 | 管理侧 `systemAccountId=all` / 缺省表示全局可管理范围，具体系统账户 ID 表示只允许回收该资源归属人的授权；个人侧忽略 query `systemAccountId` |
| 回收范围 | 资源归属人或管理员可回收当前 grant；Go 当前按 Node 现有写路径直接把目标 grant 设为 `revoked`，不额外限制原状态 |
| PG 写入 | 在事务中锁定目标 grant，标记 grant `status=revoked`、写 `revoked_by/revoked_at`；个人 grant 撤销 manual source；团队 grant 只撤销当前 `resource_type/resource_id/team_id` 展开的 active team source，不回收同团队其他资源来源 |
| Runtime 状态 | 回收后刷新 effective source；若没有 active source，`resource_authorizations.status` 写为 `revoked`，`revoked_reason=authorization_revoked`，不保留已过期状态覆盖 |
| 统计 / 缓存 | 授权回收后如果 `group_account_stats_dirty` 表存在则写入 `group_id='__all__'`、`reason='resource_authorization_revoked'`；随后发布 `gateway_runtime_cache` 与 `authorization_quota_cache` runtime state |
| 成功响应 | `200 { data: authorization }`；目标不存在、越权或不在作用域内返回 `404 { message: "授权记录不存在" }` |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=authorizations.revoke`、`module=authorizations`、`action=revoke`、`resourceType=authorization`；记录被回收 grant、资源、grantee 和 targeted viewer |
| 切片边界 | 这是 W4 授权回收的 Go opt-in 灰度能力；批量到期扫描生产周期 runner / 部署调度、生产 gateway quota snapshot 消费、真实 PG/Redis/Asynq integration、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### 授权来源 / grant PostgreSQL schema

| 项 | 内容 |
| --- | --- |
| migration | `backend-go/db/migrations/000018_w4_resource_authorization_sources_and_grants.sql` |
| 表 | `juhe_business.resource_authorization_sources` 记录最终用户授权的 manual / team 来源；`juhe_business.resource_authorization_grants` 记录人员或团队授权业务操作 |
| 约束 | `source_type` 固定 `manual/team`，`source.status` 固定 `active/superseded/revoked`；`grantee_type` 固定 `system_account/team`，`grant.status` 固定 `active/paused/expired/revoked/returned`；`limits_json` 必须是 JSON object 或 NULL |
| 索引 | 补齐来源 active manual / team 部分唯一索引、grant active user / team 部分唯一索引、owner/resource/grantee 分页索引、grant 到期扫描索引、team quota snapshot 索引和 `resource_authorizations` quota snapshot 索引 |
| 切片边界 | 这是团队更新、成员增删、授权列表、授权创建、普通授权更新、授权有效期更新、授权归还、授权回收和批量到期扫描的 schema 基线；当前只覆盖已迁移列表 / 写链路 / 到期扫描 worker 入口，不代表批量到期扫描真实 PG/Redis smoke、生产部署 / supervisor 接管或生产 gateway quota snapshot 消费已迁移 |

### 授权额度 / 统计状态 PostgreSQL schema

| 项 | 内容 |
| --- | --- |
| migration | `backend-go/db/migrations/000019_w4_authorization_quota_and_stats_state.sql` |
| 表 | `juhe_business.request_quota_hourly_window_configs` 保存 1 到 720 小时的额度窗口配置；`juhe_business.group_account_stats_dirty` 保存 group stats 增量重建脏标记 |
| 默认窗口 | `1/3/6/12/24/72/168/720` 小时，和 Node 当前默认窗口一致 |
| 切片边界 | 本表为授权写状态和统计 worker 的基础状态表；团队 PATCH 只在授权变化时写全量脏标记，不迁 usage stats worker，不接管生产 gateway quota snapshot 消费或授权额度消费逻辑 |

### 授权缓存失效前置

| 项 | 内容 |
| --- | --- |
| Go owner | `backend-go/internal/modules/gatewaycache/system_account_invalidator.go` |
| Node 对齐 | Node `notifyGatewayRuntimeCacheInvalidation(reason)` 写 `topic:gateway_runtime_cache`，`notifyAuthorizationQuotaCacheInvalidation(reason)` 写 `topic:authorization_quota_cache`，`notifyApiKeyQuotaCacheInvalidation(apiKeyId, reason)` 写 `topic:api_key_quota_cache` 并保留 `apiKeyId` |
| Go 能力 | `InvalidateAuthorizationChanged(ctx, reason)` 先后发布 gateway runtime 与 authorization quota runtime state；`InvalidateAPIKeyQuotaChanged(ctx, apiKeyID, reason)` 发布 API Key quota runtime state 并 trim `apiKeyId` |
| 当前调用点 | `PATCH /__aisys__/api/system-teams/{id}` 在团队状态切换影响授权来源后调用 `InvalidateAuthorizationChanged(ctx, "team_authorization_changed")`；成员新增 / 移除后调用 `InvalidateAuthorizationChanged(ctx, "team_members_changed")`；授权创建后调用 `InvalidateAuthorizationChanged(ctx, "resource_authorization_created")`；普通授权更新和授权有效期更新后调用 `InvalidateAuthorizationChanged(ctx, "resource_authorization_updated")`；授权归还后调用 `InvalidateAuthorizationChanged(ctx, "resource_authorization_returned")`；授权回收后调用 `InvalidateAuthorizationChanged(ctx, "resource_authorization_revoked")` |
| 切片边界 | 这是团队停用 / 启用、成员维护、授权创建、普通授权更新、授权有效期更新、授权归还、授权回收和批量到期扫描已接入的缓存失效能力；仍不代表批量到期扫描真实 PG/Redis smoke、生产部署 / supervisor 接管或生产 gateway quota snapshot 消费已迁移 |

### `GET /__aisys__/api/authorizations` / `GET /__aisys__/api/my-authorizations`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `GET /__aisys__/api/authorizations`；个人侧 `GET /__aisys__/api/my-authorizations` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端读鉴权 `ManagementAPIAuthMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active；读接口不 touch session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作用域并忽略 query `systemAccountId` |
| 查询参数 | 支持 `keyword`、`resourceType`、`resourceId`、`resourceOwnerSystemAccountId`、`granteeSystemAccountId`、`teamId`、`status`、`direction`、`sourceType`、`systemAccountId`、`startDate`、`endDate`、`page`、`pageSize`；`startDate/endDate` 仅按 Node 当前 schema 校验日期格式，列表查询暂不应用日期范围 |
| 作用域 | 管理侧 `systemAccountId=all` / 缺省表示全局列表，具体系统账户 ID 表示该账户相关授权；个人侧只返回当前用户作为资源归属人、个人被授权人或所属 active 团队相关的 grant。`direction=outbound/inbound` 只在个人侧应用，管理侧按 Node 当前行为校验但不应用 |
| PG 读取 | 以 `juhe_business.resource_authorization_grants` 为分页主表，按 `created_at DESC, id DESC` 稳定排序；关键词按 grant ID、资源 ID、备注、归属人、被授权人、团队名、账号名、授权实例账号名和分组名做 PostgreSQL `COLLATE "C"` + `starts_with` 前缀匹配 |
| 响应 | `200 { data: { items, total, hasMore, page, pageSize } }`；列表项使用轻量 DTO，包含 `sourceSummary`，不返回 `authorizationSources`、`limits`、`resourceAccountExpiresAt`、`usage`、`usageBySystemAccount*` 或 `usageRange` |
| 脱敏 | 当前查看者不能管理资源归属人时，隐藏 `effectiveSourceTeamId`、`effectiveSourceTeamName`、`createdBy`、`revokedBy` 和 team source 明细；`permissions.canEdit/canAuthorize` 只在可管理资源归属人时为 true |
| 切片边界 | 这是 W4 授权列表的 Go opt-in 灰度能力；列表只读取当前 grant 状态，不触发 Node `expireDueResourceAuthorizations` 到期扫描；授权 `:id/usage` 明细、批量到期扫描真实 PG/Redis smoke、授权用量窗口真实 PG smoke、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### `GET /__aisys__/api/authorizations/{id}` / `GET /__aisys__/api/my-authorizations/{id}`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `GET /__aisys__/api/authorizations/{id}`；个人侧 `GET /__aisys__/api/my-authorizations/{id}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端读鉴权 `ManagementAPIAuthMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active；读接口不 touch session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作用域并忽略 query `systemAccountId` |
| ID 契约 | 路径 `{id}` 固定为 `resource_authorization_grants.id` 授权业务记录 ID，和列表项、归还操作保持同一 ID 语义 |
| 查询参数 | 管理侧 `systemAccountId=all` / 缺省表示全局可见范围，具体系统账户 ID 表示该账户相关授权；个人侧忽略 query `systemAccountId` |
| PG 读取 | 复用 grant 主表读取路径，按 `rag.id` 精确过滤并应用当前用户可见性约束；不做 usage 聚合，不触发到期扫描 |
| 响应 | `200 { data: authorization }`；详情返回 grant 摘要、`limits`、`authorizationSources`、基础 `usage` 空对象和 `permissions` |
| 脱敏 | 当前查看者不能管理资源归属人时，隐藏 `effectiveSourceTeamId`、`effectiveSourceTeamName`、source `sourceTeamId/endedAt/createdBy/revokedBy/revokedAt`、authorization `createdBy/revokedBy`；source team name、status、endedReason 等可展示字段保留 |
| 切片边界 | 这是 W4 授权详情的 Go opt-in 灰度能力；详情只读取当前 grant 状态，不触发 Node `expireDueResourceAuthorizations` 到期扫描；详情本身不做 usage 聚合；批量到期扫描真实 PG/Redis smoke、授权用量窗口真实 PG smoke、生产部署 / supervisor 接管、生产 gateway quota snapshot 消费、浏览器连接真实 Go 后端的前端统一授权页 smoke、生产切流和 Node 删除仍未完成 |

### `GET /__aisys__/api/authorizations/usage/team-details` / `GET /__aisys__/api/authorizations/usage/user-details`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `GET /__aisys__/api/authorizations/usage/team-details`、`GET /__aisys__/api/authorizations/usage/user-details`；个人侧 `GET /__aisys__/api/my-authorizations/usage/team-details`、`GET /__aisys__/api/my-authorizations/usage/user-details` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端读鉴权 `ManagementAPIAuthMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active；读接口不 touch session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作用域并忽略 query `systemAccountId` |
| 查询参数 | 支持 `resourceType`、`resourceId`、`teamId`、`granteeSystemAccountId`、`startDate`、`endDate`、`page`、`pageSize`；管理侧支持 `systemAccountId=all` / 缺省读全局窗口，具体系统账户 ID 读该 owner 窗口；个人侧固定当前系统账户 |
| 日期范围 | Go service 将日期归一化到最多 31 天窗口；未来日期裁剪到当前日期，过早日期裁剪到最近 31 天内；非法日期返回中文 400 |
| PG 读取 | team overview 只读 `juhe_stats.authorization_team_usage_range_windows`，user overview 只读 `juhe_stats.authorization_user_usage_range_windows`；先按窗口表排序分页，再只对当前页补团队名、用户名、资源名和资源 owner 名 |
| 性能边界 | 请求路径不访问 `juhe_usage.usage_records`，不做实时 `SUM/GROUP BY`，不把明细加载到内存分页；summary 读取同一 range window 的汇总行 |
| 响应 | team overview 返回 `{ range, summary, rows, teamCount, total, page, pageSize, hasMore }`；user overview 返回 `{ range, summary, rows, userCount, total, page, pageSize, hasMore }`；`total` 为窗口上界估算，延续 Node `pageSize + 1` 契约 |
| schema | `000020_w4_authorization_usage_windows.sql` 补齐 `authorization_team_usage_summary_daily`、`authorization_team_usage_range_windows`、`authorization_user_usage_summary_daily`、`authorization_user_usage_range_windows` 和 lookup / sort 索引 |
| 切片边界 | 这是 W4 授权 team/user usage overview 的 Go opt-in 灰度能力；range window 刷新已有 Go worker 入口但未完成真实 PG smoke / 生产部署接管；批量到期扫描真实 PG/Redis smoke / 生产部署接管、生产 gateway quota snapshot 消费、浏览器真实后端 smoke、生产切流或 Node 删除仍未完成 |

### `GET /__aisys__/api/authorizations/{id}/usage` / `GET /__aisys__/api/my-authorizations/{id}/usage`

| 项 | 内容 |
| --- | --- |
| 路径 | 管理侧 `GET /__aisys__/api/authorizations/{id}/usage`；个人侧 `GET /__aisys__/api/my-authorizations/{id}/usage` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端读鉴权 `ManagementAPIAuthMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active；读接口不 touch session |
| 权限 | 管理侧允许 `super_admin` / `admin`，普通 `user` 返回 403；个人侧强制当前登录系统账户作用域并忽略 query `systemAccountId` |
| ID 契约 | 路径 `{id}` 固定为 `resource_authorization_grants.id` 授权业务记录 ID，和列表、详情、归还、回收保持同一 ID 语义 |
| 查询参数 | 支持 `systemAccountId`、`startDate`、`endDate`、`page`、`pageSize`；明细默认 `pageSize=200`，最大 200；日期窗口最多 31 天并裁剪到当前日期 |
| PG 读取 | 先复用 grant 主表详情读取路径确认授权存在且当前用户可见；direct 授权读取对应 runtime authorization 和 `authorization_user_usage_range_windows`；team 授权读取当前资源 / 团队来源展开的 runtime authorization 分页，并 join `authorization_user_usage_range_windows`；team 总 usage 读取 `authorization_team_usage_range_windows` |
| 性能边界 | 请求路径不访问 `juhe_usage.usage_records`，不实时 `SUM/GROUP BY`，不触发 Node `expireDueResourceAuthorizations` 到期扫描；没有窗口行时返回 0 usage，不在请求链路补算 |
| 响应 | `200 { data: authorization }`；在详情响应基础上返回 `usage`、`lastUsedAt`、`usageBySystemAccount`、`usageBySystemAccountTotal`、`usageBySystemAccountPage`、`usageBySystemAccountPageSize`、`usageBySystemAccountHasMore` 和 `usageRange` |
| 脱敏 | 授权主体字段沿用详情接口脱敏；当前查看者不能管理资源归属人时隐藏 source 内部 ID / 操作人字段，但保留自身可见用量明细 |
| 切片边界 | 这是 W4 授权详情用量页的 Go opt-in 灰度能力；range window 刷新已有 Go worker 入口但未完成真实 PG smoke / 生产部署接管；批量到期扫描真实 PG/Redis smoke / 生产部署接管、生产 gateway quota snapshot 消费、浏览器真实后端 smoke、生产切流或 Node 删除仍未完成 |

### 授权批量到期扫描

| 项 | 内容 |
| --- | --- |
| 入口 | `juhe-ai-worker authorization-expiry-sweep`；内部调用 `managementauthorizations.Service.ExpireDue`，当前不提供 HTTP 管理路由 |
| 调用边界 | 默认批量 20 条，显式传入负数时按 1 条处理；worker 默认每 1 分钟扫描一次，初始延迟 54 秒，支持 `--run-once`、`--limit`、`--interval` 和 `--initial-delay`；不 touch 管理端 session，也不写 operation log |
| PG 读取 | 只读取 `juhe_business.resource_authorization_grants`；筛选 `status IN ('active', 'paused')`、`expires_at IS NOT NULL` 且 `expires_at <= now`，按 `expires_at ASC, updated_at ASC, id ASC` 稳定排序，`LIMIT` 固定窗口并使用 `FOR UPDATE SKIP LOCKED` 避免多 worker 重复处理 |
| PG 写入 | 事务内把命中的 grant 标记为 `expired`，`revoked_at=COALESCE(revoked_at, now)`，保留原 `revoked_by` 语义；actor 仅用于 runtime 同步，取 `grant.revoked_by`，没有则取 `grant.created_by` |
| 运行态同步 | 复用 `syncManagementGrantRuntimeAfterUpdateTx`：个人 grant 更新 direct manual runtime，团队 grant 更新当前资源 / 团队来源展开的 runtime authorization，并刷新 effective source |
| 统计 / 缓存 | 有实际过期行时写 `group_account_stats_dirty` 全量脏标记，reason 为 `authorization_expired`；service 层发布 `gateway_runtime_cache` 与 `authorization_quota_cache` 授权缓存失效 |
| 性能边界 | 不读取 `juhe_usage.usage_records`，不实时 `SUM/GROUP BY`，不在扫描路径补算 usage range window 或 quota snapshot |
| 切片边界 | 这是 W4 批量到期扫描的 Go worker 入口，不代表真实 PG/Redis smoke、生产部署 / supervisor 接管、生产切流或 Node worker 删除已完成 |

### `juhe-ai-worker gateway-quota-snapshot-build`

| 项 | 内容 |
| --- | --- |
| 入口 | `juhe-ai-worker gateway-quota-snapshot-build`；内部调用 `gatewayquotasnapshot.Service.Build`，当前不提供 HTTP 管理路由 |
| 调用边界 | worker 默认每 1 分钟构建一次，初始延迟 37 秒，支持 `--run-once`、`--interval`、`--initial-delay`、`--timezone`、`--publish-runtime-state` 和 `--snapshot-ttl`；默认只构建并打日志，开启 `--publish-runtime-state` 时要求 Redis state；不 touch 管理端 session，不写 operation log |
| 快照 scope | API Key 使用 `api_key` scope 和 `api_keys.id`；direct account / group 授权使用 `account_authorization` / `group_authorization`；team account / group 授权使用 `account_authorization_team` / `group_authorization_team`，其中 account team scope 必须有授权实例账户 ID |
| PG 读取 | 只读取 `juhe_business.api_keys`、`resource_authorizations`、`resource_authorization_grants`、`accounts`、`juhe_settings.system_settings`、`juhe_stats.usage_stats_totals`、`usage_stats_daily`、`usage_stats_weekly`、`usage_stats_monthly` 和 `usage_quota_hourly_windows` |
| 成本窗口 | API Key 成本快照和授权成本快照均使用 `LIMIT 5001` 上界读取，返回 `entriesComplete` 标记；授权 direct 与 team 窗口分别完成后才视为 authorization entries complete |
| 性能边界 | 不读取 `juhe_usage.usage_records`，不实时 `SUM/GROUP BY`，不在请求路径聚合，也不全量扫描明细表 |
| 切片边界 | 这是 W4 网关配额快照构建与可选 Redis runtime state 发布能力；Node Redis 模式已能消费共享快照并在缺失 / 不完整时保留 DB service fallback，但真实 PG/Redis smoke、生产部署 / supervisor 接管、切流或 Node gateway 删除仍未完成 |

## 本轮不迁移范围

- 批量到期扫描真实 PG/Redis smoke 与生产部署 / supervisor 接管、授权用量 range window 真实 PG smoke 与生产部署 / supervisor 接管、网关配额快照真实 PG/Redis smoke 与生产部署 / supervisor 接管、授权用量详情 fanout 写入链路。
- 浏览器连接真实 Go 后端的团队 / 统一授权页面 smoke、生产切流和 Node 删除。
- 前端团队页面连接真实 Go 后端的浏览器 smoke、生产反向代理切流和 Node route / repository 删除。

## Go 实现范围

- `managementsystemteams.Service.List` / `Detail` 负责分页归一化、当前用户作用域传递、DTO 映射和空团队 ID 校验。
- `managementsystemteams.Service.Create` 负责团队创建输入归一化、默认 `active` 状态、说明 trim / 长度限制、创建人上下文和重复团队名错误映射。
- `managementsystemteams.Service.Update` 负责团队 PATCH 输入归一化、作用域传递、重复团队名错误映射、授权变化时触发 `team_authorization_changed` 缓存失效。
- `managementsystemteams.Service.AddMembers` / `RemoveMember` 负责成员新增 / 移除输入归一化、作用域传递、DTO 映射和 `team_members_changed` 授权缓存失效。
- `managementauthorizations.Service.Create` 负责授权创建输入归一化、资源 / grantee 组合校验、目标分组约束、`expiresAt` 和 quota limits 归一化、授权账户实例空凭据加密，以及 `resource_authorization_created` 授权缓存失效。
- `managementauthorizations.Service.Update` 负责授权普通 PATCH 和有效期更新的输入归一化、status / `expiresAt` / quota limits 校验、admin / self 作用域传递，以及 `resource_authorization_updated` 授权缓存失效；HTTP 层保证 `/expire` 不接收 `status` 且必须提交 `expiresAt`。
- `managementauthorizations.Service.List` 负责授权列表查询参数归一化、admin / self 作用域、分页窗口上界、轻量列表 DTO、权限字段和非资源归属人脱敏。
- `managementauthorizations.Service.Get` 负责授权详情 ID / actor 校验、admin / self 作用域、详情 DTO 权限字段和非资源归属人 source 明细脱敏。
- `managementauthorizations.Service.TeamUsageOverview` / `UserUsageOverview` 负责授权用量 overview 的 admin / self 作用域、资源 / 团队 / 用户过滤、31 天日期窗口归一化、分页窗口上界和 range DTO。
- `managementauthorizations.Service.UsageDetail` 负责授权详情用量页的 grant ID / actor 校验、admin / self 作用域、31 天日期窗口归一化、默认 200 条明细分页、权限字段和非资源归属人 source 明细脱敏。
- `managementauthorizations.Service.Return` 负责 grant ID 授权归还输入归一化、grant ID / grantee / actor 校验、按归还结果触发 `resource_authorization_returned` 授权缓存失效；`ReturnByResource` 负责账号页 / 分组页 `return-authorization` 兼容入口的 resource ID / grantee / actor 校验，并复用同一个缓存失效 reason。
- `managementauthorizations.Service.ExpireDue` 负责批量到期扫描默认批量归一化，调用 store 后仅在实际过期数量大于 0 时触发 `authorization_expired` 授权缓存失效。
- `app.RunAuthorizationExpirySweepWorker` 和 `juhe-ai-worker authorization-expiry-sweep` 负责周期调度授权到期扫描，要求 PostgreSQL、Redis state 和 Redis cache；默认间隔 1 分钟、初始延迟 54 秒，并支持一次性 `--run-once` smoke。
- `gatewayquotasnapshot.Service.Build` 负责按 Node 当前 API Key / direct authorization / team authorization scope 构建网关配额成本快照，计算 `usageStatsTimezone` 对应的日 / 周 / 月窗口，合并总量、日、周、月和小时窗口成本，并返回快照完整性标记。
- `app.RunGatewayQuotaSnapshotBuildWorker` 和 `juhe-ai-worker gateway-quota-snapshot-build` 负责周期构建网关配额快照；默认间隔 1 分钟、初始延迟 37 秒，只要求 PostgreSQL，并支持一次性 `--run-once` smoke；开启 `--publish-runtime-state` 时额外要求 Redis state，并把快照写入 Node 兼容 `gateway_quota_snapshot/current` runtime state。
- `postgres.Store.ListManagementSystemTeams` / `FindManagementSystemTeam` 通过 `w4_management_system_teams.sql` 读取团队摘要、当前页 team IDs 的 active 成员计数和详情 active 成员；查询不读取授权来源 / grant，不用说明字段做关键词搜索。
- `postgres.Store.CreateManagementSystemTeam` 通过 `w4_management_system_teams.sql` 写入 `system_teams`，捕获 `idx_system_teams_name_unique` 唯一约束并映射为团队名称冲突。
- `postgres.Store.UpdateManagementSystemTeam` 在事务中锁定团队行、更新团队字段，并在状态切换时执行 team source revoke / reactivate、effective source 刷新和全量 group stats dirty 标记。
- `postgres.Store.AddManagementSystemTeamMembers` / `RemoveManagementSystemTeamMember` 在事务中锁定团队或成员，维护 `system_team_members`，并按当前 active team grant / source 展开或回收最终用户授权来源、刷新 effective source 和全量 group stats dirty 标记。
- `postgres.Store.CreateManagementResourceAuthorization` 在事务中校验资源归属、写入或恢复 resource authorization grant、展开个人 manual source / 团队 team source、创建或恢复账户授权实例、绑定目标分组，并写全量 group stats dirty 标记。
- `postgres.Store.UpdateManagementResourceAuthorization` 在事务中锁定 grant，按资源归属作用域更新 `status/expires_at/limits_json`，同步 direct manual source 或当前资源 team source，刷新 runtime authorization，并写全量 group stats dirty 标记。
- `postgres.Store.ListManagementResourceAuthorizations` 按 grant 主表读取授权列表，支持资源 / grantee / team / 状态 / 来源类型 / 当前用户作用域 / 个人方向 / 关键词前缀过滤和稳定分页；不做 usage 聚合，不触发到期扫描。
- `postgres.Store.FindManagementResourceAuthorization` 复用 grant 主表读取路径按 `rag.id` 精确读取授权详情，并应用当前用户可见性约束；不做 usage 聚合，不触发到期扫描。
- `postgres.Store.ListManagementAuthorizationTeamUsageOverview` / `ListManagementAuthorizationUserUsageOverview` 只读 `juhe_stats.authorization_*_usage_range_windows` 预聚合窗口，先分页再补当前页团队 / 用户 / 资源 lookup；不读取 `usage_records`，不实时聚合。
- `postgres.Store.FindManagementResourceAuthorizationUsage` 先复用 grant 详情可见性查询，再按 direct / team 授权读取预聚合 range window；direct 授权读 `authorization_user_usage_range_windows`，team 授权分页读取 runtime authorization 并 join user window，总 usage 读 team window；不读取 `usage_records`，不触发到期扫描。
- `postgres.Store.ReturnManagementResourceAuthorizationForGrantee` 在事务中按 grant ID 锁定 direct system-account grant，校验 active manual source，标记 grant `returned`，撤销 manual source，刷新 runtime authorization 为 `returned`，并写全量 group stats dirty 标记。
- `postgres.Store.ExpireDueManagementResourceAuthorizations` 在事务中按 grant 到期索引读取 fixed window，使用 `FOR UPDATE SKIP LOCKED` 锁定到期 active / paused grant，标记 `expired`，复用个人 / 团队 runtime 同步 helper，并写 `authorization_expired` 全量 group stats dirty 标记。
- `postgres.Store.ListGatewayQuotaSnapshotAPIKeys` / `ListGatewayQuotaSnapshotAuthorizations` / `ListGatewayQuotaSnapshotTeamAuthorizations` 按固定窗口读取带额度配置的 API Key、direct 授权和 team 授权快照候选，使用 `LIMIT 5001` 上界判断完整性。
- `postgres.Store.LoadGatewayQuotaSnapshotCosts` 只从 `juhe_stats.usage_stats_totals`、`usage_stats_daily`、`usage_stats_weekly`、`usage_stats_monthly` 和 `usage_quota_hourly_windows` 读取预聚合成本，不读取 `usage_records`，不实时 `SUM/GROUP BY`。
- `000018_w4_resource_authorization_sources_and_grants.sql` 补齐 `resource_authorization_sources` / `resource_authorization_grants` 当前 PostgreSQL schema、部分唯一索引和过期扫描索引，供后续授权写链路分块使用。
- `000019_w4_authorization_quota_and_stats_state.sql` 补齐 `request_quota_hourly_window_configs` 和 `group_account_stats_dirty` 当前 PostgreSQL 基础状态表及默认窗口。
- `000020_w4_authorization_usage_windows.sql` 补齐授权 team/user 日汇总与 range window 预聚合表、lookup 索引和排序索引，供 Go overview 读路径使用；本迁移只建派生表，不代表刷新 worker 已迁移。
- `gatewaycache.SystemAccountInvalidator` 补齐授权业务缓存失效前置：授权变化发布 `gateway_runtime_cache` + `authorization_quota_cache`，API Key 额度变化发布 `api_key_quota_cache` 并带 `apiKeyId`，保持 Node runtime state topic / TTL / payload 兼容。
- `httpapi.NewManagementSystemTeamsHandler` / `NewManagementMySystemTeamsHandler` 负责 admin / self 权限、`systemAccountId` 作用域、列表 / 详情路由复用、中文 403 / 404 / 500 响应和 envelope。
- `httpapi.NewManagementSystemTeamCreateHandlerWithOperationLog` 负责管理员权限、strict JSON、`systemAccountId` query 合法性检查、中文错误响应、201 envelope 和 operation log best-effort 入队。
- `httpapi.NewManagementSystemTeamPatchHandlerWithOperationLog` 负责管理员权限、strict JSON、状态副作用错误映射、200 envelope 和 `system_teams.update` operation log best-effort 入队。
- `httpapi.NewManagementSystemTeamMembersAddHandlerWithOperationLog` / `NewManagementSystemTeamMemberDeleteHandlerWithOperationLog` 负责管理员权限、strict JSON、成员写入错误映射、200 envelope 和 `system_teams.add_members` / `system_teams.remove_member` operation log best-effort 入队。
- `httpapi.NewManagementAuthorizationListHandler` / `NewManagementMyAuthorizationListHandler` 负责 admin / self 权限、列表查询参数校验、管理侧 direction 校验但不应用、个人侧 direction 应用、200 envelope 和中文错误响应。
- `httpapi.NewManagementAuthorizationDetailHandler` / `NewManagementMyAuthorizationDetailHandler` 负责 admin / self 权限、grant ID path 参数、`systemAccountId` 作用域、200 / 404 envelope 和中文错误响应。
- `httpapi.NewManagementAuthorizationTeamUsageOverviewHandler` / `NewManagementMyAuthorizationTeamUsageOverviewHandler` / `NewManagementAuthorizationUserUsageOverviewHandler` / `NewManagementMyAuthorizationUserUsageOverviewHandler` 负责 admin / self 权限、overview query 校验、`systemAccountId` 作用域、200 envelope 和中文错误响应。
- `httpapi.NewManagementAuthorizationUsageHandler` / `NewManagementMyAuthorizationUsageHandler` 负责 admin / self 权限、grant ID path 参数、明细 query 校验、`systemAccountId` 作用域、200 / 404 envelope 和中文错误响应。
- `httpapi.NewManagementAuthorizationCreateHandlerWithOperationLog` / `NewManagementMyAuthorizationCreateHandlerWithOperationLog` 负责 admin / self 权限、strict JSON、目标分组错误映射、201 envelope 和 `authorizations.create` operation log best-effort 入队。
- `httpapi.NewManagementAuthorizationUpdateHandlerWithOperationLog` / `NewManagementMyAuthorizationUpdateHandlerWithOperationLog` 负责 admin / self 权限、grant ID path 参数、strict JSON、资源归属作用域、200 envelope 和 `authorizations.update` operation log best-effort 入队。
- `httpapi.NewManagementAuthorizationExpireUpdateHandlerWithOperationLog` / `NewManagementMyAuthorizationExpireUpdateHandlerWithOperationLog` 负责 admin / self 权限、grant ID path 参数、`expiresAt` 必填、`limits` 可选 / 可清空、资源归属作用域、200 envelope 和 `authorizations.update_expire` operation log best-effort 入队。
- `httpapi.NewManagementAuthorizationReturnHandlerWithOperationLog` / `NewManagementMyAuthorizationReturnHandlerWithOperationLog` 负责 admin / self 权限、grant ID path 参数、被授权人作用域、204 响应和 `authorizations.return` operation log best-effort 入队。
- `httpapi.NewManagementAuthorizationRevokeHandlerWithOperationLog` / `NewManagementMyAuthorizationRevokeHandlerWithOperationLog` 负责 admin / self 权限、grant ID path 参数、资源归属作用域、200 envelope 和 `authorizations.revoke` operation log best-effort 入队。
- `router.go` 只在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册 `GET /system-teams`、`GET /system-teams/{id}`、`GET /my-teams`、`GET /my-teams/{id}`、`POST /system-teams`、`PATCH /system-teams/{id}`、`POST /system-teams/{id}/members`、`DELETE /system-teams/{id}/members/{memberId}`、`GET /authorizations`、`GET /authorizations/{id}`、`GET /authorizations/{id}/usage`、`GET /my-authorizations`、`GET /my-authorizations/{id}`、`GET /my-authorizations/{id}/usage`、`GET /authorizations/usage/team-details`、`GET /my-authorizations/usage/team-details`、`GET /authorizations/usage/user-details`、`GET /my-authorizations/usage/user-details`、`POST /authorizations`、`POST /my-authorizations`、`PATCH /authorizations/{id}`、`PATCH /my-authorizations/{id}`、`PATCH /authorizations/{id}/expire`、`PATCH /my-authorizations/{id}/expire`、`DELETE /authorizations/{id}/return`、`DELETE /my-authorizations/{id}/return`、`DELETE /authorizations/{id}` 和 `DELETE /my-authorizations/{id}`；读接口走普通 auth，写接口纳入 touch middleware fail-fast。
- `server.go` 在管理 API opt-in 时构建 `managementsystemteams.Service`、`managementauthorizations.Service`、团队读 / 创建 / 更新 / 成员维护 handler 和授权列表 / 详情 / usage overview / usage detail / 创建 / 普通更新 / 有效期更新 / 归还 / 回收 handler；默认关闭时不构建生产路由。

## 验证命令

在 `backend-go/` 下运行：

```powershell
sqlc generate
gofmt -w internal/store/port/management.go internal/store/port/gatewayquota.go internal/modules/managementsystemteams/service.go internal/modules/managementsystemteams/service_test.go internal/modules/managementauthorizations/service.go internal/modules/managementauthorizations/service_test.go internal/modules/gatewayquotasnapshot/service.go internal/modules/gatewayquotasnapshot/service_test.go internal/store/postgres/managementsystemteams.go internal/store/postgres/managementsystemteams_test.go internal/store/postgres/managementauthorizations.go internal/store/postgres/gatewayquota.go internal/store/postgres/gatewayquota_test.go internal/httpapi/management_system_teams.go internal/httpapi/management_system_teams_test.go internal/httpapi/management_authorizations.go internal/httpapi/management_authorizations_test.go internal/httpapi/router.go internal/app/server.go internal/app/server_test.go internal/app/gateway_quota_snapshot_worker.go internal/app/ingest_worker_test.go cmd/juhe-ai-worker/main.go internal/store/postgres/postgresqueries/w4_management_system_teams.sql.go internal/store/postgres/postgresqueries/models.go
gofmt -w internal/store/postgres/managementauthorizationschema_test.go
gofmt -w internal/modules/gatewaycache/system_account_invalidator.go internal/modules/gatewaycache/system_account_invalidator_test.go
go test ./internal/modules/gatewaycache -count=1
go test ./internal/modules/managementsystemteams ./internal/modules/managementauthorizations ./internal/modules/gatewayquotasnapshot ./internal/httpapi ./internal/store/postgres ./internal/app ./cmd/juhe-ai-worker -count=1
go test ./... -count=1
go vet ./...
go mod tidy -diff
git diff --check
```

Node 授权归还 ID 契约修复后，在仓库根目录运行：

```powershell
pnpm --filter juhe-ai-backend test:authorization-return-driver
pnpm --filter juhe-ai-backend test:authorization-return
```

如需证明 integration 包可编译，再运行：

```powershell
go test -tags=integration ./internal/testkit/integration -run '^$' -count=1
```

前端团队 / 统一授权页本地调用和 UI 边界 smoke 在仓库根目录运行：

```powershell
pnpm --filter juhe-ai-frontend test:w4-team-authorization-smoke
pnpm --filter juhe-ai-frontend typecheck
pnpm --filter juhe-ai-frontend build
```

该前端 smoke 使用本地 mock HTTP，固定团队 / 授权页面调用已迁移路径和管理 / 个人作用域边界；不代表浏览器已连接真实 Go 后端。

## 删除门禁

- `GET /system-teams`、`GET /my-teams`、`POST /system-teams`、`PATCH /system-teams/{id}`、`POST /system-teams/{id}/members`、`DELETE /system-teams/{id}/members/{memberId}`、`GET /authorizations`、`GET /authorizations/{id}`、`GET /authorizations/{id}/usage`、`GET /my-authorizations`、`GET /my-authorizations/{id}`、`GET /my-authorizations/{id}/usage`、`POST /authorizations`、`POST /my-authorizations`、`PATCH /authorizations/{id}`、`PATCH /my-authorizations/{id}`、`PATCH /authorizations/{id}/expire`、`PATCH /my-authorizations/{id}/expire`、`DELETE /authorizations/{id}/return`、`DELETE /my-authorizations/{id}/return`、`DELETE /authorizations/{id}` 和 `DELETE /my-authorizations/{id}` 进入 Go opt-in 不允许删除 Node `system-teams` / `authorizations` route / repository。
- 授权缓存失效前置进入 Go 不允许删除 Node `shared/gateway-cache-invalidation.ts` 调用点；只有后续业务写接口接入并通过真实 PG/Redis smoke 后，才能把对应调用点迁移到 Go。
- W4 至少需要团队 CRUD、成员维护、授权列表 / 详情 / usage overview / usage detail / 创建 / 普通更新 / 有效期更新 / 归还 / 回收、批量到期扫描 worker、usage range window 刷新 worker、gateway quota snapshot 构建 / 发布 worker、operation log 真实消费、真实 PG/Redis/Asynq smoke、浏览器连接真实 Go 后端的前端 smoke、生产单 owner 切流和回滚记录整体完成后，才可以评估 Node 删除；当前授权列表 / 详情 / 创建 / 普通更新 / 有效期更新 / 归还 / 回收、批量到期扫描、usage range window 刷新和 gateway quota snapshot 构建 / 发布只覆盖 grant 读写入口及 worker 入口的一部分，不能代表统一授权生产接管。
- 团队读接口和团队创建本身不触发授权来源展开；授权列表 / 详情只读 grant 当前状态且不做 usage 聚合 / 到期扫描；成员维护只展开 / 回收既有 active team grant 对应来源；授权创建只新增 / 恢复 grant 与最终用户授权来源；授权普通更新只处理 PATCH `status/expiresAt/limits`；授权有效期更新只处理 PATCH `/expire` 的 `expiresAt/limits` 配置，不执行批量到期扫描；grant ID 授权归还和账号 / 分组资源页归还都只处理 direct manual grant；授权回收只按 grant ID 回收 manual source 或当前资源 team source；任何把当前 Go opt-in 路由当成统一授权生产接管证据的结论都是无效证据。
