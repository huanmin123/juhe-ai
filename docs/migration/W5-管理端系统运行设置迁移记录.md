# W5 管理端系统运行设置迁移记录

> 本文记录 W5 `GET /__aisys__/api/settings` 与 `PATCH /__aisys__/api/settings` 的 Go opt-in 迁移契约。两条路径已经进入 Go opt-in，并已完成真实依赖与真实 Go listener 写 smoke；该纵切面仍不代表完整 W5、浏览器页面写交互、生产切流或 Node settings 删除已经完成。

## 基本信息

- 模块：管理端系统运行设置
- 状态：Go 实现中；GET / PATCH 已进入 opt-in，真实依赖与 loopback Go listener smoke 已通过，仍未生产接管
- 迁移波次：W5 供应商、账号、分组、API Key、策略路由、代理、设置
- 当前 Node owner：`backend/src/modules/settings/settings.routes.ts`、`backend/src/storage/settings.repository.ts`
- 当前 Go owner：`backend-go/internal/systemsettings/`、`backend-go/internal/modules/managementsettings/`、`backend-go/internal/httpapi/management_system_settings.go`、`backend-go/internal/store/postgres/`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/app/server.go`、`backend-go/db/migrations/000024_w5_system_settings.sql`、`backend-go/db/migrations/000043_w5_remove_gpt_service_tier_multipliers.sql`
- 关联计划：`../plans/计划-0081-Node转Go渐进减法迁移.md`
- 相邻切片：[W5 管理端全局品牌设置读写记录](W5-管理端全局品牌设置读取记录.md)

## HTTP 契约

### 共用边界

| 项 | 内容 |
| --- | --- |
| 路径 | `GET /__aisys__/api/settings`、`PATCH /__aisys__/api/settings` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册 |
| 登录态 | 必须携带有效管理端 session |
| 权限 | 只允许 `admin` / `super_admin`；普通 `user` 返回 `403` |
| 响应 | 成功时返回 `{ "data": { ...完整系统设置... } }` |
| 缓存头 | 保持 system API 全局 `Cache-Control: no-store` |

### GET

- 使用只读 session 鉴权，不 touch `system_sessions.last_seen_at`。
- 进入 IP read 每分钟 / 10 秒 burst 和 authenticated user read limiter。
- 中间件顺序固定为 system API IP read limiter -> 只读 session 鉴权 -> authenticated user read limiter -> handler；GET 不经过 JSON parser，也不使用 touch 鉴权。
- 只读取 `juhe_business.system_settings` 中 `system_account_id='sys_admin'` 且位于当前白名单的行。
- 必须返回全部白名单字段；缺字段、重复字段、未知字段、非法 JSON 或非法值统一视为服务端存储错误，对外返回通用 `500`。
- 查询和响应都不包含内部固定常量，例如 data-retention worker 的批大小、批次数或固定启用标记。

### PATCH

- 使用写 session 鉴权并按现有 60 秒规则 touch 当前 session。
- 进入 IP write 每分钟 / 10 秒 burst 和 authenticated user write limiter。
- 中间件顺序固定为 system API IP write limiter -> `256 KiB` JSON parser -> 写 session 鉴权 / touch -> authenticated user write limiter -> handler。malformed JSON 或超限 body 会在 auth、touch 和用户 limiter 前分别返回 `400` / `413`，但仍已经过 IP limiter。
- 请求体必须是单个 JSON object，最大 `256 KiB`，至少包含一个白名单字段；拒绝未知字段、`null`、错误类型、空对象、顶层非对象和尾随 JSON。
- 请求体超过上限时返回 `413 { "message": "请求体过大" }`；其他 body 结构错误返回 `400`。
- 整数字段必须是 JSON 整数并满足对应闭区间；不接受浮点数、字符串数字、布尔值或自动截断。
- 成功响应返回事务提交后的完整系统设置快照，而不是只返回本次提交字段。
- Go 目标运行时只有 PostgreSQL，因此任何包含 `usageStatsTimezone` 的在线 PATCH 都返回 `400`：
  `PostgreSQL 模式下暂不支持在线修改统计时区，请停机后通过离线迁移 / 重建流程调整`。

## 字段白名单

当前白名单以 Node `systemSettingKeys` 的 55 项为准；Go catalog、前端类型和 fresh PostgreSQL seed 使用同一集合：

| 分类 | 字段与范围 |
| --- | --- |
| 网关请求体 | `gatewayTextRawBodyLimitMegabytes`：`1..64` |
| System API 限流 | `systemApiRateLimitIpReadPerMinute`、`systemApiRateLimitIpReadBurstPer10Seconds`、`systemApiRateLimitIpWritePerMinute`、`systemApiRateLimitIpWriteBurstPer10Seconds`、`systemApiRateLimitUserReadPerMinute`、`systemApiRateLimitUserWritePerMinute`：`0..1000000` |
| 临时不可调用 | `defaultTemporaryUnschedulableMinutes`：`1..1440`；`temporaryUnschedulableRetryIntervalSeconds`：`0..3600`；`temporaryUnschedulableRetryAttempts`：`0..10` |
| 网关 lane 超时与诊断 | `textFirstResponseTimeoutSeconds`：`10..3600`；`textStreamIdleTimeoutSeconds`：`1..3600`；`textUncommittedAttemptMaxLifetimeSeconds`：`60..86400`；`imageFirstResponseTimeoutSeconds`：`10..3600`；`imageStreamIdleTimeoutSeconds`：`1..3600`；`imageUncommittedAttemptMaxLifetimeSeconds`：`60..86400`；`noAvailableAccountWaitTimeoutSeconds`：`10..3600`；`streamFailureThresholdCount`：`1..100`；`streamFailureThresholdWindowMinutes`：`1..1440` |
| 操作日志 | `operationLogRetentionDays`：`1..3650`；`operationLogMaxChangesPerRecord`：`1..500` |
| 统计任务 | `statsAggregationIntervalSeconds`：`5..3600`；`statsAggregationBatchSize`：`100..10000`；`statsAggregationMaxBatchesPerRun`：`1..100`；`usageHotWindowRefreshIntervalSeconds`：`60..3600` |
| 分组与系统监控 | `groupAccountStatsRefreshIntervalSeconds`、`systemMetricsSampleIntervalSeconds`：`5..3600`；`tableMonitorMaxTablesPerRun`：`0..100` |
| 账号质量与测试 | `accountQualityRefreshIntervalSeconds`：`60..3600`；`accountQualityWindowMinutes`：`1..60`；`accountTestTaskConcurrency`：`1..1000` |
| 账号健康检测 | `accountHealthCheckIntervalHours`：`1..168`；`accountHealthCheckJitterMinutes`：`0..1440`；`accountHealthCheckBatchSize`：`1..100`；`accountHealthCheckFailureThreshold`：`1..10` |
| 冷却复测 | `cooldownAccountRetestIntervalSeconds`：`1..3600`；`cooldownAccountRetestBatchSize`：`1..100`；`cooldownAccountRetestMaxBackoffHours`：`1..720`；长期不可用阶段固定每 1 小时复测，不提供间隔设置 |
| OAuth 刷新 | `oauthAccessTokenRefreshIntervalSeconds`：`10..3600`；`oauthAccessTokenRefreshLeadSeconds`：`60..86400`；`oauthAccessTokenRefreshBatchSize`：`1..200`；`oauthAccessTokenRefreshRetryBackoffSeconds`：`0..86400` |
| 数据保留 | `modelCheckRetentionDays`：`1..365`；`runtimeLogIndexRetentionDays`：`1..90`；`publicApiLogRetentionDays`：`1..365`；`usageRecordRetentionDays`：`1..180` |
| 统计时区 | `usageStatsTimezone`：GET 返回当前非空 IANA 时区；PostgreSQL 在线 PATCH 禁止提交 |
| 统计明细保留 | `usageStatsMinuteRetentionHours`：`1..336`；`usageStatsHourlyRetentionDays`：`1..180`；`usageStatsDailyRetentionDays`：`1..800`；`usageStatsWeeklyRetentionWeeks`：`1..260`；`usageStatsMonthlyRetentionMonths`：`1..60` |
| 快照与指标保留 | `usageRankSnapshotRetentionDays`：`1..365`；`systemMetricsRetentionDays`：`1..7`；`systemMetricsHourlyRetentionDays`：`1..30` |

## 存储与事务

- GET 使用固定白名单 `IN (...)` 和 `ORDER BY key ASC`，读取行数上界等于白名单长度。
- PATCH 在单个 PostgreSQL 事务中锁定全部当前白名单行，锁顺序固定为 `ORDER BY key ASC FOR UPDATE`。
- 锁定快照必须先完成完整性和当前值校验，再按稳定 key 顺序只更新请求实际提供的字段。
- `value_json` 保存标准 JSON；整数与 decimal 字段保存 JSON number，统计时区保存 JSON string。
- decimal 接受合法 JSON 数字的整数、小数和指数形式，并规范化为普通 JSON number；例如 `1.0` 与 `1e1` 分别规范化为 `1` 与 `10`。`null`、布尔值、字符串、数组、对象、NaN、Inf 和越界值均拒绝。
- 任一读取、校验、更新或提交失败都回滚，不允许部分字段成功。
- migration `000024_w5_system_settings.sql` 为 fresh PostgreSQL schema 初始化当前 55 项设置。旧流式四字段不再种入，运行时也不做双读双写；部署时按当前 schema 离线同步。migration `000043_w5_remove_gpt_service_tier_multipliers.sql` 继续删除历史计价倍率设置行。
- GPT Priority / Flex 计价只读取模型目录精确档位价格；模型缺少精确档位价格时保持未定价，不再通过系统设置提供通用倍率。
- 当前 schema 之外的数据不做运行时兼容、自动补种或双读双写；缺失 seed 由部署迁移阶段处理。

### 统计时区 seed

- goose 执行 `000024` 时，`usageStatsTimezone` 优先使用 `JUHE_AI_USAGE_STATS_TIMEZONE`；未设置或为空时回退 PostgreSQL `TimeZone`，仍为空时回退 `UTC`。
- PowerShell 执行 migration 前应显式设置部署使用的 IANA 时区，例如 `$env:JUHE_AI_USAGE_STATS_TIMEZONE = 'Asia/Shanghai'`；示例值必须替换为实际部署 IANA 时区。
- Node / Go 共存期，seed 时区必须与 Node `Intl` 运行时实际使用的部署时区一致，避免统计自然日、周、月窗口在两个运行时之间错位。
- PostgreSQL 模式在线禁止修改 `usageStatsTimezone`；需要调整时必须停机，通过离线 migration / 统计窗口重建流程处理。

## 写后副作用

PostgreSQL 提交成功后执行以下 best-effort 副作用，任何副作用失败都不能把已提交业务写误报为失败：

1. 写 Node 兼容 `settings:system` shared cache version，使 Node settings repository 丢弃旧 Redis shared cache。
2. 发布 `gateway_runtime_cache` 失效事件，reason 固定为 `settings_updated`，使 Node gateway 清理运行时设置缓存。
3. 通过统一管理 operation log 入口入队 `settings.update`：
   - `module=settings`
   - `action=update_settings`
   - `resourceType=system_settings`
   - `resourceId=system`
   - `resourceName=系统运行设置`
   - `summary=更新系统运行设置`
   - `visibilityScope=all_users`
   - `detailLevel=summary`
4. `changes` 只包含本次提交且实际变化的字段；成功但值未变化时仍入队，`changes=[]`。
5. decimal 设置在 operation log changes 中保持 JSON number，不序列化为字符串。
6. changes 继续受 `operationLogMaxChangesPerRecord`、字符串 / 序列化值截断和 `__truncated__` 规则约束。

## 验证矩阵

| 层级 | 必须覆盖 |
| --- | --- |
| catalog / service | 53 项白名单与 Node/前端/fresh seed 一致；整数全部边界值；未知字段；错误 JSON 类型；空更新；PostgreSQL 禁止在线修改 `usageStatsTimezone`；通用 decimal infrastructure 保留但当前 catalog 不暴露倍率设置 |
| PostgreSQL store | 有界读取；稳定行锁；缺失 / 重复 / 未知 / 非法当前行；局部更新；稳定 key 顺序；事务失败不返回部分快照 |
| HTTP | admin / super_admin 成功；普通 user 403；GET 不 touch；PATCH touch；read/write limiter；strict JSON；通用 500；精确完整 DTO |
| 副作用 | `settings:system` cache version、`settings_updated` runtime invalidation、`settings.update` operation log；三者失败均不覆盖业务成功 |
| integration | fresh migrations、真实 PG/Redis、真实管理 session、HTTP GET/PATCH、DB 更新、session touch、两个 Redis 失效 topic 和 operation log payload |
| Node / 前端对照 | Node settings management driver、performance system API smoke、前端 settings 类型与 payload 回归 |

当前非容器测试已覆盖 catalog / service / store / HTTP / router / app 装配边界；真实依赖 smoke 与 loopback listener smoke 已有独立记录。Docker/testcontainers `SKIP` 只记录为未执行，不计真实 PostgreSQL / Redis / HTTP 通过。

## 2026-07-20 真实 listener 写 smoke 证据

- 提交 `03bafaa91` 新增真实 Go listener 写 smoke，提交 `73e389051` 合并远端 feature 最新 Redis 运行时角色隔离改动。
- `pnpm test:plan0081-real-go-settings-write-smoke` 本地 regression 已通过：远程 HTTPS 目标在任何 fetch 前拒绝；Go `/__aisys__/api/readyz` 必须返回 `200`、`success=true`、`status=ok`、`service=juhe-ai-go` 和 `Cache-Control: no-store`；readyz 请求不发送管理 Cookie；断连、无 header、非法 JSON、恢复失败、redirect 和第三方并发修改等边界都验证 finally 恢复行为。并发值不等于原值或 smoke 临时值时，脚本失败且不覆盖第三方值。
- 修正后的 `TestW5ManagementSystemSettingsPostgresRedisAsynqSmoke` 已在远端 Docker `29.1.3` 上使用 fresh PostgreSQL `18-bookworm`、Goose schema `61` 和三个独立 Redis `8.2.7-bookworm` 容器强制执行为 `PASS`，不是 `SKIP`；测试内再次调用 Go 配置校验确认三个 URL 的规范化 `host:port` 两两不同，全部容器与 Docker socket tunnel 已清理。
- `pnpm smoke:plan0081-real-go-settings-write` 已在隔离 PostgreSQL `18-bookworm`、Redis `8.2.7-bookworm`、Goose schema `58`、真实 Go `server` listener 和隔离管理员 session 上执行，输出 `settingsWriteChecked=true settingsRestored=true`；直接 PostgreSQL 复核 `systemMetricsHourlyRetentionDays=30`，session 已 touch。该 listener smoke 未启动 ingest worker，因此不把 operation log 入库作为本次证据。
- 临时 Go 进程、二进制、SSH tunnel、端口和远端容器均已清理；该证据不覆盖浏览器页面、生产单 owner、反向代理切流、生产回滚或 Node 删除。

## 明确排除

- 本切片不修改系统设置页面的信息架构；字段集合只同步 Node 当前 53 项契约。
- 本切片不迁移尚未进入 Go owner 的网关 `serviceTier` 选择或计价执行逻辑，也不增加 usage 字段。
- 本切片不在线迁移统计时区，也不附带统计窗口重建脚本。
- 本切片不迁移 client IP allowlist 管理、System API allowlist bypass 或 authenticated 404 限流语义。
- 本切片不完成前端连接真实 Go 后端 smoke、反向代理单 owner 切流、生产回滚演练或 Node settings 删除。

## 删除门禁

只有在 `GET/PATCH /settings/global` 与 `GET/PATCH /settings` 均完成真实依赖 smoke、前端真实 Go 后端验证、生产单 owner 切流、回滚验证和 Node 静态删除检查后，才能评估删除 Node settings route / repository。单独完成 Go opt-in 代码和非容器测试不能进入 `待删除 Node`。
