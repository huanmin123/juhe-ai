# W10 网关上游请求与流式中转核心迁移记录

## 范围

本批只迁移网关传输层的可复用 Go 核心，不注册生产路由，也不改变 `deploy/owner-manifest.json` 的 gateway owner。实现位于：

- `backend-go/internal/modules/gatewayupstream`：从已解析的请求形状、候选账号、真实资源协议和凭据构造有界 `http.Request`。
- `backend-go/internal/modules/gatewaystreamrelay`：在已建立的 source/sink 之间执行同步、可取消、有界的流式中转。
- `backend-go/internal/modules/gatewaydispatch`：把一次已选候选接到注入的 `http.Client`，提供响应 body 有界读取/关闭和 relay 适配；不持有候选、retry 或 owner 状态。
- `backend-go/internal/modules/gatewaycodexresponses`：把 Codex Responses JSON / stream contract 作为可选检查 seam 接入；流式拥有有界完整帧预提交检查与 R0 ID 字节重写，非流式拥有有界 JSON R0 修复，不拥有账户、候选、retry 或生产路由。
- `backend-go/internal/modules/gatewayresponse`：取得一次 dispatch response 的 body 所有权，实际组合 `ReadBody -> JSON guard -> downstream sink` 或 `NewInspector -> Relay`，并返回 typed retry、usage/audit 和 commit handoff；不持有候选列表、账户策略或 HTTP listener。

## Go 原生决策

- 不复制 Node 的隐式 header / body 传播。请求构造器只复制通过校验的语义 header，清除 hop-by-hop、客户端认证、代理、追踪和内部路由 header，并在 Go 的 `http.Request` 上显式设置 host、content length、context 和可重试 body。
- 不让请求构造器负责 SSRF、代理选择、重试或 response inspection。候选资源协议决定 URL 拼接和认证位置，网络策略、retry planner 和协议 inspector 保持独立接口，避免把未迁移的 Node 运行时耦合进核心。
- 不使用后台 goroutine 搬运流。relay 让一次成功 sink write 完成后才读取下一块 source，直接把客户端取消和背压传入 source/sink；这样不会因 detached goroutine 造成连接泄漏或无界 channel。
- 64 MiB body/stream cap、idle timeout、total timeout 和 bounded inspector handoff 都是 Go 侧的硬边界；首字节已写出后 `RetryAllowed=false`，客户端取消不归因到账号上游。

## 契约覆盖

- OpenAI-compatible、Anthropic Messages、Gemini native、OpenAI OAuth/Codex 和授权资源协议的 URL / auth 选择。
- body 所有权、`GetBody`、content-length、敏感 header 清洗、非法 URL/credential/header、Gemini key query 清理和 stream `alt=sse`。
- 流式成功、首字节前上游超时可重试、客户端取消不可重试、首字节后失败不可重试、sink 背压、部分写、64 MiB 超限、缺终态、终态失败和 usage/audit 终态交接。
- dispatch seam 覆盖 transport error 保真、非 2xx 响应透传、body 超限、close/read error 可 `errors.Is` 识别、HTTP status 到 usage handoff 的传递和 response body 必须关闭。
- response handler 覆盖显式 raw-upstream / gateway-bridge checkpoint、JSON safe/strict/shadow、SSE keepalive、partial write、initial commit、协议首字节前换候选 handoff，以及完整 HTTP 失败响应默认透明转发。只有调用方显式标记 `explicit_policy` 时，401 / 429 / 5xx 才进入 Go 状态/错误分类，避免 generic vendor 响应隐式轮 Key。这里的 `explicit_policy` 只是分类 opt-in，不等于 Node `decideAccountErrorPolicy` 已命中的 `retry_next / skip_account` 动作；真实账户策略尚未迁移，后续必须用独立 typed action 接线。

## Go Codex Responses contract 批次

- `backend-go/internal/protocols/codexresponses` 已新增纯 Go contract registry、JSON validator 与 stream state：登记 17 类当前 Node item、ID 前缀 / event stage / 字段类型、工具调用与输出关联、`request_history` 外部历史例外、unknown / R0 / R2 分类、32 条有界诊断、流式 `added -> delta -> done -> completed` identity consistency 和 `CommitState` / late-violation 语义。
- 这批按 Go 方式实现为无副作用校验包，不复制 Node 的 repair executor、账户策略、SQLite history sanitizer、SSE interceptor 或 HTTP owner。`timeout_ms` 遵循 JS safe integer 上限；JSON trailing value、数组 / 对象类型和 tool correlation 在入口处 fail-closed。
- `gatewaycodexresponses.Inspector` 已作为 relay 可选 transform inspector 接线：shadow 保持 byte-for-byte 透传；strict 与 safe repair 在任意 chunk boundary / EOF pending 下先缓冲完整 SSE frame，再决定下游提交。safe repair 只执行 contract 明确允许的 R0 `item.id` / `item_id` / `response.output[index].id` 重写，added / delta / done / completed 复用同一随机 client ID；R2、生成 ID 超限 / 冲突或重写失败均 fail-closed。被修复帧保留原 `event/id/retry/comment/extension` 行和换行形式，只替换 data JSON；未修复帧原字节透传。
- `gatewaycodexresponses.InspectJSON` 已补齐非流式 guard：先检查 response / compact envelope，再复用 Go validator；shadow / strict 保留原始 body，safe repair 只在未语义提交且 provenance 为 `raw_upstream` / `gateway_bridge` 时替换 output item 的 R0 ID，使用随机、有界、避免碰撞的 typed ID，重编码后再次校验。R2、unknown、request-history 修复、生成失败、超限或 post-validation 失败均不改 body 并 fail-closed；结果携带有界诊断、`repaired_safe` / `repaired_bridge`、rule ID 和 Go `CommitState` retry 语义。Go 采用结构化 JSON 重编码，保证未知字段的语义保真，不承诺 clean 路径字节重排。
- `gatewayresponse.Handler` 已把上述 guard 接入可调用的 response runtime seam。typed checkpoint 无公开字段，调用方只能通过 raw-upstream / gateway-bridge 构造器声明来源；2xx strict / safe 协议失败通过 `gatewayretry.ResponseSignal` 驱动下一候选，完整 HTTP 响应则通过 `ResponseDisposition` 默认透明写入下游。`explicit_policy` 当前只开启 Go classifier，不冒充 Node 账户策略动作。OpenAI-compatible 非流式 usage 使用与 SSE 相同的字段归一化，guard handoff 只保留有界 outcome / rule / diagnostic code，不复制正文。
- Go 有意比 Node 收紧 transport-only retry：只要任意下游字节已经写出，relay 就禁止换号；`late_violation` 仍按 semantic commit 标注。该差异避免 heartbeat 已暴露后重复流，但可能牺牲 Node 允许的 heartbeat 后透明重试，生产 owner 接线前不得写成完全等价。
- 该接线不代表已注册 Go listener、接管 Codex Responses production owner 或删除 Node firewall；仍需真实 gateway-bridge marker、账户策略读取、usage/audit explanation、上游 smoke 和 canary / rollback 证据。

## 2026-07-23 gatewaycandidatewindow 首轮迁移

- Go 新增 `gatewaycandidatewindow` 组合层：一次候选投影最多扫描 512 行，再按最多 256 行一批调用 hydrator；hydrator 报告的损坏/缺失项会继续从已扫描窗口补位，最终最多输出 256 个候选。当前完成的是组合契约和批次控制，具体 PostgreSQL hydrator 尚未接入。
- 扫描输入增加 `requestedModel/endpointFamily`。PostgreSQL 仍保持单条有界查询、无 `OFFSET`，按 direct / 有效非 identity mapping / unrestricted 三档排序并排除 restricted 不匹配账户；mapping 必须精确匹配 provider、model 和 endpoint family，model 或 endpoint 至少一项真实变化，受限账户的 upstream model 必须在支持列表中，避免错误映射或普通候选过多导致模型候选落窗。
- hydrated 候选不把明文凭据放入公开 DTO；后续具体 hydrator 负责批量解密与批量读取模型、代理、API-key runtime 和新鲜质量事实。最终排序使用模型匹配、fallback/super/priority 业务桶，再在同桶内使用可选质量分，最后按名称 / ID 稳定收敛。基础候选 SQL 不依赖质量统计表，避免陈旧质量分和统计表故障阻塞请求热路径。
- 诊断固定记录 `scanLimit/finalLimit/candidateRowCount/scannedRowCount/eligibleRowCount/hydrationBatchCount/hydratedAccountCount/hydrationDroppedCount/finalAccountCount/scanLimitReached`，不记录凭据、请求体或上游正文。该模块仍是非 lease 的候选窗口，不执行 dispatch claim、账户策略动作、circuit、retry 或生产 listener 接管。
- 组合层集中验证已覆盖正常首批、256 行失败后的补位、direct / valid mapping / unrestricted / restricted 模型边界、hydrator 协议错误和依赖错误；具体 PostgreSQL hydrator 由下一节记录，Redis circuit、上游请求循环、切流与 Node 删除仍是后续门禁。

## 2026-07-23 PostgreSQL 批量 hydrator

- `gatewaycandidatewindow.BatchHydrator` 已实现具体 hydration：先为最多 512 行投影按 view account 单次批量读取新鲜质量，结合 SQL 显式 `model_rank` 在 hydration 前完成 model/business/quality pre-rank；同一份 rank facts 随后传给每批最多 256 行的 hydration，不重复读取质量。hydration 选择授权资源账户、解密凭据、计算 API-key HMAC fingerprint，并批量读取账户模型/启用映射、代理和 key runtime。正常首批成功时仍只 hydration 一批，不存在逐账户查询。
- PostgreSQL account facts 使用 `unnest(text[])` 保持请求账户集合，模型和映射以子查询聚合为每账户 JSON；代理使用 `ANY(text[])`；质量按展示候选账户 ID 读取严格的 text ISO `last_sample_at >= now-24h`，其他事实按真实资源账户 ID 读取。基础候选 SQL 不 JOIN 质量表；纯 Goose 库尚无 Node 质量表时，undefined relation/column 明确降级为无质量，不阻塞候选热路径。
- 授权实例统一使用真实资源账户的凭据、模型、代理和 key runtime。账户密文损坏、空凭据、API key 缺失/超限以及 OAuth/Google OAuth 同时缺少 access/refresh token 均作为有界候选级 drop reason 返回并由窗口补位；API-key pool 保留原数组 index，数组存在但清洗后为空时不回退单 key。代理缺失、停用、配置非法或密码解密失败保留候选并标记 unavailable，空密码允许无认证代理，`socks5` 规范化为 `socks5h`。其他批量事实或 runtime 查询失败则整批返回 infrastructure error。
- 解密后的账户和代理凭据放入 `CredentialSet`：字段无导出、JSON 明确忽略，普通字符串格式固定为 `[REDACTED]`，只有后续受信 request assembler 显式按键读取。API-key handoff 只携带 fingerprint、index、status、cooldown 和 next probe，不复制明文 key。
- 本批仍未注册生产 listener，也未接 account policy、dispatch revision/circuit、claim、attempt loop 或 owner manifest；具体 hydrator 可被候选窗口调用，不等于真实请求循环或生产网关接管。
- 本批集中验证通过：`go test ./internal/modules/gatewaycandidatewindow ./internal/modules/gatewayaccountcandidates ./internal/store/postgres -count=1`、同范围 `go test -race`、`go vet`，以及候选 SQL / hydration SQL 守卫；未启动真实 PostgreSQL、Redis 或 upstream，因此不记录真实依赖 smoke 通过。

## 2026-07-23 account policy 与 candidate attempt loop

- 新增 `backend-go/internal/modules/gatewayattemptloop`：消费已经 hydration 的候选，按候选业务顺序和 API-key runtime 顺序执行有界 attempt；同一账户的 key-scoped failure 可先换下一个 key，显式 `retry_next/cooldown/disable` policy action 才切换账户或调用注入的 `PolicyApplier`。
- 账户错误规则按 Node 的 `error_handling_rules` 结构进行 bounded normalize：priority、status/error code/type/keyword 全匹配、duration/daily/weekly reset、2xx 禁止作为匹配条件；未配置或未命中的规则返回 `none`，不会把普通 401/429/5xx 自动改成账户状态。
- loop 强制 `maxAttempts <= 512`、每请求最多 4 个 distinct account、wall deadline <= 24h，并把每次 first-byte budget <= 1h 传播给 executor；`gatewaydeadline.Controller` 在 dispatch 前冻结 attempt 级绝对 deadline，通过真实 request context 同时约束 response headers/body，JSON 与 stream 在首个下游响应头或正文可见后停止该 timer，再由 relay idle timeout 接管。重放不再接受裸布尔放行：`protocols/gateway.ClassifyReplay(RequestShape)` 按精确 method/path 只允许 account-independent 模型读取和经请求事实证明可执行有界业务重试的推理操作；Responses 必须明确解析 `store=false`，Chat 允许缺省 / `null` / `false`，非法、重复、超限和未扫描均 fail-closed，资源型/未知请求默认拒绝。unsafe 请求即使命中 cooldown/disable 也只应用当前账户 mutation，不进入下一个账户。attempt executor 必须声明 `RetryAllowed`、`Committed`、`KeyScopedFailure`，一旦 downstream/semantic committed 或客户端取消，loop 不再换候选。usage/audit terminal handoff 原样保留在每次 `AttemptSummary` 与最后一次 `AttemptResult`。
- `HTTPExecutor` 已具体组合 `gatewayupstream -> gatewaydispatch -> gatewayresponse`：调用方只准备一次 attempt 的请求和 response 模板，adapter 接管 context、dispatch result、typed retry/commit、bounded error facts 与 usage/audit handoff。非 2xx body 读取后由 resolver 复用 typed account policy：命中规则才进入 `explicit_policy`，未命中保持透明转发；同一次 attempt 的 `PolicyDecision` handoff 给 Service，避免以不同 clock/settings 二次计算；Service 仍独占 retry action 和 mutation。
- protocol facts 采用 bounded JSON 解析：nested `error` 优先，支持字符串/数字 code、Gemini `status` fallback 和 Anthropic nested type；这只负责规则匹配，不复制 Node 各供应商的完整诊断对象或 header 画像。
- API-key candidate 每个账户每个请求最多尝试 2 个可用 key；这是 Node 当前请求级放大上限的 Go-native 硬边界。round-robin / weighted-round-robin 的跨请求共享游标和 Redis runtime 仍未接入，当前仅保留 hydration/runtime 顺序作为临时 selector，不宣称与 Node 的跨请求分布等价，后续 owner 接线时替换。
- 本批不复制 Node 的 SQLite/DB-service、副作用写入或 listener；生产 request preparation、`PolicyApplier`、dispatch revision/circuit、Redis runtime、usage/audit queue 和 owner 切换仍未接生产。downstream staging/commit 已形成可组合 Go 核心，但尚未由生产 listener 持有。
- 集中验证覆盖 policy priority/matcher/reset、动态透明/显式 response disposition、nested protocol facts、精确 replay classifier、unsafe policy mutation 不跨账户、单账户双 key 上限、凭据原始 key index、显式 retry-next、cooldown mutation、每次 attempt summary 的 usage/audit、attempt 级 deadline 对 response headers 与 JSON body 的真实 context 取消、commit/non-retry stop、max attempts、cancellation 和 result invariant；真实 upstream/PG/Redis smoke 继续后置。

本次 deadline/replay 增量集中验证通过：

```powershell
go test ./internal/modules/gatewaydeadline ./internal/protocols/gateway ./internal/modules/gatewayattemptloop ./internal/modules/gatewaystreamrelay ./internal/modules/gatewayresponse ./internal/modules/gatewaycandidatewindow ./internal/modules/gatewayretry ./internal/modules/gatewaydispatch -count=1
go test -race ./internal/modules/gatewaydeadline ./internal/protocols/gateway ./internal/modules/gatewayattemptloop ./internal/modules/gatewaystreamrelay ./internal/modules/gatewayresponse ./internal/modules/gatewaycandidatewindow ./internal/modules/gatewayretry ./internal/modules/gatewaydispatch -count=1
go vet ./internal/modules/gatewaydeadline ./internal/protocols/gateway ./internal/modules/gatewayattemptloop ./internal/modules/gatewaystreamrelay ./internal/modules/gatewayresponse ./internal/modules/gatewaycandidatewindow ./internal/modules/gatewayretry ./internal/modules/gatewaydispatch
git diff --check
```

## 最新 Node 漂移审计

远程 `master=1761c84d4` 在既有 Codex firewall 和账户路由生命周期之上继续收敛模型目录快照退场，并修正人工账号测试诊断：诊断优先保留最后一次真实上游 response body/headers，只有上游正文为空才回退客户端下游错误。该诊断调整不改变 gateway retry 语义，但提醒 Go handoff 必须区分 upstream raw diagnostic 与 downstream transformed/repaired payload。Go 本批已同步响应侧 typed signal / disposition、keepalive 和有界 handoff，但尚无 dispatch revision fan-out、Redis circuit、热质量、同层探索、公共 deadline、路由观测或真实请求循环，不能把 response seam 写成 gateway owner 接管。

最新 Node 已删除 model-catalog-snapshot rebuild/reconcile/DB-service bridge；本轮已把 Go management provider model 改为直接使用 dynamic catalog/cache invalidation，删除 model-specific bridge/readiness/timeout，保留 `NodeInternalBaseURL` 仅供账户健康与 account-test 过渡路径使用。历史 PostgreSQL snapshot 表/migrations 暂留，不作为运行时 owner。

Go 下一步应补真实 HTTP marker/account policy 读取、生产 timeout settings/lane 到 deadline config 的接线、downstream header commit owner、dispatch revision fence、Redis circuit/hot-quality/exploration、listener 接线和真实 upstream smoke；不能照搬 Node 的 SQLite/DB-service 或内存单例。直到真实请求循环、Redis/PG 状态、canary 和回滚证据完成，Node gateway 继续是唯一生产 owner。

## 2026-07-24 downstream commit 与请求元数据批次

- 新增 `gatewaydownstream.HTTPWriterSink`，以 `Stage -> Commit -> Write -> Snapshot` 管理响应状态。`WriteHeader` 本身即 transport commit；即使 flush 或首个正文写入失败、正文为零字节，也禁止跨账户重试。JSON 在 guard 后计算最终长度，透明失败响应保留状态和业务头，显式账户策略在提交前返回，SSE 延迟到首个输出或空流完成才提交。
- 响应头使用静态 hop-by-hop / 内部供应商 denylist、动态 `Connection` token 和 128 字段 / 64 KiB 上限；提交时先清除 writer 上已有的非网关自有头，避免 middleware 残留 `Set-Cookie`、旧 `Content-Length` 或传输头。网关自有 request/trace/timing/CORS 头采用严格 allowlist 保留。
- Go 不照搬 Node 总是删除 `Content-Encoding` 的行为。标准 `net/http.Transport` 自动解压时已移除该头；自定义 transport 返回仍编码的 opaque 失败正文时，Go 保留端到端编码头并以实际缓冲字节重算长度。JSON/SSE 因需要解析或流式规范化，只允许无编码或 `identity`；仍带 gzip/br 等编码时 fail-closed，不能删除 header 后把压缩字节误当结构化正文。
- `protocols/gateway.ScanRequestMetadata` 对原始 JSON 做一次有界完整扫描，区分 unscanned、absent、explicit false/true、null、invalid 和 scan-limit-exceeded；重复 `store`、非法类型、非法 JSON/UTF-8、trailing value 和超限全部拒绝重放。扫描结果已接入 typed `RequestShape` 与 replay classifier，但生产 listener/body admission 尚未迁移，不能描述为生产解析已接线，也不得在每次 attempt 重复扫描。
- 本批仍不注册 Go gateway route、不修改 owner manifest。HEAD 的无正文语义须由未来 listener 向响应计划传递 method fact；该门禁完成前不能把 downstream adapter 描述为生产 owner。

## 2026-07-24 account policy PostgreSQL writer 批次

- `gatewayattemptloop.PolicyMutation` 已从整份 hydrated candidate 收敛为脱敏 typed facts，只携带 target/source 身份、target status、双 `config_revision/dispatch_revision`、规则动作、有界 reason/trace 和时间；凭据、代理、模型、API-key runtime、上游正文及原始错误 message 不进入 writer。调用方必须提供稳定 `MutationID`，attempt 层以绑定账户、分组、授权、双 revision、status、action 和 rule 的 canonical SHA-256 生成有界 transition，支持 outbox 幂等重放。
- `PolicyApplier` 返回 `applied/idempotent/stale_target/stale_source/ineligible`，并把结果保留在 attempt summary。外部 executor 注入的非法 decision 在写库前 fail-closed；writer 基础设施错误不再返回空 Result，而是保留已经发生的 attempts、usage、audit 和 LastAttempt，并停止跨账户重放。unsafe 请求仍保持“先尝试写当前账户明确 mutation，再禁止进入下一候选”的原有安全顺序。
- 候选 PostgreSQL projection 已增加 target `dispatch_revision` 与 resource source 的 `config_revision/dispatch_revision`。授权实例只修改本地 target instance，同时在事务内锁定并核对物理 source revision、实例三元身份、enabled group binding 和 active/unexpired resource authorization；绝不更新 source 状态或 API-key runtime。普通 source/owner 状态变化会影响全部授权实例，因此同一事务按稳定账户锁推进 source 和所有存活实例的 dispatch revision，并为每行写 `dispatch_revision_changed` outbox。
- `postgres.Store.ApplyGatewayAccountPolicy` 复用既有 schema，不新增 migration：cooldown 写 `rate_limited/temporary_unavailable + schedulable=true + typed cooldown_until`，重置 retest/stream 诊断；disable 按 Node 业务语义写 `status=error + schedulable=false + upstream_failure`。Go 有意修复 Node 临时不可用固定 3 秒、状态写入与 stats/cache 分离、无 revision CAS 的竞态：它 honor typed decision TTL，并在一个事务内完成 target 状态 CAS、family revision、`account_circuit_outbox` 和 direct/authorized group stats dirty。
- outbox 契约与 000072 一致：`projection_key=account_circuit_runtime_v1`、`dedupe_key=dispatch:<transition>`、裸 account ID runtime key、pending 和 Unix 毫秒。该 writer 批次当时只完成 producer；后续 dispatch-revision consumer/projector 见下一节。提交后本地快速失效与真实 PostgreSQL 并发 smoke 仍未迁移，因此不能宣称 production policy owner 已接管。
- `retry_next` 不写数据库；普通 gateway success 不在本 writer 清 cooldown/error；账户恢复和 cooldown retest 继续属于独立 worker。账户级 explicit policy 不写 `account_api_key_runtime_states`，key-scoped success/failure 的乱序保护 writer 是后续独立批次。

## 2026-07-24 account circuit outbox revision projector 批次

- Go 已新增 outbox port、PostgreSQL consumer、Redis revision projector、有界消费 service 和默认关闭的 app worker seam。本批只领取 `projection_key=account_circuit_runtime_v1` 且 `event_type=dispatch_revision_changed` 的事件，不抢占仍由 Node 处理的 `incident_changed`。
- PostgreSQL 使用单条 CTE + `FOR UPDATE SKIP LOCKED` 原子批量 claim，覆盖 pending due 和过期 processing lease，按稳定顺序最多领取 500 条并生成每事件 fencing token。ACK 在同事务内校验 token、幂等吸收重复 ACK，并以 durable `dispatch_revision` 为栅栏单调推进 `circuit_projection_revision`；失败 release 只允许当前 token 回到 pending。Go 不复制 Node 的 SQLite/DB-service 或逐事件 N+1 claim。
- Redis Lua 对齐 Node 共存键 `juhe-ai:<namespace>:account-circuit:gateway-account-circuit:*`。乱序 revision 返回 stale、重复 revision 仍扫描并修复同 family 遗留旧 state；只有对应 outbox 事件能推进 tombstone，不能从局部较新 runtime state 猜测并跳过其他旧 state。新 revision 关闭旧 state，清理 due、lease、failure 和 escalation，并保留 CLOSED retention。Go 额外维护持久 `dispatch-revisions` tombstone，避免 Redis 暂无 state 时旧事件反向覆盖。
- worker 必须同时显式设置 `Enabled` 和 `GoExclusiveProjectionOwner` 才连接 PostgreSQL/Redis；默认不运行，也未挂到共享 CLI、supervisor 或 owner manifest。因此 Node 仍是生产 projector owner，本批不改变生产所有权。
- Node state hash 没有 runtime-key 索引，共存 Lua 暂时仍需 `HGETALL`。这只适合兼容阶段；Go 接管 state writer 前必须增加 runtimeKey -> scope 索引并移除全表扫描，完成真实 Redis Lua 原子性与容量 smoke。Node/Go 在途旧 revision state writer 还未读取 Go tombstone，生产启用 projector 前必须在所有 state writer 增加 revision CAS fence 或完成同等 gap/rebuild 修复，不能仅凭 exclusive projector 开关切 owner。
- `incident_changed`、gap repair、rebuild、CLOSED ledger、cleanup、poison event 退避/隔离、lease renewal、hot quality、提交后本地 invalidation 和 cooldown recovery 仍待后续迁移。Node cleanup repository 未接生产 scheduler 的生命周期缺口也需单独修复。

本批完成标准是：只领取已支持事件；claim/lease/token/ACK/release 契约闭合；Redis revision 单调且兼容 Node key；worker 默认关闭并有独占门禁；集中定向 normal/race/vet 与独立复查通过。真实 PostgreSQL/Redis smoke、生产接管和 Node 删除不属于本批完成证据。

集中验证已通过：`gatewaycircuitoutbox`、`platform/redis`、`store/postgres`、`app` 的普通 `go test`、`go test -race`、`go vet` 与 `git diff --check`。独立复查确认修复 family revision tombstone、实际失败时刻退避和 worker 启动前参数验证后无剩余 P1/P2；本轮未执行全仓、Docker 或真实 PostgreSQL/Redis。

## 2026-07-24 account circuit incident 精确投影与 rebuild 批次

- Go outbox consumer 已扩展到 `incident_changed`，但仍只接受 000072 的两种固定 event type。claim 继续使用单条 CTE、稳定顺序、lease reclaim 和每事件 token；service 按 event type 显式分派 revision/incident projector，不把 incident 误送到 revision Lua。Incident projector 把实际恢复的 incident ID/ledger 交给 ACK；非 obsolete ACK 必须在同事务把对应 watermark 更新一行，否则 outbox 一并回滚。只有明确旧 dispatch 的 obsolete event 可无 watermark ACK。
- PostgreSQL incident projector 改为按 `circuit_scope_key` 精确 JOIN 当前 account revision，不复制 Node 每条 event 全量分页再 `find` 的 `事件数 × 全账本` 路径。当前 dispatch snapshot 才进入 Redis；旧 dispatch event 返回 obsolete/stale 并 ACK，解决 Node rebuild 过滤旧 revision 后永久 release 的 poison。当前 ledger 比 event 更新时投影最新 snapshot，旧 event ACK 不会越过自身 ledger watermark。
- startup rebuild 按 `(updated_at_ms, circuit_scope_key)` keyset 分页，只读取当前账户 dispatch revision 下的 active 或 retention 内 CLOSED incident；分页、页数和容量均有硬上限。worker 在首次 claim 前必须完整 rebuild，任一读取、restore、容量或 cursor 错误都 fail-closed；默认仍 disabled 且要求 exclusive owner。
- Redis incident restore 使用 `dispatch revision -> per-scope ledger revision -> generation -> updatedAt` 单调顺序，先读 account revision tombstone。ledger tombstone 绑定 dispatch 与 transition incarnation：新 dispatch 可重置；同 dispatch 只有 state 已不存在、ledger 从 1 新建且 transition 已变化时才允许 cleanup 后的新 incarnation 重置。旧 revision 高 generation 不得覆盖新 revision，同 dispatch generation 回退或 runtime generation 高于 durable 都返回 ledger conflict 并拒绝 ACK。restore 执行容量检查，过期 CLOSED 在容量判断前收口，CLOSED retention 使用 durable deadline；不复制 Node restore 永不返回 capacity-exhausted 的缺陷。
- restore 同一 Lua 维护 `scope-runtime`、`runtime-scopes`、`runtime-accounts`、`account-runtimes`、per-scope ledger tombstone 与 `runtime-index-meta`，并在 idempotent rebuild 时修复 due/closed orphan。写前预验 Redis key type、state generation、连续 string array 和待删除索引；metadata 只保持 `building`，本批不会自行宣布索引 ready。现有 Node transition/escalation/restore/listDue/clear-evidence 写入口尚未共同维护 tombstone/index；evidence-only runtime 的最终反向索引清理也仍依赖统一 writer。生产启用前必须停旧 writer 并排空，或完成所有 writer 的统一 CAS/index 改造与 bounded legacy backfill。
- Node durable CAS 仍允许 generation 回退，且 `incident:<transition>` 幂等 receipt 仍寄存在可 cleanup 的 outbox；这两个已确认缺陷不能照搬。Go incident writer、独立 durable receipt、gap replay、cleanup scheduler、poison quarantine/dead-letter、完整 parent/child/failure/recovery round-trip 和全量 Go runtime transition store继续属于后续批次。

本批完成标准：两类事件 claim/路由闭合；incident 精确读取与 stale dispatch 吸收；Redis restore revision/generation/capacity fence；startup rebuild 在 claim 前 fail-closed；集中 normal/race/vet/diff 和独立复查通过。真实 PostgreSQL/Redis/Node-Go 互操作、索引 ready、生产切流及 Node 删除仍不计完成。

集中验证已通过：`gatewaycircuitprojection`、`gatewaycircuitoutbox`、`platform/redis`、`store/postgres`、`app` 的普通 `go test`、`go test -race`、`go vet` 和 `git diff --check`。两轮独立存储/Redis 复查已修复 due/closed orphan、capacity 顺序、索引删除、ledger/dispatch/transition incarnation、generation fence 和 ACK watermark；未执行真实 PostgreSQL/Redis 或 Node-Go 互操作 smoke。

## 2026-07-24 account circuit runtime state owner 与 index backfill 批次

- Go 新增 typed `GatewayAccountCircuitStore` port：scope、phase、lease、`dispatchRevision int64`、generation、ledger、transition、parent/child relation 和时间均在 Go 类型层表达；Redis adapter 才转换为 Node 兼容的 UTF-8 byte-length scope key、string revision 和 Unix millisecond JSON。Go 不接受 Node `trim()` 后的模糊 identity，也不接受非数值 fallback revision。
- 新 runtime Redis Lua 统一维护 Node 四个兼容源键和 `dispatch-revisions`、`ledger-revisions`、`scope-runtime`、`runtime-scopes`、`runtime-accounts`、`account-runtimes`。所有 transition、lease、canary、escalation、clear-evidence、restore、`listDue` 和 family revision 都先要求 `runtime-index-meta={version:1,status:ready,ownerMode:go-runtime-state-v1}`；`building`/`invalid` 直接 fail-closed，不能借 HGETALL 回退偷偷接管。
- `listDue` 只返回实际可 canary 的 `OPEN/RECOVERING` state。无 lease 的 `SUSPECT` 和 confirmation lease 到期后的 `SUSPECT` 会从 due 删除，等待下一个 request-bound confirmation；这是有意修复 Node stale SUSPECT 永远占满 `ZRANGEBYSCORE LIMIT`、饿死 OPEN/RECOVERING recovery 的问题。half-open/recovery lease 过期仍在同 Lua 内复原 origin 并重新 due。
- scoped/family revision 先读 durable account tombstone，并要求 state/evidence revision 与 tombstone 精确相等；新账户只允许 initial revision `1` 通过 `HSETNX` 建立初始 fence，其他缺失/前后不一致均 fail-closed。capacity eviction/closed retention 只删 runtime payload，不推进或删除 durable dispatch/ledger fence。indexed family revision 从 `account-runtimes -> runtime-scopes` 精确遍历，并处理 evidence-only runtime 的反向索引清理，不复制 Node 全局 HGETALL / 无关坏 JSON 阻断全账户投影的路径。
- `clearAccountEscalationEvidence` 现在验证实际包含的 `evidenceId` 才删除 evidence，修复 Node Lua 接收却完全忽略该参数的问题。未迁移的 durable parent/child receipt 仍是硬门禁：当前 Node/Go incident schema 没有保存 required/recovered child coverage，故这批不能把 Redis parent recovery 写成可重建的生产闭环。
- 新增独立 one-shot `RunGatewayAccountCircuitRuntimeIndexBackfillWorker`。它默认关闭、未挂 CLI/supervisor/HTTP，且同时要求 `GoExclusiveRuntimeStateOwner=true`、`LegacyRuntimeWritersDrained=true`、`RuntimeStateWritesPaused=true` 与 `ControlPlaneWritesPaused=true`。运行时使用每轮随机 epoch 组成 lock token，清空**仅**加法索引 hash，按 states/evidence HSCAN 有界回填，再从 PostgreSQL `accounts.dispatch_revision` keyset 分页 seed 所有账户 tombstone；双向 audit 还要求每条 state/evidence revision 与 durable snapshot 精确相等，最后才原子发布 `ready`。任何 key type、JSON、scope/runtime/account 反向关系、source mutation、lock/epoch、durable revision 或 bound 错误都标记 `invalid`，绝不清 Node 四个源键或错误地发布 ready。
- 当前 Go outbox projector 也增加 runtime-state owner + legacy-writer-drained 双门禁；ready owner 下 durable incident rebuild 通过 typed runtime restore，而旧 compatibility restorer 只允许 `building` metadata。生产 owner、Node 删除、真实 Redis/Node-Go interoperability、parent/child durable transition receipt、gap/cleanup/quarantine、request listener 和 canary/rollback 仍全部未完成。

本批完成标准：typed contract 覆盖完整 runtime 状态机；所有 Go runtime writer 共用 ready/index/revision fence；HSCAN backfill 与双向 audit 仅在 writer drain 下发布；revision projector 无 HGETALL；默认没有新的生产 worker/CLI/owner manifest。真实 Redis 多进程 lease race、Node drain/restore、PostgreSQL durable parent-child receipt、真实 gateway 和生产切流证据不计入本批完成。

集中验证已通过：`store/port`、`platform/redis`、`store/postgres`、`app`、`gatewaycircuitprojection`、`gatewaycircuitoutbox` 的普通 `go test`、`go test -race` 和 `go vet`。两路独立复查修复 lock token/续租、Lua 写前预验、dispatch tombstone seed/exact audit、expired CLOSED capacity、HALF_OPEN origin、clear-evidence、escalation cleanup 和 due fence 后未发现剩余确认的 P1/P2；未执行真实 Redis/PostgreSQL 或 Node-Go 切换 smoke。

## 验证与未完成门禁

本批已执行并通过：

```powershell
Set-Location backend-go
go test -race ./internal/modules/gatewayresponse ./internal/modules/gatewayretry ./internal/modules/gatewaycodexresponses ./internal/protocols/codexresponses ./internal/protocols/openai ./internal/modules/gatewaydispatch ./internal/modules/gatewayupstream ./internal/modules/gatewaystreamrelay -count=1
go vet ./internal/modules/gatewayresponse ./internal/modules/gatewayretry ./internal/modules/gatewaycodexresponses ./internal/protocols/codexresponses ./internal/protocols/openai ./internal/modules/gatewaydispatch ./internal/modules/gatewayupstream ./internal/modules/gatewaystreamrelay
go test -p=1 ./internal/modules/gatewayresponse ./internal/modules/gatewayretry ./internal/modules/gatewaycodexresponses ./internal/protocols/codexresponses ./internal/protocols/openai ./internal/modules/gatewaydispatch ./internal/modules/gatewayupstream ./internal/modules/gatewaystreamrelay ./internal/protocols/gateway ./internal/modules/gatewayusage ./internal/gatewayaudit -count=1
go test ./... -count=1
go vet ./...
pnpm --dir backend test:codex-responses-contract-registry
pnpm --dir backend test:codex-responses-contract-json
pnpm --dir backend test:codex-responses-contract-sse
pnpm --dir backend test:codex-responses-provenance
pnpm --dir backend test:codex-responses-account-policy
pnpm --dir backend test:codex-responses-history-sanitizer
pnpm --dir backend test:response-inspection-policy
pnpm --dir backend test:gateway-generic-upstream-opaque
```

仍未完成：生产 `http.Client` / proxy / SSRF allowlist 配置、Go HTTP listener 路由注册、Anthropic/Gemini response orchestrator、账户策略读取、candidate 请求循环、dispatch revision/circuit/hot-quality/exploration、usage/audit queue 写入、真实 PostgreSQL/Redis/upstream smoke、反向代理 canary、owner 切换和 Node gateway 删除。`gatewayresponse` 已是真实调用 dispatch/guard/relay 的组合层，但尚无生产调用者；上述门禁完成前只能记录为 gateway core/runtime seam migrated，不能记录为 gateway takeover。

## 2026-07-25 空模型约束 fail-closed 同步

- Node `4c2eca221` 已把账户 `supportedModels=[]` 定义为异常约束而非无限制账户。Go 同步移除空列表 fallback：`gatewaymodelcapability` 记录 `invalidModelConstraintCount` 并以 unsupported 排除；`gatewaycandidatewindow` 在无请求模型或空模型列表时不 hydration 后选入；PostgreSQL candidate projection 也不再为无模型行给出 unrestricted rank。
- 这是 Go 读取/候选选择层的安全修复，避免无声明能力账户进入未来请求循环；不新增请求 listener、跨请求公平游标、account policy、API-key observation writer、Redis circuit/hot-quality、side-effect queue 或 gateway owner 切换。

## 2026-07-25 路由协调、hot-quality 与 API Key observation 契约批次

- 新增 `gatewayroutecoordination`：按 `systemAccountID + routeStrategyID + mode + 完整 binding 语义` 计算稳定 SHA-256 revision；对空 scope、重复 binding、超过 20 条、非法权重和无可用 binding fail-closed。`MemoryStore` 在显式单进程/测试场景下用互斥保护共享 round-robin 与 smooth-weighted 状态，每次仅推进一次并返回完整 fallback；它不是多实例生产 fallback，Redis store 与 preflight 组合层仍待后续 owner 批次。
- 新增 `gatewayhotquality` 纯选择契约：先校验同一 route scope，再按 runtime key 首次去重。`cost_first` 不跨越模型/fallback/super-priority/local-priority 业务桶；桶内按健康度、有效可靠性、首字延迟、稳定 binding 顺序与 identity 收敛。`speed_first` 只把标记为 latency-degraded 的候选整体后移。cold 固定中性可靠性 `0.5`，不使用陈旧质量/延迟；NaN/Inf 指标 fail-closed。Redis 采样、same-tier exploration credit、TTL/Lua、attempt/terminal 记账和 listener 接线仍未迁移。
- 新增 `gatewayapikeyobservation` 纯资格契约，显式区分授权实例 `LocalAccountID` 与物理凭据源 `ResourceAccountID`，并同时冻结 target/source 双 revision、已选 HMAC fingerprint/index、绑定身份与观测时间。普通 gateway 成败均 `forbidden`；只有 gateway explicit-policy failure，或白名单 account-health/runtime-recovery/cooldown-retest probe 的 complete-success/upstream-failure 才 `persistent_eligible`，framing-complete-neutral/probe-task-failure 为 `defer_only`。fingerprint 必须是 64 位 hex，避免将原始 API Key 误传入。该包不含明文 credential、不写 `account_api_key_runtime_states`，避免在 Node 仍为 writer 时形成双 writer。
- 集中定向 `go test`、`go vet` 和 `git diff --check` 已通过；未执行全仓、Redis/PostgreSQL/upstream 或 listener smoke。三个包均未注册生产路由、worker 或 owner manifest，Node gateway/worker 继续为唯一生产 owner。

## 2026-07-25 group route plan 编排批次

- 新增 `gatewayrouteplan`，实际串联 `gatewaypreflight.Resolve -> gatewayroutecoordination.Plan -> gatewaycandidatewindow.Load`：只有 preflight ready 才推进一次 round-robin/weighted 状态；每个计划 binding 独立加载一个 group window，输出保留 plan 顺序和 group 边界，绝不把账户候选跨组扁平重排。
- API Key 的 system account、route strategy ID、模式和 binding 的 active/group-enabled/priority/weight 被显式映射到协调快照；未知 mode、`hybrid_smart`（尚缺专用评分/模型重写 planner）、重复 binding、协调器回传未知 binding 和任一 group load 错误都 fail-closed。preflight deny 时不调用 coordinator 或 candidate loader。
- round-robin/weighted 在 preflight 成功后即一次性分配 position，即便后续 group hydration 找不到候选或报错也不回滚；这是有意的 at-most-once 公平语义，防止调用者重复 preflight 夺回共享游标。未来 listener 接入若需要 reservation/commit 模型，必须引入新协调器协议，不能暗中改变该契约。

## 2026-07-25 request-local attempt identity tracker

- `gatewayattemptloop` 在 executor 前按本地 runtime、物理 credential source、protocol-model 和已选 API-key HMAC fingerprint 建立 request-local fence；共享 source 的授权别名和已见 fingerprint 不会再次 dispatch，也不消耗尝试预算。相同 runtime/source 的 key-scoped retry 只允许未见 fingerprint rotation。
- tracker 只由 `Input.Tracker` 显式跨同一请求 leg 传递，默认每个 Run 新建；不写 Redis/PG/诊断，不改变 Node owner。confirmation、semantic retry、same-account reservation、circuit/hot-quality writer 仍未迁移。
- 本服务只能显式注入 coordinator。测试使用 `MemoryStore` 验证语义，但 app 未默认构造它；后续多实例 owner 必须注入 Redis shared store，且 listener 接入时再实现 Node 的最后一组容量等待、物理凭据 attempt tracker 和跨组 fallback 生命周期。

## 2026-07-25 hot-quality 空快照冷启动边界

- `gatewayhotquality.Decide` 将完全缺失的 runtime snapshot 规范化为 `cold / unknown / 0.5`，使尚未产生采样的新候选按固定中性可靠性进入既有业务桶排序；这符合 Go 选择契约的 cold 语义，而非把零值误判成损坏运行态。
- 只有完整零值会被规范化；半填充但不一致的 snapshot 继续 fail-closed。该修复不读取或写 Redis/PG，不引入 attempt 记账、TTL、探索租约或 listener，也不改变 Node runtime owner。
