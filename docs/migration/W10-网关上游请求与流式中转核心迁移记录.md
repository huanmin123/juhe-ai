# W10 网关上游请求与流式中转核心迁移记录

## 范围

本批只迁移网关传输层的可复用 Go 核心，不注册生产路由，也不改变 `deploy/owner-manifest.json` 的 gateway owner。实现位于：

- `backend-go/internal/modules/gatewayupstream`：从已解析的请求形状、候选账号、真实资源协议和凭据构造有界 `http.Request`。
- `backend-go/internal/modules/gatewaystreamrelay`：在已建立的 source/sink 之间执行同步、可取消、有界的流式中转。
- `backend-go/internal/modules/gatewaydispatch`：把一次已选候选接到注入的 `http.Client`，提供响应 body 有界读取/关闭和 relay 适配；不持有候选、retry 或 owner 状态。

## Go 原生决策

- 不复制 Node 的隐式 header / body 传播。请求构造器只复制通过校验的语义 header，清除 hop-by-hop、客户端认证、代理、追踪和内部路由 header，并在 Go 的 `http.Request` 上显式设置 host、content length、context 和可重试 body。
- 不让请求构造器负责 SSRF、代理选择、重试或 response inspection。候选资源协议决定 URL 拼接和认证位置，网络策略、retry planner 和协议 inspector 保持独立接口，避免把未迁移的 Node 运行时耦合进核心。
- 不使用后台 goroutine 搬运流。relay 让一次成功 sink write 完成后才读取下一块 source，直接把客户端取消和背压传入 source/sink；这样不会因 detached goroutine 造成连接泄漏或无界 channel。
- 64 MiB body/stream cap、idle timeout、total timeout 和 bounded inspector handoff 都是 Go 侧的硬边界；首字节已写出后 `RetryAllowed=false`，客户端取消不归因到账号上游。

## 契约覆盖

- OpenAI-compatible、Anthropic Messages、Gemini native、OpenAI OAuth/Codex 和授权资源协议的 URL / auth 选择。
- body 所有权、`GetBody`、content-length、敏感 header 清洗、非法 URL/credential/header、Gemini key query 清理和 stream `alt=sse`。
- 流式成功、首字节前上游超时可重试、客户端取消不可重试、首字节后失败不可重试、sink 背压、部分写、64 MiB 超限、缺终态、终态失败和 usage/audit 终态交接。
- dispatch seam 覆盖 transport error 保真、非 2xx 响应透传、body 超限、close/read error 可 `errors.Is` 识别、HTTP status 到 usage handoff 的传递和 response body 必须关闭。

## Go Codex Responses contract 批次

- `backend-go/internal/protocols/codexresponses` 已新增纯 Go contract registry 与 validator：登记 17 类当前 Node item、ID 前缀 / event stage / 字段类型、工具调用与输出关联、`request_history` 外部历史例外、unknown / R0 / R2 分类、32 条有界诊断和 `CommitState` 首字节后不可重试语义。
- 这批按 Go 方式实现为无副作用校验包，不复制 Node 的 repair executor、账户策略、SQLite history sanitizer、SSE interceptor 或 HTTP owner。`timeout_ms` 遵循 JS safe integer 上限；JSON trailing value、数组 / 对象类型和 tool correlation 在入口处 fail-closed。
- contract 目前是 dispatch 可选接线的基础，不代表已注册 Go listener、接管 Codex Responses production owner 或删除 Node firewall。后续仍需实现 SSE checkpoint / safe repair guard、真实 gateway-bridge provenance、usage/audit explanation 和 canary / rollback 证据。

## 最新 Node 漂移审计

远程 `master=f16aab65e` 新增了 Node Codex Responses 防火墙与历史会话自愈：contract registry、raw-upstream / gateway-bridge provenance、JSON/SSE safe repair、strict intercept、account policy、历史 item ID sanitizer，以及 usage/audit/account 状态解释。Go 当前只有 OpenAI Responses SSE 终态/usage parser、通用 client profile 和 retry planner，没有这套 contract registry 或账户策略，因此本轮不伪造“已同步”。

Go 下一步应先把规则 ID、checkpoint、commit state、R0/R2/unknown 分类和有界诊断抽成独立 protocol contract，再将它作为 `gatewaydispatch` 的可选 inspector 接口接入；不能把 Node 的 safe repair 默认值、账户字段或历史 SQLite sanitizer 原样搬进 Go。直到 Go contract matrix、真实 bridge/provenance 和首字节后 retry 语义完成，Node Codex firewall 继续是唯一生产 owner。

## 验证与未完成门禁

本批已执行并通过：

```powershell
Set-Location backend-go
go test -race ./internal/protocols/codexresponses ./internal/modules/gatewaydispatch ./internal/modules/gatewayupstream ./internal/modules/gatewaystreamrelay -count=1
go vet ./internal/protocols/codexresponses ./internal/modules/gatewaydispatch ./internal/modules/gatewayupstream ./internal/modules/gatewaystreamrelay
go test -p=1 ./internal/protocols/codexresponses ./internal/modules/gatewaydispatch ./internal/modules/gatewayupstream ./internal/modules/gatewaystreamrelay ./internal/protocols/gateway ./internal/modules/gatewayusage ./internal/gatewayaudit -count=1
```

仍未完成：生产 `http.Client` / proxy / SSRF allowlist 配置、Go HTTP listener 路由注册、OpenAI/Anthropic/Gemini protocol inspector 实例化、retry / candidate / quota 组合、usage/audit/operation queue 写入、真实 PostgreSQL/Redis/upstream smoke、反向代理 canary、owner 切换和 Node gateway 删除。`gatewaydispatch` 只是可测试 transport seam；上述任一项完成前只能记录为 gateway core/transport migrated，不能记录为 gateway takeover。
