# Go 迁移测试与验收策略

## 1. 测试目标

迁移测试不是只证明 Go 代码能跑，而是证明没有漏功能、少逻辑、改错权限、破坏统计口径或丢失网关行为。

每个模块都要先固定当前行为，再迁移实现，最后删除旧实现。短期 Node vs Go 对比只能作为测试手段，不能变成长期运行架构。

## 2. 测试层级

| 层级 | 目标 | 示例 |
| --- | --- | --- |
| 单元测试 | 验证纯业务规则、校验、映射和错误语义 | DTO 校验、调度评分、脱敏、模型映射 |
| Store 测试 | 验证 PostgreSQL + Redis 单模式的当前 schema 行为 | 新库初始化、事务、唯一约束、分页排序、Redis TTL、Asynq 队列重试 |
| API 契约测试 | 验证 HTTP 入参、出参、状态码、错误结构、权限和中文文案 | 登录、列表、详情、创建、删除、公开接口 |
| 前端联调测试 | 验证现有 Vue 页面调用 Go API 不缺字段 | 表格、表单、options、空态、错误提示 |
| Worker 测试 | 验证后台任务游标、队列上限、重试、租约和幂等 | 使用记录写入、统计聚合、审计入队、账号测试 |
| 网关测试 | 验证真实中转协议、SSE、调度、重试和副作用 | mock AI、真实上游、取消、backpressure、usage |
| 性能测试 | 验证迁移收益和容量边界 | 管理读接口压测、网关并发、CPU/memory/pprof |
| 删除验证 | 验证 Node 旧实现已清除 | `rg` 旧 route、旧 service、旧测试命令 |

## 3. 单模块验收门禁

每个模块迁移至少覆盖：

- 成功路径：正常创建、读取、更新、删除、触发任务或转发请求。
- 权限路径：未登录、普通用户、管理员、资源所有者、被授权用户。
- 非法输入：缺字段、类型错误、越界、重复、非法状态、非法 scope。
- 空数据：空列表、空详情、空 options、未配置系统设置。
- 错误语义：HTTP 状态、错误码、中文文案、敏感信息脱敏。
- 数据边界：稳定排序、分页、cursor/window、事务、唯一约束和索引路径。
- 存储边界：已迁移模块只能走 PostgreSQL + Redis，不能保留 SQLite runtime、standalone 配置或双模式分支。
- 删除证据：旧 Node 入口、旧测试命令和旧引用已清理。

## 4. 契约固定方式

迁移前为当前 Node 行为建立契约：

- 对 HTTP API 保存请求 fixture、响应字段清单、状态码和错误结构。
- 对存储行为保存 PostgreSQL 初始化 schema、seed 默认值、关键查询、Redis state/cache/queue 和边界数据。
- 对 worker 保存输入队列、游标推进、失败重试和结果表变化。
- 对网关保存 mock upstream 请求、响应、SSE 事件、usage、审计和账号副作用。

契约固定不代表保留旧内部实现。若迁移时确认旧行为本身不合理，可以按当前最优方案调整，但必须更新功能文档、计划和测试预期。

## 5. Go 默认验证命令

后续 Go 代码落地后，默认验证命令按 PowerShell 7 书写：

```powershell
go test ./...
go test ./... -race
go test ./... -run TestName
go test ./... -bench . -benchmem
go vet ./...
```

测试分层命令边界：

| 类型 | 命令形态 | 依赖 | 规则 |
| --- | --- | --- | --- |
| 单元 | `go test ./...` | 无外部服务 | 不得隐式依赖 PostgreSQL / Redis / 真实上游 |
| Race | `go test ./... -race` | 无外部服务或本地 mock | 迁移期默认门禁；发现 data race 不允许降级跳过 |
| 集成 | `go test -tags=integration ./...` | PostgreSQL + Redis | 必须显式读取测试 env，缺少依赖时失败或标记未执行，不能静默跳过关键断言 |
| 网关 E2E | `go test -tags=gateway_e2e ./...` | mock upstream，可选真实凭据 | mock 为必跑；真实上游按凭据可用性记录覆盖范围 |
| 性能 | `go test -run '^$' -bench . -benchmem` 或专项压测命令 | PG/Redis、mock upstream | 产物写入 `reports/`，人工报告写入 `docs/reports/` |

现有前端仍需要保留：

```powershell
pnpm --filter juhe-ai-frontend typecheck
pnpm --filter juhe-ai-frontend build
```

迁移完成前，涉及未迁移 Node 后端的相邻回归仍按现有 `pnpm test:*` 执行。对应模块由 Go 接管后，必须删除或替换该模块的 Node 测试命令。

## 5.1 波次验收矩阵

| 波次 | 必跑项 | 通过标准 | 删除验证 |
| --- | --- | --- | --- |
| W0 Go 与 PG/Redis 基线 | `go test ./...`、`go test ./... -race`、PG schema / seed smoke、Redis namespace / TTL / pipeline / counter、Asynq queue smoke | 新库初始化、Redis cache/state/queue DB 隔离、Asynq enqueue / consume / retry / dead 基础断言通过 | Go 代码无 SQLite driver、SQLite env、standalone / performance 分支 |
| W1a 公开设置读接口 | `GET /__aisys__/api/settings/public` Go 契约测试、PG seed/store 测试、Redis state fixed-window 限流测试、客户端 IP / trust proxy 契约测试、`go run ./cmd/juhe-ai-maintenance w1a-public-settings-smoke`、前端品牌加载 smoke | 匿名 200、`{ data: { appName, appIcon } }`、no-store、Redis 原子 IP 读限流、trust proxy 关闭时使用 `RemoteAddr`、开启时按可信反代链路解析客户端 IP、错误不泄露内部依赖 | 正式切流后删除 Node public settings route/service 入口 |
| W1b `/__aipublic__` 外部维护接口 | Bearer 鉴权、scope、Redis penalty-window 限频、PG auth/log adapter、公开日志 snapshot/Asynq queue/worker runtime、HTTP shell / capture、默认关闭 / 显式开启 router guard、`w1b-public-api-smoke`、public group、public route strategy、public API Key 与 public account 四条纵切面、旧公开路径 404、Node 对照脚本 | 字段、错误结构、分页、业务响应敏感字段不回显、公开日志快照边界、last_used、入队、worker 消费和日志副作用一致；public group 需额外证明目标用户自动创建、provider 校验、同名幂等、并发幂等、默认分组 / 路由策略锁保护；public route strategy 需额外证明目标用户存在性、同名冲突、绑定规则、默认策略保护和 API Key 使用保护；public API Key 需额外证明目标用户必须已存在、路由策略 owner 校验、同名冲突、默认 Key 保护、hash-only secret、完整 key 只在新增响应返回且日志脱敏；public account 需额外证明目标用户 / 分组自动创建、provider profile 校验、凭据可逆加密、凭据不回显、pending_test 直启拦截、软删除和绑定清理；`JUHE_AI_PUBLIC_API_ENABLED=false` 必须保持生产 router 不接管，`true` 必须要求 Redis state、Redis queue 和 `JUHE_AI_SECRET` 后才可灰度；`w1b-public-api-smoke` 通过只证明本地 httptest shell、真实依赖和 worker ingest 链路，不代表正式切流 | 整个 W1b 前缀 Go 接管后删除 Node external integrations route/service/log 写入入口；单个公开资源纵切面、opt-in guard 或 maintenance smoke 完成不删除 Node |
| W2 管理辅助接口 | API 契约测试、权限测试、前端 smoke；当前已实现的 `proxies/options`、`system-accounts`、`system-accounts/options`、`authorization-options/grantee-accounts`、`my-authorization-options/grantee-accounts`、`authorization-options/grantee-teams`、`my-authorization-options/grantee-teams`、`authorization-options/grantee-groups`、`my-authorization-options/grantee-groups`、`providers/options`、`providers/models/options`、`providers/{code}/models`、`route-strategies/options`、`my-route-strategies/options`、`groups/options`、`my-groups/options`、`groups/account-options`、`my-groups/account-options`、`accounts/options`、`my-accounts/options`、`accounts/tags`、`my-accounts/tags`、`DELETE accounts/tags/{tagId}`、`DELETE my-accounts/tags/{tagId}`、`PATCH accounts/{id}/tags`、`PATCH my-accounts/{id}/tags`、`operation-logs`、`operation-logs/{id}`、`my-operation-logs` 和 `my-operation-logs/{id}` 还需 `go test ./internal/modules/managementauth ./internal/modules/managementproxies ./internal/modules/managementsystemaccounts ./internal/modules/managementauthorizationoptions ./internal/modules/managementproviders ./internal/modules/managementprovidermodels ./internal/modules/managementroutestrategies ./internal/modules/managementgroups ./internal/modules/managementaccounts ./internal/modules/managementoperationlogs ./internal/httpapi ./internal/store/postgres ./internal/config ./internal/app`、`go test -tags=integration ./internal/testkit/integration -run TestW2Management -count=1` 和 `JUHE_AI_MANAGEMENT_API_ENABLED=false/true` 双态 router 测试 | 字段、错误结构、中文文案、分页、敏感字段脱敏一致；`proxies/options` 只返回启用代理 `id/name/type/enabled`，按名称前缀搜索并 clamp `limit=1..50`；`system-accounts` 必须 admin-only，返回分页 `items/total/hasMore/page/pageSize`，支持 `keyword` 用户名 / 显示名前缀搜索和 `page/pageSize`，不得读取或返回 `password_hash`，`total` 只作为窗口上界；`system-accounts/options` 必须 admin-only，支持 `ids` 回填、`keyword` 用户名 / 显示名精确或前缀搜索、`limit=1..50`，只返回 `id/username/displayName/status`，不得读取或返回 `password_hash`、`role`、`must_change_password`、`last_login_at`、创建 / 更新时间等完整系统账户字段；`authorization-options/grantee-accounts` 必须 admin-only，`my-authorization-options/grantee-accounts` 允许所有登录用户，二者支持 `ids` 回填、`keyword` 用户名 / 显示名精确或前缀搜索、`limit=1..50`，只返回 `id/username/displayName/status`，不得读取完整系统账户字段；`authorization-options/grantee-groups` 必须 admin-only，`my-authorization-options/grantee-groups` 允许所有登录用户但不改写 `granteeSystemAccountId`，二者必须要求 `granteeSystemAccountId`，只返回目标 active 用户自己的启用分组轻量摘要，支持 `ids`、`providerCode`、`keyword` 分组名前缀搜索、`preferDefault`、`limit=1..50`，不得返回分组成员、账号统计、用量、并发、授权来源或 grant；`providers/options` 返回完整 `ProviderDefinition[]`、协议画像、endpoint families、启用供应商和系统账户默认测试模型偏好，普通用户不能通过 query 越权指定 `systemAccountId`；`providers/models/options` 返回启用真实供应商模型选项、协议能力和当前作用域自定义模型，不返回价格 / 备注 / `hybrid` 自身模型；`providers/{code}/models` 支持 OpenAI-compatible 聚合、hybrid 聚合、includeInactive/includeUnpriced 和可计价过滤，普通用户不能通过 query 越权指定 `systemAccountId`；`route-strategies/options` 必须 admin-only，支持 `systemAccountId` 全局 / 指定用户、`ids`、`keyword`、`limit=1..100` 和 `activeOnly`，返回 owner 字段；`my-route-strategies/options` 必须强制当前用户作用域、忽略 query `systemAccountId` 且不返回 owner 字段；`groups/options` / `groups/account-options` 必须 admin-only，支持 `systemAccountId` 全局 / 指定用户、`ids`、`keyword`、`providerCode`、`limit=1..50`、`manageableOnly`、`preferDefault`，返回 owner 字段、owner permissions、授权组 union、授权字段和 authorized permissions；`groups/account-options` 额外返回启用 owner 绑定的 `accountIds`，授权分组必须返回 `accountIds=[]`，不得返回 `accountStats`、用量、并发、账号详情或凭据；`my-groups/options` / `my-groups/account-options` 必须强制当前用户作用域、忽略 query `systemAccountId` 且不返回管理侧系统账户字段；`accounts/options` / `my-accounts/options` 必须支持 owner + 授权账户只读 union、`accessType=authorized`、`accountAuthorizationId`、授权状态 / 到期、来源账户 ID、资源 owner 字段、authorized permissions、owner `tagIds` 命中任一标签过滤和名称包含搜索，名称包含搜索必须走 `account_name_search_terms` / `account_name_search_documents` 候选收敛，禁止裸扫 `accounts` 主表，`schedulable=enabled` 必须同时校验本地实例、同一授权 ID 的启用分组绑定、统一授权和来源账户状态；`accounts/tags` / `my-accounts/tags` 必须返回 owner 标签字典和未删除 owner 账号绑定计数；`DELETE accounts/tags/{tagId}` / `DELETE my-accounts/tags/{tagId}` 必须只删除当前作用域未绑定任何未删除账户的标签，成功 `204`，绑定中 `400 { message: "标签已绑定账户，不能删除" }`，不存在或越权 `404 { message: "标签不存在" }`，删除前需在事务中锁定目标标签行并复查绑定；`PATCH accounts/{id}/tags` / `PATCH my-accounts/{id}/tags` 必须覆盖 admin / self 作用域、授权实例本地标签、不可见实例 404、未知字段 / 尾随 JSON / 数量 / 长度校验、trim / 空白折叠 / 大小写敏感去重、绑定表全量替换、并发同名标签复用、`system_account_id + name` 唯一约束、operation log payload、Asynq `operation-logs` 入队参数、worker `operation-log:write` handler、HTTP 成功入队和入队失败仍返回业务成功；当前系统账户列表读和 options 只覆盖 W2 只读范围，不覆盖系统账户创建 / 更新 / 改密 / 登录 / 完整会话撤销；authorization grantee accounts / grantee teams / grantee groups 只覆盖用户授权对象候选和授权目标分组候选，不覆盖授权写接口；分组 options / account-options 已覆盖 owner + 授权组只读 union，账户 options 已覆盖 owner + 授权账户只读 union，但不覆盖授权来源 / grant / 授权写接口；账号标签只读、未绑定删除和独立 PATCH 不覆盖主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产接管 / 完整 data-retention 或其他写接口 operation log；Go `requireAuth` 基线必须读取 `juhe_ai_session`、按 SHA-256 `token_hash` 查询 `system_sessions`、拒绝缺失 / 过期 / 停用会话、拦截普通用户初始密码修改；默认生产 router 不注册 W2 后台路由，只有 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 且注入管理端会话鉴权 middleware 时才允许灰度挂载 | W2 全范围接管后删除 Node 对应 route/service/test 命令；单个 options、catalog、标签删除 / PATCH 切片、会话鉴权基线或 opt-in guard 完成不能删除 Node |
| W2 操作日志保留清理 worker | `juhe-ai-worker operation-log-retention-cleanup`、service/store SQL guard、真实 PG run-once smoke | 读取 `operationLogRetentionDays`，缺失时默认 365 天；按 `created_at ASC, id ASC` 批量删除 `juhe_dataset.operation_logs`，每批最多 1000 条、单轮最多 20 批、满批暂停 25ms，并依赖 FK cascade 清理子表；`go run ./cmd/juhe-ai-worker operation-log-retention-cleanup --run-once` 只有真实 PG 执行成功才算 smoke 通过 | 该 worker 只覆盖 operation log 主表保留清理；完整 data-retention、生产 supervisor 接管、生产切流和 Node 删除仍需单独证据 |
| W3-W5 管理写接口 | CRUD、权限矩阵、事务回滚、审计/操作日志副作用；当前 W3 已实现 `GET auth/captcha`、`POST auth/login`、`GET auth/me`、`PATCH auth/me`、`POST auth/change-password`、`POST auth/logout`、`POST system-accounts` 和 `PATCH system-accounts/{id}` 完整 mixed PATCH 的 Go opt-in 验证，W4 已实现 `GET system-teams` / `GET my-teams` 团队列表 / 详情读接口、`POST system-teams` 团队创建、`PATCH system-teams/{id}` 团队更新、`POST system-teams/{id}/members` 成员新增和 `DELETE system-teams/{id}/members/{memberId}` 成员移除 Go opt-in 验证、`resource_authorization_sources` / `resource_authorization_grants` / `request_quota_hourly_window_configs` / `group_account_stats_dirty` PostgreSQL schema guard 和授权缓存失效；需执行 `sqlc generate`、相关 Go 快速测试、integration 包编译和 Docker/testcontainers smoke 复跑 | 当前公开契约等价或文档已更新；系统账户完整 PATCH 必须允许 `displayName/description/password/role/status/mustChangePassword/imageGenerationEnabled` 任意组合，禁止 `username` 和未知字段，提交 `password` 或 `status="disabled"` 撤销目标全部 session，禁止停用 / 降级最后一个启用 `super_admin`，状态或图像权限真实变化清理 gateway runtime cache / API Key validation cache，密码 operation log 只写“已重置”；团队读接口必须覆盖 admin / self 作用域、分页、团队名前缀搜索、详情 active 成员摘要、读接口不 touch session、不读取授权来源 / grant、不通过说明字段搜索；团队创建必须 admin-only、strict JSON、团队名全局唯一、只写团队摘要字段并记录 `system_teams.create` 操作日志，不读取成员 / 授权来源表；团队更新必须 admin-only、strict JSON、支持 name / description / status partial update，禁用团队时 revoke 当前 team source 并刷新 effective source，启用团队时按 active team grants 展开到 active members，状态副作用写全量 stats dirty 标记并发布 `team_authorization_changed` 授权缓存失效；团队成员维护必须 admin-only、写鉴权 touch、strict JSON，新增成员只允许 active 团队 / active 系统账户，单次最多 20 个且团队 active 成员最多 20 个，新增或重新加入时按既有 active team grants 展开最终用户授权，移除成员时 revoke 该成员 active team source 并刷新 effective source，成员变化写 `team_members_changed` stats dirty / 授权缓存失效和 `system_teams.add_members` / `system_teams.remove_member` operation log；授权来源 / grant schema 基线必须包含 source/grant 表、manual/team 来源约束、用户/团队 grant 互斥约束、active source/grant 唯一索引、到期扫描索引和 quota snapshot 索引；额度窗口 / 统计状态 schema 必须包含默认 1/3/6/12/24/72/168/720 小时窗口和 `group_account_stats_dirty`；授权缓存失效必须固定 Node 兼容 `gateway_runtime_cache`、`authorization_quota_cache` 和 `api_key_quota_cache` runtime state topic、TTL、reason 和 `apiKeyId` 字段；Docker/testcontainers `SKIP` 不算真实 PG+Redis 通过 | Node 对应写路径和 SQLite repository 分支删除；W3/W4 仅完成 opt-in 验证时不能删除 Node `/auth`、`/system-accounts`、`/system-teams` 或 `/authorizations` |
| W6 记录和统计读 API | cursor/window、范围查询、无实时扫描、PG 分区窗口、系统指标 API Go runtime 契约测试、[Go 系统指标字段迁移清单](Go系统指标字段迁移清单.md) W6 项 | 不全表扫描，不读旧 usage shard；`/stats/system-metrics` Go owner 不再返回 `eventLoopLagMs`、`processEventLoop*` 或 `db-service` 长期字段，前端类型改为 host/runtime/task queue/storage/stats freshness 分组 | Node 记录读路径、usage shard 读取入口和 Node 系统指标读模型删除 |
| W7 worker | Asynq task queue、游标、租约、重试、dead / archived、shutdown drain、Go runtime / Asynq / worker lag 采样、[Go 系统指标字段迁移清单](Go系统指标字段迁移清单.md) W7 项 | 重启可恢复未完成任务，重复投递幂等，queue depth / retry / dead / oldest age 可观测；stats-worker 能写 Go runtime / queue / stats lag 样本和窗口 | Node worker、IPC 队列、临时维护 worker、worker event loop 采样删除 |
| W8 storage 删除收尾 | 静态搜索、发布包检查、PG/Redis smoke、DB service / SQLite 指标删除检查、[Go 系统指标字段迁移清单](Go系统指标字段迁移清单.md) W8 项 | 无 SQLite runtime、env、schema、usage shard 写入；health / metrics / system monitor 不再暴露 DB service、SQLite 文件大小或 usage shard 文件路径 | DB service、SQLite schema、双模式测试命令、`process_event_loop_*` Go 运行引用删除 |
| W9 网关准备层 | Go 读模型压力测试、缓存失效、配额、候选过滤 | 不接管真实网关，不删除 Node preflight | 无删除；只记录契约和压测产物 |
| W10 真实网关 | mock 全矩阵、响应检查、错误兜底、side effects、慢客户端、真实 smoke、性能压测 | 首字、SSE、backpressure、取消、usage/audit/统计副作用全部通过 | Node gateway 主链路、preflight、协议 adapter、响应检查旧实现删除 |
| W11 发布收尾 | 发布包、Docker、systemd/Windows/launchd、备份恢复、回滚 smoke | 新安装可启动，PG/Redis 连接和 worker 正常，回滚步骤可执行 | Node 后端依赖、dist、启动脚本、SQLite env、旧部署文档删除或改写 |

### W1b Node 对照基线

W1b 迁移前必须先把现有 Node 行为固定为对照证据。以下命令只代表 Node 当前契约，不代表 Go 已接管：

```powershell
pnpm test:external-source-auth
pnpm test:public-api-logs
pnpm --filter juhe-ai-backend test:external-integration-source-expires-at
pnpm --filter juhe-ai-backend test:external-integration-source-async-boundary
pnpm --filter juhe-ai-backend test:external-public-account-push-async-boundary
pnpm --filter juhe-ai-backend test:public-api-log-db-service-ipc
pnpm test:sqlite-query-regression
```

PostgreSQL / Redis 对照 smoke 只能在测试库执行，不能指向生产库：

```powershell
pnpm --filter juhe-ai-backend test:external-integration-source-postgres-smoke
pnpm --filter juhe-ai-backend test:external-public-account-push-postgres-smoke
```

W1b Go 契约测试必须额外覆盖：

- 16 个 method/path/scope，旧 `demo/source-auth`、`ip/usage`、`account/usage`、`consumption/ranking`、`access/info` 路径继续 404。
- 生产 router 双态 guard：默认 `JUHE_AI_PUBLIC_API_ENABLED=false` 时 `/__aipublic__/*` 仍是根路由通用 404；显式 `true` 且注入完整依赖时才进入 public shell，且不得剥离 `r.URL.Path` 中的 `/__aipublic__` 前缀。
- `go run ./cmd/juhe-ai-maintenance w1b-public-api-smoke`：缺少 `JUHE_AI_POSTGRES_URL`、`JUHE_AI_REDIS_STATE_URL`、`JUHE_AI_REDIS_QUEUE_URL` 或少于 32 字符的 `JUHE_AI_SECRET` 必须 fail-fast；真实依赖执行时必须验证默认 guard、显式 opt-in mount、临时测试 token、Redis state 限频、Redis queue 入队和 `juhe-ai-worker ingest` 写入 PostgreSQL public API log。输出必须保留 `takeoverEvidence=false` 和 `productionTakeoverNotEvaluated=true`。
- 配置 fail-fast：开启 W1b public API 时缺 `JUHE_AI_REDIS_STATE_URL`、`JUHE_AI_REDIS_QUEUE_URL` 或少于 32 字符的 `JUHE_AI_SECRET` 必须失败，不能落回进程内限流、本地队列或开发默认凭据密钥。
- `Authorization: Bearer <source_token>` 解析、token hash、source/token 状态和过期、source/token scope 交集。
- source 级限频、`Retry-After`、`external_source_rate_limited`、details 字段、内置测试 token `60s/10`。
- `last_used_at` 成功鉴权后 60 秒节流 touch；限频发生在鉴权成功后，429 请求也可能 touch。
- 公开接口日志覆盖成功、失败、401、403、404、413、429、499 和 JSON 解析失败；499 时顶层状态、错误码、错误消息和 response snapshot `statusCode` 必须一致；不记录 `Authorization` / 来源 token / Cookie；请求 / 响应 body 按 32KB 单侧预算保存原文摘要；Asynq payload/enqueue/handler 和 `juhe-ai-worker ingest` runtime 必须用真实 Redis/Asynq/PG smoke 证明幂等写入。
- W1b HTTP capture 的 `ResponseWriter` 包装必须保留底层 `Unwrap` / `Flush` / `Hijack` / `ReadFrom` / `Push` 等可选接口组合；至少覆盖 `ResponseController.Flush`、`Flush` 和 `ReadFrom` 单测，避免后续流式网关或公开接口写法被 wrapper 破坏。
- 业务响应中账号凭据永不回显；API Key 完整 secret 只在新增接口本次返回；公开日志详情按快照预算保存业务摘要，但必须递归脱敏 `Authorization` / 来源 token / Cookie / API Key / Bearer / secret-like 字符串，不能把新增响应里的完整 key 写入日志。

W1b public group、public route strategy、public API Key 与 public account 四条真实资源纵切面必须补齐以下测试矩阵：

| 层级 | 建议命令 / 验证方式 | 必须覆盖 |
| --- | --- | --- |
| Handler | `go test ./internal/httpapi -run TestPublicGroupHandlers` | `group/list|add|update|del`、strict query/body、GET 分页 coerce、POST 数字 / 布尔不 coerce、中文错误、测试 token mock、统一 `{ data: ... }` |
| Service | `go test ./internal/modules/publicgroups -run Test` | 目标用户读取、`group/add` 自动创建目标用户、目标用户停用、provider 缺失 / 停用、同用户同供应商同名 `existing`、target / group 并发唯一冲突后事务级重试、重复名冲突、默认分组修改 / 删除保护、修改 provider 前账号绑定保护、停用 / 删除前活跃策略路由唯一可用分组保护 |
| PostgreSQL store / integration | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicGroupsPostgresSmoke -count=1` | `000004_w1b_public_groups.sql`、sqlc query、事务、唯一索引、owner 复合 FK、分页上界、稳定排序、账号绑定计数、活跃策略路由计数和 route strategy 行锁；确认不引入 SQLite / DB service / IPC |
| Shell E2E | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicGroupsShellE2E -count=1` | 必须在 Docker/testcontainers 健康环境真实通过；Bearer/auth/scope、Redis limiter、public API log 入队、worker 写 PG 和响应脱敏需贯穿 HTTP shell；仍不代表生产 router 已挂载 |
| 路由归属 | `go test ./internal/httpapi -run TestPublicAPITakeoverGuard` | `JUHE_AI_PUBLIC_API_ENABLED=false` 时生产 router 仍不注册 `/__aipublic__` 并返回根路由通用 404；显式开启时整段 prefix 进入 public handler，路径不被剥离；开启只是灰度入口，不代表正式接管 |
| 删除验证 | `rg "__aipublic__|external-source|public-api-logs" backend/src backend/package.json package.json` | W1b 未整体切流前不得删除 Node；整体接管后只剩历史文档或未迁移管理接口引用 |

W1b public route strategy 纵切面专项矩阵：

| 层级 | 建议命令 / 验证方式 | 必须覆盖 |
| --- | --- | --- |
| Handler | `go test ./internal/httpapi -run TestPublicRouteStrategyHandlers` | `route-strategy/list|add|update|del`、strict query/body、GET 分页 coerce、POST 数字不 coerce、`groupBindings` 嵌套字段白名单、中文错误、测试 token mock、统一 `{ data: ... }`；当前已通过 |
| Service | `go test ./internal/modules/publicroutestrategies -run Test` | 目标用户必须已存在且 active、同名冲突、owner 边界、绑定整体覆盖、普通路由单分组限制、故障回退备份要求、重复分组、active priority 冲突、停用分组 active 绑定拒绝、默认策略删除保护和 API Key 使用保护；当前已通过 |
| PostgreSQL store / integration | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicRouteStrategiesPostgresSmoke -count=1` | `000004_w1b_public_groups.sql` 中 route strategy / binding / api key 约束、`w1b_public_route_strategies.sql`、事务、同名唯一索引、绑定整体替换、active priority 唯一、默认策略和 API Key 使用保护；必须在 Docker/testcontainers 健康环境真实通过，且确认不引入 SQLite / DB service / IPC |
| Shell E2E | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicRouteStrategiesShellE2E -count=1` | 必须在 Docker/testcontainers 健康环境真实通过；Bearer/auth/scope、Redis limiter、public API log 入队、worker 写 PG、add/list/update/delete/limited 和响应脱敏需贯穿 HTTP shell；仍不代表生产 router 已挂载 |

W1b public API Key 纵切面专项矩阵：

| 层级 | 建议命令 / 验证方式 | 必须覆盖 |
| --- | --- | --- |
| Handler | `go test ./internal/httpapi -run TestPublicAPIKeyHandlers` | `api-key/list|add|update|del`、strict query/body、GET 分页 coerce、POST 数字 / 布尔不 coerce、`quotaLimits` / `availabilitySchedule` 字段白名单、中文错误、测试 token mock、`add` 返回完整 `key` 且 list/update/delete 只返回摘要；当前已通过 |
| Service | `go test ./internal/modules/publicapikeys -run Test` | 目标用户必须已存在且 active、路由策略同 owner 且 active、同名冲突、`update/delete` 可选 `targetUsername` 归属校验、至少一个变更字段、默认 API Key 删除 / 换路由保护、quota / schedule / expiresAt 校验、hash-only secret 和响应白名单；当前已通过 |
| PostgreSQL store / integration | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicAPIKeysPostgresSmoke -count=1` | `000004_w1b_public_groups.sql` 中 API Key 字段 / 索引 / JSON 约束、`w1b_public_api_keys.sql`、事务、同用户同名唯一、route strategy owner 校验、默认 Key 保护、secret 不明文落库、分页和摘要字段；必须在 Docker/testcontainers 健康环境真实通过，且确认不引入 SQLite / DB service / IPC |
| Shell E2E | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicAPIKeysShellE2E -count=1` | 必须在 Docker/testcontainers 健康环境真实通过；真实 Bearer/auth/scope、Redis limiter、public API log 入队 / worker 写 PG、add/list/update/delete/limited、source/token last_used、完整 key、query string 和来源 token 日志脱敏需贯穿 HTTP shell；仍不代表生产 router 已挂载 |

W1b public account 纵切面专项矩阵：

| 层级 | 建议命令 / 验证方式 | 必须覆盖 |
| --- | --- | --- |
| Handler | `go test ./internal/httpapi -run TestPublicAccountHandlers` | `account/list|add|update|del`、strict query/body、GET 分页 coerce、POST 数字不 coerce、中文错误、测试 token mock、响应不含 `apiKey` / `baseUrl` / `credentials`、请求日志遮蔽上游 `apiKey` 和 URL userinfo |
| Service | `go test ./internal/modules/publicaccounts -run Test` | 目标用户 / 目标分组自动创建、provider/profile 启用校验、`type=api_key` 限制、base URL 安全校验、凭据可逆加密、凭据指纹 hash、同名冲突、`pending_test -> active` 拒绝、局部凭据更新、软删除和 `not_found` 幂等 |
| PostgreSQL store / integration | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicAccountsPostgresSmoke -count=1` | `000005_w1b_public_accounts.sql`、`w1b_public_accounts.sql`、事务、provider profile seed、owner 复合 FK、模型替换、凭据不明文落库、软删除和绑定清理；必须在 Docker/testcontainers 健康环境真实通过，且确认不引入 SQLite / DB service / IPC |
| Shell E2E | `go test -tags=integration ./internal/testkit/integration -run TestW1bPublicAccountsShellE2E -count=1` | 测试代码已补；必须在 Docker/testcontainers 健康环境真实通过；真实 Bearer/auth/scope、Redis limiter、public API log 入队 / worker 写 PG、add/list/update/delete/limited、source/token last_used、上游 `apiKey` 日志脱敏和响应白名单需贯穿 HTTP shell；仍不代表生产 router 已挂载 |

## 6. 网关专项测试矩阵

真实网关迁移必须单独验收：

| 场景 | 必测内容 |
| --- | --- |
| OpenAI Chat JSON / SSE | raw body、模型、stream、usage、错误结构、上游 header 过滤 |
| OpenAI Responses JSON / SSE | Codex profile、Responses 事件、usage、失败事件、上下文恢复边界 |
| Anthropic Messages | native messages、OpenAI bridge、SSE 语义帧、错误转换 |
| Gemini native | generateContent、streamGenerateContent、目标协议桥接边界 |
| Hybrid | 模型映射、目标协议、不可保真能力处理、审计记录 |
| 调度 | API Key -> 路由策略 -> 分组 -> 账号 -> 供应商 / 协议能力 |
| 失败恢复 | 非 2xx、SSE 内失败、首字超时、账号屏蔽、后续账号切换 |
| 取消与反压 | 客户端断开、`ResponseWriter` flush、慢客户端、上游 cancel |
| 副作用 | usage、audit、账号状态、代理桶、运行日志、统计入队 |

额外阻断项：

- 入口路径：`/__aisys__/*`、`/__aipublic__/*`、根路径和 `/v1/*` 不能互相抢路由。
- Body：认证前可拒绝路径不得读取大 body；raw body、文本 lane 和图像 lane 上限继承当前契约。
- Header：上游危险 header、`content-length` / `content-encoding`、压缩响应解码和下游 header 过滤必须有断言。
- 响应检查：Chat / Responses JSON / SSE 污染、畸形 SSE、Codex compact、普通客户端不可见 Codex 专用错误码必须覆盖。
- 协议桥接：允许 / 禁止方向、endpoint mode、工具、图像、usage、bad SSE 和 guidance 都必须走 mock 矩阵。
- 灰度：W10 必须给出 canary 入口、失败阈值、快速停止和回退步骤。

## 7. 性能与内存验收

- 管理接口迁移后，必须对高频列表和 options 做并发读压测，确认没有全表扫描和无界内存增长。
- worker 迁移后，必须验证游标推进、批量窗口、队列容量和重试不会压垮请求路径。
- 网关迁移后，必须生成独立报告放入 `docs/reports/`，至少包含并发、首字时间、SSE 稳定性、CPU、内存、goroutine 数量、GC、数据库连接池和上游连接池观察。
- 长时间压测要检查 goroutine 泄漏、body 未关闭、timer 未释放、channel 未关闭和缓存无界增长。
- pprof、runtime metrics 和结构化日志必须能定位热点。Go 指标口径按 [Go 迁移指标与观测规划](Go迁移指标与观测规划.md) 执行，不能把 scheduler latency 或 goroutine 堆积伪装成 Node event loop。
- 性能报告必须给出当前 Node 基线或最近可用报告作为对比，至少记录管理读 P95 / P99、网关首字、SSE 完成率、worker lag、PG/Redis 延迟、CPU、内存、goroutine、GC 和文件描述符。

系统指标专项验收：

- 健康检查不能只看 HTTP 200；部署 smoke 和 watchdog 必须解析 JSON 中的 `success`、`status` 和 `dependencies`。
- `/__aisys__/metrics` 和 `/__debug/pprof` 必须验证 loopback / 受控访问，公网路径必须被反代拦截或返回不可访问。
- Go 系统监控接口必须有 API 契约测试，并按 [Go 系统指标字段迁移清单](Go系统指标字段迁移清单.md) 证明 runtime、队列、存储和统计新鲜度字段在不可观测时返回 `null` / `sampleAvailable=false`，不能用 0 伪装正常。
- 前端系统指标页面迁移时必须有类型检查和页面 smoke，证明不再依赖 `processEventLoopLatestStatus`、`processEventLoopPeakStatus`、`processEventLoopTrend`、`eventLoopLagMs` 或 `db-service`。

## 8. 不通过处理

测试失败时不允许用长期兼容分支掩盖问题。处理顺序：

1. 判断是 Go 实现偏离当前契约，还是当前契约应按当前最优方案调整。
2. 如果是实现问题，修 Go 代码并补回归。
3. 如果是契约调整，先更新功能文档和计划，再更新测试预期。
4. 如果涉及旧数据，只记录离线处理方案，不写入运行路径。
5. 如果模块无法删除 Node 旧实现，状态保持 `阻塞` 或 `待删除 Node`，不能标记 `已接管`。
6. 如果模块仍依赖 SQLite runtime 或双模式分支，状态最多只能到 `待删除 Node`，不能标记 `已接管`。
