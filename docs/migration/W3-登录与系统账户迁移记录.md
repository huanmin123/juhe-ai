# W3 登录与系统账户迁移记录

> 本文记录 W3 “登录与系统账户”迁移的当前切片。W3 涉及登录、验证码、会话、改密、系统账户写接口和会话撤销，风险高；必须按小切片推进，不能因为某个读接口完成就声明 W3 接管。

## 基本信息

- 模块：登录与系统账户
- 状态：Go 实现中
- 迁移波次：W3 登录与系统账户
- 当前 Node owner：`backend/src/modules/auth/`、`backend/src/modules/system-accounts/`、`backend/src/storage/system-accounts.repository.ts`
- 当前 Go owner：`backend-go/internal/modules/managementauth/`、`backend-go/internal/modules/managementsystemaccounts/`、`backend-go/internal/httpapi/management_auth.go`、`backend-go/internal/httpapi/management_captcha.go`、`backend-go/internal/httpapi/management_login.go`、`backend-go/internal/httpapi/management_password.go`、`backend-go/internal/httpapi/management_system_accounts.go`、`backend-go/internal/httpapi/management_system_account_create.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/httpapi/system_api_rate_limit.go`、`backend-go/internal/platform/redis/client.go`、`backend-go/internal/app/server.go`、`backend-go/internal/store/postgres/managementauth.go`、`backend-go/internal/store/postgres/managementsystemaccounts.go`、`backend-go/internal/store/postgres/managementsystemaccountcreate.go`、`backend-go/internal/store/postgres/queries/w2_management_auth.sql`、`backend-go/internal/store/postgres/queries/w3_management_system_accounts.sql`、`backend-go/internal/store/postgres/queries/w3_management_system_account_update.sql`、`backend-go/internal/store/postgres/queries/w3_management_system_account_create.sql`、`backend-go/db/migrations/000017_w3_system_account_create.sql`
- 关联功能文档：`docs/functions/接口契约与权限矩阵.md`、`docs/functions/核心功能设计.md`
- 关联计划：`docs/plans/计划-0081-Node转Go渐进减法迁移.md`
- 本轮对齐：系统账户、当前用户资料及其他已迁移管理写接口统一通过 [BUG-0044](../bug/问题-0044-Go管理操作日志未统一执行Node清洗规则.md) 的共享 operation log 入口清洗 changes；密码最终只保留 `before="未设置"`、`after="已变更"` 和 `sensitive=true`，handler 内部的“已设置 / 已重置”占位文案不再作为队列或存储契约。

## 当前已迁移切片

| 切片 | 路径 | 状态 | 边界 |
| --- | --- | --- | --- |
| 验证码发放 / 校验基础 | `GET /__aisys__/api/auth/captcha` + Go 内部 `VerifyChallenge` | Go opt-in 已实现，未生产接管 | 验证码启用时使用 Redis state 保存 5 分钟一次性 challenge，按客户端 IP 60 次 / 分钟限流，保持原图形验证码响应和 PNG data URL；`JUHE_AI_AUTH_CAPTCHA_DISABLED=true` 时只返回 `required=false`，不发放 challenge |
| 登录 / session 创建小切片 | `POST /__aisys__/api/auth/login` | Go opt-in 已实现，未生产接管 | 验证码启用时复用 Redis state `GETDEL` 校验 `captchaId/captchaCode`；关闭时可省略这两个字段，服务端跳过验证码校验。两种模式都保留 Redis 登录失败 IP / 用户名锁定、PG active 系统账户校验、Node 兼容密码 hash、session 写入和 Cookie 签发；不代表完整 W3 接管，不能删除 Node `/auth` |
| 当前用户读取 | `GET /__aisys__/api/auth/me` | Go opt-in 已实现，未生产接管 | 只读取当前 `juhe_ai_session` 对应系统账户摘要，不 touch session，不创建 / 撤销 session，不修改用户资料或密码 |
| 当前用户资料更新 | `PATCH /__aisys__/api/auth/me` | Go opt-in 已实现，未生产接管 | 只允许当前登录用户修改自己的 `displayName`；普通初始密码用户继续 403；按 Node 写模式在 `last_seen_at` 距当前时间超过 60 秒时 touch 当前 session；不修改用户名、角色、状态、密码、图像生成权限或其他会话 |
| 当前用户改密 | `POST /__aisys__/api/auth/change-password` | Go opt-in 已实现，未生产接管 | 普通初始密码用户可只提交新密码；普通改密和管理员改密必须校验旧密码；鉴权阶段按 Node 写模式在 `last_seen_at` 距当前时间超过 60 秒时 touch 当前 session；成功后写 Node 兼容 PBKDF2 hash、清除 `mustChangePassword` 并撤销同账号其他 session；不创建新 session，不写 operation log |
| 当前会话登出 | `POST /__aisys__/api/auth/logout` | Go opt-in 已实现，未生产接管 | 允许无有效登录态调用；有 `juhe_ai_session` 时只按当前 token hash 删除对应 session，成功后清 Cookie；清 Cookie 复用运行时 `Secure` / `SameSite` 配置；不创建 session，不撤销其他 session，不写 operation log |
| 当前用户会话列表 / 撤销 | `GET /__aisys__/api/auth/sessions`、`DELETE /__aisys__/api/auth/sessions/{id}` | Go opt-in 已实现，未生产接管 | 列表只返回当前系统账户未过期 session，带 `current` 标记，不读取 / 返回 `token_hash`，不 touch session，分页 `total` 只做窗口上界；撤销只允许删除当前系统账户下指定 session，写鉴权按 60 秒窗口 touch 当前 session，撤销当前 session 时清 Cookie；可随 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 全量管理开关挂载，也可由 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 窄开关只挂载这两条会话路径；暂不写 operation log |
| 系统账户创建 | `POST /__aisys__/api/system-accounts` | Go opt-in 已实现 create 子动作，未生产接管 | 只允许 `super_admin`；严格复刻 Node create schema 和空白字符拒绝；PG 事务创建系统账户、7 个默认内置分组、6 条非 hybrid 默认普通路由、6 条路由分组绑定和 6 个默认 API Key；默认 API Key 明文只加密落库，不在响应或日志返回；operation log 密码字段经统一清洗后只保留 `未设置/已变更` 敏感标记 |
| 系统账户完整更新 | `PATCH /__aisys__/api/system-accounts/{id}` | Go opt-in 已实现完整混合 PATCH，未生产接管 | 只允许 `super_admin`；严格复刻 Node update schema，允许 `displayName`、`description`、`password`、`role`、`status`、`mustChangePassword`、`imageGenerationEnabled` 任意组合，禁止 `username` 和未知字段；PG 单语句 CTE 锁定启用超级管理员集合与目标账户，禁止停用 / 降级最后一个启用 `super_admin`，按角色归一化 `mustChangePassword`，写 Node 兼容密码 hash；提交 `password` 或 `status="disabled"` 时撤销目标全部 session；状态或图像权限真实变化会清理 gateway runtime cache / API Key validation cache，状态变化优先；operation log 支持混合字段 diff，密码经统一清洗后只保留 `未设置/已变更` 敏感标记 |

## 当前契约

### `GET /__aisys__/api/auth/captcha`

| 项 | 内容 |
| --- | --- |
| 路径 | `GET /__aisys__/api/auth/captcha` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册；`JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 不注册验证码路径 |
| 登录态 | 不要求登录，不读取 `juhe_ai_session` |
| 启用时响应 | `JUHE_AI_AUTH_CAPTCHA_DISABLED=false` 时返回 `200 { data: { required: true, captchaId, image, expiresAt } }`；保持原图形验证码字段，`image` 固定为 `data:image/png;base64,...` |
| 关闭时响应 | `JUHE_AI_AUTH_CAPTCHA_DISABLED=true` 时返回 `200 { data: { required: false } }`，不创建 challenge，不进入验证码自身生成限流 |
| 验证码规则 | 5 位，字符集为 `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`；答案只写 Redis state，不写响应，不输出 SVG / 文本 |
| 存储 | Redis state key 为 `auth_captcha:challenge:{captchaId}`，JSON 值包含 `answer` 和毫秒级 `expiresAt`，TTL 5 分钟；校验时通过 Redis `GETDEL` 一次性消费，错误答案也会消费 challenge |
| 生成限流 | 按客户端 IP 经 SHA-256 派生低泄漏 key，Redis fixed-window 限制 60 次 / 分钟；超限不创建新 challenge |
| 超限响应 | `429 { message: "验证码请求过于频繁，请稍后再试" }`，带 `Retry-After` |
| system API 读限流 | 虽然是 GET，但属于写 runtime state 的入口，Go router 明确跳过 system API IP 读限流，避免和验证码自身限流叠加 |
| 副作用 | 只写 Redis state / rate-limit 计数，不写 PG，不 touch session，不写 operation log，不创建登录 session |

### `POST /__aisys__/api/auth/login`

| 项 | 内容 |
| --- | --- |
| 路径 | `POST /__aisys__/api/auth/login` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册；`JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 不注册登录路径 |
| 登录态 | 不要求已有登录态；该入口是 session 创建小切片 |
| 请求体 | 严格 JSON object，只允许 `username`、`password`、`captchaId`、`captchaCode` 四个字符串字段；当验证码启用时四个字段均必填，并保持原登录字段契约；当 `JUHE_AI_AUTH_CAPTCHA_DISABLED=true` 时只要求 `username/password`，可省略 `captchaId` 与验证码答案字段 `captchaCode`（当前契约字段名不是 `captchaAnswer`）。未知字段、必填字段缺失、非字符串、必填字段为空或尾随 JSON 按现有错误契约返回 `400` |
| 空白字符 | `username` 或 `password` 包含任意空白字符时返回 `400 { message: "用户名和密码不能包含空格" }` |
| 验证码 | 验证码启用时先调用 Go `CaptchaService.VerifyChallenge`，通过 Redis `GETDEL` 一次性消费 challenge；验证码错误 / 过期返回 `400 { message: "验证码错误或已过期" }`，且不计入登录失败锁。`JUHE_AI_AUTH_CAPTCHA_DISABLED=true` 时 handler 和 service 均跳过验证码字段必填与 challenge 校验，但不跳过登录失败锁、账号密码或 session 逻辑 |
| 登录失败锁 | 验证码启用时校验通过后、关闭时跳过校验后，都继续检查 Redis state 登录失败锁；IP 和用户名各按 10 分钟窗口累计 10 次失败，锁定 15 分钟；IP 锁优先返回 `429 { message: "尝试过于频繁，请稍后再试" }`，用户名锁返回 `429 { message: "账号暂时锁定，请稍后再试" }`，均带 `Retry-After` |
| 凭据校验 | 只允许 `system_accounts.status='active'` 的系统账户登录；密码使用 Node 兼容 PBKDF2-SHA512 hash 校验；账号不存在、停用或密码错误统一返回 `401 { message: "账号或密码错误" }` |
| session 创建 | 成功后生成 32 字节随机 base64url token，PG 只存 SHA-256 hex `token_hash`；`system_sessions.id` 使用 `sess_` 前缀；TTL 为 14 天；`created_at` 和初始 `last_seen_at` 使用同一登录时间 |
| 登录时间 | 同一条 PG CTE 中更新 `system_accounts.last_login_at` / `updated_at` 并插入 `system_sessions`，避免 session 与登录时间局部成功 |
| Cookie | 成功后签发 `juhe_ai_session`，`Path=/`、`HttpOnly`、`Max-Age=14d`、`Expires=session_expires_at`；`Secure` 与 `SameSite` 由 `JUHE_AI_COOKIE_SECURE` / `JUHE_AI_COOKIE_SAME_SITE` 控制，`SameSite=None` 要求 `Secure=true` |
| 成功响应 | `200 { data: { id, username, displayName, role, mustChangePassword } }`；Go 明确不返回 Node 当前可能泄漏的 `passwordHash` |
| 副作用 | 写 Redis 登录失败计数 / 锁、成功清理当前 IP 与用户名登录失败状态、写 PG session 和登录时间；不写 operation log，不创建系统账户默认资源，不处理系统账户管理端重置密码 / 停用带来的完整 revoke |

### `GET /__aisys__/api/auth/me`

| 项 | 内容 |
| --- | --- |
| 路径 | `GET /__aisys__/api/auth/me` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 读取 `juhe_ai_session` Cookie，按 SHA-256 token hash 查询 `system_sessions`，校验 session 未过期、session active、系统账户 active |
| 成功响应 | `200 { data: { id, username, displayName, role, mustChangePassword } }` |
| 初始密码用户 | 普通 `user` 即使 `mustChangePassword=true` 也允许读取当前用户摘要，并返回 `mustChangePassword=true`；`admin` / `super_admin` 的有效值始终为 `false` |
| 未登录 | `401 { message: "请先登录" }` |
| 会话失效 | `401 { message: "登录会话已过期" }` |
| 内部错误 | `500 { message: "服务器内部错误" }`，不泄露数据库、密钥或连接串 |
| 副作用 | 不更新 `last_seen_at`，不写 operation log，不写 Redis，不写 session |

### `PATCH /__aisys__/api/auth/me`

| 项 | 内容 |
| --- | --- |
| 路径 | `PATCH /__aisys__/api/auth/me` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并拦截普通初始密码用户 |
| 请求体 | 严格 JSON object，只允许 `displayName: string`；未知字段、缺字段、非字符串和空字符串返回 `400 { message: "用户资料参数无效" }` |
| 显示名规则 | 不允许任何空白字符；命中返回 `400 { message: "显示名称不能包含空格" }`；大小写不敏感全局唯一，冲突返回 `409 { message: "用户名称已存在" }` |
| 成功响应 | `200 { data: { id, username, displayName, role, mustChangePassword } }` |
| 初始密码用户 | 普通 `user` 且有效 `mustChangePassword=true` 时返回 `403 { message: "请先修改初始密码", code: "must_change_password" }`；管理员角色有效值始终为 `false` |
| no-op | `displayName` 与当前值完全相同时直接返回当前用户摘要，不更新 PG，不写 operation log |
| 存储约束 | `juhe_business.system_accounts` 新增 `idx_system_accounts_display_name_unique_lower`，PG 层捕获 `23505` 作为重复显示名冲突；更新 SQL 不读取 `password_hash` |
| 操作日志 | 真实变更时 best-effort 入队 `operation-log:write`，`operationKey=auth.update_profile`、`module=system_accounts`、`action=update`、`mode=self`、`changes=[displayName]`；入队失败不影响业务成功 |
| session touch | 有效 session 且 `last_seen_at < now - 60s` 时更新当前 `system_sessions.last_seen_at=now`；fresh session 跳过写库；touch 错误按内部错误返回，不继续执行业务 |
| 内部错误 | `500 { message: "服务器内部错误" }`，不泄露数据库、密钥或连接串 |

### `POST /__aisys__/api/auth/change-password`

| 项 | 内容 |
| --- | --- |
| 路径 | `POST /__aisys__/api/auth/change-password` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 读取 `juhe_ai_session` Cookie，按 SHA-256 token hash 查询 `system_sessions`，校验 session 未过期、session active、系统账户 active，并在通过鉴权后按写模式 touch 当前 session；该路由不能套普通管理端 middleware，否则普通初始密码用户会被提前 403 |
| 请求体 | 严格 JSON object，只允许 `oldPassword?: string` 和 `newPassword: string`；未知字段、缺少 `newPassword`、非字符串、`null`、`newPassword` 少于 4 个字符或 `oldPassword=""` 返回 `400 { message: "密码参数无效" }` |
| 空白字符 | `oldPassword` 或 `newPassword` 包含任意空白字符时返回 `400 { message: "登录密码不能包含空格" }` |
| 初始密码用户 | 普通 `user` 且有效 `mustChangePassword=true` 时可只提交 `{ newPassword }`，不校验旧密码；成功后 `mustChangePassword=false` |
| 普通改密 | 有效 `mustChangePassword=false` 时必须提交 `oldPassword`，缺失返回 `400 { message: "请填写当前密码" }`；旧密码校验失败或校验账号不等于当前 session 账号返回 `400 { message: "当前密码不正确" }` |
| 管理员角色 | `admin` / `super_admin` 的有效 `mustChangePassword` 始终为 `false`，因此管理员改密也必须提交旧密码 |
| 密码 hash | Go 使用 Node 兼容格式 `pbkdf2$sha512$120000$salt-base64url$derived-base64url`，16 字节随机 salt、32 字节 PBKDF2-SHA512 派生值；响应和 operation log 都不得返回 `password_hash` |
| 成功响应 | `200 { data: { id, username, displayName, description, role, status, mustChangePassword, imageGenerationEnabled, lastLoginAt, createdAt, updatedAt } }` |
| 会话副作用 | 鉴权阶段按 60 秒窗口更新当前 session `last_seen_at`；成功后删除同账号下 `id <> 当前 sessionId` 的 `system_sessions`，保留当前 session；不重新签发 Cookie，不清 Cookie |
| 操作日志 | 当前 Node 自助改密不写 operation log；Go 本切片保持一致。系统账户管理端完整 PATCH 已支持混合字段 diff、撤销 session 数和密码脱敏记录，密码字段最终固定为 `before="未设置"`、`after="已变更"`、`sensitive=true` |
| 内部错误 | `500 { message: "服务器内部错误" }`，不泄露数据库、密钥、旧密码、新密码或密码 hash |

### `POST /__aisys__/api/auth/logout`

| 项 | 内容 |
| --- | --- |
| 路径 | `POST /__aisys__/api/auth/logout` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 不要求当前 session 仍有效；只解析 `juhe_ai_session` Cookie |
| 有 Cookie | 对 Cookie token 做 SHA-256，按 `system_sessions.token_hash` 删除当前 session |
| 无 Cookie | 不报错，直接按已登出处理 |
| 成功响应 | `200 { data: { loggedOut: true } }`，并清除 `juhe_ai_session` Cookie |
| 清 Cookie | Cookie 名称固定 `juhe_ai_session`，`Path=/`，`HttpOnly`，`Max-Age=0`；`Secure` / `SameSite` 复用 `JUHE_AI_COOKIE_SECURE` / `JUHE_AI_COOKIE_SAME_SITE` 运行时配置，和 login 签发 Cookie 保持一致；`SameSite=None` 仍必须同时启用 `Secure=true` |
| 撤销失败 | `500 { message: "服务器内部错误" }`，不清 Cookie，避免前端误判服务端 session 已撤销 |
| 副作用 | 只删除当前 token hash 命中的 session，不撤销同账号其他 session，不写 operation log，不写 Redis |

### `GET /__aisys__/api/auth/sessions`

| 项 | 内容 |
| --- | --- |
| 路径 | `GET /__aisys__/api/auth/sessions` |
| 启用条件 | `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时随 W3 管理端全量路径注册；或在 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 且 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 时作为窄开关单独注册；默认生产 server 不注册 |
| 登录态 | 走普通管理端读鉴权 `ManagementAPIAuthMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active；普通初始密码用户仍按普通管理鉴权返回 `403 must_change_password` |
| 查询参数 | `page` 默认 1，`pageSize` 默认 20 且最大 100；非法整数返回 `400 { message: "会话参数无效" }` |
| 数据范围 | 只读取当前 `systemAccountId` 下 `expires_at > now` 的 session，不跨账户读取，不读取已过期 session |
| 成功响应 | `200 { data: { items, total, hasMore, page, pageSize } }`；`items[]` 字段为 `id`、`current`、`createdAt`、`lastSeenAt`、`expiresAt` |
| 分页口径 | 使用 `pageSize + 1` 窗口判断 `hasMore`，`total` 只表示当前窗口上界，不执行全表精确 `COUNT(*)` |
| 敏感字段 | 不读取 / 返回 `token_hash`、`password_hash`、Cookie token、登录 IP 或 User-Agent |
| 会话副作用 | 不 touch 当前 session，不创建 / 撤销 session，不写 Redis，不写 operation log |

### `DELETE /__aisys__/api/auth/sessions/{id}`

| 项 | 内容 |
| --- | --- |
| 路径 | `DELETE /__aisys__/api/auth/sessions/{id}` |
| 启用条件 | `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时随 W3 管理端全量路径注册；或在 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 且 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 时作为窄开关单独注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 数据范围 | 只按当前 `systemAccountId` 和路径 `sessionId` 删除一条 session；不能跨账户撤销，不能按 token hash 批量撤销 |
| 成功响应 | `200 { data: { id, revoked: true, current } }` |
| 当前 session | `current=true` 时同时清除 `juhe_ai_session` Cookie；清 Cookie 的 `Secure` / `SameSite` 复用 `JUHE_AI_COOKIE_SECURE` / `JUHE_AI_COOKIE_SAME_SITE` |
| 不存在 | `404 { message: "会话不存在" }`；不暴露该 session 是否属于其他系统账户 |
| 参数错误 | 空 session id 或非法分页参数返回 `400 { message: "会话参数无效" }` |
| 会话副作用 | 写鉴权阶段可能 touch 当前 session；删除目标 session 后不写 operation log，不写 Redis |

### `POST /__aisys__/api/system-accounts`

| 项 | 内容 |
| --- | --- |
| 路径 | `POST /__aisys__/api/system-accounts` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 只允许 `super_admin`；普通 `admin` 和 `user` 返回 `403 { message: "需要超级管理员权限" }` |
| 请求体 | 严格 JSON object，只允许 `username`、`displayName`、`description`、`password`、`role`、`status`、`mustChangePassword`、`imageGenerationEnabled`；未知字段、缺少必填、类型错误、`role/status` 非枚举和布尔字段 `null` 返回 `400 { message: "系统账户参数无效" }`；语法错误、非 object 或尾随 JSON 返回 `400 { message: "请求体无效" }` |
| 字段规则 | `username` 最少 2 字符，`displayName` 最少 1 字符，`password` 最少 4 字符；三者包含任意空白字符时分别返回“用户名不能包含空格”“用户名称不能包含空格”“登录密码不能包含空格”；`description` trim 后最长 200 字符，空白 / `null` 表示无说明；`role` 只允许 `admin/user`，默认 `user`；`status` 默认 `active`；`user` 默认 `mustChangePassword=true`，`admin` 有效值固定为 `false`；`imageGenerationEnabled` 默认 `false` |
| PG 写入 | 使用 `w3_management_system_account_create.sql` 在单个事务中写入 `system_accounts`、7 个内置默认分组、6 条非 hybrid 默认普通路由、6 条路由分组绑定和 6 个默认 API Key；通用 OpenAI 兼容默认分组使用 seed provider code `openai`，不是 `openai_compatible`；hybrid 只创建默认分组，不创建默认普通路由或默认 API Key |
| 默认 API Key | 每条非 hybrid 默认普通路由创建一个默认 API Key；明文格式为 `sk-` + 32 字节随机 hex，`key_hash` 为 SHA-256 hex，`key_prefix/key_suffix` 取前后 8 位，完整 key 以 Node 兼容 AES-256-GCM `v1:nonce:tag:ciphertext` 格式写入 `key_secret_encrypted`；响应和 operation log 不返回明文 key |
| 成功响应 | `201 { data: { id, username, displayName, description, role, status, mustChangePassword, imageGenerationEnabled, createdAt, updatedAt } }`；不返回 `password_hash`、默认 API Key 明文或默认资源 ID |
| 操作日志 | best-effort 入队 `operation-log:write`，`operationKey=system_accounts.create`、`module=system_accounts`、`action=create`、`mode=admin`、`resourceType=system_account`、`viewer=admin_managed_my_resource`；`changes.password` 经共享入口清洗为 `before="未设置"`、`after="已变更"`、`sensitive=true`，不记录用户提交的密码 |
| 切片边界 | 这是 `POST /system-accounts` 创建子动作的 Go opt-in 灰度实现；真实 PG+Redis+Asynq operation log integration、浏览器连接真实 Go 后端的前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍未完成 |

### `PATCH /__aisys__/api/system-accounts/{id}`

| 项 | 内容 |
| --- | --- |
| 路径 | `PATCH /__aisys__/api/system-accounts/{id}` |
| 启用条件 | 仅在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册；默认生产 server 不注册 |
| 登录态 | 走管理端写鉴权 `ManagementAPIAuthTouchMiddleware`，读取 `juhe_ai_session`、校验 session 未过期、系统账户 active，并按 60 秒最小间隔 touch 当前 session |
| 权限 | 只允许 `super_admin`；普通 `admin` 和 `user` 返回 `403 { message: "需要超级管理员权限" }` |
| 请求体 | 严格 JSON object，只允许 `displayName?: string`、`description?: string | null`、`password?: string`、`role?: "admin" | "user"`、`status?: "active" | "disabled"`、`mustChangePassword?: boolean`、`imageGenerationEnabled?: boolean`，至少提交一个字段；`username` 返回 `400 { message: "用户账户创建后不能修改" }`；未知字段、非预期类型、`null` 布尔值 / 必填字符串字段和尾随 JSON 均返回 400 |
| 字段规则 | `displayName` 不允许任何空白字符，大小写不敏感全局唯一，冲突返回 `409 { message: "用户名称已存在" }`；`description` 会 trim，`null` / 空白字符串表示清空，最大 200 字符；`password` 少于 4 个字符返回参数无效，包含任意空白字符返回 `400 { message: "登录密码不能包含空格" }`；管理端不允许把角色改为 `super_admin`；`admin` / `super_admin` 角色有效 `mustChangePassword` 固定为 `false` |
| PG 写入 | 使用 `w3_management_system_account_update.sql` 单条 CTE：先 `MATERIALIZED` 锁定并聚合启用 `super_admin` 集合，再锁目标账户；如果目标是最后一个启用 `super_admin` 且请求使最终角色 / 状态不再是启用超级管理员，返回 `409 { message: "至少保留一个启用的超级管理员" }`；否则原子更新资料、状态、图像权限、密码 hash 和 `updated_at`；目标不存在返回 `404 { message: "系统账户不存在" }` |
| 会话副作用 | 请求包含 `password` 时删除目标账户全部 `system_sessions`；请求包含 `status:"disabled"` 时也删除目标账户全部 `system_sessions`，即使状态本身未变化也会按 Node 语义撤销 |
| 缓存副作用 | `status` 真实变化时清理 gateway runtime cache 和 API Key validation cache，原因固定为 `system_account_status_changed`；`status` 未变但 `imageGenerationEnabled` 真实变化时清理同一批缓存，原因固定为 `system_account_image_generation_changed`；二者同时变化时状态原因优先 |
| 成功响应 | `200 { data: { id, username, displayName, description, role, status, mustChangePassword, imageGenerationEnabled, lastLoginAt, createdAt, updatedAt } }`；不返回 `password_hash` |
| 操作日志 | best-effort 入队 `operation-log:write`；包含 `password` 时使用 `operationKey=system_accounts.reset_password` / `action=reset_password` / `summary=重置系统账户密码：...`，否则使用 `system_accounts.update` / `update`；`changes` 记录真实字段 diff，密码字段经共享入口清洗为 `before="未设置"`、`after="已变更"`、`sensitive=true`，不记录用户提交的密码；`metadata.revokedSessionCount` 记录撤销 session 数 |
| 切片边界 | 这是 `PATCH /system-accounts/{id}` 完整 mixed partial update 的 Go opt-in 灰度实现；仍未执行浏览器连接真实 Go 后端的前端系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除，因此不能标记生产接管 |

## 本轮不迁移范围

- 改密提醒和系统账户页生产切流；当前已迁移 `POST /system-accounts` 和 `PATCH /system-accounts/{id}` 完整 mixed partial update，但不代表系统账户写接口已生产接管。
- 登录生产接管、前端登录页 smoke、反向代理切流和 Node `/auth` 删除
- 完整会话管理生产接管和安全日志；当前已补当前用户会话列表 / 单条撤销的 Go opt-in 实现，Cookie 登录 / 登出 / 当前 session 撤销的 `Secure` / `SameSite` 属性已对齐，但真实 HTTPS、`SameSite=None`、反向代理和浏览器前端 smoke 部署验收仍未完成；系统账户创建 / 重置密码日志已通过统一清洗入口固定为低泄漏敏感标记
- 系统指标迁移不属于本 W3 登录小切片；本切片只保持 Go 指标规划链接，不新增或改写 `/stats/system-metrics`，也不把 Node event-loop / DB service / SQLite 文件指标模拟成 Go 指标

## Go 实现范围

- `managementauth.CaptchaService` 固定验证码发放、Redis state challenge、Redis `GETDEL` 一次性消费、答案归一化、60 次 / 分钟生成限流和 PNG data URL 输出；只在验证码启用时由 handler 调用，关闭时 handler 直接返回 `required=false`；不引入额外 captcha 库，不退回进程内 Map。
- `managementauth.LoginService` 固定登录顺序：严格输入校验、按配置执行验证码一次性消费或跳过验证码校验、Redis 登录失败锁检查、active 账号密码校验、失败计数 / 锁定、成功清锁、PG session 创建和 `last_login_at` 更新；session token 只以 SHA-256 hash 落库，响应不返回 `passwordHash`。
- `managementauth.LoginGuardService` 使用 Redis state 做 IP / 用户名失败窗口和锁定，key 只保存 SHA-256 派生值，不落明文 IP 或用户名；失败计数和锁定通过 Redis Lua 原子完成，不退回进程内 Map。
- `managementauth.Authenticator` 保留受保护后台接口的初始密码 403 拦截，同时新增 current-user 读取分支，允许前端读取 `mustChangePassword` 状态；写鉴权分支复用同一 session 查询并按 60 秒最小间隔 touch `last_seen_at`。
- `managementauth.ProfileService` 只处理当前用户 `displayName` 更新，固定 no-op、空白字符、重复名和管理员有效 `mustChangePassword=false` 语义。
- `managementauth.PasswordService` 固定 Node 兼容 PBKDF2-SHA512 密码 hash / verify、初始密码用户免旧密码、普通改密校验旧密码、成功清除 `mustChangePassword` 和撤销其他 session 语义。
- `managementauth.Authenticator.LogoutCookie` 复用 Node 兼容 Cookie 解析和 SHA-256 token hash，只删除当前 session token；没有 Cookie 时直接成功。
- `managementauth.SessionService` 固定当前用户会话管理：列表只读取当前系统账户未过期 session，按 `pageSize + 1` 窗口计算 `hasMore` 和 `total` 上界，不执行精确 `COUNT(*)`；撤销只允许按当前系统账户 ID + session ID 删除，撤销当前 session 时交由 HTTP 层清 Cookie。
- `managementsystemaccounts.Service.Create` 固定管理端 create 子动作：严格校验 `username/displayName/password/role/status/mustChangePassword/imageGenerationEnabled/description`，用 Node 兼容 PBKDF2-SHA512 写 `password_hash`，在 service 层为 6 条非 hybrid 默认普通路由生成默认 API Key 明文、hash、前后缀和 Node 兼容 AES-GCM 密文，再交由 PostgreSQL 事务创建系统账户、7 个默认分组、6 条默认普通路由、6 条路由分组绑定和 6 个默认 API Key。
- `managementsystemaccounts.Service.ResetPassword` 固定管理端 reset-password 子动作：只接收目标系统账户 ID、明文新密码和可选 `mustChangePassword`，在 service 层完成空白字符 / 长度校验和 Node 兼容 hash，不处理创建、完整更新或默认资源补齐。
- `managementsystemaccounts.Service.UpdateStatus` 固定管理端 status-only 子动作：只接收目标系统账户 ID 和 `active` / `disabled` 状态；非枚举状态直接返回参数错误，不做脏数据兜底；PostgreSQL CTE 负责并发锁定、最后一个启用 `super_admin` 保护、状态写入和禁用 session 删除；SQL guard 固定先 `MATERIALIZED` 锁定并聚合启用 `super_admin` 集合，再锁目标账户，避免并发停用不同超级管理员时退化成互锁或绕过保护。
- `managementsystemaccounts.Service.UpdateImageGeneration` 固定管理端 imageGeneration 子动作：只接收目标系统账户 ID 和布尔值，PG 单语句更新图像生成权限，真实变化后清理 gateway runtime cache 和 API Key validation cache。
- `managementsystemaccounts.Service.UpdateProfile` 固定管理端 profile-only 子动作：只接收目标系统账户 ID、`displayName`、`description`、`role`、`mustChangePassword` 四类非网关字段；不允许 `super_admin` 写入、空白显示名、超长说明、空字段请求或未知字段；PostgreSQL CTE 负责显示名唯一约束最终裁决、最后一个启用 `super_admin` 降级保护和字段更新，不处理密码、状态或图像生成权限混合更新。
- `managementsystemaccounts.Service.Update` 固定管理端完整 mixed PATCH：接收目标系统账户 ID 和 `displayName/description/password/role/status/mustChangePassword/imageGenerationEnabled` 任意组合，在 service 层完成空白字符、枚举、说明长度、Node 兼容密码 hash 和显示名重复错误映射；PostgreSQL 单语句 CTE 负责并发锁定、最后一个启用 `super_admin` 保护、全字段原子更新、提交 `password` 或 `status="disabled"` 时撤销目标全部 session；service 层按 Node 语义在状态变化时优先清理 gateway runtime cache / API Key validation cache，否则在图像权限变化时清理缓存。
- `httpapi.NewManagementCaptchaHandler` 不要求登录，按 `JUHE_AI_TRUST_PROXY` 解析客户端 IP；验证码启用时返回 Node 兼容 `{ data: { required: true, captchaId, image, expiresAt } }`，并把生成限流映射为 429 + `Retry-After`，关闭时直接返回 `{ data: { required: false } }`。
- `httpapi.NewManagementLoginHandler` 不要求已有 session，严格解析登录 JSON；验证码启用时要求 `captchaId/captchaCode`，关闭时允许省略并不调用验证码 verifier；按 `JUHE_AI_TRUST_PROXY` 解析客户端 IP，成功后签发 `juhe_ai_session` Cookie，并把登录参数、验证码、凭据和登录锁错误映射为 Node 兼容中文文案。
- `httpapi.NewManagementCurrentUserHandler` 直接调用 current-user 鉴权分支并返回当前用户摘要，不套用普通管理端 middleware。
- `httpapi.NewManagementProfileUpdateHandlerWithOperationLog` 走管理端写鉴权 middleware，严格解析 JSON，成功后返回当前用户摘要并 best-effort 写 `auth.update_profile` 操作日志。
- `httpapi.NewManagementPasswordChangeHandler` 直接调用 current-user 写鉴权分支，严格解析 `oldPassword` / `newPassword` JSON，成功后返回完整系统账户摘要；它不套用普通管理端 middleware，避免初始密码用户被提前拦截。
- `httpapi.NewManagementLogoutHandler` 直接调用 logout 分支；它不套用普通管理端 middleware，避免过期 session 或无 Cookie 时被提前拦截；清 Cookie 复用登录 Cookie 的 `Secure` / `SameSite` 运行时配置。
- `httpapi.NewManagementSessionListHandler` 走普通管理端读鉴权上下文，返回当前用户未过期 session 列表，不 touch；`httpapi.NewManagementSessionRevokeHandler` 走写鉴权上下文，只撤销当前用户自己的指定 session，撤销当前 session 时清 Cookie。
- `httpapi.NewManagementSystemAccountCreateHandlerWithOperationLog` 只实现 `POST /system-accounts` 创建子动作，要求 `super_admin`，严格拒绝未知字段、非法枚举、`null` 布尔和用户名 / 用户名称 / 密码空白字符；创建成功后通过共享入口 best-effort 写脱敏 operation log。
- `httpapi.NewManagementSystemAccountPatchHandlerWithOperationLog` 实现 `PATCH /system-accounts/{id}` 完整 mixed partial update，要求 `super_admin`，严格拒绝 `username`、未知字段、尾随 JSON、`null` 布尔值和非法类型；成功后通过共享入口 best-effort 写 operation log，混合字段记录真实 diff，密码最终只保留 `未设置/已变更` 敏感标记。
- `router.go` 在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时注册 `GET /auth/captcha`、`POST /auth/login`、`GET /auth/me`、`PATCH /auth/me`、`POST /auth/change-password`、`POST /auth/logout`、`GET /auth/sessions`、`DELETE /auth/sessions/{id}`、`POST /system-accounts` 和 `PATCH /system-accounts/{id}` 完整 mixed PATCH；在 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 且 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 时只注册 `GET /auth/sessions` 和 `DELETE /auth/sessions/{id}`，即使装配了其他管理 handler 也不会暴露 W2 / W4 或其他 W3 管理路径；`PATCH /auth/me`、`DELETE /auth/sessions/{id}`、`POST /system-accounts`、`PATCH /system-accounts/{id}` 与其他已迁移管理端写接口一样必须走 `ManagementAPIAuthTouchMiddleware`，router 会拒绝缺失 touch middleware 的写接口装配；`GET /auth/captcha` 明确跳过 system API IP 读限流，`POST /auth/login` 不加入写鉴权 touch middleware。
- `server.go` 复用同一个 `Authenticator` 注入 current-user、change-password、logout 和 session handler，并注入 Redis state captcha service、login guard、login service、session service、profile / password service 与 operation log queue；`JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED=true` 且总管理开关关闭时只构造 auth middleware 与 session list / revoke handler，不构造 captcha、login、system account、W2 或 W4 handler。
- integration smoke 在现有管理端 session 鉴权 smoke 中覆盖 `GET /auth/me` 当前用户响应且不 touch、`GET /auth/sessions` 当前用户未过期会话列表且不 touch、`DELETE /auth/sessions/{id}` 当前用户会话撤销、`PATCH /auth/me` 真实 PG 更新 / operation log payload / stale session touch、`POST /auth/change-password` 真实 PG 密码 hash / 初始改密 / 普通改密 / fresh session touch 跳过 / stale session touch / 其他 session 撤销、`POST /auth/logout` 清 Cookie、清 Cookie `Secure` / `SameSite` 属性和登出后旧 Cookie 401；`TestW3ManagementSystemAccountCreatePostgresRedisSmoke` 覆盖 `POST /system-accounts` 真实 HTTP 创建、真实 PG 默认资源 fanout、默认 API Key 密文落库和 Asynq `operation-logs` 入队 / ingest worker 写 PG；`TestW3ManagementSystemAccountsPatchPostgresRedisSmoke` 覆盖既有 PATCH 真实链路的 HTTP PATCH、真实 PG 更新、目标 session 撤销、Asynq `operation-logs` 入队、ingest worker 写 PG、敏感 operation log 脱敏和最后一个启用 `super_admin` 降级保护。完整 mixed PATCH 已由 service / handler / SQL guard 快速测试覆盖；create 子动作当前由 service / handler / SQL guard 快速测试和 create smoke 代码覆盖，固定 7 个默认分组、6 条非 hybrid 默认普通路由 / 默认 API Key、默认 API Key 密文落库和创建日志脱敏。上述真实 smoke 需要 Docker / testcontainers 健康环境；本机 Docker provider 不可用时输出 `SKIP`，不计真实 PG+Redis+operation-log integration 通过。

## 后续拆分建议

1. 系统账户写接口：下一步应在 Docker/testcontainers 健康环境复跑真实 PG+Redis+Asynq create / mixed patch smoke，并补浏览器连接真实 Go 后端的系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除证据。`POST /system-accounts` 已补默认 API Key secret 加密保存、7 个默认内置分组、6 条非 hybrid 默认普通路由 / 默认 API Key fanout 事务、创建 operation log 脱敏策略和 create smoke 代码，`PATCH /system-accounts/{id}` 已补完整 mixed partial update；前端已补本地调用 / UI 边界 smoke，但当前仍只是 Go opt-in 灰度路径。
2. 完整会话管理收口：当前用户会话列表 / 单条撤销已进入 Go opt-in，但后续生产接管、安全日志、浏览器 Cookie 行为和 Cookie 部署验收仍必须继续沿用“读不 touch、写按 60 秒窗口 touch、logout 不要求有效 session、login 是唯一 session 创建 owner”的规则；本轮不代表浏览器、HTTPS 或生产反代验收完成。
3. 前端 auth smoke 与生产切流：本轮已补本地前端 auth / session / 系统账户页调用和 UI 边界 smoke；后续浏览器连接真实 Go 后端的 smoke 仍必须覆盖登录页验证码、登录成功 Cookie、初始密码修改、头像菜单、会话管理、登出和 session 过期处理；Node / Go 不能并行各自维护生产登录 session 创建 owner。

## 验证项

在 `backend-go/` 下运行：

```powershell
sqlc generate
go test ./internal/modules/managementauth ./internal/httpapi ./internal/platform/redis ./internal/store/postgres ./internal/config ./internal/app -count=1
go test ./...
go test -tags=integration ./internal/testkit/integration -run '^$'
go test -race ./...
go vet ./...
go build ./...
golangci-lint run
govulncheck ./...
go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthPostgresSmoke -count=1
go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementLoginPostgresRedisSmoke -count=1
go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountCreatePostgresRedisSmoke -count=1
go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountsPatchPostgresRedisSmoke -count=1
```

在仓库根目录运行：

```powershell
$env:CI = 'true'
pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke
pnpm --filter juhe-ai-frontend typecheck
pnpm --filter juhe-ai-frontend build
```

本轮前端 auth / 系统账户页本地 smoke 已补：`frontend/src/types/domain/identity.ts` 新增当前用户会话摘要 / 列表 / 撤销结果类型，`frontend/src/api/domains/auth.ts` 新增 `GET /auth/sessions` 和 `DELETE /auth/sessions/{id}` 调用，`AppHeader.vue` 头像菜单新增“会话管理”，`AppLayout.vue` 接入 `SessionManagementModal.vue`，支持分页刷新、撤销其他会话和撤销当前会话后清理本地登录态并返回登录页；系统账户页仍只对 `super_admin` 展示创建 / 编辑 / 重置密码入口，创建和完整 mixed PATCH 继续走 `api.systemAccounts.create/update`。新增 `pnpm --filter juhe-ai-frontend test:w3-auth-system-account-smoke` 固定 auth/session/system-account 前端 API 路径、参数、敏感字段不暴露、头像菜单与会话弹窗接线、系统账户页 super_admin 写入口和统一 update API。该 smoke 不连接真实 Go 后端，也不验证浏览器 Cookie、HTTPS、反向代理或生产切流。

本轮 `POST /system-accounts` 创建子动作已在 Go `go1.26.5 windows/amd64` 下执行 `gofmt`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1` 和 `go test -tags=integration ./internal/testkit/integration -run '^$'`，均通过；覆盖 service 默认值归一化、6 个默认 API Key 生成、Node 兼容 AES-GCM 密文、HTTP `super_admin` 权限、严格 JSON / 尾随 JSON 拒绝、密码日志敏感脱敏、PostgreSQL SQL guard、7 个默认分组和 hybrid 不生成默认普通路由 / 默认 API Key 的边界。本轮新增 `TestW3ManagementSystemAccountCreatePostgresRedisSmoke`，测试代码覆盖真实 HTTP `POST /system-accounts`、真实 PG 系统账户写入、7 个默认分组、6 条非 hybrid 默认普通路由、6 条路由分组绑定、6 个默认 API Key、默认 API Key `key_secret_encrypted` 密文落库、响应不回显默认资源 ID / 明文 API Key / 密码，以及 Asynq operation log 入队和 worker 写入；实际执行因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PostgreSQL / Redis / Asynq / operation log worker 通过。前端系统账户管理页本地调用 / UI 边界 smoke 已通过；浏览器连接真实 Go 后端的系统账户页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍未执行。

本轮 logout Cookie 安全属性收口已在 Go `go1.26.5 windows/amd64` 下执行 `gofmt`、`go test ./internal/httpapi ./internal/app -count=1` 和 `go test ./internal/modules/managementauth ./internal/store/postgres -count=1`，均通过；覆盖 `POST /auth/logout` 清 Cookie 复用 `JUHE_AI_COOKIE_SECURE` / `JUHE_AI_COOKIE_SAME_SITE` 后的 `Secure` / `SameSite=None` 响应属性。本轮前端本地 smoke 已固定 logout API 调用路径；真实 HTTPS、浏览器 Cookie 行为、反向代理和真实后端前端 logout smoke 仍待生产切流阶段验证。

本轮完整 mixed PATCH 已在 Go `go1.26.5 windows/amd64` 下执行 `sqlc generate`、`gofmt`、`go mod tidy -diff`、`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres -count=1`、`go test ./... -count=1`、`go vet ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`git diff --check` 和本次改动 Markdown 相对链接检查，均通过；新增 `ManagementSystemAccountUpdater` port、`UpdateManagementSystemAccount` CTE、PostgreSQL adapter、`managementsystemaccounts.Service.Update`、统一 PATCH handler、混合字段 operation log、service / handler / SQL guard / row mapper 测试，覆盖 `displayName/description/password/role/status/mustChangePassword/imageGenerationEnabled` 混合更新、状态优先缓存失效、密码 / 禁用撤销 session、显示名唯一冲突、最后一个启用 `super_admin` 保护和密码日志脱敏。真实 PG+Redis+Asynq mixed PATCH integration、浏览器连接真实 Go 后端的系统账户管理页 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍未执行。

本轮新增 `TestW3ManagementSystemAccountsPatchPostgresRedisSmoke`，已在当前 Go 测试编译路径执行：`go test ./internal/modules/managementsystemaccounts ./internal/httpapi ./internal/store/postgres ./internal/app -count=1` 通过；`go test -v -tags=integration ./internal/testkit/integration -run TestW3ManagementSystemAccountsPatchPostgresRedisSmoke -count=1` 因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PostgreSQL / Redis / Asynq / operation log worker 通过。

既有 reset-password、status-only、imageGeneration 和 profile-only 子动作已通过各自目标测试，并保留 service 窄方法用于小范围复用；HTTP `PATCH /system-accounts/{id}` 当前已改为统一 mixed PATCH。`TestW2ManagementAuthPostgresSmoke`、`TestW3ManagementLoginPostgresRedisSmoke`、`TestW3ManagementSystemAccountCreatePostgresRedisSmoke` 与 `TestW3ManagementSystemAccountsPatchPostgresRedisSmoke` 当前因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不能算真实 PostgreSQL / Redis / Asynq / operation log worker integration 通过；前端系统账户管理页本地调用 / UI 边界 smoke 已通过，但浏览器连接真实 Go 后端 smoke、生产单 owner 切流和 Node `/system-accounts` 删除仍未执行。

本轮 login 小切片已在 Go `go1.26.5 windows/amd64` 下执行 `sqlc generate`、`gofmt`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/platform/redis ./internal/store/postgres ./internal/config ./internal/app -count=1`、`go test ./...`、`go test -tags=integration ./internal/testkit/integration -run '^$'`、`go test -race ./...`、`go vet ./...`、`go build ./...`、`golangci-lint run`、`govulncheck ./...`、`go mod tidy -diff`、`git diff --check` 和本次改动 Markdown 相对链接检查，均通过；Node 对照已执行 `pnpm --filter juhe-ai-backend test:captcha-rate-limit`、`test:auth-memory-maintenance-boundary`、`test:system-account-session-driver`、`test:auth-must-change-password-boundary` 和 `test:performance-system-api-smoke`，均通过。新增 `TestW3ManagementLoginPostgresRedisSmoke` 覆盖 captcha -> login -> PG session / token hash / `last_login_at` -> Cookie -> `/auth/me` 和登录失败锁触发路径；当前本机 Docker / testcontainers 不可用时该真实 PG+Redis smoke 输出 `SKIP`，不能算真实容器集成通过。

验证码开关契约以当前 Node / Go 测试和 `backend-go/README.md` 为准：Node `test:auth-captcha-environment-guard` 覆盖 `required=false`、省略验证码字段后仍创建正式 session Cookie、错误密码仍被拒绝；Go `management_captcha_test.go`、`management_login_test.go` 和 `managementauth/login_test.go` 覆盖关闭时不发放 challenge、handler 可省略 `captchaId/captchaCode`、service 不调用 verifier，同时保留密码校验和登录失败锁。这些是契约和快速测试证据，不代表真实生产流量已切换到 Go，也不构成 Node `/auth` 删除证据。

`TestW2ManagementAuthPostgresSmoke` 名称沿用 W2 session 鉴权基线 smoke，本轮已扩展覆盖 `GET /__aisys__/api/auth/me`、`PATCH /__aisys__/api/auth/me`、`POST /__aisys__/api/auth/change-password`、`POST /__aisys__/api/auth/logout`、`last_seen_at` 读写差异、60 秒 touch 节流、改密后其他 session 失效和登出后旧 Cookie 失效。它需要 Docker / testcontainers 可用；本轮执行因 Windows Docker 不可用输出 `SKIP`，不能算真实 PostgreSQL integration 通过。

本轮当前用户会话列表 / 撤销已在 Go `go1.26.5 windows/amd64` 下执行 `sqlc generate`、`gofmt`、`go mod tidy -diff`、`go test ./internal/modules/managementauth ./internal/httpapi ./internal/store/postgres ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...` 和 `go test -tags=integration ./internal/testkit/integration -run '^$'`，均通过；`TestW2ManagementAuthPostgresSmoke` 已扩展覆盖 `GET /__aisys__/api/auth/sessions` 当前用户未过期会话列表不 touch、`DELETE /__aisys__/api/auth/sessions/{id}` 当前用户会话撤销和撤销后当前 session touch。实际运行 `go test -v -tags=integration ./internal/testkit/integration -run TestW2ManagementAuthPostgresSmoke -count=1` 因 Windows Docker / testcontainers provider 不可用输出 `SKIP`，不计真实 PostgreSQL integration 通过。前端会话管理入口和本地调用 / UI 边界 smoke 已通过；浏览器 Cookie 行为、安全日志、生产单 owner 切流和 Node `/auth` 删除仍未执行。

本轮 session-only 窄开关已补：新增 `JUHE_AI_MANAGEMENT_AUTH_SESSIONS_ENABLED`，在 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 时只挂载 `GET /__aisys__/api/auth/sessions` 和 `DELETE /__aisys__/api/auth/sessions/{id}`；配置校验只要求 Redis state，不要求 Redis queue；router 测试覆盖窄开关下其他管理 handler 即使被装配也返回 404，app 测试覆盖只构造 auth middleware 与 session list / revoke handler。已执行并通过 `gofmt`、`go test ./internal/config ./internal/httpapi ./internal/app -count=1`、`go test ./... -count=1`、`go vet ./...`、`go mod tidy -diff`、`go test -tags=integration ./internal/testkit/integration -run '^$' -count=1`、`git diff --check` 和本次改动 Markdown 相对链接检查；真实 PostgreSQL session smoke、生产切流和 Node `/auth` 删除仍待后续执行。

## 接管与删除门禁

- `GET /auth/captcha`、`POST /auth/login`、`GET /auth/me`、`PATCH /auth/me`、`POST /auth/change-password`、`POST /auth/logout`、`GET /auth/sessions`、`DELETE /auth/sessions/{id}`、`POST /system-accounts` 和 `PATCH /system-accounts/{id}` 完整 mixed PATCH 只代表 W3 验证码、登录 / session 创建、当前用户读、资料更新、当前用户改密、当前会话撤销、当前用户会话列表 / 单条撤销、系统账户创建和系统账户完整更新可灰度验证，不能删除 Node `/auth` 或 `/system-accounts` router。
- `POST /auth/login` 只证明验证码校验、Redis 登录失败锁定、active 账号密码校验、PG session 创建、`last_login_at` 更新和 Cookie 签发的小闭环；仍不能作为完整 `/auth` 接管证据。
- W3 必须等完整会话管理生产接管、安全日志、真实 PG+Redis 验证、前端本地 smoke 与浏览器连接真实 Go 后端的 auth / 系统账户管理页 smoke、生产单 owner 切流和 Node `/auth` / `/system-accounts` 删除证据全部完成后，才能进入 `待删除 Node`。
- 删除 Node 前必须确认前端登录页、初始密码修改页、头像菜单、系统账户管理页和权限拦截全部指向 Go 单 owner。
- 删除后使用 `rg "authRouter|requireSessionContext|system-accounts|findSessionByTokenAsync|createSessionAsync|revokeSessionAsync" backend/src` 检查旧入口，只允许未迁移历史文档引用。
