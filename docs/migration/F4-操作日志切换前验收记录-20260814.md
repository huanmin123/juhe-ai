# F4 操作日志切换前验收记录

> 验收日期：2026-08-14
> 验收提交：`78d1cdafb5a1fb74befb148754458d4fd22f1c0a`
> 范围：开发环境和仓库静态验收；不包含生产历史数据写入、candidate 切流或生产回滚。

## 1. 结论

当前提交的 F4 切换前实现通过：Go 是操作日志的唯一代码 owner，Node 只保留直接签名 RPC producer 和读 API adapter；Node queue、Redis Stream、IPC、writer、retention、schema owner 已从活跃路径清零。独立只读复审结论为：`local_implementation=pass`，`production_cutover=unavailable`。

不能据此宣称 F4 已成为生产 owner。归档清单仍保持 `pre_cutover_source_archive`，`cutoverCommit` 与 `rollbackCommit` 必须等 candidate 验收和可审计回滚提交完成后再填写。

## 2. 已执行验证

### Go 与 F3 边界

- `rtk go test ./... -count=1`：161 项通过。
- `rtk go vet ./...`：通过。
- `rtk go test -race ./internal/operationlog ./internal/auditlog -count=1`：95 项通过。
- F3 与 F4 共用的 SQLite 物理路径 helper、F3 listener/store/retention 定向测试通过；未修改 F3 协议、schema、blob、hot-search 或保留语义。

### Node、producer 与读 API

- `pnpm --filter juhe-ai-backend typecheck`：通过。
- `pnpm --filter juhe-ai-backend test:operation-log-go-rpc`：通过，覆盖签名一次提交、稳定 `id`/`createdAt`、viewer 传递和无 retry/fallback。
- `pnpm --filter juhe-ai-backend test:operation-log-performance-producer`：使用开发 PgBouncer PostgreSQL 与 cache/state/queue 三套 Redis，通过异步 settings 读取和 fire-and-forget 业务返回验证。
- `JUHE_AI_OPERATION_LOG_SYSTEM_API_SMOKE=1`、`JUHE_AI_OPERATION_LOG_OAUTH_SMOKE=1`、`JUHE_AI_OPERATION_LOG_WORKER_SMOKE=1` 的 Go smoke：32 个 producer 文件分为一般管理/外部 API、四类 OAuth、stats/table-monitor worker 三组，均完成 Node -> Go -> Node 读回。
- 管理/个人默认窗口、前端默认窗口和后台多进程拓扑回归通过。

### PostgreSQL 与部署入口

- fresh F4 PostgreSQL/PgBouncer smoke：schema catalog、事务超时、lease、幂等、viewer/search、同秒排序、详情裁剪、锁等待和 retention 通过。
- 旧 Node viewer 三列主键的 251 条记录 PostgreSQL 离线迁移 smoke：事实篡改拒绝、四列主键升级、search terms 重建、重复执行 no-op 通过。
- 两个临时数据库均已删除并核验不存在；主开发库未修改，Redis 主 namespace 未清理或改写。
- `pnpm test:dev-go-sidecar-env`、`pnpm test:release-go-sidecar-launchers`、`pnpm test:docker-go-sidecar-isolation`：通过。
- `pnpm test:macos-operations`：Windows 上的静态、语法、负向参数和 dry-run 门禁通过；Darwin 专属 `launchctl/lsof/curl` 执行未在 Windows 上伪造为通过。

## 3. 代码与归档核对

- 源码扫描固定 producer 数量为 32 个，和 F4 契约清单一致。
- `migration-backup/node/operation-log-write-retention/manifest.json` 的 21 个归档文件 SHA-256 与原路径核对一致。
- 活跃 `backend/src` 未发现旧 `operation-log-queue`、Node operation-log repository、Redis operation-log stream、`background_worker_operation_logs` 或 Node operation-log retention/schema owner；回归脚本中的负向断言不属于活跃 owner。
- F4 使用独立 loopback 端口、secret、数据库路径和 supervisor component；F3 仍为独立 listener/store/component。

## 4. 未关闭门禁

以下事项必须在明确授权的生产窗口内完成，不能由本记录或开发 smoke 代替：

1. 对生产旧 Node operation-log 数据执行完整备份、双停机确认和离线迁移；按主表、targets、viewers、search terms 计数及首尾样本核验。
2. 启动独立 candidate，检查 F3/F4 health、真实签名 Node -> Go -> Node 读回、已登录管理页和至少一个业务数据页。
3. 原子替换 route fragment 并 reload，短观察日志和业务页；确认旧 Node operation-log queue/consumer 指标不再增长。
4. 创建可审计 cutover commit 和 rollback commit，随后才更新归档 manifest 的两个字段。

在上述门禁完成前，维护者只能把 F4 描述为“切换前实现完成”，不得描述为“生产已接管”。
