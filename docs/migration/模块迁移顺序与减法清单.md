# 模块迁移顺序与减法清单

## 1. 使用方式

本文件是后续迁移执行的主清单。每迁移一个模块，都必须更新状态、Go owner、Node 删除证据和验证记录。

状态建议：

- `未开始`：尚未进入迁移。
- `契约固定`：已生成当前 Node 行为契约、fixture 或测试清单。
- `Go 实现中`：Go 代码开发中。
- `待删除 Node`：Go 已通过测试，但 Node 旧代码还未删。
- `已接管`：Go 是唯一 owner，Node 旧入口已删除。
- `阻塞`：存在设计、数据、测试或部署阻塞。

## 2. 迁移波次

| 波次 | 模块 | 迁移原因 | Go 目标实现范围 | Node 删除范围 | 必须验证 |
| --- | --- | --- | --- | --- | --- |
| W0 | Go 工程与 PG/Redis 基线 | 后续模块迁移的共同基础，避免业务模块迁移时临时直连 SQL 或调用 Node | 配置、日志、统一响应、错误、测试工具、健康检查、PostgreSQL schema / seed / store adapter、Redis cache / state namespace、Asynq queue | 无业务删除 | `go test ./...`、Redis DB 隔离、PG/Redis/Asynq smoke、Windows / Linux / macOS 构建基线 |
| W1a | 公开设置读接口 | 无登录、只读、字段少，适合作为第一个业务接口 PoC | `GET /__aisys__/api/settings/public`，返回 `{ data: { appName, appIcon } }`，Go 读取 `juhe_business.global_settings` 和 `system_settings`，使用 Redis state 做原子 IP read rate limit | 正式接管前不删除；接管后删除 Node 对应 public settings route/service 入口 | 匿名 200、`Cache-Control: no-store`、只暴露 `appName/appIcon`、Redis 原子 minute / burst 限流、客户端 IP 识别遵循 `JUHE_AI_TRUST_PROXY` 且默认不信任公网请求头、`w1a-public-settings-smoke` 真实 PG/Redis smoke、错误不泄露内部依赖 |
| W1b | `/__aipublic__` 外部维护接口 | 公开来源接口契约复杂，包含 Bearer 鉴权、scope、限频、日志和 CRUD | API Key、路由策略、分组、AI 账户等 16 个 `/__aipublic__/*` 外部维护接口；当前四类资源纵切面代码均已补，生产 router 已有 `JUHE_AI_PUBLIC_API_ENABLED=false` 默认关闭的 opt-in guard，并新增 `w1b-public-api-smoke` 独立灰度验证入口，不包含管理端来源 / token 管理 API | 整个 W1b 前缀完成 Go 接管后删除 Node external integrations route/service/log 写入入口；单个公开资源纵切面、opt-in guard 或 maintenance smoke 完成不能删 Node | Bearer 鉴权、scope、限频、公开接口日志、public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD、默认关闭 / 显式开启 router guard、`w1b-public-api-smoke`、分页、业务响应敏感字段不回显、日志快照边界、query/body/response 捕获范围内原值、`Authorization` / Cookie / 来源 token 不进入 headers 快照、API Key 完整 key 只在新增业务响应返回一次、AI 账户凭据加密落库且不回显、错误语义 |
| W2 | 管理端辅助接口 | 风险低，能快速建立前端联调方式 | providers / providers/options、provider models、proxy list / options、system account list / options、authorization grantee accounts / grantee teams / grantee groups、account/group options、group account-options、account tags、API Key 表单依赖的 route-strategy options、operation log 读接口 / 保留清理 worker 和少量默认偏好写入；当前已补 Go `requireAuth` 会话鉴权基线、admin / self 作用域和 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 默认关闭的 opt-in guard。W2 不覆盖 W3 system account create / mixed PATCH 的生产接管、完整 auth / session 生产接管、供应商定义写接口、供应商协议档案写接口、代理手动检测生产接管、授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、账号列表 / 详情完整 summary 响应、operation log 保留清理生产 supervisor 接管和其他写接口 operation log；自定义模型 CRUD 和代理 create/update/delete/test 已作为后续管理写接口补充切片单独登记 | Node 对应只读 route/service 和已迁移删除 / 小写入切片；单个 options、catalog、会话鉴权基线、标签删除 / 独立 PATCH 切片或 opt-in guard 完成不能删除 Node | 登录态、普通用户访问边界、管理员作用域、`my-*` 当前用户作用域、空数据、中文错误、敏感字段白名单、标签删除 204 / 404 / 400 语义、标签 PATCH trim / 去重 / 全量替换 / 窄响应语义 |
| W3 | 登录与系统账户 | 管理接口基础能力 | 登录、会话、系统账户、验证码/限频边界；当前 `GET /__aisys__/api/auth/captcha`、`POST /__aisys__/api/auth/login`、`GET /__aisys__/api/auth/me`、`PATCH /__aisys__/api/auth/me`、`POST /__aisys__/api/auth/change-password`、`POST /__aisys__/api/auth/logout`、`POST /__aisys__/api/system-accounts` 和 `PATCH /__aisys__/api/system-accounts/{id}` 完整 mixed PATCH 已进入 Go opt-in；登录会话列表、登录设备列表和按 ID 撤销已由 PLAN-0150-20260722T022751000Z 撤销，不属于当前或待接管范围 | Node auth/system-accounts route/service/session store | 密码、session、权限、失败限制、安全日志、gateway cache invalidation |
| W4 | 团队与统一授权 | 权限和调度前置事实 | 系统团队、授权 grant、授权列表、授权详情、授权 usage overview、授权用量明细、授权更新、授权有效期更新、授权归还、授权回收、批量授权到期；当前 `GET /__aisys__/api/system-teams`、`GET /__aisys__/api/system-teams/{id}`、`GET /__aisys__/api/my-teams`、`GET /__aisys__/api/my-teams/{id}`、`POST /__aisys__/api/system-teams`、`PATCH /__aisys__/api/system-teams/{id}`、`POST /__aisys__/api/system-teams/{id}/members`、`DELETE /__aisys__/api/system-teams/{id}/members/{memberId}`、`GET /__aisys__/api/authorizations`、`GET /__aisys__/api/authorizations/{id}`、`GET /__aisys__/api/authorizations/{id}/usage`、`GET /__aisys__/api/my-authorizations`、`GET /__aisys__/api/my-authorizations/{id}`、`GET /__aisys__/api/my-authorizations/{id}/usage`、`GET /__aisys__/api/authorizations/usage/team-details`、`GET /__aisys__/api/my-authorizations/usage/team-details`、`GET /__aisys__/api/authorizations/usage/user-details`、`GET /__aisys__/api/my-authorizations/usage/user-details`、`POST /__aisys__/api/authorizations`、`POST /__aisys__/api/my-authorizations`、`PATCH /__aisys__/api/authorizations/{id}`、`PATCH /__aisys__/api/my-authorizations/{id}`、`PATCH /__aisys__/api/authorizations/{id}/expire`、`PATCH /__aisys__/api/my-authorizations/{id}/expire`、`DELETE /__aisys__/api/authorizations/{id}/return`、`DELETE /__aisys__/api/my-authorizations/{id}/return`、`POST /__aisys__/api/accounts/{id}/return-authorization`、`POST /__aisys__/api/my-accounts/{id}/return-authorization`、`POST /__aisys__/api/groups/{id}/return-authorization`、`POST /__aisys__/api/my-groups/{id}/return-authorization`、`DELETE /__aisys__/api/authorizations/{id}` 和 `DELETE /__aisys__/api/my-authorizations/{id}` 已进入 Go opt-in，并已补 `resource_authorization_sources` / `resource_authorization_grants`、`request_quota_hourly_window_configs`、`group_account_stats_dirty`、`authorization_*_usage_*` PostgreSQL schema 基线、授权缓存失效能力、`juhe-ai-worker authorization-expiry-sweep`、`juhe-ai-worker authorization-usage-range-windows-refresh` 和 `juhe-ai-worker gateway-quota-snapshot-build`；团队 PATCH 覆盖禁用 / 启用时的 team source revoke / reactivate，成员新增 / 移除覆盖既有 active team grant / source 的展开和回收，授权列表按 grant 主表分页只读当前状态，授权详情按 grant ID 只读当前关系 / limits / source 明细且不做 usage 聚合，授权 usage overview 和用量明细只读预聚合 range window 不扫明细、不实时 SUM/GROUP BY，授权创建 / 普通更新 / 有效期更新 / grant ID 归还 / 账号分组资源页归还 / 回收写 runtime 刷新、统计全量脏标记和授权缓存失效，批量到期扫描按 grant 到期索引 fixed window + `FOR UPDATE SKIP LOCKED` 标记 `authorization_expired`，授权用量 range window worker 按 `usageStatsTimezone` 从授权日汇总表刷新 hot ranges，网关配额快照构建 worker 按 Node 当前 scope 只读预聚合统计与小时窗口构建成本快照；仍不覆盖授权用量窗口真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管或生产 gateway quota snapshot 消费 | Node teams/authorization route/service/store | 权限矩阵、授权范围、过期和分页 |
| W5 | 供应商、账号、分组、API Key、策略路由、代理、设置 | 后台核心 CRUD | 管理 CRUD、字段校验、敏感字段展示、路由策略绑定；当前自定义模型 CRUD、代理 create/update/delete/test、`GET/PATCH /settings/global` 品牌设置、`GET/PATCH /settings` 系统运行设置、分组创建 / 列表 / 详情 / 更新 / 删除、策略路由列表 / 详情 / 创建 / 更新 / 删除、API Key 列表 / 创建 / 更新 / 删除以及完整密钥查看 / 刷新已进入 Go opt-in，W5 整体未接管 | Node 对应模块 route/service/store 和测试命令 | CRUD、权限、敏感字段脱敏、稳定排序、存储 adapter；settings 额外验证 read auth / write touch、两层限流、`256 KiB` / `413`、53 key、PostgreSQL 有界事务、`000024` 初始 seed、`000043` 删除历史 GPT 通用倍率、时区在线禁改、双失效和 operation log 数值；分组额外验证 progressive pagination、owner / authorized 作用域、预聚合读取、事务保护、路由绑定保护、删除级联 / 统计脏标记、Node/Go stats worker 单 writer，以及默认只读、显式 mutation opt-in、`randomUUID` 临时资源和 finally 清理的真实 Go 管理 smoke；策略路由创建 / 更新 / 删除额外验证 strict JSON/presence、五模式、授权分组稳定锁、重复名称、事务回滚、错误优先级、默认与 API Key 引用保护、提交后跨进程运行态失效和 operation log；API Key 额外验证 AES-GCM 密文、一次性明文、strict partial update、原子硬删除与 cleanup target、validation/runtime/quota 失效、session touch、提交后失败审计、操作日志安全 changes 和失败日志零泄露 |

> 2026-07-10 更新：W4 gateway quota snapshot 已新增 Go worker 可选 Redis runtime state 发布和 Node gateway Redis 模式消费代码路径；上表中的“生产 gateway quota snapshot 消费”后续仅指真实 PG/Redis smoke、生产部署 / supervisor 接管、切流和 Node gateway 删除证据。
| W6 | 使用记录、审计、日志、统计读 API 与 system API 横向限流 | 为 worker 和网关迁移铺路，并替换系统监控读契约；同时补齐已迁移管理业务路由的基础抗滥用边界 | 列表、详情、窗口查询、聚合缓存读取；当前管理侧 / 个人侧 `usage-window`、运行日志列表 / 详情、公开接口日志列表 / 详情、`GET /__aisys__/api/ip-stats` 客户端 IP 列表和四条 IP 策略 POST 已进入 Go opt-in；system API 已补鉴权前 IP read / write 60 秒 + 10 秒 burst limiter、已注册业务路由鉴权后用户 read / write 60 秒 limiter，以及 active / 未过期 client IP allowlist 读取、两层 bypass、30 秒有界缓存和 shared cache version 失效；系统指标读 API 改为 Go runtime / storage / queue / stats freshness 分组 | Node 记录类读 route/service、同名 `usage-window` 路由、运行日志 facets / grep / writer、公开接口日志 writer / 保留清理、system API limiter、客户端 IP 详情和统计 writer / worker、`process_event_loop_*` 读模型和前端 Node 事件循环字段；限流仍有已认证未知路径 / 错误 method 的用户层挂载语义和真实依赖验证差异，当前不得删除 Node limiter | cursor/window、权限、无实时扫描、分页稳定；`usage-window` 只读系统设置且不 touch session；运行日志和公开接口日志列表不读 payload、详情按 ID 单行读、管理员权限 / 404 / no-store / session 不 touch；公开接口日志生产切流前离线同步 Node integer/text 旧结构到 fresh Goose boolean/bigint/timestamptz 并确定单写 owner，本批只验证已有 created/source 索引；客户端 IP 列表只读 Node 预聚合窗口，固定 query/date/status/sort/progressive pagination、默认静态 `requestCount desc` 和 readiness；六项限流默认值、设置 60 秒缓存、health 唯一跳过、captcha 纳入、auth/public 用户层跳过、Redis / 内存实现、allowlist policy 查询 / 到期 / 失效 / 失败降级和 429 语义；系统指标不再返回 `eventLoopLagMs` / `db-service` 长期字段 |
| W7 | ingest / stats / ops worker | 移除 Node worker 和 IPC 胶水 | 使用记录写入、审计、日志索引、统计增量、系统指标采样、账号测试、账号健康任务、模型检测、代理检测、OAuth 保活 | Node worker、temporary maintenance worker、相关 IPC、模型检测 child worker、W1b Node internal health route、Go 临时 adapter、两个 `JUHE_AI_NODE_INTERNAL_*` 配置、worker event loop 采样和本地 IPC 队列指标 | 游标、重试、队列上限、任务租约、维护脚本、健康任务 / 模型检测真实入队与最终状态、Go runtime 采样、Asynq pending / retry / dead / oldest age 和 worker lag |
| W8 | Node storage / SQLite 删除收尾 | 消除 Node storage 层和双模式维护成本 | Go store 已覆盖的 PostgreSQL / Redis 正式路径、旧 SQLite 数据离线导入说明、PG/Redis/Asynq 观测替代 | Node storage adapter、DB service、SQLite schema、SQLite 配置、usage shard 文件模式、双模式分支、DB service 状态指标和 SQLite 文件体积指标 | 新库初始化、PG/Redis smoke、数据加密、无 SQLite 运行引用、无 `db-service` / `process_event_loop` Go 运行引用 |
| W9 | 网关准备层契约与 Go 读模型 | 为真实转发降风险，但不拆裂网关 owner | API Key 校验、设置、配额快照、运行态缓存、候选账号读取的 Go 读模型和压力验证；当前已补 client catalog reader 与三条 models GET opt-in 路由 | 不删除 Node 网关 preflight；W10 完成 Go 网关灰度接管后仍保留 Node gateway 回滚入口，直到 AI Chat 完成切流且内部客户端切到 Go gateway 并重验 | 缓存失效、权限、配额、候选过滤、运行态；models 真实 PG / upstream / listener 仍待验证 |
| W10 | 真实中转网关 | 最核心链路，先完成 Go 灰度接管和稳定性验证，为最终 Chat 切流提供 Go 网关 | `/*`、`/v1/*`、OpenAI / Anthropic / Gemini / hybrid、SSE、调度、审计和 usage | AI Chat 迁移完成前保留 Node gateway 回滚入口；Chat 切流、`chatgateway.Client` 切换和真实链路重验完成后再删除 Node gateway 主链路、协议 adapter、响应检查和重试逻辑 | mock AI、真实 e2e、SSE、backpressure、取消、性能报告 |
| W10-Chat | AI Chat 最终业务迁移 | `master` 仍持续改造，按用户决定放到其他业务模块和 Go 网关稳定之后实施 | 完整接管 `/__aisys__/api/my-chat/*` 的会话、消息、生成、SSE、上下文、资产和生命周期任务；实现边界以已批准设计为准 | 完成单 owner 切流、双向 cross-read 和回滚验证后删除 Node Chat；随后把 `chatgateway.Client` 从 Node `/v1/*` 切到 Go gateway，重新验证真实生成、恢复和 usage / audit，再删除 Node gateway 回滚入口 | Node / Go golden contract、真实 Chat / Responses、断线续传、资产、并发、at-most-once、切流、回滚和 Go gateway client 切换 |
| W11 | 发布收尾 | AI Chat 与网关均完成切流后正式抛弃 Node 后端 | Go 发布包、Docker、服务化、备份恢复、文档 | `backend/` 后端源码、Node 后端脚本、后端依赖和 engine 约束 | 全量构建、安装、部署、回滚和 smoke |

## 3. 模块状态表

| 模块 | 状态 | Go owner | Node 删除证据 | 最近验证 | 备注 |
| --- | --- | --- | --- | --- | --- |
| Go 工程与 PG/Redis 基线 | Go 实现中 | `backend-go/` | 不适用 | W0 常规验证、race、vet、lint、漏洞扫描、health smoke 和三平台构建已通过；testcontainers 当前本机 Docker 不可用时 `SKIP`，待 Docker 环境复跑 | W0 常规工程基线已建：最小 HTTP + config + slog + PostgreSQL / Redis / Asynq health、goose baseline、sqlc 查询、Redis namespace / TTL / pipeline / 原子计数 / fixed-window / penalty-window、Asynq inspector / pending smoke、testcontainers 用例和 `w0-smoke`；Asynq periodic / crash recover / drain / metrics / Redis 持久化专项门禁仍待后续补齐，独立真实外部 URL smoke 仍用于部署环境验证 |
| W1a 公开设置读接口 | Go 实现中 | `backend-go/internal/modules/publicsettings`、`backend-go/internal/httpapi`、`backend-go/internal/store/postgres`、`backend-go/internal/platform/redis` | 未删除 | Go 单元测试已补；`w1a-public-settings-smoke` 已提供，真实执行状态以 PLAN-0081 验证记录为准 | 当前已实现 Go 路由、store、migration、sqlc、契约测试、Redis Lua 原子 fixed-window limiter、真实 server Redis state 注入、`JUHE_AI_TRUST_PROXY` 客户端 IP 识别和 W1a 独立 smoke 命令；真实 PG/Redis smoke、切流和 Node 删除证据未完成，不改架构 owner |
| W1b `/__aipublic__` 外部维护接口 | Go 实现中 | `backend-go/internal/modules/publicapi`、`backend-go/internal/modules/publicapi/auth`、`backend-go/internal/modules/publicapi/ratelimit`、`backend-go/internal/modules/publicapilog`、`backend-go/internal/modules/publicgroups`、`backend-go/internal/modules/publicroutestrategies`、`backend-go/internal/modules/publicapikeys`、`backend-go/internal/modules/publicaccounts`、`backend-go/internal/jobs/publicapilog`、`backend-go/internal/jobs/worker`、`backend-go/internal/app/server.go`、`backend-go/internal/app/ingest_worker.go`、`backend-go/internal/config/config.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/httpapi/public_api_shell.go`、`backend-go/internal/httpapi/public_groups.go`、`backend-go/internal/httpapi/public_route_strategies.go`、`backend-go/internal/httpapi/public_api_keys.go`、`backend-go/internal/httpapi/public_accounts.go`、`backend-go/internal/store/port/publicapi.go`、`backend-go/internal/store/postgres/publicapi.go`、`backend-go/internal/store/postgres/publicgroups.go`、`backend-go/internal/store/postgres/publicroutestrategies.go`、`backend-go/internal/store/postgres/publicapikeys.go`、`backend-go/internal/store/postgres/publicaccounts.go`、`backend-go/internal/store/postgres/queries/w1b_public_groups.sql`、`backend-go/internal/store/postgres/queries/w1b_public_route_strategies.sql`、`backend-go/internal/store/postgres/queries/w1b_public_api_keys.sql`、`backend-go/internal/store/postgres/queries/w1b_public_accounts.sql`、`backend-go/db/migrations/000004_w1b_public_groups.sql`、`backend-go/db/migrations/000005_w1b_public_accounts.sql`、`backend-go/internal/platform/accounthealthcheckdispatch`、`backend-go/internal/platform/redis`、`backend-go/internal/maintenance/w1bpublicapismoke.go`；完整目标边界见 [W1b 外部维护公开接口迁移记录](W1b-外部维护公开接口迁移记录.md) | 未删除 | 多 agent 只读契约梳理已完成；Node `test:public-api-logs` 和 Go 原值快照 targeted / race / full / integration compile 纳入本轮回归；既有 W1b HTTP shell / 499 / ResponseWriter、router guard、maintenance smoke 边界和四类资源非容器专项继续有效；独立 `crossruntime` bridge 验证不替代公开日志真实依赖；真实 PG/Redis/Asynq integration、`w1b-public-api-smoke` 真实依赖执行与四类 shell E2E 待 Docker/testcontainers 或真实 PG/Redis/worker 环境复跑 | 当前已固定 16 个公开 CRUD catalog、Bearer/token hash、source/token scope 交集、auth 错误、`last_used_at` touch 判断、PostgreSQL auth/log adapter、Redis penalty-window helper、公开接口日志 snapshot/log builder、Asynq payload/enqueue/handler、`juhe-ai-worker ingest` 日志消费 runtime、HTTP shell / capture 契约、499 客户端提前断开捕获、`httpsnoop` 保留底层 writer 可选接口组合、`JUHE_AI_PUBLIC_API_ENABLED` 默认关闭的 opt-in router guard 和 `w1b-public-api-smoke` 本地 httptest 灰度 smoke；已补 public group、public route strategy、public API Key 与 public account 四条真实资源纵切面和事务提交后的临时 Node health dispatch adapter；公开日志 query/body/response 在 32KB 与结构预算内保留原值，不再递归脱敏，headers 只捕获 `contentType` / `contentLength`，`Authorization` / Cookie / 来源 token 不进入快照；业务响应白名单不变。Node PostgreSQL integer/text 旧表与 fresh Goose boolean/bigint/timestamptz 不兼容，离线同步和单写 owner 是切流门禁；Node internal bridge 保留到 W7 |
| 管理端辅助接口 | Go 实现中 | `backend-go/internal/modules/management*`、`backend-go/internal/httpapi/management_*`、`backend-go/internal/jobs/operationlog`、`backend-go/internal/app/operation_log_retention_cleanup_worker.go`、`backend-go/internal/store/postgres/queries/w2_*`、`backend-go/db/migrations/000006_*` 到 `000015_*` | 未删除 | 已通过 W2 相关 Go 快速测试、`go test ./...` 和 integration 包编译；本机 Docker 不可用时 W2 smoke 为 `SKIP`，需在 Docker/testcontainers 健康环境复跑后才能记为真实通过 | 当前 W2 覆盖管理端 proxy list / options、系统账户列表读 / options、授权候选、providers list / options / models、route strategies、groups、accounts options / tags、标签删除 / 独立 PATCH、operation log 读接口、operation log 保留清理 worker 和默认测试模型偏好写入；W3 system account create / mixed PATCH 已在 W3 opt-in 覆盖但仍未生产接管；代理创建 / 更新 / 删除 / 手动检测已在 W5 opt-in 覆盖但仍未生产接管；供应商定义写接口、供应商协议档案写接口、自动代理检测 worker、完整 auth / session 生产接管、授权来源 / grant / 授权写接口、主账户写入 `tags`、OAuth / 导入标签写路径、完整账号 summary 响应、operation log 保留清理生产 supervisor 接管和其他写接口 operation log 仍未迁移；默认生产 server 仍不注册管理端路由 |
| 登录与系统账户 | Go 实现中 | `backend-go/internal/modules/managementauth`、`backend-go/internal/modules/managementsystemaccounts`、`backend-go/internal/httpapi/management_*`、`backend-go/internal/store/postgres/managementauth.go`、`backend-go/internal/store/postgres/managementsystemaccounts.go`、`backend-go/internal/store/postgres/managementsystemaccountcreate.go`、`backend-go/internal/store/postgres/queries/w2_management_auth.sql`、`backend-go/internal/store/postgres/queries/w3_management_system_accounts.sql`、`backend-go/internal/store/postgres/queries/w3_management_system_account_update.sql`、`backend-go/internal/store/postgres/queries/w3_management_system_account_create.sql` | 未删除 | 已通过 W3 auth / system account Go 快速测试、integration 包编译、Node 对照回归、前端 auth / system-account 及负向会话管理 smoke 和 Markdown 检查；Docker/testcontainers smoke 当前不可用时为 `SKIP` | 当前覆盖 captcha、login / session 创建、current-user、profile update、change-password、当前 token logout、`POST /system-accounts` 创建和 `PATCH /system-accounts/{id}` 完整 mixed PATCH；登录会话列表 / 按 ID 撤销及其专用开关已撤销；前端已补负向会话管理调用 / UI 边界 smoke；安全日志、浏览器连接真实 Go 后端的前端 auth 和系统账户页 smoke、生产切流和 Node 删除仍未迁移 |
| 团队与统一授权 | Go 实现中 | `backend-go/internal/modules/managementsystemteams`、`backend-go/internal/modules/managementauthorizations`、`backend-go/internal/modules/gatewayquotasnapshot`、`backend-go/internal/httpapi/management_system_teams.go`、`backend-go/internal/httpapi/management_authorizations.go`、`backend-go/internal/store/postgres/managementsystemteams.go`、`backend-go/internal/store/postgres/managementauthorizations.go`、`backend-go/internal/store/postgres/gatewayquota.go`、`backend-go/internal/store/postgres/queries/w4_management_system_teams.sql`、`backend-go/db/migrations/000018_w4_resource_authorization_sources_and_grants.sql`、`backend-go/db/migrations/000019_w4_authorization_quota_and_stats_state.sql`、`backend-go/db/migrations/000020_w4_authorization_usage_windows.sql`、`backend-go/internal/modules/gatewaycache/system_account_invalidator.go`、`backend-go/internal/app/authorization_expiry_sweep_worker.go`、`backend-go/internal/app/authorization_usage_range_window_refresh_worker.go`、`backend-go/internal/app/gateway_quota_snapshot_worker.go`、`backend-go/cmd/juhe-ai-worker/main.go`；当前边界见 [W4 团队与统一授权迁移记录](W4-团队与统一授权迁移记录.md) | 未删除 | 已通过 W4 team list/detail/create/update/member、authorization list/detail/usage overview/usage detail/create/update/expire/return/revoke/expiry sweep service / worker、authorization usage range window refresh service / worker / store、gateway quota snapshot service / worker / store、router / app 快速测试；授权来源 / grant schema guard、授权到期扫描 SQL guard、额度窗口 / 统计脏标记 schema guard、授权 usage window schema guard、授权缓存失效和前端团队 / 统一授权页本地调用 smoke 已补；真实 PG/Redis/Asynq operation-log integration、授权用量窗口真实 PG smoke、浏览器连接真实 Go 后端的前端团队 / 统一授权页 smoke、生产切流和 Node 删除仍未执行 | 当前覆盖 `GET /system-teams`、`GET /system-teams/{id}`、`GET /my-teams`、`GET /my-teams/{id}`、`POST /system-teams`、`PATCH /system-teams/{id}`、`POST /system-teams/{id}/members`、`DELETE /system-teams/{id}/members/{memberId}`、`GET /authorizations`、`GET /authorizations/{id}`、`GET /authorizations/{id}/usage`、`GET /my-authorizations`、`GET /my-authorizations/{id}`、`GET /my-authorizations/{id}/usage`、`GET /authorizations/usage/team-details`、`GET /my-authorizations/usage/team-details`、`GET /authorizations/usage/user-details`、`GET /my-authorizations/usage/user-details`、`POST /authorizations`、`POST /my-authorizations`、`PATCH /authorizations/{id}`、`PATCH /my-authorizations/{id}`、`PATCH /authorizations/{id}/expire`、`PATCH /my-authorizations/{id}/expire`、`DELETE /authorizations/{id}/return`、`DELETE /my-authorizations/{id}/return`、`DELETE /authorizations/{id}` 和 `DELETE /my-authorizations/{id}` Go opt-in，并补 `juhe-ai-worker authorization-expiry-sweep`、`juhe-ai-worker authorization-usage-range-windows-refresh` 与 `juhe-ai-worker gateway-quota-snapshot-build`。团队读接口包含 admin / self 作用域、分页、团队名前缀搜索和 active 成员详情；团队创建 / 更新 / 成员维护覆盖 strict JSON、状态副作用、成员授权来源展开 / 回收、stats dirty / 授权缓存失效和 operation log best-effort；授权列表以 grant 为主行分页，返回轻量 DTO 和 `sourceSummary`，只读当前状态且不触发到期扫描；授权详情按 grant ID 读取当前授权关系、limits、source 明细和基础 usage 空对象，并对非资源归属人脱敏 source 内部字段；授权 usage overview 只读预聚合窗口并返回团队 / 用户维度当前页，授权用量明细按 grant ID 只读 user/team range window 并返回系统账户分页，不扫明细表；授权创建 / 普通更新 / 有效期更新 / 归还 / 回收覆盖 grant、manual / team source、授权实例、目标分组绑定、runtime 刷新、stats dirty、授权缓存失效和 operation log best-effort；批量到期扫描按 grant 到期索引 fixed window + `FOR UPDATE SKIP LOCKED` 刷新 runtime 并触发 `authorization_expired`；授权用量 range window worker 从授权日汇总表刷新 hot ranges；网关配额快照构建 worker 只读预聚合统计与小时窗口构建成本快照，不读取 `usage_records`；仍不覆盖授权用量窗口真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管、生产 gateway quota snapshot 消费或最终用户授权完整 CRUD |
| 供应商自定义模型 CRUD | Go 实现中 | `backend-go/internal/modules/managementprovidermodels`、`backend-go/internal/httpapi/management_provider_models.go`、`backend-go/internal/store/postgres/managementprovidermodels.go`、`backend-go/internal/store/postgres/queries/w2_management_provider_models.sql`、`backend-go/db/migrations/000021_w3_custom_provider_model_crud.sql` | 未删除 | 已通过 `go test ./internal/modules/managementprovidermodels ./internal/httpapi -count=1`；真实 `TestW3ManagementProviderModelCRUDPostgresSmoke` 当前本机 Docker 不可用时为 `SKIP`，需在健康环境复跑 | 覆盖 `POST/PATCH/DELETE /__aisys__/api/providers/{code}/models` Go opt-in，自定义模型权限、价格校验、int4 上界、错误优先级、绑定删除保护、默认测试模型清理和网关 runtime cache 失效；不代表供应商定义写接口、供应商协议档案写接口、前端真实 Go 后端 smoke、生产切流或 Node 删除 |
| 管理端全局品牌设置读写 | Go 实现中 | `backend-go/internal/httpapi/management_settings.go`、`backend-go/internal/modules/publicsettings`、`backend-go/internal/modules/managementsettings`、`backend-go/internal/store/postgres/store.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/app/server.go`、`backend-go/db/migrations/000023_w5_operation_log_settings.sql`；当前边界见 [W5 管理端全局品牌设置读取记录](W5-管理端全局品牌设置读取记录.md) | 未删除 | Go `httpapi` / `app` / `store` 定向、全量、race、vet、tidy diff 和 integration 包编译已通过；Node `test:settings-management-driver` 与 `test:performance-system-api-smoke` 已通过；真实 PostgreSQL / Redis / 管理 session 和前端 smoke 待补 | 覆盖 `GET/PATCH /settings/global` Go opt-in；GET 只读 session 不 touch，PATCH 写鉴权 touch、strict partial update、PostgreSQL 事务、`settings:global` shared cache version 与 `settings.update_global` operation log；不代表生产切流或 Node 删除 |
| 管理端系统运行设置 | Go 实现中 | `backend-go/internal/systemsettings`、`backend-go/internal/modules/managementsettings`、`backend-go/internal/httpapi/management_system_settings.go`、`backend-go/internal/store/postgres/managementsettings.go`、`backend-go/internal/store/postgres/queries/w5_management_settings.sql`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/app/server.go`、`backend-go/db/migrations/000024_w5_system_settings.sql`、`backend-go/db/migrations/000043_w5_remove_gpt_service_tier_multipliers.sql`；当前边界见 [W5 管理端系统运行设置迁移记录](W5-管理端系统运行设置迁移记录.md) | 未删除 | 现有非容器测试覆盖 53 key catalog、service、固定有界 store、HTTP / router / app 装配；真实 PostgreSQL / Redis / 管理 session / HTTP smoke 仍受本机无 Docker 阻塞，不计真实依赖通过 | `GET/PATCH /settings` 已进入 Go opt-in；GPT Priority / Flex 只使用模型目录精确档位价格；PATCH parser 位于 IP limiter 后且 auth / touch / user limiter 前，GET read auth 不 touch、PATCH touch；PostgreSQL 使用稳定 key、完整 snapshot 和 `FOR UPDATE`，`000024` 初始化 53 项并按 env / PG TimeZone / UTC seed 时区，`000043` 删除历史 GPT 通用倍率设置；在线禁改统计时区，提交后 best-effort 执行 `settings:system` / `settings_updated` 双失效和 `settings.update` operation log；不代表生产接管 |
| 管理端分组创建 | Go opt-in 已实现 | service / store / HTTP / router / app、Node 对照和 integration 代码已完成；当前边界见 [W5 管理端分组创建迁移记录](W5-管理端分组创建迁移记录.md) | 未删除 | 非 Docker Go 全量 / targeted race / vet / tidy、integration 编译和 Node 对照已通过；追加审计已固定 Express JSON transport、admin-before-guard、有效 owner 去重、真实审计角色、严格 `groupType` 和 provider 优先错误；`TestW5ManagementGroupCreatePostgresRedisAsynqSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实依赖通过 | 只迁移 `POST /groups` 与 `POST /my-groups`；固定 admin / self owner 作用域、strict body、`256 KiB`、个人 / 高并发完整策略、唯一约束、`201` 基础摘要、gateway runtime 失效和 `groups.create` operation log；其他分组接口按各自切片验收 |
| 管理端分组列表 | Go opt-in 已实现 | `backend-go/internal/modules/managementgroups` list、HTTP/router/app、migration `000026`、PostgreSQL/sqlc 批量读取和 integration smoke 已完成；当前边界见 [W5 管理端分组列表迁移记录](W5-管理端分组列表迁移记录.md) | 未删除 | 非 Docker service / HTTP / store / router / app 测试和 integration 包编译已完成；`TestW5ManagementGroupListPostgresSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实 PostgreSQL 通过 | 覆盖 `GET /groups` 与 `GET /my-groups` Go opt-in；固定 admin/self、1000 行 progressive pagination、owner/authorized union、稳定排序、轻量 DTO和预聚合 stats/usage/authorization source 读取；共存期 Node stats worker 单写，仍缺 Node writer 到 Go reader、统计 freshness、前端真实 Go 后端 smoke、生产切流和 Node 删除证据 |
| 管理端分组详情 | Go opt-in 已实现 | `backend-go/internal/modules/managementgroups` detail、HTTP/router/app、Redis v2 concurrency reader、PostgreSQL/sqlc 和 integration smoke 已完成；当前边界见 [W5 管理端分组详情迁移记录](W5-管理端分组详情迁移记录.md) | 未删除 | 非 Docker service / HTTP / store / Redis / router / app 测试和 integration 包编译已完成；`TestW5ManagementGroupDetailPostgresRedisSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实依赖通过 | 覆盖 `GET /groups/{id}` 与 `GET /my-groups/{id}` Go opt-in；固定 owner / authorized 可见性、owner 实时并发、authorized 账户隐藏和预聚合统计；仍缺前端真实 Go 后端 smoke、生产切流和 Node 删除证据 |
| 管理端分组更新 | Go opt-in 已实现 | `backend-go/internal/modules/managementgroups` update、HTTP/router/app、migration `000032`、PostgreSQL/sqlc 和 integration smoke 已完成；当前边界见 [W5 管理端分组更新迁移记录](W5-管理端分组更新迁移记录.md) | 未删除 | Go store / service / HTTP 定向测试、全量测试、vet、integration 编译和 Node 三项对照命令已通过；`TestW5ManagementGroupUpdatePostgresRedisSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实依赖通过 | 覆盖 `PATCH /groups/{id}` 与 `PATCH /my-groups/{id}` Go opt-in；固定 owner / authorized 字段边界、事务锁、provider 变化保护、全作用域 / 当前 grantee 路由绑定保护、授权本地设置、shared cache / runtime 失效和 `groups.update` operation log；仍缺前端真实 Go 后端 smoke、生产切流和 Node 删除证据 |
| 管理端分组删除 | Go opt-in 已实现 | `backend-go/internal/modules/managementgroups` delete、HTTP/router/app、PostgreSQL/sqlc 和 integration smoke 已完成；当前边界见 [W5 管理端分组删除迁移记录](W5-管理端分组删除迁移记录.md) | 未删除 | Go store / service / HTTP 定向测试、全量测试、vet、integration 编译和 Node 三项对照命令已通过；`TestW5ManagementGroupDeletePostgresRedisSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实依赖通过 | 覆盖 `DELETE /groups/{id}` 与 `DELETE /my-groups/{id}` Go opt-in；固定 owner-only、authorized 404、默认分组保护、全 owner / grantee 路由策略保护、账户与绑定级联、授权历史保留、事务内 `group_deleted` 统计脏标记、双 shared cache / runtime 失效和 `groups.delete` operation log；仍缺前端真实 Go 后端 smoke、生产切流和 Node 删除证据 |
| 管理端策略路由创建 | Go opt-in 已实现 | `backend-go/internal/modules/managementroutestrategies/service_create.go`、`backend-go/internal/httpapi/management_route_strategy_create.go`、公共 route strategy PostgreSQL transaction/store、router/app、前端 request-capture 和 integration smoke 已完成；当前边界见 [W5 管理端策略路由创建迁移记录](W5-管理端策略路由创建迁移记录.md) | 未删除 | 非 Docker service / HTTP / store / app、targeted race、frontend request-capture/typecheck 和 integration 包编译已通过；`TestW5ManagementRouteStrategyCreatePostgresSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实 PostgreSQL 通过 | 覆盖 `POST /route-strategies` 与 `POST /my-route-strategies` Go opt-in；固定 admin/self owner、strict JSON/presence、五模式当前 Node 规则、自有或有效授权分组、稳定事务锁、同 owner 名称唯一、201 详情、`route_strategy_created` 运行态失效和 `route_strategies.create` operation log；更新、删除、前端真实 Go 后端 smoke、生产切流和 Node 删除仍未完成 |
| 管理端策略路由更新 | Go opt-in 已实现 | `backend-go/internal/modules/managementroutestrategies/service_update.go`、`backend-go/internal/httpapi/management_route_strategy_update.go`、公共 route strategy PostgreSQL transaction/store、router/app、前端 request-capture 和 integration smoke 已完成；当前边界见 [W5 管理端策略路由更新迁移记录](W5-管理端策略路由更新迁移记录.md) | 未删除 | 提交 `dca25497e`、`446e949cf`、`badd99152`、`b632997a3`；service / HTTP / store / app、targeted race、frontend request-capture/typecheck 和 integration 包编译已通过；`TestW5ManagementRouteStrategyUpdatePostgresSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实 PostgreSQL 通过 | 覆盖 `PATCH /route-strategies/{id}` 与 `PATCH /my-route-strategies/{id}` Go opt-in；固定 admin global / owner narrowing、self actor、strict partial JSON、事务锁定 current snapshot、绑定整体替换、完整独立配置校验、错误优先级、回滚、`route_strategy_updated` 运行态失效和 `route_strategies.update` operation log；跨进程 speed-first 状态真实 Redis 验证、前端真实 Go 后端 smoke、删除接口、生产切流和 Node 删除仍未完成 |
| 管理端策略路由删除 | Go opt-in 已实现 | `backend-go/internal/modules/managementroutestrategies/service_delete.go`、`backend-go/internal/httpapi/management_route_strategy_delete.go`、公共 route strategy PostgreSQL transaction/store、router/app、前端 request-capture 和 integration smoke 已完成；当前边界见 [W5 管理端策略路由删除迁移记录](W5-管理端策略路由删除迁移记录.md) | 未删除 | 提交 `962b2db3c`、`eaf1d06f0`、`62eae1a56`、`e5ef0877c`；service / HTTP / app、目标 race、全量 Go、vet、tidy diff、frontend request-capture/typecheck 和 integration 包编译已通过；目标 smoke 因本机 Docker 未运行输出 `SKIP`，不计真实 PostgreSQL 通过 | 覆盖 `DELETE /route-strategies/{id}` 与 `DELETE /my-route-strategies/{id}` Go opt-in；固定 admin global / owner narrowing、self actor、无 body parser / mutation guard、默认和 API Key 引用 400 保护、事务 `FOR UPDATE`、binding 级联与回滚、204 空 body、`route_strategy_deleted` 失效和 `route_strategies.delete` operation log；真实 Redis 互操作、前端真实 Go 后端 smoke、生产切流和 Node 删除仍未完成 |
| 管理端 API Key 创建、更新与密钥生命周期 | Go opt-in 已实现 | `backend-go/internal/modules/managementapikeys` create/update/secret、`backend-go/internal/httpapi/management_api_key_create.go`、`backend-go/internal/httpapi/management_api_key_update.go`、`backend-go/internal/httpapi/management_api_key_secret.go`、PostgreSQL/sqlc、router/app 和 Redis/Asynq integration smoke 已完成；create/secret 边界见 [W5 管理端 API Key 密钥生命周期迁移记录](W5-管理端APIKey密钥生命周期迁移记录.md) | 未删除 | Go service / HTTP / store / router / app 定向与 race、全量、vet、tidy、integration 编译、Node API Key/route/schedule/scope/limit 对照和 frontend payload/typecheck 已通过；`TestW5ManagementAPIKeyUpdatePostgresRedisSmoke` 因本机 Docker 未运行输出 `SKIP`，不计真实 PostgreSQL / Redis / Asynq / HTTP 通过 | 覆盖 admin / self `POST /api-keys`、`PATCH /api-keys/{id}`、`GET .../secret` 与 `POST .../refresh-key` Go opt-in；固定 owner scope、strict create / partial update body、AES-GCM 密文、一次性完整 key、create runtime/quota 与 update/refresh validation/runtime/quota 失效、写 session touch、marker-only secret audit、七字段 update audit，以及提交后 validation Redis 失败仍记录真实 mutation 与最终 HTTP 500 状态；前端真实 Go 后端 smoke、生产切流和 Node 删除仍未完成 |
| 管理端 API Key 删除 | Go opt-in 已实现 | `backend-go/internal/modules/managementapikeys` delete、`backend-go/internal/httpapi/management_api_key_delete.go`、`backend-go/internal/store/postgres/managementapikeydelete.go`、`backend-go/db/migrations/000037_w5_api_key_record_cleanup_targets.sql`、共享 cleanup-target upsert、router/app 和 integration smoke 已完成；当前边界见 [W5 管理端 API Key 删除迁移记录](W5-管理端APIKey删除迁移记录.md) | 未删除 | C1 `7bdbc08c7` + `c51c08e7a`、C2a `010f8f104`、C2b `caf9edbe2`；deterministic unit / full Go validation 和 integration 包编译通过；目标 smoke 已实际发起但因 Docker provider 不可用输出 `SKIP`，不计真实 PostgreSQL / Redis / Asynq / HTTP / operation-log ingest 通过 | 覆盖 admin / self `DELETE /api-keys/{id}`；中间件顺序为鉴权前 IP write limiter、写鉴权并 touch session、鉴权后已认证用户 write limiter，管理路由再校验 admin；阈值读取当前系统设置，fresh schema 默认值为 `180/40/120`。同时固定 admin global / owner narrowing、self actor scope、无 body parser / mutation guard、204 空 body / no-store、默认 Key 409、同事务行锁 / 硬删除 / cleanup-target upsert、validation 必需失效、lookup/runtime/quota best-effort 和 `api_keys.delete` 最终状态日志。当前没有 validation 持久化重试；前端真实 Go smoke、生产单 owner 切流、回滚和 Node 删除仍未完成 |
| 核心后台 CRUD | Go 实现中 | 待定 | 未删除 | 子切片定向验证见独立行 | 账号和供应商定义 / 协议档案等整体仍待迁移；供应商自定义模型 CRUD、代理管理写接口、管理端全局品牌设置读写、系统运行设置读写、分组 CRUD、策略路由列表 / 详情 / 创建 / 更新 / 删除、API Key 列表、创建 / 更新 / 删除与密钥生命周期已单独登记，不代表 W5 整体接管 |
| 记录与统计读 API | Go 实现中 | `backend-go/internal/modules/managementstats`、`managementusagerecords`、`managementruntimelogs`、`managementpublicapilogs`、`managementauditlogs`、`runtimelogretention`、对应 HTTP / PostgreSQL reader、runtime log retention app 入口和 W6 migration；当前边界见 [W6 记录与统计读接口迁移记录](W6-记录与统计读接口迁移记录.md) | 未删除 | 管理侧 / 个人侧 `usage-window`、使用记录列表 / 详情、运行日志列表 / 详情 / facets / runtime、公开接口日志列表 / 详情和审计日志列表 / 详情的定向测试已纳入验证；runtime log retention 已覆盖默认 14 / 1..90、index switch、固定 cutoff、稳定小批删除、facet 同事务维护、完成 cursor 条件、25ms pause 和 context cancel；未迁移子路径保持 404；真实 usage / audit PostgreSQL 与 EXPLAIN、runtime importer-cleanup 并发尚未执行 | 当前为 Go opt-in 读和未挂载的 runtime log retention app 能力，不代表生产接管。Node 继续拥有 usage / runtime / audit writer、worker、文件、cursor/facet 写入和生产调度；usage 真实 writer-reader / 分区 EXPLAIN、审计 payload 原文 / error groups 列表与 events / hot search / runtime、运行日志 grep、真实并发和系统指标契约仍未迁移 |
| System API 限流对齐 | Go 实现中 | `backend-go/internal/httpapi/system_api_rate_limit.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/app/server.go`、`backend-go/internal/store/postgres/store.go`、`backend-go/internal/store/postgres/queries/w6_system_api_client_ip_allowlist.sql`、`backend-go/db/migrations/000025_w6_system_api_client_ip_allowlist.sql`；当前边界见 [W6 System API 限流对齐记录](W6-System-API限流对齐记录.md) | 未删除 | 已补 IP read / write、authenticated user read / write、Redis / 内存 limiter、router 顺序、captcha / health / auth / public 跳过范围、429、生产 Redis 双注入和 client IP allowlist 测试；非 Docker schema guard 固定 fresh DB 六项默认值，cache 单测固定 60 秒 settings TTL、30 秒 allowlist positive / negative TTL、policy expiry、shared cache version 失效和读取失败继续限流；真实 PG/Redis/反向代理 smoke 待执行 | 只对齐已迁移、已注册管理业务路由；Docker integration 不是默认值唯一验证。Go 已补 allowlist 消费和两层 bypass，客户端 IP 列表和四条 policy 写接口也已进入 Go opt-in，但已认证未知路径 / 错误 method 不经过用户 limiter；详情、统计生产 writer / worker、真实依赖、切流和删除证据仍未完成，因此不能标记完整 Node 等价或进入删除阶段 |
| 管理端客户端 IP 列表与策略写接口 | Go opt-in 已实现 | 列表：`backend-go/internal/modules/managementclientipstats`、`backend-go/internal/httpapi/management_client_ip_stats.go`、`backend-go/internal/store/postgres/managementclientipstats.go`、migration `000040`；写接口：`backend-go/internal/modules/managementclientippolicies`、`backend-go/internal/httpapi/management_client_ip_policies.go`、`backend-go/internal/store/postgres/managementclientippolicies.go`；另含 router/app、前端 request-capture / real-Go-management mock、allowlist 隔离 fixture smoke 和 integration smoke；当前边界见 [W6 管理端客户端 IP 统计与策略迁移记录](W6-管理端客户端IP策略迁移记录.md) | 未删除 | 四写路径既有定向验证继续有效；列表全量 Go、目标 race、vet、integration 编译 / vet / race 编译、前端 request-capture / mock real-Go-management / typecheck 已通过。`TestW6ManagementClientIPStatsListPostgresRedisSmoke` 与 Node production writer -> Go reader smoke 代码已加入；本机 Docker provider 不可用，普通模式 `SKIP`、`JUHE_AI_REQUIRE_INTEGRATION=1` 强制模式非零，未执行真实 PostgreSQL / Redis / EXPLAIN 或跨运行时断言 | 覆盖 admin-only `GET /ip-stats` 与四条 `POST /ip-stats/{ipHash}/{action}`。列表只读 Node 生产的 ready range window，固定 query/date/status/sort、1000 行分页窗口、progressive `pageUpperBound/rangeReady` 和默认静态 `ORDER BY request_count DESC, ip_hash ASC`；migration `000040` 只补列表 range output、两张 dirty 表与索引，不迁移 daily/account/detail writer。详情、Node writer / worker、真实 listener、生产切流、回滚和 Node 删除仍未完成 |
| 模型检测写入与任务 | 契约固定 | `backend-go/internal/jobs/modelcheck` 只提供 payload / lifecycle contract；当前边界见 [W7 模型检测写入与任务契约迁移记录](W7-模型检测写入与任务契约迁移记录.md) | 未删除 | `go test ./internal/jobs/modelcheck -count=1`、同包 race、vet、tidy diff 和 whitespace 检查通过 | 仅固定 queue/payload/config revision/probe version/request fingerprint、内部 checkpoint、幂等写阶段和 terminal CAS；Node 继续拥有 schema、writer、executor、stop、SSE、retention 和生产 owner，Go DDL/worker/HTTP/真实依赖均未完成 |
| 后台 worker | 未开始 | 待定 | 未删除 | 未执行 | 这里指 W7 全量 ingest / stats / ops worker 迁移；W1b public API log 单任务消费 runtime 已在 W1b 行记录，不代表后台 worker 已生产接管。删除 Node worker 前要有游标回归，并按 [Go 迁移指标与观测规划](Go迁移指标与观测规划.md) 补 Go runtime / Asynq / worker lag 观测 |
| Node storage / SQLite 删除收尾 | 未开始 | 待定 | 未删除 | 未执行 | PostgreSQL + Redis 为唯一目标，DB service 和 SQLite 删除前置；收尾时必须删除 `db-service`、SQLite 文件体积和 `process_event_loop_*` 长期指标契约 |
| 网关准备层契约与 Go 读模型 | Go 实现中 | `backend-go/internal/modules/gatewayclientcatalog`、`backend-go/internal/httpapi` | 未删除 | 定向 Go 测试通过；生产 owner 未变化 | `JUHE_AI_GATEWAY_MODELS_ENABLED=false` 默认关闭；三条 models 路由和 OAuth 契约基础不构成网关接管 |
| 真实中转网关 | 未开始 | 待定 | 未删除 | 未执行 | 先完成 Go 网关灰度接管和稳定性验证；AI Chat 切流前保留 Node gateway 回滚入口 |
| AI Chat 最终业务迁移 | 设计已确认，实施延后 | 待定 | 未删除 | 已批准 `docs/superpowers/specs/2026-07-18-AI问答Go无缝接管设计.md`；当前只维护契约漂移审计 | 排在其他业务模块和 Go 网关稳定之后；`master` Chat 持续改造期间不编写实施计划、不开始 Go Chat 代码；最终删除 Node gateway 前必须把 Chat 内部客户端切到 Go gateway 并重验 |
| 发布收尾 | 未开始 | 待定 | 未删除 | 未执行 | AI Chat 与网关均完成切流后执行 |

## 4. 单模块删除门禁

一个模块只有同时满足以下条件，才能把状态改为 `已接管`：

- Go route/job/script 已经是正式入口。
- 当前模块 Node route/service/store/script/test 命令已删除。
- 已迁移模块不能继续引用 SQLite adapter、SQLite 环境变量、usage shard 文件写入或 standalone / performance 模式分支。
- 前端或调用方不再引用旧路径、旧字段或旧错误结构。
- 已迁移的系统监控和运行态页面不再引用 Node 专属字段；Go 指标不能继续暴露或模拟 `eventLoopLagMs`、`process_event_loop_*`、`db-service`、SQLite 文件体积或 usage shard 文件路径作为长期契约。
- `rg` 检查旧模块名、旧 route、旧测试命令和旧脚本入口，只剩文档历史引用或无结果。
- Go 单元测试、API 契约测试、权限测试和必要的 smoke 已通过。
- 文档更新到 `docs/functions/`、`docs/develop/`、`docs/deploy/` 或本目录对应文件。

W1b 额外门禁：public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD 四条纵切面、`JUHE_AI_PUBLIC_API_ENABLED` opt-in guard 和 `w1b-public-api-smoke` 只能证明资源迁移路径、灰度入口和真实依赖链路可行，不能单独进入 `待删除 Node`。必须等 `/__aipublic__` 4 类资源 16 个 CRUD、鉴权、限频、公开日志、account shell E2E 在真实 Docker/testcontainers 环境通过、真实 PG/Redis/Asynq、反向代理单 owner 切流和生产回滚记录整体通过，且 Node external integrations / public API logs 运行入口和专用脚本完成删除验证后，才能把 W1b 标为 `已接管`。

W1b 健康任务 bridge 只是提交后 best-effort 临时适配：`w1b-public-api-smoke` 使用 no-op dispatcher；`TestAccountHealthCheckDispatchNodeE2E` 已在 Windows 本机通过普通目标测试和 `-race`，但也只验证真实 Go client -> Node 生产 router，不验证真实 public HTTP 写入、worker 入队或最终状态。健康任务正式接管和临时 adapter / Node route / env 删除属于 W7，当前不得写成正式接管或可靠交付。

W2 额外门禁：单个 options / catalog 接口、Go `requireAuth` 会话鉴权基线、标签删除切片、独立标签 PATCH 切片、operation log 保留清理 worker 或 `JUHE_AI_MANAGEMENT_API_ENABLED` opt-in guard 完成，不能单独进入 `待删除 Node`。必须等 W2 约定范围全部完成、Go 管理端 `requireAuth` / `requireAdmin` / `my-*` 作用域等价、前端 smoke 通过、生产入口单 owner 切流，并删除 Node 对应 route/service/test 后，才能把 W2 标为 `已接管`。当前 `proxies`、`proxies/options`、`system-accounts`、`system-accounts/options`、`authorization-options/grantee-accounts`、`my-authorization-options/grantee-accounts`、`authorization-options/grantee-teams`、`my-authorization-options/grantee-teams`、`authorization-options/grantee-groups`、`my-authorization-options/grantee-groups`、`providers`、`providers/options`、`providers/models/options`、`providers/{code}/models`、`route-strategies/options`、`my-route-strategies/options`、`groups/options`、`my-groups/options`、`groups/account-options`、`my-groups/account-options`、`accounts/options`、`my-accounts/options`、`accounts/tags`、`my-accounts/tags`、`DELETE accounts/tags/{tagId}`、`DELETE my-accounts/tags/{tagId}`、`PATCH accounts/{id}/tags`、`PATCH my-accounts/{id}/tags`、`operation-logs`、`operation-logs/{id}`、`my-operation-logs` 和 `my-operation-logs/{id}` Go handler 只有在显式开启管理端 opt-in 并注入会话鉴权 middleware 时才允许注册；默认生产 server 不注册，避免后台接口被误暴露或被误判为已接管。代理列表读只覆盖只读摘要，不覆盖代理写接口生产接管；代理创建 / 更新 / 删除 / 手动检测属于 W5 opt-in 补充切片但仍不能作为 Node 删除证据；供应商列表读只覆盖 admin-only 供应商定义读取，不覆盖供应商定义写接口或协议档案写接口；系统账户列表读和 options 只覆盖当前 W2 只读范围，不覆盖系统账户写接口或登录；登录会话管理已撤销，不作为 W2/W3 接管缺口；系统账户 create / mixed PATCH 属于 W3 opt-in 范围，不能反向扩大 W2 只读接管范围；authorization grantee accounts / grantee teams / grantee groups 只覆盖授权对象用户候选和授权目标分组候选，不覆盖授权写接口；分组 options / account-options 已覆盖 Node 授权组只读 union，账户 options 已覆盖 Node 授权账户只读 union，但仍未迁授权来源 / grant / 授权写接口；标签只读、未绑定删除和独立 PATCH 不覆盖主账户写入 `tags`、OAuth / 导入等标签写路径、完整账号 summary 响应或其他写接口 operation log，不能作为完整账号标签生产接管证据。operation log 保留清理 worker 只覆盖 `operation_logs` 主表按保留期批量删除，并依赖 FK cascade 清子表；不覆盖完整 data-retention、生产 supervisor 接管或 Node 删除。

本轮补充的 `PUT /__aisys__/api/providers/{code}/default-test-model` 只覆盖 `provider_default_test_models` 当前系统账户偏好写入，并沿用同一个 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 默认关闭门禁；它不代表供应商定义写接口、供应商协议档案写接口或账号测试链路已经迁移，也不能单独作为 Node 删除证据。`POST/PATCH/DELETE /__aisys__/api/providers/{code}/models` 自定义模型 CRUD 已作为后续管理写接口补充切片单独登记，不能反向扩大 W2 接管范围。

W5 代理写接口补充门禁：`POST /proxies`、`PATCH /proxies/{id}`、`DELETE /proxies/{id}` 和 `POST /proxies/{id}/test` 已进入 Go opt-in，覆盖 admin-only、strict JSON、Node 兼容代理密码密文、检测缓存重置、固定 4 条账户占用窗口、操作日志密码脱敏、gateway runtime cache 失效、手动检测 `ProxyTestReport`、共享诊断闸门、25 秒总 deadline、15 秒单次探测、出口信息保留语义和 `proxies.test` 操作日志。启用完整管理 API 时必须配置不少于 32 字符的稳定 `JUHE_AI_SECRET`。本切片不包含前端连接真实 Go 后端 smoke、真实部署外网代理诊断证据、自动代理检测 worker、生产单 owner 切流或 Node `/proxies` 删除，因此不能进入 `待删除 Node`。

W5 管理端全局品牌设置读写补充门禁：`GET/PATCH /settings/global` 当前只在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 时 Go opt-in 注册，复用 `publicsettings.Service`、`managementsettings.Service` 和 PostgreSQL `global_settings` reader / writer，只允许 `admin` / `super_admin`，普通 `user` 返回 403。GET 使用只读 session 鉴权且不 touch，进入 IP read `600/120` 和认证用户 read `300`；PATCH 使用写鉴权 touch，进入 IP write `180/40` 和认证用户 write `120`，执行 strict partial update、稳定事务锁、`settings:global` shared cache version 与 `settings.update_global` operation log best-effort。已有 Go 定向、全量、race、vet、tidy diff、integration 包编译和 Node `test:settings-management-driver` / `test:performance-system-api-smoke` 通过记录；真实 PostgreSQL / Redis / 管理 session 和前端真实 Go 后端 smoke 待补。生产单 owner 切流、回滚和 Node settings 删除尚未完成，因此不能进入 `待删除 Node`。

W5 管理端系统运行设置补充门禁：`GET/PATCH /settings` 已在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 下进入 Go opt-in。当前契约固定 53 key，GPT Priority / Flex 只使用模型目录精确档位价格；PATCH body 上限为 `256 KiB`，超限返回 `413`，JSON parser 位于 system API IP limiter 后且写 auth / touch / authenticated user limiter 前。GET 使用 read auth 不 touch，PATCH 使用 write auth touch。PostgreSQL GET 只按固定白名单有界读取；PATCH 在单事务内按 `ORDER BY key ASC FOR UPDATE` 锁定完整 snapshot，校验后按稳定 key 更新。migration `000024` 初始化 53 项设置，其中 `usageStatsTimezone` seed 优先 `JUHE_AI_USAGE_STATS_TIMEZONE`，未设时回退 PostgreSQL `TimeZone` / `UTC`；migration `000043` 删除历史 GPT service tier 通用倍率设置行，Down 使用 `ON CONFLICT DO NOTHING` 避免覆盖已有值。Node / Go 共存期统计时区必须与 Node `Intl` 部署时区一致，PostgreSQL 在线 PATCH 禁止修改时区。提交后 best-effort 执行 `settings:system` shared cache version、reason=`settings_updated` 的 gateway runtime 失效，以及 `settings.update` / `action=update_settings` operation log。尚未迁移的网关 `serviceTier` 选择与计价执行不在本切片重复实现。真实依赖 smoke 与 loopback listener 写 smoke 已有记录；前端真实 Go 后端 smoke、生产单 owner 切流、回滚和 Node settings 删除也未完成，因此不能进入 `待删除 Node`。

W5 管理端分组更新补充门禁：`PATCH /groups/{id}` 与 `PATCH /my-groups/{id}` 已在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 下进入 Go opt-in。owner 可更新完整分组字段，authorized grantee 只能写本地 `enabled`、`groupType` 和 `schedulingPolicy`；owner 禁用检查所有授权作用域的路由策略，authorized 禁用只检查当前 grantee，查询固定 `LIMIT 101` 并拒绝超过 100 条绑定。provider 变化只允许没有当前有效账户绑定的分组；migration `000032` 支持授权分组绑定 grantee 自有路由策略。提交后 best-effort 执行 `lookup:group` shared cache version、对应 runtime reason 和 `groups.update` operation log。真实依赖 smoke 仍受本机无 Docker 阻塞，不得记录为真实 PostgreSQL / Redis / HTTP 通过；前端真实 Go 后端 smoke、生产单 owner 切流、回滚和 Node 分组更新删除也未完成，因此不能进入 `待删除 Node`。

W5 管理端分组删除补充门禁：`DELETE /groups/{id}` 与 `DELETE /my-groups/{id}` 已在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 下进入 Go opt-in。仅 owner 可删除非默认分组，authorized grantee 返回 `404`；删除前按 owner 和所有有效 grantee 作用域检查 active 路由策略，固定 `LIMIT 101`，超过 100 条或任一策略失去最后可用分组时拒绝。账户绑定、路由绑定和授权本地设置按 fresh schema 外键级联，授权 source / grant 历史保留；同一事务写 `group_account_stats_dirty` reason=`group_deleted`，提交后 best-effort 执行 `lookup:group`、`lookup:group-account-ids`、gateway runtime 和 `groups.delete` operation log。真实依赖 smoke 仍受本机无 Docker 阻塞，不得记录为真实 PostgreSQL / Redis / HTTP 通过；前端真实 Go 后端 smoke、生产单 owner 切流、回滚和 Node 分组删除也未完成，因此不能进入 `待删除 Node`。

W5 分组 CRUD 真实管理 smoke 补充门禁：根命令为 `pnpm smoke:plan0081-real-go-management`，本地 mock regression 为 `pnpm test:plan0081-real-go-management-smoke`。真实命令必须显式提供 `JUHE_REAL_GO_MANAGEMENT_BASE_URL` 和 `JUHE_REAL_GO_MANAGEMENT_COOKIE`，可选提供 system account、group provider code 和 timeout。默认只能读取既有列表 / 详情；只有 `JUHE_REAL_GO_MANAGEMENT_ALLOW_GROUP_MUTATIONS=1` 时才允许创建带 `randomUUID` 标识的临时 `personal` 分组，随后完成列表、详情、名称 / 描述 / 五项调度字段 PATCH 到 `high_concurrency`、更新后回读、DELETE、删除后 404 和 finally 安全清理。`JUHE_REAL_GO_MANAGEMENT_GROUP_ID` 只允许只读选择，禁止对既有分组写入。本地 mock regression 与前端 typecheck 已通过；当前本机没有执行真实 Go HTTP / PostgreSQL / Redis / session 环境，该门禁仍未通过，不能据此进入 `待删除 Node` 或声明生产接管。

W5 API Key 创建、更新与密钥生命周期补充门禁：管理 / 个人 `POST .../api-keys`、`PATCH .../api-keys/{id}`、`GET .../secret` 与 `POST .../refresh-key` 只有在 `JUHE_AI_MANAGEMENT_API_ENABLED=true`、稳定 `JUHE_AI_SECRET`、PostgreSQL、Redis cache/state、Redis queue 和 operation log ingest worker 均可用时才允许灰度。完整密钥只能出现在创建或受权 secret / refresh 成功响应中，列表、普通 DTO、update 响应、operation log、测试输出和 smoke 摘要不得持久化或打印。create 只发布 runtime/quota 失效；update / refresh 在数据库提交后必须刷新 validation shared cache version，该失效失败会返回 500，但不能把已提交更新或新密钥误判为数据库回滚，且 update 必须先写入真实 `api_keys.update` operation log。真实 PG/Redis/Asynq smoke、前端真实 Go 后端创建 / 更新 / 复制 / 刷新 / 旧 key 失效验证、生产单 owner 切流和 Node 删除证据整体完成前，不得进入 `待删除 Node`。

W5 API Key 删除补充门禁：管理 / 个人 `DELETE .../api-keys/{id}` 只在完整管理 API opt-in 下注册，先进入鉴权前 IP write limiter，再执行写鉴权 touch 和已认证用户 write limiter；阈值按当前系统设置读取，fresh schema 默认值为 IP write `180/minute` + `40/10 seconds` burst、已认证用户 write `120/minute`。接口不解析请求 body，也不使用 mutation guard。admin 支持全局精确 ID 或单 owner 收窄，self 强制当前 actor；默认 Key 返回 409，成功返回带 `no-store` / `no-cache` 的 204 空响应。PostgreSQL 在同一事务内行锁、默认保护、硬删除并 upsert cleanup target；冲突只刷新 owner 和 `updated_at`，保留 `created_at`、`attempt_count`、`last_attempt_at`、`last_blocked_reason`、`last_error_message`，表中没有 `retry_after` 列。请求路径不扫描或同步清理 usage / audit / log 明细。提交后 validation 失效必需，失败返回通用 500 但仍记录已提交的 `api_keys.delete status_code=500`；lookup/runtime/quota 和 operation-log enqueue 为 best-effort。当前没有 validation 持久化重试，必须保留安全风险并补生产证据。目标 integration 因 Docker provider 不可用 `SKIP`，前端真实 Go smoke、生产单 owner 切流、回滚和 Node DELETE 路由删除完成前，不得进入 `待删除 Node`。

W3 额外门禁：`GET /auth/captcha`、`POST /auth/login`、`GET /auth/me`、`PATCH /auth/me`、`POST /auth/change-password`、`POST /auth/logout`、`POST /system-accounts` 和 `PATCH /system-accounts/{id}` 完整 mixed PATCH 当前都只是 Go opt-in 灰度能力。登录会话列表、登录设备列表、按 ID 撤销及其专用开关已撤销，不能作为接管前置、待办或恢复项。W3 进入 `待删除 Node` 前，必须整体完成前端本地 smoke、浏览器连接真实 Go 后端的 auth / 系统账户页 smoke、真实 PG+Redis 验证、安全日志、operation log 敏感字段脱敏、生产单 owner 切流和 Node `/auth` / `/system-accounts` 删除证据；Node `/auth` 和 `/system-accounts` 不能因为小切片完成而删除。

W4 额外门禁：团队列表 / 详情读接口、`POST /system-teams` 团队创建、`PATCH /system-teams/{id}` 团队更新、`POST /system-teams/{id}/members` 成员新增、`DELETE /system-teams/{id}/members/{memberId}` 成员移除、`GET /authorizations` / `GET /my-authorizations` 授权列表、`GET /authorizations/{id}` / `GET /my-authorizations/{id}` 授权详情、授权 team/user usage overview、授权 `:id/usage` 明细、授权创建 / 普通更新 / 有效期更新 / 归还 / 回收、批量到期扫描 worker、usage range window 刷新 worker、gateway quota snapshot 构建 / 发布 worker 和授权来源 / grant / usage window schema 基线当前只是 Go opt-in / 存储前置能力，不能单独进入 `待删除 Node`。W4 必须等授权用量窗口真实 PG smoke / 生产部署接管、批量到期扫描真实 PG/Redis smoke / 生产部署接管、网关配额快照真实 PG/Redis smoke / 生产部署接管、最终用户授权重算、operation log 真实消费、真实 PG/Redis/Asynq smoke、浏览器连接真实 Go 后端的前端团队与统一授权页面 smoke、生产单 owner 切流和 Node `/system-teams` / `/authorizations` 删除证据整体完成后，才能把团队与统一授权标为 `已接管`。

System API 限流额外门禁：当前 Go 已读取 `systemApiRateLimitIpReadPerMinute`、`systemApiRateLimitIpReadBurstPer10Seconds`、`systemApiRateLimitIpWritePerMinute`、`systemApiRateLimitIpWriteBurstPer10Seconds`、`systemApiRateLimitUserReadPerMinute`、`systemApiRateLimitUserWritePerMinute`，默认值依次为 `600/120/180/40/300/120`，并以 60 秒设置缓存驱动鉴权前 IP read / write 和已注册业务路由鉴权后用户 read / write limiter。IP 层仅跳过 health，captcha、login、`settings/public` 都进入 IP 层；`/auth/*` 与 `settings/public` 跳过用户层；生产 server 同时注入两层 Redis limiter，超限返回 `429`、`Retry-After` 和中文文案。Go 已按 IPv4 hash 读取 active / 未过期 allowlist policy，两层 limiter 共用 bypass；positive / negative 判定缓存 30 秒且受 policy 到期约束，配置 cache Redis 时通过 `gateway:client-ip-policy-by-ip` shared cache version 立即失效，读取失败时继续限流。该实现只覆盖已迁移、已注册管理业务路由，不能写成 100% Node 等价。Node 全局 `requireAuth` 后的已认证未知路径 / 错误 method 会进入用户 limiter，而 Go 精确路由未命中时不会进入该层；客户端 IP 列表和四条 policy 写接口已进入 Go opt-in，但详情与统计生产 writer / worker 仍在 Node。完成所有目标 system API 注册、IP 详情、真实 PG/Redis/可信反向代理与跨运行时失效 smoke、生产单 owner 切流和 Node 静态删除证据前，不得删除 Node system API limiter。

W6 表监控只读补充门禁：`GET /table-monitor/overview`、`/history`、`/database-history` 已作为 Go opt-in PostgreSQL reader 迁移，Node 继续是 `juhe_stats.database_storage_snapshots` 与 `table_storage_snapshots` 的 schema、采样、保留清理和数据质量单 owner。Go 每次读取先检查两张表及实际使用列；缺表、旧列或目录查询失败不会伪造空数据，也不执行 DDL/DML。当前权威 Goose catalog 已正式连续到 `000073`，本切片未新增 migration；后续 schema 变更必须从真实 catalog 派生下一连续版本。必须等真实 schema contract preflight、Node writer -> Go reader PostgreSQL smoke、索引计划、listener / 反向代理单 owner 与回滚证据齐备后，才可以删除 Node 同名 reader 或考虑转移 writer。

## 5. 网关专项门禁

真实中转网关迁移前，必须先完成：

- 管理接口、存储 adapter、worker 和统计/审计写入已经由 Go 接管或有明确 Go owner。
- API Key、路由策略、分组、账号候选、配额和运行态缓存的 Go 读取路径已通过压力验证。
- OpenAI Chat / Responses、Anthropic Messages、Gemini native、hybrid bridge 的 mock AI 回归已准备好。
- SSE 增量解析、下游 backpressure、客户端取消、上游超时和已写出后的失败语义有专项测试。
- 使用记录、审计、账号副作用和统计入队都不阻塞已可返回的响应。

W10 必须按这些阻断门禁推进：

| 门禁 | 必须证明 |
| --- | --- |
| 契约冻结 | `/*`、`/v1/*`、`/__aisys__/*` 和 `/__aipublic__/*` 路径归属、认证前 body 读取边界、header 白名单和错误结构已固定 |
| Mock 全矩阵 | OpenAI Chat / Responses、Anthropic Messages、Gemini native、Hybrid、协议允许 / 禁止方向、JSON / SSE、usage、工具、图像、坏 SSE 都有 mock 回归 |
| 响应检查和错误兜底 | Chat / Responses JSON / SSE 污染、畸形 SSE、Codex compact、普通客户端不可见 Codex 专用错误码、非 2xx 和 SSE 内失败都通过 |
| Side effects / 队列 | usage、audit、operation / public logs、账号副作用、统计入队在 Asynq 队列积压、worker 停止、重试和 dead / archived 情况下不阻塞已可返回响应 |
| 生命周期和压测 | 慢客户端、半帧 SSE、大事件、客户端断开、上游 cancel、goroutine / FD / 内存、pprof 和 backpressure 都有验证 |
| 真实 smoke | OpenAI-compatible、Anthropic、Gemini、Codex / Claude Code / Gemini CLI 代表性真实请求按凭据可用性执行或记录未覆盖原因 |
| 灰度切换 | 按 API Key、系统账户或独立端口 canary；定义失败阈值、快速停止和回退步骤 |
| Node 删除证据 | W10 先证明 Go gateway 可灰度接管且稳定；AI Chat 完成单 owner 切流和回滚验证后，将 `chatgateway.Client` 切到 Go gateway，并重新验证真实 Chat / Responses、恢复链路与 usage / audit；全部通过后再统一删除 Node gateway 主链路、preflight、协议 adapter、响应检查和重试旧实现 |
