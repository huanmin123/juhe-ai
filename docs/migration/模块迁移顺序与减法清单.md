# 模块迁移顺序与减法清单

## 1. 使用方式

本文件是后续迁移执行的主清单。每迁移一个模块，都必须更新状态、Go owner、Node 删除证据和验证记录。

状态建议：

- `未开始`：尚未进入迁移。
- `契约固定`：已生成当前 Node 行为契约、fixture 或测试清单。
- `Go 实现中`：Go 代码开发中。
- `待删除 Node`：Go 已通过测试，但 Node 旧代码还未删。
- `已接管`：Go 是唯一 owner，Node 旧入口已删除。
- `阻塞`：存在设计、数据、测试或部署阻塞。

## 2. 迁移波次

| 波次 | 模块 | 迁移原因 | Go 目标实现范围 | Node 删除范围 | 必须验证 |
| --- | --- | --- | --- | --- | --- |
| W0 | Go 工程与 PG/Redis 基线 | 后续模块迁移的共同基础，避免业务模块迁移时临时直连 SQL 或调用 Node | 配置、日志、统一响应、错误、测试工具、健康检查、PostgreSQL schema / seed / store adapter、Redis cache / state namespace、Asynq queue | 无业务删除 | `go test ./...`、Redis DB 隔离、PG/Redis/Asynq smoke、Windows / Linux / macOS 构建基线 |
| W1a | 公开设置读接口 | 无登录、只读、字段少，适合作为第一个业务接口 PoC | `GET /__aisys__/api/settings/public`，返回 `{ data: { appName, appIcon } }`，Go 读取 `juhe_business.global_settings` 和 `system_settings`，使用 Redis state 做原子 IP read rate limit | 正式接管前不删除；接管后删除 Node 对应 public settings route/service 入口 | 匿名 200、`Cache-Control: no-store`、只暴露 `appName/appIcon`、Redis 原子 minute / burst 限流、客户端 IP 识别遵循 `JUHE_AI_TRUST_PROXY` 且默认不信任公网请求头、`w1a-public-settings-smoke` 真实 PG/Redis smoke、错误不泄露内部依赖 |
| W1b | `/__aipublic__` 外部维护接口 | 公开来源接口契约复杂，包含 Bearer 鉴权、scope、限频、日志和 CRUD | API Key、路由策略、分组、AI 账户等 16 个 `/__aipublic__/*` 外部维护接口；当前四类资源纵切面代码均已补，生产 router 已有 `JUHE_AI_PUBLIC_API_ENABLED=false` 默认关闭的 opt-in guard，并新增 `w1b-public-api-smoke` 独立灰度验证入口，不包含管理端来源 / token 管理 API | 整个 W1b 前缀完成 Go 接管后删除 Node external integrations route/service/log 写入入口；单个公开资源纵切面、opt-in guard 或 maintenance smoke 完成不能删 Node | Bearer 鉴权、scope、限频、公开接口日志、public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD、默认关闭 / 显式开启 router guard、`w1b-public-api-smoke`、分页、业务响应敏感字段不回显、日志快照边界、API Key 完整 key 只在新增响应返回且日志脱敏、AI 账户凭据加密落库且不回显、错误语义 |
| W2 | 管理端只读辅助接口 | 风险低，能快速建立前端联调方式 | providers/options、provider models、proxy options、account/group options、group account-options、account tags、API Key 表单依赖的 route-strategy options；当前 `proxies/options`、`providers/options`、`providers/models/options`、`providers/{code}/models`、`route-strategies/options`、`my-route-strategies/options`、`groups/options`、`my-groups/options`、`groups/account-options`、`my-groups/account-options`、`accounts/options`、`my-accounts/options`、`accounts/tags` 和 `my-accounts/tags` 已补 Go `requireAuth` 会话鉴权基线、admin / self 作用域和 `JUHE_AI_MANAGEMENT_API_ENABLED=false` 默认关闭的 opt-in guard；其中分组 options / account-options 已覆盖 owner + 授权组只读 union，账户 options / tags 仍是 owner-only，账户 options 已覆盖 owner-only 名称包含搜索，授权账户 union 和标签写接口仍未迁移 | Node 对应只读 route/service；单个 options、catalog、会话鉴权基线或 opt-in guard 完成不能删除 Node | 登录态、普通用户访问边界、管理员作用域、`my-*` 当前用户作用域、空数据、中文错误、敏感字段白名单 |
| W3 | 登录与系统账户 | 管理接口基础能力 | 登录、会话、系统账户、验证码/限频边界 | Node auth/system-accounts route/service/session store | 密码、session、权限、失败限制、安全日志 |
| W4 | 团队与统一授权 | 权限和调度前置事实 | 系统团队、授权 grant、授权归还、授权到期 | Node teams/authorization route/service/store | 权限矩阵、授权范围、过期和分页 |
| W5 | 供应商、账号、分组、API Key、策略路由、代理、设置 | 后台核心 CRUD | 管理 CRUD、字段校验、敏感字段展示、路由策略绑定 | Node 对应模块 route/service/store 和测试命令 | CRUD、权限、敏感字段脱敏、稳定排序、存储 adapter |
| W6 | 使用记录、审计、日志和统计读 API | 为 worker 和网关迁移铺路，并替换系统监控读契约 | 列表、详情、窗口查询、聚合缓存读取；系统指标读 API 改为 Go runtime / storage / queue / stats freshness 分组 | Node 记录类读 route/service、`process_event_loop_*` 读模型和前端 Node 事件循环字段 | cursor/window、权限、无实时扫描、分页稳定；系统指标不再返回 `eventLoopLagMs` / `db-service` 长期字段 |
| W7 | ingest / stats / ops worker | 移除 Node worker 和 IPC 胶水 | 使用记录写入、审计、日志索引、统计增量、系统指标采样、账号测试、代理检测、OAuth 保活 | Node worker、temporary maintenance worker、相关 IPC、worker event loop 采样和本地 IPC 队列指标 | 游标、重试、队列上限、任务租约、维护脚本、Go runtime 采样、Asynq pending / retry / dead / oldest age 和 worker lag |
| W8 | Node storage / SQLite 删除收尾 | 消除 Node storage 层和双模式维护成本 | Go store 已覆盖的 PostgreSQL / Redis 正式路径、旧 SQLite 数据离线导入说明、PG/Redis/Asynq 观测替代 | Node storage adapter、DB service、SQLite schema、SQLite 配置、usage shard 文件模式、双模式分支、DB service 状态指标和 SQLite 文件体积指标 | 新库初始化、PG/Redis smoke、数据加密、无 SQLite 运行引用、无 `db-service` / `process_event_loop` Go 运行引用 |
| W9 | 网关准备层契约与 Go 读模型 | 为真实转发降风险，但不拆裂网关 owner | API Key 校验、设置、配额快照、运行态缓存、候选账号读取的 Go 读模型和压力验证 | 不删除 Node 网关 preflight；等待 W10 整条 `/*` / `/v1/*` Go 网关可灰度接管后统一删除 | 缓存失效、权限、配额、候选过滤、运行态 |
| W10 | 真实中转网关 | 最核心链路，最后迁移 | `/*`、`/v1/*`、OpenAI / Anthropic / Gemini / hybrid、SSE、调度、审计和 usage | Node gateway 主链路、协议 adapter、响应检查、重试逻辑 | mock AI、真实 e2e、SSE、backpressure、取消、性能报告 |
| W11 | 发布收尾 | 正式抛弃 Node 后端 | Go 发布包、Docker、服务化、备份恢复、文档 | `backend/` 后端源码、Node 后端脚本、后端依赖和 engine 约束 | 全量构建、安装、部署、回滚和 smoke |

## 3. 模块状态表

| 模块 | 状态 | Go owner | Node 删除证据 | 最近验证 | 备注 |
| --- | --- | --- | --- | --- | --- |
| Go 工程与 PG/Redis 基线 | Go 实现中 | `backend-go/` | 不适用 | W0 常规验证、race、vet、lint、漏洞扫描、health smoke 和三平台构建已通过；testcontainers 当前本机 Docker 不可用时 `SKIP`，待 Docker 环境复跑 | W0 常规工程基线已建：最小 HTTP + config + slog + PostgreSQL / Redis / Asynq health、goose baseline、sqlc 查询、Redis namespace / TTL / pipeline / 原子计数 / fixed-window / penalty-window、Asynq inspector / pending smoke、testcontainers 用例和 `w0-smoke`；Asynq periodic / crash recover / drain / metrics / Redis 持久化专项门禁仍待后续补齐，独立真实外部 URL smoke 仍用于部署环境验证 |
| W1a 公开设置读接口 | Go 实现中 | `backend-go/internal/modules/publicsettings`、`backend-go/internal/httpapi`、`backend-go/internal/store/postgres`、`backend-go/internal/platform/redis` | 未删除 | Go 单元测试已补；`w1a-public-settings-smoke` 已提供，真实执行状态以 PLAN-0081 验证记录为准 | 当前已实现 Go 路由、store、migration、sqlc、契约测试、Redis Lua 原子 fixed-window limiter、真实 server Redis state 注入、`JUHE_AI_TRUST_PROXY` 客户端 IP 识别和 W1a 独立 smoke 命令；真实 PG/Redis smoke、切流和 Node 删除证据未完成，不改架构 owner |
| W1b `/__aipublic__` 外部维护接口 | Go 实现中 | `backend-go/internal/modules/publicapi`、`backend-go/internal/modules/publicapi/auth`、`backend-go/internal/modules/publicapi/ratelimit`、`backend-go/internal/modules/publicapilog`、`backend-go/internal/modules/publicgroups`、`backend-go/internal/modules/publicroutestrategies`、`backend-go/internal/modules/publicapikeys`、`backend-go/internal/modules/publicaccounts`、`backend-go/internal/jobs/publicapilog`、`backend-go/internal/jobs/worker`、`backend-go/internal/app/server.go`、`backend-go/internal/app/ingest_worker.go`、`backend-go/internal/config/config.go`、`backend-go/internal/httpapi/router.go`、`backend-go/internal/httpapi/public_api_shell.go`、`backend-go/internal/httpapi/public_groups.go`、`backend-go/internal/httpapi/public_route_strategies.go`、`backend-go/internal/httpapi/public_api_keys.go`、`backend-go/internal/httpapi/public_accounts.go`、`backend-go/internal/store/port/publicapi.go`、`backend-go/internal/store/postgres/publicapi.go`、`backend-go/internal/store/postgres/publicgroups.go`、`backend-go/internal/store/postgres/publicroutestrategies.go`、`backend-go/internal/store/postgres/publicapikeys.go`、`backend-go/internal/store/postgres/publicaccounts.go`、`backend-go/internal/store/postgres/queries/w1b_public_groups.sql`、`backend-go/internal/store/postgres/queries/w1b_public_route_strategies.sql`、`backend-go/internal/store/postgres/queries/w1b_public_api_keys.sql`、`backend-go/internal/store/postgres/queries/w1b_public_accounts.sql`、`backend-go/db/migrations/000004_w1b_public_groups.sql`、`backend-go/db/migrations/000005_w1b_public_accounts.sql`、`backend-go/internal/platform/redis`、`backend-go/internal/maintenance/w1bpublicapismoke.go`；完整目标边界见 [W1b 外部维护公开接口迁移记录](W1b-外部维护公开接口迁移记录.md) | 未删除 | 多 agent 只读契约梳理已完成；Node 非 PG 对照命令已通过；Go 常规验证矩阵、W1b HTTP shell / 499 / ResponseWriter 透传契约测试、默认关闭 / 显式开启 router guard 测试、`w1b-public-api-smoke` 缺配置 / 输出边界测试，以及四类资源 handler / service 非容器专项已通过；integration 包已可编译；account shell E2E 测试代码已补；真实 PG/Redis/Asynq integration、`w1b-public-api-smoke` 真实依赖执行与四类 shell E2E 待 Docker/testcontainers 或真实 PG/Redis/worker 环境复跑 | 当前已固定 16 个公开 CRUD catalog、Bearer/token hash、source/token scope 交集、auth 错误、`last_used_at` touch 判断、PostgreSQL auth/log adapter、Redis penalty-window helper、公开接口日志 snapshot/log builder、Asynq payload/enqueue/handler、`juhe-ai-worker ingest` 日志消费 runtime、HTTP shell / capture 契约、499 客户端提前断开捕获、`httpsnoop` 保留底层 writer 可选接口组合、`JUHE_AI_PUBLIC_API_ENABLED` 默认关闭的 opt-in router guard 和 `w1b-public-api-smoke` 本地 httptest 灰度 smoke；已补 public group、public route strategy、public API Key 与 public account 四条真实资源纵切面，覆盖 handler/service/store/query/schema、target / group 并发幂等、owner 复合 FK、账号绑定保护、同策略并发删除保护、路由策略绑定整体替换、mode/config/status 约束、默认策略保护、API Key 使用保护、API Key hash-only secret、quota / schedule JSON 约束、默认 Key 删除 / 换路由保护、AI 账户 provider profile、凭据 AES-GCM 加密、凭据指纹 hash、模型替换、pending_test 直启拦截、软删除和绑定清理；account shell E2E 测试代码覆盖 add/list/update/delete/limited、source/token last_used、请求日志脱敏和响应白名单；默认不注册生产 `/__aipublic__`，开启 guard 或 smoke 通过也只表示可灰度验证，不等于正式接管；公开日志禁止记录 `Authorization` / 来源 token / Cookie / API Key / Bearer / secret-like 字符串和 URL userinfo |
| 管理端只读辅助接口 | Go 实现中 | `backend-go/internal/modules/managementauth`、`backend-go/internal/modules/managementproxies`、`backend-go/internal/modules/managementproviders`、`backend-go/internal/modules/managementprovidermodels`、`backend-go/internal/modules/managementroutestrategies`、`backend-go/internal/modules/managementgroups`、`backend-go/internal/modules/managementaccounts`、`backend-go/internal/httpapi/management_auth.go`、`backend-go/internal/httpapi/management_proxies.go`、`backend-go/internal/httpapi/management_providers.go`、`backend-go/internal/httpapi/management_provider_models.go`、`backend-go/internal/httpapi/management_route_strategies.go`、`backend-go/internal/httpapi/management_groups.go`、`backend-go/internal/httpapi/management_accounts.go`、`backend-go/internal/store/port/management.go`、`backend-go/internal/store/postgres/managementauth.go`、`backend-go/internal/store/postgres/managementproxies.go`、`backend-go/internal/store/postgres/managementproviders.go`、`backend-go/internal/store/postgres/managementprovidermodels.go`、`backend-go/internal/store/postgres/managementroutestrategies.go`、`backend-go/internal/store/postgres/managementgroups.go`、`backend-go/internal/store/postgres/managementaccounts.go`、`backend-go/internal/store/postgres/queries/w2_management_auth.sql`、`backend-go/internal/store/postgres/queries/w2_management_proxy_options.sql`、`backend-go/internal/store/postgres/queries/w2_management_provider_options.sql`、`backend-go/internal/store/postgres/queries/w2_management_provider_models.sql`、`backend-go/internal/store/postgres/queries/w2_management_route_strategy_options.sql`、`backend-go/internal/store/postgres/queries/w2_management_group_options.sql`、`backend-go/internal/store/postgres/queries/w2_management_group_account_options.sql`、`backend-go/internal/store/postgres/queries/w2_management_account_options.sql`、`backend-go/internal/store/postgres/queries/w2_management_account_tags.sql`、`backend-go/db/migrations/000006_w2_management_proxy_options.sql`、`backend-go/db/migrations/000007_w2_management_session_auth.sql`、`backend-go/db/migrations/000008_w2_management_provider_options.sql`、`backend-go/db/migrations/000009_w2_management_provider_model_catalog.sql`、`backend-go/db/migrations/000010_w2_management_account_tags.sql`、`backend-go/db/migrations/000011_w2_management_group_authorizations.sql`、`backend-go/db/migrations/000012_w2_management_account_name_search.sql` | 未删除 | 已通过 `sqlc generate`、`go test ./internal/modules/managementauth ./internal/modules/managementproxies ./internal/modules/managementproviders ./internal/modules/managementprovidermodels ./internal/modules/managementroutestrategies ./internal/modules/managementgroups ./internal/modules/managementaccounts ./internal/httpapi ./internal/store/postgres ./internal/config ./internal/app`、`go test ./...` 和 integration 包编译；W2 proxy options / provider options / provider model catalog / route strategy options / group options / group account options / account options / account tags / management auth integration smoke 代码已补，待 Docker/testcontainers 环境真实执行 | 当前 `GET /__aisys__/api/proxies/options`、`GET /__aisys__/api/providers/options`、`GET /__aisys__/api/providers/models/options`、`GET /__aisys__/api/providers/{code}/models`、`GET /__aisys__/api/route-strategies/options`、`GET /__aisys__/api/my-route-strategies/options`、`GET /__aisys__/api/groups/options`、`GET /__aisys__/api/my-groups/options`、`GET /__aisys__/api/groups/account-options`、`GET /__aisys__/api/my-groups/account-options`、`GET /__aisys__/api/accounts/options`、`GET /__aisys__/api/my-accounts/options`、`GET /__aisys__/api/accounts/tags` 和 `GET /__aisys__/api/my-accounts/tags` 已补 Go PG schema / 既有表复用、store、service、handler、`requireAuth` 会话鉴权、admin-only 管理侧下拉、self-only 用户侧下拉、OpenAI-compatible / hybrid 模型目录聚合、owner / authorized permissions、owner `accountIds`、授权组 `accountIds=[]`、owner `tagIds` 过滤、owner-only 名称包含搜索、标签绑定计数和 `JUHE_AI_MANAGEMENT_API_ENABLED` router opt-in guard；分组 options / account-options 已覆盖授权组只读 union，授权来源 / grant / 授权写接口、授权账户 union 和标签写接口仍未迁移，默认生产 server 仍不注册管理端只读路由，显式开启只代表灰度验证，必须等 W2 全范围、前端 smoke、生产单 owner 切流和 Node 删除证据完成后才能接管 |
| 登录与系统账户 | 未开始 | 待定 | 未删除 | 未执行 | 管理接口基础 |
| 团队与统一授权 | 未开始 | 待定 | 未删除 | 未执行 | 涉及权限矩阵 |
| 核心后台 CRUD | 未开始 | 待定 | 未删除 | 未执行 | 账号、分组、API Key、策略路由等 |
| 记录与统计读 API | 未开始 | 待定 | 未删除 | 未执行 | 必须保留窗口/游标边界 |
| 后台 worker | 未开始 | 待定 | 未删除 | 未执行 | 这里指 W7 全量 ingest / stats / ops worker 迁移；W1b public API log 单任务消费 runtime 已在 W1b 行记录，不代表后台 worker 已生产接管。删除 Node worker 前要有游标回归，并按 [Go 迁移指标与观测规划](Go迁移指标与观测规划.md) 补 Go runtime / Asynq / worker lag 观测 |
| Node storage / SQLite 删除收尾 | 未开始 | 待定 | 未删除 | 未执行 | PostgreSQL + Redis 为唯一目标，DB service 和 SQLite 删除前置；收尾时必须删除 `db-service`、SQLite 文件体积和 `process_event_loop_*` 长期指标契约 |
| 网关准备层契约与 Go 读模型 | 未开始 | 待定 | 未删除 | 未执行 | 不删除 Node preflight，不拆裂网关 owner |
| 真实中转网关 | 未开始 | 待定 | 未删除 | 未执行 | 最后迁移 |
| 发布收尾 | 未开始 | 待定 | 未删除 | 未执行 | 迁移完成后执行 |

## 4. 单模块删除门禁

一个模块只有同时满足以下条件，才能把状态改为 `已接管`：

- Go route/job/script 已经是正式入口。
- 当前模块 Node route/service/store/script/test 命令已删除。
- 已迁移模块不能继续引用 SQLite adapter、SQLite 环境变量、usage shard 文件写入或 standalone / performance 模式分支。
- 前端或调用方不再引用旧路径、旧字段或旧错误结构。
- 已迁移的系统监控和运行态页面不再引用 Node 专属字段；Go 指标不能继续暴露或模拟 `eventLoopLagMs`、`process_event_loop_*`、`db-service`、SQLite 文件体积或 usage shard 文件路径作为长期契约。
- `rg` 检查旧模块名、旧 route、旧测试命令和旧脚本入口，只剩文档历史引用或无结果。
- Go 单元测试、API 契约测试、权限测试和必要的 smoke 已通过。
- 文档更新到 `docs/functions/`、`docs/develop/`、`docs/deploy/` 或本目录对应文件。

W1b 额外门禁：public group CRUD、public route strategy CRUD、public API Key CRUD、public account CRUD 四条纵切面、`JUHE_AI_PUBLIC_API_ENABLED` opt-in guard 和 `w1b-public-api-smoke` 只能证明资源迁移路径、灰度入口和真实依赖链路可行，不能单独进入 `待删除 Node`。必须等 `/__aipublic__` 4 类资源 16 个 CRUD、鉴权、限频、公开日志、account shell E2E 在真实 Docker/testcontainers 环境通过、真实 PG/Redis/Asynq、反向代理单 owner 切流和生产回滚记录整体通过，且 Node external integrations / public API logs 运行入口和专用脚本完成删除验证后，才能把 W1b 标为 `已接管`。

W2 额外门禁：单个 options / catalog 接口、Go `requireAuth` 会话鉴权基线或 `JUHE_AI_MANAGEMENT_API_ENABLED` opt-in guard 完成，不能单独进入 `待删除 Node`。必须等 W2 约定范围全部完成、Go 管理端 `requireAuth` / `requireAdmin` / `my-*` 作用域等价、前端 smoke 通过、生产入口单 owner 切流，并删除 Node 对应 route/service/test 后，才能把 W2 标为 `已接管`。当前 `proxies/options`、`providers/options`、`providers/models/options`、`providers/{code}/models`、`route-strategies/options`、`my-route-strategies/options`、`groups/options`、`my-groups/options`、`groups/account-options`、`my-groups/account-options`、`accounts/options`、`my-accounts/options`、`accounts/tags` 和 `my-accounts/tags` Go handler 只有在显式开启管理端 opt-in 并注入会话鉴权 middleware 时才允许注册；默认生产 server 不注册，避免后台接口被误暴露或被误判为已接管。分组 options / account-options 已覆盖 Node 授权组只读 union，但仍未迁授权来源 / grant / 授权写接口；账户 options 目前未覆盖 Node 的授权账户 union，标签只读接口不覆盖标签写接口，不能作为完整 `/my-accounts/options` 或账号标签生产接管证据。

## 5. 网关专项门禁

真实中转网关迁移前，必须先完成：

- 管理接口、存储 adapter、worker 和统计/审计写入已经由 Go 接管或有明确 Go owner。
- API Key、路由策略、分组、账号候选、配额和运行态缓存的 Go 读取路径已通过压力验证。
- OpenAI Chat / Responses、Anthropic Messages、Gemini native、hybrid bridge 的 mock AI 回归已准备好。
- SSE 增量解析、下游 backpressure、客户端取消、上游超时和已写出后的失败语义有专项测试。
- 使用记录、审计、账号副作用和统计入队都不阻塞已可返回的响应。

W10 必须按这些阻断门禁推进：

| 门禁 | 必须证明 |
| --- | --- |
| 契约冻结 | `/*`、`/v1/*`、`/__aisys__/*` 和 `/__aipublic__/*` 路径归属、认证前 body 读取边界、header 白名单和错误结构已固定 |
| Mock 全矩阵 | OpenAI Chat / Responses、Anthropic Messages、Gemini native、Hybrid、协议允许 / 禁止方向、JSON / SSE、usage、工具、图像、坏 SSE 都有 mock 回归 |
| 响应检查和错误兜底 | Chat / Responses JSON / SSE 污染、畸形 SSE、Codex compact、普通客户端不可见 Codex 专用错误码、非 2xx 和 SSE 内失败都通过 |
| Side effects / 队列 | usage、audit、operation / public logs、账号副作用、统计入队在 Asynq 队列积压、worker 停止、重试和 dead / archived 情况下不阻塞已可返回响应 |
| 生命周期和压测 | 慢客户端、半帧 SSE、大事件、客户端断开、上游 cancel、goroutine / FD / 内存、pprof 和 backpressure 都有验证 |
| 真实 smoke | OpenAI-compatible、Anthropic、Gemini、Codex / Claude Code / Gemini CLI 代表性真实请求按凭据可用性执行或记录未覆盖原因 |
| 灰度切换 | 按 API Key、系统账户或独立端口 canary；定义失败阈值、快速停止和回退步骤 |
| Node 删除证据 | W10 成功后统一删除 Node gateway 主链路、preflight、协议 adapter、响应检查和重试旧实现 |
