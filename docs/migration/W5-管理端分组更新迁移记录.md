# W5 管理端分组更新迁移记录

> 本文记录 `PATCH /__aisys__/api/groups/{id}` 与 `PATCH /__aisys__/api/my-groups/{id}` 的 Go opt-in 迁移实现与验收边界。当前 store、service、HTTP、router、app 接线和真实依赖 smoke 代码已完成；本机 Docker 不可用，因此 PostgreSQL / Redis smoke 尚未真实执行通过。

## 基本信息

- 模块：管理端 / 个人端分组更新
- 状态：Go opt-in 代码已完成，待真实依赖、前端联调与生产切流
- 迁移波次：W5
- 当前 Node owner：`backend/src/modules/groups/groups.routes.ts`、`backend/src/storage/group-write.repository.ts`
- 目标 Go owner：`backend-go/internal/modules/managementgroups/`、`backend-go/internal/httpapi/management_group_update.go`、`backend-go/internal/store/postgres/managementgroups.go`
- 关联计划：`../plans/计划-0081-Node转Go渐进减法迁移.md`
- 相邻记录：[W5 管理端分组详情迁移记录](W5-管理端分组详情迁移记录.md)

## 当前切片

本切片只包含：

- `PATCH /__aisys__/api/groups/{id}`
- `PATCH /__aisys__/api/my-groups/{id}`

两条路径只在 `JUHE_AI_MANAGEMENT_API_ENABLED=true` 且完整注入管理鉴权、PostgreSQL store、shared cache invalidator 和 gateway runtime invalidator 时注册。请求使用 strict partial JSON，body 上限为 `256 KiB`；空对象、未知字段和类型错误均返回 `400`。

本切片不包含分组删除、前端生产入口切换、生产单 owner 切流或 Node 路由删除。

## 权限与字段边界

- `/groups/{id}` 只允许 `admin` / `super_admin`，按 owner 视图更新。
- `/my-groups/{id}` 强制当前 session 账户作用域，不接受 query 参数扩大作用域；真实 actor role 保留在操作日志中。
- 路径 `{id}` 保持原值查询，不做 trim。
- owner 可更新名称、描述、供应商、启用状态、分组类型和调度策略。
- authorized grantee 只能更新本地 `enabled`、`groupType` 和 `schedulingPolicy`；修改 owner 字段返回明确的 `400`。
- 默认分组保持只读。
- provider 先做存在性校验，再查目标分组，保持 Node 的错误优先级。
- 名称唯一冲突按最终合并后的名称返回。

调度策略按当前完整策略与 partial input 合并，继续执行模式、权重、轮询、故障回退和混合智能路由约束，不允许 partial PATCH 绕过完整校验。

## 存储与并发边界

- owner 更新在单个 PostgreSQL 事务中锁定目标分组和相关绑定。
- provider 变化时，只允许分组没有当前有效绑定账户；已删除、禁用或授权失效的账户不构成阻断。
- owner 禁用分组时检查所有 owner / grantee 作用域中的有效路由策略绑定。
- authorized grantee 禁用本地分组时只检查当前 grantee 的有效路由策略绑定。
- 路由策略保护查询固定 `LIMIT 101`；超过 100 条时拒绝更新，避免请求路径无界读取。
- authorized 本地设置使用 `group_authorization_settings` upsert，不修改 owner 分组记录。
- migration `000032_w5_authorized_group_route_bindings.sql` 允许授权账户把可见分组绑定到自己的路由策略，同时用复合外键保持 owner 关系完整。
- 成功写入后重新读取完整详情，保证 owner / authorized 响应与详情接口一致。

## 写后副作用

- owner 更新递增 `lookup:group` shared cache version，并发布 runtime reason=`group_updated`。
- authorized 本地设置更新递增同一 shared cache version，并发布 runtime reason=`group_authorization_settings_updated`。
- 成功请求 best-effort 写入 operation key=`groups.update`；失败写入不产生日志。
- cache、runtime 和 operation log 副作用均在数据库提交后执行，不把外部依赖失败伪装成数据库回滚。

## 实现提交

| 提交 | 内容 |
| --- | --- |
| `c9325e9a4` | 允许授权分组参与 grantee 自有路由策略绑定 |
| `e81412104` | 新增 W5 分组更新 port、SQL、PostgreSQL adapter 与 store 测试 |
| `0742c70ba` | 新增 owner / authorized 更新 service、详情回读和副作用测试 |
| `7e4c77a16` | 新增 admin / self PATCH HTTP、router、server 接线与回归 |
| `be535fc49` | 新增真实 PostgreSQL / Redis / session integration smoke |

## 验证记录

| 验证 | 结果 |
| --- | --- |
| `go test ./internal/store/postgres -run 'TestManagementGroup' -count=1` | 通过 |
| `go test ./internal/store/... -count=1` | 通过 |
| `go test ./internal/modules/managementgroups -count=1` | 通过 |
| `go test ./internal/httpapi ./internal/app ./internal/modules/managementgroups -count=1` | 通过 |
| `go test ./... -count=1` | 通过 |
| `go vet ./...` | 通过 |
| `go test -tags=integration ./internal/testkit/integration -run '^$' -count=1` | 通过 |
| `TestW5ManagementGroupUpdatePostgresRedisSmoke` | 本机 Docker 不可用，按 testcontainers 健康门禁 `SKIP`，不计真实依赖通过 |
| Node `test:group-management-driver`、`test:group-scheduling-policy`、`test:authorization-return` | 通过 |

integration smoke 已编码覆盖 owner 更新持久化、authorized 字段边界、跨 grantee 与当前 grantee 路由保护、shared cache / runtime 失效、成功操作日志和失败无日志。

## 剩余门禁

满足以下条件前不得声明生产接管或删除 Node 分组更新：

- 在 Docker/testcontainers 健康环境真实通过 `TestW5ManagementGroupUpdatePostgresRedisSmoke`。
- 前端分组编辑流程连接真实 Go HTTP 服务完成 owner、authorized、校验错误、禁用保护和权限 smoke。
- 生产反向代理按路径形成单 owner，并完成停止 Go 后回退 Node 的演练。
- 对应 Node route/service/store/test 已删除并提供 `rg` 静态证据。
- PLAN-0081、功能、测试验收和部署文档完成最终切流同步。

## 当前结论

Go 已具备 W5 分组更新的独立 opt-in 实现和非容器回归证据，但真实 PostgreSQL / Redis 执行、前端真实后端联调、生产单 owner 切流和 Node 删除均未完成。本切片不能作为生产接管证据。
