# 迁移恢复清单

> 本文是 Node 到 Go 渐进减法迁移的 repo 可读恢复索引。它保存当前可消费的事实、顺序和限制，不能代替运行时队列、锁、生产 owner 或部署控制面。

## 清单元数据

```yaml
schema_version: codex-recovery-manifest/v1
manifest_revision: 76
persisted_at: 2026-08-05T16:06:03+08:00
goal_id: current-codex-goal
run_id: current-codex-run
root_work_id: node-to-go-progressive-subtraction-migration
goal_state: active
state_provider: Codex goal/plan API 的持久任务状态
recovery_state: manual-single-consumer-required
```

- `goal_id` 和 `run_id` 是状态提供者的运行时标识；仓库文档不复制或伪造其内部值。
- 当前 Codex goal 为 `active`。状态提供者是 Codex goal/plan API；其原子 claim、版本比较和幂等消费能力在本清单中均不可验证。
- 因此恢复只能由单线程的人工维护者或主控消费：先重新核验实际 worktree、直接 WorkUnit、diff 和验证输出，再按本清单顺序推进。本文不表示任务已排队、可自动唤醒、可自动重试或可并发 claim。
- 状态提供者或 claim 能力不可用时，保持 goal `active`，记录 `recovery_required` / `runtime_wait` 并结束该轮恢复；不得把运行时缺口写成领域 blocked，也不得重复创建 writer。

## 目标与当前 Git 基线

迁移目标是按可验证模块将 Node.js + TypeScript 后端逐步迁至 Go：在每个已接管模块保持唯一运行时 owner，完成真实依赖、owner、drain、回滚与删除门禁后才进入 Node 减法阶段。当前不授权切流、删除 Node、注册 Go listener/worker 或修改生产 owner。

| 对象 | 已核验事实 | 恢复要求 |
| --- | --- | --- |
| 共享主线 | `origin/master=e489de493a7063be3a5a1d42acab3e7ac9c66c5a`（`fix(stats): cast client IP dirty generation in postgres`） | 每轮恢复都重新 `fetch` 并读取共享 `HEAD`、`origin/master`、状态和 merge-base；不在共享目录写入。 |
| 迁移 worktree | `F:\sub2api-lite-worktrees\go-migration-w7-persistence-integration-20260802`，分支 `codex/go-migration-w7-persistence-integration-20260802` | 本轮 W10 代码基线已包含 `263fa818f`（speed-first capacity limit Node parity）、pre-body candidate-capacity snapshot 与单次 pre-body runtime/admission 组合契约；文档提交会推进分支，因此恢复时必须以实际 `HEAD` 为准。后续只在此 worktree 实施，不得将共享工作区文件复制进来。 |
| W7 expiry core | `3942bd76f` 已提交独立 `accounts` expiry port、service 与 PostgreSQL transaction；本轮将错误复用的 `000094` 纠正为连续的 `000101_w7_account_expiry_sweep.sql`，并把 schema test 移出 migration catalog 目录 | 独立 `avsp_terra_xhigh` 最终验收相对 `79822a4c0` 通过，P0/P1/P2 均为无。授权远端隔离 Docker 严格 smoke 已以 fresh Goose `000001..000101`、PostgreSQL 18 和 Redis 8 通过 scope、20+1、并发 `SKIP LOCKED`、rollback 与 post-commit invalidation；不注册 worker/listener/CLI，不改变 Node owner。 |
| W10 gateway | Node gateway 仍是唯一 production owner；`origin/master=e489de493`。五项 ingress 语义与 golden 已冻结为 fail-closed，Go 已实现 staged `/v1/responses` ingress（先 credential/preflight，仅 allow 时读取 body）、可选 pre-body rejection/lease 到 body-stage continuation 的无二次 route 组合、route-only plan、pre-body candidate-capacity snapshot、Node runtime group 选择与 speed-first body lease、Node 一致的 credential-source capacity 归一化、catalog/mapping finalization、高并发 queue/claim、按 Node 顺序的 initial busy/fallback -> current-group client-IP acquire -> queue/refresh/attempt 两阶段 handoff、请求级 high-concurrency fallback callback、client-IP runtime-state driver 的无降级选择、attempt-loop 到 request lifecycle 的 opaque generation / sink / terminal handoff、future handler 可消费的 response-terminal/body-lease binding contract、三协议 native request 的共享 bounded raw-body/metadata/preparation reader、使用 staged downstream sink 的三协议 pre-commit client-error envelope renderer、execution batch 的 authenticated API key ID / group runtime window handoff、仅消费第一条 frozen runtime window 的 current-group execution owner、未注册的 cross-group fallback owner seam、将显式 response result/下游 commit/writer action 接到 lifecycle terminal 的 `gatewayresponseterminal` adapter，以及将 caller-owned response completion、client cancellation、usage final record、audit snapshot、retained lease release 和 side-effect enqueue 错误收口为 detached `gatewayrequestfinalization` handoff。attempt loop 现在还能输出显式的 `excluded` / `recoverable` 账户事实；未知分类保持不完整。 | Go 仍没有 HTTP listener、真实 response finish/close/cancel 的 lifecycle owner、完整 response/audit/usage owner 或 Node rule runtime resolver。新 owner seam 只在 caller 提供 terminal observer 时，以 fresh target route-plan + singleton execution 续跑同一 request lifecycle，并在 terminal 才释放 retained high target lease；它不接 response writer 或生产 route。`gatewayresponseterminal` 只验证并收敛 caller 已观察的事实，`gatewayrequestfinalization` 只生成 detached usage/audit handoff 与显式 queue 边界；两者都不创建 `http.Handler`、不监听真实 finish/close、不写 response，也不拥有 audit/usage storage。runtime window 只适用于当前 group；fallback 不能执行旧 batch，必须完整重建目标 group。新的 error renderer 只消费已经分类的公开失败，不拥有 status/type/code 分类、审计或用量。`FallbackAccounts.Complete=false` 时 outer owner 必须拒绝跨组接管，不能把所有尝试账号静默加入排除集。授权远端 Redis 的 account-slot、client-IP、high-concurrency queue、session-affinity 及 client-IP Redis driver integration gate 已在随机测试键空间通过。W10 catalog 的 fresh PostgreSQL smoke 已在可销毁 Testcontainers 容器通过；它不替代完整 PostgreSQL、三协议 upstream、listener 或 owner/canary 验收。 |

## 已同步 Node 输入

`ed90607f0` 的 Node 主线新增账户 Images API 健康检查模式，后续 `e489de493` 修正 PostgreSQL client-IP dirty generation 的 `bigint` CAS 参数；两者都不改变已冻结的 W10 ingress/lane 语义。本轮另外以 `e489de493` 的 Node fallback candidate 顺序为准，新增 Go fresh target dispatch policy preparation 契约。每一实施批仍须先审计最新 `origin/master`，Node gateway 行为是唯一业务真源。

这项输入不放宽 Go 的通用 transport/commit retry fence，也不改变已冻结的 W10 ingress final-lane 契约。它不注册 Go listener、worker 或 owner，不改变 `deploy/owner-manifest.json`，不触发 canary、drain、切流或删除。

同一基线的 `pnpm.cmd --dir backend run test:protocol-cross-matrix-mock-ai` 当前在 OpenAI Chat 同协议 mapping 的 usage 断言失败：期望 `pricingModel=cross-openai-chat-upstream`，实际为 `undefined`。失败发生在 Node regression 的 usage record 断言，当前 Go shared inbound reader 未被调用；它必须由 Node 业务 owner 修复或判定后重跑，不能作为 Go 三协议语义通过证据，也不使当前迁移 goal 进入 blocked。

## 直接 WorkUnit 状态

| work_id | parent_work_id | depth / agent_type | execution_state | result_state / settlement_state | failure_class | 恢复动作 |
| --- | --- | --- | --- | --- | --- | --- |
| `w7-account-expiry-final-acceptance` | `node-to-go-progressive-subtraction-migration` | `1 / avsp_terra_xhigh` | `completed` | `verified_complete / accepted` | `none` | 已消费独立最终验收：相对 `79822a4c0` 的 P0/P1/P2 均为无。下一步是隔离 PostgreSQL/Redis smoke，不把本结果外推为 worker、listener 或 owner 验收。 |
| `w7-account-expiry-real-dependency-smoke` | `node-to-go-progressive-subtraction-migration` | `1 / integration-test` | `completed` | `verified_complete / accepted` | `none` | 授权远端隔离 Docker provider 下严格 `JUHE_AI_REQUIRE_INTEGRATION=1` 通过：fresh Goose `000001..000101`、PostgreSQL 18、Redis 8、scope、20+1、并发、`SKIP LOCKED`、empty、rollback fence 与 post-commit invalidation 均已实际验证。 |
| `w10-staged-ingress-semantics` | `node-to-go-progressive-subtraction-migration` | `1 / Node-and-product-decision` | `active` | `implementation_in_progress / semantics_frozen` | `none` | 五项 ingress 语义与 golden 已冻结：重复字段、alias 冲突、未知工具、mapping/catalog snapshot 缺失一律 fail-closed；继续按最新 Node 主线实施，最终 lane 必须贯穿 queue、claim、slot、观测与 listener lifecycle。 |
| `w10-staged-ingress-preflight-body-order` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / Node-order-aligned` | `none` | 最新 Node `preResolveGatewayRuntime -> express.raw` 次序已移植到未注册 Go staged ingress：credential/preflight 拒绝不触碰 body，只有 allow 才读取、解析并 orchestration。已通过 normal/`-race`/vet 定向验证；不含 body admission、listener、dispatch 或 owner。 |
| `w10-prebody-route-only-plan` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / single-route-state-advance` | `none` | `PlanFromPreflight` 只将已认证 preflight 转为一次 coordinator plan 与 ordered binding 副本；连续调用按 route policy 推进，但不得调用 preflight resolver 或 candidate loader。已通过 normal/`-race`/vet；不加载 capacity snapshot、不接 body admission、listener 或 dispatch。 |
| `w10-body-admission-capacity-normalization` | `w10-staged-ingress-semantics` | `2 / Node-parity-fix` | `completed` | `verified_complete / one-slot-invalid-limit` | `none` | `EffectiveCapacity` 已按 Node 将零/负并发限制归一为 1，再按 credential source 取最小值；不会把有效 source 静默排除。已通过 normal/`-race`/vet；不涉及 store、lease acquire、listener 或 owner。 |
| `w10-prebody-candidate-capacity-snapshot` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / bounded-hydrated-capacity-facts` | `none` | `LoadPreBodyCapacity` 以空 model/endpoint、active-only projection 和 256 分批 hydration 建立最多 256 个 account/source/limit 事实；凭据、claim、slot 和 listener 均不外泄或接线。已通过 normal/`-race`/vet。 |
| `w10-prebody-runtime-admission-composition` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / single-route-plan-runtime-selection-admission` | `none` | `gatewayprebodyadmission` 一次 route plan 后按 Node runtime group 规则选择容量 snapshot，并在 normal speed-first/high-concurrency/path-text 条件下返回 body lease；不读 body、不注册 listener，lease terminal release 与 body-stage 不重复推进 route 仍是后续门禁。 |
| `w10-route-body-continuation` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / no-second-route-state-advance` | `none` | `gatewayrouteplan.BuildFromRoute` 与 `gatewayrequestorchestration.BuildFromRoute` 只能继续同一 opaque preflight scope/mode/generation/binding plan；不支持 continuation 时拒绝，不能回落再次 plan。已通过 normal/`-race`/vet 与 Node route-coordination regression。 |
| `w10-responses-prebody-continuation` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / body-error-release-and-no-legacy-replan` | `none` | staged Responses boundary 已可显式消费 pre-body result：拒绝不读 body，body/parse/continuation failure release acquired lease，成功继续同一 route result；仍无 HTTP terminal subscriber 或 listener。已通过 Go normal/`-race`/vet、全量 `go test ./...`、全量 `go vet ./...` 与两项 Node regression。 |
| `w10-prebody-lease-terminal-contract` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / listener-consumable-first-terminal-wins` | `none` | `gatewayhttpcompletion` 将 future handler 的显式 completion 与 request-context cancel 收敛为同一一次终态，并提供未注册的标准 `net/http` wrapper；staged Responses 已可把成功 pre-body lease 绑定后交给注入 response/dispatch owner；`gatewaybodyadmission` 仅在 acquired lease 上订阅该终态。已通过 Go normal/`-race`/vet、串行全量 `go test -p 1 ./...` 和全量 vet；不注册 route，不支持 hijack，不能替代完整 response/audit/usage、dispatch 或 production owner 验收。 |
| `w10-three-protocol-shared-inbound` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / native-body-facts-and-detached-shape-without-listener` | `none` | `GatewayProtocolInboundReader` 在 native OpenAI/Anthropic/Gemini shape 已分类后读取一次 bounded raw body；empty/non-JSON 透明保留且 `store` 保持 unscanned，non-empty JSON 以全层 duplicate-key fail-closed scanner 提取 model/stream/store，并生成 immutable preparation 和 copy-safe detached `RequestShape`。Gemini action path model 按 Node 覆盖正文 model，Gemini interaction query stream 计入 shape，坏 percent escape 保留原 segment。三协议定向 normal/`-race` 与 httpapi/protocol vet 已通过；它不替换 Responses full ingress/final-lane parser、不注册 route 或 owner，也不进行 dispatch/audit/usage。 |
| `w10-precommit-client-error-contract` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / exact-three-protocol-precommit-envelope-without-listener` | `none` | `gatewayclienterror` 以 immutable `gatewayrequestprep.Result` 的已知协议选择 Node `responses.ts` 的 OpenAI、Anthropic、Gemini JSON error envelope 与 type/status mapping；未知协议明确拒绝。它经 caller-owned staged sink 执行 stage/commit/write，已提交或有可见字节的 sink 均拒绝覆盖；不分类失败、不注册 route、不发起 dispatch，也不拥有 audit、usage 或 post-commit stream failure。定向 normal/`-race` 与相邻 response/attempt/stream regression 已通过。 |
| `w10-orchestrated-execution-handoff` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / frozen-shape-final-lane-and-current-group-runtime-without-listener` | `none` | `gatewayrequestexecution.BuildFromOrchestration` 仅接受同一 allowed preflight 的 route、boundary 显式 parsed intent、orchestration parsed intent、finalization/admission；两份 intent 与 preparation shape 的 model/stream 必须一致，并验证 final lane、capacity 与 snapshot revision 同源后，才在 execution plan 暴露 copy-safe `RequestShape`、final lane、authenticated API key ID 及每个 batch 的 detached runtime window。window 的 group ID、caller system account 和 group type 必须与已验证 binding/route scope 一致；任何缺失、伪造或不一致均 fail-closed。runtime window 只能供当前 group 的 high-concurrency/claim owner 使用，不能把后续旧 batch 当作 fallback；Node fallback 必须完整 preparation 目标 group 并重新 acquire client-IP lease。legacy `Build` 不会返回 final lane，不能用于 listener handoff。已通过相关 ingress/orchestration/execution/attempt/high-concurrency normal/`-race`/vet；不进行 claim、lease、dispatch、response/audit/usage 或 route 注册。 |
| `w10-current-group-execution-owner` | `w10-orchestrated-execution-handoff` | `2 / implementation-and-test` | `completed` | `verified_complete / first-frozen-window-only-without-listener` | `none` | `gatewaycurrentgroupexecution` 只接收 concrete finalized `Execution`，拒绝 legacy/non-finalized execution；它仅消费第一条 batch 的 detached runtime window，将 frozen request shape、final lane、identity 和 API key ID 传入既有 normal attempt loop 或 high-concurrency execution service。高并发 fallback outcome 原样返回，绝不继续旧 batch；无 high runner 明确失败。它只透传由未来跨组 owner 明确授权的 candidates-exhausted lifecycle continuation，默认不推导。`RunWithRequestLifecycle` 在拒绝 injected lifecycle 前先验证 finalized handoff，随后从同一 execution 创建唯一 opaque adapter，并在 adapter/attempt 前通知同一 lifecycle 的外层 callback；runner error 仍返还 concrete lifecycle。真实 failover 双 batch 已验证只执行首 batch。它不写 HTTP、审计或用量，不做 fallback preparation/lease、route 注册或 production owner 切换。 |
| `w10-http-terminal-reason` | `w10-staged-ingress-semantics` | `2 / implementation-and-test` | `completed` | `verified_complete / explicit-response-finish-versus-client-canceled-without-owner` | `none` | `gatewayhttpcompletion.Observer` 仅把实际 response owner 的显式 `Complete` 作为 `response_finished`，handler 执行期间的 request context cancel 为 `client_canceled`；wrapper 返回前停止 watcher，因此 server 后置 cancel 不伪造 Node `res.finish`/`res.close`。遗漏 completion 或 panic 保持 lease 占用，fail-closed。定向 `-race`/vet、全量 Go test/vet 与独立 Sol 总审均通过；它不接 response writer、audit/usage 或 route。 |
| `w10-current-group-request-owner` | `w10-current-group-execution-owner` | `2 / implementation-and-test` | `completed` | `verified_complete / cancellation-precedes-gateway-failure-without-listener` | `none` | `gatewayrequestowner` 在 current-group lifecycle 创建、adapter/attempt 前订阅同一 terminal 的 `client_canceled`，并在 runner error 终结 gateway failure 前同步收敛已发生的 context cancellation。它拒绝 lifecycle/hook 注入和跨组 continuation；normal completion 不猜测 success/failure。定向 `-race`/vet、全量 Go test/vet 与独立 Sol 总审均通过；它会调用既有 attempt runner，但不拥有 transport、HTTP、audit/usage、lease、response writer、route 或 target-group preparation。 |
| `w10-response-terminal-adapter` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `active` | `verified_unregistered_contract / default-off-deferred-response-handoff` | `targeted-normal-race-and-vet-passed / main-review-complete / production-owner-out-of-scope` | `gatewayresponseterminal` 提供窄 `AttemptLifecycle`、有界 `Handoff` 与两阶段 `Adapter`。普通 attempt-loop 行为保持不变；只有 current-group normal/high 显式设置 `DeferResponseTerminal` 时，已产生 typed `gatewayresponse.Result` 且 committed 的 success/failure 才跳过 loop 自身的 `ObserveSink`/`Finish*`，返回 `PendingResponseTerminal`。缺 typed response/sink facts 会显式 gateway failure，不静默回到提前终态。future response owner 用 `NewFromHandoff` 和 `RecordHandoff` 提供显式 disposition、writer action、protocol validation，再在真实 HTTP 完成证据后调用 `CompleteResponse`；`client_canceled` 先到时仍只收敛 `CancelClient`。handoff 不携带 body、stream payload、audit 或 usage，且不注册 handler、观察真实 finish/close、写 response、释放 lease 或接管 production owner。normal/high/current-group 与跨包端到端回归、`-race`、`go vet` 均通过；本轮主控复审已按 Node finish/close、提交、取消和审计顺序核对，真实 listener/production owner 接线仍明确不在范围内。 |
| `w10-request-finalization-handoff` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / terminal-bound-usage-audit-side-effect-handoff-without-owner` | `targeted-normal-race-vet-passed / gofmt-diff-check-passed` | `gatewayrequestfinalization` 要求 caller-owned `gatewayhttpcompletion.Observer`，可选要求 caller-owned `ResponseCompleter` 才能显式发布 `response_finished`；它从既有 `gatewayresponse.Handoff` 生成一次 detached `gatewayusage.FinalRecord` 与有界 `gatewayaudit.Snapshot`，client cancellation 覆盖 response success 为 `downstream_closed`，retained target lease 由 observer terminal once-only release。`SideEffectQueue` 只作为未来异步 usage/audit enqueue 边界，队列错误显式返回、成功 enqueue once-only；该包不写 response、不做存储/网络 I/O、不创建 listener/route/worker，也不接 production owner。normal、`-race`、vet、gofmt 与 diff-check 已通过。 |
| `w10-http-owner-composition-seam` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / unregistered-staged-dispatch-response-recording-only` | `targeted-normal-race-vet-passed / independent-sol-review-passed` | `httpapi.GatewayResponsesHTTPOwner` 将 staged ingress、预读 admission lease、注入的 dispatch/response executor 与 `gatewayresponseterminal` 串成只供验收的 handler seam。早期 preflight/admission 拒绝在 body 读取前交给显式 error handler；允许路径只进入一次 executor，response terminal handoff 必须显式提供 disposition/writer/protocol facts，随后仅 `RecordHandoff`。它绝不从 executor 或 handler return 推断 `response_finished`，不创建 `gatewayrequestfinalization`、不调用 `CompleteResponse`、不发布 usage/audit handoff、不入队副作用且不释放 retained/pre-body lease；真实 finish/close owner 取得证据后才可另行组合完成。client cancellation 优先，缺 handoff/不一致事实均回调可观察；该 seam 不注册 listener/route、不选择 upstream、不生成错误 payload、不修改 Node、不拥有 production owner。P1 修复后定向 normal/`-race`/vet 与独立 Sol 总审均通过。 |
| `w10-client-ip-account-pending-failure-transfer` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / unregistered-caller-owned-pending-fact-transfer` | `fresh-independent-sol-review-passed / targeted-normal-race-vet-passed / full-compile-gofmt-diff-check-passed` | `gatewayclientipconcurrency.Tracker` 新增 request-local、并发安全的 client-IP account pending-failure facts。scope 保留 system account、API key（缺省规范化为 Node 的 `internal`）并 trim client IP；只有缺 client IP 是显式 no-op，空 system account 仍按 Node 参与 scope。caller 只能提供完整 account/状态/错误/endpoint 事实，phase 仅接受 `upstream_response`、`upstream_request`、`stream`；256 上限和同账户 latest replacement 均用 result 显式报告。transfer 保留 target 原顺序、source 追加顺序和 target 去重，并在所有复制尝试完成后清源；同一 tracker 自转移也按 Node 清源，容量丢弃可观察。它不推断或产生 failure、不确认/持久化 avoidance、不获取/释放 lease，也不接 cross-group owner、HTTP/listener/route/worker。Node 当前没有 `rememberClientIpAccountPendingFailure` 的生产调用方；未来接线仍必须在 target full preparation 成功后执行 transfer，再 release source slot，并将 target lease 保留到终态。 |
| `w10-cross-group-pending-failure-transfer-handoff` | `w10-cross-group-fallback-owner-seam` | `2 / implementation-and-test` | `completed` | `verified_complete / unregistered-caller-owned-transfer-before-source-release` | `fresh-independent-sol-review-passed / targeted-normal-race-vet-passed / full-compile-gofmt-diff-check-passed` | `gatewaycrossgroupowner` 要求 caller 明确提供 `PendingFailureTransfer`，并仅在 target 已完成 fresh route-plan、singleton execution、target client-IP acquire/validation 后，以 canonical source/target scope 调用它；成功且 result 满足严格守恒/清源证明后，才经既有 strict handoff 释放来源 high lease。首次独立 Sol 发现负数可抵消计数的 P1，现已改为拒绝全部负值并用溢出安全的逐项扣减校验，high-source malformed-result 回归证明 target 清理而 source lease 保持；新的独立 Sol 总审已通过。该 seam 不产生 pending-failure facts、不确认/持久化 tracker、不直接操作 Redis/lease，也不注册 HTTP/listener/route/worker 或 production owner；Node 仍无该 tracker 的生产 producer。 |
| `w10-cross-group-fallback-target-handoff` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / forward-only-target-proof-without-execution` | `main-review-complete / targeted-normal-race-vet-passed / diff-check-passed` | `gatewayrouteplan.FallbackCursor` 将已进入的 binding 与 route plan 的 revision、dispatch generation、位置和 group 绑定，`NextFallbackTarget` 只选择同一计划中后续且尚未进入的 group，不回绕、不重排、不把重复 alias 当作独立 group；entered 集合必须包含 current group，且每项必须是 current cursor 之前的 route group，未知、未来、空、重复或超量值和 stale handoff 均 fail-closed。route-plan 还逐条把 ordered binding 与原 preflight 的完整事实核对，拒绝同 ID/group 的跨 preflight 拼接。它只交出 opaque target binding，绝不携带旧 candidate window、capacity 决策、lease 或 runnable execution。未来 outer route owner 必须先按 Node 完整准备 target group 并获取 target client-IP decision，再 once-only release 已取得的 source lease，同时接入 audit/usage/response listener。 |
| `w10-cross-group-fallback-fresh-target-preparation` | `w10-cross-group-fallback-target-handoff` | `2 / implementation-and-test` | `completed` | `verified_complete / fresh-candidate-target-without-route-owner` | `main-review-complete / targeted-normal-race-vet-passed / diff-check-passed` | `gatewayrouteplan.PrepareFallbackTarget` 与 `PrepareDispatchFallbackTarget` 从 opaque current cursor 后向扫描，按原 model/endpoint 重新加载每个候选窗口，跳过无候选或 policy 不可用 group，返回首个 scope 与 authenticated preflight 一致的 fresh target；candidate loader 错误、scope 拼接、policy 越权和 stale handoff 均 fail-closed。它不把 skipped group 伪装成已进入 group，也不推进共享 route coordinator、acquire lease、claim/attempt、写 audit/usage 或注册 listener。 |
| `w10-cross-group-fallback-dispatch-policy-preparation` | `w10-cross-group-fallback-fresh-target-preparation` | `2 / implementation-and-test` | `completed` | `verified_complete / Node-ordered-policy-with-reviewed-concrete-but-unregistered-capability-adapter` | `main-review-complete / targeted-normal-race-vet-passed / full-suite-residual-unrelated` | `PrepareDispatchFallbackTarget` 仅在每个 fresh window 经 mandatory policy 后交出 opaque dispatch handoff；`gatewayfallbackpolicy` 固定 excluded -> capability -> mapping-first/direct-second model resolution -> direct-before-mapped model tier -> authorization quota -> runtime degradation -> reason-specific capacity 的 Node 顺序，并在 degradation 后恢复 model/fallback/super-priority/priority tier。policy 返回值只能从原 fresh window 选择 ID；quota、runtime degradation、capability adapter 的 fail-closed 约束已按 Node 源码完成主审。任何有效 mapping 都不回落 direct，Codex Responses context 与所有 mapping bridge 仍明确排除。adapter 未注册，不接 outer runner、lease、claim、response、audit/usage 或 listener。 |
| `w10-cross-group-fallback-singleton-execution` | `w10-cross-group-fallback-dispatch-policy-preparation` | `2 / implementation-and-test` | `completed` | `verified_complete / dispatch-proven-frozen-request-plus-fresh-target-without-owner` | `main-review-complete / targeted-normal-race-vet-passed / diff-check-passed` | `gatewayrequestexecution.BuildFallbackTarget` 只接受 `PrepareDispatchFallbackTarget` 产出的独立 opaque handoff；legacy fresh-only target 在类型上不能进入。handoff proof 同时绑定 route cursor/fence、entered/excluded 集合、reason、protocol、request shape、client compatibility、parsed nested-tool intent、final lane 与 catalog/mapping finalization snapshot revision。随后才在 source first batch/current 同源、target 严格向后、API key/window/candidate scope 完整一致且 downstream 未 committed 时，创建仅含 fresh target 的 singleton execution。它不选择 fallback、不改变 entered/exhausted 状态、不创建 lifecycle、不 acquire/release lease、不 claim/attempt、写 audit/usage 或注册 listener。 |
| `w10-cross-group-fallback-owner-seam` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `active` | `verified_unregistered_contract / route-loop-and-terminal-bound-target-lease` | `targeted-race-and-vet-passed / fresh-independent-sol-review-passed` | `gatewaycrossgroupowner` 只以 caller 提供的 terminal observer 执行首个 current group，随后仅在完整账户事实和明确 Node reason 下按 opaque dispatch target preparation 构造 fresh singleton execution，并用同一 ready lifecycle 续跑 later group。每组的 proven excluded account IDs 以请求级有序集合累积，组内空值/重复保持拒绝、跨组重复去重；source high lease 只可经 strict handoff 在 target prepared 后释放；high target 的 pre-acquired lease 必须保留到 terminal，normal target 用 disabled no-op handoff proof。未完成 terminal 时，account exhaustion 终结为 upstream，high busy/client-IP reject/aborted 终结为 gateway；已观察到 `client_canceled` 才取消 lifecycle，`response_finished` 不伪造取消。malformed target acquire 即使携带 lease，也会在 error 或 decision 校验失败前释放，避免异常依赖泄漏容量。独立 Sol 已按当前快照复审通过；它仍不注册 HTTP route、不写 response、不拥有 terminal completion、audit、usage、pending failure 迁移或 Node production owner，listener 接线前必须重新审计完整 response/audit/usage 语义。 |
| `w10-node-protocol-cross-matrix-baseline` | `w10-staged-ingress-semantics` | `2 / Node-regression-audit` | `active` | `source_baseline_failed / not_a_go_owner_failure` | `Node regression failure` | 已在当前 `origin/master=e489de493` 实际复跑 `pnpm.cmd --dir backend run test:protocol-cross-matrix-mock-ai`：OpenAI Chat 同协议 mapping 的 usage `pricingModel` 为 `undefined`，期望 `cross-openai-chat-upstream`，断言位于 `protocol-cross-matrix-mock-ai-regression.ts:671`。本批 Go 未参与该 Node execution；等待 Node owner 修复或明确基线裁决后重跑，期间不得声称该 Node 三协议回归已通过。 |
| `w10-redis-runtime-integration` | `node-to-go-progressive-subtraction-migration` | `1 / integration-test` | `completed` | `verified_complete / Redis primitives and client-IP driver accepted` | `none` | 已在授权远端 Redis 的隔离数据库执行 normal 与 `-race` 强制 integration：account slot、client-IP、high-concurrency queue、session affinity 和 client-IP Redis driver 均通过。测试仅创建并清理随机测试键；不外推为 PostgreSQL、upstream、listener 或 owner 验收。 |
| `node-client-ip-dirty-generation-bigint` | `client-ip-statistics-postgres-migration` | `0 / Node-mainline-sync` | `active` | `source_recorded / no-go-postgres-dirty-window-owner` | `pending Go statistics owner` | 最新 `origin/master=e489de493` 修复 Node PostgreSQL client-IP dirty-window 清理：claimed `generation` 在 `VALUES` 中显式 `::bigint`，保持 dirty marker 的 generation CAS。当前 Go 不存在对应 PostgreSQL dirty-window writer，不能把 Redis client-IP runtime driver 误作等价实现；后续统计 owner 必须将该 SQL 参数类型与 concurrent CAS 一并迁移并以真实 PostgreSQL 验收。 |
| `w10-client-ip-runtime-driver-selection` | `node-to-go-progressive-subtraction-migration` | `1 / runtime-contract` | `completed` | `verified_complete / explicit no-fallback selection` | `none` | `memory` / `redis` factory 已按 Node 值规范化并 fail-closed：缺失、未知、缺所选 driver 都拒绝，Redis 绝不回退 local。它不读取环境、不开 Redis client、不接 app/listener/owner。 |
| `w10-cross-group-fallback-lifecycle` | `node-to-go-progressive-subtraction-migration` | `1 / Node-contract-audit` | `active` | `partial_contract / unregistered-route-loop-present-listener-contract-required` | `fresh-independent-sol-review-passed / listener-contract-pending` | 最新 Node 已核验：来源 high-concurrency group 的 initial all-busy fallback 必须发生在来源 client-IP acquire 前；已 acquired 来源 lease 则必须在 target full preparation 成功后 once-only release。`gatewayclientipconcurrency.LeaseHandoff` 只允许带完整 target `Input` 的 acquired decision 完成交接：enabled target 的 driver-private active lease、scope、limit 与 client-IP policy fingerprint 均须精确匹配，且 target scope 必须不同于 source；disabled target 只能是 Node no-op acquire。形态、scope 或 policy 不一致均保留 source 给终态。`gatewaycrossgroupowner` 现在只作为未注册 seam 消费严格 handoff：它以 immutable route-only plan 与 fresh target policy preparation 构造 singleton target，在 caller-owned terminal 才释放 retained high target lease，并累积请求级 proven exclusions；缺完整账户事实/reason 或无 target 时封口。它将未完成的 high busy/client-IP reject/aborted 分类为 gateway failure，只有 observer 已证明 client cancel 才取消 lifecycle。最新独立 Sol 复审已通过；它仍不拥有 HTTP terminal completion、response、audit/usage、pending failure 迁移或 listener。`gatewayattemptloop` 默认封口；`RunWithExistingRequestLifecycle` 验证 immutable request lineage 与 ready/无 commit lifecycle，随后才创建新 opaque adapter。完整 listener-owner contract 仍为前置。 |
| `w10-high-concurrency-request-fallback-callback` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / request-scoped-callback-before-source-client-ip` | `main-review-complete / targeted-normal-race-vet-passed / diff-check-passed` | `gatewayhighconcurrencyorchestration.Input` 允许 future outer owner 按请求覆盖 service-level fallback requester；initial all-busy 始终调用该 override，queue/recheck 后在未启用 strict handoff 时也调用它。启用 `PostSourceLeaseFallbackPreparer` 后，已取得 source lease 的 post-lease callback 必须改经 strict adapter，不能误写为直接 callback。nil 保留原 service default，当前不准备 target、不更新 entered/exhausted、不开 lifecycle continuation、不迁移 source lease、不安装 target lease，且不注册 listener 或修改 Node owner。Node source-lease 顺序与 callback gate 已完成主审。 |
| `w10-target-preacquired-client-ip-execution` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / scope-bound-target-lease-consumer-and-strict-callback-forwarding-without-owner` | `main-review-complete / targeted-normal-race-vet-passed / diff-check-passed` | `gatewayhighconcurrencyexecution` 允许已 complete target preparation 的 outer owner 传入 `PreAcquiredClientIP`；enabled decision 必须由 driver 签发、仍 active，且与 frozen target group/system account/API key/client IP/policy scope 精确一致；disabled decision 只能是 Node acquired no-op，错误或不匹配一律 fail-closed。`ClientIPInputForWindow` 公开同一 scope 的纯构造，避免 future owner 重写 policy/scope。已 retain 的 high target 若在 pre-lease initial all-busy 阶段再次 fallback，也改走 strict adapter，避免旧 target lease 被普通 callback 绕过。`gatewaycurrentgroupexecution` 只在 high-concurrency group 原样传递 pre-acquired input 和 strict `PostSourceLeaseFallbackPreparer`；normal group 对 pre-acquired、retain 或 strict post-source handoff 任一输入直接拒绝，避免把不存在的 client-IP lease 混入普通执行。显式 `RetainPreAcquiredClientIPLease` 时 execution 不 release target lease，future target terminal owner 必须持有并 once-only release；默认仍沿用本组内部 release。它不创建 target、route cursor、terminal、response/audit/usage 或 listener，也不接生产 owner。 |
| `w10-cross-group-fallback-account-facts` | `w10-cross-group-fallback-lifecycle` | `2 / implementation-and-test` | `completed` | `verified_complete / explicit-exclusion-and-reason-facts-without-owner` | `main-review-complete / targeted-normal-race-vet-passed / diff-check-passed` | `gatewayattemptloop` 仅在 `candidates_exhausted` 时，按 input candidate/account roster 验证每个账号均有 executor 明示的 `excluded` / `recoverable` 分类；无 attempt、无 eligible key、tracker 跳过、unknown 或 foreign account 都令 `FallbackAccounts.Complete=false`，同账号按最后分类收敛。reason 使用有限 Node 白名单，且仅在账户事实完整和最终 pre-commit executor 明示时发布顶层 `FallbackReason`；它不会泄漏到 attempt diagnostics。HTTP executor 的普通 pre-commit error 保持 unknown，不从 status/retry/inspection 猜测 stream 或 response owner reason；candidate stale/capacity/lease loss 为 recoverable。该批不创建跨组 owner、target singleton、lease transfer、listener、audit/usage 或 production 注册。 |
| `w10-postgres-provider-preflight` | `node-to-go-progressive-subtraction-migration` | `1 / dependency-preflight` | `completed` | `verified_complete / disposable PostgreSQL catalog smoke accepted` | `none` | 已通过授权远端 Docker 的 SSH tunnel 严格 gate，使用可销毁 `postgres:18-bookworm` 容器执行 fresh Goose `000001..000103` 与 `TestW10GatewayIngressCatalogPostgresSmoke`。覆盖 catalog 的 `gpt-image-1` image 事实、大小写 provider 归一化、缺失模型的显式 non-image 事实、hybrid physical-provider capability OR 及每协议 50 source provider 上限；测试结束后已核验容器销毁、tunnel 关闭。该 provider 的 Ryuk sidecar 无法建立回连，复跑时显式 `TESTCONTAINERS_RYUK_DISABLED=true`，并必须保留测试 defer 销毁与远端容器残留核验。 |

两个条目的 `instance_id`、`attempt`、`packet_revision`、`replacement_count` 与原子 claim 由 Codex goal/plan API 持有，本文不伪造数值。恢复者先从状态提供者读取这些字段；若不可读，按本清单元数据进入 `recovery_required` / `runtime_wait`，而不是将 W10 标为领域 blocked。

## W10 五项已冻结语义

1. lane 输入与优先级：model、stream、顶层 `type`、`tools`、`tool_choice`、`generationConfig` / `generation_config` 及后续 mapping/catalog 的顺序和冲突优先级。
2. staged envelope 与重复别名：有界、强制早出现的 envelope，以及重复 JSON key 和 alias 的接受、拒绝或确定性覆盖规则。
3. 工具与嵌套规则：嵌套 image tool、`responseModalities`、image MIME 的位置、递归边界、冲突处置和 lane 影响。
4. mapping/catalog：immutable snapshot、image 升级、更新/缺失/冲突的 fail-closed 语义，以及其与 candidate capacity 的顺序。
5. 权限降级：image 权限不足时 auto tool 降级为 text 的条件、禁止降级情形、错误语义及 admission 所见最终结果。

## 恢复顺序与边界

1. 重新审计共享 `master` / `origin/master`、迁移 HEAD、merge-base、两个 worktree 状态和正式 Node diff；漂移时先记录并在迁移 worktree 普通 merge。
2. W7 的独立最终验收已完成；若后续真实依赖验证发现问题，由一级 writer 在已定 W7 契约内修复并运行相应 guards。
3. W7 已通过授权远端隔离 Docker provider 的真实 PostgreSQL/Redis smoke；后续仅在修改该 durable core 或其 migration catalog 时重跑。它不覆盖 Node-Go 生产共存、触发 owner 或 handoff，Windows rootless Docker 仍不能作为通过证据。
4. 每批继续 `fetch` 并同步审计 Node / `origin/master` 的语义变化；有漂移时以最新 Node 行为更新 W10 契约和 golden。
5. W10 已完成未注册 pre-body 组合、route-only continuation、Responses body error release、标准 HTTP handler 可消费的 body-lease terminal contract、三协议共享 native body facts reader，以及三协议 pre-commit client-error renderer。下一步是将注入 response/dispatch owner 继续补成三协议 response writer、dispatch、audit/usage、client-IP 和跨 group fallback 的完整 lifecycle，并做真实依赖验收；随后才可考虑 route 注册。此前不接线、不改 owner。
6. Redis 原语和 client-IP driver 的真实 gate 已在授权远端隔离数据库通过；修改 Redis adapter 或 driver 后必须重跑。W10 catalog PostgreSQL smoke 已在授权远端 Docker 的可销毁容器通过；修改 catalog SQL、capability migration 或其 decoder 后必须以 strict gate 重跑。完成 production-equivalent PostgreSQL、完整三协议 upstream smoke、owner manifest/canary/drain/rollback 验收前，保持 Node gateway 唯一 production owner。

本清单没有生产或外部副作用：没有切流、删除、注册 listener/worker、owner 变更、canary 或 drain。
