# Redis 运行态与可靠队列隔离实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`逐任务落地；每个任务先写失败回归，再做最小实现并运行目标验证。步骤使用 checkbox 跟踪。

**Goal:** 在不扩容主机、不丢失未落库 Redis Stream 消息的前提下，将 Redis cache、runtime state、reliable queue 物理隔离，消除 AOF rewrite 对调度状态的连带延迟，并让运行期 Redis 瞬时故障不再造成 Node 主进程异常退出。

**Architecture:** 保持仓库既定的 `6379 cache / 6380 state / 6381 queue` 角色。6380 state 关闭 AOF/RDB 但保持 `noeviction`；6381 queue 保留 AOF `everysec`、关闭 RDB、保持 `noeviction`。生产切换前停止 main 写入并要求六条 Stream 的 `pending=0 && lag=0`，先切 queue URL，确认消息连续性后再关闭 6380 持久化。

**Tech Stack:** Node.js 22、TypeScript、Redis 8、Redis Streams、PostgreSQL、pnpm、Docker Compose、macOS launchd、PowerShell 7、Bash。

---

## 0. 范围、证据和固定决策

### 当前证据

- 生产 `JUHE_AI_REDIS_STATE_URL` 与 `JUHE_AI_REDIS_QUEUE_URL` 当前都为 `127.0.0.1:6380/0`。
- 6379 约 12.5MB/768MB，6380 约 46.6MB/2GB，主机 16GB 内存、磁盘剩余约 347GB，无淘汰/OOM/拒绝连接证据。
- 6380 AOF 约 1.05GB；1GB rewrite 阈值仍约每 11 至 12 分钟重写，并伴随 runtime-log XADD 超时和 Node 进程恢复。
- `RedisStreamQueue` 的消息只有在消费成功后才 ACK/XDEL；未 ACK 条目不能作为普通缓存删除。

### 固定决策

- 不扩容主机；不把容量问题用更大 `maxmemory` 掩盖。
- 不直接在共享 6380 上关闭 AOF/RDB。
- 不把 state/queue 改成任何淘汰策略；state/queue 都是 `noeviction`。
- 不保留生产长期共享 URL 兼容；`JUHE_AI_ALLOW_SHARED_REDIS_URLS=false`。
- 不把队列错误伪装成成功；runtime-log 等非核心 producer 使用现有有界 drop 语义并增加观测，关键 state 失败必须 fail-closed。
- 生产写操作必须单独取得正式上线确认和 `deployment-plan-lock.ps1 acquire`；本计划开发阶段不执行生产配置、进程、数据或切流操作。

## 1. 文件地图

| 文件 | 责任 | 本计划改动 |
| --- | --- | --- |
| `backend/src/config/runtime.ts` | 高性能模式 URL、共享资源和启动校验 | 固化 production 禁止共享与错误文案边界 |
| `backend/src/shared/redis-client.ts` | Redis client 创建、超时、连接错误和销毁 | 增加运行期异常可控边界与测试钩子 |
| `backend/src/shared/logger.ts` | 进程级错误兜底 | 不改最终 uncaught 兜底；移除调用方不当 fatal 升级 |
| `backend/src/shared/redis-stream-queue.ts` | Stream producer/consumer/ACK/XDEL | 保持可靠语义，补 queue 角色和超时回归 |
| `backend/src/shared/gateway-cache-invalidation.ts` | state 同步失败错误模型 | 将瞬时 Redis 错误映射为受控降级，不直接退出进程 |
| `backend/src/modules/runtime-logs/runtime-log-redis-producer.ts` | runtime-log 有界入队 | 确保超时、断连和 drop 不触发主进程退出 |
| `backend/src/scripts/regression/*.ts` | 运行时配置、Stream、故障注入回归 | 新增隔离/故障门禁脚本，扩展现有回归 |
| `backend/package.json` | 回归命令入口 | 注册新增脚本并纳入 performance boundary |
| `docker/compose.performance.yml` | 三 Redis 进程及持久化参数 | state 关闭 AOF/RDB，queue 关闭 RDB并设置 rewrite 参数 |
| `docker/.env.performance.example` | 三 Redis 端口、密码、容量 | 默认值与角色说明同步 |
| `docs/architecture/架构总览.md` | 长期模块/数据边界 | 记录三 Redis 物理角色和可靠性边界 |
| `docs/functions/PostgreSQL与Redis高性能模式设计.md` | 功能与存储事实 | 记录 state 可重建、queue 未 ACK 不可丢失 |
| `docs/deploy/高性能模式部署指南.md` | 通用部署、配置、drain 和回滚 | 修正非 Docker/macOS 等价边界和门禁 |
| `docs/deploy/macos/README.md`、`docs/deploy/watchdog/README.md` | macOS/launchd/watchdog | 补独立 Redis 服务和健康语义 |
| `docs/develop/测试与验证说明.md` | 验证入口 | 加入三实例、断连和队列连续性命令 |
| `docs/plans/计划-0122-Redis队列隔离与后台任务降载.md` | 上位计划 | 回填本阶段任务、验证和未覆盖范围 |
| `F:\服务部署\juhe-ai\03-部署流程\*` | 生产现场脚本/发布资料 | 仅在正式上线阶段按锁和授权更新，不在开发阶段修改 |

## 2. Task 1：先建立配置和角色边界回归

**Files:**
- Modify: `backend/src/scripts/regression/runtime-config-env-override-regression.ts`
- Modify: `backend/src/scripts/regression/performance-redis-boundary-regression.ts`
- Modify: `backend/package.json`

- [ ] **Step 1: 写失败回归**：在 runtime config regression 中加入四组 URL：完全共享、同 host:port 不同 DB、三实例独立、缺少 queue URL；断言 production/performance 对前两组拒绝，对独立组接受，对缺 queue 拒绝。

```ts
assertRegressionFailure(spawnRegression({
  JUHE_AI_RUNTIME_MODE: 'performance',
  JUHE_AI_REDIS_CACHE_URL: 'redis://:secret@127.0.0.1:6379/0',
  JUHE_AI_REDIS_STATE_URL: 'redis://:secret@127.0.0.1:6380/0',
  JUHE_AI_REDIS_QUEUE_URL: 'redis://:secret@127.0.0.1:6380/1',
  JUHE_AI_ALLOW_SHARED_REDIS_URLS: 'false'
}), /不能与/)
```

- [ ] **Step 2: 运行失败回归**：执行 `pnpm --filter juhe-ai-backend test:runtime-config-env-override`，预期新增断言在现状下至少有一项 FAIL；若现有实现已经满足，则把检查改为覆盖未覆盖的 production shared override 路径并记录实际红灯原因。
- [ ] **Step 3: 实现最小配置边界**：将 `redisUrlResourceKey()` 改为只按规范化 scheme、hostname、显式/默认 port 判重，不把 DB pathname 纳入物理隔离键；performance/production 禁止通过环境变量绕过共享检查，standalone/test-only 场景保留现有明确例外。
- [ ] **Step 4: 运行绿灯回归**：执行 `pnpm --filter juhe-ai-backend test:runtime-config-env-override` 和 `pnpm --filter juhe-ai-backend test:performance-redis-boundary`，退出码必须为 0，输出不得出现共享拓扑误报。
- [ ] **Step 5: 提交独立增量**：`git add backend/src/scripts/regression backend/package.json backend/src/config/runtime.ts && git commit -m "test: enforce redis role separation"`。

## 3. Task 2：固定本地/测试三 Redis 持久化参数

**Files:**
- Modify: `docker/compose.performance.yml`
- Modify: `docker/.env.performance.example`
- Modify: `docs/deploy/高性能模式部署指南.md`

- [ ] **Step 1: 写配置渲染回归**：新增 `backend/src/scripts/regression/redis-role-config-regression.ts`，读取 compose 文本并断言：cache `appendonly no/save ""/allkeys-lru`，state `appendonly no/save ""/noeviction`，queue `appendonly yes/appendfsync everysec/save ""/noeviction`，端口分别 6379/6380/6381。
- [ ] **Step 2: 运行回归确认缺口**：执行 `pnpm --filter juhe-ai-backend test:redis-role-config`，预期现有 state AOF 断言失败；输出必须指出 service 名和缺失参数。
- [ ] **Step 3: 修改 compose 参数**：state command 改为 `--appendonly no --save "" --maxmemory-policy noeviction`；queue 保留 `--appendonly yes --appendfsync everysec` 并增加 `--save "" --auto-aof-rewrite-min-size` 的显式初始值，初始 rewrite 值以生产已验证的 1GB 为基线，禁止在同一增量改变 fsync 语义。
- [ ] **Step 4: 修改示例容量**：cache 保持 768MB 现场建议；state 首次 2GB、queue 2GB，说明 `maxmemory` 是上限而非预分配；示例密码仍为空占位，不写真实凭据。
- [ ] **Step 5: 运行验证**：执行 `pnpm --filter juhe-ai-backend test:redis-role-config` 和 `docker compose --env-file docker/.env.performance.example -f docker/compose.performance.yml config --quiet`；预期命令退出码 0。
- [ ] **Step 6: 提交配置增量**：`git add docker docs/deploy/高性能模式部署指南.md backend/src/scripts/regression/redis-role-config-regression.ts backend/package.json && git commit -m "chore: define redis persistence roles"`。

## 4. Task 3：收敛 Redis 运行期错误边界

**Files:**
- Modify: `backend/src/shared/redis-client.ts`
- Modify: `backend/src/shared/gateway-cache-invalidation.ts`
- Modify: `backend/src/modules/gateway/usage/record-queue.service.ts`
- Modify: `backend/src/modules/audit-logs/audit-log-queue.service.ts`
- Modify: `backend/src/modules/operation-logs/operation-log-queue.service.ts`
- Modify: `backend/src/modules/public-api-logs/public-api-log-queue.service.ts`
- Modify: `backend/src/modules/runtime-logs/runtime-log-redis-producer.ts`
- Modify: `backend/src/modules/record-maintenance/record-maintenance-queue.service.ts`
- Create: `backend/src/scripts/regression/redis-runtime-failure-boundary-regression.ts`
- Modify: `backend/package.json`

- [ ] **Step 1: 先写故障注入测试**：用 fake Redis client 注入 `ECONNRESET`、connect timeout、command timeout 和 error event；断言 producer 返回受控失败、记录 reason，进程级 `uncaughtException` 监听器不会收到 Redis 错误。

```ts
const result = await enqueueRuntimeLogWithClient(fakeClientThatRejects('ECONNRESET'))
assert.equal(result.accepted, false)
assert.equal(result.reason, 'command_timeout_or_connection')
assert.equal(processFatalCalls, 0)
```

- [ ] **Step 2: 运行红灯测试**：执行 `pnpm --filter juhe-ai-backend test:redis-runtime-failure-boundary`，确认测试先因当前调用链可能触发 `scheduleProcessFatalError` 或悬空 rejection 而失败。
- [ ] **Step 3: 实现最小错误边界**：保留 `redis-client.ts` 的 error listener；为超时封装增加 `Promise` rejection 消费和 client invalidation；usage、audit、operation、public-api、runtime-log、record-maintenance 六类 producer 的 Redis 瞬时失败都返回各自有界 drop/失败指标，不调用 `scheduleProcessFatalError`；gateway cache invalidation 将瞬时 state 错误转为已有受控降级/错误响应，不在单次 Redis 失败时调用 process fatal。
- [ ] **Step 4: 保留真正 fatal 边界**：配置缺失、启动初始化失败、数据库核心持久化损坏和明确内部不变量破坏继续 fail-fast；不修改 `logger.ts` 的最终 uncaughtException 兜底来掩盖 bug。
- [ ] **Step 5: 运行绿灯与相邻回归**：执行新增故障脚本以及 `test:runtime-log-redis-enqueue-failure-boundary`、`test:gateway-cache-invalidation-runtime-state`、usage/audit/operation/public-api/record-maintenance 对应队列回归；每类都必须断言 `processFatalCalls=0`，每项退出码 0。
- [ ] **Step 6: 提交错误边界增量**：`git add backend/src/shared backend/src/modules/runtime-logs backend/src/scripts/regression backend/package.json && git commit -m "fix: contain transient redis failures"`。

## 5. Task 4：验证 Stream 角色、消息连续性和冷启动语义

**Files:**
- Modify: `backend/src/shared/redis-stream-queue.ts`
- Modify: `backend/src/scripts/regression/redis-stream-queue-regression.ts`
- Create: `backend/src/scripts/regression/redis-role-stream-continuity-regression.ts`
- Modify: `backend/package.json`

- [ ] **Step 1: 写 role-aware fake client 测试**：构造独立 state/queue URL，断言 `RedisStreamQueue` 的 producer、consumer、ACK/XDEL 均只调用 queue URL；state client 不出现 `XADD/XREADGROUP/XAUTOCLAIM/XACK/XDEL`。
- [ ] **Step 2: 写连续性测试**：向旧 queue 写入 6 类测试信封，模拟 pending、lag、ACK 后 XDEL；按每条 Stream 的全部 consumer groups 检查；切换 producer URL 后只接受新 queue，断言旧 pending/lag 未 drain 时迁移函数拒绝继续，且停止 main 后必须再次检查并通过两个静默窗口的 XADD 增量为 0。
- [ ] **Step 2a: 写六类重复重放测试**：对 usage、audit、operation、public-api、runtime-log、record-maintenance 分别模拟“PG commit 成功、XACK 前崩溃、同一消息重新消费”；断言稳定 ID/唯一约束只形成一条事实。任一队列缺少幂等键时，本任务先补该 repository/schema 当前契约和离线重复检查，再允许继续。
- [ ] **Step 3: 运行红灯**：执行 `pnpm --filter juhe-ai-backend test:redis-role-stream-continuity`，记录缺少的角色和 drain 门禁。
- [ ] **Step 4: 实现最小检查**：增加只读 `inspectRedisStreamContinuity()`，返回每条 Stream 的 XLEN、每个 consumer group 的 pending、lag、oldest pending idle 和 last-delivered-id；增加 `quiesce` producer fence 与 two-window 静默检查；禁止以仅 `pending=0` 代替完整 drain。
- [ ] **Step 5: 运行绿灯**：执行 `pnpm --filter juhe-ai-backend test:redis-role-stream-continuity`、`pnpm --filter juhe-ai-backend test:redis-stream-queue`、`pnpm --filter juhe-ai-backend test:runtime-log-redis-stream-smoke`。
- [ ] **Step 6: 提交队列边界增量**：`git add backend/src/shared backend/src/scripts/regression backend/package.json && git commit -m "test: verify redis stream role continuity"`。

## 6. Task 5：同步长期架构与部署文档

**Files:**
- Modify: `docs/architecture/架构总览.md`
- Modify: `docs/functions/PostgreSQL与Redis高性能模式设计.md`
- Modify: `docs/deploy/高性能模式部署指南.md`
- Modify: `docs/deploy/macos/README.md`
- Modify: `docs/deploy/watchdog/README.md`
- Modify: `docs/develop/测试与验证说明.md`
- Modify: `docs/plans/计划-0122-Redis队列隔离与后台任务降载.md`

- [ ] **Step 1: 更新架构事实**：写清 cache/state/queue 三个物理进程、6380 state 无持久化、6381 queue AOF、`noeviction` 关键状态，以及 Stream 未 ACK 消息不是普通缓存。
- [ ] **Step 2: 更新部署矩阵**：为 Docker 和非 Docker/macOS 分别列出端口、目录、密码、launchd label、日志、启动顺序、健康检查和 rollback；敏感值只引用私有资源，不写入文档。
- [ ] **Step 3: 更新 watchdog 语义**：说明单次 Redis 命令失败的应用错误模型、readiness degraded、持续故障的既有恢复边界；禁止 watchdog 通过并行启动重复 Redis 实例。
- [ ] **Step 4: 更新 PLAN-0122**：将本阶段标为进行中，链接本设计和本计划，明确大 payload 引用化/全面 producer 降载仍为后续阶段。
- [ ] **Step 5: 文档检查**：执行 `git diff --check`，使用 `rg` 检查生产真实密码、IP、token、URL 中的密码未进入 `docs/`；预期无敏感值匹配。
- [ ] **Step 6: 提交文档增量**：`git add docs && git commit -m "docs: record redis role and recovery boundaries"`。

## 7. Task 6：建立 macOS 生产只读预检和 drain 脚本

**Files:**
- Create: `F:\服务部署\juhe-ai\03-部署流程\inspect-redis-role-isolation.sh`
- Create: `F:\服务部署\juhe-ai\03-部署流程\verify-redis-stream-drain.sh`
- Create: `F:\服务部署\juhe-ai\03-部署流程\verify-redis-role-services.sh`
- Modify: `F:\服务部署\juhe-ai\03-部署流程\macOS临时服务接管发布方案.md`

- [ ] **Step 1: 只读脚本设计**：脚本只执行 `redis-cli INFO/CONFIG GET/XLEN/XPENDING/XINFO GROUPS`、`launchctl print`、端口和 PID 查询；脚本默认拒绝 `CONFIG SET`、`DEL`、`FLUSHDB`、`launchctl kickstart`。temporary env 必须验证 namespace、三个临时 Redis URL、临时 PostgreSQL URL 与 production 均不同，并确认不启动生产 queue consumer。
- [ ] **Step 2: 编写 drain 门禁**：六条 Stream 的全部 consumer groups 每组打印 XLEN、pending、lag、oldest pending idle；main 停止后再次检查，连续两个固定窗口确认 XADD commandstats 无增量；任意 pending/lag 非零退出码非 0，并列出具体 stream/group，不自动重试超过固定窗口。
- [ ] **Step 3: 编写角色门禁**：验证 6379/6380/6381 的 PING、密码来源、`maxmemory-policy`、AOF 状态、目录/launchd label、监听 PID 互不相同；system LaunchDaemon 变更使用 `sudo launchctl bootout` -> 等待 PID/端口释放 -> 原子替换 plist/config -> `sudo launchctl bootstrap` -> `sudo launchctl kickstart` -> 验证 PID/端口/cwd/config hash；任一步 sudo/launchctl 失败立即回滚，不让 KeepAlive 以旧配置复活；输出中脱敏 URL，不显示密码。
- [ ] **Step 4: 本地 shell 验证**：在 macOS 目标之外先执行 `bash -n`；在 Windows 仅做静态检查，不把 Windows 成功当作 macOS 运行验证。
- [ ] **Step 5: 将脚本接入发布方案**：补写正式上线前置、失败处理和回滚，不执行 apply；生产脚本修改必须在用户正式上线授权和部署锁之后进行。

## 8. Task 7：本地/测试环境验证矩阵

- [ ] **Step 1: 安装与构建**：在隔离 worktree 执行 `pnpm install --frozen-lockfile`；预期退出码 0，lockfile 不变化。
- [ ] **Step 2: 配置和边界回归**：执行 `pnpm --filter juhe-ai-backend test:runtime-config-env-override`、`pnpm --filter juhe-ai-backend test:performance-redis-boundary`、`pnpm --filter juhe-ai-backend test:redis-role-config`。
- [ ] **Step 3: Stream 和错误回归**：执行 `pnpm --filter juhe-ai-backend test:redis-stream-queue`、`pnpm --filter juhe-ai-backend test:redis-role-stream-continuity`、`pnpm --filter juhe-ai-backend test:redis-runtime-failure-boundary`、`pnpm --filter juhe-ai-backend test:runtime-log-redis-enqueue-failure-boundary`。
- [ ] **Step 4: 类型和构建**：执行 `pnpm --filter juhe-ai-backend typecheck`、`pnpm build`；预期无 TypeScript 错误且构建退出码 0。
- [ ] **Step 5: Compose smoke**：在独立 namespace 和测试数据库执行 `docker compose --env-file docker/.env.performance -f docker/compose.performance.yml config --quiet`、`docker compose ... up -d`、三 Redis PING/CONFIG 检查、`curl /__aisys__/health`、`curl /__aisys__/api/health` 和现有 performance smoke。
- [ ] **Step 6: 冷启动/故障注入**：仅在隔离测试环境依次重启 state、重启 queue、触发 AOF rewrite；断言 state key 可重建、queue 未 ACK 消息仍消费、Node PID 不异常退出、evicted/OOM/rejected 为 0。
- [ ] **Step 7: macOS 预生产演练**：在真实 macOS、独立端口/目录/namespace 上演练 system LaunchDaemon bootout/bootstrap/KeepAlive、state 冷启动、queue graceful restart、AOF rewrite 和 producer fence；Windows `bash -n`、fake client 或 Docker 结果均不能替代该发布阻断证据。断电测试按 AOF everysec 明确最多约 1 秒 RPO，不宣称断电零丢失。

## 9. Task 8：生产预检、发布、终验与回滚（需另行授权）

### 发布前

- [ ] 用户明确回复正式上线授权。
- [ ] 执行 `F:\服务部署\juhe-ai\03-部署流程\deployment-plan-lock.ps1 acquire`；有持有人时等待，不接管。
- [ ] 读取并哈希当前 release、env、launchd plist、Redis 配置、Nginx/Caddy 入口和 rollback 资料；不把凭据写入日志。
- [ ] 记录 main/temporary PID、拓扑、Redis INFO、六条 Stream XLEN/pending/lag，并确认远端 deployment-change lock/recovery marker 状态。

### 发布顺序

- [ ] 启动独立 6381 queue 并只读验证角色。
- [ ] 启动完全隔离的 temporary 三 Redis（16379/16380/16381 或现场批准端口），验证 namespace、URL、PID、PostgreSQL 和 consumer owner 与 production 不同；temporary 承流，执行 main producer quiesce fence，等待旧 6380 六条 Stream 的全部 consumer groups `pending=0 && lag=0`。
- [ ] 停止 main 全部 server/worker/scheduler 后，重复六条 Stream 全 consumer group 检查，并确认两个静默窗口内旧 6380 `XADD` commandstats 无增量。
- [ ] 切换 main `JUHE_AI_REDIS_QUEUE_URL` 到 6381，先不关闭 6380 AOF/RDB；temporary 承流观察。
- [ ] 验证 6381 新消息入队、消费、PostgreSQL 落库、ACK/XDEL 连续；验证 6380 无新增 Stream 命令。
- [ ] 停止 main，在确认 drain 和备份证据后按 bootout/原子替换/bootstrap/kickstart 顺序把 6380 改为 state `appendonly no/save ""/noeviction`，启动并验证新 PID/端口/cwd/config hash 和状态重建；temporary 使用隔离 state，不受影响。
- [ ] 执行 1+1+3 拓扑、业务 smoke、固定 PID 60 秒/15 分钟/30 分钟观察；确认无 fatal stderr、unexpected restart、evicted/OOM/rejected。
- [ ] 切回 main，清理 temporary 资源，写入 `VERIFY_SUCCESS`，释放部署计划锁。

### 失败回退

- [ ] queue URL 切换前失败：不改 6380 持久化，停止新 6381，恢复原发布包。
- [ ] queue URL 切换后失败：不得直接恢复 queue URL=6380；6381 保持新的 queue 事实源，继续使用可兼容发布包完成落库 drain；无法 drain 时保留 6381 AOF 修复，不清理、不丢消息。
- [ ] state 持久化改变后失败：优先继续使用 6381 queue 和 volatile 6380 state；不恢复旧 state AOF，不复活过期租约/锁；只有 queue 完全 drain、producer 已 quiesce 且有独立迁移证据时才回旧拓扑。
- [ ] 任意验收门禁失败不得切回 main，temporary 继续承流，记录现场和锁状态；每条回退路径最后执行 `deployment-plan-lock.ps1 release` 并记录 outcome=`rolled-back` 或 `cancelled`，temporary/Redis 资源只在证据归档后清理。

## 10. 完成验收

- [ ] 设计规格已提交并自审：`docs/superpowers/specs/2026-07-19-Redis运行态与可靠队列隔离设计.md`。
- [ ] 三实例配置和 URL 共享校验通过。
- [ ] state 无 AOF/RDB、queue AOF everysec + noeviction 的本地/测试证据齐全。
- [ ] Redis 短时断连回归通过，主 Node 无 uncaught/unexpected restart。
- [ ] queue 切换前后六条 Stream 消息连续性有 XLEN/pending/lag/落库证据。
- [ ] AOF rewrite 期间 state 延迟、Node PID 和队列 P95/P99 达到发布门禁；没有以“错误变少”替代性能数据。
- [ ] 生产 temporary 已清理、main 稳定、锁正常释放、计划验证记录完整。
- [ ] 代码、文档、测试和生产脚本提交到隔离分支；经测试后按用户要求合并 master，再反向合并当前 `feature/20260706-go`，最后推送远端。

## 11. 计划自审

- 规格第 2 至 4 节的根因、约束和目标由 Task 1 至 4 覆盖。
- 规格第 5 至 7 节的三实例参数、AOF 分阶段调优和错误模型由 Task 2 至 4 覆盖。
- 规格第 9 节的迁移、回滚和部署锁由 Task 6、8 覆盖；生产写操作明确留到正式授权阶段。
- 规格第 10 节的指标、告警和验收由 Task 5、7、8 覆盖。
- 未覆盖内容：大 payload 引用化、所有 producer 全量重写、维护任务全面降载；这些继续由 PLAN-0122 后续任务处理，不在本阶段伪装完成。
- 计划中没有 TBD/TODO 或“适当处理”占位；每个代码增量都有文件、测试命令和预期结果。
