# AI 问答上游 SSE 事件预算实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让两种 OpenAI 上游 SSE collector 使用统一、可配置且有界的事件预算。

**Architecture:** runtime config 负责严格环境解析，chat route 显式注入，两个 collector 只消费数值参数。现有单事件和累计内容边界保持不变。

**Tech Stack:** Node.js 22、TypeScript、项目 regression scripts、PowerShell 7。

---

### Task 1: Runtime 配置

**Files:**
- Modify: `backend/src/config/runtime.ts`
- Test: `backend/src/scripts/regression/runtime-config-env-override-regression.ts`

- [x] 写入默认值 `65536`、下界 `2048`、上界 `262144` 的失败断言。
- [x] 运行 `pnpm --dir backend test:runtime-config-env-override`，确认字段缺失导致 RED。
- [x] 使用现有 `integerConfig` 增加 `chat.upstreamSseMaxEvents`。
- [x] 重跑回归确认 GREEN。

### Task 2: 两协议 Collector

**Files:**
- Modify: `backend/src/modules/chat/chat-responses-sse.ts`
- Modify: `backend/src/modules/chat/chat-gateway-sse.ts`
- Test: `backend/src/scripts/regression/chat-responses-sse-regression.ts`
- Test: `backend/src/scripts/regression/chat-gateway-mock-ai-regression.ts`

- [x] 为两协议分别写入 2049+ 合法小 delta 成功和显式低上限失败断言。
- [x] 运行定向测试，确认固定 2048 上限导致 RED。
- [x] 给两个 collector 增加显式 `maxEvents` 参数，并在错误中使用实际限制。
- [x] 重跑两协议回归确认 GREEN，检查 `64 KiB` 与 `192 KiB` 断言仍通过。

### Task 3: 路由注入与文档

**Files:**
- Modify: `backend/src/modules/chat/chat.routes.ts`
- Modify: `docs/functions/AI问答设计.md`
- Modify: `docs/develop/运行说明.md`
- Modify: `docs/develop/测试与验证说明.md`

- [x] 写路由源码回归，要求两个 collector 都显式接收 `runtimeConfig.chat.upstreamSseMaxEvents`。
- [x] 修改路由并运行聊天定向回归。
- [x] 记录默认值、范围、常量内存与 DoS 权衡。
- [x] 运行 `pnpm --dir backend typecheck` 和 `git diff --check`。
