# Chat Conversation ID Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在会话详情中展示完整、可复制的会话 ID，便于用户准确反馈问题会话。

**Architecture:** 直接使用现有 `ChatConversation.id`，在详情描述表第一行渲染 ID 与复制图标。复制操作复用 `shared/clipboard.ts`，不修改接口、数据库或会话状态。

**Tech Stack:** Vue 3、TypeScript、Ant Design Vue、Node.js 回归脚本

---

### Task 1: 会话详情 ID 展示与复制

**Files:**
- Modify: `frontend/src/views/chat/ChatView.vue`
- Modify: `frontend/package.json`
- Test: `frontend/src/scripts/regression/chat-conversation-details-regression.ts`

- [x] **Step 1: Write the failing test**

新增源码回归断言，要求详情弹窗显示 `detailConversation.id`、提供“复制会话 ID”图标按钮，并调用公共 `copyTextToClipboard`。

- [x] **Step 2: Run test to verify it fails**

Run: `pnpm --dir backend exec tsx --tsconfig ../frontend/tsconfig.json ../frontend/src/scripts/regression/chat-conversation-details-regression.ts`

Observed: FAIL，指出“会话详情必须显示完整会话 ID”。

- [x] **Step 3: Write minimal implementation**

在详情第一行输出完整 ID，使用可换行等宽文本；右侧增加带中文 tooltip 和 aria-label 的复制图标按钮，复制成功提示“会话 ID 已复制”。

- [x] **Step 4: Run test to verify it passes**

Run: `pnpm --dir backend exec tsx --tsconfig ../frontend/tsconfig.json ../frontend/src/scripts/regression/chat-conversation-details-regression.ts`

Observed: PASS，并输出“AI 问答会话详情回归通过”。

- [x] **Step 5: Verify frontend types**

Run: `pnpm --dir frontend typecheck`

Observed: exit code 0。
