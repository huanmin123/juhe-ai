# BUG-0140 余额自动探测误判不支持结果

## 基本信息

- 编号：BUG-0140
- 状态：已修复（本地验证完成）
- 严重程度：P1
- 发现时间：2026-07-29
- 发现方式：用户反馈 / 本地 Mock
- 模块：后端 / AI 账户 / ops-worker / 余额查询 / 测试
- 关联计划：PLAN-20260729T071220373Z
- 关联 bug：BUG-0073、BUG-0113
- 责任人：AI

## 问题概述

- 现象：新账户余额自动探测没有稳定表现为“仅支持时自动开启”，手动开启后还可能长时间停在待查询。
- 期望：只有明确返回余额或账户级不限额时才自动开启；未命中保持关闭。
- 实际：统一内置查询器正常返回 `status='unsupported'` 时，自动探测服务仍构造命中结果并执行开启。
- 影响范围：首次激活自动探测和一次性余额回填；手动开启与周期刷新不使用该命中函数，但页面现象容易混淆。

## 复现步骤

1. 构造合格的物理单 Key 自动探测候选。
2. Mock `queryBuiltinAccountBalance()` 返回 `{ adapter, snapshot: { status: 'unsupported' } }`。
3. 调用 `detectAccountBalanceAdapter()`。
4. 旧实现返回非空命中，后续 `autoDetectAccountBalanceCandidate()` 会尝试开启。

## 环境信息

- 分支 / 版本：当前工作区
- 数据状态：纯内存 Mock，不依赖真实账户或凭据
- 浏览器 / 系统 / Node 版本：Windows / Node 22
- 是否稳定复现：是

## 根因分析

- 表象：自动开启看似没有按“支持能力”工作，列表又可能只显示待查询。
- 真实根因：自动探测服务把“查询 Promise 正常返回”当成“能力命中”，没有检查返回快照状态。
- 为什么会发生：原回归用抛异常模拟全部不支持，而真实统一查询器会正常返回 `unsupported`；测试替身与生产契约不一致。

## 修复方案

- 修改点：自动探测仅接受 `fresh`、`unlimited`；明确 `unsupported` 条件清除持久意图，其他失败状态延后重试。探测、自动刷新和手动刷新共享账户租约，所有探测写回同时核对活动/可调度资格、配置版本和预期到期时间。历史 Node PostgreSQL 路径对 `schedulable`、`balance_query_enabled` 和分组启用状态保留 `INTEGER(0/1)`，必须使用 `0/1` 谓词与数值参数；不能从 Goose 的 boolean 迁移定义反推这些 Node 表。
- 行为影响：不支持、失败和中间态账户保持关闭，不写自动探测失败快照；worker 重启、临时失败或在途状态切换不会丢失后续探测机会，成功路径不变。
- 发布异常处理：`000093` 只新增 PostgreSQL partial index，不迁移历史数据；回退到前一构建时保留该前向兼容索引即可。

## 验证记录

| 验证类型 | 验证内容 | 命令 / 步骤 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 回归验证 | `unsupported` 正常返回 | `pnpm --filter juhe-ai-backend test:account-balance-auto-detect` | 保持关闭 | 2026-07-30 通过 | 通过 |
| 回归验证 | `failed/pending` 防御 | `pnpm --filter juhe-ai-backend test:account-balance-auto-detect` | 保持关闭 | 2026-07-30 通过 | 通过 |
| 主流程 | `fresh/unlimited` 命中 | `pnpm --filter juhe-ai-backend test:account-balance-auto-detect` | 自动开启 | 2026-07-30 通过 | 通过 |
| 动态 Mock | 真实返回联合类型 | 自动探测专项的注入查询器 | `treatedAsDetected=false` | 2026-07-30 通过；直接返回生产联合类型 | 通过 |
| 并发回归 | 在途状态切换与租约互斥 | `pnpm --filter juhe-ai-backend test:account-balance-refresh` | 不丢失探测意图且不重复请求上游 | 2026-07-30 通过 | 通过 |
| PostgreSQL 迁移 | catalog、schema gate 与索引目录 | `go test ./internal/migrationcatalog ./db/migrationtests`、`pnpm --filter juhe-ai-backend test:postgres-goose-schema-gate` | 版本与 partial index 一致 | 2026-07-30 通过 | 通过 |
| PostgreSQL 静态契约 | 历史 Node 余额候选与健康成功的 `INTEGER(0/1)` 谓词 | `pnpm --filter juhe-ai-backend test:account-balance-refresh`、`pnpm --filter juhe-ai-backend test:account-health-check` | 不将 legacy Node PostgreSQL `INTEGER` 当作 boolean | 2026-07-30 通过；真实 PostgreSQL 未接入 | 通过 |
| 全量验证 | 类型检查与构建 | `pnpm typecheck`、`pnpm build` | 通过 | 2026-07-30 通过；构建仅输出既有 Vite 大包提示 | 通过 |

## 复发记录

- 无。

## 下次遇到

- 先核对生产函数的返回联合类型，不要用抛异常替代所有失败或不支持状态。
- 重点检查“Promise 成功”和“业务成功”是否被混为一谈。
- 状态机回归必须覆盖每个可返回状态，而不是只覆盖成功和异常。

## 完成总结

- 完成时间：2026-07-30
- 结论：状态分类、持久探测意图、候选提交门禁和共享租约均有自动化证据；类型检查与构建通过。
- 后续建议：fresh Goose 管理库可按其独立发布流程执行 `000093`；历史 Node PostgreSQL 库禁止运行或基线 Goose，只能先 `inspect` 再使用遗留索引桥接执行 `apply -> verify`，随后验证恢复扫描与计划。本次未执行真实上游调用或生产数据操作。
