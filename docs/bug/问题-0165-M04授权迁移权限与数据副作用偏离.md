# BUG-0165 M04 授权迁移权限、校验与副作用偏离

## 基本信息

- 编号：BUG-0165
- 状态：待修复
- 严重程度：P0
- 发现时间：2026-09-04
- 发现方式：自查（已提交 Git 历史审计）
- 模块：后端 / 网关 / 存储 / 迁移
- 关联计划：`docs/migration/Node全量清零迁移总计划-20260904.md`
- 关联 bug：BUG-0152、BUG-0155
- 责任人：待定

## 问题概述

- 现象：M04 `authorizations` Go 实现没有接入 gateway，且其路由、owner scope、资源绑定、校验、幂等、DTO 和副作用均未达到 Node 契约。
- 期望：授权创建、查询、修改、撤销、过期、用量和 team/account 绑定必须保持 Node 的权限边界、状态结果、CAS、缓存/统计/健康副作用。
- 实际：
  1. Go 缺少普通用户 self create/update/revoke、usage 聚合和 team/user detail 等 Node 端点。
  2. 历史 Go 实现未用 `?systemAccountId` 做 owner scope 校验，管理员可操作其他 owner 的授权（已修复创建路径；列表/修改/撤销 scope 仍待复核）；account 资源读取了不存在的 `resource_owner_system_account_id` 列，且 `targetGroupId` 未写入 `group_accounts` 绑定，后者还依赖未迁移的授权实例创建链路，当前不能用局部写入冒充完整修复。
  3. team grant fanout 历史实现未排除 owner（已修复）；历史实现对 owner-only 团队仍创建空 fanout grant（已修复）；历史实现 active grant 上限存在 20→21 的 off-by-one（已修复）；历史实现对相同 active 授权未返回 Node 的 `{created:false}` 幂等结果（已修复）；历史实现对 terminal grant 会插入第二条 active 行并返回 `created=true`，现已修复为复用原行并返回 `created=false` + `previousStatus`，变更后的 active 重复请求仍按 Node 返回冲突。
  4. expiresAt/limits/remark 校验放宽，self list 忽略 direction，team member 详情可见性缺失；Summary/usage DTO 缺少前端需要的 effective source、权限、limits、usage 和聚合字段。创建路径的 expiresAt 严格 RFC3339、UTC 毫秒规范化、不得早于当前时间及不得晚于源账户到期时间已在本轮确认并修复。PATCH 首轮对照已确认仍存在：路由/Store 不能区分字段缺失和 JSON `null`，因而不能按 Node 清除 expiresAt/limits；Store 直接写入 expiresAt，遗漏 RFC3339 规范化、过去 expiresAt→expired、expired→active 恢复及账户到期上限规则，并且没有把变更同步到 runtime；相同值 PATCH 仍写入新版本而 Node 返回 unchanged；`?systemAccountId` 管理员 scope 未传入 Store，可能修改筛选 owner 之外的授权。PATCH 保持单独待修复。
  5. 已撤销授权再次撤销返回 404 而 Node 返回 unchanged 200；过期/写入缺少 quota/group/cache/health 副作用。
  6. create 的 `ProcessingTTL: 120_000_000` 在 Go 中为 120ms，Node 默认 120s（BUG-0155）。
- 进一步确认：账号资源 owner 查询使用不存在的 `resource_owner_system_account_id` 列，真实 schema 只有 `system_account_id`；`targetGroupId` 未写入 `group_accounts`，导致账号授权创建失败或丢失绑定。团队 runtime 刷新还未按 resource type/id 精确过滤，可能误激活无关授权。
- 影响范围：授权功能无法完整使用，并存在越权操作、账号授权创建失败、授权来源错误、重复写入和旧缓存继续生效风险。

## 根因与证据

- Go：`backend-go/projects/gateway/internal/authz/routes.go`、`mutations.go`、`store.go`、`team_cascade.go`。
- Node/前端：`backend/src/modules/authorizations/authorizations.routes.ts`、`backend/src/storage/resource-authorization-write.repository.ts`、`resource-authorization-read.repository.ts`、`frontend/src/api/domains/authorizations.ts`。
- 当前 gateway main 未发现 authz 挂载；Node 路由仍在 system-api-app 中运行，未有 Node 删除或归档 manifest。

## 修复与验证

- 修改点：冻结完整授权端点/权限矩阵和 golden，修正 owner scope、资源绑定、fanout、上限、幂等、严格校验、DTO、ProcessingTTL 及所有提交后副作用；接入真实 listener 后做 SQLite/PG/Redis、并发和前端回放。
- 当前验证：提交 `5f2341504` 修复相同 active 授权的幂等创建结果；提交 `a1dafba60` 复用 terminal 行并返回 `created=false`、`previousStatus`，新增 returned→active 回归并验证不会产生第二行；提交 `c4b2886e7` 将团队上限判断修正为“新增后不得超过 20”，并覆盖第 20 条允许、第 21 条拒绝；提交 `f8a3c9669` 过滤团队内资源 owner，验证不会生成 owner runtime 行；提交 `25ddb320e` 在 fanout 前拒绝 owner-only 团队并验证不落 grant；提交 `a5ef114ab` 在创建路径强制 owner scope，并覆盖跨 scope 拒绝；后续共享提交 `d6ea6eef1` 带入空 `limits:{}` 与 `NULL` 等价的幂等修复，独立复审通过。本轮对照 Node `normalizeResourceAuthorizationExpiresAtInput`、`validateResourceAuthorizationExpiresAt` 确认历史 Go 创建路径遗漏严格时间输入、未来时间、账户到期上限，并在修复前确认“资源不存在 + 已过期时间”的错误优先级偏差；提交 `46a61aae4` 实施最小创建路径修复并加入回归，独立复审通过。定向 `go test`、`-race`、`go vet` 与 `git diff --check` 均通过；仍仅覆盖窄路径，未覆盖上述权限和跨表副作用。

- PATCH 取证与修复：Node `resourceAuthorizationPatch` 以字段存在性区分缺失与 `null`，规范化 expiresAt/limits；显式过去 expiresAt 会转为 expired，清除/更新 expired 授权可按规则恢复 active，并对未来值执行账户上限校验；随后 `syncResourceAuthorizationGrantRuntime` 投影 grant 变化，且无差异时返回 unchanged。管理员 `systemAccountId` 被 `RequestAccessScope` 下推到 `WHERE resource_owner_system_account_id`。Go 原实现存在 presence 丢失、直接写入未规范化时间、过期状态机缺失、runtime expiry/status 不投影、paused→active 仍保持 paused、空变更返回 conflict 等偏差。owner scope 子项已于提交 `f4360fb45` 完成最小修复并经独立复审通过；提交 `3fb3379d4` 修复 expiresAt presence/null、RFC3339 UTC 毫秒规范化、过期/恢复状态机、账户到期上限、runtime expiry/status/source 投影及 unchanged 回执。独立复审随后发现 expired+null 未恢复状态、移除团队成员可能被重新激活以及 unchanged 仍记操作日志三个问题；提交 `8288bfb6f` 修复状态回退、按当前 active team member 解析 runtime 并跳过 unchanged 日志，补充对应回归；再次复审发现 active 状态切换未重验已有 expiry，提交 `f512accc1` 修复并补充回归。最新定向 `go test`、`-race`、`go vet`、`git diff --check` 已通过。limits schema/DTO、expectedUpdatedAt 严格校验和其他端点仍待后续切片。【回退说明：`3fb3379d4`、`8288bfb6f`、`f512accc1` 已随提交 `662bd85f5` 整体回退（owner scope `f4360fb45` 保留），「已修复」状态以本条末的重修记录为准。】
- PATCH limits 切片已修复：提交 `eee4cf59b` 将 PATCH/创建路径改为保留 Raw JSON presence，接入共享 `gatewayquota` 归一化（严格字段、启用状态、金额精度、小时窗口、最大金额），将 `null`/`{}` 规范化为 NULL，grant/runtime 在 active、paused、expired 状态下同步，并以当前规范化结果返回；创建路径同时拒绝 Node 不接受的 `limits:null`。独立复审随后发现终止 grant 的 runtime 不应被新额度改写，提交 `7d83f65d1` 限制 revoked/returned 投影并补回归。`go test ./projects/gateway/internal/authz ./projects/gateway/internal/gatewayquota -count=1`、authz `-race`、`go vet` 和切片范围 `git diff --check` 均通过。该切片不关闭 expectedUpdatedAt 严格校验、完整 DTO、其他端点及缓存/统计/健康副作用缺口。【回退说明：`eee4cf59b`、`7d83f65d1` 已随提交 `662bd85f5` 整体回退。】
- PATCH/expire `expectedUpdatedAt` 切片已修复：Node 的 `rfc3339InstantSchema` 要求带 `Z`/数值 offset 的 RFC3339，并规范化为 UTC 毫秒后再做 CAS；提交 `c1073e9c1` 为 Go PATCH/expire 接入同一格式校验、UTC 毫秒规范化和按时间瞬间比较，CAS 使用数据库当前版本生成下一版本；提交 `d61af7577` 补齐缺失/null 版本值与 Node 一致的 400 错误。新增回归覆盖等价 offset、bare/非法值及缺失/null，定向 `go test`、`-race`、`go vet` 和切片范围 `git diff --check` 均通过，独立复审 PASS。revoke/return 等其他端点的严格版本输入仍待后续切片，不能据此宣称 M04 完整收口。【回退说明：`c1073e9c1`、`d61af7577` 已随提交 `662bd85f5` 整体回退。】
- revoke/return `expectedUpdatedAt` 切片已修复：Node 两条端点共用 `authorizationMutationVersionSchema`，严格要求 RFC3339 instant 并在路由解析阶段 canonicalize；提交 `3c4ac0c0c` 为 Go 两条路由和 Store 接入同一格式校验及 UTC 毫秒规范化，按时间瞬间比较 CAS，并兼容历史 Go 记录省略 `.000` 的合法时间戳。新增回归覆盖等价 offset 和非法/bare 值，定向 `go test`、`-race`、`go vet`、`git diff --check` 均通过。其他端点及 DTO/跨表副作用仍待后续切片。【回退说明：`3c4ac0c0c` 已随提交 `662bd85f5` 整体回退。】
- revoke 幂等/日志切片已修复：Node 对已 `revoked` 且版本匹配的请求返回 `unchanged` 200，不更新版本且不写操作日志；更新其他状态时日志使用真实 `previousStatus`。提交 `85a6062a7` 使 Go 在 CAS 成功后对已 revoked 返回 unchanged、保留结果与前状态，并仅对 updated 写日志且记录实际前状态；新增重复 revoke、旧版本 conflict、paused→revoked 回归。定向 `go test`、`-race`、`go vet`、`git diff --check` 均通过。其他端点及 DTO/跨表副作用仍待后续切片。【回退说明：`85a6062a7` 已随提交 `662bd85f5` 整体回退。】
- 回退后的正规重修（2026-09-04，本工作区）：独立审查裁决并行审计的 12 项 M04 契约主张中 9 项为真实偏差、3 项基线已对齐。当前工作区按该裁决单切片正规修复 `internal/authz`：新增 `sync.go` 逐字移植 Node `syncResourceAuthorizationGrantRuntimeAsync`/`upsertResourceAuthorizationForUserAsync`/`refreshResourceAuthorizationEffectiveSourceAsync`（direct/team 分流、runtime status/revoked_*/expires_at/limits_json 显式写回、source upsert 回 active、四分支 refresh），patch 回 active 不再用内联近似；Patch 接入 expiresAt 毫秒 Z 规范化、JSON null 清空、空串报错、presence 区分、相同值 unchanged、过期状态机、expired 恢复须同调过期时间、激活（含 paused→active）按已存 expires_at 重验与账户到期上限；limits create/patch 复用 `gatewayquota` schema 归一化并随 sync 投影 runtime、响应回显规范化值，revoked/returned 终态 runtime limits 不可变；revoke/return 版本路由层 `rfc3339InstantSchema('授权配置版本格式不正确')` 校验+canonicalize，Store 按 instant 比较 CAS；revoke/return 的 conflict 判定先于终态检查，已 revoked/returned 且版本匹配返回 unchanged+previousStatus，操作日志仅 updated 记录且 revoke 记录真实前状态，return 额外校验 runtime 与 active manual source；`/my-authorizations` 的 `direction` 在 selfOnly 解析（非法 400）并传入 `Filters.Direction`，管理面列表仍忽略该参数。`authorized_reads_test.go` 的 resume 断言按 Node 契约改回「恢复可见」，并为每项修复补对齐测试（含 Node 证据行号注释）。定向 `go test`（authz/accounts/systemteams，`-count=1`）、authz `-race`、`go vet`、`go build ./...` 通过；代码尚未提交，复审后随本切片提交。
- `my-authorizations` 的 `direction` 下一切片取证：Node 仅在 self-list 将 `direction=outbound|inbound` 下推到读仓储；Go 的 `Filters` 与 SQL 已具备对应条件，但路由未填充该字段，导致合法 direction 查询始终返回 all。该差异已确认，先登记为独立读路径修复切片。（已随上方「回退后的正规重修」一并实现：selfOnly 解析 + 非法 400 + 传入 `Filters.Direction`。）
- 结论：M04 不是可接管 Node 的完整授权功能；越权、账号资源写入失败和跨资源 runtime 误激活按 P0 处理。

### 2026-09-05：管理面团队授权用量汇总端点未迁移

- Node 基线：历史 Node 在 `/authorizations` 与 `/my-authorizations` 两套挂载下注册 `GET /usage/team-summary`。查询 schema 严格接受可选 `systemAccountId`、`resourceType(account|group)`、`resourceId`、`teamId`、`startDate/endDate`（日期必须为 `YYYY-MM-DD`）；仅给 `resourceId` 而缺少 `resourceType` 返回 400，未知参数也返回 400。服务按当前 usage 统计时区补齐缺省日期窗口，并将 `systemAccountId` 解析为可管理 owner scope；无可访问 scope 时返回空聚合，不越权读取其他 owner。结果从 `authorization_team_usage_range_windows` 读取对应团队/资源/日期窗口，返回 `{ range, summary }`，summary 保留 `requestCount`、`inputTokens`、`cacheWriteTokens`、`totalTokens`、`totalCost`、`lastUsedAt` 等聚合字段；无统计行也返回规范化零值 summary（`backend/src/modules/authorizations/authorizations.routes.ts`、`storage/authorization-usage.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/authz/routes.go` 未注册 `/authorizations/usage/team-summary` 或 `/my-authorizations/usage/team-summary`；其 `usage` handler 只支持 self 单条 `/my-authorizations/{id}/usage`，读取授权摘要且明确省略 J5 usage 窗口数据，authz Store 也没有团队汇总查询/时区窗口/过滤 DTO。
- 结果偏差：授权管理页的团队维度用量汇总在 Go 直接返回路由不存在，无法按团队/资源/日期显示请求量、token、费用和最后使用时间；退回单授权详情或直接读原始表会丢失 Node 的日期时区补齐、resourceType/resourceId 校验、owner scope 以及零值 summary 契约，不能视为功能等价。
- 结论：这是已提交 M04 授权用量读链的确定管理/self 端点与聚合 DTO 缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：管理面团队授权用量明细端点未迁移

- Node 基线：历史 Node 在 `/authorizations` 与 `/my-authorizations` 两套挂载下注册 `GET /usage/team-details`。查询严格复用 usage 公共筛选（`systemAccountId`、`resourceType/resourceId`、`teamId`、`startDate/endDate`），并接受 `page`、`pageSize`；日期按 usage 统计时区规范化，`resourceId` 无 `resourceType`、非法日期/页码或未知参数返回 400。仓储按 owner scope 和团队/资源筛选读取 `authorization_team_usage_range_windows`，默认每页 20、最大 200，并将窗口限制在 1001 行；按 `totalCost`、`requestCount`、`lastUsedAt` 降序及 team/resource ID 稳定排序，返回 `{ range, rows, total, page, pageSize, hasMore }`。每行投影团队 ID/名称、资源类型/ID/名称、资源 owner ID/名称、请求/token/费用 usage 摘要和 `lastUsedAt`；无可访问 scope 返回空页而非越权数据（`backend/src/modules/authorizations/authorizations.routes.ts`、`storage/authorization-usage.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/authz/routes.go` 未注册 `/authorizations/usage/team-details` 或 `/my-authorizations/usage/team-details`；authz Store 没有 `authorization_team_usage_range_windows` 分页读取、统计时区、团队/资源名称批量投影、稳定排序或 `hasMore` 结果结构。
- 结果偏差：管理面团队授权 usage 明细在 Go 直接返回路由不存在，无法按团队和资源分页查看请求量、token、费用及最后使用时间；用列表或单授权 usage 替代会丢失团队维度、owner 资源投影、筛选/分页窗口和 scope 保护，不能视为功能等价。
- 结论：这是已提交 M04 授权团队 usage 明细读链的确定端点、分页和 DTO 缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：管理面用户授权用量汇总端点未迁移

- Node 基线：历史 Node 在 `/authorizations` 与 `/my-authorizations` 两套挂载下注册 `GET /usage/user-summary`。查询严格接受 usage 公共筛选（`systemAccountId`、`resourceType/resourceId`、`teamId`、`startDate/endDate`）及可选 `granteeSystemAccountId`；日期必须是 `YYYY-MM-DD`，资源 ID 必须同时指定资源类型，未知参数/空用户 ID 返回 400。服务按 `usageStatsTimezone` 规范化缺省/单端日期窗口和 owner scope，将团队与被授权用户过滤映射到 `authorization_user_usage_range_windows`；无可访问 scope 或无行时返回零值聚合。响应为 `{ range, summary }`，summary 包含 `requestCount`、`inputTokens`、`cacheWriteTokens`、`totalTokens`、`totalCost`、`lastUsedAt` 等字段，不暴露原始窗口行（`backend/src/modules/authorizations/authorizations.routes.ts`、`storage/authorization-usage.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/authz/routes.go` 未注册 `/authorizations/usage/user-summary` 或 `/my-authorizations/usage/user-summary`；其 self 单授权 `usage` 不支持团队/用户/资源/日期筛选，也没有 `authorization_user_usage_range_windows` 聚合读取、时区归一化或 summary DTO。
- 结果偏差：授权管理页无法在 Go 查看某团队/用户/资源时间窗口的汇总用量，直接返回路由不存在；用单授权详情或团队汇总替代会混淆用户维度并丢失 `granteeSystemAccountId` 过滤、零值和日期窗口契约，不能视为功能等价。
- 结论：这是已提交 M04 授权用户 usage 汇总读链的确定端点、筛选和聚合 DTO 缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：管理面用户授权用量明细端点未迁移

- Node 基线：历史 Node 在 `/authorizations` 与 `/my-authorizations` 两套挂载下注册 `GET /usage/user-details`。查询严格复用 usage 公共字段并支持 `granteeSystemAccountId`、`page`、`pageSize`；日期、资源过滤、用户 ID 和未知参数的 400 边界与 user-summary 一致。仓储按 owner scope、团队 ID、被授权用户 ID、资源类型/ID 和时区归一化日期读取 `authorization_user_usage_range_windows`，默认每页 20、最大 200、窗口最多 1001 行，按费用/请求量/最后使用时间及用户和资源 ID 稳定排序。响应 `{ range, rows, total, page, pageSize, hasMore }` 的每行包含 grantee ID/display name/username、所属团队名称、资源类型/名称、资源 owner 名称、usage 摘要和 `lastUsedAt`；无可访问 scope 返回空页（`backend/src/modules/authorizations/authorizations.routes.ts`、`storage/authorization-usage.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/authz/routes.go` 未注册 `/authorizations/usage/user-details` 或 `/my-authorizations/usage/user-details`；authz Store 没有用户 usage 窗口查询、分页、团队/账户名称批量投影或 `hasMore` 结果结构。
- 结果偏差：授权管理页无法在 Go 按被授权用户查看明细，直接返回路由不存在；用团队明细、单授权 usage 或原始统计表替代会丢失 grantee 过滤、稳定排序、分页窗口及用户/资源 owner 投影，不能视为功能等价。
- 结论：这是已提交 M04 授权用户 usage 明细读链的确定端点、分页与 DTO 缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：单条授权用量详情端点未完整迁移

- Node 基线：历史 Node 在 `/authorizations/:id/usage` 与 `/my-authorizations/:id/usage` 两套挂载下提供带窗口的授权详情。查询严格校验 `systemAccountId`、可选 `startDate/endDate`（`YYYY-MM-DD`）及 `page/pageSize`（正整数；详情成员页上限 200），参数或授权 ID 不合法返回 400，不存在/越权授权返回 404；读取前还执行到期授权扫描。成功响应保留完整授权 summary，并附 `usageRange`、总 `usage`、`lastUsedAt`、`usageBySystemAccount`、成员总数/页码/页大小/hasMore。team grant 会按资源 owner、团队来源和当前 runtime authorization 分页聚合每个成员的 usage；system-account grant 返回该 grantee 的单成员 usage 行；缺少 runtime/成员时返回规范化零值页（`backend/src/modules/authorizations/authorizations.routes.ts`、`storage/resource-authorization-usage.repository.ts`、`resource-authorization-read.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/authz/routes.go` 仅注册 `/my-authorizations/{id}/usage`，没有管理面 `/authorizations/{id}/usage`；其 `usage` handler 只调用 `Store.Find` 返回基础授权 summary，并明确注释“usage omitted”，未读取日期窗口、成员分页、`usageBySystemAccount`、聚合 usage 或到期扫描。
- 结果偏差：管理员无法查看授权用量详情，self 页面即使能访问也只能得到无 usage 的基础授权记录；无法按 team 成员/日期窗口显示请求量、token、费用及最后使用时间，缺失与越权状态码/分页结果，不能视为功能等价。
- 结论：这是已提交 M04 单条授权 usage 详情的确定管理端点缺失及 self DTO/统计实现缺口，当前仅记录，未修改 Go 代码。
