# BUG-0175 二次伪迁移首轮核对缺陷清单（storage 账户域）

## 基本信息

- 编号：BUG-0175
- 状态：待修复（第一波核验产出，作为第二轮修复队列）
- 发现方式：二次伪迁移逐文件核验（第一波 B01–B05/B07–B09，38 文件：landed 23 / eliminated 4 / partial 11）
- 台账：`docs/migration/final-migration/second-pass/台账-第一波.md`

## 缺陷清单（按影响排序）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-01 | RecordSuccess fenced 分支 SQL 谓词缺陷（模板第三 %s 绑定列名非条件，丢 `AND status NOT IN ('disabled','error')`） | accountkeystates\probe.go:694-703（正确写法参照 proberepo\mutation.go:343） | PG 报错写失败；SQLite 恒 0 行静默 stale_probe_state |
| D-02 | initializeAddedAccountApiKeyRuntimeStates(+InClient) 未落地（新 Key 播种 unverified+计数器重置） | 归档 account-api-key-runtime-state.repository.ts:467-632 | 新增 Key 无运行态行 |
| D-03 | per-Key 运行明细投影未接线（SetAPIKeyRuntimeDetailsReader 无实现无调用） | m11_reads.go:634-646 | GET /accounts/{id}/api-key-runtime 恒空 items |
| D-04 | J2 周期余额结果投影链无 owner（outcome→accounts.balance_query_next_refresh_at CAS + juhe_stats relay_balance 投影 + 游标） | 全仓（两个桥接文件 eliminated 但消费者未重建） | 周期余额结果不写回业务/统计 |
| D-05 | authorized-dispatch PATCH 三缺口（配额门降级、post-commit 失效三件套、SQL 形状放宽） | m11_authorized_dispatch.go:336,264-287,315-319 | 额度耗尽可改写、统计/缓存滞后、非实例账户可改 binding |
| D-06 | 排期同步 ActivationHook 三个调用点全 nil（激活不推进 dispatch revision 族+根锁） | jobs oauthrefreshvailability.go:41-43 调用点 | 重新激活账户沿用旧 circuit 状态 |
| D-07 | batch 路径余额快照清理未接线（端口与 reason 常量已备，仅 PATCH 接线；PLAN.md:127 登记与实际不一致） | batch.go:544-548、batch_effects.go:34-36 | proxy 变更批量的旧快照残留 |
| D-08 | 排期错误文案重品牌仅覆盖批量/导入（单账户创建/编辑透出「API Key 时间计划」） | write.go:509-512、patch.go:452-455 | 用户可见文案偏差 |
| D-09 | input_versions 版本预留 SELECT 三处缺 FOR UPDATE | write.go:948、delete.go:320、cleanuprepo\deleteaccount.go:839 | 并发预留理论重复版本（窗口极小） |
| D-10 | 管理列表授权行 effective status 过滤/排序走 owner 分支（归档 :702-725 授权分支缺失）；groupBindStatus 缺 authorization_unavailable 三态 | list.go:534,590,863-868 | 授权行筛选/排序不等价 |
| D-11 | PATCH 超级优先/降级互斥「压平」缺失（单边开启不压掉行内已开的另一方） | patch.go:404-424 ↔ 归档 :522-526 | 可产生双开状态影响调度 |
| D-12 | 单体 PATCH 后置 effect 家族缺失（dispatch revision 推进、group_accounts local 同步、J1 outbox、stats dirty、改名/搜索词连锁、oauth 两列、clearFailureState 简化臂、credentialsPatch 静默、groupChanged 旧 priority） | patch.go 多处（batch.go:496-549 有完整范式可对照） | 编辑后联动全面弱化；oauth 列是 jobs 刷新触发条件 |
| D-13 | 授权实例编辑无分支（白名单+active 预检+锁序缺失，Store.Patch 按 owner 路径处理实例行） | patch.go ↔ 归档 :982-1112,1226-1307 | grantee 可经 PATCH 改来源控制字段 |
| D-14 | SetModelCatalogReader 生产未接线（模型目录校验实际 no-op） | compose.go accountStore 装配段（实现已在 model_catalog_validation.go） | 保存路径目录校验失效 |
| D-15 | 写路径协议矩阵断言未迁移（保存可过、运行时不命中） | write.go/patch.go/batch.go/import.go 保存链 | 非法映射 200 保存 |
| D-16 | supportedModels 供应商目录校验缺失（normalizeAccountSupportedModelsForProvider 族） | 同上 | 不在目录的模型可保存 |
| D-17 | options 授权实例 union 缺失（授权实例在下拉不可见；W2 文档与代码矛盾） | list.go:1068-1193 | 用户可感知 |
| D-18 | statreads PG 方言风险（keyword contains 仅 instr 分支；PG 业务库下可能非法 SQL） | statreadsihealth.go:411 | PG 部署域待验证 |
| D-19 | NextEwma int64 截断 vs Math.round（EWMA ≥0.5 差 1ms 传导排序边缘） | accountquality\score.go:55 | 低危 |
| D-20 | 手动测试上下文凭据解密失败按「无凭据账户」处理（Node 500） | test_options_service.go:229-252 | 错误归因偏差 |
| D-21 | schedule 错误文案重品牌仅覆盖批量/导入（同 D-08，单账户两路径） | write.go:509-512、patch.go:452-455 | 文案偏差 |

## 处置

按第二轮修复队列推进；D-14 为一行接线可先行；D-01/D-03/D-11/D-12/D-13/D-17 建议优先。


## 第二波新增（B06/B11–B18）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-22 | ⚠️ completeAccountLockSuccess 整体缺失（锁端口无该方法、无实现、引擎成功路径无调用） | gatewaydispatch\ports.go:710-719、chain_account_locks.go ↔ 归档 account-lock.repository.ts:319-335 | 锁死账户后续成功不解锁→死亡期限结算 DEAD_CONFIRMED+original_status=active 时翻 temporary_unavailable；BUG-0174 B-2 修复自身的缺口 |
| D-23 | 管理列表 todayUsage 恒 0（usage summary 水合在 accounts 域缺失；PLAN 仅登记 groups 域 J5） | list.go:726 emptyUsageSummary | 用户可见 |
| D-24 | force-activate 的 dispatch 家族推进降级为单账户（应为根+全部授权实例）；instance 戳预检过宽（把 authorization_instance_source_account_id 也算实例戳） | m11_balance.go:599-601,654 | 授权家族围栏静默收窄 |
| D-25 | m11 三条运行态写路径（force-activate/authorized dispatch/traffic migration）缺写后失效（gateway runtime invalidation + group stats 刷新；归档三处均有） | m11_routes.go 三 store 文件 | 跨请求可见性风险 |
| D-26 | weekly 配额 resetAt=now+7d 日历日而非下一周一（非周一时最多晚报 6 天；statsagg\timekeys.go:44-51 有现成 Monday 算法） | circuitstore\listavailability_sources.go:511-513 | 展示与自然到期调度时点偏差 |
| D-27 | account-usage 读族只实现 SQLite 方言（instr+?），PG 分支（COLLATE "C" LIKE/$n）缺失 | statreads\accountusage.go（同 D-18 族） | PG 部署域静态不可用 |
| D-28 | api-key 排程状态翻转后无网关缓存失效广播（jobs 无 inval 能力，生效延迟最长 60s TTL） | jobs oauthrefresh\availability.go + worker_assembly.go:546-551 | 有界行为差异 |
| D-29 | tags 授权可见性过滤缺失（COUNT 与删 tag 均不含 visible_authorizations join） | accounts\tags.go:46-58,101-107 | 计数失真+失效实例专属标签拒删 |
| D-30 | aipublic updateApiKey 缺 3 次冲突重试包装 | aipublic\apikeys.go:377-417 | 外部推送退化为直接失败 |
| D-31 | authorization stats 聚合投影缺失（前端 authorizationCount/TeamCount 恒 false/0） | statreads\accountusage.go:1081-1094 | 用户可见（兜底不崩溃） |
| D-32 | authz options PG keyword 前缀缺 COLLATE "C"+starts_with | authz\options_store.go:127-129 | 行集偏差+索引失效风险 |
| D-33 | business\account_test_task 未接线的并行重复实现——建议删除或明确归档定位 | gateway internal\business\account_test_task\ | 死代码卫生 |
| D-34 | 请求成功观察（成功提前结束冷却+清残留失败列）无存活对应；恢复-to-active 收窄为纯时间过期+J1 投影器 | business\account_runtime\runtime.go:1091-1098 | 调度恢复即时性 |
| D-35 | 存储层 MarkAccountTemporaryUnavailable 冷却固定 now+1min（Node 3s 退避+settings 兜底）等时长细节 | runtime.go:950,970 | 低危 |


## 第三波新增（B10/B21–B27）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-36 | ⚠️ codex 桥接状态存储组合根未接线：NewChatBridgeStateService/NewSQLiteShardContextStateStore/NewPostgresContextStateStore 零非测试调用、ChainComposeDeps.CodexBridge 无生产赋值→运行链落降级适配器（ApplyContextStatePreflight 恒 false 继续） | gatewaycodex\contextstore.go/chatbridgestate.go + chain_compose.go:106 + chain_ports.go:1620-1647 | Responses 桥接状态恢复/保存/touch 实际不生效（Node 生产行为不等价）；jobs 侧清理/结算已接线 |
| D-37 | ⚠️ gateway-quota-snapshot 构建/发布链整体缺失：旧单体实现 133cd4e48 删除后未重落地、无减法裁决、jobregistry 仍登记 gateway、BUG-0039 修复随删 | 消费侧 gatewayquota\snapshot.go 完整；无生产者 | 额度判定退化为 DB 精确补判（可用但双层架构不等价） |
| D-38 | ⚠️ juhe_stats.group_account_stats 读链断开：写侧（脏标记+刷新 job）已落地，groups 读面渲染空形状（自述「待接线」）、mapper populated 臂与 stats 读 loader 无消费者 | groups\store.go:203-204,498-500,1016 | 分组统计显示恒空 |
| D-39 | ⚠️ SQLite 模式 client-ip 统计聚合数据源断点：聚合读统计库 usage_records 单表但生产写路径（分片文件+catalog）从不写它；statsverify 声明「Go 布局无分片文件」与 cleanuprepo\usageshards.go 矛盾 | jobs statsverify\store.go:408-412,jobs.go:25-28 | SQLite standalone 下聚合恒空（PG 不受影响） |
| D-40 | 自定义模型写后无运行时目录缓存失效发布者（reason 白名单在 failclass.go 但全仓无发布点；/v1 消费 24h TTL 目录缓存） | providers 包无 inval 依赖；gatewayruntimecache\service.go:57-58 | 写后最长 24h 旧目录 |
| D-41 | chat 压缩源页游标语义弱化：空页不推进到 claim.sourceThroughSequence→孤儿消息位于覆盖末端时压缩 stalled 失败 | chat\assets_store.go:548-551 + compaction_service.go:255-257 | 可达：失败 turn 孤儿消息场景压缩失败 |
| D-42 | chat 图片编辑谱系 root 不追溯根（RootAssetID 恒空退化为直接来源）+生成时刻来源/根资产续期缺失 | stream_execute.go:613-621、assets_store.go:350-356 | 多代编辑谱系/保留期一致性 |
| D-43 | IP stats range 归一化缺绝对钳制（today-30），响应 range 回显与 Node 不同 | ipstats\list.go:553-580 ↔ usage-stats-helpers.ts:182-212 | 回显字段差异 |
| D-44 | database.ts assertDistinctStoragePaths 六库物理身份门禁（realpath/nlink/inode/符号链接）无一对一承接 | gateway cmd\storage_bootstrap.go:47-65 只查非空；shared\platform\sqlitepath\physical.go 可复用 | SQLite 多文件布局防线缺失（PG 主部署不受影响） |
| D-45 | jobs 数据保留组合根固定 Asia/Shanghai+硬编码默认策略（自述临时边界）；PG 模式 stats/metrics 清理失去单事务整批回滚 | worker_retention.go:344,354-376 | 设置不生效+PG 部分删除 |
| D-46 | gateway-api-key 验证缓存启动预热（prewarm）缺失 | 归档 server.ts:181 | 冷启动首查回源 |
| D-47 | aipublic auth 三处宽容化（touch 失败不传播/损坏 JSON 返回空/非法 status 判 disabled）——已注释声明，登记备查 | aipublic\auth.go | 异常路径方向放宽 |


## 第四波新增（B19/B28/B30–B36）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-48 | ⚠️ usage_quota_hourly_windows 在 Go 无任何生产者：DerivedWindowRefresher 组合根固定传 nil、statsagg 阶段集合不含 quota hourly、dirty 标记只写不读；api_key/delegated 绑定重建不打脏标记 | worker_retention.go:91、statsagg\windows.go:23-34、apikeys\store.go:1466 | **小时级请求配额判定恒 0 成本**（配额执法缺口，高优先） |
| D-49 | ⚠️ chat 图片观察认领 15min 独占窗丢失：staleBefore=nowMs 本身（常量 chatObservationClaimStaleMs 定义未用）；说明字节上限 64KB vs 128KB；用量递减钳 0 不报错；generated- 前缀缺失 | cmd\chain_chat_observation.go:37-38,154、chat\assets.go:309-316,319-323 | 观察认领可被立即抢占、说明 JSON 被拒、账目漂移静默吸收 |
| D-50 | vectorstores jsonScalarEqual 非标量（对象/数组 filter）比较 panic→500 | openaicompat\vectorstoresstore.go:1124-1129（请求可触发） | 用户可触发 500 |
| D-51 | usage_records 每日分区 ensure 在 PG 写入链缺失（Node 每批前 ensure+memo；无 DEFAULT 分区；retention 只删不建） | jobs usagewriter\store.go:805-809 ↔ postgres-usage-record-partitions.ts:51-64 | PG 新日期首写可报 no partition found |
| D-52 | 模型目录写后失效发布者缺失（三 reason 全仓无发布点；消费侧就绪）+ built-in PATCH 响应丢 defaultHealthCheckModelCleared | providers 包 ↔ 归档 custom-provider-models.repository.ts:671 等 | 写后最长 24h 旧目录（=D-40 同根，本波交叉证实为整族缺口） |
| D-53 | PG 模式 catalog_visible=1 与 boolean 列类型错配（catalog.go 三处+test_options_service+chain_catalog+business\settings 共 6+ 处） | providers\catalog.go:299,819,1157 等 | PG 模式查询类型不匹配/静默失败 |
| D-54 | 代理写后 runtime 缓存失效缺失（60s 旧代理窗口）+ 代理变更即时重探入队缺失（退化为 due 周期） | proxyprofiles Store 无失效端口 ↔ 归档 proxy.repository.ts:524 等 | 有界行为差异+重探延迟 |
| D-55 | gateway-api-key 验证缓存启动预热（prewarm）缺失 | 归档 gateway-api-key.repository.ts:300-363 + server.ts:181 | 冷启动首查回源 |
| D-56 | authz 三条归还路径缺写后失效扇出（clearGatewayApiKeyValidationCache/invalidateGroupAccountIds/lookup/runtime/quota；总线已在、账户写路径在用） | authz mutations.go:649-739、groups\routes.go:661、m11_return_authorization.go ↔ 归档 :791-800 | 归还后运行时/配额快照滞留至 TTL |
| D-57 | authz 关键字搜索只搜 id/resource_id/remark（username/显示名/团队名/资源账户名/分组名全不命中）；详情读层裸 Summary（富化字段缺失）+visibleTo 缺团队成员分支+管理员 scope 详情收窄为 owner-only（与列表自相矛盾） | authz\store.go:253-262,279-282,294-302 | 管理面可感知功能回退 |
| D-58 | 授权分组 patch 写分支整体缺失（group_authorization_settings 无任何 INSERT/UPDATE；被授权人本地启用/调度覆盖写不生效） | groups\store.go:1245-1481 ↔ 归档 group-write.repository.ts:276-538 | 被授权人设置写落空 |
| D-59 | oauthUsage 读路径整条未迁移（写侧已入库；前端 accountUsageFormatters 持续为空） | 归档 oauth-usage-loaders.ts ↔ list.go ListItem 无字段 | 用户可见 |
| D-60 | 授权分组列表 accountStats/usage 水合=J5 顺延（PLAN:98 登记）；OAuth 轮换恢复状态分支=M17 顺延（PLAN:105）——两处为已登记顺延，非新缺口（复核确认） | groups\store.go:201-204、oauthmgmt\store.go:585-587 | 已知 |
| D-61 | 授权分组写面 group_authorization_settings 表无任何写入方（同 D-58 存储面）；authz options PG COLLATE 缺失（=D-32 同族）；向量库 jsonScalarEqual panic（=D-50） | — | 复核项 |
| D-62 | SHA256SUMS 清单过期（80 文件移入 -1 + 7 文件经 be0725f72 前向同步；sha256sum -c 失败 87/1700）——战役收尾时重生成或注记基线构成 | migration-backup\node\final-archive\SHA256SUMS | 簿记 |


## 第五波新增（B29/B37–B44）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-63 | ⚠️ **OpenAI OAuth 账户调度水合丢弃（需运行时复核）**：secret 投影 chain_accounts_secret.go:84-88 只读 api_keys/api_key，OAuth 凭据键集合（credentials_normalize.go:34-43）无这两键→return nil 丢弃；chainRuntimeCredentialSource（access_token→refresh_token）已移植但零调用（死代码）；openai 协议上游 Bearer 头随之失去凭据来源；hydration 测试无 oauth 凭据用例 | chain_accounts_secret.go:84-88,325 ↔ 归档 repository.ts:685,792-803 | OAuth 账户无法进入调度候选（项目主力账户类型；若证实为核心断裂） |
| D-64 | ⚠️ **授权实例派生+分组绑定族整体缺失**：CreateInput.TargetGroupID 路由必填校验但 Create 从不消费；ensureAccountAuthorizationInstance/bindActiveAccountAuthorizationToGranteeGroup/uniqueAuthorizedAccountInstanceName 全仓零命中；实例不创建、分组不绑定 | authz\mutations.go:23 ↔ 归档 resource-authorization-write-state.repository.ts:183-422 | 授权 AI 账户给用户后被授权人无可调度实例、/v1 无路由对象（授权主链断裂） |
| D-65 | authz 写后失效扇出缺失（create/patch/revoke/return 四路径；归档 refreshAfterResourceAuthorizationBusinessWrite 六件套） | authz 包非测试文件零 Invalidate 命中 | 管理面连改两次授权读旧投影 |
| D-66 | ResolvePolicyRuntime 动作映射四处分歧：drop_event→拦截失败（应丢事件）、retry_no_avoidance→retryEnabled=false、runtime_avoidance case 不可达、intercept→enforce 词汇入审计元数据 | gatewayresponse\inspection.go:115-130 | 配置 drop_event 策略行为改变+审计词汇变化 |
| D-67 | scheduled-job-lease 写事务内 FOR UPDATE pin 未移植（AssertScheduledLease 无事务无调用方）→协作式 fence 双写窗口 | jobs taskruns\store.go:720-755 ↔ 归档 :180-200 | 失约 owner 在途写与新 owner 接管不串行（待结合单写者假设裁决） |
| D-68 | schema-defaults 种子分歧：gpt-6-astra/claude-fable-5-1 未入 provider 默认模型清单；usageStatsTimezone 固定 Asia/Shanghai 无裁决（jobs 回退默认 UTC） | maintenance pg_schema.go:7389,7425,7771 | 全新库种子内容可见 |
| D-69 | 响应检查策略副作用端口生产未接线（FinalizationDeps.AccountEffects nil 短路；接口仅测试 mock 实现）——G13 装配缺口或待迁切片待裁决 | chain_v1.go:308-312 | Node 策略运行时副作用（本地抑制/上游 bucket）无执行点 |
| D-70 | sqlite-maintenance checkpoint 用 TRUNCATE（Node PASSIVE）且无 optimize | jobs worker_retention.go:287-292 | WAL 收缩时机/阻塞窗口 |
| D-71 | OAuth 代理刷新顺延（M17 登记）确认仍挂账：两处生产 OAuth 令牌传输明示不走代理、oauth_proxy_configuration_invalid 全库无命中 | oauthmgmt\transport.go:52-54、oauthrefresh\protocol.go:55-57 | 依赖代理出口的部署令牌刷新直连（PLAN:105 登记） |


## 第六波新增（B29 四试/B45–B52）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-72 | ⚠️⚠️ **用量记录投递链组合根断开**：gateway 只写文件 spool（自述 TAKEOVER POINT）、StartReplay 无生产调用、jobs usagewriter 装配了 Writer 但无任何 Enqueue 生产者、jobs 不读 spool | chain_usage.go:137-146、spool.go:362、worker_assembly.go:617-621 | **usage_records 无新数据来源**→统计/质量/配额下游全断供（核心断裂） |
| D-73 | ⚠️ **OpenAI OAuth 账户水合丢弃（证实）**：水合只读 api_keys→OAuth 账户 return nil；chainRuntimeCredentialSource 死代码；dispatch Bearer 失去凭据来源 | chain_accounts_secret.go:84-88,324-336 | OAuth 账户不可调度（主力类型） |
| D-74 | ⚠️ **授权实例派生+分组绑定缺失**（TargetGroupID 死字段）：被授权人无可调度实例 | authz\mutations.go:23 ↔ 归档 write-state:183-422 | 授权主链断裂（=D-64 确认） |
| D-75 | ⚠️ usage_quota_hourly_windows 无生产者（D-48 三重交叉证实：snapshot-helpers 视角同结论；消费者 costs.go/listavailability_sources.go/direct_input_reader.go 仍在读） | worker_retention.go:91 固定 nil | 小时级配额恒 0 |
| D-76 | usage_range_window_requests 请求-响应机制整体未迁移（表+retention 成孤儿）→ 非热点自定义范围窗口永不生成 | 归档 usage-range-window-requests.repository.ts 全文 | 统计范围能力回退 |
| D-77 | system-accounts PATCH：相同密码无条件旋转+吊销全会话（Node no-op）；仅改 role 留 must_change_password=1（管理员强制改密循环）；keyword contains vs 前缀；未知字段静默忽略 | authsys\store_accounts.go:868-881,958-968,390 | 管理面行为偏差 |
| D-78 | ResolvePolicyRuntime 动作映射四处分歧（drop_event/retry_no_avoidance/runtime_avoidance/intercept 词汇） | gatewayresponse\inspection.go:115-130 | drop_event 策略行为改变 |
| D-79 | scheduled-job-lease FOR UPDATE pin 未移植（协作式 fence 双写窗口） | taskruns\store.go:720-755 | 待单写者假设裁决 |
| D-80 | schema-defaults：gpt-6-astra/claude-fable-5-1 未入默认清单；usageStatsTimezone 固定无裁决 | pg_schema.go:7389,7425,7771 | 种子可见 |
| D-81 | SQLite stats 扣减链缺陷族：authorization daily 引用不存在列（no such column）；totals 缺 max 归零 CASE；quality 加侧缺 success 门控 | statssubtract.go:718-721,288-295 + statsagg\aggregate.go:568 | SQLite 部署域（PG 全保真） |
| D-82 | usage mappers 字段集缩减（overview/trend 多列读侧不输出，需产品确认）+accountKeyword 缺 NFKC | statreads 读侧 | 展示差异 |
| D-83 | worker_assembly 未注入 BusinessDB/Catalog/FreezePricing（SQLite 模式 last_used 副作用与写时快照不生效） | worker_assembly.go:607-620 | SQLite 部署域 |
| D-84 | chat 压缩源页游标 stalled（孤儿消息场景）+图片谱系 root/续期缺失（B20，补登记） | chat\assets_store.go:548-551、stream_execute.go:613-621 | 可达压缩失败+谱系漂移 |
| D-85 | table-monitor 缺启动预热+2 个 env 窗口旋钮（运维可调性）；角色词表缺 db-service: 前缀 | tablemonitor routes.go、statreads\systemmetrics.go:205 | 运维域 |


## 第七波新增（B53–B62，gateway 核心域）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-86 | ⚠️ 配额小时窗 refresher 函数体+调度接线整体缺失（=D-48/D-75 本体）：DerivedWindowRefresher 接口无实现、组合根注释「已由调度任务覆盖」与 worker_assembly 实际注册不符；三张脏范围表只写不读（死写+膨胀） | jobs statsagg ↔ 归档 usage-stats.repository.ts:1783-1951 | 小时配额恒 0 |
| D-87 | usage summary totals/daily 通用读链未落地（账户列表/分组 accountStats/授权列表 usage 注水恒 0，源码自证待接线） | groups\store.go:203、accounts\list.go:164、apikeys\usage.go:358 | 用户可见（=D-23/D-31/D-87 族） |
| D-88 | 响应检查审计元数据 32 字段完整 port 无生产调用方；现役接线 16 字段子集（缺 executionMode/dataHandling/retryEnabled/accountState/policyScope 等） | gatewayresponse\finalize.go:701 + gatewayusage\audit_capture.go:1533 | 审计/排障可见性削弱 |
| D-89 | turn-availability 探针协调器错误直接上抛不结算 generation（Node catch→probe_task_failure 结算+告警） | gatewaycodex\turnprobe.go:227-260 | 避让语义靠过期兜底 |
| D-90 | ⚠️ **D-36 升级三重证实**：CodexBridge 零值桩→桥恢复/持久化/compact 全不发生，且 compaction-expected 请求的「仅 native 账号」围栏被绕过（bridge 账号可接 compaction 请求=行为分歧非仅缺失） | chain_ports.go:1620-1653、chatbridgestate.go、compactpreflight.go | Responses 桥三行为缺失+一分歧 |
| D-91 | SourceIdentityResolver.GeminiInteractionResourceID 生产未接线→gemini interactions 源识别退化为 ip_api_key_fallback（可对接函数已存在仅差适配器） | chain_compose.go:291-293、gatewaygemini\affinity.go:177 | gemini 源身份语义缺失 |
| D-92 | AuditFinalizeInput 缺 accountId 字段（compact 成功审计少账号维度） | gatewaypreauth\types.go:225-235 | 审计维度 |
| D-93 | ⚠️ SanitizeCodexHistory 钩子全仓无赋值点→codex 历史脱敏三处调用全部 no-op，且 oauthnormalizer 按 input 标志虚标 CodexHistorySanitized=true（残留 item id 原样发上游） | oauthnormalizer.go:211,218、accountpreparation.go:568,615 | 脱敏失效+虚标 |
| D-94 | ⚠️ 容量 busy 谓词 any vs Node every（部分账户 busy 即 group_capacity_busy 回退/跳组；Node 整组全 busy 才触发）；无裁决无测试 | capacity.go:99-104 | 调度过早回退 |
| D-95 | upstream-dispatch 波次缺陷：并发等待预算结转空壳（每账户重置满额等待）、circuit-blocked 误记 failedAccountIDs（影响 codex 反转 exhausted）、未派发 confirmation reportUnknown 缺失（租约滞留）、观测面系统性缺口（stage 日志/审计标签/tier_escape）、HighConcurrencyQueue 无 nil 守卫 | upstreamdispatch.go:696,735,751,759 + enginehelpers.go:55-57 | 调度行为/可观测性 |
| D-96 | ⚠️ hybrid 质量检查/修复链「库级落地、生产零挂载」（qualityInspection 配置静默 no-op；文档仍描述该能力）；auxiliary assembled-minimal（审计/热质量/circuit/settings 覆盖缺+错误码漂移）；路由可观测性三件套实现完整但无生产入口 | gatewayhybrid + chain_compose.go:536-681 | 功能配置静默失效 |
| D-97 | 上游响应模型观察器已实现未接线（usage.upstreamResponseModel 归因不生效） | gatewayobs\upstreamresponsemodel.go ↔ upstreamattempts.go | 审计归因 |
| D-98 | 配额恢复 hint 数组递归缺失（数组内 reset_at 拿不到→计划边界冷却，方向安全但时长不同） | chain_error_policy.go:1172-1189 | 冷却时长偏差 |
| D-99 | ⚠️ openai-v1 api-key-client-compatibility 组合入口缺失（codex_responses→API Key 上游 body 未规范化+SSE 未强制；无 eliminated 裁决） | chain_driver.go:128-160 ↔ openai-v1\api-key-client-compatibility.ts | 该组合路径上游收到未规范化请求 |
| D-100 | ⚠️ 流式响应检查拦截器未接入生产管线（policies 传到 pipe options 但 Interceptor 恒 nil）+codex compaction 流式契约帧缺失+fast-path 保守化 | gatewayresponse\pipe.go:113,191 + gatewayopenai\buffer.go | 策略流式拦截不生效 |
| D-101 | IsolateOpenAIOAuthCodexSessionID 哈希键序与 Node 不同（迁移切点 session/prompt-cache 命名空间一次轮换） | gatewaydispatch\oauthnormalizer.go:507-526 | 切点一次性影响 |
| D-102 | 角色词表缺口：gateway 读侧缺 db-service: 前缀族、jobs 聚合 worker 副本 1-8 vs Node 1-64（历史多副本数据读侧丢行） | statreads\systemmetrics.go:205、statsagg\stages_metrics.go:926 | 历史数据统计 |
| D-103 | 系统账户 PATCH 语义（=D-77 详细化）：相同密码旋转+吊销、role 变更留 must_change=1、display_name 唯一性 409 文案泛化、未知字段静默 | authsys\store_accounts.go | 管理面 |
| D-104 | SQLite stats 扣减链缺陷族（=D-81 详细化）：authz daily 不存在列/totals 缺 max 归零/quality 缺 success 门控 | statssubtract.go、statsagg\aggregate.go:568 | SQLite 部署域 |
| D-105 | usage_range_window_requests 机制整体未迁移（=D-76 确认：非热点范围窗口永不生成） | 归档全文 | 统计能力回退 |
| D-106 | chat 压缩源页游标 stalled+图片谱系 root/续期缺失（B20 补登记） | chat\assets_store.go:548-551、stream_execute.go:613-621 | 可达压缩失败+谱系漂移 |
| D-107 | table-monitor 缺预热+env 旋钮；角色词表（=D-85/D-102）；向量库 panic（=D-50）；git mv 双列重复项说明（B29 types/files 已在第五波移动） | — | 登记 |


## 第八波新增（B64–B72，request/response/routes 核心）

| # | 缺陷 | 位置 | 影响 |
| --- | --- | --- | --- |
| D-108 | ⚠️ **非流式缓冲体丢弃**：PipeNonStreamUpstreamResponse 无条件启用 ≤1MB 检查缓冲（RequireFullyBuffered 字段声明后未读），HandleNonStreamUpstreamResponse 仅 protocolValidation&&FullyBuffered 才发送 → 上游 4xx/5xx 错误体与协议校验关闭的小 2xx（如 /v1/audio/speech）客户端收**空 200** | gatewayresponse\nonstream.go:63-185,324-347 ↔ finalization.ts:944-949,1061-1076 | 用户可感知核心断裂（Mock 复现：上游 429+chat 路径） |
| D-109 | ⚠️ client-IP 并发槽生命周期：engine.ClientIPConcurrency 组合根无赋值（high_concurrency 分组请求 panic）；ReleaseClientIPConcurrency 成功路径零调用（槽泄漏至 TTL 180s） | preparation.go:574、preflight.go:1060 ↔ routes.ts:2751-2756 | panic+并发上限失效 |
| D-110 | ⚠️ G08 动态路由选择器已实现未装配（GroupBindingOrderer nil→绑定保持存储顺序） | gatewayruntimecache\service.go:79-85 + 组合根零赋值 | API Key rr/weighted 不轮转（一行修复） |
| D-111 | ⚠️ API Key 运行态效果链生产未接线：NewAccountAPIKeyEffects 无构造、Engine.APIKeyEffects 从未赋值（confirmed rotation 失败静默短路）、RecordSuccess 零调用、dispatch 瞬态加载 stub | engine.go:100、enginehelpers.go:130-152、chain_dispatch.go:282-287 | 网关流量的 Key 级失败/成功/瞬态避让基本不发生 |
| D-112 | ⚠️ 非流式 JSON 响应检查策略主链不执行（语义帧提取端口零生产调用；比 D-69 更前一层：匹配本身缺失；SSE 生效） | nonstreamjson.go + driveradapters.go:24 | 管理面响应检查策略对非流式 JSON 不生效 |
| D-113 | ⚠️ 流式响应检查拦截器未接线（Interceptor 恒 nil）+codex compaction 流式契约帧缺失+PendingEvent 合并语义差+handlePipeError 缺 interpreted 基码+markRequestProtocolTerminalOutcome 缺失 | finalize.go:247-277、pipe.go:113,191 | 流式策略拦截/观察不生效 |
| D-114 | ⚠️ speed-first 全链生产休眠：NormalRouteFirstByteConfig/OnNormalRouteFirstByteDeadline 组合根不设（决策闭包无实现）、RecordSlow/Success 零调用、排序面显式降级 disabledDegradation——仅代码注释自述、无缺陷登记 | chain_v1.go:405-411、chain_ports.go:1304-1322、chain_dispatch.go:38-50 | speed-first 产品能力静默失效 |
| D-115 | ⚠️ authz 写后失效扇出缺失（=D-56/D-65 确认扩展：create/patch/revoke/return 四路径+三归还路径全缺；authz 包零 Invalidate） | authz 包 | 授权变更后投影滞留 |
| D-116 | 上游响应头不再向客户端转发（CopyResponseHeaders 零生产调用）；http metric upstream 失败标注两处未实现 | transport.go:771、finalize.go:607-609、sink.go:64 | 客户端头缺失+指标缺口 |
| D-117 | DELETE /interactions 空体白名单生产不可达→真实空 2xx 落协议校验 502 | finalize.go:163-176 挂在 Body==nil 分支 | gemini interactions DELETE 失败 |
| D-118 | 图像权限降级解析包装缺失→权限禁用+auto 工具时 Go 403 vs Node 移除工具继续（两分支死代码） | gatewaybody↔归档 image-permission-downgrade.ts | 用户可见 |
| D-119 | body/speed-first 拒绝记录 Recorder 未装配（G17 seam）：413/503/429 不写 dropped audit+usage failure；文本上限 settings 未接 | chain_compose.go:252-254,400-403 | 拒绝面审计缺失 |
| D-120 | SSE 等待心跳零装配（实现+钩子都在：retrybudget.SetWaitObserver/heartbeat.go 无调用；Node 两处装配）→ 长等待下游空闲超时断连 | preflight.ts:855-860 ↔ heartbeat.go | 流式等待断连风险 |
| D-121 | RequestStream 短路偏离归档且有测试固化反向断言（bodyState.stream=false 应回退 gemini ?stream=true） | gatewaypreauth\metadata.go:118-120 + metadata_test.go:108 | 需裁决对齐或记录 |
| D-122 | failure-response：failureScope 弃用（http metric 无承接）、HTTPCompletion 未接线（completedAt 退化）、审计缺 ResponseHeaders、usage 快照缺 errorMessage | sink.go:64,90,104 | 指标+审计 |
| D-123 | failure attempt 审计缺 status/headers/body；上游响应头透传缺失（=D-116）；错误体读取中断处理差异 | chain_ports.go:422-426 ↔ 归档 failure-dispatch.ts:276-283 | 审计完整性 |
| D-124 | finalization 杂项：applyHandledUpstreamRoutingEffects 缺失+成功恢复双重失活、cyber_policy 透传死码、204/205 条件少截、墙预算 503 vs 504、accountStateMutationEnabled 硬编码、模型观测冲突告警缺失 | nonstream.go/finalize.go 多处 | 逐项登记 |
| D-125 | fixed-responses：models 审计丢 firstTokenMs、usageSemantic 空占位 | sink.go:241-271 | 审计字段 |
| D-126 | usage-summary totals/daily 通用读链未落地（=D-87 确认扩展：账户/分组/授权三列表 usage 恒 0，源码自证待接线） | list.go:164、groups\store.go:203 | 用户可见 |
| D-127 | 系统指标角色词表（=D-102 确认）+usage mappers 字段缩减（=D-82 确认）+table-monitor 预热/旋钮（=D-85） | — | 复核项 |
| D-128 | upstream-models-probe 归档 gemini-v1beta 目录为空无法比对（归档裁剪痕迹；仅 OpenAI 供应商下无影响） | 归档目录空 | 归档完整性 |
