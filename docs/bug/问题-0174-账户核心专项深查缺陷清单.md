# BUG-0174 账户核心专项深查缺陷清单（调度/状态/探活/协议）

## 基本信息

- 编号：BUG-0174
- 状态：已修复（2026-09-07 两波清偿：波1 B-1/B-3/M-7 + M-1 + B-5/M-4/M-5/M-6；波2 M-2/M-3/M-9 + M-8 + B-2；见文末修复记录）
- 严重程度：含 blocker
- 发现方式：账户核心专项深查（5 个只读代理与 Node 归档行为级比对，基线 3f524b4ee）
- 关联报告：`docs/reports/Go去跨进程战役报告-20260907.md`（新缝背景）

## Blocker（核心路径断裂）

| # | 缺陷 | 位置（Go / Node 基线） | 后果 |
| --- | --- | --- | --- |
| B-1 | 调度链 Key 指纹用裸 sha256，全系统其余四处为 HMAC-SHA256(secret,key)（归档 account-api-key-rotation.ts:173-175） | gatewaydispatch\accountpreparation.go:290-295 ↔ accountkeystates\probe.go:106-108 等 | 失败路径 Key 级 DB 状态写恒 skip（指纹必 miss）；固定指纹分支误判账户不可用 |
| B-2 | ENGAGED 锁运行链生产装配恒 disabledAccountLocks | cmd\juhe-ai-gateway\compose.go 未赋值 deps.AccountLocks；chain_compose.go:330-333 ↔ 归档 upstream-dispatch.ts:432-589 | ENGAGED→DEAD_CONFIRMED、跨账户重试阻断、deadline 结算（temporary_unavailable+cooldown+失效通知）全部不发生；锁定账户仍被 /v1 选中 |
| B-3 | 调度 Key 轮转策略族缺失（rr/weighted Redis 计数 30d TTL/failover、unverified 剔除） | accountpreparation.go:372-430 ↔ account-api-key-rotation.ts:87-115,183-252 | 恒选「第一把可用 Key」，无轮转语义 |
| B-4 | 跨协议 body/响应转换器整体未移植，但映射许可表放行 9 种转换 | gatewayopenai\driver.go:100-160、mapping.go:147-181 ↔ drivers\_shared\*-bridge.ts 五个转换器 | 配置了 responses→chat / 混合转换的账户把未转换 body 发往重写路径，上游必然 400 |
| B-5 | 裸 POST /v1beta/interactions 不被识别为 Gemini 原生 | gatewaygemini\routehelpers.go:28 ↔ gemini-endpoint-modes.ts:126 | Interactions create 直接失败；包内无测试覆盖 |

## Major

| # | 缺陷 | 位置 | 后果 |
| --- | --- | --- | --- |
| M-1 | J1 outcome→业务 accounts 投影者在 Go 无继任（三代理交叉证实） | jobs accounthealth（outcome 仅写 juhe_jobs；account_health_projection_receipts 零写入）↔ 归档 account-health-jobs-outcome-drain.service.ts + account-health-projection.repository.ts | 冷却成功账户业务行停留 temporary_unavailable，/v1 主旋转（仅 active）不再纳入，账户无法自动回归轮换；激活成功滞留 pending_test；57535d3ad 的恢复尊重 availability schedule 无对应 |
| M-2 | skipAccount 终端跳过作用域错误：Go 中止整组候选周期 | dispatchsingle.go:423-426 + upstreamdispatch.go:738-744 ↔ upstream-dispatch.ts:619 for-of 内仅终止单账户 | 一坏账户带一好账户的分组直接整体失败（guidance/无 URL/Key 池排空均为常规触发） |
| M-3 | Key-model busy/轮换三处偏差：并发槽不重取（死代码）、恒 25ms 同 Key 热循环（Node 1200ms 窗轮换）、previousSelectedApiKeyFingerprint 不更新 | dispatchsingle.go:529-565,753-755 ↔ upstream-dispatch.ts:856-875,1183-1242 | 高峰同 Key 空转至 wall budget 熔断；轮换窗口绕过账户并发上限 |
| M-4 | Anthropic 主链认证头三处不符：anthropic-version 发 "v1"（应 2023-06-01）、OAuth Bearer 分支+beta 合并+CLI 身份头缺失 | chain_driver.go:295-300 ↔ anthropic\driver.ts:150,278-366（探针 probe.go:415-434 已对齐） | 请求头字节级差异；OAuth 账户认证错误 |
| M-5 | Gemini 主链 google_oauth/code_assist 缺失：access_token 放 x-goog-api-key、无 /v1internal 包装 | chain_driver.go:301-304 ↔ gemini\driver.ts:124-176（探针 probe.go:436-446,999-1016 已对齐） | google_oauth 账户认证错误；code_assist 账户不可用 |
| M-6 | Gemini 错误体解析弱化：仅 {error:{}} 信封、数值 code 丢弃、无根对象回退 | gatewayresponse\convert.go:192-243 ↔ gemini-v1beta\error-payload.ts + _shared | 429 数值 code 变空串→失败分类与错误策略输入失真 |
| M-7 | 池隔离谓词三层不一致（dispatch 层 gpt vendor+openai 协议+含 OAuth ↔ 管理面/hydration Node 对齐版） | accountpreparation.go:313-325 ↔ patch_runtime_state.go:307-334、chain_accounts_secret.go:289-311 ↔ 归档 :141-171 | deepseek/glm/gemini/anthropic 池不隔离，行为随入口不同 |
| M-8 | 创建路径链缺失：缓存失效/组统计 dirty/ai_account_limit 断言/dispatch revision 家族+J1 snapshot outbox | accounts\write.go:384-385 ↔ repositories.ts:2342-2491 | 新建账户对探活不可见（input_versions 无行，候选 SQL 硬 JOIN）、组统计不刷新 |
| M-9 | gatewayAccountRuntimeKey 引擎内双格式（复合格式冒称镜像 Node 契约） | upstreamdispatch.go:1177-1185 ↔ gatewaycircuit\runtimekeys.go:27-40（正确实现） | 确认派发接线时跨格式比较永不匹配（地雷）；authorized 绑定身份去重语义丢失 |
| M-10 | 源 fence 结算 in-scope 断裂：outcome 携带 fence 无消费者；jobs 结算写 Redis 而 gateway 协调器是内存驱动 | outbox_drain.go:128-149、worker_health_probe_outbox.go:345-358 ↔ chain_turn_probe_store.go:8-17 | codex 避让探活停留 dispatch-pending 至 TTL，成功结果无法按 fence 清除 |

## Minor（登记，随波次顺带修复或保留披露）

m1 SafetyLimit/并发重试三参数无 env 管道；m2 电路 waiter 上限 env 旋钮丢失（硬编码 5000）；m3 手动测试队列实际串行（Concurrency=512 只喂探针槽，需披露）；m4 fallback busy 检查 nil 守卫（Node 无条件）；m5 大小写变体请求 body 经 map 重序列化（键序/float64 精度）；m6 images 成功证据收紧未对归档逐行核验；m7 outbox 毒丸行致整批 claim 失败；m8 outbox 写失败不结算 fence=unknown；m9 非法 RFC3339 静默按未过期（Node 抛错）；m10 account_health_jobs_input_outbox 删除路径仍写、无消费者无清理（慢增长）；m11 投影 payload probe 事实收窄；m12 key-model ForegroundLimit=100000 vs 2（已声明偏差）。

## 修复波次

- 波1：B-1+B-3+M-7（accountpreparation 选 Key 面）；M-1（J1 投影器移植）；B-4 短期显式拒绝+B-5+M-4/5/6（协议主链）
- 波2：M-2+M-3+M-9（dispatchsingle 决策面）；M-8（创建路径链）；B-2（ENGAGED 锁运行接线）
- 每波附 mock 闭环测试；独立复审后收口。


### 2026-09-07 修复记录（两波清偿）

- 波1：B-1 指纹统一 HMAC-SHA256（accountpreparation.go:312-318，EngineConfig.Secret 由组合根注入同源 cfg.Secret）；B-3 Key 轮转策略族（apikeyrotation.go：failover/rr/weighted + states 过滤 status!=='active' + Redis 计数器端口与 Lua 适配器 chain_apikey_rotation_redis.go，未配 Redis 回落进程内计数）；M-7 池隔离谓词统一 Node 版（api_key+支持集+>1 keys）；M-1 J1 outcome 投影器移植（accounthealth/projection.go：9 分支真值表+fence 真值表逐字对齐、57535d3ad availability schedule 尊重、activation 恢复+dispatch 家族推进+balance 自动探测安排、receipts/cursors 幂等、supervisor 组件可 env 关闭）。
- 波2：M-2 skipAccount 作用域（废除 SkipRestOfCycle，终端路径按归档归类「单账户跳过 vs 整组终止」，坏+好分组不再整体失败）；M-3 busy 轮换三处（并发槽重取激活/1200ms 窗口+三重钳制/游标推进）；M-9 运行态键统一 Node 契约格式（委托 gatewaycircuit.GatewayAccountRuntimeKey）；M-8 创建路径链（ai_account_limit 断言、dispatch revision 家族+J1 snapshot outbox=新建账户探活可见、三路缓存失效、组统计 dirty、balance_query_next_refresh_at 播种）；B-2 ENGAGED 锁运行链接线（chain_account_locks.go SQL 桥按归档逐条实现四操作+租约族+观察栅栏，compose 注入，JUHE_AI_ACCOUNT_LOCKS_DISABLED=true 显式关闭）。
- 协议：B-5 裸 /interactions 正则（含 (?i)，对齐归档）；M-4 anthropic-version 透传/默认 2023-06-01；M-5 anthropic 三分支认证+beta 合并+CLI 身份头、gemini google_oauth/code_assist 运行时（主链对齐探针）；M-6 gemini 错误体硬化（根对象回退/别名/数值 code）。
- B-4 跨协议转换器：短期方案（映射应用点对不支持组合返回预留错误码 account_model_mapping_conversion_unsupported）**未实施**——为避免与转换器移植方向冲突，登记为待用户裁决项：移植 5 个 bridge 转换器（完整方案）或先显式拒绝（止损方案）；未裁决前配置了跨协议映射的账户仍会发出未转换 body。
- Minor 处置：m4 已修（fallback busy 守卫删除）；m1/m2/m3/m5/m6/m7/m8/m9/m10/m11/m12 登记保留。
- 待办（登记）：gatewaypreauth\preflight.go:1493 运行态键副本仍为纯 account.ID（跨包统一需另行授权）；key-model ForegroundLimit=100000 vs Node 2 为已声明偏差。
