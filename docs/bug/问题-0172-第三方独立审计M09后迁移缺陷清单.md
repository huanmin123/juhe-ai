# 问题-0172：第三方独立审计确认的 M09 之后迁移缺陷清单

- 状态：待修复
- 登记时间：2026-09-05
- 登记来源：独立审计（与 PLAN-20260904T071852927Z 互补；该计划范围为 af841ce7..M08，本清单只登记 M09 及之后的迁移单元与宏观完整性矩阵）。
- 证据标准：每项均经两个独立代理分别从 Node 与 Go 源码推导并得出一致结论（双重确证）；证据行号以 2026-09-05 master（366cb0771 前后）为准。
- 已在本轮修复的部分不进本清单，见 `docs/reports/Node-Go迁移独立审计报告-20260905.md`。

## T1（高）accounts 批量编辑副作用链整体缺失【2026-09-05 已修复并独立复审通过（a-g 全 PASS）】

- 已完成（internal/accounts 包内，未提交→随本切片提交）：① proxy 变更时事务内推进 dispatch revision family（root+授权实例，含锁序父→子、指针重查 fail-closed、dedupe 重放幂等+身份断言、outbox 17 列 INSERT，与 Node `account-circuit-control-plane.repository.ts:428-546` 逐段对齐；`batch_effects.go`）；② 提交后 best-effort 链：lookup 失效（新增 nil 安全 `CacheInvalidator` 端口）、分组统计标脏（`group_account_stats_dirty` upsert 与 Node `group-account-stats-cache.repository.ts:45-51` 逐字一致，触发受众 status/schedulable/concurrencyLimit 同 Node）、gateway runtime 失效一次（reason `account_batch_updated`）；失败仅 warn 不影响 200。死代码 groupStatsAffected/gatewayRuntimeAffected 已被消费。
- 移交项：① compose.go 一行接线 `accountsStore.SetCacheInvalidator(...)`（适配器：lookup→账户 lookup 失效、runtime→`inval.TopicGatewayRuntime`，参照 apikeys.BusInvalidator；compose 由装配工作占用）；② 操作日志 targets（authsys sink 扩展，PLAN M09 已登记顺延）；③ 余额快照清理（Go 无该机制，待余额快照切片）。
- 已知非阻塞差异（登记备查）：event_id 用随机 hex 替代 randomUUID；`nullPtrString` 把 `''` 折叠为 NULL 与 Node 分叉（仅脏数据可触达，正常写入路径两侧均拒绝空串代理值）；PG 模式 dual-mode 代码级就绪但包测试为 SQLite，真实 PG 冒烟列入集成验收。
- 原始登记（留存）：Node `account-batch-update.repository.ts:216-241`、`:167-174`；Go `batch.go:448-505` 无副作用、死代码 `batch.go:374-375,1111-1125`。

## T2（高）OAuth 凭据轮换与账户写路径未接运行态失效，且跨进程失效协议不兼容

- Node：`backend/src/storage/oauth-credential-rotation.repository.ts:223-226` 轮换后 `invalidateAccountLookupCache` + `invalidateGatewayRuntimeAfterBusinessWrite('oauth_credentials_rotated')`；`:202-214` 凭据身份变化时事务内推进 circuit dispatch revision family。
- Go：`internal/oauthmgmt/store.go:582-687` RotateCredentials 仅更新凭据列；`compose.go:268` 未注入 inval bus，包内零失效调用；accounts 包同缺口（`compose.go:236`）。
- 协议缺口（架构级）：Go `inval.Bus` 版本值为 int64 递增（`internal/inval` 实现且 `SetSharedStore` 全仓无非测试调用方），Node 发布 JSON `{version:"<millis>-<rand>",reason,publishedAt}`（`backend/src/shared/gateway-cache-invalidation.ts:389-398`）——即使接线，Go 写路径也无法让 Node gateway 失效。
- 影响：混合期 Node gateway 与 Go 管理面并存时，轮换/编辑凭据后旧 token 在 Node 网关缓存中存活到自然过期（上游持续 401）；Go 侧 circuit projector 消费不到 `account_circuit_outbox` 事件。
- 顺延状态：未声明（PLAN.md M17 顺延清单不含此项）。
- 修复方向：先统一 shared version 协议与 Redis 共享版本键，再为 oauthmgmt/accounts 注入 bus 并补 outbox 写入。

## T3（高）pricing 计费引擎与查找闭环缺失，且完成登记失真

- Node：`backend/src/modules/model-pricing/model-pricing.service.ts:158-185` findProviderModelPricing（candidates 循环 + canonical alias 双重回退）；provider-billing.service/policies（tier 精确价、cache split、长上下文乘数）；`model-catalog.service.ts:546` 消费。
- Go：`internal/pricing/` 全包无导出函数（`rg '^func [A-Z]'` 零命中）、`canonicalOpenAIModelAlias`（catalog.go:19-24）零调用、零测试文件；`gatewayusage.PricingCatalog`（gatewayusage/ports.go:151-166）与 `usagewriter.CatalogPricing`（jobs usagewriter/freeze.go:90-97）两端口仅测试 mock，`WithPricingCatalog`/`WithCatalog` 无非测试调用方。
- 登记失真：PLAN.md:22 W2+W3 行 "C03 pricing ✓"、:90 "模型定价目录+计算器"；work-packages.md:118 C03 范围明确含计费。无任何 C03 顺延登记。
- 影响：J-F（F6 usage writer）的 pricing freeze 前置被掏空；`pricing.go` 包文档原声称已覆盖 billing policies（本轮已改写为如实现述，但实现缺口仍在）。
- 修复方向：作为独立切片实现查找闭环 + `BuildCostBreakdown` 等价物，并把 PLAN.md C03 行改回 in-progress 或补顺延登记（登记修正归迁移主线 Agent）。

## T4（中）delegated-api 两处契约缺口【2026-09-05 已修复，余一处生产接线移交】

- 字段缺失：Node `backend/src/modules/delegated-api/delegated-api.routes.ts:638-639` 输出 `supportedModels`/`modelMappings`（数据源 `account-summary.repository.ts:972-973` 恒返回数组，JS spread 对 `[]` 恒展开——即无数据时也输出空数组）。已修复：delegated ai-accounts/PATCH 响应恒携带两字段（空时 `[]`），SQL 排序对齐 Node。提交：见 `docs/reports/Node-Go迁移独立审计报告-20260905.md` §3 追加批次。
- 缓存失效缺失：Node `backend/src/storage/api-key.repository.ts:1256-1271` validation/committed/runtime 失效 + 路由 500 契约。已修复：`internal/delegated/apikeypatch.go` 镜像 `apikeys/patch.go` 判定与 500 文案。
- **余留移交项（一行）**：`cmd/juhe-ai-gateway/compose.go` 的 delegated.Deps 装配未传 `Inval`（生产当前 no-op）。接法：把 compose.go:271 的 `apikeys.BusInvalidator{Bus: bus}` 同值传入 `Inval:` 字段。compose.go 现由 W9 装配工作占用，由装配波接线并补一条"失效被调用"集成断言。

## T5（中）providers 管理读缺口与契约漂移【2026-09-05 已修复并独立复审，复审发现的 2 处分歧修复中】

- 已完成：补齐 6 条读端点（/list、根 GET 扁平 ProviderDefinition[]、/options、/definitions、/models/options、/{code}/models、capabilities）、/{code} 按 code 查找 + 停用 404 三处文案/旁路差异逐一移植、移除 /my-providers 反向注册、写端点路径参数对齐 {code}（400 顺延文案保持）。
- 复审发现并修复中：① /{code}/models 内置目录缺 `status='active' AND catalog_visible=1 AND shutdown_date>today` 过滤（includeInactive=false 时，provider-model-catalog.repository.ts:250-273），曾把过期模型暴露且测试钉死错误行为；② /models/options 的 selectedIds 无 keyword 时不应进 WHERE（provider-model-options.service.ts:131-134，应为排序置顶+并集扩容）。
- 遗留登记：catalogDisplay 及静态能力派生字段（inputModalities/generationParameterCapabilities 等）依赖 C03，Go 暂输出空形状或缺席（包注释已声明）；写端点 Go 挂 admin 包装而 Node 为 session 级开放（C03 落地时调整鉴权，routes.ts:188-241,334-405 无 admin 门）；`requestSystemAccountID` 单值 vs Node 取数组首值（request-context.ts:41-46）边角差异；cmd/juhe-ai-gateway/compose.go:61 挂载矩阵注释仍写 /my-providers（文件由装配工作占用，移交接线波顺带修正）。

## T6（中）W8 后台任务族装配期契约缺口

- 零装配事实：`backend-go/projects/jobs/internal/` 下 accountquality/oauthrefresh/statsagg/statsverify/retention/opsjobs/usagewriter/taskruns/internalapi 九包无非测试 import；`cmd/juhe-ai-jobs/main.go` 仅装配 J1/J2/J3a/F1/F2/gometrics/keymodelrecovery 等；gateway 未挂 `/__aiinternal__/v1/account-test/dispatch`；gateway 的 UsageRecorder port 无 usagewriter 适配器（gateway go.mod 不依赖 jobs 模块）。九包"代码归属"成立、"运行接管"不成立（PLAN.md W9 G20 为装配波，属计划内，但下述子项是装配即触发的缺口）：
  - T6a（中）ingest 排干门控缺失：Node `backend/src/modules/background/account-probe-jobs.ts:46` + `background-jobs.ts:627-644`（队列未排干即跳过本轮，防统计游标越过排队记录）；Go `accountquality/refresh.go:60-65` 无门控、`RefreshDeps` 无钩子，移植注释自称完整移植但静默省略 Node 首步；`statsverify/jobs.go:15-18` 声明"宿主自行门控"但全库无宿主兑现。
  - T6b（低）调度模式分支无冻结清单：同一 job 在 PG/非 PG、redis/非 redis、特性开关下的参数分支（`background-jobs.ts:294/296-300/310-314/325-330/334/366`）在 Go 无代码或清单承载，按单一模板装配必与某一 Node 模式不一致。
  - T6c（低）`dispatchAccountCandidateLimit` 缺失：Node `runtime.ts:770`（默认 globalConcurrencyMax=5000，env 可覆盖）+ `gateway-dispatch-candidate-window.repository.ts` 消费；backend-go 对该配置零匹配。
  - T6d（低）sweep GrantFinalizer 缺省 nil 时过期授权的下游副作用（runtime sync、quota scope bindings、健康任务输入 fanout）不发生（`oauthrefresh/sweep.go:12-16` 已注释，装配时必须注入）。
    - gateway authz 侧下游同步补齐（2026-09-06，随 authz 归还/撤销域切片）：Node grant 写路径末尾的两段下游同步此前未移植——① quota hourly window scope bindings 重同步（`request-quota-hourly-windows.repository.ts:122-144`，写入 `request_quota_hourly_window_scope_bindings` 并在 PG 侧标脏 `juhe_stats.usage_quota_hourly_window_dirty_scopes`；sqlite 臂同 bindings 无标脏）；② 账号健康任务输入 fanout（`account-health-jobs-input-authorization-fanout.repository.ts:51-76`，事务内 reserve 版本并写 `account_health_jobs_input_outbox`，kind='snapshot'、reason='authorization_grant_changed'，白名单 provider/type 与 jobs 侧 `internal/oauthrefresh/healthfanout.go` 同源）。已按调用点补齐：`syncGrantRuntime` 尾部（覆盖 patch `:809`、expire sweep `:957`、revoke `:992`）、return 尾部（`return.repository.ts:587-596`，`Return` 与 `ReturnGroupForGrantee` 两条路径）、create 尾部（`:219/:233/:402/:439`，仅 bindings，create 不 fanout）。实现在 `internal/authz/downstream.go`；表已由 maintenance schema 建齐，无需运行时建表。`syncGrantQuotaScopeBindings` 仅在 PG 执行标脏（与 Node sqlite/PG 分臂一致）；event_id 沿用随机 16 字节 hex（T1 已登记的非阻塞差异）。装配期 GrantFinalizer 注入要求不变。
- 提交溯源断裂（过程性）：J-A/J-B 实际代码落在 `32bb54673`（提交信息 "feat(chat)：添加聊天功能相关路由和接口支持"，180 文件）；`6eff66efe`（自称 W8 全量完成）仅改 PLAN.md 2 行；收敛增量 `662bd85f5` 在其之后。按提交信息无法定位 W8 交付物，revert/cherry-pick 边界不清。

## T7（低）authorized 实例列表投影缺 source 实时替换【2026-09-05 已修复收口：投影随迁移主线 ef3665b74 入库，clientCompatibility 修正为 c9f381fe1】

- 已完成：source 实时替换（provider/protocol/type/concurrency/clientCompatibility/proxy，含 providerName 按 COALESCE join）、bound_group_local_* priority（带回退）与两个 flag（无回退 NULL→false）、accountAuthorizationId/authorizationInstanceSourceAccountId 两字段、canReturnAuthorization=manual 实现（team/NULL→false，owner 恒 false）。独立复审确认 owner 行零回归、分页安全、测试为真实数据漂移断言。
- 修正中（复审唯一 FAIL）：clientCompatibility 归一化 Go 只透传值（list.go 既有 owner 简化被对称扩展到 authorized 分支），Node 是五参 normalizeOpenAIAccountClientCompatibility（`account-client-compatibility.ts:30-49`：非 gpt vendor 恒 openai_standard、gpt+oauth 恒 codex_responses）；旧测试用 claude fixture 钉死了相反行为，正在按 Node 修正。
- 遗留登记（复审范围外观察，先于本切片存在）：① 列表过滤/排序仍是实例冻结列，Node 为 source 感知 COALESCE（:617,621,639,676-694）——授权后改 source provider 时按 provider 过滤/排序与显示值不一致；② Node 状态种子 authorized 分支混合 source 健康字段（:700-737），Go 仍按实例列算；③ groupBindStatus 缺 authorization_unavailable 分支（Node :739-745）；④ admin /accounts 视角 Go 全渲染 owner（authorized.go:36）而 Node 按行 join 判定；⑤ authorizedReadableIDs 失败静默降级 owner 视图（authorized.go:47-50）；⑥ owner 行带 bound group 时 Go 输出 bindingSystemAccountId 而 Node 仅 authorized 输出（Node :573）。
- 风险（继承性、未验证）：PG 下 `proxy_profiles.enabled` 为 boolean，Go 扫进 `sql.NullInt64`（新旧两列同模式）在 pgx stdlib 上大概率 Scan 报错——本包测试 SQLite-only，双模式声称需真实 PG 冒烟后才能成立；建议列入 PG 集成验收清单。

## T8（低）导入路径残留边界

- "创建即投递"健康检查缺失（已声明顺延）但有残留：jobs accounthealth 周期扫描仅覆盖已绑定分组（`direct_input_reader.go:397-441` 的 `binding.group_id IS NOT NULL` 硬条件）且 provider/type/endpoint_mode 受白名单限制；未绑定分组的导入 `pending_test` 账户在 Go 中不会被自动检查（Node dispatch 不依赖分组）。
- `TemporaryUnavailableContinuousProbeEnabled` 整条创建路径缺失：Go `CreateInput`（`internal/accounts/write.go:62-85`）无该字段（非仅导入），显式 `false` 落库为默认 `true`（Node `account-import-account-payload.ts:18,52`、`repositories.ts:1995,2288`、`business-schema.ts:367` DEFAULT 1）。
- base_url 仅结构校验、无 SSRF 拦截：Go `import_source.go:897-911` 已声明归属 platform request layer，但 Node 在导入适配层即拒绝内网地址（`account-import-source-adapters.ts:9,578-586` + `shared/upstream-url-policy.ts:73`），Go 会接受并落库——行为差异已声明但切换前需补齐或明确豁免。

## T9（低）M14 日志读挂载前的路由遮蔽隐患

- `internal/logreads/audit_reads.go:421-430` 注册 `GET /audit-logs/{id}`、`GET /runtime-logs/{id}` 单段通配；未注册 Node 仍保留在 Node 侧的 `search-hot`（audit-logs.routes.ts:64）与 `grep-options/grep/grep-detail`（runtime-logs.routes.ts:40/48/57）。kernel 为 Go 1.22 ServeMux，已注册模式必胜 bridge——未来挂载 ReadsDeps 的同一刻起，这些字面段会被 `{id}` 吞掉返回 404，bridge 永远收不到。
- 修复方向：挂载 ReadsDeps 的变更必须同时迁入 search-hot/grep 族或注册字面转发，并把该前置条件写进挂载检查单。

## 已核实为真、暂不登记为缺陷的已声明顺延（抽样）

- settings `/global` 与 `/sections/:sectionKey` 留在 Node（`internal/settings/store.go:9-10`）；注意 `docs/migration/W5-管理端全局品牌设置读取记录.md` 描述的旧架构 Go 实现已不存在，该文档未标注过期，会误导读者。
- gemini 富化顺延把 Node 的"缺 project_id 显式报错"变为 Go 的"静默建户"（`oauthmgmt/gemini.go:509-535` + PLAN.md:89 登记）；切换前建议补齐探测或对齐报错。
- OpenAI OAuth 可恢复 error 账户刷新后不复活（仅 OpenAI 路径，`oauthmgmt/store.go:579-581` + PLAN.md:89）。
- 三供应商保活从 dispatch 惰性刷新变为常驻后台 job（`oauthrefresh/keepalive.go`），装配时需新定 interval（Node 无对应参数）。
- 批量编辑 5 个凭据配置字段 fail-closed 400（`batch.go:719-733`，PLAN.md M09 登记）；批量 supportedModels/modelMappings 目录/协议校验（`crypto.go:11-12` 包级声明）。
- M17a 提交说明（e3308eb38 "ttlSeconds 严格整数校验"）与实际内容（system-accounts PATCH 可空字段语义）不符，实际 ttlSeconds 校验已在 19e9c41da 落地——过程性记录。
- `deploy/owner-manifest.json` routeOwners 仍全为 "node"、goVersion `0.1.0-w0`，滞后于代码事实（/auth、/system-accounts 已 Go 原生），作部署参考时需注意。

## 第三轮常驻审查 7 项偏差处置（2026-09-04 清尾完成）

以下 7 项为第三轮常驻审查在审查波发现的偏差，逐项处置、证据与回归如下（全部通过 `go build ./...`、`go vet ./internal/...`、`go test ./internal/{authz,accounts,inval,kernel,pricing}/... ./cmd/juhe-ai-gateway/... -count=1 -race`，acceptance 绿）。

1. **authz create TTL（已修+补测试）**：`authorizations.create` 的 mutationGuard 归档语义为 `succeededTtlMs: 0`（归档 `authorizations.routes.ts:219`）——成功后同指纹立即可重试、失败走默认 10s 窗口。主代理已把 `SucceededTTL` 改为 `kernel.DedupNoRetention`（`FailedTTL` 保持 0=默认）。新增 `internal/authz/create_ttl_test.go` 双测试：冻结时钟成功后立即重试不 409；失败后立即重试 409 且命中 `请求刚刚失败，请稍后重试`、越过 10s 窗口后 claim 重开。该测试文件逐字段镜像 `routes.go` 内联的 MutationGuardOptions，两侧必须同步修改（文件头已声明）。
2. **accounts 删除授权链终态（注释修正+终态保留测试）**：审查原判断「归档 SQLite 逐 grant 路径置 status='returned'」与归档证据不符——`revokeAccountAuthorizationsForDeletedResource` 的 SQLite 臂逐 grant 走 `revokeResourceAuthorizationGrant`（`resource-authorization-write-state.repository.ts:726`）终态同为 'revoked'；置 'returned' 的 `revokeAuthorizationInstanceForDeletedAccount`（`account-delete-cleanup.repository.ts:356`）在归档全仓无调用方。真实方言差异在到达方式：SQLite 为 manual-source 作用域 + 条件式终态刷新 + 逐 grant 配额重同步（ended_reason `authorization_revoked`），PG 臂为批量 'revoked' + `account_deleted` reason + 配额绑定整体删除。处置：Go 维持镜像 PG 异步臂（`internal/accounts/delete.go`），修正原注释中「lands identically in both dialects」的错误表述为上述实证差异；新增 `delete_authorization_test.go` 钉住 PG 批量臂的边界保留规则（returned grant/authorization 不动、COALESCE 保留 revoked_by/revoked_at/ended_at、superseded source 翻转、配额绑定按资源全量删除）。SQLite 逐 grant 运行态同步域仍归资源授权独立切片。
3. **inval Bus 节流丢弃→无损（已修）**：原实现（`inval.go` Invalidate 前沿 1s 节流直接 return）会吞掉 1s 窗口内第二次同 topic 失效；归档语义是本地 invalidator 每个 notify 立即执行、1s 节流只在 Redis 同步协调器且为尾沿合并（`gateway-cache-invalidation.ts:86-134`），发布路径每次都发布（`:383-399`）。处置：删除 Bus 的 drop-throttle（`throttle`/`coalesce` 字段移除），Invalidate 每次都 bump+发布+通知；包注释与 Invalidate 注释改述无损语义。测试改写 `TestInvalidateBumpsVersionAndNotifies`、删除 `TestThrottleCoalescesWithinOneSecond`、新增 `TestTwoInvalidationsInsideOneSecondBothTakeEffect`（1s 窗口两次失效：两次通知、版本 2、共享发布 2）。
4. **kernel ExtractClientIP（已修）**：对照归档 `request-context.ts:456/716`（`normalizeClientIp` `isIP===4` → IPv6 语义为 undefined）与 `internal/gatewaypreauth` 的正确实现，重写 `kernel/ctx.go` 的 `normalizeClientIP` 为 IPv4-only（trim、`[..]`、dotted-quad 去端口、`::ffff:` 前缀剥离）；`ExtractClientIP` 在 XFF 条目数少于受信代理数（`len(parts) < trustProxyCount`）时不再钳位到客户端可控的首条目，回落 socket 地址（防伪造短链）。新增 `TestExtractClientIPDialectAlignment` 12 例（含 `len==count` 边界仍取 XFF、IPv6 socket/XFF 归空回落、mapped/带端口归一化）；排查 `authsys`（operationlog/captcha/login 经 `kernel.Context(r).ClientIP` 消费）无 IP 值断言测试需更新，`TestTraceAndClientIPContext` 语义保持不变。
5. **dedupe 客户端中断语义（已修）**：归档 `mutation-guard.middleware.ts:70-74` 的 `res.once('close')` 臂——响应未写完（`writableEnded === false`）连接即断 → `complete('failed')`；Go 原实现按状态码定性，中断可能记 succeeded。处置：`MutationGuardMiddleware` 在 claim 后挂 request context Done 侦测（goroutine+`serveDone` 关停，exactly-once complete 由互斥保护）；handler 返回后若 context 已死且无任何响应字节写出则定性 failed，否则按状态码定性；已完成响应之后的迟到断开不重写结果（Node finish 先于 close 序）。新增 `TestMutationGuardDisconnectSettlesFailed`（中断→failed 条目+默认 10s 窗口内重试 409）与 `TestMutationGuardCompletedResponseOutranksLateDisconnect`。
6. **inval Subscribe 真实退订（已修）**：原 unsubscribe 为 no-op（注释自认 func 不可比较）。处置：handler 注册改为 `*subscription` 身份切片，unsubscribe 精确移除本次注册（对照归档 Set+delete，`gateway-cache-invalidation.ts:146-174`），重复退订为 no-op。新增 `TestSubscribeUnsubscribeRemovesExactlyOneHandler`。
7. **pricing 重名+shutdown 循环微差（已修）**：归档 `findProviderModelPricing`（`model-pricing.service.ts:169-181`）每层先 `models.find(byName)` 取首个同名行、再对该行做 shutdown 判定——首行已 shutdown 即放弃该层，不会重扫后续同名行；Go 原循环把 shutdown 判定并入遍历条件会跳到后续同名活行。处置：`lookup.go` 抽出 `firstNamedRawRow` 并在三层（exact/candidates/canonical alias）统一为先取名再判 shutdown。新增 golden `TestFindRawProviderModelPricingDuplicateNameShutdownGivesUpLayer`（合成重名行钉住三层语义）；既有 `TestFindProviderModelPricingShutdownAsOf`（真实数据 gpt-4.1-nano）在修正后语义下仍按候选层回落 gpt-4.1，保持通过。
