# 问题-0172：第三方独立审计确认的 M09 之后迁移缺陷清单

- 状态：待修复
- 登记时间：2026-09-05
- 登记来源：独立审计（与 PLAN-20260904T071852927Z 互补；该计划范围为 af841ce7..M08，本清单只登记 M09 及之后的迁移单元与宏观完整性矩阵）。
- 证据标准：每项均经两个独立代理分别从 Node 与 Go 源码推导并得出一致结论（双重确证）；证据行号以 2026-09-05 master（366cb0771 前后）为准。
- 已在本轮修复的部分不进本清单，见 `docs/reports/Node-Go迁移独立审计报告-20260905.md`。

## T1（高）accounts 批量编辑副作用链整体缺失

- Node：`backend/src/storage/account-batch-update.repository.ts:216-241` 提交后逐账户 `invalidateAccountLookupCache`、`refreshGroupAccountStatsAfterWriteAsync`、`invalidateGatewayRuntimeAfterBusinessWrite`；`:167-174` 事务内 `advanceAccountCircuitDispatchRevisionFamilyInTransaction`（`dispatchRevisionChanged = proxyChanged`，service :391）；`account-batch-edit.service.ts:97,398-412` 余额快照清理；`account-batch-edit.routes.ts:85-90` 操作日志逐账户 targets。
- Go：`internal/accounts/batch.go:448-505` 提交前后无任何失效/统计/revision/快照调用；`batch.go:374-375,1111-1125` 的 `groupStatsAffected`/`gatewayRuntimeAffected` 为无消费死代码；`m09_routes.go:124-125` 注释自认日志无 targets。
- 影响：批量改 proxy 后网关 circuit 派发 revision 不推进（`gatewaycircuit` Lua 以 `dispatch_revision` 做 CAS），分组统计延迟漂移（statsverify 有周期对账兜底），余额快照无清理机制（Go 侧机制未迁移）。
- 顺延状态：仅 targets（PLAN.md M09）与 circuit outbox（PLAN.md M08）有登记；lookup 失效、分组统计标脏、余额快照清理无声明。
- 修复方向：接线 K5 inval bus + groups 包既有的 `group_account_stats_dirty` 标脏 + circuit dispatch revision 推进；跨进程失效需先解决 T2 的协议问题。

## T2（高）OAuth 凭据轮换与账户写路径未接运行态失效，且跨进程失效协议不兼容

- Node：`backend/src/storage/oauth-credential-rotation.repository.ts:223-226` 轮换后 `invalidateAccountLookupCache` + `invalidateGatewayRuntimeAfterBusinessWrite('oauth_credentials_rotated')`；`:202-214` 凭据身份变化时事务内推进 circuit dispatch revision family。
- Go：`internal/oauthmgmt/store.go:582-687` RotateCredentials 仅更新凭据列；`compose.go:268` 未注入 inval bus，包内零失效调用；accounts 包同缺口（`compose.go:236`）。
- 协议缺口（架构级）：Go `inval.Bus` 版本值为 int64 递增（`internal/inval` 实现且 `SetSharedStore` 全仓无非测试调用方），Node 发布 JSON `{version:"<millis>-<rand>",reason,publishedAt}`（`backend/src/shared/gateway-cache-invalidation.ts:389-398`）——即使接线，Go 写路径也无法让 Node gateway 失效。
- 影响：混合期 Node gateway 与 Go 管理面并存时，轮换/编辑凭据后旧 token 在 Node 网关缓存中存活到自然过期（上游持续 401）；Go 侧 circuit projector 消费不到 `account_circuit_outbox` 事件。
- 顺延状态：未声明（PLAN.md M17 顺延清单不含此项）。
- 修复方向：先统一 shared version 协议与 Redis 共享版本键，再为 oauthmgmt/accounts 注入 bus 并补 outbox 写入。

## T3（高）pricing 计费引擎与查找闭环缺失，且完成登记失真

- Node：`backend/src/modules/model-pricing/model-pricing.service.ts:158-185` findProviderModelPricing（candidates 循环 + canonical alias 双重回退）；provider-billing.service/policies（tier 精确价、cache split、长上下文乘数）；`model-catalog.service.ts:546` 消费。
- Go：`internal/pricing/` 全包无导出函数（`rg '^func [A-Z]'` 零命中）、`canonicalOpenAIModelAlias`（catalog.go:19-24）零调用、零测试文件；`gatewayusage.PricingCatalog`（gatewayusage/ports.go:151-166）与 `usagewriter.CatalogPricing`（jobs usagewriter/freeze.go:90-97）两端口仅测试 mock，`WithPricingCatalog`/`WithCatalog` 无非测试调用方。
- 登记失真：PLAN.md:22 W2+W3 行 "C03 pricing ✓"、:90 "模型定价目录+计算器"；work-packages.md:118 C03 范围明确含计费。无任何 C03 顺延登记。
- 影响：J-F（F6 usage writer）的 pricing freeze 前置被掏空；`pricing.go` 包文档原声称已覆盖 billing policies（本轮已改写为如实现述，但实现缺口仍在）。
- 修复方向：作为独立切片实现查找闭环 + `BuildCostBreakdown` 等价物，并把 PLAN.md C03 行改回 in-progress 或补顺延登记（登记修正归迁移主线 Agent）。

## T4（中）delegated-api 两处契约缺口【2026-09-05 已修复，余一处生产接线移交】

- 字段缺失：Node `backend/src/modules/delegated-api/delegated-api.routes.ts:638-639` 输出 `supportedModels`/`modelMappings`（数据源 `account-summary.repository.ts:972-973` 恒返回数组，JS spread 对 `[]` 恒展开——即无数据时也输出空数组）。已修复：delegated ai-accounts/PATCH 响应恒携带两字段（空时 `[]`），SQL 排序对齐 Node。提交：见 `docs/reports/Node-Go迁移独立审计报告-20260905.md` §3 追加批次。
- 缓存失效缺失：Node `backend/src/storage/api-key.repository.ts:1256-1271` validation/committed/runtime 失效 + 路由 500 契约。已修复：`internal/delegated/apikeypatch.go` 镜像 `apikeys/patch.go` 判定与 500 文案。
- **余留移交项（一行）**：`cmd/juhe-ai-gateway/compose.go` 的 delegated.Deps 装配未传 `Inval`（生产当前 no-op）。接法：把 compose.go:271 的 `apikeys.BusInvalidator{Bus: bus}` 同值传入 `Inval:` 字段。compose.go 现由 W9 装配工作占用，由装配波接线并补一条"失效被调用"集成断言。

## T5（中）providers 管理读缺口与契约漂移

- 完全缺失 6 条读端点：`/list`、`/options`、`/definitions`、`/models/options`、`/:code/models`、`/:code/models/:modelId/capabilities`（Node `providers.routes.ts:52,74,83,94,141,161`；前端依赖 `frontend/src/api/domains/providers.ts:26-45`）。
- 契约不等价 2 条：Node 根 GET 返回扁平 `ProviderDefinition[]`（含 protocolProfiles），Go 返回分页信封；Node `/:code` 按 code 返回单个定义，Go `{id}` 返回 Detail，键集不同。切流即破坏前端。
- 反向多注册：Go 注册了 Node 不存在的 `GET /my-providers`、`/my-providers/{id}`（Node 后端与前端 rg 双零命中）；写端点路径参数 `{id}` 与 Node `:code` 不一致（C03 解除顺延时将引入第二重差异）。
- 停用语义：Node :131-134 非管理请求访问停用供应商 404"供应商不存在或已停用"；Go `providers/store.go:345-399` 无 enabled 过滤。
- 顺延状态：写端点已登记（PLAN.md:88/95）；读端点仅范围式模糊登记，响应契约漂移完全未声明。
- 修复方向：读端点按 Node 契约补齐（含停用语义），`/my-providers` 决定移除或登记为有意加法。

## T6（中）W8 后台任务族装配期契约缺口

- 零装配事实：`backend-go/projects/jobs/internal/` 下 accountquality/oauthrefresh/statsagg/statsverify/retention/opsjobs/usagewriter/taskruns/internalapi 九包无非测试 import；`cmd/juhe-ai-jobs/main.go` 仅装配 J1/J2/J3a/F1/F2/gometrics/keymodelrecovery 等；gateway 未挂 `/__aiinternal__/v1/account-test/dispatch`；gateway 的 UsageRecorder port 无 usagewriter 适配器（gateway go.mod 不依赖 jobs 模块）。九包"代码归属"成立、"运行接管"不成立（PLAN.md W9 G20 为装配波，属计划内，但下述子项是装配即触发的缺口）：
  - T6a（中）ingest 排干门控缺失：Node `backend/src/modules/background/account-probe-jobs.ts:46` + `background-jobs.ts:627-644`（队列未排干即跳过本轮，防统计游标越过排队记录）；Go `accountquality/refresh.go:60-65` 无门控、`RefreshDeps` 无钩子，移植注释自称完整移植但静默省略 Node 首步；`statsverify/jobs.go:15-18` 声明"宿主自行门控"但全库无宿主兑现。
  - T6b（低）调度模式分支无冻结清单：同一 job 在 PG/非 PG、redis/非 redis、特性开关下的参数分支（`background-jobs.ts:294/296-300/310-314/325-330/334/366`）在 Go 无代码或清单承载，按单一模板装配必与某一 Node 模式不一致。
  - T6c（低）`dispatchAccountCandidateLimit` 缺失：Node `runtime.ts:770`（默认 globalConcurrencyMax=5000，env 可覆盖）+ `gateway-dispatch-candidate-window.repository.ts` 消费；backend-go 对该配置零匹配。
  - T6d（低）sweep GrantFinalizer 缺省 nil 时过期授权的下游副作用（runtime sync、quota scope bindings、健康任务输入 fanout）不发生（`oauthrefresh/sweep.go:12-16` 已注释，装配时必须注入）。
- 提交溯源断裂（过程性）：J-A/J-B 实际代码落在 `32bb54673`（提交信息 "feat(chat)：添加聊天功能相关路由和接口支持"，180 文件）；`6eff66efe`（自称 W8 全量完成）仅改 PLAN.md 2 行；收敛增量 `662bd85f5` 在其之后。按提交信息无法定位 W8 交付物，revert/cherry-pick 边界不清。

## T7（低）authorized 实例列表投影缺 source 实时替换

- Node：`backend/src/storage/account-management-list.repository.ts:500-573` authorized 行用 `source_*` join 字段实时替换 provider/protocol/type/concurrency/proxy 等，`:547-555` priority 用 `bound_group_local_*`，`:568-569` 输出 `accountAuthorizationId`/`authorizationInstanceSourceAccountId`；`:747-770` `canReturnAuthorization: sourceType==='manual'`。
- Go：`internal/accounts/list.go:604-679` 用实例自身冻结列，无 source_accounts join；`authorized.go:74-84` `canReturnAuthorization` 恒 false。
- 顺延状态：canReturnAuthorization 已声明顺延（authorized.go 注释）；source 实时替换与两个缺失字段未声明。
- 影响：源账户变更后 Go 列表对 authorized 行显示授权时快照值；手动来源授权在列表无"可归还"标识（归还走 /my-authorizations 不受影响）。

## T8（低）导入路径残留边界

- "创建即投递"健康检查缺失（已声明顺延）但有残留：jobs accounthealth 周期扫描仅覆盖已绑定分组（`direct_input_reader.go:397-441` 的 `binding.group_id IS NOT NULL` 硬条件）且 provider/type/endpoint_mode 受白名单限制；未绑定分组的导入 `pending_test` 账户在 Go 中不会被自动检查（Node dispatch 不依赖分组）。
- `TemporaryUnavailableContinuousProbeEnabled` 整条创建路径缺失：Go `CreateInput`（`internal/accounts/write.go:62-85`）无该字段（非仅导入），显式 `false` 落库为默认 `true`（Node `account-import-account-payload.ts:18,52`、`repositories.ts:1995,2288`、`business-schema.ts:367` DEFAULT 1）。
- base_url 仅结构校验、无 SSRF 拦截：Go `import_source.go:897-911` 已声明归属 platform request layer，但 Node 在导入适配层即拒绝内网地址（`account-import-source-adapters.ts:9,578-586` + `shared/upstream-url-policy.ts:73`），Go 会接受并落库——行为差异已声明但切换前需补齐或明确豁免。

## T9（低）M14 日志读挂载前的路由遮蔽隐患

- `internal/logreads/audit_reads.go:421-430` 注册 `GET /audit-logs/{id}`、`GET /runtime-logs/{id}` 单段通配；未注册 Node 仍保留在 Node 侧的 `search-hot`（audit-logs.routes.ts:64）与 `grep-options/grep/grep-detail`（runtime-logs.routes.ts:40/48/57）。kernel 为 Go 1.22 ServeMux，已注册模式必胜 bridge——未来挂载 ReadsDeps 的同一刻起，这些字面段会被 `{id}` 吞掉返回 404，bridge 永远收不到。
- 修复方向：挂载 ReadsDeps 的变更必须同时迁入 search-hot/grep 族或注册字面转发，并把该前置条件写进挂载检查单。

## 已核实为真、暂不登记为缺陷的已声明顺延（抽样）

- settings `/global` 与 `/sections/:sectionKey` 留在 Node（`internal/settings/store.go:9-10`）；注意 `docs/migration/W5-管理端全局品牌设置读取记录.md` 描述的旧架构 Go 实现已不存在，该文档未标注过期，会误导读者。
- gemini 富化顺延把 Node 的"缺 project_id 显式报错"变为 Go 的"静默建户"（`oauthmgmt/gemini.go:509-535` + PLAN.md:89 登记）；切换前建议补齐探测或对齐报错。
- OpenAI OAuth 可恢复 error 账户刷新后不复活（仅 OpenAI 路径，`oauthmgmt/store.go:579-581` + PLAN.md:89）。
- 三供应商保活从 dispatch 惰性刷新变为常驻后台 job（`oauthrefresh/keepalive.go`），装配时需新定 interval（Node 无对应参数）。
- 批量编辑 5 个凭据配置字段 fail-closed 400（`batch.go:719-733`，PLAN.md M09 登记）；批量 supportedModels/modelMappings 目录/协议校验（`crypto.go:11-12` 包级声明）。
- M17a 提交说明（e3308eb38 "ttlSeconds 严格整数校验"）与实际内容（system-accounts PATCH 可空字段语义）不符，实际 ttlSeconds 校验已在 19e9c41da 落地——过程性记录。
- `deploy/owner-manifest.json` routeOwners 仍全为 "node"、goVersion `0.1.0-w0`，滞后于代码事实（/auth、/system-accounts 已 Go 原生），作部署参考时需注意。
