# BUG-0162 M08 账户迁移遗漏端点与运行态副作用

## 基本信息

- 编号：BUG-0162
- 状态：待修复
- 严重程度：P1
- 发现时间：2026-09-04
- 发现方式：自查（已提交 Git 历史审计）
- 模块：后端 / 网关 / 前端 / 存储 / 迁移
- 关联计划：`docs/migration/Node全量清零迁移总计划-20260904.md`
- 关联 bug：BUG-0152
- 责任人：待定

## 问题概述

- 现象：M08 Go accounts 只实现核心列表、详情、创建、编辑、锁定族、标签和删除；Node/前端仍有高级详情、clone、runtime reset、批量、导入导出、余额/测试、模型目录、分组绑定、OAuth 重新授权等能力。
- 期望：账户功能完整迁移后，所有端点、状态机、凭据策略、删除清理、缓存/运行态和初始健康探针与 Node 结果一致，才允许归档 Node。
- 实际：
  1. Go 路由缺少上述大量端点，owner manifest 仍为 `missing`，Node `/accounts`、`/my-accounts` 仍挂载。
  2. Go 删除未撤销账户作为资源拥有者的 authorization/grant/source，未写 health tombstone outbox，也未刷新 group stats、lookup/authorization/runtime/cache。
  3. Go ENGAGED 锁配置更新始终复用旧 deadline 并清空 retry lease；Node 会按新的 death timeout 重新计算 deadline，无配置变化时保留 lease。
  4. Go create/patch 没有 Node 的凭据归一化、deprecated 字段清理、provider policy 校验和完整运行态副作用；create 也没有 dispatch 初始 health check。
  5. Go edit-basic 对凭据主动 mask，Node sanitizer 不 mask，返回结果存在产品可观察偏差（需产品确认是否有意安全加固）。
- 影响范围：账户管理页面功能缺失；删除后授权和缓存可能继续生效；锁定恢复时间/租约漂移；新建或修改账户的健康状态与网关运行态不更新。

## 根因与证据

- Go 路由：`backend-go/projects/gateway/internal/accounts/routes.go`。
- Node 路由/前端：`backend/src/modules/accounts/accounts.routes.ts`、`frontend/src/api/domains/accounts.ts`。
- 删除清理：Node `backend/src/storage/account-delete-cleanup.repository.ts`；Go `backend-go/projects/gateway/internal/accounts/delete.go`。
- 锁状态机：Node `backend/src/storage/account-lock.repository.ts`；Go `backend-go/projects/gateway/internal/accounts/lock.go`。
- 凭据与副作用：Node `account-credentials-normalization.ts`、`account-management-patch.repository.ts`、`accounts.routes.ts`；Go `write.go`、`patch.go`、`routes.go`。

## 修复与验证

- 修改点：以 Node/前端完整端点矩阵为基线拆分后续切片；先补删除授权撤销、health tombstone、缓存/运行态失效、锁状态机和凭据 golden，再补余下端点与初始探针；统一唯一 owner 后才归档 Node。
- 当前验证：Go SQLite 包内测试不能证明 PostgreSQL、Redis、worker、runtime 和前端行为；未执行真实 gateway 或浏览器验证。
- 结论：M08 是核心 CRUD 实现，不是完整账户功能迁移。

## 已核实子项

### 2026-09-05：删除资源拥有者账户未撤销授权链

- Node 基线：`backend/src/storage/account-delete-cleanup.repository.ts` 的 `deleteAccountWithRelatedCleanup` 在软删除账户前，于同一事务调用 `revokeAccountAuthorizationsForDeletedResource`（SQLite 行 145-148）；该函数筛选 `resource_type='account'` 且状态不是 `revoked/returned` 的 `resource_authorization_grants`，逐一执行撤销（行 479-492）。PostgreSQL 异步路径同样先查询该账户的 `resource_authorizations`，删除授权 grant 产生的 quota scope 绑定，并把 grant、source、authorization 状态更新为 revoked/returned（行 420-475）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/delete.go` 的 `Store.Delete` 仅查询账户与授权实例标识（行 35-49），随后更新 `accounts` 状态为 disabled、写 `deleted_at/deleted_by`，删除标签和名称搜索项后提交事务（行 76-101）；没有查询或更新 `resource_authorizations`、`resource_authorization_grants`、`resource_authorization_sources`，也没有清理授权 grant 产生的 quota scope 绑定。
- 结果偏差：删除一个作为资源拥有者、且仍有 active/paused 等有效授权的账户时，Go 返回 204 并将账户标记删除，但授权及其来源/grant 仍保持可用状态。授权读取、配额和被授权账户运行时仍可能把已删除账户当作有效来源；后续物理清理还会面对悬挂的授权链。Node 则在删除事务内先撤销这些关系，再提交并触发对应运行态失效。
- 结论：这是已提交 M08 的确定功能偏差，属于删除状态机和跨表副作用缺失；当前仅记录，未修改 Go 代码。

### 2026-09-05：删除账户未写入健康探针 tombstone outbox

- Node 基线：`backend/src/storage/account-delete-cleanup.repository.ts` 的 `logicallyDeleteAccounts` 在更新账户为 `disabled`、`schedulable=0` 后，按删除批次读取 `config_revision` 和 `dispatch_revision`，仅针对支持健康检查的 provider/type 调用 `reserveAndEnqueueAccountHealthJobsInputInTransaction`，写入 `kind='tombstone'`、`reason='account_deleted'` 的 health input outbox（SQLite 行 259-275）；PostgreSQL 路径同样在事务中执行异步 outbox 写入（行 307-325）。这样删除提交后，健康投影能消费带版本栅栏的 tombstone，清除该账户运行态。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/delete.go` 的 `Store.Delete` 只更新 `accounts`、删除标签和名称搜索项，然后直接提交事务（行 76-101）；文件及 M08 账户包没有查询 `config_revision/dispatch_revision`，也没有调用 `account_health_jobs_input_outbox` 的 reserve/enqueue 入口。
- 结果偏差：已存在健康状态/运行态的账户被 Go 删除后，health worker 不会收到删除 tombstone，旧的 `account_health_current_state`、抑制/排程状态或下游运行态可能继续保留，直到其他路径偶然刷新；Node 删除事务保证该清理事件与软删除原子提交。该差异独立于授权撤销，即使没有任何授权关系也会发生。
- 结论：这是已提交 M08 的确定运行态副作用遗漏，当前仅记录，未修改 Go 代码。

### 2026-09-05：删除账户未触发缓存与网关运行态失效

- Node 基线：Node SQLite 删除事务提交后，只要确实删除了账户，就依次刷新全部分组账户统计、失效每个账户 lookup cache、失效 group-account-id cache、清空 resource-authorization lookup caches，并广播 `account_deleted` 的 gateway runtime 与 authorization quota 失效（`backend/src/storage/account-delete-cleanup.repository.ts` 行 149-157）；PostgreSQL 异步路径同样在事务提交后执行这些失效。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 的 `remove` 在 `Store.Delete` 成功后只写操作日志并返回 204（行 496-543）；历史 `Store` 仅持有数据库、密钥、时钟和 ID 工厂，没有 cache invalidator、runtime invalidation bus、统计 dirty marker 或授权 lookup 清理依赖，也没有删除后的等价调用。
- 结果偏差：Go 删除后，已有账户 lookup、分组账户 ID/统计、资源授权读取及网关运行时/授权配额缓存可以继续命中删除前状态；即使数据库软删除已经提交，请求仍可能短时间选择已删除账户或旧授权来源。Node 将这些失效安排在提交后，保证读路径最终切换到删除后的状态。
- 结论：这是独立于“数据库是否删除成功”的确定副作用缺失，当前仅记录，未修改 Go 代码。

### 2026-09-05：ENGAGED 锁配置更新未按 Node 规则重算 deadline/保留 lease

- Node 基线：`backend/src/storage/account-lock.repository.ts` 的 `setAccountLockAsync` 在保留 `ENGAGED` 事件时，若 death timeout 改变，会以原 `incidentStartedAt + timeout` 重算 `deadlineAt`（行 174-181）；只有 retry/death 配置实际变化时才清除 `nextRetryAtMs`、`leaseId`、`leaseUntilMs`，两项都未变化则完整保留已有 retry lease（行 212-214）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/lock.go` 的 `SetLock` 只把已有 `incidentStart/deadlineAt` 原样带回，不依据新 timeout 重算 deadline（行 165-190）；其 upsert 的 `ON CONFLICT` 更新无条件将 `next_retry_at_ms`、`lease_id`、`lease_until_ms` 设为 `NULL`（行 192-210），即使 lock-config 只是重复提交相同配置也会清掉当前重试租约。
- 结果偏差：处于 `ENGAGED` 的账户修改 death timeout 后，Go 仍沿用旧死亡截止时间，可能提前或延后锁死；重复提交未变化的配置也会释放正在执行/等待中的 retry lease，改变失败重试调度。Node 对两种场景分别保持时间语义和租约语义。
- 结论：这是已提交 M08 锁状态机的确定行为偏差，当前仅记录，未修改 Go 代码。

### 2026-09-05：API Key 凭据写入未执行 Node 的池归一化

- Node 基线：`backend/src/storage/account-credentials-normalization.ts` 的 API Key 写入先要求非空 `base_url`，再把 `api_key/api_keys` 去空白、去重并限制最多 10 个；单 Key 只保存规范化后的 `api_key`，多 Key 才保存规范化池、策略和权重（行 152-174、184-202）。因此例如 `api_keys: ['sk-a', ' sk-a ']` 最终只有一个有效 Key，缺少 `base_url` 或超过 10 个 Key 会直接失败。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/write.go` 的 `createInTx` 明确将 `input.Credentials` “kept as provided”，仅用 `requiredAccountCredentialSource` 选出第一个字符串后直接 `EncryptJSON`（行 360-375）；该函数只检查是否存在可用凭据，不校验 `base_url`、池大小、去空白/去重、策略或权重。历史 `patch.go` 同样将当前凭据与输入键浅合并后直接重新加密（行 377-403），不经过归一化器。
- 结果偏差：同一个创建/编辑请求在 Node 可能被拒绝或落成单 Key/规范化池，在 Go 却成功保存重复、带空白或超限的原始池；后续 API Key 轮换按池条目和策略运行时会得到不同的候选数量与选择序列，缺失 `base_url` 也不会在写入阶段被拦截。
- 结论：这是已提交 M08 凭据写入契约的确定偏差，当前仅记录，未修改 Go 代码。

### 2026-09-05：凭据 `base_url` 未执行 Node 的上游地址安全策略

- Node 基线：`backend/src/storage/account-credentials-normalization.ts` 在 API Key、OAuth、Google OAuth 三类凭据写入时都调用 `assertSafeUpstreamBaseUrl`（API Key 行 156-158，OAuth 行 236-244，Google OAuth 行 286-294）。其策略拒绝 localhost、环回、私网、链路本地及保留地址，只有显式 allowlist/临时配置才放行；例如 `http://127.0.0.1:9/v1` 默认会在创建/编辑阶段报错。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/write.go` 的 `createInTx` 只从凭据中提取必要 secret、直接加密保存（行 360-375），`patch.go` 也仅浅合并后重新加密（行 377-403）；M08 账户包没有对应 URL 解析、私网地址判断或 allowlist 配置检查。`provider_protocol_profiles.base_url` 的读取只用于 profile 元数据，不等价于对用户凭据 `base_url` 的安全校验。
- 结果偏差：同一带本地/内网 `base_url` 的创建或编辑请求，Node 默认 400，历史 Go 只要 secret 存在就成功落库；后续上游请求可能被引导到本机或内网地址，既改变 Node 的可观察结果，也绕过既有 SSRF 防护边界。
- 结论：这是已提交 M08 凭据写入的确定安全与行为偏差，当前仅记录，未修改 Go 代码。

### 2026-09-05：创建账户未投递 Node 的初始健康检查

- Node 基线：`backend/src/modules/accounts/accounts.routes.ts` 创建事务成功后立即调用 `dispatchInitialAccountHealthCheck(account)`，再返回 201（行 273-278）。该 dispatch 条件由 `account-health-check-dispatch.service.ts` 定义：账户为 `pending_test`，或账户为 active、单个 API Key 且尚未启用余额查询时，都要以 `activation` 原因投递健康检查（行 18-31、33-43）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 的 create 在 `Store.Create` 成功后只写操作日志并返回 201（行 248-286），没有任何 health-check dispatch/outbox 调用。`write.go` 仅把状态设为 `pending_test` 或 `active` 并写入账户表（行 411-437、578-622），不会创建健康检查输入。
- 结果偏差：默认创建得到 `pending_test` 的账户不会触发首次健康探针，可能长期停留在等待检查；显式创建为 active 的单 Key API Key 账户也不会触发 Node 用于余额能力探测的 activation 检查，后续状态/余额能力判定与 Node 不一致。
- 结论：这是已提交 M08 创建流程的确定运行态副作用遗漏，当前仅记录，未修改 Go 代码。

### 2026-09-05：PATCH 连接/模型变更未重置健康状态

- Node 基线：`backend/src/storage/account-management-patch.repository.ts` 将凭据、代理、支持模型、模型映射、检查模型/协议和 endpoint modes 的变化归为 `healthCheckRequired`，把 `next_health_check_at` 清空；连接凭据变化还会清空最近检查时间、成功时间、失败计数、失败详情和 trace（行 677-694）。事务结果携带 `healthCheckRequired`，路由提交后再投递 `dispatchAccountHealthCheck(account.id, reason)`（`backend/src/modules/accounts/accounts.routes.ts` 行 364-369）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/patch.go` 对 `CredentialsPresent` 只解密、浅合并并更新 `credentials_encrypted/credential_fingerprint/credential_mask`（行 377-403），对模型/检查字段只更新对应配置列（行 318-375）；查询结构没有读取 `next_health_check_at`、最近检查/成功时间或失败计数，PatchResult 也没有 `healthCheckRequired`/reason，整个包没有提交后的健康状态重置或 dispatch 调用。
- 结果偏差：账户已经有旧健康成功/失败或未来排程时，Go 修改凭据、代理、支持模型、模型映射或检查协议后仍保留旧健康结果和下一次检查时间，可能继续按旧连接/模型被视为健康；Node 会先使旧状态失效并安排新的 configuration 探针。
- 结论：这是已提交 M08 PATCH 运行态一致性的确定偏差，独立于创建时是否投递初始探针，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户高级详情端点未迁移

- Node 基线：历史 Node 注册 `GET /:id/advanced`，通过 `findAccountAdvancedDetailAsync` 返回高级编辑模型；响应包含 `accessType`、仅 owner 可见的高级凭据策略、有效 quota recovery/error handling 规则、模型映射、代理/排程/过期配置、余额配置以及锁状态和锁参数（`backend/src/modules/accounts/account-detail.routes.ts` 行 227-243；`backend/src/storage/account-advanced-detail.repository.ts` 行 76-94、175-208）。前端 `accountsApi.advancedDetail` 与 `myAccountsApi.advancedDetail` 均直接请求该路径（`frontend/src/api/domains/accounts.ts` 行 111、155）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 只注册 `GET /accounts/{id}` 与 `/edit-basic`，没有 `/advanced`（行 30-43、46-58）；两者都调用同一个 `Store.FindEditBasicDetail`（`routes.go` 行 215-227），没有高级详情查询、授权实例有效来源投影或高级策略字段。
- 结果偏差：管理页或自有账户页请求高级详情时，Go 返回路由不存在，而不是 Node 的 200 高级 DTO；即使客户端退回普通详情，也拿不到有效策略、模型映射、余额配置和锁状态等高级编辑数据，相关编辑功能无法工作。
- 结论：这是已提交 M08 端点矩阵的确定缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户 clone-context 端点未迁移

- Node 基线：历史 Node `GET /:id/clone-context` 查询账户、模型、标签、模型映射和绑定分组，并返回经过脱敏/计数的 `credentialOptions`、完整克隆所需配置和 `configRevision`（`backend/src/storage/account-interaction-context.repository.ts` 行 236-240、247-300、377-419）。它明确拒绝授权实例克隆（行 297-300），并在读取关系后再次核对配置版本及绑定分组快照；并发变化时返回 409（行 348-375、420-422）。路由还区分 404/403/409（`backend/src/modules/accounts/account-detail.routes.ts` 行 103-125）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 没有注册 `/clone-context`（行 30-43、46-58），账户包也没有对应的交互上下文查询、凭据选项投影、授权实例禁止克隆或版本一致性检查。
- 结果偏差：管理端/自有账户克隆页面请求该接口时 Go 直接返回路由不存在；无法得到 Node 的克隆上下文，也无法在授权实例或并发修改场景下返回相同的 403/409 保护结果。
- 结论：这是已提交 M08 端点与并发保护契约的确定缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：Google OAuth 重新授权上下文端点未迁移

- Node 基线：历史 Node 注册 `GET /:id/oauth-reauthorization-context`，仅查询未删除的 Gemini `google_oauth` 账户，解密后只投影重新授权所需的 `oauthType`、client/quota/project/tier/baseUrl 等元数据，不返回 access/refresh secret（`backend/src/storage/account-interaction-context.repository.ts` 行 174-217）。授权实例会明确返回 403“授权实例不能重新授权”，不存在或非该类型账户返回 404（`backend/src/modules/accounts/account-detail.routes.ts` 行 81-100）。前端 admin/self API 均有对应调用（`frontend/src/api/domains/accounts.ts` 行 113、157）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 没有注册该路径，账户包也没有 OAuth 类型筛选、owner scope、元数据投影或授权实例禁止重新授权逻辑（路由仅覆盖行 30-43、46-58 的基础 CRUD/锁/标签）。
- 结果偏差：Google OAuth 账户重新授权页面无法取得 Node 所需上下文，Go 返回路由不存在；同时无法得到 Node 对授权实例 403 与非匹配账户 404 的安全边界。
- 结论：这是已提交 M08 OAuth 管理端点的确定缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户 API Key 运行态详情端点未迁移

- Node 基线：历史 Node `GET /:id/api-key-runtime` 先按 owner scope 查询账户，授权实例返回 403，owner 账户读取各 Key 的持久化运行态并经过 sanitizer 后返回 `{ accountId, configRevision, items }`；无权或不存在分别返回 403/404（`backend/src/modules/accounts/account-detail.routes.ts` 行 129-154；`backend/src/storage/account-api-key-runtime.repository.ts` 行 18-58；`backend/src/modules/accounts/account-api-key-pool-runtime.ts` 行 6-29）。前端 admin/self API 都直接依赖该路径（`frontend/src/api/domains/accounts.ts` 行 114、158）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 没有注册 `/api-key-runtime`，账户包也没有读取 `account_api_key_runtime_states`、按 owner/授权实例分流或脱敏运行态 DTO 的实现。
- 结果偏差：API Key 池管理页面无法加载每个 Key 的 active/disabled/error 等运行态详情；授权实例的 403 安全边界和 owner 账户的 `configRevision/items` 响应均不存在，Go 直接返回路由不存在。
- 结论：这是已提交 M08 API Key 运行态读端点的确定缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户运行态重置端点未迁移

- Node 基线：历史 Node 注册 `POST /:id/runtime-reset`，由 mutation guard 保护 `accounts.runtime_reset`，要求有效 `expectedConfigRevision` 和 scope；参数错误返回 400，账户不存在返回 404，配置版本冲突返回 409，其他已知运行态错误也按 400 返回（`backend/src/modules/accounts/account-detail.routes.ts` 的 runtime-reset 路由段）。`resetAccountRuntimeStateAsync` 会先读取 owner/authorized 账户摘要并执行 revision CAS，再按账户视角处理锁死、授权来源阻断、持久失败状态和可调度性；随后清理 gateway runtime、normal-route latency degradation、API Key 池运行态和 transient failure，必要时推进 circuit dispatch revision，并在配置变化时投递 health check，返回 `configRevision`、`dispatchRevision`、`gatewayRuntime`、各类清理计数、`cleared/skipped/failed` 明细和操作日志（`backend/src/modules/accounts/account-runtime-reset.service.ts` 的 `AccountRuntimeResetResult` 与 `resetAccountRuntimeStateAsync`）。锁死、过期、授权来源不可用等边界不会被“重置”成可调度状态。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 仅注册账户列表/options/详情/edit-basic、创建、PATCH、锁/标签和删除等路由，没有 `/accounts/:id/runtime-reset` 或 `/my-accounts/:id/runtime-reset`；M08 账户包也没有对应的运行态重置服务、revision CAS、锁/授权边界、gateway/latency/API Key transient 清理、dispatch revision 推进或 health dispatch 接线。
- 结果偏差：账户管理页无法执行 Node 的“一键清理运行状态”操作，Go 直接返回路由不存在；失败熔断、延迟降级、Key 临时失败和授权实例运行态不会按 Node 规则被清理，也没有返回逐项清理/跳过/失败结果及审计日志。该缺口独立于 API Key 专用重验证端点，不能由普通 PATCH 或 lock/unlock 替代。
- 结论：这是已提交 M08 运行态管理功能的确定端点与副作用缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：API Key 运行态主动重验证端点未迁移

- Node 基线：历史 Node 注册 `POST /:id/api-key-runtime/revalidate`，使用 mutation guard 与 `accounts.api_key_runtime_revalidate` 操作键；请求体必须提供有效的 `expectedConfigRevision`，空/非法 scope 返回 400，账户不存在返回 404，授权实例返回 403，版本不匹配返回 409（`backend/src/modules/accounts/account-detail.routes.ts` 运行态重验证路由段）。通过 `revalidateAccountApiKeyRuntimePoolAsync` 继续校验账户必须 active、schedulable、受支持的多 Key API Key 池，并只处理非 `active`/`disabled` 且没有有效 probe lease 的 Key；`error` 会恢复为 `unverified`，清空 recovery/cooldown，立即设置 `next_probe_at`/`last_attempt_at`，按配置版本栅栏更新并触发运行态/统计失效（`backend/src/storage/account-api-key-runtime-state.repository.ts` 的 `AccountApiKeyRuntimeRevalidateResult` 与 `revalidateAccountApiKeyRuntimePoolAsync`）。没有可重验证 Key 时返回 409、代码 `ACCOUNT_API_KEY_RUNTIME_REVALIDATE_NOT_EXECUTABLE` 和原因 `no_revalidatable_key`；不支持的账户返回同一代码及 `not_supported`，成功则返回 `{ id, configRevision, changed }` 并写操作日志。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 只注册基础详情、编辑、创建、PATCH、锁/标签和删除路由，没有 `POST /api-key-runtime/revalidate`；M08 账户包也没有对应的运行态状态查询/更新、资格判断、config revision CAS、probe lease 保护、错误代码映射或失效通知。
- 结果偏差：API Key 池管理页无法手动将失败/未验证 Key 标记为立即探测，Go 直接返回路由不存在；即使调用等价内部逻辑，也无法得到 Node 对 active/schedulable、多 Key、授权实例、版本冲突和 disabled/lease 状态的保护，以及 `changed` 计数和运行态/统计失效副作用。
- 结论：这是已提交 M08 API Key 运行态写端点的确定缺口，独立于运行态详情读取，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户批量更新端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/batch-update` 与 self 镜像 `/my-accounts/batch-update`，先解析 scope 和严格的 `targets`/`updates` schema，再由 `batchEditAccountsAsync` 在同一事务内锁定所有 owner 账户（PostgreSQL 使用 `FOR UPDATE`）、校验每个 `configRevision`、账户未删除且不是授权实例，并要求所有目标属于同一系统账户作用域。每个账户按字段做深比较和 provider/type 同质性校验，复用凭据、模型、排程、过期、标签、代理、调度绑定等单账户归一化规则；任一目标版本冲突或权限不满足即整体失败，不会部分提交。成功响应包含 `batchId`、每账户新版本和 `changedFields`，提交后按实际变化刷新分组统计、失效账户/网关运行态、推进 dispatch revision，并清理余额快照；有变更时还写带受影响账户 targets 的批量操作日志（`backend/src/modules/accounts/account-batch-edit.routes.ts`、`account-batch-edit.service.ts`、`backend/src/storage/account-batch-update.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 只注册列表/options/详情/edit-basic、创建、单账户 PATCH、锁/标签和删除路由；`git ls-tree aed567991` 的历史 accounts 包没有 batch service/repository 或 `batch-update`/`batch-edit-context` handler。单账户 `PATCH` 只能处理一个账户，不能提供 Node 的多目标同事务 CAS、同作用域/授权实例边界、字段归一化和批量副作用。
- 结果偏差：管理页或自有账户批量编辑请求在 Go 直接返回路由不存在；无法一次性覆盖多个账户，也不存在 Node 的“全成功或全回滚”版本保护和逐账户新版本回执。客户端若退回循环 PATCH，还会产生不同的并发窗口、部分成功结果、操作日志 targets 及统计/运行态失效时序，因此不能视为功能等价。
- 结论：这是已提交 M08 账户批量写功能的确定端点与事务语义缺口；`batch-edit-context` 上下文读取将作为下一独立审计项，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户批量编辑上下文端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/batch-edit-context` 与 `/my-accounts/batch-edit-context`。请求体严格要求 2–100 个不重复 `accountIds`，并只允许 `supportedModels`、`modelMappings`、`supportedEndpointModes` 三类上下文字段；scope 不存在时返回 403，任一账户不可访问/缺失返回 404，同一批次跨系统账户作用域返回 400。`loadAccountBatchEditContextAsync` 会按请求字段读取每个 owner 账户的 `configRevision`、provider/protocol/type，并按需投影支持模型、模型映射和凭据中的 endpoint modes；授权实例、已删除账户和越权账户不会混入结果（`backend/src/modules/accounts/account-batch-edit.routes.ts`、`account-batch-edit.service.ts`、`backend/src/storage/account-batch-edit-context.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述两套路径；M08 账户包没有 batch-edit context handler/repository，也没有按字段加载关联模型/映射/endpoint modes、同作用域完整性检查或 2–100 不重复校验。普通详情只返回单账户编辑信息，不能替代该批量快照。
- 结果偏差：批量编辑页面无法在 Go 获取各目标的版本和可覆盖字段上下文，直接返回路由不存在；客户端无法按 Node 结果决定字段是否可批量覆盖、比较 provider/type 同质性或在提交时携带正确版本。即使前端自行串行调用普通详情，也会丢失 Node 的统一 scope/越权/授权实例边界和同一请求快照语义。
- 结论：这是已提交 M08 批量编辑前置读功能的确定端点缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户导出端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/export` 与 self 镜像 `/my-accounts/export`。请求可按不重复 `accountIds`（1–500）或按关键字、provider、group、tag、type、status、schedulable、排序条件筛选；筛选结果超过 500 会明确 400，空结果也返回业务错误。导出读取会按 access scope 只保留 owner 且具备查看凭据/编辑权限的账户，排除授权实例和已删除账户；账户凭据按类型仅导出允许的字段，代理只导出启用代理及其引用，结果带固定导入协议 `type/version`、账户/代理文档和 `accounts/proxies/skippedAccounts/matchedAccounts` 摘要。导出成功还记录 `accounts.export` 操作日志，管理员日志仅 admin 可见，self 日志带 owner viewer（`backend/src/modules/accounts/account-export.routes.ts`、`account-export-request.ts`、`account-export.service.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/export`；M08 账户包没有导出请求 schema、按筛选分页收集 ID、500 条上限、owner/授权过滤、凭据字段白名单、代理去重引用、导入协议版本或导出操作日志。账户详情/列表响应也不能替代导入兼容的文档结构。
- 结果偏差：管理端与自有账户页的导出请求在 Go 直接返回路由不存在，无法生成可再导入的账户文档；更无法得到 Node 对授权实例/凭据可见性、筛选总数上限、代理引用和敏感字段白名单的统一边界。若客户端自行拼装详情，不仅缺少固定协议和摘要，还可能把不应导出的运行态/凭据字段带出，因此不等价。
- 结论：这是已提交 M08 账户迁移的确定导出端点缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户导入预览端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/import/preview` 与 `/my-accounts/import/preview`。请求体严格校验 `data`、可选 `sourceMode`（native/sub2api/newapi/cpa/oneapi）和导入选项；预览阶段只构建计划、不落库，先适配不同来源格式，再校验固定协议 `juhe-ai-account-import`/版本 1、最多 50 个账户和 20 个代理，规划代理/分组/账户，执行 provider、凭据、模型、代理和重复项检查，并返回每项 `create/reuse/skip/failed`、messages/warnings、来源摘要和 `canImport`（`backend/src/modules/accounts/account-import.routes.ts`、`account-import.service.ts` 及其 plan/validation/resolver 模块）。scope/参数错误统一返回 400；预览结果必须足够让前端在确认前展示将创建、复用、跳过和失败的完整计划。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/import/preview` 或 self 镜像；M08 账户包没有来源适配器、导入协议根校验、50/20 数量上限、代理/分组解析、凭据/模型验证、重复检测或只读计划结果结构。
- 结果偏差：管理端和自有账户导入页面在 Go 无法先预览，直接返回路由不存在；客户端既不能看到 Node 的逐项失败/警告和 `canImport` 决策，也无法在不写入数据库的前提下确认目标账户/代理/分组冲突。若直接调用创建接口替代，还会绕过来源格式适配、重复/资源复用判断和“确认前不落库”语义，功能结果不一致。
- 结论：这是已提交 M08 账户导入流程的确定预览端点与安全边界缺口，`/import/confirm` 执行端点将作为下一独立审计项，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户导入确认端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/import/confirm` 与 `/my-accounts/import/confirm`，在严格请求 schema 后使用 mutation guard 的 `accounts.import` 操作键，指纹包含 owner、数据、来源模式和选项，避免重复提交。确认阶段重新构建并执行导入计划：按计划创建/复用代理和分组，无法解析代理或分组时把关联账户改为 failed；逐账户创建时复用完整账户创建 payload，遇到名称重复且 `skipDuplicates` 开启则记为 skip，否则记为 failed；每个新建账户按 Node 规则投递 pending health check。结果保留 `type/version/mode='import'/canImport=false`、逐项 action/messages/warnings、来源/资源/账户汇总和已创建 ID；单项失败不会伪装成整体成功，最终还写 `accounts.import` 审计日志（`backend/src/modules/accounts/account-import.routes.ts`、`account-import.service.ts`、`account-import-executor.ts`、`account-import-resource-creator.ts`、`account-import-account-creator.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/import/confirm` 或 self 镜像；M08 账户包没有 mutation guard 指纹、来源计划执行器、代理/分组创建复用、重复账户 skip、逐项失败统计、pending health dispatch 或导入审计日志。
- 结果偏差：确认导入请求在 Go 直接返回路由不存在，无法创建或复用多个资源，也无法返回 Node 的部分成功/部分失败明细和已创建账户 ID。用普通 create 循环替代会丢失来源适配、重复提交保护、代理/分组关联、错误聚合和健康探针副作用，不能视为等价实现。
- 结论：这是已提交 M08 账户导入写功能的确定端点与执行语义缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户余额手动刷新端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/:id/balance/refresh` 与 `/my-accounts/:id/balance/refresh`。请求先解析 scope 并按 owner 账户查询，账户不存在返回 404，授权实例或无编辑权限返回 403；只有 `type='api_key'`、已开启余额查询且未删除的账户才生成手动刷新 candidate，否则返回 400。刷新执行通过 `refreshAccountBalanceCandidateWithOutcome(candidate, { mode: 'manual' })`，对同一账户取得共享 lease，按单/多 API Key 选择内置或自定义适配器，处理代理、超时、上游错误和多 Key 聚合，并以 `configRevision`（手动模式不依赖旧 due 时间）条件写回账户配置与统计快照；lease 被占用或配置版本已变化时返回 409，成功返回带逐 Key 状态/金额/错误的最新 snapshot（`backend/src/modules/accounts/account-balance.routes.ts`、`backend/src/storage/account-balance.repository.ts`、`backend/src/modules/accounts/account-balance-query.service.ts`）。历史 Node 还保留一个明确受配置开关控制的 Go 任务所有者分支，但多 Key 仍会拒绝切换；默认 Node 路径的上述资格、lease 和 CAS 语义不能省略。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/balance/refresh` 或 self 镜像；M08 账户包没有余额 candidate 查询、账户/授权边界、共享 lease、单/多 Key 适配器、快照版本 CAS、409 outcome 或余额统计写回实现。虽然 create/PATCH body parser 能接收 `balanceQueryEnabled`/`balanceQueryConfig`，这只是保存字段，不能提供手动刷新功能。
- 结果偏差：账户页点击刷新余额在 Go 直接返回路由不存在；无法得到 Node 的无权/未开启/lease busy/配置过期错误语义，也不会实际查询上游、聚合 Key 余额或更新快照。用普通账户 PATCH 或后台自动任务替代会改变触发时机、并发保护和返回的 snapshot 结果，不能视为等价。
- 结论：这是已提交 M08 账户余额功能的确定手动刷新端点与运行态写回缺口，余额详情和草稿余额测试将作为后续独立审计项，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户余额明细端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/:id/balance/details` 与 `/my-accounts/:id/balance/details`。它只允许 owner 且拥有 `canViewCredentials` 的账户查看，授权实例、授权来源账户或无权限返回 403；未启用余额查询返回 404。读取快照时必须同时匹配当前账户 `configRevision` 与 `balanceQueryNextRefreshAt` 世代，旧配置/旧 Key 池快照被视为 stale；响应按当前有效 API Key 池逐项补齐不存在的 Key 为 masked `pending`，不暴露旧世代时间戳，并返回 `keyCount`、`queriedKeyCount`、`scope`、`aggregation`、可选 `updatedAt` 和逐 Key 状态/金额/错误，设置 `Cache-Control: no-store`（`backend/src/modules/accounts/account-balance.routes.ts`、`account-balance.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/balance/details` 或 self 镜像；M08 账户包没有余额快照读取、config revision/next-refresh 世代匹配、当前 Key 指纹投影、掩码 pending 补齐、授权/凭据查看权限判断或 no-store 响应。
- 结果偏差：余额详情页在 Go 直接返回路由不存在，无法区分“账户未开启”与“无权查看”，也不能显示当前 Key 池的逐项 pending/成功/失败状态。若直接返回数据库旧 snapshot，会把已更换凭据或配置的金额和时间错误地展示为当前结果，和 Node 的 stale 防护不一致。
- 结论：这是已提交 M08 账户余额读功能的确定端点与快照世代缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户草稿余额测试端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/balance/test-draft` 与 `/my-accounts/balance/test-draft`。请求体必须包含完整草稿账户和 `balanceQueryConfig`；服务先按请求 scope 解析 provider/profile、账户类型、可管理分组、凭据、支持模型、检查模型/协议、模型映射和代理，复用账户创建/草稿测试的归一化与兼容性校验，但只在内存中构造临时账户快照，不创建账户、不写余额快照或配置。随后 `validateAccountBalanceCapability` 判断该草稿是否支持余额查询，多 Key 会按产品规则返回明确“不支持多 Key 余额”的 400，单 Key 才调用 `testAccountBalanceCandidate` 查询上游并返回带时间戳的测试 snapshot；所有输入或上游错误均返回 400（`backend/src/modules/accounts/account-balance.routes.ts`、`account-draft-test.service.ts`、`account-balance-query.service.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/balance/test-draft` 或 self 镜像；M08 账户包没有草稿账户构造、provider/profile/group 解析、凭据/模型/endpoint mode 兼容校验、余额能力判断、只读上游测试或 snapshot 响应。create/PATCH 的余额字段解析不能替代该“未保存配置测试”边界。
- 结果偏差：账户创建/编辑页面在保存前测试余额时，Go 直接返回路由不存在；若改为先创建再刷新，会把 Node 的只读草稿行为变成落库副作用，且改变多 Key 拒绝、模型/协议校验和错误返回时机，功能结果不一致。
- 结论：这是已提交 M08 余额草稿测试功能的确定端点与只读语义缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户上游模型目录刷新端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/model-catalog/refresh` 与 `/my-accounts/model-catalog/refresh`。请求体严格校验草稿账户；服务先按 owner scope 解析启用 provider/profile、可管理且同供应商的分组、代理和规范化凭据，构造不落库的临时账户/测试快照。随后沿真实网关/代理/凭据链路发现上游模型；多 Key API Key 会对每个 Key 单独发现并取交集，避免把某个 Key 独有模型误报为池级可用。结果再与本地 provider model catalog 和 test catalog 对齐，只返回当前协议支持且上游存在、但尚未选择的 `addedModels`，并推荐兼容的 `healthCheckModel`；客户端断开会中止上游请求，不写账户配置或模型表（`backend/src/modules/accounts/accounts.routes.ts`、`account-draft-test.service.ts`、`account-model-catalog-refresh.service.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/model-catalog/refresh` 或 self 镜像；M08 账户包没有草稿账户/分组解析、provider/profile 协议校验、上游模型发现、API Key 池交集、协议过滤、推荐检查模型或请求取消处理。
- 结果偏差：账户创建/编辑页无法在保存前刷新上游模型目录，Go 直接返回路由不存在；若只读取本地模型表，会把未在当前 Key/代理/上游实际可用的模型误加入候选，或无法给出 Node 的 `addedModels`/推荐检查模型结果。该功能也不能由保存后普通 PATCH 替代，因为 Node 明确不落库且使用真实草稿凭据链路。
- 结论：这是已提交 M08 账户模型目录发现功能的确定端点与运行时探测缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：待检查账户人工强制激活端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/:id/force-activate` 与 `/my-accounts/:id/force-activate`，使用 mutation guard 的 `accounts.force_activate_pending` 操作键，并要求 body 中 `acknowledgedAccountAvailable === true`；缺少确认返回 400。账户不存在返回 404，授权实例返回 400，只有 `pending_test` 状态允许继续，否则返回 409。存储层再以 owner、未删除、非授权实例、未过期和原 `config_revision` 条件 CAS，将状态按当前 availability schedule 设为 `active` 或 `disabled`，清除 cooldown/错误/健康失败/流失败字段；变为 active 时推进 circuit dispatch revision，刷新分组统计，提交后失效账户 lookup 和 gateway runtime。成功记录人工恢复审计并返回 sanitizer 后完整账户 DTO，竞争更新则返回 409（`backend/src/modules/accounts/account-force-activate.routes.ts`、`backend/src/storage/account-runtime-mutation.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/force-activate` 或 self 镜像；M08 账户包没有确认参数校验、pending-only/授权/过期/schedule 保护、config revision CAS、健康/流失败清理、dispatch revision、统计/runtime 失效或人工恢复日志。
- 结果偏差：后台健康检查长期停留在 `pending_test` 的账户无法通过 Go 人工恢复，直接返回路由不存在；若用普通 PATCH 设置 active，还会绕过 Node 的确认门槛、过期与排程判定以及失败状态清理，可能把不可用账户直接纳入调度，功能和安全结果均不一致。
- 结论：这是已提交 M08 账户恢复流程的确定端点与状态机副作用缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试模型选项端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/:id/test-options` 与 `/my-accounts/:id/test-options`。它先按 access scope 查询受控测试上下文：owner 账户只读自身，授权实例只有在 authorization 状态为 active/paused/expired 且来源账户仍存在时才可投影；凭据仅在服务端用于解析 endpoint modes，不进入响应。查询参数支持 keyword、limit、selectedIds，并自动把账户当前 `healthCheckModel` 加入 selected 集合，避免当前值在筛选时消失。`accountManualTestOptionsAsync` 从 provider test catalog 过滤音频/图片等不适合当前 provider/profile/type 的模型，再结合账户 endpoint modes、模型映射和上游协议能力逐模型计算可用 `testEndpointModes`，最终只返回至少一种可测试形态的 `{ id, name, testEndpointModes }`（`backend/src/modules/accounts/account-test-dispatch.routes.ts`、`account-test-options.service.ts`、`account-manual-test-context.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/test-options` 或 self 镜像；M08 账户包没有测试上下文查询、授权来源有效性边界、provider test catalog 过滤、selectedIds/keyword/limit 归一化、模型映射解析或 endpoint mode 能力计算。
- 结果偏差：账户测试页面在 Go 无法加载可用模型和请求形态，直接返回路由不存在；若客户端只使用账户支持模型列表，会把当前协议/映射不支持的模型显示为可测试，或丢失授权实例的有效来源投影，导致后续测试请求选择错误模型/协议。
- 结论：这是已提交 M08 账户手工测试前置读功能的确定端点与能力决策缺口，`/test-options/models/:modelId` 的单模型能力端点将作为下一独立审计项，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户单模型测试能力端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/:id/test-options/models/:modelId` 与 `/my-accounts/:id/test-options/models/:modelId`。它按账户可见性读取 owner/有效授权来源上下文，去 provider test catalog 查找指定模型；模型不存在或不属于当前账户供应商/协议/type 时返回 400，账户不存在返回 404。`accountManualTestModelCapabilitiesAsync` 会结合账户启用的 endpoint modes、OpenAI 模型映射后的上游模型协议、Anthropic/Gemini 协议能力以及 API Key 图片能力，返回唯一规范化模型 ID 和允许的 `testEndpointModes`；没有任何可连接测试形态也明确返回 400，而不是让后续测试任务自行失败（`backend/src/modules/accounts/account-test-dispatch.routes.ts`、`account-test-options.service.ts`、`account-manual-test-context.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册该路径；M08 账户包没有单模型 catalog 查询、授权来源投影、模型映射上游解析、协议/endpoint mode 能力计算或无能力错误映射。
- 结果偏差：客户端无法在选择具体模型后得到 Node 的协议形态集合，Go 直接返回路由不存在；若只相信账户支持模型列表，可能把映射后的上游不支持模型或错误 endpoint mode 交给测试任务，造成测试请求失败或测试结果与 Node 不同。
- 结论：这是已提交 M08 账户测试能力读功能的确定端点缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户手工测试任务端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/:id/test` 与 `/my-accounts/:id/test`。请求先严格解析 `model`、`testEndpointMode`、可选 `testSessionId` 和保存中草稿账户；账户不存在返回 404，非 OpenAI/Anthropic/Gemini 协议或当前有效性不允许测试返回 400。授权实例只能在 Node 允许的有效授权/失败状态下测试，管理员或 owner 使用 `diagnostics='full'`，普通授权实例使用 `diagnostics='limited'`。若提交草稿，Node 会重新校验草稿与当前账户/provider/type 一致，并用草稿的模型、映射、凭据和 endpoint mode 做能力选择；否则按持久账户通过 test-options 能力解析目标模型/协议。任务写入 `account_test_tasks`，校验会话归属和单任务约束，保存加密草稿快照，关联 `testSessionId`，状态为 queued；worker 投递失败会把任务置 failed 并返回 503，成功返回 202 和完整任务 DTO（`backend/src/modules/accounts/account-test-dispatch.routes.ts`、`account-test-options.service.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `POST /test` 或 self 镜像；M08 账户包没有测试上下文/可测性判断、授权诊断级别、草稿快照、模型与 endpoint mode 解析、会话绑定、任务持久化、worker 投递或 202/503 回执。
- 结果偏差：账户测试页面在 Go 无法创建测试任务，直接返回路由不存在；若简单同步调用上游，会丢失 Node 的异步 queued/202 语义、会话取消/权限边界、草稿不落库以及 worker 不可用时的失败任务记录，客户端轮询和诊断结果都会偏离。
- 结论：这是已提交 M08 账户手工测试执行链的确定端点与任务语义缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试会话创建端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/test-sessions` 与 `/my-accounts/test-sessions`。路由先解析 scope 和登录上下文，缺少 `requestAccess` 返回 403；成功调用 `createAccountTestSessionAsync`，在 `account_test_sessions` 写入独立 ID、请求系统账户、角色、可选 `systemAccountFilterId`，状态 `running` 及当前 heartbeat 时间，并返回 201 会话 DTO。会话与账户测试任务分离，后续创建任务时会校验会话仍属于同一请求 scope、未取消/过期且只能绑定一个任务；worker/定时器可据 heartbeat 和任务状态自动收口空闲会话（`backend/src/modules/accounts/account-test-session.routes.ts`、`backend/src/storage/account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 `/test-sessions` 或 self 镜像；M08 账户包没有测试会话表写入、请求 scope/角色归属、heartbeat 初值、单任务约束或会话生命周期服务。
- 结果偏差：前端无法在批量/手工测试前建立可复用会话，Go 直接返回路由不存在；若跳过会话直接创建任务，会丢失 Node 的取消/心跳/空闲自动完成边界，客户端无法得到一致的 sessionId 和任务关联状态。
- 结论：这是已提交 M08 账户测试会话基础能力的确定端点与生命周期缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试会话心跳端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/test-sessions/:sessionId/heartbeat` 与 `/my-accounts/test-sessions/:sessionId/heartbeat`。路由先解析 scope，再按会话创建时保存的 `request_system_account_id` 和可选 `request_system_account_filter_id` 校验当前请求是否可读；不存在或越权统一返回 404。会话若已是 `canceled/expired/completed` 等非 `running` 状态，接口原样返回终态而不重新打开；只有 running 会话才更新 `last_heartbeat_at` 与 `updated_at`，然后返回最新会话 DTO。定时器依赖 heartbeat 判断空闲会话，只有无 queued/running 任务时才自动完成（`backend/src/modules/accounts/account-test-session.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 heartbeat 路径，且 M08 账户包没有会话存储/读权限或状态更新时间逻辑。
- 结果偏差：前端无法续期测试会话，Go 直接返回路由不存在；若客户端仅重复创建会话，会造成会话泄漏或无法把任务绑定到同一 session。若简单更新任意 session 行，还可能越权续期他人会话或把已结束会话重新变为可运行，破坏 Node 的空闲收口和取消状态机。
- 结论：这是已提交 M08 测试会话生命周期的确定心跳端点与权限/状态缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试会话完成端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/test-sessions/:sessionId/complete` 与 `/my-accounts/test-sessions/:sessionId/complete`。路由按会话创建时的请求系统账户和 filter scope 做可读性校验，不存在或越权返回 404；随后调用 `completeAccountTestSessionAsync`，仅当会话仍为 `running` 且关联任务不存在 `queued/running` 时才原子改为 `completed` 并写 `finished_at`，若仍有活动任务或会话已是终态则原样返回当前状态，不会提前结束或重新打开（`backend/src/modules/accounts/account-test-session.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 complete 路径，账户包也没有测试会话状态读取、活动任务检查或完成时间写回。
- 结果偏差：前端无法主动收口已结束的测试会话，Go 直接返回路由不存在；若简单把会话标记 completed，会在任务仍 queued/running 时截断轮询和任务关联，或允许越权完成他人会话，破坏 Node 的会话生命周期。
- 结论：这是已提交 M08 测试会话完成状态机的确定端点缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试会话取消端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/test-sessions/:sessionId/cancel` 与 `/my-accounts/test-sessions/:sessionId/cancel`。它按会话保存的请求系统账户/filter scope 校验可读权限，不存在或越权返回 404；取消时把 running 会话原子改为 `canceled`，记录 `cancel_reason`、`cancel_requested_at`、`finished_at`，返回关联的 queued/running 任务 ID。queued 任务立即改为 `canceled` 并设置 `cancel_requested=1`/`finished_at`，running 任务只设置取消请求和“正在停止测试”消息，让 worker 自己观察取消信号并收尾；路由随后逐个调用 `dispatchAccountTestCancel` 通知队列。已终态会话仍按 Node 读模型返回，不会伪造新任务（`backend/src/modules/accounts/account-test-session.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册 session cancel 路径，且账户包没有会话/任务关联查询、queued/running 分支更新、取消信号投递或 scope 隔离。
- 结果偏差：前端无法停止测试会话，Go 直接返回路由不存在；若只改会话状态而不更新 queued/running 任务和 worker cancel 信号，排队任务仍会执行、运行任务无法及时停止，或后续 worker 回写覆盖取消结果，和 Node 的可观察状态不一致。
- 结论：这是已提交 M08 测试会话取消状态机的确定端点与任务协同缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试任务取消端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/test-tasks/:taskId/cancel` 与 `/my-accounts/test-tasks/:taskId/cancel`。路由先解析请求 scope；scope 参数非法返回 400，随后按任务创建时保存的 `request_system_account_id` 与 `request_system_account_filter_id` 校验可读范围，不存在或越权任务统一返回 404。`cancelAccountTestTaskAsync` 对 `queued` 任务立即写为 `canceled`，设置 `cancel_requested=1`、`finished_at` 和“已停止测试”；对 `running` 任务只设置 `cancel_requested=1`、更新时间和“正在停止测试”，保留 running 状态让 worker 观察取消信号并自行收尾；对 success/failed/canceled 等终态任务保持幂等，原样返回当前 DTO。状态处理后路由调用 `dispatchAccountTestCancel(task.id)` 通知后台 worker，成功响应为任务 DTO（`backend/src/modules/accounts/account-test-session.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有 `account_test_tasks` 的读取、scope 过滤、queued/running 分支更新、终态幂等返回或 worker cancel dispatch 实现。
- 结果偏差：客户端无法停止单个测试任务，Go 请求直接返回路由不存在；即使只补一个状态写入，也可能把 running 任务过早改成终态、遗漏 `cancel_requested` 使 worker 继续执行，或不发送取消信号导致后续结果覆盖取消状态，同时缺少 Node 的 404 scope 隔离边界，不能视为功能等价。
- 结论：这是已提交 M08 账户测试任务控制链的确定端点、状态机与 worker 协同缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试任务详情端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/test-tasks/:taskId` 与 `/my-accounts/test-tasks/:taskId`。路由先解析请求 scope；参数非法返回 400，随后由 `getAccountTestTaskAsync` 按任务创建时保存的 `request_system_account_id` 和 `request_system_account_filter_id` 做可读性校验，不存在、空 ID 或越权任务统一返回 404。成功响应为完整任务 DTO：包括 `sessionId`、账户/供应商/协议标识、规范化 `status`、状态消息、模型与 `testEndpointMode`、解析后的 `result`、`cancelRequested` 及 queued/deadline/started/finished/updated 时间；底层对数据库日期、布尔值、状态、结果 JSON 做统一规范化，损坏数据进入错误处理，不静默拼装半残 DTO（`backend/src/modules/accounts/account-test-status.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有测试任务表读取、请求 scope 过滤、任务状态/结果 DTO 投影或 404 错误映射。
- 结果偏差：前端轮询单个任务状态在 Go 直接返回路由不存在，无法观察异步测试的 queued/running/终态、取消标志和结果；若仅暴露原始数据库行，还会绕过 Node 的 owner/filter 隔离及时间、状态、结果规范化，越权读取或把脏值展示给客户端，不能视为功能等价。
- 结论：这是已提交 M08 账户测试状态读取链的确定端点、权限边界与 DTO 投影缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试任务批量状态端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/test-tasks` 与 `/my-accounts/test-tasks`，请求先解析 scope，再读取查询参数 `ids`。`queryTextList(req.query.ids, 200)` 会接受重复 query 参数或逗号分隔字符串，逐项 trim、去空、去重并保留首次出现顺序，最多取 200 个；缺少/空 `ids` 时返回空数组而不是报错。`listAccountTestTasksAsync` 按规范化 ID 查询任务，使用任务创建时保存的 `request_system_account_id` 与 `request_system_account_filter_id` 做 scope 过滤，越权/不存在 ID 静默从结果中剔除，最后按请求 ID 顺序返回完整规范化任务 DTO（`backend/src/modules/accounts/account-test-status.routes.ts`、`account-test-tasks.repository.ts`、`shared/query-values.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册两套 `/test-tasks` 路径；M08 账户包没有 `ids` 列表解析、200 条上限、去重/顺序保持、批量 scope 过滤或任务 DTO 投影实现。
- 结果偏差：前端批量轮询测试任务在 Go 直接返回路由不存在，无法一次取得 queued/running/终态任务集合；若改为循环调用单任务详情，还会改变 Node 的缺失 ID 静默过滤、请求顺序、去重和单次读取边界，并增加竞态窗口，不能视为功能等价。
- 结论：这是已提交 M08 账户测试状态批量读取链的确定端点与查询契约缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试会话详情端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/test-sessions/:sessionId` 与 `/my-accounts/test-sessions/:sessionId`。路由先解析请求 scope；参数非法返回 400，之后按会话保存的 `request_system_account_id` 和 `request_system_account_filter_id` 做可读性校验，不存在、空 ID 或越权会话统一返回 404。成功响应为规范化 `AccountTestSession` DTO：`status` 只允许 `running/canceled/expired/completed`（未知存值按 Node 读模型归一为 `expired`），并返回 `cancel_reason`、`lastHeartbeatAt`、`cancelRequestedAt`、`finishedAt`、`createdAt`、`updatedAt` 等时间字段；读取只读，不会因查询而重新打开终态或修改 heartbeat（`backend/src/modules/accounts/account-test-status.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有测试会话表读取、请求 scope/filter 隔离、状态/时间规范化或会话 DTO 响应。
- 结果偏差：前端无法取得会话 ID 对应的 running/终态及取消、心跳时间，Go 直接返回路由不存在；若仅回传原始数据库行，可能把未知状态、无效时间或越权会话暴露给客户端，并偏离 Node 的只读与 404 边界，不能视为功能等价。
- 结论：这是已提交 M08 测试会话状态读取链的确定端点、权限边界与 DTO 规范化缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户测试会话任务列表端点未迁移

- Node 基线：历史 Node 注册 `GET /accounts/test-sessions/:sessionId/tasks` 与 `/my-accounts/test-sessions/:sessionId/tasks`。路由先解析请求 scope，使用 `getAccountTestSessionDetailAsync` 校验会话 ID 及其保存的 `request_system_account_id`/filter scope；不存在、空 ID 或越权会话返回 404。成功时只返回该会话下通过 `account_test_session_tasks` 关联的任务数组，不返回会话外任务；任务按 `queued_at ASC, id ASC` 排序，并再次按任务 request scope 过滤、映射为完整规范化 `AccountTestTask` DTO（`sessionId`、状态/取消标志、结果和全套时间字段），不会把未绑定或不可见任务混入响应（`backend/src/modules/accounts/account-test-status.routes.ts`、`account-test-tasks.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有会话任务关联查询、会话 scope 校验、任务二次可见性过滤、稳定排序或 DTO 投影。
- 结果偏差：前端无法按 sessionId 获取批量测试任务，Go 直接返回路由不存在；若退回全局任务列表再在客户端筛选，会丢失 Node 的会话存在性/越权 404 边界和服务端绑定过滤，可能展示其他会话任务或漏掉正确排序，不能视为功能等价。
- 结论：这是已提交 M08 测试会话任务读取链的确定端点、关联范围与排序投影缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户分组绑定端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/:id/group` 与 `/my-accounts/:id/group`。请求体严格要求 `groupId`（trim 后非空）和 `expectedConfigRevision >= 1`，未知字段或显式 null 返回 400。底层在同一事务锁定账户、当前启用绑定及目标分组：目标分组必须属于同一 system account、provider 相同且 enabled；账户必须仍可管理，授权实例还要满足授权关系 active/未过期。随后按 `config_revision` 做 CAS，冲突返回 409；替换 `group_accounts` 绑定时保留/归一化优先级、超级优先和降级备用等本地调度字段，避免重复绑定。成功返回新版本和 `changedFields`，写 `accounts.bind_group` 操作日志，并按前后分组刷新统计、分组成员缓存、正常路由延迟降级、账户 lookup 与 gateway runtime（`backend/src/modules/accounts/account-group-binding.routes.ts`、`storage/account-management-patch.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；其普通 `PATCH /accounts/{id}` 的历史 `patchBody` 也不接受 `groupId`，M08 账户包没有目标分组锁定/同 provider 校验、授权实例边界、绑定替换、版本 CAS 或相关副作用。
- 结果偏差：账户编辑页无法执行绑定或换组请求，Go 直接返回路由不存在；不能用普通 PATCH 代替，因为该 PATCH 会把 `groupId` 判为未知字段。即使客户端绕过路由自行写绑定表，也会丢失 Node 的 scope/授权/分组可用性校验、409 并发保护以及前后分组统计和运行态失效，功能结果不一致。
- 结论：这是已提交 M08 账户分组绑定流程的确定端点与事务/副作用缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户流量迁移端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/:id/traffic-migration` 与 `/my-accounts/:id/traffic-migration`。请求体严格要求非空 `targetAccountId`，`sourceStatus` 仅允许 `temporary_unavailable`、`disabled`、`unchanged`；非法或未知字段返回 400。服务先按请求 scope 读取源账户，源/目标不能相同，必须在同一 system account、同一 provider、同一启用分组内；目标必须 `active`、`schedulable`、未过期且不在 cooldown，否则 400；不存在或无权源/目标返回 404。若源是授权实例，还要走 grantee 视角，目标需位于同一授权分组并通过授权状态/来源账户可用性检查。`sourceStatus=unchanged` 不改源账户，只偏向迁移已有会话；其他状态在事务内把源账户切到 `temporary_unavailable` 或 `disabled`，清理/重置错误、冷却、重试和流失败字段，提交后刷新分组统计并失效 gateway runtime。随后调用运行态会话迁移，按授权视角设置 affinity/preference scope，返回脱敏的源/目标账户、源 cooldown、迁移会话数和源状态，并写带目标账户 target 的 `accounts.traffic_migration` 审计日志（`backend/src/modules/accounts/account-traffic-migration.routes.ts`、`storage/account-runtime-mutation.repository.ts`、gateway session-affinity service）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有流量迁移 schema、同分组/provider/状态守卫、授权实例分支、源状态事务更新、会话 affinity 迁移、统计/runtime 失效或审计日志。普通历史 `PATCH` 也不接受 `targetAccountId/sourceStatus`，不存在可替代入口。
- 结果偏差：用户无法把某账户流量迁移到同组可用账户，Go 直接返回路由不存在；若只停用源账户或修改调度字段，会丢失现有会话迁移与偏好 TTL，无法保持授权实例范围、目标可用性保护和返回的 `migratedSessionCount`，可能导致新请求仍命中源账户或错误跨组迁移，功能结果不一致。
- 结论：这是已提交 M08 账户流量迁移的确定端点、状态事务与网关运行态协同缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：账户归还授权端点未迁移

- Node 基线：历史 Node 注册 `POST /accounts/:id/return-authorization` 与 `/my-accounts/:id/return-authorization`，并以 `accounts.return_authorization` 的 mutation guard 按账户 ID + grantee scope 做幂等去重。路由先解析 scope（非法返回 400），随后只允许当前 grantee 名下、未删除的授权账户实例；必须存在对应 runtime authorization、active 的 manual source 和仍可归还的直接 system-account grant，owner 自有账户、授权已失效/已归还/已撤销或不满足来源条件统一返回 404。成功在事务内把 grant 标记 `returned`，记录 actor/revoked 时间，撤销 manual authorization source 并刷新 effective source/终态，更新 quota hourly scope 绑定，投递 authorization health 输入；提交后刷新分组统计（SQLite）、清理 API Key/authorization lookup 缓存、广播 gateway runtime 与授权额度缓存失效。成功响应为 204，审计日志同时通知授权 owner 与 grantee viewers，并包含 owner/grantee targets；业务异常返回 400（`backend/src/modules/accounts/account-authorization-return.routes.ts`、`storage/resource-authorization-return.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有按账户实例定位 grant、manual source 校验、returned 状态链、授权 quota/health/runtime/cache 同步、mutation guard 或双 viewer 审计。
- 结果偏差：被授权用户无法从账户页归还账户授权，Go 直接返回路由不存在；若仅把账户软删或改为 revoked，会与 Node 的 `returned` 终态、来源切换、quota/health 失效和 204/404 幂等契约不同，授权实例可能继续出现在分组/调度中，功能结果不一致。
- 结论：这是已提交 M08 账户授权归还流程的确定端点、授权状态机与运行态副作用缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：授权账户调度设置端点未迁移

- Node 基线：历史 Node 注册 `PATCH /accounts/:id/authorized-dispatch` 与 `/my-accounts/:id/authorized-dispatch`。请求体为严格 schema：必须有 `expectedConfigRevision >= 1`，且至少提交 `status(active|disabled)`、`priority(>=0 整数)`、`superPriorityEnabled`、`fallbackEnabled` 或 `clearFailureState=true` 之一；显式 null/未知字段/空命令均返回 400。存储层只允许当前请求 scope 下、已绑定分组且仍有效的授权实例，先锁定账户和绑定，按 `config_revision` CAS，冲突返回 409；同时检查授权/授权方账户的到期、暂停、额度、删除、健康、冷却和 schedulable 状态，禁止超级优先与降级备用同时开启。状态切换会同步映射 `schedulable`，清除失败状态时重置 cooldown/error/stream failure 字段；成功后递增版本，返回 `changedFields`/`patch`，写授权调度操作日志，并按实际变化刷新分组统计、账户 lookup、gateway runtime，恢复场景还清理服务端 runtime（`backend/src/modules/accounts/account-authorized-dispatch.routes.ts`、`storage/account-authorized-dispatch.repository.ts`）。
- Go 实现：历史 `backend-go/projects/gateway/internal/accounts/routes.go` 未注册上述 admin/self 路径；M08 账户包没有授权实例绑定查询、scope/授权方可用性守卫、优先级互斥、版本 CAS、失败状态清理或统计/runtime/审计副作用。
- 结果偏差：授权账户无法在 Go 管理页调整参与调度、优先级或恢复异常状态，直接返回路由不存在；若退回普通账户 PATCH，还会绕过授权实例边界和额度/来源健康保护，无法区分 400/404/409，且可能不更新分组调度字段或运行态，导致授权调用资格与 Node 不一致。
- 结论：这是已提交 M08 授权账户调度控制链的确定端点、CAS、可用性守卫与副作用缺口，当前仅记录，未修改 Go 代码。

### 2026-09-05：OAuth 刷新批次结果错误地把 `scanned/due` 限制为已选批次

- Node 证据：历史 `backend/src/modules/openai-oauth/openai-oauth-access-token-refresh.service.ts` 先读取 `candidateFetchLimit` 范围内的全部 `dueAccounts`，再以 `result.scanned = dueAccounts.length`、`result.due = dueAccounts.length` 初始化结果；之后 `selectOpenAIOAuthRefreshBatchCandidates` 才按 `batchSize`、失败退避、准入预算和锁状态挑选本轮真正启动的候选。因此当 due 数量大于 batch 时，指标仍反映本轮扫描/到期总量，并用 `skippedBackoff`、`deferredBudget` 区分未启动原因。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 的 `RunOnce` 调用 `selectBatchCandidates` 后直接执行 `result.Scanned = len(candidates)`、`result.Due = len(candidates)`；`selectBatchCandidates` 返回的已经是最多 `batchSize` 个、且已过滤失败退避的候选，而不是 SQL 读取的 due 集合。其 `ListDueOpenAIRefreshAccounts` 默认可取 `batchSize*5`（默认 100）行，故该赋值并非等价移植。
- 结果偏差：例如有 100 个到期账户、`batchSize=20` 时，Node 返回 `scanned=100,due=100,refreshed≤20`，Go 返回 `scanned=20,due=20`；运维指标、告警、调度积压判断和后续审计会低估到期规模。失败退避跳过或准入预算耗尽时，Go 还会把这些已扫描但未进入候选的账户从 `due` 统计中抹掉，无法由结果字段还原 Node 的观察口径。
- 结论：这是 OAuth 保活任务结果 DTO/可观测行为的确定性偏差，与是否放宽并发无关。当前仅登记，未修改 Go 代码；修复时应独立保留“扫描到期集合”与“本轮选中候选”两个计数，并补充 `due > batchSize`、退避跳过和准入预算耗尽的 Node/Go 回放。

### 2026-09-05：OAuth 刷新丢失账户代理，生产 transport 明确不做代理

- Node 证据：历史 `openai-oauth-access-token-refresh.service.ts` 在每次刷新前调用 `resolveRefreshProxyUrlOrThrow`，优先使用账户已解析的 `proxyUrl`；同步持久化模式下还会根据 `proxyProfileId` 查询代理，代理不存在/不可用会作为本地配置错误阻止请求。随后 `refreshOpenAIOAuthToken` 将该 `proxyUrl` 传给 `requestProviderOAuthToken`，HTTP 请求通过 HTTP/HTTPS/SOCKS 代理发出。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/RotationAccount` 只保留 `ProxyProfileID`，`RefreshJob.refreshWithRaceRetry` 调用 `RefreshOpenAIToken` 时没有传递任何代理参数；`TokenHTTPRequest` 没有代理字段，`HTTPTokenExchanger.Do` 直接用默认 `http.Client` 发请求，源码注释也明确写着 “without proxying”。`ListDueOpenAIRefreshAccounts` 虽然查询 `proxy_profile_id`，但没有解析代理地址、校验启用状态或把代理交给 transport。
- 结果偏差：绑定了出口代理的 OpenAI OAuth 账户在 Node 会从该代理访问 token endpoint，Go 若接管则直接从 jobs 主机出口访问。代理不可用时 Node 在本地配置错误路径累计失败并按阈值停用自动刷新，Go 可能绕过代理访问成功、访问被网络策略拒绝，或把本应可区分的代理配置错误变成普通 transport 失败；跨地域、IP 白名单和审计出口约束都会产生不同结果。
- 结论：这是 OAuth 凭据刷新请求边界的确定性遗漏，独立于并发数量和 token 解析。当前仅登记，未修改 Go 代码；修复时必须把代理解析/可用性校验与 token transport 作为同一依赖注入，覆盖 HTTP/HTTPS/SOCKS、代理失效、fallback 代理和不应直连的回放。

### 2026-09-05：OAuth 凭据刷新成功后未失效网关运行态缓存

- Node 证据：历史 `openai-oauth-access-token-refresh.service.ts` 在凭据 DB service 写回成功、竞争恢复拿到更新账户、以及“数据库凭据已被其他请求更新且仍可用”的短路分支，都会调用 `clearGatewayRuntimeCache()`；同步持久化模式的 `finalizeSuccessfulTokenRefresh` 也无条件执行同一失效。这样网关不会继续使用旧 access token/过期时间缓存。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 在 `RotateCredentials` 成功、`tryRecoverRace` 返回 fresh、或批处理 `processCandidate` 成功后，都只更新数据库/失败状态计数并返回结果；`oauthrefresh.Store`、`RefreshJob` 和 `TokenExchanger` 没有 cache invalidator/event bus 依赖，也没有任何 gateway runtime/validation 失效调用。该 jobs 包不能直接复用 gateway 进程内的内存清理函数。
- 结果偏差：Go 刷新成功后，网关各实例可能继续从 validation/lookup/runtime cache 读取旧 access token 和旧过期时间，直到 TTL 或其他业务写入触发失效；Node 在刷新提交后立即清理。旧 token 已被上游轮换时，请求会继续失败或触发不必要的再次刷新，最终可用性和故障恢复时间均不同。
- 结论：这是 OAuth 刷新成功副作用的确定性遗漏，独立于 token endpoint 是否成功。当前仅登记，未修改 Go 代码；修复时应通过跨进程可观测的 Redis/runtime invalidation 端口在提交成功后发出事件，并覆盖正常写回、竞争恢复、短路复用和多网关实例回放。

### 2026-09-05：非法 `expires_at` 被 Go 静默当成到期值继续刷新

- Node 证据：历史 `openai-oauth-access-token-refresh.service.ts` 的 `parseCredentialExpiresAt` 对非空但无法解析为带 `Z` 或数值 offset 的 RFC3339 字符串直接抛错；`shouldPreRefreshAccessToken` 在 due 候选过滤阶段调用该解析器，因此某账户存在非法 `credentials.expires_at` 时，本轮 `refreshDueOpenAIOAuthAccessTokens` 会失败而不会把该账户送入 token endpoint。相同解析器也用于竞争恢复和过期判断，错误会保持可见。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 的 `shouldPreRefreshAccessToken` 调用 `parseCredentialExpiresAt` 后忽略 `err`，解析失败直接 `return true`；`accessTokenExpiredOrMissing` 也把同类错误当成过期/缺失。于是坏时间值会被当作 due，继续发起刷新并可能用新 token 覆盖坏字段。
- 结果偏差：同一条损坏凭据在 Node 暴露为刷新任务错误、保留原始数据等待修复；Go 可能绕过错误告警继续访问上游，成功时修复/覆盖数据，失败时又按普通上游失败退避。管理员看到的错误、上游调用次数、凭据最终内容和恢复路径均不一致。
- 结论：这是损坏凭据的错误处理语义偏离，不能以“继续刷新有机会自愈”替代 Node 契约。当前仅登记，未修改 Go 代码；修复时应让解析错误沿 Node 的失败路径传播，并单独回放非法字符串、空值、缺失字段和合法 offset 时间。

### 2026-09-05：OAuth 刷新锁只有进程内互斥，缺少 Node 的跨进程租约

- Node 证据：历史 `backend/src/modules/providers/drivers/_shared/oauth-refresh-lock.ts` 的 `runWithProviderOAuthRefreshLock` 默认使用 `provider-oauth:refresh-locks` runtime-state（Redis）键，以 provider + account 组成锁名；锁有 90 秒 TTL、续租、丢锁检测和释放 token，批处理 `lockMode='skip'` 在锁被其他节点持有时跳过，手动刷新则等待。OpenAI 刷新还保留旧兼容锁，保证 gateway、worker 和多实例之间不会同时旋转同一 Refresh Token。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 的 `accountLocks` 只是 `map[string]*sync.Mutex`，`TryLock`/`Lock` 仅在当前 `RefreshJob` 进程内生效；`RefreshJob` 没有 Redis/runtime-state 锁端口，`HTTPTokenExchanger`、`Store` 也无法续租或检测跨进程锁丢失。
- 结果偏差：两个 Go jobs 实例，或 Go jobs 与仍在运行的 gateway/手动刷新，同时处理同一账户时都能通过本地锁并各自调用 token endpoint。若上游轮换 Refresh Token，其中一个写回可能使另一个请求失效；CAS 只能在写库时拒绝旧版本，不能撤销已经发出的重复请求，也不能保证 Node 的 `skippedLocked`/等待语义，最终会出现额外失败、锁竞争告警和凭据状态差异。
- 结论：这是 OAuth 刷新并发保护的确定性缺失，不能用本地 mutex 代替跨进程租约。当前仅登记，未修改 Go 代码；修复时必须接入共享 Redis/runtime-state 锁、TTL 续租/丢锁 fencing，并覆盖多 jobs 实例、gateway 手动刷新和 Refresh Token 旋转竞态回放。

### 2026-09-05：Gemini OAuth 刷新省略重试与旧客户端回退

- Node 证据：历史 `backend/src/modules/gemini-oauth/gemini-oauth.service.ts` 的 `refreshGeminiAuthToken` 先用解析出的 OAuth client 调用 `requestGeminiTokenWithRetry`；该函数对可重试错误最多尝试 4 次，重试间隔为 1s、2s、4s，并在 `invalid_grant`、`invalid_client`、`unauthorized_client`、`access_denied` 等明确不可重试错误时立即结束。若内置 `code_assist`/`google_one` 客户端最终返回 `unauthorized_client`，Node 还会在存在且不同的存储客户端凭据时用 `legacyGeminiOAuthRefreshClient` 再完整执行一轮同样的重试。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/providers.go` 的 `RefreshGeminiToken` 注释明确写着省略 “retry/backoff and legacy-client fallback loops”，解析一个 client 后只调用一次 `requestGeminiToken`；没有重试等待、可重试错误分类或内置客户端失败后的旧凭据回退。
- 结果偏差：Gemini token endpoint 遇到瞬时网络错误、5xx 或短暂限流时，Node 可能在本轮成功并写回新凭据，Go 会直接失败并进入失败/退避路径；内置客户端已轮换但旧 Refresh Token 仍接受存储客户端时，Node 还能通过旧客户端回退成功，Go 则永久按当前客户端失败。两者的刷新成功率、失败计数、账户状态和上游调用次数均不同，这不是并发放宽或实现风格差异。
- 结论：这是 Gemini OAuth 刷新可靠性和客户端兼容性的确定性遗漏。当前仅登记，未修改 Go 代码；修复时应保留 Node 的可重试错误边界、退避和取消语义，并在仅针对内置客户端的 `unauthorized_client` 场景复现旧客户端回退，覆盖瞬时 5xx、网络失败、不可重试错误、客户端回退成功/失败等回放。

### 2026-09-05：OAuth token transport 丢失调用方取消与更早 deadline

- Node 证据：历史 `token-exchange-transport.ts` 将调用方 `AbortSignal` 原样传入 `requestUpstream`；Anthropic/Gemini/Grok token 请求在发起前检查 `signal.aborted`，Gemini 重试等待也监听同一 signal，传输层和上层在取消后均会结束。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/protocol.go` 的 `HTTPTokenExchanger.Do` 先执行 `context.WithoutCancel(ctx)`，再仅套用固定 `DefaultTokenTimeout=25s`。因此调用方取消、较短的 context deadline 和取消原因都不会传到 HTTP 请求；`TokenHTTPRequest.Timeout` 为零时三家 provider 都落到这一个固定值。
- 结果偏差：任务 shutdown、上层超时或手工请求取消后，Node 会立即停止 token 请求/重试，Go 仍可能继续运行至 25 秒（即使原 context 已过 deadline），占用连接、重试预算和 worker 槽位，并可能在调用方已经放弃后写回凭据或失败状态。取消点和结果时序不一致，不能归为 Go 并发实现差异。
- 结论：这是跨 provider 的请求取消契约遗漏。当前仅登记，未修改 Go 代码；修复时应保留父 context 的取消与 deadline，仅在未设置 provider 专用 timeout 时补充上限，并回放已取消 context、短 deadline、重试等待中取消及正常完成四种路径。

### 2026-09-05：Grok OAuth token 请求超时被统一缩短为 25 秒

- Node 证据：历史 `backend/src/modules/grok-oauth/grok-oauth.service.ts` 使用 `grokOAuthRequestTimeoutMs = 60_000`，并将该值同时传给 token transport 的 `timeoutMs/requestTimeoutMs`；Anthropic/Gemini 才使用各自的 25 秒常量。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/protocol.go` 只有通用 `DefaultTokenTimeout = 25s`。`formRequest`/`jsonRequest` 都不设置 `TokenHTTPRequest.Timeout`，历史 `RefreshGrokToken` 也没有覆盖它，因此 Grok 刷新实际被 25 秒硬截止，未保留 Node 的 60 秒窗口。
- 结果偏差：Grok token endpoint 在 25–60 秒内返回时，Node 会完成刷新并写回凭据，Go 已经超时并进入失败/退避路径；慢网络、代理链路或上游高峰期间账户状态和可用性会不同。该差异与并发限制无关，是 provider-specific timeout 遗漏。
- 结论：这是 Grok OAuth 请求时限的确定性偏差。当前仅登记，未修改 Go 代码；修复时应让 provider 请求携带与 Node 相同的超时，并覆盖 24 秒、26 秒、59 秒和 61 秒响应回放。

### 2026-09-05：OAuth token `expires_in` 数字字符串未按 Node 规则归一化

- Node 证据：历史 Anthropic、Gemini、Grok OAuth service 的 `finitePositiveInteger` 对 `number` 及可转换为数字的字符串统一执行 `Number(value)`、有限性/正数校验和 `Math.trunc`；例如 JSON `expires_in: "3600"` 会得到 3600，`"3600.9"` 会截断为 3600。Grok 在无法解析时再回退固定 6 小时，Anthropic/Gemini 则保留无 `expiresAt` 的语义。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/protocol.go` 的 `finitePositiveInt` 只接受 `float64`，字符串值直接判无效；Anthropic/Gemini 因此写入 `ExpiresIn=0` 且不写 `expires_at`，Grok 则误走固定默认 TTL，而不是使用上游给出的数字字符串。
- 结果偏差：上游或代理把 `expires_in` 序列化为字符串时，Node 会按真实寿命安排下一次刷新，Go 会把令牌视为无过期时间或错误地延长/缩短为固定默认时长；后续 due 判断、刷新次数、凭据内容和账户可用性均可能不同。这是响应字段归一化遗漏，不是 Go 类型表达差异。
- 结论：这是三家 OAuth token 响应解析的确定性偏差。当前仅登记，未修改 Go 代码；修复时应复用与 Node 等价的数字/数字字符串归一化，覆盖整数、带小数数字字符串、空串、非数字字符串和缺失字段回放。

### 2026-09-05：OpenAI `expires_in` 截断为 0 后 Go 未拒绝

- Node 证据：历史 `backend/src/modules/openai-oauth/openai-oauth.service.ts` 的 `parseOpenAIOAuthExpiresIn` 先将值转为有限正数并 `Math.trunc`，随后明确拒绝 `normalized <= 0`；`expires_in=0.5`、`"0.5"` 会抛出“必须是至少 1 秒”，不会产生 token info。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/protocol.go` 的 `finitePositiveInt` 只检查原始 `float64 > 0`，直接 `int(number)` 并返回 `ok=true`；`requestOpenAIToken` 没有再次检查转换后的整数，因此 `0.5` 变成 `ExpiresIn=0`，仍生成当前时间的 `ExpiresAt` 并写回。
- 结果偏差：同一份小数 TTL 响应在 Node 是解析失败并进入刷新失败路径，Go 却把 access token 当作成功凭据落库且立即到期，随后每次请求/保活都会再次触发刷新，可能造成请求失败、重复轮换和错误状态差异。
- 结论：这是 OpenAI token TTL 下限校验的确定性偏差，不能由通用“正数”判断替代。当前仅登记，未修改 Go 代码；修复时应先按 Node 规则完成数字/字符串转换和截断，再拒绝小于 1 的结果，覆盖 `0`、`0.5`、`1`、`"0.5"` 与缺失值回放。

### 2026-09-05：三家 OAuth 保活把非法 `expires_at` 静默当作到期

- Node 证据：历史 Anthropic、Gemini、Grok 各自的 `shouldRefresh...OAuthCredentials` 先解析凭据中的 `expires_at`；非空值若不是带 `Z` 或数值 offset 的 RFC3339，会抛出明确错误，而不是进入 token endpoint。该解析也用于重新读取源账户和 CAS 竞争恢复，因此坏值会保持在可见的配置错误路径。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/keepalive.go` 的共享 `shouldKeepaliveRefresh` 在 `rfc3339Millis` 返回 `ok=false` 时直接 `return true`，随后 `refreshOne` 会读取 Refresh Token 并发起 Anthropic/Gemini/Grok 刷新。源码注释还明确将该行为描述为“read as due”。
- 结果偏差：同一账户含非法 `expires_at` 时，Node 本轮暴露配置错误并停止刷新，Go 会继续消耗上游请求；若刷新成功，Go 还会用新 token 覆盖坏时间，若失败则按普通上游失败计数/退避。告警、调用次数、凭据最终内容和账户恢复路径都不一致。
- 结论：这是三家 OAuth 保活入口共同的损坏凭据语义偏离，不能以“尝试自愈”替代 Node 契约。当前仅登记，未修改 Go 代码；修复时应让解析错误沿配置失败路径传播，并分别回放有/无 access token、空值、缺失字段、非法 RFC3339 和合法 offset 时间。

### 2026-09-05：Anthropic/Gemini/Grok 保活刷新丢失账户代理

- Node 证据：历史 Anthropic、Gemini、Grok `oauth-dispatch-preparation.ts` 在刷新调用中均传入 `dispatchAccount.proxyUrl`；各自 token service 再把账户代理传给共享 transport，缺省时回退全局 `runtimeConfig.oauthProxyUrl`。代理属于请求边界的一部分，而非仅用于普通上游调用。
- Go 证据：历史 `ListDueKeepaliveAccounts` 虽从账户表读取 `proxy_profile_id`，但 `KeepaliveJob.refreshOne` 调用三家 `Refresh*Token` 时没有代理参数；`TokenHTTPRequest` 没有代理字段，`HTTPTokenExchanger.Do` 使用默认 `http.Client` 直连。源码仅保留 profile ID，未解析 profile、校验可用性或建立 HTTP/SOCKS transport。
- 结果偏差：配置了出口代理的 Anthropic/Gemini/Grok 账户在 Node 从指定代理（或全局 OAuth proxy）访问 token endpoint，Go 接管后从 jobs 主机直连。跨地域出口、IP 白名单、代理故障分类和审计来源都会不同；代理不可用时 Node 会暴露相应配置/transport 错误，Go 可能绕过限制成功或在不同网络位置失败。
- 结论：这是三家 OAuth 保活请求边界的确定性遗漏，独立于 OpenAI 刷新代理问题。当前仅登记，未修改 Go 代码；修复时应把代理解析/可用性校验和 transport 注入作为同一端口，覆盖 HTTP/HTTPS/SOCKS、profile 缺失/禁用、全局 fallback 与禁止直连的回放。

### 2026-09-05：Gemini OAuth 刷新省略 project/tier/Drive 富化链

- Node 证据：历史 `backend/src/modules/gemini-oauth/gemini-oauth.service.ts` 的 `refreshGeminiAuthToken` 在 token exchange 后调用 `enrichGeminiTokenInfo`。对 `code_assist`/`google_one`，当缺少 `project_id`（或 Code Assist 缺少 tier）时，会通过 `loadCodeAssist`、`onboardUser` 或 Resource Manager 探测项目；Code Assist 最终探测不到 project 会抛错，Google One 同样对缺失 project 抛错。若 Google One scope 含 Drive metadata，还会读取 Drive storage quota、更新 `drive_storage_limit/usage/drive_tier_updated_at` 并按容量推导 tier。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/providers.go` 的 `RefreshGeminiToken` 只执行一次 token request，`enrichGeminiTokenInfo` 仅对已有/默认 tier 做静态 canonicalize；没有 `loadCodeAssist`、`onboardUser`、Resource Manager、Drive quota 或任何网络富化调用。`BuildGeminiOAuthCredentials` 只从 token/fallback 合并已有 `project_id` 和 quota/ tier 字段。
- 结果偏差：缺少 `project_id` 的 Code Assist/Google One 账户在 Node 刷新阶段失败并保留配置错误，Go 却可能成功写回 access token 但仍缺 project，后续 Gemini 请求无法按 Node 的项目语义运行；Google One 已授权 Drive scope 时 Node 会刷新额度和档位投影，Go 保留旧值或完全缺失，导致账户详情、计费/路由能力和后续刷新结果不一致。
- 结论：这是 Gemini OAuth 刷新后的必要富化链确定性缺失，不是静态字段映射差异。当前仅登记，未修改 Go 代码；修复时应把项目探测、缺失 project 的失败边界、Drive quota 容错和代理/取消传递作为独立可 Mock 端口，覆盖 code_assist/google_one 的有无 project、onboard 未完成、Resource Manager fallback、Drive 403 与成功容量回放。

### 2026-09-05：三家 OAuth 保活写回后未清理网关运行态

- Node 证据：历史 Anthropic/Gemini/Grok dispatch-preparation 的默认 `persistCredentials` 都调用 `updateAccountAsync({ credentials }, expectedConfigRevision)`；该写路径在提交成功后执行账户 lookup 与 gateway runtime 失效（SQLite/PG 均有对应分支），保证下一次 dispatch 读取新凭据。
- Go 证据：历史 `backend-go/projects/jobs/internal/oauthrefresh/keepalive.go` 的 `persistCredentials` 直接调用 `Store.UpdateAccountCredentials`，成功后只返回更新行；`KeepaliveJob`、`Store`、`TokenExchanger` 没有 cache invalidator/event bus 依赖，`oauthrefresh` 目录也没有任何 runtime/lookup 失效调用。
- 结果偏差：三家保活成功后，Go gateway 实例仍可能从旧账户 lookup/runtime cache 取得旧 access token、过期时间或身份字段，直到自然 TTL 或其他写入触发失效；Node 在 CAS 提交后立即清理。若 token 已轮换或旧 token 被撤销，请求失败与再次刷新时序都会不同。
- 结论：这是三家 OAuth 保活成功副作用的确定性遗漏，独立于已登记的 OpenAI 刷新失效缺口。当前仅登记，未修改 Go 代码；修复时应接入跨进程可观察的 invalidation 端口，并覆盖 Anthropic/Gemini/Grok 正常写回、CAS 竞争恢复与重复刷新短路。

### 2026-09-05：OpenAI OAuth 单账户刷新未实现失败状态恢复选项

- Node 基线：历史 `refreshOpenAIOAuthAccountAccessToken` 的单账户路径在同步持久化成功后统一进入 `finalizeSuccessfulTokenRefresh`。当 `restoreFailureState` 未显式设为 `false`（默认行为）且账户当前状态为 `error`、`last_error_code` 属于 OAuth 刷新管理错误码时，会调用 `clearAccountFailureState`，把账户恢复为 `active` 并清除该刷新失败状态；只有批处理路径才显式传入 `restoreFailureState: false`，随后由批处理自己清理失败记录并执行恢复。这一分工也覆盖竞争恢复后复用最新凭据的成功分支。
- Go 实现：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 的 `RefreshAccountOptions` 虽定义了 `RestoreFailureState bool`，但 `RefreshAccount` 仅调用 `refreshWithRaceRetry` 并直接返回；`refreshWithRaceRetry` 的成功写回分支也没有调用 `ClearAccountFailureState`。只有 `RunOnce -> processCandidate` 在批处理成功后另行调用 `restoreIfRecovered`，不能替代单账户 API 的默认语义；同时 Go 的 `bool` 还无法区分“未提供（Node 默认 true）”与“显式 false”。
- 结果偏差：对一个已经处于 `error/oauth_token_refresh_failed` 或 `error/oauth_token_refresh_local_configuration_invalid` 的账户直接调用 Go `RefreshAccount`，即使 token 已成功刷新，返回对象和数据库状态仍保持 `error`，账户继续不可调度；同样调用 Node 单账户刷新则会自动清理受管失败状态并恢复 `active`（除非调用方明确关闭恢复）。这会使手动刷新、请求前阻塞刷新或其他直接复用该库 API 的调用结果不同，且版本/运行态恢复链无法由批处理补偿。
- 结论：这是已提交 M08 OpenAI OAuth 单账户刷新完成后状态机副作用的确定缺口，独立于 token transport、失败退避和批处理计数；当前仅登记，未修改 Go 代码。

### 2026-09-05：OpenAI OAuth 单账户刷新缺少供应商/协议/账户类型入口门禁

- Node 基线：历史 `refreshOpenAIOAuthAccountAccessToken` 在获取锁和读取数据库前调用 `isOpenAIOAuthRefreshAccount`，要求对象存在、供应商为 GPT、协议档案为 OpenAI 且账户类型为 `oauth`；不满足时立即抛出“仅支持刷新 OpenAI OAuth 账户”，不会读取其他类型凭据、访问 OpenAI token endpoint 或触发竞争恢复/失败记录（`backend/src/modules/openai-oauth/openai-oauth-access-token-refresh.service.ts` 行 192-200、634-639）。数据库重读后的 `findLatestRefreshableOpenAIOAuthAccount` 还会再次执行同一检查，避免调用方传入的旧对象绕过边界。
- Go 实现：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 的 `RefreshAccount` 直接对 `account.ID` 加锁并进入 `refreshWithRaceRetry`（行 544-554），该函数重读账户后只检查是否存在、是否有 `refresh_token` 和过期窗口，没有先验证 `ProviderCode == gpt`、`Type == oauth` 或 OpenAI 协议档案。它会用当前账户的刷新令牌调用 `RefreshOpenAIToken`，只有成功后 `RotateCredentials` 才通过固定的 `ExpectedProviderCode/ExpectedAccountType` 让写回失败；非 OpenAI 账户还可能在失败恢复分支再次被读取。`account == nil` 时 Go 甚至在 `account.ID` 处直接 panic，而 Node 返回统一业务错误。
- 结果偏差：错误调用（例如把 Anthropic/Gemini `RotationAccount` 传给该入口）在 Node 是无副作用的立即拒绝，Go 会先向 OpenAI 上游发送不应发送的刷新请求，可能产生上游审计/限流/令牌轮换副作用，再返回与 Node 不同的错误；若传入已删除、授权实例或不完整对象，Go 的错误时序和异常类型也不同。该入口门禁与 `RotateCredentials` 的写回 CAS 不是一回事，不能依赖“最终写不进去”替代前置校验。
- 结论：这是已提交 M08 单账户刷新入口的确定性边界缺口，独立于失败状态恢复、token transport 和并发锁问题；当前仅登记，未修改 Go 代码。修复时应在加锁/读库前复用与 Node 等价的 provider、OpenAI profile、`oauth` 类型和非空对象校验，并为错误输入补 Mock 回归，确认不会触发 token endpoint 或失败状态写入。

### 2026-09-05：OAuth 刷新批处理在取消后的返回契约偏离 Node

- Node 基线：历史 `refreshDueOpenAIOAuthAccessTokens` 在完成 due 候选选择后由 worker 检查 `options.signal?.aborted`；收到取消或停机信号时，worker 直接停止领取新候选，已启动的候选按各自请求结果收口，函数最终返回当前累计的 `OpenAIOAuthAccessTokenRefreshResult`，不会因为“尚未开始的候选”抛出取消异常（`backend/src/modules/openai-oauth/openai-oauth-access-token-refresh.service.ts` 行 488-500、512-516）。仅在进入函数前 signal 已取消时返回全零结果。
- Go 实现：历史 `backend-go/projects/jobs/internal/oauthrefresh/refresh.go` 的 worker 在领取候选后再次检查 `ctx.Err()`；如果此时上下文被取消，它不是像 Node 一样结束当前 worker，而是把 `ctx.Err()` 写入共享 `firstErr`（行 285-301），`RunOnce` 等待所有 worker 后直接 `return result, err`（行 304-311）。因此批次已选出但尚未启动的候选会把正常取消提升为 `context.Canceled` 错误。
- 结果偏差：相同的任务取消/进程停机时序下，Node 调度器收到部分结果并按取消结束，Go jobs runner 收到错误返回，可能触发失败告警、重试或错误计数；未启动候选的 failure/backoff 也不会与 Node 的 `deferredBudget`/静默停止语义一致。该差异不依赖 token endpoint 是否支持取消，属于批处理外层收口契约。
- 结论：这是已提交 M08 OAuth 刷新批处理取消语义的确定性偏差，独立于 token transport 丢失父 context 的问题；当前仅登记，未修改 Go 代码。修复时应让批次内取消沿 Node 结果路径收口，不把尚未开始候选的 `context.Canceled` 作为任务失败返回，并补回放“选批后取消、已有请求中取消、入口前已取消”三种时序。
