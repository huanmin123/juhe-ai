// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: w2_management_group_account_options.sql

package postgresqueries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listManagementGroupAccountOptionIDs = `-- name: ListManagementGroupAccountOptionIDs :many
SELECT
  group_accounts.group_id,
  group_accounts.account_id
FROM juhe_business.group_accounts AS group_accounts
INNER JOIN juhe_business.accounts AS accounts
  ON accounts.id = group_accounts.account_id
  AND accounts.system_account_id = group_accounts.system_account_id
WHERE group_accounts.group_id = ANY($1::text[])
  AND (
    $2::text = ''
    OR group_accounts.system_account_id = $2::text
  )
  AND group_accounts.enabled = true
  AND group_accounts.account_authorization_id IS NULL
  AND accounts.deleted_at IS NULL
ORDER BY group_accounts.group_id ASC, group_accounts.created_at ASC, group_accounts.account_id ASC
`

type ListManagementGroupAccountOptionIDsParams struct {
	GroupIds        []string
	SystemAccountID string
}

type ListManagementGroupAccountOptionIDsRow struct {
	GroupID   string
	AccountID string
}

func (q *Queries) ListManagementGroupAccountOptionIDs(ctx context.Context, arg ListManagementGroupAccountOptionIDsParams) ([]ListManagementGroupAccountOptionIDsRow, error) {
	rows, err := q.db.Query(ctx, listManagementGroupAccountOptionIDs, arg.GroupIds, arg.SystemAccountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementGroupAccountOptionIDsRow
	for rows.Next() {
		var i ListManagementGroupAccountOptionIDsRow
		if err := rows.Scan(&i.GroupID, &i.AccountID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementGroupAccountOptions = `-- name: ListManagementGroupAccountOptions :many
SELECT
  groups.id,
  groups.system_account_id,
  system_accounts.display_name AS system_account_name,
  groups.name,
  groups.provider_code,
  groups.enabled,
  groups.is_default,
  groups.group_type,
  groups.scheduling_policy_json
FROM juhe_business.groups AS groups
LEFT JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = groups.system_account_id
WHERE ($1::text = '' OR groups.system_account_id = $1::text)
  AND (
    coalesce(array_length($2::text[], 1), 0) = 0
    OR groups.id = ANY($2::text[])
  )
  AND (
    $3::text = ''
    OR groups.provider_code = $3::text
  )
  AND (
    $4::boolean = false
    OR (
      (
        groups.name COLLATE "C" >= $5::text
        AND groups.name COLLATE "C" < $6::text
      )
      OR (
        groups.provider_code COLLATE "C" >= $5::text
        AND groups.provider_code COLLATE "C" < $6::text
      )
    )
  )
ORDER BY
  CASE WHEN $7::boolean THEN groups.is_default ELSE false END DESC,
  groups.updated_at DESC,
  groups.id DESC
LIMIT $8::int
`

type ListManagementGroupAccountOptionsParams struct {
	SystemAccountID string
	Ids             []string
	ProviderCode    string
	HasKeyword      bool
	Keyword         string
	KeywordUpper    string
	PreferDefault   bool
	RowLimit        int32
}

type ListManagementGroupAccountOptionsRow struct {
	ID                   string
	SystemAccountID      string
	SystemAccountName    pgtype.Text
	Name                 string
	ProviderCode         string
	Enabled              bool
	IsDefault            bool
	GroupType            string
	SchedulingPolicyJson pgtype.Text
}

func (q *Queries) ListManagementGroupAccountOptions(ctx context.Context, arg ListManagementGroupAccountOptionsParams) ([]ListManagementGroupAccountOptionsRow, error) {
	rows, err := q.db.Query(ctx, listManagementGroupAccountOptions,
		arg.SystemAccountID,
		arg.Ids,
		arg.ProviderCode,
		arg.HasKeyword,
		arg.Keyword,
		arg.KeywordUpper,
		arg.PreferDefault,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementGroupAccountOptionsRow
	for rows.Next() {
		var i ListManagementGroupAccountOptionsRow
		if err := rows.Scan(
			&i.ID,
			&i.SystemAccountID,
			&i.SystemAccountName,
			&i.Name,
			&i.ProviderCode,
			&i.Enabled,
			&i.IsDefault,
			&i.GroupType,
			&i.SchedulingPolicyJson,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
