// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: w8_cooldown_account_retest.sql

package postgresqueries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const findCooldownAccountRetestCandidate = `-- name: findCooldownAccountRetestCandidate :one
SELECT a.id, a.name, a.config_revision, a.cooldown_until, a.priority, a.created_at,
  a.cooldown_retest_observation_started_at, a.system_account_id, ga.group_id,
  a.health_check_model, a.health_check_endpoint_mode
FROM juhe_business.accounts AS a
JOIN LATERAL (
  SELECT group_id FROM juhe_business.group_accounts
  WHERE account_id = a.id AND system_account_id = a.system_account_id AND enabled = true
    AND (a.authorization_instance_authorization_id IS NULL OR account_authorization_id = a.authorization_instance_authorization_id)
  ORDER BY updated_at DESC, group_id ASC LIMIT 1
) AS ga ON true
LEFT JOIN juhe_business.resource_authorizations AS ra ON ra.id = a.authorization_instance_authorization_id
WHERE a.id = $1 AND a.deleted_at IS NULL
  AND a.type IN ('api_key', 'oauth', 'google_oauth')
  AND a.health_check_endpoint_mode IN ('chat_json', 'chat_sse', 'responses_json', 'responses_sse', 'messages_json', 'messages_sse', 'generate_content_json', 'generate_content_sse', 'interactions_json', 'interactions_sse')
  AND a.status IN ('temporary_unavailable', 'rate_limited')
  AND a.schedulable = true AND a.cooldown_until IS NOT NULL AND a.cooldown_until <= $2
  AND (a.account_expires_at IS NULL OR a.account_expires_at > $2)
  AND (a.authorization_instance_authorization_id IS NULL OR (ra.status = 'active' AND (ra.expires_at IS NULL OR ra.expires_at > $2)))
`

type findCooldownAccountRetestCandidateParams struct {
	ID            string
	CooldownUntil pgtype.Timestamptz
}

type findCooldownAccountRetestCandidateRow struct {
	ID                                 string
	Name                               string
	ConfigRevision                     int32
	CooldownUntil                      pgtype.Timestamptz
	Priority                           int32
	CreatedAt                          pgtype.Timestamptz
	CooldownRetestObservationStartedAt pgtype.Timestamptz
	SystemAccountID                    string
	GroupID                            string
	HealthCheckModel                   string
	HealthCheckEndpointMode            string
}

func (q *Queries) findCooldownAccountRetestCandidate(ctx context.Context, arg findCooldownAccountRetestCandidateParams) (findCooldownAccountRetestCandidateRow, error) {
	row := q.db.QueryRow(ctx, findCooldownAccountRetestCandidate, arg.ID, arg.CooldownUntil)
	var i findCooldownAccountRetestCandidateRow
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.ConfigRevision,
		&i.CooldownUntil,
		&i.Priority,
		&i.CreatedAt,
		&i.CooldownRetestObservationStartedAt,
		&i.SystemAccountID,
		&i.GroupID,
		&i.HealthCheckModel,
		&i.HealthCheckEndpointMode,
	)
	return i, err
}

const listCooldownAccountRetestCandidates = `-- name: listCooldownAccountRetestCandidates :many
SELECT
  a.id, a.name, a.config_revision, a.cooldown_until, a.priority, a.created_at,
  a.cooldown_retest_observation_started_at, a.system_account_id,
  ga.group_id, a.health_check_model, a.health_check_endpoint_mode
FROM juhe_business.accounts AS a
JOIN LATERAL (
  SELECT group_id
  FROM juhe_business.group_accounts
  WHERE account_id = a.id AND system_account_id = a.system_account_id AND enabled = true
    AND (a.authorization_instance_authorization_id IS NULL OR account_authorization_id = a.authorization_instance_authorization_id)
  ORDER BY updated_at DESC, group_id ASC
  LIMIT 1
) AS ga ON true
LEFT JOIN juhe_business.resource_authorizations AS ra
  ON ra.id = a.authorization_instance_authorization_id
WHERE a.deleted_at IS NULL
  AND a.type IN ('api_key', 'oauth', 'google_oauth')
  AND a.health_check_endpoint_mode IN ('chat_json', 'chat_sse', 'responses_json', 'responses_sse', 'messages_json', 'messages_sse', 'generate_content_json', 'generate_content_sse', 'interactions_json', 'interactions_sse')
  AND a.status IN ('temporary_unavailable', 'rate_limited')
  AND a.schedulable = true
  AND a.cooldown_until IS NOT NULL AND a.cooldown_until <= $1
  AND (a.account_expires_at IS NULL OR a.account_expires_at > $1)
  AND (a.authorization_instance_authorization_id IS NULL OR (ra.status = 'active' AND (ra.expires_at IS NULL OR ra.expires_at > $1)))
  AND ($2::timestamptz IS NULL OR (a.cooldown_until, a.priority, a.created_at, a.id) > ($2, $3, $4, $5))
ORDER BY a.cooldown_until, a.priority, a.created_at, a.id
LIMIT $6
`

type listCooldownAccountRetestCandidatesParams struct {
	CooldownUntil   pgtype.Timestamptz
	Column2         pgtype.Timestamptz
	CooldownUntil_2 pgtype.Timestamptz
	CooldownUntil_3 pgtype.Timestamptz
	CooldownUntil_4 pgtype.Timestamptz
	Limit           int32
}

type listCooldownAccountRetestCandidatesRow struct {
	ID                                 string
	Name                               string
	ConfigRevision                     int32
	CooldownUntil                      pgtype.Timestamptz
	Priority                           int32
	CreatedAt                          pgtype.Timestamptz
	CooldownRetestObservationStartedAt pgtype.Timestamptz
	SystemAccountID                    string
	GroupID                            string
	HealthCheckModel                   string
	HealthCheckEndpointMode            string
}

// First-pass scheduler shell. sqlc generation is intentionally deferred.
func (q *Queries) listCooldownAccountRetestCandidates(ctx context.Context, arg listCooldownAccountRetestCandidatesParams) ([]listCooldownAccountRetestCandidatesRow, error) {
	rows, err := q.db.Query(ctx, listCooldownAccountRetestCandidates,
		arg.CooldownUntil,
		arg.Column2,
		arg.CooldownUntil_2,
		arg.CooldownUntil_3,
		arg.CooldownUntil_4,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []listCooldownAccountRetestCandidatesRow
	for rows.Next() {
		var i listCooldownAccountRetestCandidatesRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.ConfigRevision,
			&i.CooldownUntil,
			&i.Priority,
			&i.CreatedAt,
			&i.CooldownRetestObservationStartedAt,
			&i.SystemAccountID,
			&i.GroupID,
			&i.HealthCheckModel,
			&i.HealthCheckEndpointMode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
