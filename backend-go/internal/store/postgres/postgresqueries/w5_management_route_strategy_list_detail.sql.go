// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: w5_management_route_strategy_list_detail.sql

package postgresqueries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const findManagementRouteStrategyDetail = `-- name: FindManagementRouteStrategyDetail :one
SELECT
  route_strategies.id,
  route_strategies.system_account_id,
  system_accounts.display_name AS system_account_name,
  route_strategies.name,
  route_strategies.description,
  route_strategies.mode,
  route_strategies.status,
  route_strategies.is_default,
  route_strategies.config_json,
  (
    SELECT count(*)::bigint
    FROM juhe_business.api_keys
    WHERE api_keys.route_strategy_id = route_strategies.id
      AND api_keys.system_account_id = route_strategies.system_account_id
  ) AS api_key_count,
  route_strategies.created_at,
  route_strategies.updated_at
FROM juhe_business.route_strategies AS route_strategies
INNER JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = route_strategies.system_account_id
WHERE route_strategies.id = $1::text
  AND (
    $2::text = ''
    OR route_strategies.system_account_id = $2::text
  )
`

type FindManagementRouteStrategyDetailParams struct {
	RouteStrategyID string
	SystemAccountID string
}

type FindManagementRouteStrategyDetailRow struct {
	ID                string
	SystemAccountID   string
	SystemAccountName string
	Name              string
	Description       pgtype.Text
	Mode              string
	Status            string
	IsDefault         bool
	ConfigJson        pgtype.Text
	ApiKeyCount       int64
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) FindManagementRouteStrategyDetail(ctx context.Context, arg FindManagementRouteStrategyDetailParams) (FindManagementRouteStrategyDetailRow, error) {
	row := q.db.QueryRow(ctx, findManagementRouteStrategyDetail, arg.RouteStrategyID, arg.SystemAccountID)
	var i FindManagementRouteStrategyDetailRow
	err := row.Scan(
		&i.ID,
		&i.SystemAccountID,
		&i.SystemAccountName,
		&i.Name,
		&i.Description,
		&i.Mode,
		&i.Status,
		&i.IsDefault,
		&i.ConfigJson,
		&i.ApiKeyCount,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listManagementOwnedRouteStrategies = `-- name: ListManagementOwnedRouteStrategies :many
SELECT
  route_strategies.id,
  route_strategies.system_account_id,
  system_accounts.display_name AS system_account_name,
  route_strategies.name,
  route_strategies.description,
  route_strategies.mode,
  route_strategies.status,
  route_strategies.is_default,
  route_strategies.config_json,
  route_strategies.created_at,
  route_strategies.updated_at
FROM juhe_business.route_strategies AS route_strategies
INNER JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = route_strategies.system_account_id
WHERE route_strategies.system_account_id = $1::text
  AND (
    $2::text = ''
    OR route_strategies.mode = $2::text
  )
  AND (
    $3::text = ''
    OR route_strategies.status = $3::text
  )
ORDER BY
  route_strategies.updated_at DESC,
  route_strategies.created_at DESC,
  route_strategies.id DESC
LIMIT $5::bigint
OFFSET $4::bigint
`

type ListManagementOwnedRouteStrategiesParams struct {
	SystemAccountID string
	Mode            string
	Status          string
	RowOffset       int64
	RowLimit        int64
}

type ListManagementOwnedRouteStrategiesRow struct {
	ID                string
	SystemAccountID   string
	SystemAccountName string
	Name              string
	Description       pgtype.Text
	Mode              string
	Status            string
	IsDefault         bool
	ConfigJson        pgtype.Text
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) ListManagementOwnedRouteStrategies(ctx context.Context, arg ListManagementOwnedRouteStrategiesParams) ([]ListManagementOwnedRouteStrategiesRow, error) {
	rows, err := q.db.Query(ctx, listManagementOwnedRouteStrategies,
		arg.SystemAccountID,
		arg.Mode,
		arg.Status,
		arg.RowOffset,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementOwnedRouteStrategiesRow
	for rows.Next() {
		var i ListManagementOwnedRouteStrategiesRow
		if err := rows.Scan(
			&i.ID,
			&i.SystemAccountID,
			&i.SystemAccountName,
			&i.Name,
			&i.Description,
			&i.Mode,
			&i.Status,
			&i.IsDefault,
			&i.ConfigJson,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementOwnedRouteStrategiesByKeyword = `-- name: ListManagementOwnedRouteStrategiesByKeyword :many
WITH matched_route_strategy_scopes AS MATERIALIZED (
  SELECT
    route_strategies.id AS route_strategy_id,
    route_strategies.system_account_id
  FROM juhe_business.route_strategies AS route_strategies
  WHERE route_strategies.system_account_id = $5::text
    AND route_strategies.name COLLATE "C" >= $6::text
    AND route_strategies.name COLLATE "C" < $7::text
    AND starts_with(route_strategies.name, $6::text)
)
SELECT
  route_strategies.id,
  route_strategies.system_account_id,
  system_accounts.display_name AS system_account_name,
  route_strategies.name,
  route_strategies.description,
  route_strategies.mode,
  route_strategies.status,
  route_strategies.is_default,
  route_strategies.config_json,
  route_strategies.created_at,
  route_strategies.updated_at
FROM matched_route_strategy_scopes
INNER JOIN juhe_business.route_strategies AS route_strategies
  ON route_strategies.id = matched_route_strategy_scopes.route_strategy_id
  AND route_strategies.system_account_id = matched_route_strategy_scopes.system_account_id
INNER JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = route_strategies.system_account_id
WHERE (
    $1::text = ''
    OR route_strategies.mode = $1::text
  )
  AND (
    $2::text = ''
    OR route_strategies.status = $2::text
  )
ORDER BY
  route_strategies.updated_at DESC,
  route_strategies.created_at DESC,
  route_strategies.id DESC
LIMIT $4::bigint
OFFSET $3::bigint
`

type ListManagementOwnedRouteStrategiesByKeywordParams struct {
	Mode            string
	Status          string
	RowOffset       int64
	RowLimit        int64
	SystemAccountID string
	Keyword         string
	KeywordUpper    string
}

type ListManagementOwnedRouteStrategiesByKeywordRow struct {
	ID                string
	SystemAccountID   string
	SystemAccountName string
	Name              string
	Description       pgtype.Text
	Mode              string
	Status            string
	IsDefault         bool
	ConfigJson        pgtype.Text
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) ListManagementOwnedRouteStrategiesByKeyword(ctx context.Context, arg ListManagementOwnedRouteStrategiesByKeywordParams) ([]ListManagementOwnedRouteStrategiesByKeywordRow, error) {
	rows, err := q.db.Query(ctx, listManagementOwnedRouteStrategiesByKeyword,
		arg.Mode,
		arg.Status,
		arg.RowOffset,
		arg.RowLimit,
		arg.SystemAccountID,
		arg.Keyword,
		arg.KeywordUpper,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementOwnedRouteStrategiesByKeywordRow
	for rows.Next() {
		var i ListManagementOwnedRouteStrategiesByKeywordRow
		if err := rows.Scan(
			&i.ID,
			&i.SystemAccountID,
			&i.SystemAccountName,
			&i.Name,
			&i.Description,
			&i.Mode,
			&i.Status,
			&i.IsDefault,
			&i.ConfigJson,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementRouteStrategies = `-- name: ListManagementRouteStrategies :many
SELECT
  route_strategies.id,
  route_strategies.system_account_id,
  system_accounts.display_name AS system_account_name,
  route_strategies.name,
  route_strategies.description,
  route_strategies.mode,
  route_strategies.status,
  route_strategies.is_default,
  route_strategies.config_json,
  route_strategies.created_at,
  route_strategies.updated_at
FROM juhe_business.route_strategies AS route_strategies
INNER JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = route_strategies.system_account_id
WHERE (
    $1::text = ''
    OR route_strategies.mode = $1::text
  )
  AND (
    $2::text = ''
    OR route_strategies.status = $2::text
  )
ORDER BY
  route_strategies.updated_at DESC,
  route_strategies.created_at DESC,
  route_strategies.id DESC
LIMIT $4::bigint
OFFSET $3::bigint
`

type ListManagementRouteStrategiesParams struct {
	Mode      string
	Status    string
	RowOffset int64
	RowLimit  int64
}

type ListManagementRouteStrategiesRow struct {
	ID                string
	SystemAccountID   string
	SystemAccountName string
	Name              string
	Description       pgtype.Text
	Mode              string
	Status            string
	IsDefault         bool
	ConfigJson        pgtype.Text
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) ListManagementRouteStrategies(ctx context.Context, arg ListManagementRouteStrategiesParams) ([]ListManagementRouteStrategiesRow, error) {
	rows, err := q.db.Query(ctx, listManagementRouteStrategies,
		arg.Mode,
		arg.Status,
		arg.RowOffset,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementRouteStrategiesRow
	for rows.Next() {
		var i ListManagementRouteStrategiesRow
		if err := rows.Scan(
			&i.ID,
			&i.SystemAccountID,
			&i.SystemAccountName,
			&i.Name,
			&i.Description,
			&i.Mode,
			&i.Status,
			&i.IsDefault,
			&i.ConfigJson,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementRouteStrategiesByKeyword = `-- name: ListManagementRouteStrategiesByKeyword :many
WITH matched_route_strategy_scopes AS MATERIALIZED (
  SELECT
    route_strategies.id AS route_strategy_id,
    route_strategies.system_account_id
  FROM juhe_business.route_strategies AS route_strategies
  WHERE route_strategies.name COLLATE "C" >= $5::text
    AND route_strategies.name COLLATE "C" < $6::text
    AND starts_with(route_strategies.name, $5::text)
)
SELECT
  route_strategies.id,
  route_strategies.system_account_id,
  system_accounts.display_name AS system_account_name,
  route_strategies.name,
  route_strategies.description,
  route_strategies.mode,
  route_strategies.status,
  route_strategies.is_default,
  route_strategies.config_json,
  route_strategies.created_at,
  route_strategies.updated_at
FROM matched_route_strategy_scopes
INNER JOIN juhe_business.route_strategies AS route_strategies
  ON route_strategies.id = matched_route_strategy_scopes.route_strategy_id
  AND route_strategies.system_account_id = matched_route_strategy_scopes.system_account_id
INNER JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = route_strategies.system_account_id
WHERE (
    $1::text = ''
    OR route_strategies.mode = $1::text
  )
  AND (
    $2::text = ''
    OR route_strategies.status = $2::text
  )
ORDER BY
  route_strategies.updated_at DESC,
  route_strategies.created_at DESC,
  route_strategies.id DESC
LIMIT $4::bigint
OFFSET $3::bigint
`

type ListManagementRouteStrategiesByKeywordParams struct {
	Mode         string
	Status       string
	RowOffset    int64
	RowLimit     int64
	Keyword      string
	KeywordUpper string
}

type ListManagementRouteStrategiesByKeywordRow struct {
	ID                string
	SystemAccountID   string
	SystemAccountName string
	Name              string
	Description       pgtype.Text
	Mode              string
	Status            string
	IsDefault         bool
	ConfigJson        pgtype.Text
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) ListManagementRouteStrategiesByKeyword(ctx context.Context, arg ListManagementRouteStrategiesByKeywordParams) ([]ListManagementRouteStrategiesByKeywordRow, error) {
	rows, err := q.db.Query(ctx, listManagementRouteStrategiesByKeyword,
		arg.Mode,
		arg.Status,
		arg.RowOffset,
		arg.RowLimit,
		arg.Keyword,
		arg.KeywordUpper,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementRouteStrategiesByKeywordRow
	for rows.Next() {
		var i ListManagementRouteStrategiesByKeywordRow
		if err := rows.Scan(
			&i.ID,
			&i.SystemAccountID,
			&i.SystemAccountName,
			&i.Name,
			&i.Description,
			&i.Mode,
			&i.Status,
			&i.IsDefault,
			&i.ConfigJson,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementRouteStrategyDetailBindings = `-- name: ListManagementRouteStrategyDetailBindings :many
WITH visible_route AS MATERIALIZED (
  SELECT
    route_strategies.id,
    route_strategies.system_account_id
  FROM juhe_business.route_strategies AS route_strategies
  WHERE route_strategies.id = $1::text
    AND (
      $2::text = ''
      OR route_strategies.system_account_id = $2::text
    )
)
SELECT
  route_strategy_groups.id,
  route_strategy_groups.group_id,
  groups.name AS group_name,
  groups.provider_code,
  route_strategy_groups.priority,
  route_strategy_groups.weight,
  route_strategy_groups.status,
  CASE
    WHEN groups.id IS NULL THEN false
    WHEN groups.system_account_id = visible_route.system_account_id THEN groups.enabled
    WHEN group_authorization.id IS NOT NULL THEN
      CASE
        WHEN groups.enabled THEN coalesce(group_authorization_settings.enabled, true)
        ELSE false
      END
    ELSE false
  END AS group_enabled
FROM visible_route
INNER JOIN juhe_business.route_strategy_groups AS route_strategy_groups
  ON route_strategy_groups.route_strategy_id = visible_route.id
  AND route_strategy_groups.system_account_id = visible_route.system_account_id
LEFT JOIN juhe_business.groups AS groups
  ON groups.id = route_strategy_groups.group_id
LEFT JOIN juhe_business.resource_authorizations AS group_authorization
  ON group_authorization.resource_type = 'group'
  AND group_authorization.resource_id = groups.id
  AND group_authorization.resource_owner_system_account_id = groups.system_account_id
  AND group_authorization.grantee_system_account_id = visible_route.system_account_id
  AND group_authorization.status = 'active'
  AND (
    group_authorization.expires_at IS NULL
    OR group_authorization.expires_at > CURRENT_TIMESTAMP
  )
LEFT JOIN juhe_business.group_authorization_settings AS group_authorization_settings
  ON group_authorization_settings.authorization_id = group_authorization.id
  AND group_authorization_settings.system_account_id = visible_route.system_account_id
  AND group_authorization_settings.group_id = groups.id
ORDER BY
  CASE WHEN route_strategy_groups.status = 'active' THEN 0 ELSE 1 END ASC,
  route_strategy_groups.priority ASC,
  route_strategy_groups.created_at ASC,
  route_strategy_groups.id ASC
LIMIT 21
`

type ListManagementRouteStrategyDetailBindingsParams struct {
	RouteStrategyID string
	SystemAccountID string
}

type ListManagementRouteStrategyDetailBindingsRow struct {
	ID           string
	GroupID      string
	GroupName    pgtype.Text
	ProviderCode pgtype.Text
	Priority     int32
	Weight       int32
	Status       string
	GroupEnabled bool
}

func (q *Queries) ListManagementRouteStrategyDetailBindings(ctx context.Context, arg ListManagementRouteStrategyDetailBindingsParams) ([]ListManagementRouteStrategyDetailBindingsRow, error) {
	rows, err := q.db.Query(ctx, listManagementRouteStrategyDetailBindings, arg.RouteStrategyID, arg.SystemAccountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementRouteStrategyDetailBindingsRow
	for rows.Next() {
		var i ListManagementRouteStrategyDetailBindingsRow
		if err := rows.Scan(
			&i.ID,
			&i.GroupID,
			&i.GroupName,
			&i.ProviderCode,
			&i.Priority,
			&i.Weight,
			&i.Status,
			&i.GroupEnabled,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementRouteStrategyListEnrichment = `-- name: ListManagementRouteStrategyListEnrichment :many
WITH requested_scopes AS MATERIALIZED (
  SELECT
    requested.route_strategy_id::text AS route_strategy_id,
    (($1::text[])[requested.ordinality::int])::text AS system_account_id,
    requested.ordinality
  FROM unnest($2::text[])
    WITH ORDINALITY AS requested(route_strategy_id, ordinality)
),
binding_counts AS MATERIALIZED (
  SELECT
    requested_scopes.route_strategy_id,
    requested_scopes.system_account_id,
    count(route_strategy_groups.id)::bigint AS binding_count
  FROM requested_scopes
  LEFT JOIN juhe_business.route_strategy_groups AS route_strategy_groups
    ON route_strategy_groups.route_strategy_id = requested_scopes.route_strategy_id
    AND route_strategy_groups.system_account_id = requested_scopes.system_account_id
  GROUP BY requested_scopes.route_strategy_id, requested_scopes.system_account_id
),
api_key_counts AS MATERIALIZED (
  SELECT
    requested_scopes.route_strategy_id,
    requested_scopes.system_account_id,
    count(api_keys.id)::bigint AS api_key_count
  FROM requested_scopes
  LEFT JOIN juhe_business.api_keys AS api_keys
    ON api_keys.route_strategy_id = requested_scopes.route_strategy_id
    AND api_keys.system_account_id = requested_scopes.system_account_id
  GROUP BY requested_scopes.route_strategy_id, requested_scopes.system_account_id
),
ranked_bindings AS MATERIALIZED (
  SELECT
    requested_scopes.route_strategy_id,
    requested_scopes.system_account_id,
    route_strategy_groups.id AS binding_id,
    route_strategy_groups.group_id,
    groups.name AS group_name,
    groups.provider_code,
    route_strategy_groups.priority,
    route_strategy_groups.weight,
    route_strategy_groups.status AS binding_status,
    CASE
      WHEN groups.id IS NULL THEN false
      WHEN groups.system_account_id = requested_scopes.system_account_id THEN groups.enabled
      WHEN group_authorization.id IS NOT NULL THEN
        CASE
          WHEN groups.enabled THEN coalesce(group_authorization_settings.enabled, true)
          ELSE false
        END
      ELSE false
    END AS group_enabled,
    ROW_NUMBER() OVER (
      PARTITION BY requested_scopes.route_strategy_id, requested_scopes.system_account_id
      ORDER BY
        CASE WHEN route_strategy_groups.status = 'active' THEN 0 ELSE 1 END ASC,
        route_strategy_groups.priority ASC,
        route_strategy_groups.created_at ASC,
        route_strategy_groups.id ASC
    ) AS row_number
  FROM requested_scopes
  INNER JOIN juhe_business.route_strategy_groups AS route_strategy_groups
    ON route_strategy_groups.route_strategy_id = requested_scopes.route_strategy_id
    AND route_strategy_groups.system_account_id = requested_scopes.system_account_id
  LEFT JOIN juhe_business.groups AS groups
    ON groups.id = route_strategy_groups.group_id
  LEFT JOIN juhe_business.resource_authorizations AS group_authorization
    ON group_authorization.resource_type = 'group'
    AND group_authorization.resource_id = groups.id
    AND group_authorization.resource_owner_system_account_id = groups.system_account_id
    AND group_authorization.grantee_system_account_id = requested_scopes.system_account_id
    AND group_authorization.status = 'active'
    AND (
      group_authorization.expires_at IS NULL
      OR group_authorization.expires_at > CURRENT_TIMESTAMP
    )
  LEFT JOIN juhe_business.group_authorization_settings AS group_authorization_settings
    ON group_authorization_settings.authorization_id = group_authorization.id
    AND group_authorization_settings.system_account_id = requested_scopes.system_account_id
    AND group_authorization_settings.group_id = groups.id
)
SELECT
  requested_scopes.route_strategy_id,
  requested_scopes.system_account_id,
  binding_counts.binding_count,
  api_key_counts.api_key_count,
  ranked_bindings.binding_id,
  ranked_bindings.group_id,
  ranked_bindings.group_name,
  ranked_bindings.provider_code,
  ranked_bindings.priority,
  ranked_bindings.weight,
  ranked_bindings.binding_status,
  ranked_bindings.group_enabled
FROM requested_scopes
INNER JOIN binding_counts
  ON binding_counts.route_strategy_id = requested_scopes.route_strategy_id
  AND binding_counts.system_account_id = requested_scopes.system_account_id
INNER JOIN api_key_counts
  ON api_key_counts.route_strategy_id = requested_scopes.route_strategy_id
  AND api_key_counts.system_account_id = requested_scopes.system_account_id
LEFT JOIN ranked_bindings
  ON ranked_bindings.route_strategy_id = requested_scopes.route_strategy_id
  AND ranked_bindings.system_account_id = requested_scopes.system_account_id
  AND ranked_bindings.row_number <= 3
ORDER BY
  requested_scopes.ordinality ASC,
  ranked_bindings.row_number ASC
`

type ListManagementRouteStrategyListEnrichmentParams struct {
	SystemAccountIds []string
	RouteStrategyIds []string
}

type ListManagementRouteStrategyListEnrichmentRow struct {
	RouteStrategyID string
	SystemAccountID string
	BindingCount    int64
	ApiKeyCount     int64
	BindingID       pgtype.Text
	GroupID         pgtype.Text
	GroupName       pgtype.Text
	ProviderCode    pgtype.Text
	Priority        pgtype.Int4
	Weight          pgtype.Int4
	BindingStatus   pgtype.Text
	GroupEnabled    pgtype.Bool
}

func (q *Queries) ListManagementRouteStrategyListEnrichment(ctx context.Context, arg ListManagementRouteStrategyListEnrichmentParams) ([]ListManagementRouteStrategyListEnrichmentRow, error) {
	rows, err := q.db.Query(ctx, listManagementRouteStrategyListEnrichment, arg.SystemAccountIds, arg.RouteStrategyIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementRouteStrategyListEnrichmentRow
	for rows.Next() {
		var i ListManagementRouteStrategyListEnrichmentRow
		if err := rows.Scan(
			&i.RouteStrategyID,
			&i.SystemAccountID,
			&i.BindingCount,
			&i.ApiKeyCount,
			&i.BindingID,
			&i.GroupID,
			&i.GroupName,
			&i.ProviderCode,
			&i.Priority,
			&i.Weight,
			&i.BindingStatus,
			&i.GroupEnabled,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
