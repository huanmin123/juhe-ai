// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: w2_management_system_account_options.sql

package postgresqueries

import (
	"context"
)

const listManagementSystemAccountOptions = `-- name: ListManagementSystemAccountOptions :many
SELECT id, username, display_name, status
FROM juhe_business.system_accounts
WHERE (
    $1::boolean = false
    OR id = ANY($2::text[])
  )
  AND (
    $3::boolean = false
    OR (
      username COLLATE "C" >= $4::text
      AND username COLLATE "C" < $5::text
      AND starts_with(username, $4::text)
    )
    OR (
      display_name COLLATE "C" >= $4::text
      AND display_name COLLATE "C" < $5::text
      AND starts_with(display_name, $4::text)
    )
  )
ORDER BY status ASC, display_name ASC, username ASC, id ASC
LIMIT $6::int
`

type ListManagementSystemAccountOptionsParams struct {
	HasIds       bool
	Ids          []string
	HasKeyword   bool
	Keyword      string
	KeywordUpper string
	RowLimit     int32
}

type ListManagementSystemAccountOptionsRow struct {
	ID          string
	Username    string
	DisplayName string
	Status      string
}

func (q *Queries) ListManagementSystemAccountOptions(ctx context.Context, arg ListManagementSystemAccountOptionsParams) ([]ListManagementSystemAccountOptionsRow, error) {
	rows, err := q.db.Query(ctx, listManagementSystemAccountOptions,
		arg.HasIds,
		arg.Ids,
		arg.HasKeyword,
		arg.Keyword,
		arg.KeywordUpper,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementSystemAccountOptionsRow
	for rows.Next() {
		var i ListManagementSystemAccountOptionsRow
		if err := rows.Scan(
			&i.ID,
			&i.Username,
			&i.DisplayName,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
