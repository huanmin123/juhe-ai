// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: w2_management_proxy_options.sql

package postgresqueries

import (
	"context"
)

const listManagementProxyOptions = `-- name: ListManagementProxyOptions :many
SELECT id, name, type, enabled
FROM juhe_business.proxy_profiles
WHERE enabled = true
  AND (
    $1::boolean = false
    OR (
      name COLLATE "C" >= $2::text
      AND name COLLATE "C" < $3::text
      AND starts_with(name, $2::text)
    )
  )
ORDER BY name ASC, updated_at DESC, id ASC
LIMIT $4::int
`

type ListManagementProxyOptionsParams struct {
	HasKeyword   bool
	Keyword      string
	KeywordUpper string
	RowLimit     int32
}

type ListManagementProxyOptionsRow struct {
	ID      string
	Name    string
	Type    string
	Enabled bool
}

func (q *Queries) ListManagementProxyOptions(ctx context.Context, arg ListManagementProxyOptionsParams) ([]ListManagementProxyOptionsRow, error) {
	rows, err := q.db.Query(ctx, listManagementProxyOptions,
		arg.HasKeyword,
		arg.Keyword,
		arg.KeywordUpper,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementProxyOptionsRow
	for rows.Next() {
		var i ListManagementProxyOptionsRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.Enabled,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
