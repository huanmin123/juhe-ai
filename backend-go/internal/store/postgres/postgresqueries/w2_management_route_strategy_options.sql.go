// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: w2_management_route_strategy_options.sql

package postgresqueries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listManagementRouteStrategyOptions = `-- name: ListManagementRouteStrategyOptions :many
SELECT
  route_strategies.id,
  route_strategies.system_account_id,
  system_accounts.display_name AS system_account_name,
  route_strategies.name,
  route_strategies.mode,
  route_strategies.status,
  route_strategies.is_default
FROM juhe_business.route_strategies AS route_strategies
LEFT JOIN juhe_business.system_accounts AS system_accounts
  ON system_accounts.id = route_strategies.system_account_id
WHERE ($1::text = '' OR route_strategies.system_account_id = $1::text)
  AND (
    coalesce(array_length($2::text[], 1), 0) = 0
    OR route_strategies.id = ANY($2::text[])
  )
  AND (
    $3::boolean = false
    OR (
      route_strategies.name COLLATE "C" >= $4::text
      AND route_strategies.name COLLATE "C" < $5::text
      AND starts_with(route_strategies.name, $4::text)
    )
  )
  AND ($6::boolean = false OR route_strategies.status = 'active')
ORDER BY route_strategies.is_default DESC,
  route_strategies.updated_at DESC,
  route_strategies.name ASC,
  route_strategies.id ASC
LIMIT $7::int
`

type ListManagementRouteStrategyOptionsParams struct {
	SystemAccountID string
	Ids             []string
	HasKeyword      bool
	Keyword         string
	KeywordUpper    string
	ActiveOnly      bool
	RowLimit        int32
}

type ListManagementRouteStrategyOptionsRow struct {
	ID                string
	SystemAccountID   string
	SystemAccountName pgtype.Text
	Name              string
	Mode              string
	Status            string
	IsDefault         bool
}

func (q *Queries) ListManagementRouteStrategyOptions(ctx context.Context, arg ListManagementRouteStrategyOptionsParams) ([]ListManagementRouteStrategyOptionsRow, error) {
	rows, err := q.db.Query(ctx, listManagementRouteStrategyOptions,
		arg.SystemAccountID,
		arg.Ids,
		arg.HasKeyword,
		arg.Keyword,
		arg.KeywordUpper,
		arg.ActiveOnly,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListManagementRouteStrategyOptionsRow
	for rows.Next() {
		var i ListManagementRouteStrategyOptionsRow
		if err := rows.Scan(
			&i.ID,
			&i.SystemAccountID,
			&i.SystemAccountName,
			&i.Name,
			&i.Mode,
			&i.Status,
			&i.IsDefault,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
