// PostgreSQL schema contract version accepted by this release. Node-owned
// deployments validate this contract read-only; Goose-owned deployments use
// the same number as their formal migration ledger target.
export const CURRENT_RELEASE_SCHEMA_VERSION = 93
