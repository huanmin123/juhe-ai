# 聚合 AI 前端本地配置
# 复制为 frontend/.env 后修改；配置随项目目录走，不依赖系统环境变量。

# 开发代理目标。只影响 pnpm dev；前端开发服务器会把 /api 和 /v1 转发到这里。
VITE_JUHE_AI_BACKEND_TARGET=http://127.0.0.1:3000

# 前端管理 API 请求地址。前后端同源部署保持 /api；分离部署时改成 http(s)://你的后端/api。
VITE_JUHE_AI_API_BASE_URL=/api

# API Key 页面展示和复制的网关 Base URL。留空时自动按当前访问地址推断；分离部署或公网访问时可填写完整 /v1 地址。
VITE_JUHE_AI_GATEWAY_BASE_URL=
